# 本文件不是转换器产物：T 是 target/CSV/VariableSize.csv:84 声明的单字母
# 全局整数数组（T,100000），没有名字表、纯数字下标（E.yml/DA.yml 同款先例，
# 引擎按「开发者自定义一维表」建 data.t 桶）。
#
# T:0 在原作的语义是「触手调教的经过回合计数」：
#   - 写者：COMF44（触手紧缚开始时清 0，:112-113）、EQUIP_COM44/46/49 的
#     触手支（每回合 +1）、COMF100 自身（:63 清 0、:206-240 累加）；
#   - 消费者：@EQUIP_COM100（COMF100:240-244——PRINTVL T / EXP:55 += T /
#     T = 0，随 J17 触手族落地）。
# 其余下标（T:1 起）在原作全部是各函数的临时暂存（SHOP_LABO/SELL_CHARA 等
# 以 T 当局部变量用），无跨函数语义，不声明。
#
# 空表即够用（CFlag.yml/DA.yml 同款）：未声明下标读侧 getter 兜 0，1:1 于
# 原作「全区 0 起步」。J13（#223）首次写入 t:0。
