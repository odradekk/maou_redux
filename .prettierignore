# 移植源，只读参考资料，禁止格式化
target/

# 引擎运行时，第三方产物
ere-4.8.0-win-x64/

# 引擎手册：经本项目按引擎代码修正过的副本（#163），非上游逐字原文；
# 仍不格式化——重排会淹没按引擎逐字修正的少数几行
dev-guides/

# 外部技能包，上游材料：保持与上游逐字一致才能无痛升级
# （prettier 实测会重写其中 32 个文件）
.agents/skills/emuera-basic-agent-guide/

node_modules/
sav/

# 静态数据产物：键名一律双引号是转换器约定（tools/csv-to-yml.js，issue #17），
# prettier 的 singleQuote 风格会把它改坏，勿格式化
yml/

# 区段所有权产物：与 yml/ 同规格的生成物（tools/ownership-scan.js，issue #66），
# 键名双引号 + 固定排序是扫描器约定，勿格式化
ownership/

# 口上转译器产物（issue #107 原型）：待复核初稿，含 ERB 变量名直译
# （如 if (FLAG:7 == 0) 的 FLAG:7 是 ERB 语法，不是合法 JS），
# 格式由 tools/kojo-transpiler.js 约定，复核后进入 ere/ 才受 prettier 约束
products/
