# git 采用最后一条匹配规则，故顺序即优先级：先定通用，再定例外。

# 默认：由 git 自行判定文本/二进制，不强制改写换行符
* -text

# 我们自己写的源码与文档：仓库与检出文件统一 LF
# （.prettierrc 与 .eslintrc.js 均为 endOfLine: auto，不与此冲突）
*.js text eol=lf
*.mjs text eol=lf
*.sh text eol=lf
*.json text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.csv text eol=lf

# 引擎 SDK 取自上游，保持原样以便与上游比对
ere/era-electron.js -text

