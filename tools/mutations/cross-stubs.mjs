// issue #333：阶段 5a 段 0 的九个跨域前置函数。
/** 本分片条数（门 1）：增删条目必须同步改它，理由见 tools/mutation-check.mjs 头注 */
export const COUNT = 40;

export default [
  {
    desc: 'M6562 SEARCH_FAMILY 村娘 A 标志改错',
    file: 'ere/chara/chara-family.js',
    find: '  if (era.get(`talent:${cid}:165`)) {\n    for (const candidate of added) {',
    replace:
      '  if (era.get(`talent:${cid}:166`)) {\n    for (const candidate of added) {',
    tests: ['cross-stubs'],
    must_mention: '村娘 A/B 走专用互找分支',
  },
  {
    desc: 'M6563 SEARCH_FAMILY 村娘分支由最后匹配退化为首个匹配',
    file: 'ere/chara/chara-family.js',
    find: '      if (era.get(`talent:${candidate}:171`)) found = candidate;',
    replace: '      if (era.get(`talent:${candidate}:171`)) return candidate;',
    tests: ['cross-stubs'],
    must_mention: '保留最后一个匹配',
  },
  {
    desc: 'M6564 SEARCH_FAMILY 魔王婚姻识别码 901 改错',
    file: 'ere/chara/chara-family.js',
    find: '      if (target_relation.data === 901) return candidate;',
    replace: '      if (target_relation.data === 902) return candidate;',
    tests: ['cross-stubs'],
    must_mention: '魔王的婚姻关系只认候选侧 901',
  },
  {
    desc: 'M6565 SEARCH_FAMILY 略过名字编号校验',
    file: 'ere/chara/chara-family.js',
    find: '    if (target_relation.name !== char_name) continue;',
    replace: '    if (false && target_relation.name !== char_name) continue;',
    tests: ['cross-stubs'],
    must_mention: '婚姻要求名字、前身、性格与家族构成一致',
  },
  {
    desc: 'M6566 SEARCH_FAMILY 前身编码除数改错',
    file: 'ere/chara/chara-family.js',
    find: '    const former_life = Math.trunc((source_relation.data % 1_000) / 10);',
    replace:
      '    const former_life = Math.trunc((source_relation.data % 1_000) / 100);',
    tests: ['cross-stubs'],
    must_mention: '婚姻要求名字、前身、性格与家族构成一致',
  },
  {
    desc: 'M6567 SEARCH_FAMILY 性格编号基数 160 改错',
    file: 'ere/chara/chara-family.js',
    find: '      Math.trunc((source_relation.data % 100_000) / 1_000) + 160;',
    replace:
      '      Math.trunc((source_relation.data % 100_000) / 1_000) + 161;',
    tests: ['cross-stubs'],
    must_mention: '婚姻要求名字、前身、性格与家族构成一致',
  },
  {
    desc: 'M6568 SEARCH_FAMILY 家族构成编码位移改错',
    file: 'ere/chara/chara-family.js',
    find: '    const encoded_family = Math.trunc(source_relation.data / 100_000);',
    replace:
      '    const encoded_family = Math.trunc(source_relation.data / 10_000);',
    tests: ['cross-stubs'],
    must_mention: '婚姻要求名字、前身、性格与家族构成一致',
  },
  {
    desc: 'M6569 SEARCH_FAMILY 男性本人不计入父母的儿子数',
    file: 'ere/chara/chara-family.js',
    find: '    (male ? 1 : 0);',
    replace: '    (male ? 0 : 0);',
    tests: ['cross-stubs'],
    must_mention: '子找父时按家庭中的儿女数核对',
  },
  {
    desc: 'M6570 SEARCH_FAMILY 候选儿子位从千位错读成百位',
    file: 'ere/chara/chara-family.js',
    find: '    digit(target_family, 1_000) === source_sons &&',
    replace: '    digit(target_family, 100) === source_sons &&',
    tests: ['cross-stubs'],
    must_mention: '子找父时按家庭中的儿女数核对',
  },
  {
    desc: 'M6571 SEARCH_FAMILY 家族关系 0 的未找到值改错',
    file: 'ere/chara/chara-family.js',
    find: "  if (search_type === 0 && kind === 'FAMILY') return found;",
    replace: "  if (search_type === 0 && kind === 'FAMILY') return 0;",
    tests: ['cross-stubs'],
    must_mention: '家族数据个位为 0 时不进入兄弟姐妹推断',
  },
  {
    desc: 'M6572 FAMILY_BIRTHTO_MOM 取消负母亲异常',
    file: 'ere/chara/chara-family.js',
    find: '  if (mother < 0) {',
    replace: '  if (mother > 0) {',
    tests: ['cross-stubs'],
    must_mention: '非法母亲抛错',
  },
  {
    desc: 'M6573 FAMILY_BIRTHTO_MOM 母亲关系码 6 改为 5',
    file: 'ere/chara/chara-family.js',
    find: '  return rf_join_to(child, mother, 6, 0, rand);',
    replace: '  return rf_join_to(child, mother, 5, 0, rand);',
    tests: ['cross-stubs'],
    must_mention: '普通父母先重建关系，再以 6/5 加入家庭',
  },
  {
    desc: 'M6574 FAMILY_BIRTHTO_DAD 负父亲误走家庭加入路径',
    file: 'ere/chara/chara-family.js',
    find: '  if (father < 0) return rf_set_both(child, father, 5);',
    replace: '  if (father > 0) return rf_set_both(child, father, 5);',
    tests: ['cross-stubs'],
    must_mention: '父亲负号只记双方关系',
  },
  {
    desc: 'M6575 FAMILY_BIRTHTO_DAD 父亲关系码 5 改为 6',
    file: 'ere/chara/chara-family.js',
    find: '  return rf_join_to(child, father, 5, 0, rand);',
    replace: '  return rf_join_to(child, father, 6, 0, rand);',
    tests: ['cross-stubs'],
    must_mention: '普通父母先重建关系，再以 6/5 加入家庭',
  },
  {
    desc: 'M6576 SELECT_YES_NO 接受非法输入',
    file: 'ere/page/page-life-list.js',
    find: '    if (result === 0 || result === 1) return result;',
    replace: '    return result;',
    tests: ['cross-stubs'],
    must_mention: '非法输入重问',
  },
  {
    desc: 'M6577 SELECT_YES_NO 提示选项改错',
    file: 'ere/page/page-life-list.js',
    find: "    era.print('  [0] 是的   [1] 不要');",
    replace: "    era.print('  [0] 不要   [1] 是的');",
    tests: ['cross-stubs'],
    must_mention: '非法输入重问',
  },
  {
    desc: 'M6578 ITEM_DETOX 私处恢复文本改错',
    file: 'ere/system/equip/item-detox.js',
    find: "detail += '*从私处寄生中恢复* ';",
    replace: "detail += '*从肛门寄生中恢复* ';",
    tests: ['cross-stubs'],
    must_mention: '按原状态输出并经属主门面清除四种寄生素质',
  },
  {
    desc: 'M6579 ITEM_DETOX 不清私处寄生状态',
    file: 'ere/system/equip/item-detox.js',
    find: '  view.dungeon.私处产卵 = 0;',
    replace: '  view.dungeon.私处产卵 = 1;',
    tests: ['cross-stubs'],
    must_mention: '清除四种寄生素质',
  },
  {
    desc: 'M6580 ITEM_DETOX 丢失条目间空格',
    file: 'ere/system/equip/item-detox.js',
    find: "detail += '*蠕虫被排出了* ';",
    replace: "detail += '*蠕虫被排出了*';",
    tests: ['cross-stubs'],
    must_mention: '按原状态输出',
  },
  {
    desc: 'M6581 NAKADASHI_CHECK 妊娠开关掩码 4 改错',
    file: 'ere/event/event-pregnancy.js',
    find: "  if (((era.get('flag:5') || 0) & 4) === 0) {",
    replace: "  if (((era.get('flag:5') || 0) & 2) === 0) {",
    tests: ['cross-stubs'],
    must_mention: '剂量、体型和排卵药共同决定掷骰上界',
  },
  {
    desc: 'M6582 NAKADASHI_CHECK 男性早退条件改坏',
    file: 'ere/event/event-pregnancy.js',
    find: '  if (view.chara.男人 || view.train.未熟) return 0;',
    replace: '  if (view.chara.男人 && view.train.未熟) return 0;',
    tests: ['cross-stubs'],
    must_mention: '男性和非兽耳犬交配早退时不消费精液池',
  },
  {
    desc: 'M6583 NAKADASHI_CHECK 犬父系编号 5 改错',
    file: 'ere/event/event-pregnancy.js',
    find: '  if (kind === 5 && !view.chara.动物耳朵) return 0;',
    replace: '  if (kind === 4 && !view.chara.动物耳朵) return 0;',
    tests: ['cross-stubs'],
    must_mention: '男性和非兽耳犬交配早退时不消费精液池',
  },
  {
    desc: 'M6584 NAKADASHI_CHECK 剂量 10 档阈值改错',
    file: 'ere/event/event-pregnancy.js',
    find: '  else if (pool >= 10) [base, success] = [4, 2];',
    replace: '  else if (pool >= 11) [base, success] = [4, 2];',
    tests: ['cross-stubs'],
    must_mention: '剂量、体型和排卵药共同决定掷骰上界',
  },
  {
    desc: 'M6585 NAKADASHI_CHECK 娇小素质倍率 2 改错',
    file: 'ere/event/event-pregnancy.js',
    find: '  const upper = (base + (era.get(`talent:${cid}:100`) || 0) * 2) * ovulation;',
    replace:
      '  const upper = (base + (era.get(`talent:${cid}:100`) || 0)) * ovulation;',
    tests: ['cross-stubs'],
    must_mention: '剂量、体型和排卵药共同决定掷骰上界',
  },
  {
    desc: 'M6586 NAKADASHI_CHECK 成功比较从小于等于退化为小于',
    file: 'ere/event/event-pregnancy.js',
    find: '  if (rand(upper) <= success) view.event.妊娠相手 = kind;',
    replace: '  if (rand(upper) < success) view.event.妊娠相手 = kind;',
    tests: ['cross-stubs'],
    must_mention: '剂量、体型和排卵药共同决定掷骰上界',
  },
  {
    desc: 'M6587 NAKADASHI_CHECK 预产日守卫阈值改错',
    file: 'ere/event/event-pregnancy.js',
    find: '  if (view.event.预产日 > 0 || view.chara.妊娠 || view.chara.育儿中) {',
    replace:
      '  if (view.event.预产日 > 10 || view.chara.妊娠 || view.chara.育儿中) {',
    tests: ['cross-stubs'],
    must_mention: '已有预产日或正在妊娠时清池但不改妊娠相手',
  },
  {
    desc: 'M6588 MONSTER_DATA 把侵略兵调用范围缩窄到有数据编号',
    file: 'ere/dungeon/monster-data.js',
    find: '  } else if (inum >= 1000 && inum < 2000) {',
    replace: '  } else if (inum >= 1000 && inum <= 1009) {',
    tests: ['cross-stubs'],
    must_mention: '1010-1999 仍走 ENEMY_DATA_CHECK',
  },
  {
    desc: 'M6589 ENEMY_DATA_CHECK 不再保留源函数的无操作行为',
    file: 'ere/dungeon/monster-data.js',
    find: '  void top;',
    replace: '  e_set(top, inum);',
    tests: ['cross-stubs'],
    must_mention: '原作未绑定局部变量造成的无操作行为',
  },
  {
    desc: 'M6590 CAMPAIGN_DUNGEON_LV 战役 1 等级常量改错',
    file: 'ere/dungeon/monster-data.js',
    find: '  return campaign === 1 ? 45 : 0;',
    replace: '  return campaign === 1 ? 44 : 0;',
    tests: ['cross-stubs'],
    must_mention: '战役 1 为 45',
  },
  {
    desc: 'M6591 CAMPAIGN_DUNGEON_LV 忽略当前战役编号',
    file: 'ere/dungeon/monster-data.js',
    find: '  return campaign === 1 ? 45 : 0;',
    replace: '  return campaign > 0 ? 45 : 0;',
    tests: ['cross-stubs'],
    must_mention: '缺失编号为 0',
  },
  {
    desc: 'M6592 NTR_CHILD_BIRTH 公开出产口上编号 20 改错',
    file: 'ere/system/ntr.js',
    find: '  await ntr_koujo(20, rand);',
    replace: '  await ntr_koujo(19, rand);',
    tests: ['cross-stubs'],
    must_mention: '以 P=20 调用已有 NTR 口上族',
  },
  {
    desc: 'M6593 NTR_CHILD_BIRTH 狂王父系编号 7 改错',
    file: 'ere/system/ntr.js',
    find: '  if (father === 7 || father === 4 || father === 2 || father === 3) {',
    replace:
      '  if (father === 8 || father === 4 || father === 2 || father === 3) {',
    tests: ['cross-stubs'],
    must_mention: '父亲分支输出后以 P=20',
  },
  {
    desc: 'M6594 NTR_CHILD_BIRTH 第一轮随机上界 3 改错',
    file: 'ere/system/ntr.js',
    find: '    if (rand(3) === 0) {',
    replace: '    if (rand(2) === 0) {',
    tests: ['cross-stubs'],
    must_mention: '怪物父系按两次独立 RAND',
  },
  {
    desc: 'M6595 NTR_KOUJO 分发参数顺序颠倒',
    file: 'ere/kojo/kojo-system.js',
    find: '    await ntr_koujo_family.call(id, { whenMissing: 0, args: [rand, p] });',
    replace:
      '    await ntr_koujo_family.call(id, { whenMissing: 0, args: [p, rand] });',
    tests: ['cross-stubs'],
    must_mention: '父亲分支输出后以 P=20 调用已有 NTR 口上族',
  },
  {
    desc: 'M6596 NTR_KOUJO_K2 未注册进分发族',
    file: 'ere/kojo/kojo-k2-timid.js',
    find: 'ntr_koujo_family.register(2, ntr_koujo_k2);',
    replace: '// NTR_KOUJO_K2 未注册',
    tests: ['cross-stubs'],
    must_mention: 'K2 与 K4 真身按统一的 rand、P 参数注册',
  },
  {
    desc: 'M6597 NTR_KOUJO_K4 未注册进分发族',
    file: 'ere/kojo/kojo-k4-stoic.js',
    find: 'ntr_koujo_family.register(4, (rand, p) => ntr_koujo_k4(p));',
    replace: '// NTR_KOUJO_K4 未注册',
    tests: ['cross-stubs'],
    must_mention: 'K2 与 K4 真身按统一的 rand、P 参数注册',
  },
  {
    desc: 'M6598 PARTY_DEL 不再调用 SEARCH_FAMILY 真身',
    file: 'ere/dungeon/dungeon-party.js',
    find: "    const partner = search_family(cid, 'MARRIAGE'); // :291",
    replace: '    const partner = -1; // :291',
    tests: ['cross-stubs'],
    must_mention: '队伍离队调用点会清掉找到的结婚对象',
  },
  {
    desc: 'M6599 慈爱来袭口上不再调用 SEARCH_FAMILY 真身',
    file: 'ere/kojo/kojo-k0-tender.js',
    find: '    const family_id = search_family(target); // :8082-8083',
    replace: '    const family_id = -1; // :8082-8083',
    tests: ['cross-stubs'],
    must_mention: '慈爱来袭口上会找到家人并按关系称呼',
  },
  {
    desc: 'M6600 NTR_KOUJO 旧单参数适配器错误传入 rand',
    file: 'ere/kojo/kojo-system.js',
    find: '  return (rand, p) => handler(p ?? rand);',
    replace: '  return (rand, p) => handler(rand ?? p);',
    tests: ['cross-stubs'],
    must_mention: '旧单参数口上适配器优先传 P',
  },
  {
    desc: 'M6601 慈爱来袭口上的弟弟称呼分支文本改错',
    file: 'ere/kojo/kojo-k0-tender.js',
    find: ': `「${chara_callname(family_id)}……等着我！」`; // :8093',
    replace: ': `「${chara_callname(family_id)}……我来救你了！」`; // :8093',
    tests: ['cross-stubs'],
    must_mention: '慈爱来袭口上会找到家人并按关系称呼',
  },
];
