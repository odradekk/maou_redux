/* eslint-disable no-undef, no-unused-vars, no-irregular-whitespace, no-redeclare, no-unreachable, no-dupe-else-if */
/**
 * @file EVENT_K14_貴公子.ERB 的口上转译产物（issue #107 原型，待复核）
 *
 * 源: target/ERB/口上/EVENT_K14_貴公子.ERB
 *
 * == 已有门面名的下标（复核时可升级为门面读写，裁定一） ==
 *   cflag:1 = 状态（facade-names）
 *   cflag:201 = 初调教（facade-names）
 *   cflag:214 = 首次C绝顶_K14（facade-names）
 *   cflag:221 = 首次润滑Lv2（facade-names）
 *   cflag:222 = 首次欲情Lv2（facade-names）
 *   cflag:223 = 首次耻情Lv2（facade-names）
 *   cflag:224 = 首次恐怖Lv2（facade-names）
 *   cflag:226 = 首次V绝顶（facade-names）
 *   cflag:227 = 首次A绝顶（facade-names）
 *   cflag:228 = 首次B绝顶（facade-names）
 *   cflag:229 = 处女丧失（facade-names）
 *   cflag:261 = 调教后自慰（facade-names）
 *   cflag:262 = 百合PLAY（facade-names）
 *   cflag:263 = 朝口交（facade-names）
 *   cflag:264 = 调教后性交（facade-names）
 *   cflag:265 = 夜袭（facade-names）
 *   cflag:271 = 妊娠发觉（facade-names）
 *   cflag:272 = 生产（facade-names）
 *   cflag:297 = 苦痛刻印Lv3（facade-names）
 *   cflag:298 = 快乐刻印Lv3（facade-names）
 *   cflag:299 = 屈服刻印Lv3（facade-names）
 *   cflag:300 = 反抗刻印Lv3（facade-names）
 *   cflag:301 = 爱抚（facade-names）
 *   cflag:302 = 舔阴（facade-names）
 *   cflag:303 = 肛门爱抚（facade-names）
 *   cflag:304 = 自慰（facade-names）
 *   cflag:306 = 胸爱抚（facade-names）
 *   cflag:307 = 接吻（facade-names）
 *   cflag:308 = 自己扒开（facade-names）
 *   cflag:309 = 插入手指（facade-names）
 *   cflag:310 = 舔肛（facade-names）
 *   cflag:311 = 振动宝石（facade-names）
 *   cflag:312 = 壶虫（facade-names）
 *   cflag:313 = 振动杖（facade-names）
 *   cflag:314 = 肛门虫（facade-names）
 *   cflag:318 = 飞机杯（facade-names）
 *   cflag:320 = 肛珠（facade-names）
 *   cflag:321 = 正常位（facade-names）
 *   cflag:322 = 背后位（facade-names）
 *   cflag:323 = 对面座位（facade-names）
 *   cflag:324 = 背面座位（facade-names）
 *   cflag:327 = 正常位肛交（facade-names）
 *   cflag:328 = 背后位肛交（facade-names）
 *   cflag:329 = 对面座位肛交（facade-names）
 *   cflag:330 = 背面座位肛交（facade-names）
 *   cflag:331 = 手淫（facade-names）
 *   cflag:332 = 口交_奴（facade-names）
 *   cflag:333 = 乳交（facade-names）
 *   cflag:334 = 股间性交（facade-names）
 *   cflag:335 = 骑乘位（facade-names）
 *   cflag:336 = 全身擦洗（facade-names）
 *   cflag:337 = 骑乘位肛交（facade-names）
 *   cflag:338 = 肛门侍奉（facade-names）
 *   cflag:341 = 打屁股（facade-names）
 *   cflag:342 = 鞭（facade-names）
 *   cflag:343 = 针（facade-names）
 *   cflag:344 = 眼罩（facade-names）
 *   cflag:345 = 绳子（facade-names）
 *   cflag:346 = 口塞（facade-names）
 *   cflag:347 = 灌肠肛塞（facade-names）
 *   cflag:348 = 穿环（facade-names）
 *   cflag:356 = 放置PLAY（facade-names）
 *   cflag:357 = 交谈（facade-names）
 *   cflag:360 = 乳夹口交（facade-names）
 *   cflag:361 = 口交时自慰（facade-names）
 *   cflag:362 = 手搓口交（facade-names）
 *   cflag:363 = 真空口交（facade-names）
 *   cflag:364 = 六九式（facade-names）
 *   cflag:365 = 深喉（facade-names）
 *   cflag:370 = 魔族化（facade-names）
 *   cflag:372 = 壶虫着脱（facade-names）
 *   cflag:374 = 肛门虫着脱（facade-names）
 *   cflag:378 = 飞机杯着脱（facade-names）
 *   cflag:379 = 肛珠着脱（facade-names）
 *   cflag:380 = 眼罩着脱（facade-names）
 *   cflag:381 = 强制口交（facade-names）
 *   cflag:385 = 绳子着脱（facade-names）
 *   cflag:386 = 口塞着脱（facade-names）
 *   cflag:444 = 兽奸眼罩（facade-names）
 *   cflag:650 = NTR再捕获（facade-names）
 *   flag:62 = 肉便器行动（facade-names）
 *   flag:63 = 肉便器常识改写（facade-names）
 *   flag:7 = 口上开关（facade-names）
 *   tflag:13 = 初吻与自我口上（facade-names）
 *   tflag:150 = 反抗刻印回避（facade-names）
 *   tflag:16 = 犬射精或处刑口上（facade-names）
 *   tflag:18 = 足交射精或处遇口上（facade-names）
 *   tflag:20 = 主人导致处女丧失（facade-names）
 *   tflag:21 = 反抗刻印变动（facade-names）
 *   tflag:22 = 苦痛刻印变动（facade-names）
 *   tflag:23 = 快乐刻印变动（facade-names）
 *   tflag:24 = 屈服刻印变动（facade-names）
 *   tflag:3 = 处女丧失（facade-names）
 *   tflag:400 = 死斗场敌种（facade-names）
 *   tflag:500 = 博物馆口上（facade-names）
 *   tflag:510 = 流放口上（facade-names）
 *   tflag:520 = 公开处刑口上（facade-names）
 *   tflag:530 = 猎奇处刑口上（facade-names）
 *   tflag:899 = 失神（facade-names）
 *
 * == 复核标记（133 处） ==
 * 本文件由 tools/kojo-transpiler.js 生成。以下位置是机械转换无法
 * 确定的，须 agent 逐字对照 ERB 源复核（裁定 7：agent 逐字对照，
 * 不是抽查）。复核成果 = 在本文件内改写成最终形态，并把本 REVIEW
 * 清单逐条删掉；转译器默认不覆盖本文件（产物边界，issue #10），
 * 复核成果不会被重跑覆盖。
 *   1. :53 同名函数 @EVENTTRAIN 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTTRAIN', …) 注册，参照 ere/kojo/kojo-k5.js
 *   2. :319 假名残留 归一后仍含日文假名：あなたのでうっとりしくがまにをけよていはもわだろ —— 查 lang-table 是否应收
 *   3. :360 CALL K14_KOJO2 —— 口上文件里多为存根调用，人工定存根名
 *   4. :420 CALL K14_KOJO2 —— 口上文件里多为存根调用，人工定存根名
 *   5. :472 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   6. :474 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   7. :485 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   8. :487 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   9. :500 同名函数 @EVENTEND 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTEND', …) 注册，参照 ere/kojo/kojo-k5.js
 *   10. :615 CALL DOG_KOJO_14 —— 口上文件里多为存根调用，人工定存根名
 *   11. :620 CALL COLOSSEUM_KOJO_14 —— 口上文件里多为存根调用，人工定存根名
 *   12. :719 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   13. :721 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   14. :725 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   15. :729 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   16. :733 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   17. :737 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   18. :767 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   19. :769 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   20. :778 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   21. :791 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   22. :793 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   23. :802 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   24. :811 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   25. :820 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   26. :1663 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   27. :1665 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   28. :1673 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   29. :1675 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   30. :1736 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   31. :1738 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   32. :1746 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   33. :1748 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   34. :1808 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   35. :1810 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   36. :1818 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   37. :1820 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   38. :1865 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   39. :1867 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   40. :1879 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   41. :1924 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   42. :1969 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   43. :1971 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   44. :1983 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   45. :2028 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   46. :2040 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   47. :2088 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   48. :2096 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   49. :2195 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   50. :2207 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   51. :2311 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   52. :2313 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   53. :2315 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   54. :2323 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   55. :2325 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   56. :2327 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   57. :2335 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   58. :2337 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   59. :2339 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   60. :2419 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   61. :2431 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   62. :2930 表达式寻址 PALAMLV:3 → era.get('palamlv:3')——族名直译，语义与归属人工定
 *   63. :3853 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   64. :3855 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   65. :3863 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   66. :3865 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   67. :3873 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   68. :3875 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   69. :3924 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   70. :3932 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   71. :3940 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   72. :3988 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   73. :3996 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   74. :4004 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   75. :4124 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   76. :4126 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   77. :4134 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   78. :4136 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   79. :4138 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   80. :4146 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   81. :4148 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   82. :4150 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   83. :4158 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   84. :4160 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   85. :4162 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   86. :4385 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   87. :4386 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   88. :4412 局部变量 P = PALAM:5 + UP:5 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   89. :4413 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   90. :4439 局部变量 P = PALAM:8 + UP:8 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   91. :4440 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   92. :4454 局部变量 P = PALAM:10 + UP:10 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   93. :4455 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   94. :4469 表达式寻址 NOWEX:0 → era.get('nowex:0')——族名直译，语义与归属人工定
 *   95. :4483 表达式寻址 NOWEX:1 → era.get('nowex:1')——族名直译，语义与归属人工定
 *   96. :4500 表达式寻址 NOWEX:2 → era.get('nowex:2')——族名直译，语义与归属人工定
 *   97. :4517 表达式寻址 NOWEX:3 → era.get('nowex:3')——族名直译，语义与归属人工定
 *   98. :4531 局部变量 A = UP:11 + UP:12 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   99. :5225 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   100. :5226 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   101. :5227 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   102. :5231 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   103. :5232 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   104. :5233 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   105. :5289 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   106. :5291 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   107. :5300 假名残留 归一后仍含日文假名：のこれほどとは —— 查 lang-table 是否应收
 *   108. :5306 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   109. :5308 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   110. :5343 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   111. :5345 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   112. :5360 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   113. :5362 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   114. :5380 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   115. :5382 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   116. :5397 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   117. :5399 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   118. :5735 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   119. :5738 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   120. :5749 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   121. :5752 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   122. :5755 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   123. :5758 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   124. :5761 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   125. :5764 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   126. :5911 函数参数 @GOBI_KOUJO_K14, ARG:0 —— 参数声明已剥（JS 函数签名人工定）
 *   127. :5914 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   128. :5917 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   129. :5920 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   130. :5923 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   131. :5926 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   132. :5932 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   133. :5934 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 */

'use strict';

// 需要复核 agent 补的导入（产物初稿不 require，保真锁会红）：
// const era = require('#/era-electron');
// const { heart, self_call, self_call_first } = require('#/kojo/kojo-text');
// const { chara_callname } = require('#/utils/callname-utils');
// 以及 target_name / player_name / assi_name / master_name / sc() / scf()
// 的取值（%SAVESTR:TARGET% 等插值的 JS 侧表达式）。






































// @EVENTTRAIN // :39
async function EVENTTRAIN() {
  // #PRI（事件优先级修饰符，JS 侧用 on() 的 TIER 表达） // :40
  // FLAG:114  = 1（变量语义：FLAG 族，114） // :41
  era.set('flag:114', 1); // :41
  if (era.get('flag:7') == 0) { // :43
    // FLAG:7  = 2（变量语义：FLAG 族，7） // :43
    era.set('flag:7', 2); // :43
  } // :43

}

// @EVENTEND // :45
async function EVENTEND() {
  // #LATER（事件优先级修饰符，JS 侧用 on() 的 TIER 表达） // :46
  // FLAG:114  = 0（变量语义：FLAG 族，114） // :47
  era.set('flag:114', 0); // :47





}

// @EVENTTRAIN // :53
async function EVENTTRAIN() {
  if (era.get('flag:7') <= 0) { // :55
    return 0; // :55
  } // :55
  if (era.get(`talent:${target}:174`) != 1) { // :57
    return 0; // :57
  } // :57




  if (era.get(`cflag:${target}:201`) == 0) { // :62
    era.drawLine(); // :63

    if (era.get(`talent:${target}:122`) && era.get(`talent:${target}:314`) == 9) { // :65
      await era.printAndWait(`经过多次改造后，${target_name}转生成为魔族了。`); // :66
      await era.printAndWait(`${master_name}前来看看情况，就看到${target_name}一脸焦虑地烦恼着发生在自己身上的事。`); // :67
      await era.printAndWait(`（这种不可言喻的感觉…，啊！！这…这难道就是…暗之魔力…吗！！？）`); // :68
      await era.printAndWait(`「啊…魔、魔王大人…，嗯唔…！？魔…魔王…！！！不…！${sc()}是不会对你…！？啊…这…这绝对不可能…，我竟然会对你…！」`); // :69
      await era.printAndWait(`成为魔族了的${target_name}，想要发泄对于转生成魔族的怨恨。`); // :70
      await era.printAndWait(`但对于魔族之王的你的忠诚已经深刻于心，从心底感觉到无法违抗………`); // :71
      // CFLAG:201  = 1（变量语义：CFLAG 族，201） // :72
      era.set(`cflag:${target}:201`, 1); // :72

      // CFLAG:370  = 1（变量语义：CFLAG 族，370） // :74
      era.set(`cflag:${target}:370`, 1); // :74


    } else if (era.get(`talent:${target}:314`) == 9 && era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :77
      await era.printAndWait(`${target_name}经过多次改造的过程中变成了女性，之后更是转生成为了魔族。`); // :78
      await era.printAndWait(`${master_name}前来看看情况，就看到${target_name}一脸焦虑地烦恼着发生在自己身上的事。`); // :79
      await era.printAndWait(`（像这样畅快的感觉…而且总感觉…，身体里面…好像有一种奇怪的冲动…${heart(1)}）`); // :80
      await era.printAndWait(`「难道是受到了魔王植入的魔力所影响的么…？还是说…」`); // :81
      await era.printAndWait(`「啊…魔、魔王大人…，嗯唔…！？魔…魔王…！！！不…！${sc()}是不会对你…！？啊…这…这绝对不可能…，我竟然会对你…！」`); // :82
      await era.printAndWait(`成为魔族了的${target_name}，想要发泄对于自己完全发生转变的怨恨。`); // :83
      await era.printAndWait(`但对于魔族之王的你的忠诚所带来的愉快感，更胜过想要抵抗的想法………`); // :84
      // CFLAG:201  = 1（变量语义：CFLAG 族，201） // :85
      era.set(`cflag:${target}:201`, 1); // :85

      // CFLAG:370  = 1（变量语义：CFLAG 族，370） // :87
      era.set(`cflag:${target}:370`, 1); // :87


    } else if (era.get(`talent:${target}:122`)) { // :90
      await era.printAndWait(`「可…可恶啊！！！你这个肮脏的魔王！！我郑重告诉你！${sc()}是绝对不会屈服于你的…！！」`); // :91
      await era.printAndWait(`怒目圆睁的眼睛中，隐约可以窥见他内心的恐惧……`); // :92
      // CFLAG:201  = 1（变量语义：CFLAG 族，201） // :93
      era.set(`cflag:${target}:201`, 1); // :93


    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :96
      await era.printAndWait(`在${master_name}的戏弄下、${target_name}被改造成了女性的肉体了。`); // :97
      await era.printAndWait(`「你这个可恶的魔王…！！赶快把${sc()}的身体变回原来的样子啊！」`); // :98
      await era.printAndWait(`${master_name}来到了房间，${target_name}就瞪了过来并大声抗议了起来。`); // :99
      await era.printAndWait(`「${sc()}可是个男人啊！才不是一个女人啊！！喂…！！你这家伙有在听我说话吗！？」`); // :100
      await era.printAndWait(`这么呼喊着的${target_name}，被${master_name}按倒在了床上。`); // :101
      await era.printAndWait(`「啊…喂！！你…你这家伙…！想要对${sc()}做什么…！赶快放开你的手啊…！你这个肮脏的家伙…！」`); // :102
      await era.printAndWait(`他、不，她的身心将因被刻上迄今为止从未体会过的快感而顺从吧……`); // :103
      // CFLAG:201  = 1（变量语义：CFLAG 族，201） // :104
      era.set(`cflag:${target}:201`, 1); // :104
      return 1; // :105
    } // :106



  } else if (era.get(`cflag:${target}:201`) < 5 && era.get(`cflag:${target}:370`) == 0 && era.get(`talent:${target}:314`) == 9 && era.get(`talent:${target}:85`) == 0 && era.get(`talent:${target}:76`) == 0) { // :110
    await era.printAndWait(`经过多次改造后完全成为魔族的${target_name}对自己的模样感到绝望…`); // :111
    await era.printAndWait(`注意到来到房间了的你，不知所措地看着你。`); // :112
    await era.printAndWait(`「可恶的…魔王…！！赶快把${sc()}的身体…，彻彻底底的变回去…！」`); // :113
    await era.printAndWait(`成为魔族的${sc()}，已经开始从本能上感觉到无法违抗身为魔族之王的你了………`); // :114

    // CFLAG:370  = 2（变量语义：CFLAG 族，370） // :116
    era.set(`cflag:${target}:370`, 2); // :116
    return 1; // :117



  } else if (era.get(`cflag:${target}:201`) >= 1 && era.get(`cflag:${target}:650`) == 1) { // :121
    if (era.get(`talent:${target}:85`) || era.get(`talent:${target}:76`)) { // :122

      era.drawLine(); // :124
      await era.printAndWait(`「还…还真是抱歉呢…，因为${sc()}我…好像有点太容易就败给诱惑了呢…」`); // :125

      // CFLAG:650  = 0（变量语义：CFLAG 族，650） // :127
      era.set(`cflag:${target}:650`, 0); // :127
    } else { // :128
      era.drawLine(); // :129
      await era.printAndWait(`「嗛…怎么又是你啊…！」`); // :130

      // CFLAG:650  = 0（变量语义：CFLAG 族，650） // :132
      era.set(`cflag:${target}:650`, 0); // :132
    } // :133
    return 1; // :134




  } else if (era.get(`cflag:${target}:201`) < 2 && era.get(`mark:${target}:2`) == 1) { // :139
    era.drawLine(); // :140
    await era.printAndWait(`「可恶的…魔王…！${sc()}是绝对不会输给你的…也不会顺从你的！！」`); // :141
    // CFLAG:201  = 2（变量语义：CFLAG 族，201） // :142
    era.set(`cflag:${target}:201`, 2); // :142
    return 1; // :143


  } else if (era.get(`cflag:${target}:201`) < 3 && era.get(`mark:${target}:2`) == 2) { // :146
    era.drawLine(); // :147
    if (era.get(`talent:${target}:122`)) { // :148
      await era.printAndWait(`「呃…，今天也要继续做那种事情啊…！？」`); // :149

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :151
      await era.printAndWait(`「啊…，这种快乐而且微妙的感觉…真的是好棒啊…，但…但是…」`); // :152
    } // :153
    // CFLAG:201  = 3（变量语义：CFLAG 族，201） // :154
    era.set(`cflag:${target}:201`, 3); // :154
    return 1; // :155


  } else if (era.get(`cflag:${target}:201`) < 4 && era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0) { // :158
    era.drawLine(); // :159
    if (era.get(`talent:${target}:122`)) { // :160
      await era.printAndWait(`「抱歉了…大家…，因为${sc()}已经…已经快要…」`); // :161

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :163
      await era.printAndWait(`「像这种舒适的快感…，${sc()}感觉到…好像已经、已经完全的要沦陷成为真正的女人了啊…」`); // :164
    } // :165
    // CFLAG:201  = 4（变量语义：CFLAG 族，201） // :166
    era.set(`cflag:${target}:201`, 4); // :166
    return 1; // :167


  } else if (era.get(`cflag:${target}:201`) < 5 && era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:85`) == 0) { // :170
    era.drawLine(); // :171

    if (era.get(`talent:${target}:122`)) { // :173
      await era.printAndWait(`「啊~，魔王大人~，${sc()}啊…，一直都在这里等待着您的到来呢~」`); // :174

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :176
      await era.printAndWait(`「啊~，魔王大人~，${sc()}啊…，一直都在这里等待着您的到来呢~」`); // :177
    } // :178
    // CFLAG:201  = 5（变量语义：CFLAG 族，201） // :179
    era.set(`cflag:${target}:201`, 5); // :179
    return 1; // :180


































































  } else if (era.get(`cflag:${target}:201`) < 6 && era.get(`talent:${target}:85`) == 1) { // :247
    era.drawLine(); // :248
    if (era.get(`talent:${target}:122`)) { // :249
      await era.printAndWait(`「魔…魔王大人…，${sc()}…，已经绝对不会再反抗您了。所…所以…」`); // :250
      await era.printAndWait(`「${sc()}、想要尽可能的帮上魔王大人的忙…想要在呆我所尊敬的魔王大人身边、支持着您…可以吗…！？」`); // :251
      await era.printAndWait(`你听了${target_name}的愿望之后、向他传达了既然如此今后就好好侍奉的意思…`); // :252
      await era.printAndWait(`「好的…，那么就如魔王大人所愿。。。」`); // :253
      await era.printAndWait(`套弄抚摸着${target_name}已经勃起了的阴茎、你脸上浮现了扭曲的笑容…`); // :254

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :256
      await era.printAndWait(`「魔…魔王大人…，${sc()}…，已经绝对不会再反抗您了。所…所以…」`); // :257
      await era.printAndWait(`「${sc()}、想要尽自己全部的能力帮助魔王大人…，所以…我为了尊敬的魔王大人、不管是什么事情我都愿意去做！」`); // :258
      await era.printAndWait(`「就连这副身体…也请随便使用吧…、啊…！但是…像${sc()}这样原来是男人的女人…真的能接受么…？」`); // :259
      await era.printAndWait(`你听了${target_name}的愿望之后、向他传达了既然如此今后就好好侍奉的意思…`); // :260
      await era.printAndWait(`「好的…如魔王大人所愿${heart(1)}」`); // :261
      if (era.get(`talent:${target}:0`) == 1) { // :263
        await era.printAndWait(`「${target_name}的『第一次』、因为想被魔王大人夺走…所以有在好好的为您保存着呢哦…${heart(1)}」`); // :263
      } // :263
      await era.printAndWait(`爱抚着满心欢喜的${target_name}的雌性身体、你脸上浮现了扭曲的笑容…`); // :264
    } // :265
    // CFLAG:201  = 6（变量语义：CFLAG 族，201） // :266
    era.set(`cflag:${target}:201`, 6); // :266
    return 1; // :267


  } else if (era.get(`talent:${target}:314`) == 9 && era.get(`cflag:${target}:201`) < 8 && era.get(`talent:${target}:85`) == 1 && era.get(`talent:${target}:76`) == 0) { // :270
    era.drawLine(); // :271
    if (era.get(`talent:${target}:122`)) { // :272

      if (era.get(`cflag:${target}:370`) == 1) { // :274
        await era.printAndWait(`「啊啊…魔王大人…，之所以把${sc()}转化成了魔族的原因、其实就是为了这个对吧…」`); // :275
        await era.printAndWait(`你进入房间的时候、${target_name}正带着温柔的表情跪在地上迎接你的到来。`); // :276
        await era.printAndWait(`「伟大的魔王大人啊…，${sc()}我…，已经完全的接受了身为魔族的一切了哦…，并且…会永远…与您相伴到最后的…」`); // :277
        await era.printAndWait(`「所以说不管是什么样的命令…${target_name}全部都会、按照魔王大人所愿去做的…」`); // :278
        if (era.get(`talent:${target}:77`) == 1) { // :280
          await era.printAndWait(`「${target_name}已经开发完全的菊穴、也请魔王大人毫不客气的使用吧…${heart(1)}」`); // :280
        } // :280
        await era.printAndWait(`不久前还叫嚣着要讨灭你的男冒险者、现在完全转生成了誓死效忠你的魔族了。`); // :281
        await era.printAndWait(`你抱紧了${target_name}的身体、脸上浮现扭曲的笑容…`); // :282
        // CFLAG:201  = 8（变量语义：CFLAG 族，201） // :283
        era.set(`cflag:${target}:201`, 8); // :283
        return 1; // :284

      } else if (era.get(`cflag:${target}:370`) == 2) { // :286
        await era.printAndWait(`「嗯…？啊…！魔…魔王大人…！！${scf()}、${sc()}…」`); // :287
        await era.printAndWait(`你进入房间的时候、${target_name}虽然还有点迷茫、但还是跪在地上迎接你的到来。`); // :288
        await era.printAndWait(`「嗯…，伟大的魔王大人啊…${sc()}从现在开始再也不会迷茫了…因为我已经完全的接受了身为魔族的一切了、所以我愿时刻陪伴在您左右」`); // :289
        await era.printAndWait(`「所以…就对${scf()}、对${sc()}…，更加的…更加多的来疼爱我吧…！」`); // :290
        if (era.get(`talent:${target}:77`) == 1) { // :292
          await era.printAndWait(`「所以也请魔王大人来更多的使用、${target_name}那已经开发完全的菊穴…！」`); // :292
        } // :292
        await era.printAndWait(`你、轻轻地抱住了几乎要哭出来的${target_name}。`); // :293
        await era.printAndWait(`从调教的结果来看、${target_name}似乎对你产生了爱慕之情…`); // :294
        // CFLAG:201  = 8（变量语义：CFLAG 族，201） // :295
        era.set(`cflag:${target}:201`, 8); // :295
        return 1; // :296

      } else { // :298
        await era.printAndWait(`「哦…魔王大人…，能将${sc()}彻底改造成了魔族、这还真的是万分感激呢…」`); // :299
        await era.printAndWait(`你进入房间的时候、${target_name}正带着温柔的表情跪在地上迎接你的到来。`); // :300
        await era.printAndWait(`「那么这样一来…，${sc()}也就和您一样都是魔族了啊…♪」`); // :301
        await era.printAndWait(`${target_name}端详了自己已经成为魔族的身体、再次跪了下来。`); // :302
        await era.printAndWait(`「那么…，从今往后…！${target_name}！！将会作为您身边的奴仆来随时听从着魔王大人号令！！」`); // :303
        if (era.get(`talent:${target}:77`) == 1) { // :305
          await era.printAndWait(`「${target_name}开发完全的菊穴、也请魔王大人随意使用吧${heart(1)}」`); // :305
        } // :305
        // CFLAG:201  = 8（变量语义：CFLAG 族，201） // :306
        era.set(`cflag:${target}:201`, 8); // :306
        return 1; // :307
      } // :308

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :310

      if (era.get(`cflag:${target}:370`) == 1) { // :312
        await era.printAndWait(`「啊啊…魔王大人…，之所以把${sc()}转化成了魔族的原因、其实就是为了这个对吧…」`); // :313
        await era.printAndWait(`你进入房间的时候、${target_name}正带着温柔的表情跪在地上迎接你的到来。`); // :314
        await era.printAndWait(`「伟大的魔王大人啊…，${sc()}我…，已经完全的接受了身为魔族的一切了哦…，并且…会永远…与您相伴到最后的…」`); // :315
        await era.printAndWait(`「按照魔王大人所愿去做的…」`); // :316
        if (era.get(`talent:${target}:0`) == 1) { // :318
          await era.printAndWait(`「${target_name}的『第一次』、可是为了献给魔王大人…才一直留到现在的哦…${heart(1)}」`); // :318
        } // :318
        await era.printAndWait(`あなたの目の前でうっとりとした表情で跪く女性が、少し前まであなたに刃を向けようとしていた男勇者とは谁も思わないだろう。`); // :319
        await era.printAndWait(`你抱紧了${target_name}的身体、脸上浮现扭曲的笑容…`); // :320
        // CFLAG:201  = 8（变量语义：CFLAG 族，201） // :321
        era.set(`cflag:${target}:201`, 8); // :321
        return 1; // :322

      } else if (era.get(`cflag:${target}:370`) == 2) { // :324
        await era.printAndWait(`「嗯…？啊…！魔…魔王大人…！！${scf()}、${sc()}…」`); // :325
        await era.printAndWait(`你进入房间的时候、${target_name}虽然还有点迷茫、但还是跪在地上迎接你的到来。`); // :326
        await era.printAndWait(`「嗯…，伟大的魔王大人啊…${sc()}从现在开始再也不会迷茫了…因为我已经完全的接受了身为魔族的一切了、所以我愿时刻陪伴在您左右」`); // :327
        await era.printAndWait(`「所以…就对${scf()}、对${sc()}…，更加的…更加多的来疼爱我吧…！」`); // :328
        if (era.get(`talent:${target}:0`) == 1) { // :330
          await era.printAndWait(`「${target_name}的『第一次』、如果不是魔王大人的话…那可是绝对不允许的哦…！！」`); // :330
        } // :330
        await era.printAndWait(`你、轻轻地抱住了几乎要哭出来的${target_name}。`); // :331
        await era.printAndWait(`从调教的结果来看、${target_name}似乎对你产生了爱慕之情…`); // :332
        // CFLAG:201  = 8（变量语义：CFLAG 族，201） // :333
        era.set(`cflag:${target}:201`, 8); // :333
        return 1; // :334

      } else { // :336
        await era.printAndWait(`「哦…魔王大人…，能将${sc()}彻底改造成了魔族、这还真的是万分感激呢…」`); // :337
        await era.printAndWait(`你进入房间的时候、${target_name}正带着温柔的表情跪在地上迎接你的到来。`); // :338
        await era.printAndWait(`「那么这样一来…，${sc()}也就和您一样都是魔族了啊…♪」`); // :339
        await era.printAndWait(`${target_name}端详了自己已经成为魔族的身体、再次跪了下来。`); // :340
        await era.printAndWait(`「那么…，从今往后…！${target_name}！！将会作为您身边的奴仆来随时听从着魔王大人号令！！」`); // :341
        if (era.get(`talent:${target}:0`) == 1) { // :343
          await era.printAndWait(`「${target_name}的『第一次』、留了那么久，就是为了让魔王大人来取走的啊…${heart(1)}」`); // :343
        } // :343
        // CFLAG:201  = 8（变量语义：CFLAG 族，201） // :344
        era.set(`cflag:${target}:201`, 8); // :344
        return 1; // :345
      } // :346
    } // :347











  } else if (era_flag.assi < 0) { // :359
    // CALL K14_KOJO2 // :360


























































  } else { // :419
    // CALL K14_KOJO2 // :420
  } // :421





}

// @K14_KOJO2 // :427
async function K14_KOJO2() {

  if (era.get(`mark:${target}:3`) == 3 && era.get('flag:7') == 2) { // :429
    era.drawLine(); // :430
    await era.printAndWait(`「去死一死吧！！你这个又脏又可恶的魔王！！」`); // :431
    return 1; // :432


  } else if (era.get(`mark:${target}:2`) == 0 && era.get('flag:7') == 2) { // :435
    era.drawLine(); // :436
    await era.printAndWait(`「不…不要在过来了！！快住手啊！！！」`); // :437
    return 1; // :438


  } else if (era.get(`mark:${target}:2`) == 1 && era.get('flag:7') == 2) { // :441
    era.drawLine(); // :442
    await era.printAndWait(`「可恶的…魔王…！${sc()}是绝对不会输给你的…也不会顺从你的！！」`); // :443
    return 1; // :444


  } else if (era.get(`mark:${target}:2`) == 2 && era.get('flag:7') == 2) { // :447
    era.drawLine(); // :448
    if (era.get(`talent:${target}:122`)) { // :449
      await era.printAndWait(`「呃…，今天也要继续做那种事情啊…！？」`); // :450

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :452
      await era.printAndWait(`「咕呜…！我…我是不可能就这么轻易的输给快感的…！但…但是…」`); // :453
    } // :454
    return 1; // :455


  } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0 && era.get(`talent:${target}:76`) == 0 && era.get('flag:7') == 2) { // :458
    era.drawLine(); // :459
    if (era.get(`talent:${target}:122`)) { // :460
      await era.printAndWait(`「我明白了…，那么你想干什么就随你喜欢好了…」`); // :461

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :463
      await era.printAndWait(`「既然…我已经没有办法再次变回男人的话…，那么…！魔…魔王大人…！就请您再给${sc()}、传授更多的快乐吧…」`); // :464
    } // :465
    return 1; // :466


  } else if (era.get(`talent:${target}:76`) == 1 && era.get('flag:7') == 2) { // :469
    era.drawLine(); // :470

    if (rand_n(3) == 0) { // :472
      await era.printAndWait(`「啊~，魔王大人~，${sc()}啊…，一直都在这里等待着您的到来呢~」`); // :473
    } else if (rand_n(2) == 0) { // :474
      await era.printAndWait(`「那么…魔王大人？今天的话…您打算对我做什么事情呢~？」`); // :475
    } else { // :476
      await era.printAndWait(`「嗯…，我已经等您好久了呢~♪」`); // :477
    } // :478
    return 1; // :479


  } else if (era.get(`talent:${target}:85`) == 1 && era.get('flag:7') == 2) { // :482
    era.drawLine(); // :483

    if (rand_n(3) == 0) { // :485
      await era.printAndWait(`「啊…！魔…魔王大人…！！您来了啊！！好开心呢…」`); // :486
    } else if (rand_n(2) == 0) { // :487
      await era.printAndWait(`「就按照魔王大人想做的…，来进行调教吧…」`); // :488
    } else { // :489
      await era.printAndWait(`「如果是魔王大人的话…，不管是对我做什么都是可以的哦…」`); // :490
    } // :491
    return 1; // :492
  } // :493
  return 0; // :494





}

// @EVENTEND // :500
async function EVENTEND() {
  if (era.get('flag:7') <= 0) { // :502
    return 0; // :502
  } // :502
  if (era.get(`talent:${target}:174`) != 1) { // :504
    return 0; // :504
  } // :504


  if (era.get(`base:${target}:0`) <= 0) { // :508
    return 0; // :508
  } // :508





  if (era.get(`mark:${target}:3`) == 3 && era.get(`talent:${target}:85`) == 0) { // :514
    era.drawLine(); // :515
    await era.printAndWait(`「嘁…！给我去死啊…！！！」`); // :516
    return 1; // :517


  } else if (era.get(`mark:${target}:2`) <= 1 && era.get(`talent:${target}:85`) == 0) { // :520
    era.drawLine(); // :521
    await era.printAndWait(`「哼…，总算是结束了呢…」`); // :522
    return 1; // :523


  } else if (era.get(`mark:${target}:2`) == 2 && era.get(`talent:${target}:85`) == 0) { // :526
    era.drawLine(); // :527
    if (era.get(`talent:${target}:122`)) { // :528
      await era.printAndWait(`「哈啊…嗯…，结…结束了么…？」`); // :529

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :531
      await era.printAndWait(`「哈…呀啊…，这…这种快乐…、总觉得…要上瘾了啊…，咿嗯…！？${sc()}…！到底在说什么呢…！！」`); // :532
    } // :533
    return 1; // :534


  } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0) { // :537
    era.drawLine(); // :538
    if (era.get(`talent:${target}:122`)) { // :539
      await era.printAndWait(`「请…请放过我吧…！！」`); // :540

    } else if (era.get(`cflag:${target}:70`) && era.get(`talent:${target}:122`) == 0) { // :542
      await era.printAndWait(`「呼啊…啊哈哈…，这种感觉…真的…，好棒呢…哈啊…」`); // :543
    } // :544
    return 1; // :545


  } else if (era.get(`talent:${target}:76`) == 1 && era.get(`base:${target}:0`) >= 500) { // :548
    era.drawLine(); // :549

    if (era.get(`talent:${target}:314`) == 9) { // :551
      await era.printAndWait(`「哈…！？只是这样就结束了么…！？喂…，魔王大人啊，不带你这个样子的吧…？」`); // :552
    } else { // :553
      await era.printAndWait(`「哈…！？只是这样就结束了么…！？喂…，魔王大人啊，不带你这个样子的吧…？」`); // :554
    } // :555
    return 1; // :556

  } else if (era.get(`talent:${target}:76`) == 1 && era.get(`base:${target}:0`) <= 500) { // :558
    era.drawLine(); // :559

    if (era.get(`talent:${target}:314`) == 9) { // :561
      await era.printAndWait(`「啊呜…哈啊…嗯…，${sc()}…，感觉…真的是太满足了呢~」`); // :562
    } else { // :563
      await era.printAndWait(`「啊呜…哈啊…嗯…，${sc()}…，感觉…真的是太满足了呢~」`); // :564
    } // :565
    return 1; // :566


  } else if (era.get(`talent:${target}:85`) == 1 && era.get(`base:${target}:0`) >= 500) { // :569
    era.drawLine(); // :570

    if (era.get(`talent:${target}:314`) == 9 && era.get(`talent:${target}:122`) == 1) { // :572
      await era.printAndWait(`「已经结束了么…？啊…我明白了…」`); // :573
    } else if (era.get(`talent:${target}:122`) == 1) { // :574
      await era.printAndWait(`「已经结束了么…？啊…我明白了…」`); // :575
    } else if (era.get(`talent:${target}:314`) == 9) { // :576
      await era.printAndWait(`「已经结束了么…？明明还想要更多的再亲爱一会的说…」`); // :577
    } else { // :578
      await era.printAndWait(`「这就要结束了么…？可是…还想要被您更多的疼爱的说呢…」`); // :579
    } // :580
    return 1; // :581

  } else if (era.get(`talent:${target}:85`) == 1 && era.get(`base:${target}:0`) <= 500) { // :583
    era.drawLine(); // :584

    if (era.get(`talent:${target}:314`) == 9 && era.get(`talent:${target}:122`) == 1) { // :586
      await era.printAndWait(`「啊…，今天真的是辛苦您了…，还真的是非常感谢呢~」`); // :587
    } else if (era.get(`talent:${target}:122`) == 1) { // :588
      await era.printAndWait(`「啊…，今天真的是辛苦您了…，还真的是非常感谢呢~」`); // :589
    } else if (era.get(`talent:${target}:314`) == 9) { // :590
      await era.printAndWait(`「啊…嗯哼~，像这个样子来疼爱我…还真的是感谢了呢~！那么…明天也要继续来才行哦…！${heart(1)}」`); // :591
    } else { // :592
      await era.printAndWait(`「啊…嗯…，今天还真的是十分感谢了啊…，可是我真的已经很累了哦…所以要休息一下了…，不过在我休息好之后，就继续的在一起相亲相爱吧~」`); // :593
    } // :594
    return 1; // :595
  } // :596
  return 0; // :597





}

// @KOJO_MESSAGE_COM_14 // :603
async function KOJO_MESSAGE_COM_14() {




  if (era.get(`tequip:${target}:45`) && era_flag.selectcom != 45) { // :609
    return 0; // :609
  } // :609

  if (era.get('tflag:899')) { // :612
    return 0; // :612
  } // :612

  if (era.get(`tequip:${target}:89`)) { // :614
    // CALL DOG_KOJO_14 // :615
    return 0; // :616
  } // :617

  if (era.get(`tequip:${target}:55`)) { // :619
    // CALL COLOSSEUM_KOJO_14 // :620
    return 0; // :621
  } // :622







  if (era_flag.selectcom == 0) { // :630

    if (era.get(`cflag:${target}:301`) == 0) { // :632

      if (era.get(`mark:${target}:2`) >= 2) { // :634
        await era.printAndWait(''); // :635

      } else { // :637
        await era.printAndWait(''); // :638
      } // :639
      // CFLAG:301  = 1（变量语义：CFLAG 族，301） // :640
      era.set(`cflag:${target}:301`, 1); // :640
      return 0; // :641

    } else { // :643

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:301`) <= 5 || era.get('flag:7') == 2)) { // :645
        await era.printAndWait(''); // :646
        // CFLAG:301  = 6（变量语义：CFLAG 族，301） // :647
        era.set(`cflag:${target}:301`, 6); // :647

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:301`) <= 4 || era.get('flag:7') == 2)) { // :649
        await era.printAndWait(''); // :650
        // CFLAG:301  = 5（变量语义：CFLAG 族，301） // :651
        era.set(`cflag:${target}:301`, 5); // :651

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:301`) <= 3 || era.get('flag:7') == 2)) { // :653
        await era.printAndWait(''); // :654
        // CFLAG:301  = 4（变量语义：CFLAG 族，301） // :655
        era.set(`cflag:${target}:301`, 4); // :655

      } else if (era.get(`mark:${target}:2`) == 2 && (era.get(`cflag:${target}:301`) <= 2 || era.get('flag:7') == 2)) { // :657
        await era.printAndWait(''); // :658
        // CFLAG:301  = 3（变量语义：CFLAG 族，301） // :659
        era.set(`cflag:${target}:301`, 3); // :659

      } else if (era.get(`mark:${target}:2`) <= 1 && (era.get(`cflag:${target}:301`) <= 1 || era.get('flag:7') == 2)) { // :661
        await era.printAndWait(''); // :662
        // CFLAG:301  = 2（变量语义：CFLAG 族，301） // :663
        era.set(`cflag:${target}:301`, 2); // :663
      } // :664
      return 0; // :665
    } // :666
  } // :667





  if (era_flag.selectcom == 1) { // :673

    if (era.get(`cflag:${target}:302`) == 0) { // :675

      if (era.get(`talent:${target}:0`) == 1) { // :677
        await era.printAndWait(''); // :678

      } else { // :680
        await era.printAndWait(''); // :681
      } // :682
      // CFLAG:302  = 1（变量语义：CFLAG 族，302） // :683
      era.set(`cflag:${target}:302`, 1); // :683
      return 0; // :684

    } else { // :686

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:302`) <= 4 || era.get('flag:7') == 2)) { // :688
        await era.printAndWait(''); // :689
        // CFLAG:302  = 5（变量语义：CFLAG 族，302） // :690
        era.set(`cflag:${target}:302`, 5); // :690

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:302`) <= 3 || era.get('flag:7') == 2)) { // :692
        await era.printAndWait(''); // :693
        // CFLAG:302  = 4（变量语义：CFLAG 族，302） // :694
        era.set(`cflag:${target}:302`, 4); // :694

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:302`) <= 2 || era.get('flag:7') == 2)) { // :696
        await era.printAndWait(''); // :697
        // CFLAG:302  = 3（变量语义：CFLAG 族，302） // :698
        era.set(`cflag:${target}:302`, 3); // :698

      } else if (era.get(`cflag:${target}:302`) <= 1 || era.get('flag:7') == 2) { // :700
        await era.printAndWait(''); // :701
        // CFLAG:302  = 2（变量语义：CFLAG 族，302） // :702
        era.set(`cflag:${target}:302`, 2); // :702
      } // :703
      return 0; // :704
    } // :705
  } // :706




  if (era_flag.selectcom == 2) { // :711

    if (era.get(`cflag:${target}:303`) == 0) { // :713
      await era.printAndWait(''); // :714
      // CFLAG:TARGET:303  = 1（变量语义：CFLAG 族，TARGET:303） // :715
      era.set(`cflag:${target}:303`, 1); // :715
      return 0; // :716

    } else { // :718
      // 赋值 P = PALAM:3 + UP:3 // :719

      if (era.get(`talent:${target}:76`) == 1 && P >= era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 6 || era.get('flag:7') == 2)) { // :721
        await era.printAndWait(''); // :722
        // CFLAG:303  = 7（变量语义：CFLAG 族，303） // :723
        era.set(`cflag:${target}:303`, 7); // :723

      } else if (era.get(`talent:${target}:76`) == 1 && P < era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 5 || era.get('flag:7') == 2)) { // :725
        await era.printAndWait(''); // :726
        // CFLAG:303  = 6（变量语义：CFLAG 族，303） // :727
        era.set(`cflag:${target}:303`, 6); // :727

      } else if (era.get(`talent:${target}:85`) == 1 && P >= era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 4 || era.get('flag:7') == 2)) { // :729
        await era.printAndWait(''); // :730
        // CFLAG:303  = 5（变量语义：CFLAG 族，303） // :731
        era.set(`cflag:${target}:303`, 5); // :731

      } else if (era.get(`talent:${target}:85`) == 1 && P < era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 3 || era.get('flag:7') == 2)) { // :733
        await era.printAndWait(''); // :734
        // CFLAG:303  = 4（变量语义：CFLAG 族，303） // :735
        era.set(`cflag:${target}:303`, 4); // :735

      } else if (P >= era.get('palamlv:2') && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:303`) <= 2 || era.get('flag:7') == 2)) { // :737
        await era.printAndWait(''); // :738
        // CFLAG:303  = 3（变量语义：CFLAG 族，303） // :739
        era.set(`cflag:${target}:303`, 3); // :739

      } else if (era.get(`cflag:${target}:223`) <= 1 || era.get('flag:7') == 2) { // :741
        await era.printAndWait(''); // :742
        // CFLAG:303  = 2（变量语义：CFLAG 族，303） // :743
        era.set(`cflag:${target}:303`, 2); // :743
      } // :744
      return 0; // :745
    } // :746
  } // :747




  if (era_flag.selectcom == 3) { // :752

    if (era.get(`cflag:${target}:304`) == 0) { // :754
      await era.printAndWait(''); // :755
      // CFLAG:TARGET:304  = 1（变量语义：CFLAG 族，TARGET:304） // :756
      era.set(`cflag:${target}:304`, 1); // :756
      return 0; // :757

    } else { // :759

      if (era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:304`) <= 8 || era.get('flag:7') == 2)) { // :761
        await era.printAndWait(''); // :762
        // CFLAG:304  = 9（变量语义：CFLAG 族，304） // :763
        era.set(`cflag:${target}:304`, 9); // :763

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:304`) <= 7 || era.get('flag:7') == 2)) { // :765

        if (rand_n(3) == 0) { // :767
          await era.printAndWait(''); // :768
        } else if (rand_n(2) == 0) { // :769
          await era.printAndWait(''); // :770
        } else { // :771
          await era.printAndWait(''); // :772
        } // :773
        // CFLAG:304  = 8（变量语义：CFLAG 族，304） // :774
        era.set(`cflag:${target}:304`, 8); // :774

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:31`) < 3 && (era.get(`cflag:${target}:304`) <= 6 || era.get('flag:7') == 2)) { // :776

        if (rand_n(2) == 0) { // :778
          await era.printAndWait(''); // :779
        } else { // :780
          await era.printAndWait(''); // :781
        } // :782
        // CFLAG:304  = 7（变量语义：CFLAG 族，304） // :783
        era.set(`cflag:${target}:304`, 7); // :783

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:304`) <= 5 || era.get('flag:7') == 2)) { // :785
        await era.printAndWait(''); // :786
        // CFLAG:304  = 6（变量语义：CFLAG 族，304） // :787
        era.set(`cflag:${target}:304`, 6); // :787

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:304`) <= 4 || era.get('flag:7') == 2)) { // :789

        if (rand_n(3) == 0) { // :791
          await era.printAndWait(''); // :792
        } else if (rand_n(2) == 0) { // :793
          await era.printAndWait(''); // :794
        } else { // :795
          await era.printAndWait(''); // :796
        } // :797
        // CFLAG:304  = 5（变量语义：CFLAG 族，304） // :798
        era.set(`cflag:${target}:304`, 5); // :798

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:31`) < 3 && (era.get(`cflag:${target}:304`) <= 3 || era.get('flag:7') == 2)) { // :800

        if (rand_n(2) == 0) { // :802
          await era.printAndWait(''); // :803
        } else { // :804
          await era.printAndWait(''); // :805
        } // :806
        // CFLAG:304  = 4（变量语义：CFLAG 族，304） // :807
        era.set(`cflag:${target}:304`, 4); // :807

      } else if (era.get(`mark:${target}:2`) == 3 &&era.get(`abl:${target}:31`) >= 1 && (era.get(`cflag:${target}:304`) <= 2 || era.get('flag:7') == 2)) { // :809

        if (rand_n(2) == 0) { // :811
          await era.printAndWait(''); // :812
        } else { // :813
          await era.printAndWait(''); // :814
        } // :815
        // CFLAG:304  = 3（变量语义：CFLAG 族，304） // :816
        era.set(`cflag:${target}:304`, 3); // :816

      } else if (era.get(`cflag:${target}:304`) <= 1 || era.get('flag:7') == 2) { // :818

        if (rand_n(2) == 0) { // :820
          await era.printAndWait(''); // :821
        } else { // :822
          await era.printAndWait(''); // :823
        } // :824
        // CFLAG:304  = 2（变量语义：CFLAG 族，304） // :825
        era.set(`cflag:${target}:304`, 2); // :825
      } // :826
      return 0; // :827
    } // :828
  } // :829





  if (era_flag.selectcom == 5) { // :835

    if (era.get(`cflag:${target}:306`) == 0) { // :837

      if (era.get(`talent:${target}:85`) == 1) { // :839
        await era.printAndWait(''); // :840

      } else { // :842
        await era.printAndWait(''); // :843
      } // :844
      // CFLAG:TARGET:306  = 1（变量语义：CFLAG 族，TARGET:306） // :845
      era.set(`cflag:${target}:306`, 1); // :845
      return 0; // :846

    } else { // :848

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:306`) <= 4 || era.get('flag:7') == 2)) { // :850
        await era.printAndWait(''); // :851
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :852
        era.set(`cflag:${target}:306`, 5); // :852

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:306`) <= 3 || era.get('flag:7') == 2)) { // :854
        await era.printAndWait(''); // :855
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :856
        era.set(`cflag:${target}:306`, 4); // :856

      } else if (era.get(`abl:${target}:1`) >= 3 && (era.get(`cflag:${target}:306`) <= 2 || era.get('flag:7') == 2)) { // :858
        await era.printAndWait(''); // :859
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :860
        era.set(`cflag:${target}:306`, 3); // :860

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :862
        await era.printAndWait(''); // :863
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :864
        era.set(`cflag:${target}:306`, 2); // :864
      } // :865
      return 0; // :866
    } // :867
  } // :868




  if (era_flag.selectcom == 6) { // :873

    if (era.get(`cflag:${target}:307`) == 0 && era.get('tflag:13')) { // :875

      if (era.get(`talent:${target}:76`) == 1 && era_flag.assiplay == 0 && era.get(`tequip:${target}:89`) == 0 && era.get(`tequip:${target}:90`) == 0) { // :877
        await era.printAndWait(''); // :878

      } else if (era.get(`talent:${target}:85`) == 1 && era_flag.assiplay == 0 && era.get(`tequip:${target}:89`) == 0 && era.get(`tequip:${target}:90`) == 0) { // :880
        await era.printAndWait(''); // :881

      } else { // :883
        await era.printAndWait(''); // :884
      } // :885
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :886
      era.set(`cflag:${target}:307`, 1); // :886
      return 0; // :887

    } else if (era.get(`cflag:${target}:307`) == 0) { // :889

      if (era.get(`talent:${target}:76`) == 1) { // :891
        await era.printAndWait(''); // :892

      } else if (era.get(`talent:${target}:85`) == 1) { // :894
        await era.printAndWait(''); // :895

      } else { // :897
        await era.printAndWait(''); // :898
      } // :899
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :900
      era.set(`cflag:${target}:307`, 1); // :900
      return 0; // :901

    } else { // :903

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:307`) <= 4 || era.get('flag:7') == 2)) { // :905
        await era.printAndWait(''); // :906
        // CFLAG:307  = 5（变量语义：CFLAG 族，307） // :907
        era.set(`cflag:${target}:307`, 5); // :907

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:307`) <= 3 || era.get('flag:7') == 2)) { // :909
        await era.printAndWait(''); // :910
        // CFLAG:307  = 4（变量语义：CFLAG 族，307） // :911
        era.set(`cflag:${target}:307`, 4); // :911

      } else if (era.get(`abl:${target}:10`) >=2 && (era.get(`cflag:${target}:307`) <= 2 || era.get('flag:7') == 2)) { // :913
        await era.printAndWait(''); // :914
        // CFLAG:307  = 3（变量语义：CFLAG 族，307） // :915
        era.set(`cflag:${target}:307`, 3); // :915

      } else if (era.get(`cflag:${target}:307`) <= 1 || era.get('flag:7') == 2) { // :917
        await era.printAndWait(''); // :918
        // CFLAG:307  = 2（变量语义：CFLAG 族，307） // :919
        era.set(`cflag:${target}:307`, 2); // :919
      } // :920
      return 0; // :921
    } // :922
  } // :923





  if (era_flag.selectcom == 7) { // :929

    if (era.get(`cflag:${target}:308`) == 0) { // :931

      if (era.get(`talent:${target}:76`) == 1) { // :933
        await era.printAndWait(''); // :934

      } else if (era.get(`talent:${target}:85`) == 1) { // :936
        await era.printAndWait(''); // :937

      } else { // :939
        await era.printAndWait(''); // :940
      } // :941
      // CFLAG:TARGET:308  = 1（变量语义：CFLAG 族，TARGET:308） // :942
      era.set(`cflag:${target}:308`, 1); // :942
      return 0; // :943

    } else { // :945

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:308`) <= 4 || era.get('flag:7') == 2)) { // :947
        await era.printAndWait(''); // :948
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :949
        era.set(`cflag:${target}:306`, 5); // :949

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:308`) <= 3 || era.get('flag:7') == 2)) { // :951
        await era.printAndWait(''); // :952
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :953
        era.set(`cflag:${target}:306`, 4); // :953

      } else if (era.get(`abl:${target}:17`) >= 3 && (era.get(`cflag:${target}:308`) <= 2 || era.get('flag:7') == 2)) { // :955
        await era.printAndWait(''); // :956
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :957
        era.set(`cflag:${target}:306`, 3); // :957

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :959
        await era.printAndWait(''); // :960
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :961
        era.set(`cflag:${target}:306`, 2); // :961
      } // :962
      return 0; // :963
    } // :964
  } // :965





  if (era_flag.selectcom == 8) { // :971

    if (era.get(`cflag:${target}:309`) == 0) { // :973

      if (era.get(`talent:${target}:76`) == 1) { // :975
        await era.printAndWait(''); // :976

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 1) { // :978
        await era.printAndWait(''); // :979

      } else { // :981
        await era.printAndWait(''); // :982
      } // :983
      // CFLAG:TARGET:309  = 1（变量语义：CFLAG 族，TARGET:309） // :984
      era.set(`cflag:${target}:309`, 1); // :984
      return 0; // :985

    } else { // :987

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:309`) <= 4 || era.get('flag:7') == 2)) { // :989
        await era.printAndWait(''); // :990
        // CFLAG:309  = 5（变量语义：CFLAG 族，309） // :991
        era.set(`cflag:${target}:309`, 5); // :991

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:309`) <= 3 || era.get('flag:7') == 2)) { // :993
        await era.printAndWait(''); // :994
        // CFLAG:309  = 4（变量语义：CFLAG 族，309） // :995
        era.set(`cflag:${target}:309`, 4); // :995

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:309`) <= 2 || era.get('flag:7') == 2)) { // :997
        await era.printAndWait(''); // :998
        // CFLAG:309  = 3（变量语义：CFLAG 族，309） // :999
        era.set(`cflag:${target}:309`, 3); // :999

      } else if (era.get(`cflag:${target}:309`) <= 1 || era.get('flag:7') == 2) { // :1001
        await era.printAndWait(''); // :1002
        // CFLAG:309  = 2（变量语义：CFLAG 族，309） // :1003
        era.set(`cflag:${target}:309`, 2); // :1003
      } // :1004
      return 0; // :1005
    } // :1006
  } // :1007




  if (era_flag.selectcom == 9) { // :1012

    if (era.get(`cflag:${target}:310`) == 0) { // :1014

      if (era.get(`talent:${target}:76`) == 1) { // :1016
        await era.printAndWait(''); // :1017

      } else if (era.get(`talent:${target}:85`) == 1) { // :1019
        await era.printAndWait(''); // :1020

      } else { // :1022
        await era.printAndWait(''); // :1023
      } // :1024
      // CFLAG:TARGET:310  = 1（变量语义：CFLAG 族，TARGET:310） // :1025
      era.set(`cflag:${target}:310`, 1); // :1025
      return 0; // :1026

    } else { // :1028

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:310`) <= 4 || era.get('flag:7') == 2)) { // :1030
        await era.printAndWait(''); // :1031
        // CFLAG:310  = 5（变量语义：CFLAG 族，310） // :1032
        era.set(`cflag:${target}:310`, 5); // :1032

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:310`) <= 3 || era.get('flag:7') == 2)) { // :1034
        await era.printAndWait(''); // :1035
        // CFLAG:310  = 4（变量语义：CFLAG 族，310） // :1036
        era.set(`cflag:${target}:310`, 4); // :1036

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:310`) <= 2 || era.get('flag:7') == 2)) { // :1038
        await era.printAndWait(''); // :1039
        // CFLAG:310  = 3（变量语义：CFLAG 族，310） // :1040
        era.set(`cflag:${target}:310`, 3); // :1040

      } else if (era.get(`cflag:${target}:310`) <= 1 || era.get('flag:7') == 2) { // :1042
        await era.printAndWait(''); // :1043
        // CFLAG:310  = 2（变量语义：CFLAG 族，310） // :1044
        era.set(`cflag:${target}:310`, 2); // :1044
      } // :1045
      return 0; // :1046
    } // :1047
  } // :1048




  if (era_flag.selectcom == 10) { // :1053

    if (era.get(`cflag:${target}:311`) == 0) { // :1055

      if (era.get(`talent:${target}:76`) == 1) { // :1057
        await era.printAndWait(''); // :1058

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 1) { // :1060
        await era.printAndWait(''); // :1061

      } else { // :1063
        await era.printAndWait(''); // :1064
      } // :1065
      // CFLAG:TARGET:311  = 1（变量语义：CFLAG 族，TARGET:311） // :1066
      era.set(`cflag:${target}:311`, 1); // :1066
      return 0; // :1067

    } else { // :1069

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:311`) <= 4 || era.get('flag:7') == 2)) { // :1071
        await era.printAndWait(''); // :1072
        // CFLAG:311  = 5（变量语义：CFLAG 族，311） // :1073
        era.set(`cflag:${target}:311`, 5); // :1073

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:311`) <= 3 || era.get('flag:7') == 2)) { // :1075
        await era.printAndWait(''); // :1076
        // CFLAG:311  = 4（变量语义：CFLAG 族，311） // :1077
        era.set(`cflag:${target}:311`, 4); // :1077

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:311`) <= 2 || era.get('flag:7') == 2)) { // :1079
        await era.printAndWait(''); // :1080
        // CFLAG:311  = 3（变量语义：CFLAG 族，311） // :1081
        era.set(`cflag:${target}:311`, 3); // :1081

      } else if (era.get(`cflag:${target}:311`) <= 1 || era.get('flag:7') == 2) { // :1083
        await era.printAndWait(''); // :1084
        // CFLAG:311  = 2（变量语义：CFLAG 族，311） // :1085
        era.set(`cflag:${target}:311`, 2); // :1085
      } // :1086
      return 0; // :1087
    } // :1088
  } // :1089






  if (era_flag.selectcom == 11 && era.get(`tequip:${target}:11`)) { // :1096

    if (era.get(`cflag:${target}:312`) == 0) { // :1098

      if (era.get(`talent:${target}:0`) == 1) { // :1100

        if (era.get(`talent:${target}:76`) == 1) { // :1102
          await era.printAndWait(''); // :1103

        } else if (era.get(`talent:${target}:85`) == 1) { // :1105
          await era.printAndWait(''); // :1106

        } else { // :1108
          await era.printAndWait(''); // :1109
        } // :1110

      } else { // :1112

        if (era.get(`talent:${target}:76`) == 1) { // :1114
          await era.printAndWait(''); // :1115

        } else if (era.get(`talent:${target}:85`) == 1) { // :1117
          await era.printAndWait(''); // :1118

        } else { // :1120
          await era.printAndWait(''); // :1121
        } // :1122
      } // :1123
      // CFLAG:312  = 1（变量语义：CFLAG 族，312） // :1124
      era.set(`cflag:${target}:312`, 1); // :1124
      return 0; // :1125

    } else { // :1127

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:312`) <= 4 || era.get('flag:7') == 2)) { // :1129
        await era.printAndWait(''); // :1130
        // CFLAG:312  = 5（变量语义：CFLAG 族，312） // :1131
        era.set(`cflag:${target}:312`, 5); // :1131

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:312`) <= 3 || era.get('flag:7') == 2)) { // :1133
        await era.printAndWait(''); // :1134
        // CFLAG:312  = 4（变量语义：CFLAG 族，312） // :1135
        era.set(`cflag:${target}:312`, 4); // :1135

      } else if (era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:312`) <= 2 || era.get('flag:7') == 2)) { // :1137
        await era.printAndWait(''); // :1138
        // CFLAG:312  = 3（变量语义：CFLAG 族，312） // :1139
        era.set(`cflag:${target}:312`, 3); // :1139

      } else if (era.get(`cflag:${target}:312`) <= 1 || era.get('flag:7') == 2) { // :1141
        await era.printAndWait(''); // :1142
        // CFLAG:312  = 2（变量语义：CFLAG 族，312） // :1143
        era.set(`cflag:${target}:312`, 2); // :1143
      } // :1144
      return 0; // :1145
    } // :1146

  } else if (era_flag.selectcom == 11 && era.get(`tequip:${target}:11`) == 0) { // :1148

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:372`) < 3 || era.get('flag:7') == 2)) { // :1150
      await era.printAndWait(''); // :1151
      // CFLAG:372  = 3（变量语义：CFLAG 族，372） // :1152
      era.set(`cflag:${target}:372`, 3); // :1152

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:372`) < 2 || era.get('flag:7') == 2)) { // :1154
      await era.printAndWait(''); // :1155
      // CFLAG:372  = 2（变量语义：CFLAG 族，372） // :1156
      era.set(`cflag:${target}:372`, 2); // :1156

    } else if (era.get(`cflag:${target}:372`) < 1 || era.get('flag:7') == 2) { // :1158
      await era.printAndWait(''); // :1159
      // CFLAG:372  = 1（变量语义：CFLAG 族，372） // :1160
      era.set(`cflag:${target}:372`, 1); // :1160
    } // :1161
    return 0; // :1162
  } // :1163




  if (era_flag.selectcom == 12) { // :1168

    if (era.get(`cflag:${target}:313`) == 0) { // :1170

      if (era.get(`talent:${target}:76`) == 1) { // :1172
        await era.printAndWait(''); // :1173

      } else if (era.get(`talent:${target}:85`) == 1) { // :1175
        await era.printAndWait(''); // :1176

      } else { // :1178
        await era.printAndWait(''); // :1179
      } // :1180
      // CFLAG:313  = 1（变量语义：CFLAG 族，313） // :1181
      era.set(`cflag:${target}:313`, 1); // :1181
      return 0; // :1182

    } else { // :1184

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:313`) <= 4 || era.get('flag:7') == 2)) { // :1186
        await era.printAndWait(''); // :1187
        // CFLAG:313  = 5（变量语义：CFLAG 族，313） // :1188
        era.set(`cflag:${target}:313`, 5); // :1188

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:313`) <= 3 || era.get('flag:7') == 2)) { // :1190
        await era.printAndWait(''); // :1191
        // CFLAG:313  = 4（变量语义：CFLAG 族，313） // :1192
        era.set(`cflag:${target}:313`, 4); // :1192

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:313`) <= 2 || era.get('flag:7') == 2)) { // :1194
        await era.printAndWait(''); // :1195
        // CFLAG:313  = 3（变量语义：CFLAG 族，313） // :1196
        era.set(`cflag:${target}:313`, 3); // :1196

      } else if (era.get(`cflag:${target}:313`) <= 1 || era.get('flag:7') == 2) { // :1198
        await era.printAndWait(''); // :1199
        // CFLAG:313  = 2（变量语义：CFLAG 族，313） // :1200
        era.set(`cflag:${target}:313`, 2); // :1200
      } // :1201
      return 0; // :1202
    } // :1203
  } // :1204





  if (era_flag.selectcom == 13 && era.get(`tequip:${target}:13`)) { // :1210

    if (era.get(`cflag:${target}:314`) == 0) { // :1212

      if (era.get(`talent:${target}:76`) == 1) { // :1214
        await era.printAndWait(''); // :1215

      } else if (era.get(`talent:${target}:85`) == 1) { // :1217
        await era.printAndWait(''); // :1218

      } else { // :1220
        await era.printAndWait(''); // :1221
      } // :1222
      // CFLAG:TARGET:314  = 1（变量语义：CFLAG 族，TARGET:314） // :1223
      era.set(`cflag:${target}:314`, 1); // :1223
      return 0; // :1224

    } else { // :1226

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 6 || era.get('flag:7') == 2)) { // :1228
        await era.printAndWait(''); // :1229
        // CFLAG:314  = 6（变量语义：CFLAG 族，314） // :1230
        era.set(`cflag:${target}:314`, 6); // :1230

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:314`) <= 5 || era.get('flag:7') == 2)) { // :1232
        await era.printAndWait(''); // :1233
        // CFLAG:314  = 6（变量语义：CFLAG 族，314） // :1234
        era.set(`cflag:${target}:314`, 6); // :1234

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 4 || era.get('flag:7') == 2)) { // :1236
        await era.printAndWait(''); // :1237
        // CFLAG:314  = 5（变量语义：CFLAG 族，314） // :1238
        era.set(`cflag:${target}:314`, 5); // :1238

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:314`) <= 3 || era.get('flag:7') == 2)) { // :1240
        await era.printAndWait(''); // :1241
        // CFLAG:314  = 4（变量语义：CFLAG 族，314） // :1242
        era.set(`cflag:${target}:314`, 4); // :1242

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 2 || era.get('flag:7') == 2)) { // :1244
        await era.printAndWait(''); // :1245
        // CFLAG:314  = 3（变量语义：CFLAG 族，314） // :1246
        era.set(`cflag:${target}:314`, 3); // :1246

      } else if (era.get(`cflag:${target}:314`) <= 1 || era.get('flag:7') == 2) { // :1248
        await era.printAndWait(''); // :1249
        // CFLAG:314  = 2（变量语义：CFLAG 族，314） // :1250
        era.set(`cflag:${target}:314`, 2); // :1250
      } // :1251
      return 0; // :1252
    } // :1253

  } else if (era_flag.selectcom == 13 && era.get(`tequip:${target}:13`) == 0) { // :1255

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:374`) < 4 || era.get('flag:7') == 2)) { // :1257
      await era.printAndWait(''); // :1258
      // CFLAG:374  = 4（变量语义：CFLAG 族，374） // :1259
      era.set(`cflag:${target}:374`, 4); // :1259

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:374`) < 3 || era.get('flag:7') == 2)) { // :1261
      await era.printAndWait(''); // :1262
      // CFLAG:374  = 3（变量语义：CFLAG 族，374） // :1263
      era.set(`cflag:${target}:374`, 3); // :1263

    } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:374`) < 2 || era.get('flag:7') == 2)) { // :1265
      await era.printAndWait(''); // :1266
      // CFLAG:374  = 2（变量语义：CFLAG 族，374） // :1267
      era.set(`cflag:${target}:374`, 2); // :1267

    } else if (era.get(`cflag:${target}:374`) < 1 || era.get('flag:7') == 2) { // :1269
      await era.printAndWait(''); // :1270
      // CFLAG:374  = 1（变量语义：CFLAG 族，374） // :1271
      era.set(`cflag:${target}:374`, 1); // :1271
    } // :1272
    return 0; // :1273
  } // :1274









































































































































































  if (era_flag.selectcom == 17 && era.get(`tequip:${target}:17`)) { // :1444

    if (era.get(`cflag:${target}:318`) == 0) { // :1446

      if (era.get(`talent:${target}:76`) == 1) { // :1448
        await era.printAndWait(''); // :1449

      } else if (era.get(`talent:${target}:85`) == 1) { // :1451
        await era.printAndWait(''); // :1452

      } else { // :1454
        await era.printAndWait(''); // :1455
      } // :1456
      // CFLAG:318  = 1（变量语义：CFLAG 族，318） // :1457
      era.set(`cflag:${target}:318`, 1); // :1457
      return 0; // :1458

    } else { // :1460

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:318`) <= 3 || era.get('flag:7') == 2)) { // :1462
        await era.printAndWait(''); // :1463
        // CFLAG:318  = 4（变量语义：CFLAG 族，318） // :1464
        era.set(`cflag:${target}:318`, 4); // :1464

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:318`) <= 2 || era.get('flag:7') == 2)) { // :1466
        await era.printAndWait(''); // :1467
        // CFLAG:318  = 3（变量语义：CFLAG 族，318） // :1468
        era.set(`cflag:${target}:318`, 3); // :1468

      } else if (era.get(`cflag:${target}:318`) <= 1 || era.get('flag:7') == 2) { // :1470
        await era.printAndWait(''); // :1471
        // CFLAG:318  = 2（变量语义：CFLAG 族，318） // :1472
        era.set(`cflag:${target}:318`, 2); // :1472
      } // :1473
      return 0; // :1474
    } // :1475

  } else if (era_flag.selectcom == 17 && era.get(`tequip:${target}:17`) == 0) { // :1477

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:378`) < 3 || era.get('flag:7') == 2)) { // :1479
      await era.printAndWait(''); // :1480
      // CFLAG:378  = 3（变量语义：CFLAG 族，378） // :1481
      era.set(`cflag:${target}:378`, 3); // :1481

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:378`) < 2 || era.get('flag:7') == 2)) { // :1483
      await era.printAndWait(''); // :1484
      // CFLAG:378  = 2（变量语义：CFLAG 族，378） // :1485
      era.set(`cflag:${target}:378`, 2); // :1485

    } else if (era.get(`cflag:${target}:378`) < 1 || era.get('flag:7') == 2) { // :1487
      await era.printAndWait(''); // :1488
      // CFLAG:378  = 1（变量语义：CFLAG 族，378） // :1489
      era.set(`cflag:${target}:378`, 1); // :1489
    } // :1490
    return 0; // :1491
  } // :1492





  if (era_flag.selectcom == 19 && era.get(`tequip:${target}:19`)) { // :1498

    if (era.get(`cflag:${target}:320`) == 0) { // :1500

      if (era.get(`talent:${target}:76`) == 1) { // :1502
        await era.printAndWait(''); // :1503

      } else if (era.get(`talent:${target}:85`) == 1) { // :1505
        await era.printAndWait(''); // :1506

      } else { // :1508
        await era.printAndWait(''); // :1509
      } // :1510
      // CFLAG:TARGET:320  = 1（变量语义：CFLAG 族，TARGET:320） // :1511
      era.set(`cflag:${target}:320`, 1); // :1511
      return 0; // :1512

    } else { // :1514

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 6 || era.get('flag:7') == 2)) { // :1516
        await era.printAndWait(''); // :1517
        // CFLAG:320  = 7（变量语义：CFLAG 族，320） // :1518
        era.set(`cflag:${target}:320`, 7); // :1518

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:320`) <= 5 || era.get('flag:7') == 2)) { // :1520
        await era.printAndWait(''); // :1521
        // CFLAG:320  = 6（变量语义：CFLAG 族，320） // :1522
        era.set(`cflag:${target}:320`, 6); // :1522

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 4 || era.get('flag:7') == 2)) { // :1524
        await era.printAndWait(''); // :1525
        // CFLAG:320  = 5（变量语义：CFLAG 族，320） // :1526
        era.set(`cflag:${target}:320`, 5); // :1526

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:320`) <= 3 || era.get('flag:7') == 2)) { // :1528
        await era.printAndWait(''); // :1529
        // CFLAG:320  = 4（变量语义：CFLAG 族，320） // :1530
        era.set(`cflag:${target}:320`, 4); // :1530

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 2 || era.get('flag:7') == 2)) { // :1532
        await era.printAndWait(''); // :1533
        // CFLAG:320  = 3（变量语义：CFLAG 族，320） // :1534
        era.set(`cflag:${target}:320`, 3); // :1534

      } else if (era.get(`cflag:${target}:320`) <= 1 || era.get('flag:7') == 2) { // :1536
        await era.printAndWait(''); // :1537
        // CFLAG:320  = 2（变量语义：CFLAG 族，320） // :1538
        era.set(`cflag:${target}:320`, 2); // :1538
      } // :1539
      return 0; // :1540
    } // :1541

  } else if (era_flag.selectcom == 19 && era.get(`tequip:${target}:19`) == 0) { // :1543

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:379`) < 4 || era.get('flag:7') == 2)) { // :1545
      await era.printAndWait(''); // :1546
      // CFLAG:379  = 4（变量语义：CFLAG 族，379） // :1547
      era.set(`cflag:${target}:379`, 4); // :1547

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:379`) < 3 || era.get('flag:7') == 2)) { // :1549
      await era.printAndWait(''); // :1550
      // CFLAG:379  = 3（变量语义：CFLAG 族，379） // :1551
      era.set(`cflag:${target}:379`, 3); // :1551

    } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:379`) < 2 || era.get('flag:7') == 2)) { // :1553
      await era.printAndWait(''); // :1554
      // CFLAG:379  = 2（变量语义：CFLAG 族，379） // :1555
      era.set(`cflag:${target}:379`, 2); // :1555

    } else if (era.get(`cflag:${target}:379`) < 1 || era.get('flag:7') == 2) { // :1557
      await era.printAndWait(''); // :1558
      // CFLAG:379  = 1（变量语义：CFLAG 族，379） // :1559
      era.set(`cflag:${target}:379`, 1); // :1559
    } // :1560
    return 0; // :1561
  } // :1562





  if (era_flag.selectcom == 20) { // :1568

    if (era.get(`cflag:${target}:321`) == 0) { // :1570

      if (era.get(`talent:${target}:0`) == 1) { // :1572

        if (era.get(`talent:${target}:76`) == 1) { // :1574
          await era.printAndWait(''); // :1575

        } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:10`) >= 5) { // :1577
          await era.printAndWait(''); // :1578

        } else { // :1580
          await era.printAndWait(''); // :1581
        } // :1582

      } else { // :1584
        if (era.get(`talent:${target}:76`) == 1) { // :1585
          await era.printAndWait(''); // :1586

        } else if (era.get(`talent:${target}:85`) == 1) { // :1588
          await era.printAndWait(''); // :1589

        } else { // :1591
          await era.printAndWait(''); // :1592
        } // :1593
      } // :1594
      // CFLAG:321  = 1（变量语义：CFLAG 族，321） // :1595
      era.set(`cflag:${target}:321`, 1); // :1595
      return 0; // :1596

    } else { // :1598

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:321`) <= 5 || era.get('flag:7') == 2)) { // :1600
        await era.printAndWait(''); // :1601
        // CFLAG:321  = 6（变量语义：CFLAG 族，321） // :1602
        era.set(`cflag:${target}:321`, 6); // :1602

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:321`) <= 4 || era.get('flag:7') == 2)) { // :1604
        await era.printAndWait(''); // :1605
        // CFLAG:321  = 5（变量语义：CFLAG 族，321） // :1606
        era.set(`cflag:${target}:321`, 5); // :1606

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:321`) <= 3 || era.get('flag:7') == 2)) { // :1608
        await era.printAndWait(''); // :1609
        // CFLAG:321  = 4（变量语义：CFLAG 族，321） // :1610
        era.set(`cflag:${target}:321`, 4); // :1610

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:321`) <= 2 || era.get('flag:7') == 2)) { // :1612
        await era.printAndWait(''); // :1613
        // CFLAG:321  = 3（变量语义：CFLAG 族，321） // :1614
        era.set(`cflag:${target}:321`, 3); // :1614

      } else if (era.get(`cflag:${target}:321`) <= 1 || era.get('flag:7') == 2) { // :1616
        await era.printAndWait(''); // :1617
        // CFLAG:321  = 2（变量语义：CFLAG 族，321） // :1618
        era.set(`cflag:${target}:321`, 2); // :1618
      } // :1619
      return 0; // :1620
    } // :1621
  } // :1622





  if (era_flag.selectcom == 21) { // :1628

    if (era.get(`cflag:${target}:322`) == 0) { // :1630

      if (era.get(`talent:${target}:0`) == 1) { // :1632

        if (era.get(`talent:${target}:76`) == 1) { // :1634
          await era.printAndWait(''); // :1635

        } else if (era.get(`talent:${target}:85`) == 1) { // :1637
          await era.printAndWait(''); // :1638


        } else { // :1641
          await era.printAndWait(''); // :1642
        } // :1643

      } else { // :1645

        if (era.get(`talent:${target}:76`) == 1) { // :1647
          await era.printAndWait(''); // :1648

        } else if (era.get(`talent:${target}:85`) == 1) { // :1650
          await era.printAndWait(''); // :1651

        } else { // :1653
          await era.printAndWait(''); // :1654
        } // :1655
      } // :1656
      // CFLAG:322  = 1（变量语义：CFLAG 族，322） // :1657
      era.set(`cflag:${target}:322`, 1); // :1657
      return 0; // :1658

    } else { // :1660

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:322`) <= 5 || era.get('flag:7') == 2)) { // :1662
        if (rand_n(3) == 0) { // :1663
          await era.printAndWait(''); // :1664
        } else if (rand_n(2) == 0) { // :1665
          await era.printAndWait(''); // :1666
        } else { // :1667
          await era.printAndWait(''); // :1668
        } // :1669
        // CFLAG:322  = 6（变量语义：CFLAG 族，322） // :1670
        era.set(`cflag:${target}:322`, 6); // :1670

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:322`) <= 4 || era.get('flag:7') == 2)) { // :1672
        if (rand_n(3) == 0) { // :1673
          await era.printAndWait(''); // :1674
        } else if (rand_n(2) == 0) { // :1675
          await era.printAndWait(''); // :1676
        } else { // :1677
          await era.printAndWait(''); // :1678
        } // :1679
        // CFLAG:322  = 5（变量语义：CFLAG 族，322） // :1680
        era.set(`cflag:${target}:322`, 5); // :1680

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:322`) <= 3 || era.get('flag:7') == 2)) { // :1682
        await era.printAndWait(''); // :1683
        // CFLAG:322  = 4（变量语义：CFLAG 族，322） // :1684
        era.set(`cflag:${target}:322`, 4); // :1684

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:322`) <= 2 || era.get('flag:7') == 2)) { // :1686
        await era.printAndWait(''); // :1687
        // CFLAG:322  = 3（变量语义：CFLAG 族，322） // :1688
        era.set(`cflag:${target}:322`, 3); // :1688

      } else if (era.get(`cflag:${target}:322`) <= 1 || era.get('flag:7') == 2) { // :1690
        await era.printAndWait(''); // :1691

        // CFLAG:322  = 2（变量语义：CFLAG 族，322） // :1693
        era.set(`cflag:${target}:322`, 2); // :1693
      } // :1694
      return 0; // :1695
    } // :1696
  } // :1697





  if (era_flag.selectcom == 22) { // :1703
    if (era.get(`cflag:${target}:323`) == 0) { // :1704

      if (era.get(`talent:${target}:0`) == 1) { // :1706

        if (era.get(`talent:${target}:76`) == 1) { // :1708
          await era.printAndWait(''); // :1709

        } else if (era.get(`talent:${target}:85`) == 1) { // :1711
          await era.printAndWait(''); // :1712

        } else { // :1714
          await era.printAndWait(''); // :1715
        } // :1716

      } else { // :1718

        if (era.get(`talent:${target}:76`) == 1) { // :1720
          await era.printAndWait(''); // :1721

        } else if (era.get(`talent:${target}:85`) == 1) { // :1723
          await era.printAndWait(''); // :1724

        } else { // :1726
          await era.printAndWait(''); // :1727
        } // :1728
      } // :1729
      // CFLAG:323  = 1（变量语义：CFLAG 族，323） // :1730
      era.set(`cflag:${target}:323`, 1); // :1730
      return 0; // :1731

    } else { // :1733

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:323`) <= 5 || era.get('flag:7') == 2)) { // :1735
        if (rand_n(3) == 0) { // :1736
          await era.printAndWait(''); // :1737
        } else if (rand_n(2) == 0) { // :1738
          await era.printAndWait(''); // :1739
        } else { // :1740
          await era.printAndWait(''); // :1741
        } // :1742
        // CFLAG:323  = 6（变量语义：CFLAG 族，323） // :1743
        era.set(`cflag:${target}:323`, 6); // :1743

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:323`) <= 4 || era.get('flag:7') == 2)) { // :1745
        if (rand_n(3) == 0) { // :1746
          await era.printAndWait(''); // :1747
        } else if (rand_n(2) == 0) { // :1748
          await era.printAndWait(''); // :1749
        } else { // :1750
          await era.printAndWait(''); // :1751
        } // :1752
        // CFLAG:323  = 5（变量语义：CFLAG 族，323） // :1753
        era.set(`cflag:${target}:323`, 5); // :1753

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:323`) <= 3 || era.get('flag:7') == 2)) { // :1755
        await era.printAndWait(''); // :1756
        // CFLAG:323  = 4（变量语义：CFLAG 族，323） // :1757
        era.set(`cflag:${target}:323`, 4); // :1757

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:323`) <= 2 || era.get('flag:7') == 2)) { // :1759
        await era.printAndWait(''); // :1760
        // CFLAG:323  = 3（变量语义：CFLAG 族，323） // :1761
        era.set(`cflag:${target}:323`, 3); // :1761

      } else if (era.get(`cflag:${target}:323`) <= 1 || era.get('flag:7') == 2) { // :1763
        await era.printAndWait(''); // :1764
        // CFLAG:323  = 2（变量语义：CFLAG 族，323） // :1765
        era.set(`cflag:${target}:323`, 2); // :1765
      } // :1766
      return 0; // :1767
    } // :1768
  } // :1769





  if (era_flag.selectcom == 23) { // :1775
    if (era.get(`cflag:${target}:324`) == 0) { // :1776

      if (era.get(`talent:${target}:0`) == 1) { // :1778

        if (era.get(`talent:${target}:76`) == 1) { // :1780
          await era.printAndWait(''); // :1781

        } else if (era.get(`talent:${target}:85`) == 1) { // :1783
          await era.printAndWait(''); // :1784

        } else { // :1786
          await era.printAndWait(''); // :1787
        } // :1788

      } else { // :1790

        if (era.get(`talent:${target}:76`) == 1) { // :1792
          await era.printAndWait(''); // :1793

        } else if (era.get(`talent:${target}:85`) == 1) { // :1795
          await era.printAndWait(''); // :1796

        } else { // :1798
          await era.printAndWait(''); // :1799
        } // :1800
      } // :1801
      // CFLAG:324  = 1（变量语义：CFLAG 族，324） // :1802
      era.set(`cflag:${target}:324`, 1); // :1802
      return 0; // :1803

    } else { // :1805

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:324`) <= 5 || era.get('flag:7') == 2)) { // :1807
        if (rand_n(3) == 0) { // :1808
          await era.printAndWait(''); // :1809
        } else if (rand_n(2) == 0) { // :1810
          await era.printAndWait(''); // :1811
        } else { // :1812
          await era.printAndWait(''); // :1813
        } // :1814
        // CFLAG:324  = 6（变量语义：CFLAG 族，324） // :1815
        era.set(`cflag:${target}:324`, 6); // :1815

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:324`) <= 4 || era.get('flag:7') == 2)) { // :1817
        if (rand_n(3) == 0) { // :1818
          await era.printAndWait(''); // :1819
        } else if (rand_n(2) == 0) { // :1820
          await era.printAndWait(''); // :1821
        } else { // :1822
          await era.printAndWait(''); // :1823
        } // :1824
        // CFLAG:324  = 5（变量语义：CFLAG 族，324） // :1825
        era.set(`cflag:${target}:324`, 5); // :1825

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:324`) <= 3 || era.get('flag:7') == 2)) { // :1827
        await era.printAndWait(''); // :1828
        // CFLAG:324  = 4（变量语义：CFLAG 族，324） // :1829
        era.set(`cflag:${target}:324`, 4); // :1829

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:324`) <= 2 || era.get('flag:7') == 2)) { // :1831
        await era.printAndWait(''); // :1832
        // CFLAG:324  = 3（变量语义：CFLAG 族，324） // :1833
        era.set(`cflag:${target}:324`, 3); // :1833

      } else if (era.get(`cflag:${target}:324`) <= 1 || era.get('flag:7') == 2) { // :1835
        await era.printAndWait(''); // :1836
        // CFLAG:324  = 2（变量语义：CFLAG 族，324） // :1837
        era.set(`cflag:${target}:324`, 2); // :1837
      } // :1838
      return 0; // :1839
    } // :1840
  } // :1841




  if (era_flag.selectcom == 26) { // :1846

    if (era.get(`cflag:${target}:327`) == 0) { // :1848

      if (era.get(`talent:${target}:76`) == 1) { // :1850
        await era.printAndWait(''); // :1851

      } else if (era.get(`talent:${target}:85`) == 1) { // :1853
        await era.printAndWait(''); // :1854

      } else { // :1856
        await era.printAndWait(''); // :1857
      } // :1858
      // CFLAG:TARGET:327  = 1（变量语义：CFLAG 族，TARGET:327） // :1859
      era.set(`cflag:${target}:327`, 1); // :1859
      return 0; // :1860

    } else { // :1862

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 6 || era.get('flag:7') == 2)) { // :1864
        if (rand_n(3) == 0) { // :1865
          await era.printAndWait(''); // :1866
        } else if (rand_n(2) == 0) { // :1867
          await era.printAndWait(''); // :1868
        } else { // :1869
          await era.printAndWait(''); // :1870
        } // :1871
        // CFLAG:327  = 7（变量语义：CFLAG 族，327） // :1872
        era.set(`cflag:${target}:327`, 7); // :1872

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:327`) <= 5 || era.get('flag:7') == 2)) { // :1874
        await era.printAndWait(''); // :1875
        // CFLAG:327  = 6（变量语义：CFLAG 族，327） // :1876
        era.set(`cflag:${target}:327`, 6); // :1876

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 4 || era.get('flag:7') == 2)) { // :1878
        if (rand_n(2) == 0) { // :1879
          await era.printAndWait(''); // :1880
        } else { // :1881
          await era.printAndWait(''); // :1882
        } // :1883
        // CFLAG:327  = 5（变量语义：CFLAG 族，327） // :1884
        era.set(`cflag:${target}:327`, 5); // :1884

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:327`) <= 3 || era.get('flag:7') == 2)) { // :1886
        await era.printAndWait(''); // :1887
        // CFLAG:327  = 4（变量语义：CFLAG 族，327） // :1888
        era.set(`cflag:${target}:327`, 4); // :1888

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 2 || era.get('flag:7') == 2)) { // :1890
        await era.printAndWait(''); // :1891
        // CFLAG:327  = 3（变量语义：CFLAG 族，327） // :1892
        era.set(`cflag:${target}:327`, 3); // :1892

      } else if (era.get(`cflag:${target}:327`) <= 1 || era.get('flag:7') == 2) { // :1894
        await era.printAndWait(''); // :1895
        // CFLAG:327  = 2（变量语义：CFLAG 族，327） // :1896
        era.set(`cflag:${target}:327`, 2); // :1896
      } // :1897
      return 0; // :1898
    } // :1899
  } // :1900




  if (era_flag.selectcom == 27) { // :1905

    if (era.get(`cflag:${target}:328`) == 0) { // :1907

      if (era.get(`talent:${target}:76`) == 1) { // :1909
        await era.printAndWait(''); // :1910

      } else if (era.get(`talent:${target}:85`) == 1) { // :1912
        await era.printAndWait(''); // :1913

      } else { // :1915
        await era.printAndWait(''); // :1916
      } // :1917
      // CFLAG:TARGET:328  = 1（变量语义：CFLAG 族，TARGET:328） // :1918
      era.set(`cflag:${target}:328`, 1); // :1918
      return 0; // :1919

    } else { // :1921

      if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 4 || era.get('flag:7') == 2)) { // :1923
        if (rand_n(2) == 0) { // :1924
          await era.printAndWait(''); // :1925
        } else { // :1926
          await era.printAndWait(''); // :1927
        } // :1928
        // CFLAG:328  = 5（变量语义：CFLAG 族，328） // :1929
        era.set(`cflag:${target}:328`, 5); // :1929

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:328`) <= 3 || era.get('flag:7') == 2)) { // :1931
        await era.printAndWait(''); // :1932
        // CFLAG:328  = 4（变量语义：CFLAG 族，328） // :1933
        era.set(`cflag:${target}:328`, 4); // :1933

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 2 || era.get('flag:7') == 2)) { // :1935
        await era.printAndWait(''); // :1936
        // CFLAG:328  = 3（变量语义：CFLAG 族，328） // :1937
        era.set(`cflag:${target}:328`, 3); // :1937

      } else if (era.get(`cflag:${target}:328`) <= 1 || era.get('flag:7') == 2) { // :1939
        await era.printAndWait(''); // :1940
        // CFLAG:328  = 2（变量语义：CFLAG 族，328） // :1941
        era.set(`cflag:${target}:328`, 2); // :1941
      } // :1942
      return 0; // :1943
    } // :1944
  } // :1945




  if (era_flag.selectcom == 28) { // :1950

    if (era.get(`cflag:${target}:329`) == 0) { // :1952

      if (era.get(`talent:${target}:76`) == 1) { // :1954
        await era.printAndWait(''); // :1955

      } else if (era.get(`talent:${target}:85`) == 1) { // :1957
        await era.printAndWait(''); // :1958

      } else { // :1960
        await era.printAndWait(''); // :1961
      } // :1962
      // CFLAG:TARGET:329  = 1（变量语义：CFLAG 族，TARGET:329） // :1963
      era.set(`cflag:${target}:329`, 1); // :1963
      return 0; // :1964

    } else { // :1966

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 6 || era.get('flag:7') == 2)) { // :1968
        if (rand_n(3) == 0) { // :1969
          await era.printAndWait(''); // :1970
        } else if (rand_n(2) == 0) { // :1971
          await era.printAndWait(''); // :1972
        } else { // :1973
          await era.printAndWait(''); // :1974
        } // :1975
        // CFLAG:329  = 7（变量语义：CFLAG 族，329） // :1976
        era.set(`cflag:${target}:329`, 7); // :1976

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:329`) <= 5 || era.get('flag:7') == 2)) { // :1978
        await era.printAndWait(''); // :1979
        // CFLAG:329  = 6（变量语义：CFLAG 族，329） // :1980
        era.set(`cflag:${target}:329`, 6); // :1980

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 4 || era.get('flag:7') == 2)) { // :1982
        if (rand_n(2) == 0) { // :1983
          await era.printAndWait(''); // :1984
        } else { // :1985
          await era.printAndWait(''); // :1986
        } // :1987
        // CFLAG:329  = 5（变量语义：CFLAG 族，329） // :1988
        era.set(`cflag:${target}:329`, 5); // :1988

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:329`) <= 3 || era.get('flag:7') == 2)) { // :1990
        await era.printAndWait(''); // :1991
        // CFLAG:329  = 4（变量语义：CFLAG 族，329） // :1992
        era.set(`cflag:${target}:329`, 4); // :1992

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 2 || era.get('flag:7') == 2)) { // :1994
        await era.printAndWait(''); // :1995
        // CFLAG:329  = 3（变量语义：CFLAG 族，329） // :1996
        era.set(`cflag:${target}:329`, 3); // :1996

      } else if (era.get(`cflag:${target}:329`) <= 1 || era.get('flag:7') == 2) { // :1998
        await era.printAndWait(''); // :1999
        // CFLAG:329  = 2（变量语义：CFLAG 族，329） // :2000
        era.set(`cflag:${target}:329`, 2); // :2000
      } // :2001
      return 0; // :2002
    } // :2003
  } // :2004




  if (era_flag.selectcom == 29) { // :2009

    if (era.get(`cflag:${target}:330`) == 0) { // :2011

      if (era.get(`talent:${target}:76`) == 1) { // :2013
        await era.printAndWait(''); // :2014

      } else if (era.get(`talent:${target}:85`) == 1) { // :2016
        await era.printAndWait(''); // :2017

      } else { // :2019
        await era.printAndWait(''); // :2020
      } // :2021
      // CFLAG:TARGET:330  = 1（变量语义：CFLAG 族，TARGET:330） // :2022
      era.set(`cflag:${target}:330`, 1); // :2022
      return 0; // :2023

    } else { // :2025

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 6 || era.get('flag:7') == 2)) { // :2027
        if (rand_n(2) == 0) { // :2028
          await era.printAndWait(''); // :2029
        } else { // :2030
          await era.printAndWait(''); // :2031
        } // :2032
        // CFLAG:330  = 7（变量语义：CFLAG 族，330） // :2033
        era.set(`cflag:${target}:330`, 7); // :2033

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:330`) <= 5 || era.get('flag:7') == 2)) { // :2035
        await era.printAndWait(''); // :2036
        // CFLAG:330  = 6（变量语义：CFLAG 族，330） // :2037
        era.set(`cflag:${target}:330`, 6); // :2037

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 4 || era.get('flag:7') == 2)) { // :2039
        if (rand_n(2) == 0) { // :2040
          await era.printAndWait(''); // :2041
        } else { // :2042
          await era.printAndWait(''); // :2043
        } // :2044
        // CFLAG:330  = 5（变量语义：CFLAG 族，330） // :2045
        era.set(`cflag:${target}:330`, 5); // :2045

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:330`) <= 3 || era.get('flag:7') == 2)) { // :2047
        await era.printAndWait(''); // :2048
        // CFLAG:330  = 4（变量语义：CFLAG 族，330） // :2049
        era.set(`cflag:${target}:330`, 4); // :2049

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 2 || era.get('flag:7') == 2)) { // :2051
        await era.printAndWait(''); // :2052
        // CFLAG:330  = 3（变量语义：CFLAG 族，330） // :2053
        era.set(`cflag:${target}:330`, 3); // :2053

      } else if (era.get(`cflag:${target}:330`) <= 1 || era.get('flag:7') == 2) { // :2055
        await era.printAndWait(''); // :2056
        // CFLAG:330  = 2（变量语义：CFLAG 族，330） // :2057
        era.set(`cflag:${target}:330`, 2); // :2057
      } // :2058
      return 0; // :2059
    } // :2060
  } // :2061




  if (era_flag.selectcom == 30) { // :2066

    if (era.get(`cflag:${target}:331`) == 0) { // :2068

      if (era.get(`talent:${target}:76`) == 1) { // :2070
        await era.printAndWait(''); // :2071

      } else if (era.get(`talent:${target}:85`) == 1) { // :2073
        await era.printAndWait(''); // :2074

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2076
        await era.printAndWait(''); // :2077

      } else { // :2079
        await era.printAndWait(''); // :2080
      } // :2081
      // CFLAG:TARGET:331  = 1（变量语义：CFLAG 族，TARGET:331） // :2082
      era.set(`cflag:${target}:331`, 1); // :2082
      return 0; // :2083

    } else { // :2085

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 5 || era.get('flag:7') == 2)) { // :2087
        if (rand_n(2) == 0) { // :2088
          await era.printAndWait(''); // :2089
        } else { // :2090
          await era.printAndWait(''); // :2091
        } // :2092
        // CFLAG:331  = 6（变量语义：CFLAG 族，331） // :2093
        era.set(`cflag:${target}:331`, 6); // :2093

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:331`) <= 4 || era.get('flag:7') == 2)) { // :2095
        if (rand_n(2) == 0) { // :2096
          await era.printAndWait(''); // :2097
        } else { // :2098
          await era.printAndWait(''); // :2099
        } // :2100
        // CFLAG:331  = 5（变量语义：CFLAG 族，331） // :2101
        era.set(`cflag:${target}:331`, 5); // :2101

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 3 || era.get('flag:7') == 2)) { // :2103
        await era.printAndWait(''); // :2104
        // CFLAG:331  = 4（变量语义：CFLAG 族，331） // :2105
        era.set(`cflag:${target}:331`, 4); // :2105

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 2 || era.get('flag:7') == 2)) { // :2107
        await era.printAndWait(''); // :2108
        // CFLAG:331  = 3（变量语义：CFLAG 族，331） // :2109
        era.set(`cflag:${target}:331`, 3); // :2109

      } else if (era.get(`cflag:${target}:331`) <= 1 || era.get('flag:7') == 2) { // :2111
        await era.printAndWait(''); // :2112
        // CFLAG:331  = 2（变量语义：CFLAG 族，331） // :2113
        era.set(`cflag:${target}:331`, 2); // :2113
      } // :2114
      return 0; // :2115
    } // :2116
  } // :2117




  if (era_flag.selectcom == 31) { // :2122

    if (era.get(`cflag:${target}:332`) == 0) { // :2124

      if (era.get(`talent:${target}:76`) == 1) { // :2126
        await era.printAndWait(''); // :2127

      } else if (era.get(`talent:${target}:85`) == 1) { // :2129
        await era.printAndWait(''); // :2130

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2132
        await era.printAndWait(''); // :2133

      } else { // :2135
        await era.printAndWait(''); // :2136
      } // :2137
      // CFLAG:TARGET:332  = 1（变量语义：CFLAG 族，TARGET:332） // :2138
      era.set(`cflag:${target}:332`, 1); // :2138
      return 0; // :2139

    } else { // :2141

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2143
        await era.printAndWait(''); // :2144
        // CFLAG:332  = 6（变量语义：CFLAG 族，332） // :2145
        era.set(`cflag:${target}:332`, 6); // :2145

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2147
        await era.printAndWait(''); // :2148
        // CFLAG:332  = 5（变量语义：CFLAG 族，332） // :2149
        era.set(`cflag:${target}:332`, 5); // :2149

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2151
        await era.print(''); // :2152
        await era.printAndWait(''); // :2153
        // CFLAG:332  = 4（变量语义：CFLAG 族，332） // :2154
        era.set(`cflag:${target}:332`, 4); // :2154

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:332`) <= 2 || era.get('flag:7') == 2)) { // :2156
        await era.print(''); // :2157
        await era.printAndWait(''); // :2158
        // CFLAG:332  = 3（变量语义：CFLAG 族，332） // :2159
        era.set(`cflag:${target}:332`, 3); // :2159

      } else if (era.get(`cflag:${target}:332`) <= 1 || era.get('flag:7') == 2) { // :2161
        await era.printAndWait(''); // :2162
        // CFLAG:332  = 2（变量语义：CFLAG 族，332） // :2163
        era.set(`cflag:${target}:332`, 2); // :2163
      } // :2164
      return 0; // :2165
    } // :2166
  } // :2167





  if (era_flag.selectcom == 32) { // :2173

    if (era.get(`cflag:${target}:333`) == 0) { // :2175

      if (era.get(`talent:${target}:76`) == 1) { // :2177
        await era.printAndWait(''); // :2178

      } else if (era.get(`talent:${target}:85`) == 1) { // :2180
        await era.printAndWait(''); // :2181

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2183
        await era.printAndWait(''); // :2184

      } else { // :2186
        await era.printAndWait(''); // :2187
      } // :2188
      // CFLAG:TARGET:333  = 1（变量语义：CFLAG 族，TARGET:333） // :2189
      era.set(`cflag:${target}:333`, 1); // :2189
      return 0; // :2190

    } else { // :2192

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 5 || era.get('flag:7') == 2)) { // :2194
        if (rand_n(2) == 0) { // :2195
          await era.printAndWait(''); // :2196
        } else { // :2197
          await era.printAndWait(''); // :2198
        } // :2199
        // CFLAG:333  = 6（变量语义：CFLAG 族，333） // :2200
        era.set(`cflag:${target}:333`, 6); // :2200

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 4 || era.get('flag:7') == 2)) { // :2202
        await era.printAndWait(''); // :2203
        // CFLAG:333  = 5（变量语义：CFLAG 族，333） // :2204
        era.set(`cflag:${target}:333`, 5); // :2204

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:333`) <= 3 || era.get('flag:7') == 2)) { // :2206
        if (rand_n(2) == 0) { // :2207
          await era.printAndWait(''); // :2208
        } else { // :2209
          await era.printAndWait(''); // :2210
        } // :2211
        // CFLAG:333  = 4（变量语义：CFLAG 族，333） // :2212
        era.set(`cflag:${target}:333`, 4); // :2212

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:333`) <= 2 || era.get('flag:7') == 2)) { // :2214
        await era.printAndWait(''); // :2215
        // CFLAG:333  = 3（变量语义：CFLAG 族，333） // :2216
        era.set(`cflag:${target}:333`, 3); // :2216

      } else if (era.get(`cflag:${target}:333`) <= 1 || era.get('flag:7') == 2) { // :2218
        await era.printAndWait(''); // :2219
        // CFLAG:333  = 2（变量语义：CFLAG 族，333） // :2220
        era.set(`cflag:${target}:333`, 2); // :2220
      } // :2221
      return 0; // :2222
    } // :2223
  } // :2224




  if (era_flag.selectcom == 33) { // :2229

    if (era.get(`cflag:${target}:334`) == 0) { // :2231

      if (era.get(`talent:${target}:76`) == 1) { // :2233
        await era.printAndWait(''); // :2234

      } else if (era.get(`talent:${target}:85`) == 1) { // :2236
        await era.printAndWait(''); // :2237

      } else { // :2239
        await era.printAndWait(''); // :2240
      } // :2241
      // CFLAG:TARGET:334  = 1（变量语义：CFLAG 族，TARGET:334） // :2242
      era.set(`cflag:${target}:334`, 1); // :2242
      return 0; // :2243

    } else { // :2245

      if (era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:334`) <= 5 || era.get('flag:7') == 2)) { // :2247
        await era.printAndWait(''); // :2248
        // CFLAG:334  = 6（变量语义：CFLAG 族，334） // :2249
        era.set(`cflag:${target}:334`, 6); // :2249

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:334`) <= 4 || era.get('flag:7') == 2)) { // :2251
        await era.printAndWait(''); // :2252
        // CFLAG:334  = 5（变量语义：CFLAG 族，334） // :2253
        era.set(`cflag:${target}:334`, 5); // :2253

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:334`) <= 3 || era.get('flag:7') == 2)) { // :2255
        await era.printAndWait(''); // :2256
        await era.printAndWait(''); // :2257
        // CFLAG:334  = 4（变量语义：CFLAG 族，334） // :2258
        era.set(`cflag:${target}:334`, 4); // :2258

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:334`) <= 2 || era.get('flag:7') == 2)) { // :2260
        await era.printAndWait(''); // :2261
        await era.printAndWait(''); // :2262
        // CFLAG:334  = 3（变量语义：CFLAG 族，334） // :2263
        era.set(`cflag:${target}:334`, 3); // :2263

      } else if (era.get(`cflag:${target}:334`) <= 1 || era.get('flag:7') == 2) { // :2265
        await era.printAndWait(''); // :2266
        // CFLAG:334  = 2（变量语义：CFLAG 族，334） // :2267
        era.set(`cflag:${target}:334`, 2); // :2267
      } // :2268
      return 0; // :2269
    } // :2270
  } // :2271





  if (era_flag.selectcom == 34) { // :2277

    if (era.get(`cflag:${target}:335`) == 0) { // :2279

      if (era.get(`talent:${target}:0`) == 1) { // :2281

        if (era.get(`talent:${target}:76`) == 1) { // :2283
          await era.printAndWait(''); // :2284

        } else if (era.get(`talent:${target}:85`) == 1) { // :2286
          await era.printAndWait(''); // :2287

        } else { // :2289
          await era.printAndWait(''); // :2290
        } // :2291

      } else { // :2293

        if (era.get(`talent:${target}:76`) == 1) { // :2295
          await era.printAndWait(''); // :2296

        } else if (era.get(`talent:${target}:85`) == 1) { // :2298
          await era.printAndWait(''); // :2299

        } else { // :2301
          await era.printAndWait(''); // :2302
        } // :2303
      } // :2304
      // CFLAG:TARGET:335  = 1（变量语义：CFLAG 族，TARGET:335） // :2305
      era.set(`cflag:${target}:335`, 1); // :2305
      return 0; // :2306

    } else { // :2308

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:335`) <= 5 || era.get('flag:7') == 2)) { // :2310
        if (rand_n(4) == 0) { // :2311
          await era.printAndWait(''); // :2312
        } else if (rand_n(3) == 0) { // :2313
          await era.printAndWait(''); // :2314
        } else if (rand_n(2) == 0) { // :2315
          await era.printAndWait(''); // :2316
        } else { // :2317
          await era.printAndWait(''); // :2318
        } // :2319
        // CFLAG:335  = 6（变量语义：CFLAG 族，335） // :2320
        era.set(`cflag:${target}:335`, 6); // :2320

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:335`) <= 4 || era.get('flag:7') == 2)) { // :2322
        if (rand_n(4) == 0) { // :2323
          await era.print(''); // :2324
        } else if (rand_n(3) == 0) { // :2325
          await era.printAndWait(''); // :2326
        } else if (rand_n(2) == 0) { // :2327
          await era.printAndWait(''); // :2328
        } else { // :2329
          await era.printAndWait(''); // :2330
        } // :2331
        // CFLAG:335  = 5（变量语义：CFLAG 族，335） // :2332
        era.set(`cflag:${target}:335`, 5); // :2332

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:335`) <= 3 || era.get('flag:7') == 2)) { // :2334
        if (rand_n(4) == 0) { // :2335
          await era.printAndWait(''); // :2336
        } else if (rand_n(3) == 0) { // :2337
          await era.printAndWait(''); // :2338
        } else if (rand_n(2) == 0) { // :2339
          await era.printAndWait(''); // :2340
        } else { // :2341
          await era.printAndWait(''); // :2342
        } // :2343
        // CFLAG:335  = 4（变量语义：CFLAG 族，335） // :2344
        era.set(`cflag:${target}:335`, 4); // :2344

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:335`) <= 2 || era.get('flag:7') == 2)) { // :2346
        await era.print(''); // :2347
        await era.printAndWait(''); // :2348
        // CFLAG:335  = 3（变量语义：CFLAG 族，335） // :2349
        era.set(`cflag:${target}:335`, 3); // :2349

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :2351
        await era.printAndWait(''); // :2352
        // CFLAG:335  = 2（变量语义：CFLAG 族，335） // :2353
        era.set(`cflag:${target}:335`, 2); // :2353
      } // :2354
      return 0; // :2355
    } // :2356
  } // :2357




  if (era_flag.selectcom == 35) { // :2362

    if (era.get(`cflag:${target}:336`) == 0) { // :2364

      if (era.get(`abl:${target}:16`) >= 3) { // :2366
        await era.printAndWait(''); // :2367

      } else { // :2369
        await era.printAndWait(''); // :2370
      } // :2371
      // CFLAG:TARGET:336  = 1（变量语义：CFLAG 族，TARGET:336） // :2372
      era.set(`cflag:${target}:336`, 1); // :2372
      return 0; // :2373

    } else { // :2375

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:336`) <= 4 || era.get('flag:7') == 2)) { // :2377
        await era.printAndWait(''); // :2378
        // CFLAG:336  = 5（变量语义：CFLAG 族，336） // :2379
        era.set(`cflag:${target}:336`, 5); // :2379

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:336`) <= 3 || era.get('flag:7') == 2)) { // :2381
        await era.printAndWait(''); // :2382
        // CFLAG:336  = 4（变量语义：CFLAG 族，336） // :2383
        era.set(`cflag:${target}:336`, 4); // :2383

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:336`) <= 2 || era.get('flag:7') == 2)) { // :2385
        await era.printAndWait(''); // :2386
        // CFLAG:336  = 3（变量语义：CFLAG 族，336） // :2387
        era.set(`cflag:${target}:336`, 3); // :2387

      } else if (era.get(`cflag:${target}:336`) <= 1 || era.get('flag:7') == 2) { // :2389
        await era.printAndWait(''); // :2390
        // CFLAG:336  = 2（变量语义：CFLAG 族，336） // :2391
        era.set(`cflag:${target}:336`, 2); // :2391
      } // :2392
      return 0; // :2393
    } // :2394
  } // :2395




  if (era_flag.selectcom == 36) { // :2400

    if (era.get(`cflag:${target}:337`) == 0) { // :2402

      if (era.get(`talent:${target}:76`) == 1) { // :2404
        await era.printAndWait(''); // :2405

      } else if (era.get(`talent:${target}:85`) == 1) { // :2407
        await era.printAndWait(''); // :2408

      } else { // :2410
        await era.printAndWait(''); // :2411
      } // :2412
      // CFLAG:TARGET:337  = 1（变量语义：CFLAG 族，TARGET:337） // :2413
      era.set(`cflag:${target}:337`, 1); // :2413
      return 0; // :2414

    } else { // :2416

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 6 || era.get('flag:7') == 2)) { // :2418
        if (rand_n(2) == 0) { // :2419
          await era.printAndWait(''); // :2420
        } else { // :2421
          await era.printAndWait(''); // :2422
        } // :2423
        // CFLAG:337  = 7（变量语义：CFLAG 族，337） // :2424
        era.set(`cflag:${target}:337`, 7); // :2424

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:337`) <= 5 || era.get('flag:7') == 2)) { // :2426
        await era.printAndWait(''); // :2427
        // CFLAG:337  = 6（变量语义：CFLAG 族，337） // :2428
        era.set(`cflag:${target}:337`, 6); // :2428

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 4 || era.get('flag:7') == 2)) { // :2430
        if (rand_n(2) == 0) { // :2431
          await era.printAndWait(''); // :2432
        } else { // :2433
          await era.printAndWait(''); // :2434
        } // :2435
        // CFLAG:337  = 5（变量语义：CFLAG 族，337） // :2436
        era.set(`cflag:${target}:337`, 5); // :2436

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:337`) <= 3 || era.get('flag:7') == 2)) { // :2438
        await era.printAndWait(''); // :2439
        // CFLAG:337  = 4（变量语义：CFLAG 族，337） // :2440
        era.set(`cflag:${target}:337`, 4); // :2440

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 2 || era.get('flag:7') == 2)) { // :2442
        await era.printAndWait(''); // :2443
        // CFLAG:337  = 3（变量语义：CFLAG 族，337） // :2444
        era.set(`cflag:${target}:337`, 3); // :2444

      } else if (era.get(`cflag:${target}:337`) <= 1 || era.get('flag:7') == 2) { // :2446
        await era.printAndWait(''); // :2447
        // CFLAG:337  = 2（变量语义：CFLAG 族，337） // :2448
        era.set(`cflag:${target}:337`, 2); // :2448
      } // :2449
      return 0; // :2450
    } // :2451
  } // :2452




  if (era_flag.selectcom == 37) { // :2457

    if (era.get(`cflag:${target}:338`) == 0) { // :2459

      if (era.get(`abl:${target}:16`) >= 3) { // :2461
        await era.printAndWait(''); // :2462

      } else { // :2464
        await era.printAndWait(''); // :2465
      } // :2466
      // CFLAG:TARGET:338  = 1（变量语义：CFLAG 族，TARGET:338） // :2467
      era.set(`cflag:${target}:338`, 1); // :2467
      return 0; // :2468

    } else { // :2470

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 4 || era.get('flag:7') == 2)) { // :2472
        await era.printAndWait(''); // :2473
        // CFLAG:338  = 5（变量语义：CFLAG 族，338） // :2474
        era.set(`cflag:${target}:338`, 5); // :2474

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 3 || era.get('flag:7') == 2)) { // :2476
        await era.print(''); // :2477
        // CFLAG:338  = 4（变量语义：CFLAG 族，338） // :2478
        era.set(`cflag:${target}:338`, 4); // :2478

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:338`) <= 2 || era.get('flag:7') == 2)) { // :2480
        await era.printAndWait(''); // :2481
        // CFLAG:338  = 3（变量语义：CFLAG 族，338） // :2482
        era.set(`cflag:${target}:338`, 3); // :2482

      } else if (era.get(`cflag:${target}:338`) <= 1 || era.get('flag:7') == 2) { // :2484
        await era.printAndWait(''); // :2485
        // CFLAG:338  = 2（变量语义：CFLAG 族，338） // :2486
        era.set(`cflag:${target}:338`, 2); // :2486
      } // :2487
      return 0; // :2488
    } // :2489
  } // :2490




  if (era_flag.selectcom == 40) { // :2495

    if (era.get(`cflag:${target}:341`) == 0) { // :2497
      await era.printAndWait(''); // :2498
      // CFLAG:TARGET:341  = 1（变量语义：CFLAG 族，TARGET:341） // :2499
      era.set(`cflag:${target}:341`, 1); // :2499
      return 0; // :2500

    } else { // :2502

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:341`) <= 4 || era.get('flag:7') == 2)) { // :2504
        await era.printAndWait(''); // :2505
        // CFLAG:TARGET:341  = 5（变量语义：CFLAG 族，TARGET:341） // :2506
        era.set(`cflag:${target}:341`, 5); // :2506

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:341`) <= 3 || era.get('flag:7') == 2)) { // :2508
        await era.printAndWait(''); // :2509
        // CFLAG:TARGET:341  = 4（变量语义：CFLAG 族，TARGET:341） // :2510
        era.set(`cflag:${target}:341`, 4); // :2510
        return 0; // :2511

      } else if (era.get(`mark:${target}:0`) == 3 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:341`) <= 2 || era.get('flag:7') == 2)) { // :2513
        await era.printAndWait(''); // :2514
        // CFLAG:TARGET:341  = 3（变量语义：CFLAG 族，TARGET:341） // :2515
        era.set(`cflag:${target}:341`, 3); // :2515
        return 0; // :2516

      } else if (era.get(`cflag:${target}:341`) <= 1 && era.get('flag:7') == 2) { // :2518
        await era.printAndWait(''); // :2519
        // CFLAG:TARGET:341  = 2（变量语义：CFLAG 族，TARGET:341） // :2520
        era.set(`cflag:${target}:341`, 2); // :2520
      } // :2521
      return 0; // :2522
    } // :2523
  } // :2524




  if (era_flag.selectcom == 41) { // :2529

    if (era.get(`cflag:${target}:342`) == 0) { // :2531

      if (era.get(`talent:${target}:76`) == 1) { // :2533
        await era.printAndWait(''); // :2534

      } else if (era.get(`talent:${target}:85`) == 1) { // :2536
        await era.printAndWait(''); // :2537

      } else { // :2539
        await era.printAndWait(''); // :2540
      } // :2541
      // CFLAG:TARGET:342  = 1（变量语义：CFLAG 族，TARGET:342） // :2542
      era.set(`cflag:${target}:342`, 1); // :2542
      return 0; // :2543

    } else { // :2545

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:342`) <= 8 || era.get('flag:7') == 2)) { // :2547
        await era.printAndWait(''); // :2548
        // CFLAG:TARGET:342  = 9（变量语义：CFLAG 族，TARGET:342） // :2549
        era.set(`cflag:${target}:342`, 9); // :2549

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 7 || era.get('flag:7') == 2)) { // :2551
        await era.printAndWait(''); // :2552
        // CFLAG:TARGET:342  = 8（变量语义：CFLAG 族，TARGET:342） // :2553
        era.set(`cflag:${target}:342`, 8); // :2553

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:342`) <= 6 || era.get('flag:7') == 2)) { // :2555
        await era.printAndWait(''); // :2556
        // CFLAG:TARGET:342  = 7（变量语义：CFLAG 族，TARGET:342） // :2557
        era.set(`cflag:${target}:342`, 7); // :2557

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:342`) <= 5 || era.get('flag:7') == 2)) { // :2559
        await era.printAndWait(''); // :2560
        // CFLAG:TARGET:342  = 6（变量语义：CFLAG 族，TARGET:342） // :2561
        era.set(`cflag:${target}:342`, 6); // :2561

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 4 || era.get('flag:7') == 2)) { // :2563
        await era.printAndWait(''); // :2564
        // CFLAG:TARGET:342  = 5（变量语义：CFLAG 族，TARGET:342） // :2565
        era.set(`cflag:${target}:342`, 5); // :2565

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:342`) <= 3 || era.get('flag:7') == 2)) { // :2567
        await era.printAndWait(''); // :2568
        // CFLAG:TARGET:342  = 4（变量语义：CFLAG 族，TARGET:342） // :2569
        era.set(`cflag:${target}:342`, 4); // :2569

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 2 || era.get('flag:7') == 2)) { // :2571
        await era.printAndWait(''); // :2572
        // CFLAG:TARGET:342  = 3（变量语义：CFLAG 族，TARGET:342） // :2573
        era.set(`cflag:${target}:342`, 3); // :2573

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :2575
        await era.printAndWait(''); // :2576
        // CFLAG:TARGET:342  = 2（变量语义：CFLAG 族，TARGET:342） // :2577
        era.set(`cflag:${target}:342`, 2); // :2577
      } // :2578
      return 0; // :2579
    } // :2580
  } // :2581




  if (era_flag.selectcom == 42) { // :2586

    if (era.get(`cflag:${target}:343`) == 0) { // :2588

      if (era.get(`talent:${target}:76`) == 1) { // :2590
        await era.printAndWait(''); // :2591

      } else if (era.get(`talent:${target}:85`) == 1) { // :2593
        await era.printAndWait(''); // :2594

      } else { // :2596
        await era.printAndWait(''); // :2597
      } // :2598
      // CFLAG:TARGET:343  = 1（变量语义：CFLAG 族，TARGET:343） // :2599
      era.set(`cflag:${target}:343`, 1); // :2599
      return 0; // :2600

    } else { // :2602

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:343`) <= 8 || era.get('flag:7') == 2)) { // :2604
        await era.printAndWait(''); // :2605
        // CFLAG:TARGET:343  = 9（变量语义：CFLAG 族，TARGET:343） // :2606
        era.set(`cflag:${target}:343`, 9); // :2606

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 7 || era.get('flag:7') == 2)) { // :2608
        await era.printAndWait(''); // :2609
        // CFLAG:TARGET:343  = 8（变量语义：CFLAG 族，TARGET:343） // :2610
        era.set(`cflag:${target}:343`, 8); // :2610

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:343`) <= 6 || era.get('flag:7') == 2)) { // :2612
        await era.printAndWait(''); // :2613
        // CFLAG:TARGET:343  = 7（变量语义：CFLAG 族，TARGET:343） // :2614
        era.set(`cflag:${target}:343`, 7); // :2614

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:343`) <= 5 || era.get('flag:7') == 2)) { // :2616
        await era.printAndWait(''); // :2617
        // CFLAG:TARGET:343  = 6（变量语义：CFLAG 族，TARGET:343） // :2618
        era.set(`cflag:${target}:343`, 6); // :2618

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 4 || era.get('flag:7') == 2)) { // :2620
        await era.printAndWait(''); // :2621
        // CFLAG:TARGET:343  = 5（变量语义：CFLAG 族，TARGET:343） // :2622
        era.set(`cflag:${target}:343`, 5); // :2622

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:343`) <= 3 || era.get('flag:7') == 2)) { // :2624
        await era.printAndWait(''); // :2625
        // CFLAG:TARGET:343  = 4（变量语义：CFLAG 族，TARGET:343） // :2626
        era.set(`cflag:${target}:343`, 4); // :2626

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 2 || era.get('flag:7') == 2)) { // :2628
        await era.printAndWait(''); // :2629
        // CFLAG:TARGET:343  = 3（变量语义：CFLAG 族，TARGET:343） // :2630
        era.set(`cflag:${target}:343`, 3); // :2630

      } else if (era.get(`cflag:${target}:343`) <= 1 || era.get('flag:7') == 2) { // :2632
        await era.printAndWait(''); // :2633
        // CFLAG:TARGET:343  = 2（变量语义：CFLAG 族，TARGET:343） // :2634
        era.set(`cflag:${target}:343`, 2); // :2634
      } // :2635
      return 0; // :2636
    } // :2637
  } // :2638





  if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`)) { // :2644

    if (era.get(`cflag:${target}:344`) == 0) { // :2646

      if (era.get(`talent:${target}:76`) == 1) { // :2648
        await era.printAndWait(''); // :2649

      } else if (era.get(`talent:${target}:85`) == 1) { // :2651
        await era.printAndWait(''); // :2652

      } else { // :2654
        await era.printAndWait(''); // :2655
      } // :2656
      // CFLAG:TARGET:344  = 1（变量语义：CFLAG 族，TARGET:344） // :2657
      era.set(`cflag:${target}:344`, 1); // :2657
      return 0; // :2658

    } else { // :2660

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 8 || era.get('flag:7') == 2)) { // :2662
        await era.printAndWait(''); // :2663
        // CFLAG:TARGET:344  = 9（变量语义：CFLAG 族，TARGET:344） // :2664
        era.set(`cflag:${target}:344`, 9); // :2664

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 7 || era.get('flag:7') == 2)) { // :2666
        await era.printAndWait(''); // :2667
        // CFLAG:TARGET:344  = 8（变量语义：CFLAG 族，TARGET:344） // :2668
        era.set(`cflag:${target}:344`, 8); // :2668

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:344`) <= 6 || era.get('flag:7') == 2)) { // :2670
        await era.printAndWait(''); // :2671
        // CFLAG:TARGET:344  = 7（变量语义：CFLAG 族，TARGET:344） // :2672
        era.set(`cflag:${target}:344`, 7); // :2672

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 5 || era.get('flag:7') == 2)) { // :2674
        await era.printAndWait(''); // :2675
        // CFLAG:TARGET:344  = 6（变量语义：CFLAG 族，TARGET:344） // :2676
        era.set(`cflag:${target}:344`, 6); // :2676

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 4 || era.get('flag:7') == 2)) { // :2678
        await era.printAndWait(''); // :2679
        // CFLAG:TARGET:344  = 5（变量语义：CFLAG 族，TARGET:344） // :2680
        era.set(`cflag:${target}:344`, 5); // :2680

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:344`) <= 3 || era.get('flag:7') == 2)) { // :2682
        await era.printAndWait(''); // :2683
        // CFLAG:TARGET:344  = 4（变量语义：CFLAG 族，TARGET:344） // :2684
        era.set(`cflag:${target}:344`, 4); // :2684

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 2 || era.get('flag:7') == 2)) { // :2686
        await era.printAndWait(''); // :2687
        // CFLAG:TARGET:344  = 3（变量语义：CFLAG 族，TARGET:344） // :2688
        era.set(`cflag:${target}:344`, 3); // :2688

      } else if (era.get(`cflag:${target}:344`) <= 1 || era.get('flag:7') == 2) { // :2690
        await era.printAndWait(''); // :2691
        // CFLAG:TARGET:344  = 2（变量语义：CFLAG 族，TARGET:344） // :2692
        era.set(`cflag:${target}:344`, 2); // :2692
      } // :2693
      return 0; // :2694
    } // :2695

  } else if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`) == 0) { // :2697

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:380`) < 3 || era.get('flag:7') == 2)) { // :2699
      await era.printAndWait(''); // :2700
      // CFLAG:380  = 3（变量语义：CFLAG 族，380） // :2701
      era.set(`cflag:${target}:380`, 3); // :2701

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:380`) < 2 || era.get('flag:7') == 2)) { // :2703
      await era.printAndWait(''); // :2704
      // CFLAG:380  = 2（变量语义：CFLAG 族，380） // :2705
      era.set(`cflag:${target}:380`, 2); // :2705

    } else if (era.get(`cflag:${target}:380`) < 1 || era.get('flag:7') == 2) { // :2707
      await era.printAndWait(''); // :2708
      // CFLAG:380  = 1（变量语义：CFLAG 族，380） // :2709
      era.set(`cflag:${target}:380`, 1); // :2709
    } // :2710
    return 0; // :2711
  } // :2712





  if (era_flag.selectcom == 44 && era.get(`tequip:${target}:44`)) { // :2718

    if (era.get(`cflag:${target}:345`) == 0) { // :2720

      if (era.get(`talent:${target}:76`) == 1) { // :2722
        await era.printAndWait(''); // :2723

      } else if (era.get(`talent:${target}:85`) == 1) { // :2725
        await era.printAndWait(''); // :2726

      } else { // :2728
        await era.printAndWait(''); // :2729
      } // :2730
      // CFLAG:TARGET:345  = 1（变量语义：CFLAG 族，TARGET:345） // :2731
      era.set(`cflag:${target}:345`, 1); // :2731
      return 0; // :2732

    } else { // :2734

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:345`) <= 8 || era.get('flag:7') == 2)) { // :2736
        await era.printAndWait(''); // :2737
        // CFLAG:TARGET:345  = 9（变量语义：CFLAG 族，TARGET:345） // :2738
        era.set(`cflag:${target}:345`, 9); // :2738

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 7 || era.get('flag:7') == 2)) { // :2740
        await era.printAndWait(''); // :2741
        // CFLAG:TARGET:345  = 8（变量语义：CFLAG 族，TARGET:345） // :2742
        era.set(`cflag:${target}:345`, 8); // :2742

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:345`) <= 6 || era.get('flag:7') == 2)) { // :2744
        await era.printAndWait(''); // :2745
        // CFLAG:TARGET:345  = 7（变量语义：CFLAG 族，TARGET:345） // :2746
        era.set(`cflag:${target}:345`, 7); // :2746

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:345`) <= 5 || era.get('flag:7') == 2)) { // :2748
        await era.printAndWait(''); // :2749
        // CFLAG:TARGET:345  = 6（变量语义：CFLAG 族，TARGET:345） // :2750
        era.set(`cflag:${target}:345`, 6); // :2750

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 4 || era.get('flag:7') == 2)) { // :2752
        await era.printAndWait(''); // :2753
        // CFLAG:TARGET:345  = 5（变量语义：CFLAG 族，TARGET:345） // :2754
        era.set(`cflag:${target}:345`, 5); // :2754

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:345`) <= 3 || era.get('flag:7') == 2)) { // :2756
        await era.printAndWait(''); // :2757
        // CFLAG:TARGET:345  = 4（变量语义：CFLAG 族，TARGET:345） // :2758
        era.set(`cflag:${target}:345`, 4); // :2758

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 2 || era.get('flag:7') == 2)) { // :2760
        await era.printAndWait(''); // :2761
        // CFLAG:TARGET:345  = 3（变量语义：CFLAG 族，TARGET:345） // :2762
        era.set(`cflag:${target}:345`, 3); // :2762

      } else if (era.get(`cflag:${target}:345`) <= 1 || era.get('flag:7') == 2) { // :2764
        await era.printAndWait(''); // :2765
        // CFLAG:TARGET:345  = 2（变量语义：CFLAG 族，TARGET:345） // :2766
        era.set(`cflag:${target}:345`, 2); // :2766
      } // :2767
      return 0; // :2768
    } // :2769

  } else if (era_flag.selectcom == 44 && era.get(`tequip:${target}:44`) == 0) { // :2771

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:385`) < 2 || era.get('flag:7') == 2)) { // :2773
      await era.printAndWait(''); // :2774
      // CFLAG:385  = 2（变量语义：CFLAG 族，385） // :2775
      era.set(`cflag:${target}:385`, 2); // :2775

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:385`) < 2 || era.get('flag:7') == 2)) { // :2777
      await era.printAndWait(''); // :2778
      // CFLAG:385  = 2（变量语义：CFLAG 族，385） // :2779
      era.set(`cflag:${target}:385`, 2); // :2779

    } else if (era.get(`cflag:${target}:385`) < 1 || era.get('flag:7') == 2) { // :2781
      await era.printAndWait(''); // :2782
      // CFLAG:385  = 1（变量语义：CFLAG 族，385） // :2783
      era.set(`cflag:${target}:385`, 1); // :2783
    } // :2784
    return 0; // :2785
  } // :2786





  if (era_flag.selectcom == 45 && era.get(`tequip:${target}:45`)) { // :2792

    if (era.get(`cflag:${target}:346`) == 0) { // :2794

      if (era.get(`talent:${target}:76`) == 1) { // :2796
        await era.printAndWait(''); // :2797

      } else if (era.get(`talent:${target}:85`) == 1) { // :2799
        await era.printAndWait(''); // :2800

      } else { // :2802
        await era.printAndWait(''); // :2803
      } // :2804
      // CFLAG:TARGET:346  = 1（变量语义：CFLAG 族，TARGET:346） // :2805
      era.set(`cflag:${target}:346`, 1); // :2805
      return 0; // :2806

    } else { // :2808

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:346`) <= 8 || era.get('flag:7') == 2)) { // :2810
        await era.printAndWait(''); // :2811
        // CFLAG:TARGET:346  = 9（变量语义：CFLAG 族，TARGET:346） // :2812
        era.set(`cflag:${target}:346`, 9); // :2812

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 7 || era.get('flag:7') == 2)) { // :2814
        await era.printAndWait(''); // :2815
        // CFLAG:TARGET:346  = 8（变量语义：CFLAG 族，TARGET:346） // :2816
        era.set(`cflag:${target}:346`, 8); // :2816

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:346`) <= 6 || era.get('flag:7') == 2)) { // :2818
        await era.printAndWait(''); // :2819
        // CFLAG:TARGET:346  = 7（变量语义：CFLAG 族，TARGET:346） // :2820
        era.set(`cflag:${target}:346`, 7); // :2820

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:346`) <= 5 || era.get('flag:7') == 2)) { // :2822
        await era.printAndWait(''); // :2823
        // CFLAG:TARGET:346  = 6（变量语义：CFLAG 族，TARGET:346） // :2824
        era.set(`cflag:${target}:346`, 6); // :2824

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 4 || era.get('flag:7') == 2)) { // :2826
        await era.printAndWait(''); // :2827
        // CFLAG:TARGET:346  = 5（变量语义：CFLAG 族，TARGET:346） // :2828
        era.set(`cflag:${target}:346`, 5); // :2828

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:346`) <= 3 || era.get('flag:7') == 2)) { // :2830
        await era.printAndWait(''); // :2831
        // CFLAG:TARGET:346  = 4（变量语义：CFLAG 族，TARGET:346） // :2832
        era.set(`cflag:${target}:346`, 4); // :2832

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 2 || era.get('flag:7') == 2)) { // :2834
        await era.printAndWait(''); // :2835
        // CFLAG:TARGET:346  = 3（变量语义：CFLAG 族，TARGET:346） // :2836
        era.set(`cflag:${target}:346`, 3); // :2836

      } else if (era.get(`cflag:${target}:346`) <= 1 || era.get('flag:7') == 2) { // :2838
        await era.printAndWait(''); // :2839
        // CFLAG:TARGET:346  = 2（变量语义：CFLAG 族，TARGET:346） // :2840
        era.set(`cflag:${target}:346`, 2); // :2840
      } // :2841
      return 0; // :2842
    } // :2843

  } else if (era_flag.selectcom == 45 && era.get(`tequip:${target}:45`) == 0) { // :2845

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:386`) < 3 || era.get('flag:7') == 2)) { // :2847
      await era.printAndWait(''); // :2848
      // CFLAG:386  = 3（变量语义：CFLAG 族，386） // :2849
      era.set(`cflag:${target}:386`, 3); // :2849

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:386`) < 2 || era.get('flag:7') == 2)) { // :2851
      await era.printAndWait(''); // :2852
      // CFLAG:386  = 2（变量语义：CFLAG 族，386） // :2853
      era.set(`cflag:${target}:386`, 2); // :2853

    } else if (era.get(`cflag:${target}:386`) < 1 || era.get('flag:7') == 2) { // :2855
      await era.printAndWait(''); // :2856
      // CFLAG:386  = 1（变量语义：CFLAG 族，386） // :2857
      era.set(`cflag:${target}:386`, 1); // :2857
    } // :2858
    return 0; // :2859
  } // :2860





  if (era_flag.selectcom == 46 && era.get(`tequip:${target}:46`)) { // :2866

    if (era.get(`cflag:${target}:347`) == 0) { // :2868

      if (era.get(`talent:${target}:76`) == 1) { // :2870
        await era.printAndWait(''); // :2871

      } else if (era.get(`talent:${target}:85`) == 1) { // :2873
        await era.printAndWait(''); // :2874

      } else { // :2876
        await era.printAndWait(''); // :2877
      } // :2878
      // CFLAG:TARGET:347  = 1（变量语义：CFLAG 族，TARGET:347） // :2879
      era.set(`cflag:${target}:347`, 1); // :2879
      return 0; // :2880

    } else { // :2882

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 6 || era.get('flag:7') == 2)) { // :2884
        await era.printAndWait(''); // :2885
        // CFLAG:347  = 7（变量语义：CFLAG 族，347） // :2886
        era.set(`cflag:${target}:347`, 7); // :2886

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:347`) <= 5 || era.get('flag:7') == 2)) { // :2888
        await era.printAndWait(''); // :2889
        // CFLAG:347  = 6（变量语义：CFLAG 族，347） // :2890
        era.set(`cflag:${target}:347`, 6); // :2890

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 4 || era.get('flag:7') == 2)) { // :2892
        await era.printAndWait(''); // :2893
        // CFLAG:347  = 5（变量语义：CFLAG 族，347） // :2894
        era.set(`cflag:${target}:347`, 5); // :2894

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:347`) <= 3 || era.get('flag:7') == 2)) { // :2896
        await era.printAndWait(''); // :2897
        // CFLAG:347  = 4（变量语义：CFLAG 族，347） // :2898
        era.set(`cflag:${target}:347`, 4); // :2898

      } else if (era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 2 || era.get('flag:7') == 2)) { // :2900
        await era.printAndWait(''); // :2901
        // CFLAG:347  = 3（变量语义：CFLAG 族，347） // :2902
        era.set(`cflag:${target}:347`, 3); // :2902

      } else if (era.get(`cflag:${target}:347`) <= 1 || era.get('flag:7') == 2) { // :2904
        await era.printAndWait(''); // :2905
        // CFLAG:347  = 2（变量语义：CFLAG 族，347） // :2906
        era.set(`cflag:${target}:347`, 2); // :2906
      } // :2907
      return 0; // :2908
    } // :2909
  } // :2910




  if (era_flag.selectcom == 55) { // :2915

    if (era.get(`cflag:${target}:356`) == 0) { // :2917

      if (era.get(`talent:${target}:85`) == 1) { // :2919
        await era.printAndWait(''); // :2920

      } else { // :2922
        await era.printAndWait(''); // :2923
      } // :2924
      // CFLAG:356  = 1（变量语义：CFLAG 族，356） // :2925
      era.set(`cflag:${target}:356`, 1); // :2925
      return 0; // :2926

    } else { // :2928

      if (era.get(`talent:${target}:85`) == 1 && era.get(`palam:${target}:5`) >= era.get('palamlv:3') && (era.get(`cflag:${target}:356`) <= 3 || era.get('flag:7') == 2)) { // :2930
        await era.printAndWait(''); // :2931
        // CFLAG:356  = 4（变量语义：CFLAG 族，356） // :2932
        era.set(`cflag:${target}:356`, 4); // :2932

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:356`) <= 2 || era.get('flag:7') == 2)) { // :2934
        await era.printAndWait(''); // :2935
        // CFLAG:356  = 3（变量语义：CFLAG 族，356） // :2936
        era.set(`cflag:${target}:356`, 3); // :2936

      } else if (era.get(`cflag:${target}:356`) <= 1 || era.get('flag:7') == 2) { // :2938
        await era.printAndWait(''); // :2939
        // CFLAG:356  = 2（变量语义：CFLAG 族，356） // :2940
        era.set(`cflag:${target}:356`, 2); // :2940
      } // :2941
      return 0; // :2942
    } // :2943
  } // :2944





  if (era_flag.selectcom == 56) { // :2950

    if (era.get(`cflag:${target}:357`) == 0) { // :2952
      if (era.get(`tequip:${target}:53`)) { // :2953


        if (era.get(`talent:${target}:76`) == 1) { // :2956
          await era.printAndWait(''); // :2957

        } else if (era.get(`talent:${target}:85`) == 1) { // :2959
          await era.printAndWait(''); // :2960

        } else { // :2962
          await era.printAndWait(''); // :2963
        } // :2964
      } else { // :2965

        if (era.get(`talent:${target}:76`) == 1) { // :2967
          await era.printAndWait(''); // :2968

        } else if (era.get(`talent:${target}:85`) == 1) { // :2970
          await era.printAndWait(''); // :2971

        } else { // :2973
          await era.printAndWait(''); // :2974
        } // :2975
      } // :2976
      // CFLAG:357  = 1（变量语义：CFLAG 族，357） // :2977
      era.set(`cflag:${target}:357`, 1); // :2977
      return 0; // :2978

    } else { // :2980
      if (era.get(`tequip:${target}:53`)) { // :2981


        if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :2984
          await era.printAndWait(''); // :2985
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :2986
          era.set(`cflag:${target}:357`, 4); // :2986

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :2988
          await era.printAndWait(''); // :2989
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :2990
          era.set(`cflag:${target}:357`, 3); // :2990

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :2992
          await era.printAndWait(''); // :2993
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :2994
          era.set(`cflag:${target}:357`, 2); // :2994
        } // :2995
      } else { // :2996

        if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :2998
          await era.printAndWait(''); // :2999
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :3000
          era.set(`cflag:${target}:357`, 4); // :3000

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :3002
          await era.printAndWait(''); // :3003
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :3004
          era.set(`cflag:${target}:357`, 3); // :3004

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :3006
          await era.printAndWait(''); // :3007
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :3008
          era.set(`cflag:${target}:357`, 2); // :3008
        } // :3009
      } // :3010
      return 0; // :3011
    } // :3012
  } // :3013



  if (era_flag.selectcom == 123) { // :3017

    if (era.get(`cflag:${target}:360`) == 0) { // :3019

      if (era.get(`talent:${target}:76`) == 1) { // :3021
        await era.printAndWait(''); // :3022

      } else if (era.get(`talent:${target}:85`) == 1) { // :3024
        await era.printAndWait(''); // :3025

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3027
        await era.printAndWait(''); // :3028

      } else { // :3030
        await era.printAndWait(''); // :3031
      } // :3032
      // CFLAG:TARGET:360  = 1（变量语义：CFLAG 族，TARGET:360） // :3033
      era.set(`cflag:${target}:360`, 1); // :3033
      return 0; // :3034

    } else { // :3036

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:360`) <= 4 || era.get('flag:7') == 2)) { // :3038
        await era.printAndWait(''); // :3039
        // CFLAG:360  = 5（变量语义：CFLAG 族，360） // :3040
        era.set(`cflag:${target}:360`, 5); // :3040

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:360`) <= 3 || era.get('flag:7') == 2)) { // :3042
        await era.printAndWait(''); // :3043
        // CFLAG:360  = 4（变量语义：CFLAG 族，360） // :3044
        era.set(`cflag:${target}:360`, 4); // :3044

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:360`) <= 2 || era.get('flag:7') == 2)) { // :3046
        await era.printAndWait(''); // :3047
        // CFLAG:360  = 3（变量语义：CFLAG 族，360） // :3048
        era.set(`cflag:${target}:360`, 3); // :3048

      } else if (era.get(`cflag:${target}:360`) <= 1 || era.get('flag:7') == 2) { // :3050
        await era.printAndWait(''); // :3051
        // CFLAG:360  = 2（变量语义：CFLAG 族，360） // :3052
        era.set(`cflag:${target}:360`, 2); // :3052
      } // :3053
      return 0; // :3054
    } // :3055
  } // :3056



  if (era_flag.selectcom == 114) { // :3060

    if (era.get(`cflag:${target}:361`) == 0) { // :3062

      if (era.get(`talent:${target}:76`) == 1) { // :3064
        await era.printAndWait(''); // :3065

      } else if (era.get(`talent:${target}:85`) == 1) { // :3067
        await era.printAndWait(''); // :3068

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3070
        await era.printAndWait(''); // :3071

      } else { // :3073
        await era.printAndWait(''); // :3074
      } // :3075
      // CFLAG:TARGET:361  = 1（变量语义：CFLAG 族，TARGET:361） // :3076
      era.set(`cflag:${target}:361`, 1); // :3076
      return 0; // :3077

    } else { // :3079

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:361`) <= 4 || era.get('flag:7') == 2)) { // :3081
        await era.printAndWait(''); // :3082
        // CFLAG:361  = 5（变量语义：CFLAG 族，361） // :3083
        era.set(`cflag:${target}:361`, 5); // :3083

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:361`) <= 3 || era.get('flag:7') == 2)) { // :3085
        await era.printAndWait(''); // :3086
        // CFLAG:361  = 4（变量语义：CFLAG 族，361） // :3087
        era.set(`cflag:${target}:361`, 4); // :3087

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:361`) <= 2 || era.get('flag:7') == 2)) { // :3089
        await era.printAndWait(''); // :3090
        // CFLAG:361  = 3（变量语义：CFLAG 族，361） // :3091
        era.set(`cflag:${target}:361`, 3); // :3091

      } else if (era.get(`cflag:${target}:361`) <= 1 || era.get('flag:7') == 2) { // :3093
        await era.printAndWait(''); // :3094
        // CFLAG:361  = 2（变量语义：CFLAG 族，361） // :3095
        era.set(`cflag:${target}:361`, 2); // :3095
      } // :3096
      return 0; // :3097
    } // :3098
  } // :3099




  if (era_flag.selectcom == 126) { // :3104

    if (era.get(`cflag:${target}:362`) == 0) { // :3106

      if (era.get(`talent:${target}:76`) == 1) { // :3108
        await era.printAndWait(''); // :3109

      } else if (era.get(`talent:${target}:85`) == 1) { // :3111
        await era.printAndWait(''); // :3112

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3114
        await era.printAndWait(''); // :3115

      } else { // :3117
        await era.printAndWait(''); // :3118
      } // :3119
      // CFLAG:TARGET:362  = 1（变量语义：CFLAG 族，TARGET:362） // :3120
      era.set(`cflag:${target}:362`, 1); // :3120
      return 0; // :3121

    } else { // :3123

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:362`) <= 4 || era.get('flag:7') == 2)) { // :3125
        await era.printAndWait(''); // :3126
        // CFLAG:362  = 5（变量语义：CFLAG 族，362） // :3127
        era.set(`cflag:${target}:362`, 5); // :3127

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:362`) <= 3 || era.get('flag:7') == 2)) { // :3129
        await era.printAndWait(''); // :3130
        // CFLAG:362  = 4（变量语义：CFLAG 族，362） // :3131
        era.set(`cflag:${target}:362`, 4); // :3131

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:362`) <= 2 || era.get('flag:7') == 2)) { // :3133
        await era.printAndWait(''); // :3134
        // CFLAG:362  = 3（变量语义：CFLAG 族，362） // :3135
        era.set(`cflag:${target}:362`, 3); // :3135

      } else if (era.get(`cflag:${target}:362`) <= 1 || era.get('flag:7') == 2) { // :3137
        await era.printAndWait(''); // :3138
        // CFLAG:362  = 2（变量语义：CFLAG 族，362） // :3139
        era.set(`cflag:${target}:362`, 2); // :3139
      } // :3140
      return 0; // :3141
    } // :3142
  } // :3143




  if (era_flag.selectcom == 127) { // :3148

    if (era.get(`cflag:${target}:363`) == 0) { // :3150

      if (era.get(`talent:${target}:76`) == 1) { // :3152
        await era.printAndWait(''); // :3153

      } else if (era.get(`talent:${target}:85`) == 1) { // :3155
        await era.printAndWait(''); // :3156

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3158
        await era.printAndWait(''); // :3159

      } else { // :3161
        await era.printAndWait(''); // :3162
      } // :3163
      // CFLAG:TARGET:363  = 1（变量语义：CFLAG 族，TARGET:363） // :3164
      era.set(`cflag:${target}:363`, 1); // :3164
      return 0; // :3165

    } else { // :3167

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:363`) <= 4 || era.get('flag:7') == 2)) { // :3169
        await era.printAndWait(''); // :3170
        // CFLAG:363  = 5（变量语义：CFLAG 族，363） // :3171
        era.set(`cflag:${target}:363`, 5); // :3171

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:363`) <= 3 || era.get('flag:7') == 2)) { // :3173
        await era.printAndWait(''); // :3174
        // CFLAG:363  = 4（变量语义：CFLAG 族，363） // :3175
        era.set(`cflag:${target}:363`, 4); // :3175

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:363`) <= 2 || era.get('flag:7') == 2)) { // :3177
        // CFLAG:363  = 3（变量语义：CFLAG 族，363） // :3178
        era.set(`cflag:${target}:363`, 3); // :3178

      } else if (era.get(`cflag:${target}:363`) <= 1 || era.get('flag:7') == 2) { // :3180
        await era.printAndWait(''); // :3181
        // CFLAG:363  = 2（变量语义：CFLAG 族，363） // :3182
        era.set(`cflag:${target}:363`, 2); // :3182
      } // :3183
      return 0; // :3184
    } // :3185
  } // :3186




  if (era_flag.selectcom == 69) { // :3191

    if (era.get(`cflag:${target}:364`) == 0) { // :3193

      if (era.get(`talent:${target}:76`) == 1) { // :3195
        await era.printAndWait(''); // :3196

      } else if (era.get(`talent:${target}:85`) == 1) { // :3198
        await era.printAndWait(''); // :3199

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3201
        await era.printAndWait(''); // :3202

      } else { // :3204
        await era.printAndWait(''); // :3205
      } // :3206
      // CFLAG:TARGET:364  = 1（变量语义：CFLAG 族，TARGET:364） // :3207
      era.set(`cflag:${target}:364`, 1); // :3207
      return 0; // :3208

    } else { // :3210

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:364`) <= 4 || era.get('flag:7') == 2)) { // :3212
        await era.printAndWait(''); // :3213
        // CFLAG:364  = 5（变量语义：CFLAG 族，364） // :3214
        era.set(`cflag:${target}:364`, 5); // :3214

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:364`) <= 3 || era.get('flag:7') == 2)) { // :3216
        await era.printAndWait(''); // :3217
        // CFLAG:364  = 4（变量语义：CFLAG 族，364） // :3218
        era.set(`cflag:${target}:364`, 4); // :3218

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:364`) <= 2 || era.get('flag:7') == 2)) { // :3220
        await era.printAndWait(''); // :3221
        // CFLAG:364  = 3（变量语义：CFLAG 族，364） // :3222
        era.set(`cflag:${target}:364`, 3); // :3222

      } else if (era.get(`cflag:${target}:364`) <= 1 || era.get('flag:7') == 2) { // :3224
        await era.printAndWait(''); // :3225
        // CFLAG:364  = 2（变量语义：CFLAG 族，364） // :3226
        era.set(`cflag:${target}:364`, 2); // :3226
      } // :3227
      return 0; // :3228
    } // :3229
  } // :3230




  if (era_flag.selectcom == 124) { // :3235

    if (era.get(`cflag:${target}:365`) == 0) { // :3237

      if (era.get(`talent:${target}:76`) == 1) { // :3239
        await era.printAndWait(''); // :3240

      } else if (era.get(`talent:${target}:85`) == 1) { // :3242
        await era.printAndWait(''); // :3243

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3245
        await era.printAndWait(''); // :3246

      } else { // :3248
        await era.printAndWait(''); // :3249
      } // :3250
      // CFLAG:TARGET:365  = 1（变量语义：CFLAG 族，TARGET:365） // :3251
      era.set(`cflag:${target}:365`, 1); // :3251
      return 0; // :3252

    } else { // :3254

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:363`) <= 4 || era.get('flag:7') == 2)) { // :3256
        await era.printAndWait(''); // :3257
        // CFLAG:365  = 5（变量语义：CFLAG 族，365） // :3258
        era.set(`cflag:${target}:365`, 5); // :3258

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:363`) <= 3 || era.get('flag:7') == 2)) { // :3260
        await era.printAndWait(''); // :3261
        // CFLAG:365  = 4（变量语义：CFLAG 族，365） // :3262
        era.set(`cflag:${target}:365`, 4); // :3262

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:363`) <= 2 || era.get('flag:7') == 2)) { // :3264
        await era.printAndWait(''); // :3265
        // CFLAG:365  = 3（变量语义：CFLAG 族，365） // :3266
        era.set(`cflag:${target}:365`, 3); // :3266

      } else if (era.get(`cflag:${target}:363`) <= 1 || era.get('flag:7') == 2) { // :3268
        await era.printAndWait(''); // :3269
        // CFLAG:365  = 2（变量语义：CFLAG 族，365） // :3270
        era.set(`cflag:${target}:365`, 2); // :3270
      } // :3271
      return 0; // :3272
    } // :3273
  } // :3274




  if (era_flag.selectcom == 80) { // :3279

    if (era.get(`cflag:${target}:381`) == 0) { // :3281

      if (era.get(`talent:${target}:76`) == 1) { // :3283
        await era.printAndWait(''); // :3284

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3286
        await era.printAndWait(''); // :3287

      } else { // :3289
        await era.printAndWait(''); // :3290
      } // :3291
      // CFLAG:TARGET:381  = 1（变量语义：CFLAG 族，TARGET:381） // :3292
      era.set(`cflag:${target}:381`, 1); // :3292
      return 0; // :3293

    } else { // :3295

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:381`) <= 4 || era.get('flag:7') == 2)) { // :3297
        await era.printAndWait(''); // :3298
        // CFLAG:381  = 5（变量语义：CFLAG 族，381） // :3299
        era.set(`cflag:${target}:381`, 5); // :3299

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:381`) <= 3 || era.get('flag:7') == 2)) { // :3301
        await era.printAndWait(''); // :3302
        // CFLAG:381  = 4（变量语义：CFLAG 族，381） // :3303
        era.set(`cflag:${target}:381`, 4); // :3303

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:381`) <= 2 || era.get('flag:7') == 2)) { // :3305
        await era.printAndWait(''); // :3306
        // CFLAG:381  = 3（变量语义：CFLAG 族，381） // :3307
        era.set(`cflag:${target}:381`, 3); // :3307

      } else if (era.get(`cflag:${target}:381`) <= 1 || era.get('flag:7') == 2) { // :3309
        await era.printAndWait(''); // :3310
        // CFLAG:381  = 2（变量语义：CFLAG 族，381） // :3311
        era.set(`cflag:${target}:381`, 2); // :3311
      } // :3312
      return 0; // :3313
    } // :3314
  } // :3315






  if (era_flag.selectcom == 87) { // :3322


    if (era.get(`cflag:${target}:348`) == 0) { // :3325

      if (era_flag.assi > 0 && era_flag.assiplay) { // :3327
        await era.printAndWait(''); // :3328

      } else if (era.get(`talent:${target}:76`) == 1) { // :3330

        if (era.get(`cflag:${target}:7`) & P) { // :3332
          await era.printAndWait(''); // :3333

          if (P == 1) { // :3335
            await era.printAndWait(''); // :3336

          } else if (P == 2) { // :3338
            await era.printAndWait(''); // :3339

          } else if (P == 4) { // :3341
            await era.printAndWait(''); // :3342

          } else if (P == 8) { // :3344

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3346
              await era.printAndWait(''); // :3347
            } else { // :3348
              await era.printAndWait(''); // :3349
            } // :3350

          } else if (P == 16) { // :3352
            await era.printAndWait(''); // :3353

          } else if (P == 32) { // :3355
            await era.printAndWait(''); // :3356

          } else if (P == 64) { // :3358
            await era.printAndWait(''); // :3359
          } // :3360

        } else { // :3362
          await era.printAndWait(''); // :3363
        } // :3364

      } else if (era.get(`talent:${target}:85`) == 1) { // :3366

        if (era.get(`cflag:${target}:7`) & P) { // :3368
          await era.printAndWait(''); // :3369

          if (P == 1) { // :3371
            await era.printAndWait(''); // :3372

          } else if (P == 2) { // :3374
            await era.printAndWait(''); // :3375

          } else if (P == 4) { // :3377
            await era.printAndWait(''); // :3378

          } else if (P == 8) { // :3380

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3382
              await era.printAndWait(''); // :3383
            } else { // :3384
              await era.printAndWait(''); // :3385
            } // :3386

          } else if (P == 16) { // :3388
            await era.printAndWait(''); // :3389

          } else if (P == 32) { // :3391
            await era.printAndWait(''); // :3392

          } else if (P == 64) { // :3394
            await era.printAndWait(''); // :3395
          } // :3396

        } else { // :3398
          await era.printAndWait(''); // :3399
        } // :3400

      } else { // :3402

        if (era.get(`cflag:${target}:7`) & P) { // :3404
          await era.printAndWait(''); // :3405

          if (P == 1) { // :3407
            await era.printAndWait(''); // :3408

          } else if (P == 2) { // :3410
            await era.printAndWait(''); // :3411

          } else if (P == 4) { // :3413
            await era.printAndWait(''); // :3414

          } else if (P == 8) { // :3416

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3418
              await era.printAndWait(''); // :3419
            } else { // :3420
              await era.printAndWait(''); // :3421
            } // :3422

          } else if (P == 16) { // :3424
            await era.printAndWait(''); // :3425

          } else if (P == 32) { // :3427
            await era.printAndWait(''); // :3428

          } else if (P == 64) { // :3430
            await era.printAndWait(''); // :3431
          } // :3432

        } else { // :3434
          await era.printAndWait(''); // :3435
        } // :3436
      } // :3437
      // CFLAG:TARGET:348  = 1（变量语义：CFLAG 族，TARGET:348） // :3438
      era.set(`cflag:${target}:348`, 1); // :3438
      return 0; // :3439

    } else { // :3441

      if (era_flag.assi > 0 && era_flag.assiplay) { // :3443
        await era.printAndWait(''); // :3444

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:348`) <= 3 || era.get('flag:7') == 2)) { // :3446

        if (era.get(`cflag:${target}:7`) & P) { // :3448

          if (P == 1) { // :3450
            await era.printAndWait(''); // :3451

          } else if (P == 2) { // :3453
            await era.printAndWait(''); // :3454

          } else if (P == 4) { // :3456
            await era.printAndWait(''); // :3457

          } else if (P == 8) { // :3459

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3461
              await era.printAndWait(''); // :3462
            } else { // :3463
              await era.printAndWait(''); // :3464
            } // :3465

          } else if (P == 16) { // :3467
            await era.printAndWait(''); // :3468

          } else if (P == 32) { // :3470
            await era.printAndWait(''); // :3471

          } else if (P == 64) { // :3473
            await era.printAndWait(''); // :3474
          } // :3475

        } else { // :3477
          await era.printAndWait(''); // :3478
        } // :3479
        // CFLAG:348  = 4（变量语义：CFLAG 族，348） // :3480
        era.set(`cflag:${target}:348`, 4); // :3480

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:348`) <= 2 || era.get('flag:7') == 2)) { // :3482

        if (era.get(`cflag:${target}:7`) & P) { // :3484

          if (P == 1) { // :3486
            await era.printAndWait(''); // :3487

          } else if (P == 2) { // :3489
            await era.printAndWait(''); // :3490

          } else if (P == 4) { // :3492
            await era.printAndWait(''); // :3493

          } else if (P == 8) { // :3495
            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3496
              await era.printAndWait(''); // :3497
            } else { // :3498
              await era.printAndWait(''); // :3499
            } // :3500

          } else if (P == 16) { // :3502
            await era.printAndWait(''); // :3503

          } else if (P == 32) { // :3505
            await era.printAndWait(''); // :3506

          } else if (P == 64) { // :3508
            await era.printAndWait(''); // :3509
          } // :3510

        } else { // :3512
          await era.printAndWait(''); // :3513
        } // :3514
        // CFLAG:348  = 3（变量语义：CFLAG 族，348） // :3515
        era.set(`cflag:${target}:348`, 3); // :3515

      } else if (era.get(`cflag:${target}:348`) <= 1 || era.get('flag:7') == 2) { // :3517

        if (era.get(`cflag:${target}:7`) & P) { // :3519

          if (P == 1) { // :3521
            await era.printAndWait(''); // :3522

          } else if (P == 2) { // :3524
            await era.printAndWait(''); // :3525

          } else if (P == 4) { // :3527
            await era.printAndWait(''); // :3528

          } else if (P == 8) { // :3530

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3532
              await era.printAndWait(''); // :3533
            } else { // :3534
              await era.printAndWait(''); // :3535
            } // :3536

          } else if (P == 16) { // :3538
            await era.printAndWait(''); // :3539

          } else if (P == 32) { // :3541
            await era.printAndWait(''); // :3542

          } else if (P == 64) { // :3544
            await era.printAndWait(''); // :3545
          } // :3546

        } else { // :3548
          await era.printAndWait(''); // :3549
        } // :3550
        // CFLAG:348  = 2（变量语义：CFLAG 族，348） // :3551
        era.set(`cflag:${target}:348`, 2); // :3551
      } // :3552
    } // :3553
    return 0; // :3554
  } // :3555







}

// @DOG_KOJO_14 // :3563
async function DOG_KOJO_14() {




  if (era_flag.selectcom == 0) { // :3568

    if (era.get(`cflag:${target}:301`) == 0) { // :3570

      if (era.get(`mark:${target}:2`) >= 2) { // :3572
        await era.printAndWait(''); // :3573

      } else { // :3575
        await era.printAndWait(''); // :3576
      } // :3577
      // CFLAG:301  = 1（变量语义：CFLAG 族，301） // :3578
      era.set(`cflag:${target}:301`, 1); // :3578
      return 0; // :3579

    } else { // :3581

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:301`) <= 6 || era.get('flag:7') == 2)) { // :3583
        await era.printAndWait(''); // :3584
        // CFLAG:301  = 7（变量语义：CFLAG 族，301） // :3585
        era.set(`cflag:${target}:301`, 7); // :3585

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:301`) <= 5 || era.get('flag:7') == 2)) { // :3587
        await era.printAndWait(''); // :3588
        // CFLAG:301  = 6（变量语义：CFLAG 族，301） // :3589
        era.set(`cflag:${target}:301`, 6); // :3589

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:301`) <= 4 || era.get('flag:7') == 2)) { // :3591
        await era.printAndWait(''); // :3592
        // CFLAG:301  = 5（变量语义：CFLAG 族，301） // :3593
        era.set(`cflag:${target}:301`, 5); // :3593

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:301`) <= 3 || era.get('flag:7') == 2)) { // :3595
        await era.printAndWait(''); // :3596
        // CFLAG:301  = 4（变量语义：CFLAG 族，301） // :3597
        era.set(`cflag:${target}:301`, 4); // :3597

      } else if (era.get(`mark:${target}:2`) == 2 && (era.get(`cflag:${target}:301`) <= 2 || era.get('flag:7') == 2)) { // :3599
        await era.printAndWait(''); // :3600
        // CFLAG:301  = 3（变量语义：CFLAG 族，301） // :3601
        era.set(`cflag:${target}:301`, 3); // :3601

      } else if (era.get(`mark:${target}:2`) <= 1 && (era.get(`cflag:${target}:301`) <= 1 || era.get('flag:7') == 2)) { // :3603
        await era.printAndWait(''); // :3604
        // CFLAG:301  = 2（变量语义：CFLAG 族，301） // :3605
        era.set(`cflag:${target}:301`, 2); // :3605
      } // :3606
      return 0; // :3607
    } // :3608
  } // :3609




  if (era_flag.selectcom == 1) { // :3614

    if (era.get(`cflag:${target}:302`) == 0) { // :3616

      if (era.get(`talent:${target}:0`) == 1) { // :3618
        await era.printAndWait(''); // :3619

      } else { // :3621
        await era.printAndWait(''); // :3622
      } // :3623
      // CFLAG:302  = 1（变量语义：CFLAG 族，302） // :3624
      era.set(`cflag:${target}:302`, 1); // :3624
      return 0; // :3625

    } else { // :3627

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:302`) <= 5 || era.get('flag:7') == 2)) { // :3629
        await era.printAndWait(''); // :3630
        // CFLAG:302  = 6（变量语义：CFLAG 族，302） // :3631
        era.set(`cflag:${target}:302`, 6); // :3631

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:302`) <= 4 || era.get('flag:7') == 2)) { // :3633
        await era.printAndWait(''); // :3634
        // CFLAG:302  = 5（变量语义：CFLAG 族，302） // :3635
        era.set(`cflag:${target}:302`, 5); // :3635

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:302`) <= 3 || era.get('flag:7') == 2)) { // :3637
        await era.printAndWait(''); // :3638
        // CFLAG:302  = 4（变量语义：CFLAG 族，302） // :3639
        era.set(`cflag:${target}:302`, 4); // :3639

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:302`) <= 2 || era.get('flag:7') == 2)) { // :3641
        await era.printAndWait(''); // :3642
        // CFLAG:302  = 3（变量语义：CFLAG 族，302） // :3643
        era.set(`cflag:${target}:302`, 3); // :3643

      } else if (era.get(`cflag:${target}:302`) <= 1 || era.get('flag:7') == 2) { // :3645
        await era.printAndWait(''); // :3646
        // CFLAG:302  = 2（变量语义：CFLAG 族，302） // :3647
        era.set(`cflag:${target}:302`, 2); // :3647
      } // :3648
      return 0; // :3649
    } // :3650
  } // :3651





  if (era_flag.selectcom == 5) { // :3657

    if (era.get(`cflag:${target}:306`) == 0) { // :3659

      if (era.get(`talent:${target}:85`) == 1) { // :3661
        await era.printAndWait(''); // :3662

      } else { // :3664
        await era.printAndWait(''); // :3665
      } // :3666
      // CFLAG:TARGET:306  = 1（变量语义：CFLAG 族，TARGET:306） // :3667
      era.set(`cflag:${target}:306`, 1); // :3667
      return 0; // :3668

    } else { // :3670

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:306`) <= 5 || era.get('flag:7') == 2)) { // :3672
        await era.printAndWait(''); // :3673
        // CFLAG:306  = 6（变量语义：CFLAG 族，306） // :3674
        era.set(`cflag:${target}:306`, 6); // :3674

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:306`) <= 4 || era.get('flag:7') == 2)) { // :3676
        await era.printAndWait(''); // :3677
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :3678
        era.set(`cflag:${target}:306`, 5); // :3678

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:306`) <= 3 || era.get('flag:7') == 2)) { // :3680
        await era.printAndWait(''); // :3681
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :3682
        era.set(`cflag:${target}:306`, 4); // :3682

      } else if (era.get(`abl:${target}:1`) >= 3 && (era.get(`cflag:${target}:306`) <= 2 || era.get('flag:7') == 2)) { // :3684
        await era.printAndWait(''); // :3685
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :3686
        era.set(`cflag:${target}:306`, 3); // :3686

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :3688
        await era.printAndWait(''); // :3689
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :3690
        era.set(`cflag:${target}:306`, 2); // :3690
      } // :3691
      return 0; // :3692
    } // :3693
  } // :3694




  if (era_flag.selectcom == 6) { // :3699

    if (era.get(`cflag:${target}:307`) == 0 && era.get('tflag:13')) { // :3701

      if (era.get(`talent:${target}:136`) == 1) { // :3703
        await era.printAndWait(''); // :3704

      } else if (era.get(`talent:${target}:76`) == 1) { // :3706
        await era.printAndWait(''); // :3707

      } else if (era.get(`talent:${target}:85`) == 1) { // :3709
        await era.printAndWait(''); // :3710

      } else { // :3712
        await era.printAndWait(''); // :3713
      } // :3714
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :3715
      era.set(`cflag:${target}:307`, 1); // :3715
      return 0; // :3716

    } else if (era.get(`cflag:${target}:307`) == 0) { // :3718

      if (era.get(`talent:${target}:136`) == 1) { // :3720
        await era.printAndWait(''); // :3721

      } else if (era.get(`talent:${target}:76`) == 1) { // :3723
        await era.printAndWait(''); // :3724

      } else if (era.get(`talent:${target}:85`) == 1) { // :3726
        await era.printAndWait(''); // :3727

      } else { // :3729
        await era.printAndWait(''); // :3730
      } // :3731
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :3732
      era.set(`cflag:${target}:307`, 1); // :3732
      return 0; // :3733

    } else { // :3735

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:307`) <= 5 || era.get('flag:7') == 2)) { // :3737
        await era.printAndWait(''); // :3738
        // CFLAG:307  = 6（变量语义：CFLAG 族，307） // :3739
        era.set(`cflag:${target}:307`, 6); // :3739

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:307`) <= 4 || era.get('flag:7') == 2)) { // :3741
        await era.printAndWait(''); // :3742
        // CFLAG:307  = 5（变量语义：CFLAG 族，307） // :3743
        era.set(`cflag:${target}:307`, 5); // :3743

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:307`) <= 3 || era.get('flag:7') == 2)) { // :3745
        await era.printAndWait(''); // :3746
        // CFLAG:307  = 4（变量语义：CFLAG 族，307） // :3747
        era.set(`cflag:${target}:307`, 4); // :3747

      } else if (era.get(`abl:${target}:10`) >=2 && (era.get(`cflag:${target}:307`) <= 2 || era.get('flag:7') == 2)) { // :3749
        await era.printAndWait(''); // :3750
        // CFLAG:307  = 3（变量语义：CFLAG 族，307） // :3751
        era.set(`cflag:${target}:307`, 3); // :3751

      } else if (era.get(`cflag:${target}:307`) <= 1 || era.get('flag:7') == 2) { // :3753
        await era.printAndWait(''); // :3754
        // CFLAG:307  = 2（变量语义：CFLAG 族，307） // :3755
        era.set(`cflag:${target}:307`, 2); // :3755
      } // :3756
      return 0; // :3757
    } // :3758
  } // :3759




  if (era_flag.selectcom == 9) { // :3764

    if (era.get(`cflag:${target}:310`) == 0) { // :3766

      if (era.get(`talent:${target}:136`) == 1) { // :3768
        await era.printAndWait(''); // :3769

      } else if (era.get(`talent:${target}:76`) == 1) { // :3771
        await era.printAndWait(''); // :3772

      } else if (era.get(`talent:${target}:85`) == 1) { // :3774
        await era.printAndWait(''); // :3775

      } else { // :3777
        await era.printAndWait(''); // :3778
      } // :3779
      // CFLAG:TARGET:310  = 1（变量语义：CFLAG 族，TARGET:310） // :3780
      era.set(`cflag:${target}:310`, 1); // :3780
      return 0; // :3781

    } else { // :3783

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:310`) <= 5 || era.get('flag:7') == 2)) { // :3785
        await era.printAndWait(''); // :3786
        // CFLAG:310  = 6（变量语义：CFLAG 族，310） // :3787
        era.set(`cflag:${target}:310`, 6); // :3787

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:310`) <= 4 || era.get('flag:7') == 2)) { // :3789
        await era.printAndWait(''); // :3790
        // CFLAG:310  = 5（变量语义：CFLAG 族，310） // :3791
        era.set(`cflag:${target}:310`, 5); // :3791

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:310`) <= 3 || era.get('flag:7') == 2)) { // :3793
        await era.printAndWait(''); // :3794
        // CFLAG:310  = 4（变量语义：CFLAG 族，310） // :3795
        era.set(`cflag:${target}:310`, 4); // :3795

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:310`) <= 2 || era.get('flag:7') == 2)) { // :3797
        await era.printAndWait(''); // :3798
        // CFLAG:310  = 3（变量语义：CFLAG 族，310） // :3799
        era.set(`cflag:${target}:310`, 3); // :3799

      } else if (era.get(`cflag:${target}:310`) <= 1 || era.get('flag:7') == 2) { // :3801
        await era.printAndWait(''); // :3802
        // CFLAG:310  = 2（变量语义：CFLAG 族，310） // :3803
        era.set(`cflag:${target}:310`, 2); // :3803
      } // :3804
      return 0; // :3805
    } // :3806
  } // :3807




  if (era_flag.selectcom == 21) { // :3812

    if (era.get(`cflag:${target}:322`) == 0) { // :3814

      if (era.get(`talent:${target}:0`) == 1) { // :3816

        if (era.get(`talent:${target}:136`) == 1) { // :3818
          await era.printAndWait(''); // :3819

        } else if (era.get(`talent:${target}:76`) == 1) { // :3821
          await era.printAndWait(''); // :3822

        } else if (era.get(`talent:${target}:85`) == 1) { // :3824
          await era.printAndWait(''); // :3825


        } else { // :3828
          await era.printAndWait(''); // :3829
        } // :3830

      } else { // :3832

        if (era.get(`talent:${target}:136`) == 1) { // :3834
          await era.printAndWait(''); // :3835

        } else if (era.get(`talent:${target}:76`) == 1) { // :3837
          await era.printAndWait(''); // :3838

        } else if (era.get(`talent:${target}:85`) == 1) { // :3840
          await era.printAndWait(''); // :3841

        } else { // :3843
          await era.printAndWait(''); // :3844
        } // :3845
      } // :3846
      // CFLAG:322  = 1（变量语义：CFLAG 族，322） // :3847
      era.set(`cflag:${target}:322`, 1); // :3847
      return 0; // :3848

    } else { // :3850

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:322`) <= 6 || era.get('flag:7') == 2)) { // :3852
        if (rand_n(3) == 0) { // :3853
          await era.printAndWait(''); // :3854
        } else if (rand_n(2) == 0) { // :3855
          await era.printAndWait(''); // :3856
        } else { // :3857
          await era.printAndWait(''); // :3858
        } // :3859
        // CFLAG:322  = 7（变量语义：CFLAG 族，322） // :3860
        era.set(`cflag:${target}:322`, 7); // :3860

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:322`) <= 5 || era.get('flag:7') == 2)) { // :3862
        if (rand_n(3) == 0) { // :3863
          await era.printAndWait(''); // :3864
        } else if (rand_n(2) == 0) { // :3865
          await era.printAndWait(''); // :3866
        } else { // :3867
          await era.printAndWait(''); // :3868
        } // :3869
        // CFLAG:322  = 6（变量语义：CFLAG 族，322） // :3870
        era.set(`cflag:${target}:322`, 6); // :3870

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:322`) <= 4 || era.get('flag:7') == 2)) { // :3872
        if (rand_n(3) == 0) { // :3873
          await era.printAndWait(''); // :3874
        } else if (rand_n(2) == 0) { // :3875
          await era.printAndWait(''); // :3876
        } else { // :3877
          await era.printAndWait(''); // :3878
        } // :3879
        // CFLAG:322  = 5（变量语义：CFLAG 族，322） // :3880
        era.set(`cflag:${target}:322`, 5); // :3880

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:322`) <= 3 || era.get('flag:7') == 2)) { // :3882
        await era.printAndWait(''); // :3883
        // CFLAG:322  = 4（变量语义：CFLAG 族，322） // :3884
        era.set(`cflag:${target}:322`, 4); // :3884

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:322`) <= 2 || era.get('flag:7') == 2)) { // :3886
        await era.printAndWait(''); // :3887
        // CFLAG:322  = 3（变量语义：CFLAG 族，322） // :3888
        era.set(`cflag:${target}:322`, 3); // :3888

      } else if (era.get(`cflag:${target}:322`) <= 1 || era.get('flag:7') == 2) { // :3890
        await era.printAndWait(''); // :3891

        // CFLAG:322  = 2（变量语义：CFLAG 族，322） // :3893
        era.set(`cflag:${target}:322`, 2); // :3893
      } // :3894
      return 0; // :3895
    } // :3896
  } // :3897




  if (era_flag.selectcom == 27) { // :3902

    if (era.get(`cflag:${target}:328`) == 0) { // :3904

      if (era.get(`talent:${target}:136`) == 1) { // :3906
        await era.printAndWait(''); // :3907

      } else if (era.get(`talent:${target}:76`) == 1) { // :3909
        await era.printAndWait(''); // :3910

      } else if (era.get(`talent:${target}:85`) == 1) { // :3912
        await era.printAndWait(''); // :3913

      } else { // :3915
        await era.printAndWait(''); // :3916
      } // :3917
      // CFLAG:TARGET:328  = 1（变量语义：CFLAG 族，TARGET:328） // :3918
      era.set(`cflag:${target}:328`, 1); // :3918
      return 0; // :3919

    } else { // :3921

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 6 || era.get('flag:7') == 2)) { // :3923
        if (rand_n(2) == 0) { // :3924
          await era.printAndWait(''); // :3925
        } else { // :3926
          await era.printAndWait(''); // :3927
        } // :3928
        // CFLAG:328  = 7（变量语义：CFLAG 族，328） // :3929
        era.set(`cflag:${target}:328`, 7); // :3929

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 5 || era.get('flag:7') == 2)) { // :3931
        if (rand_n(2) == 0) { // :3932
          await era.printAndWait(''); // :3933
        } else { // :3934
          await era.printAndWait(''); // :3935
        } // :3936
        // CFLAG:328  = 6（变量语义：CFLAG 族，328） // :3937
        era.set(`cflag:${target}:328`, 6); // :3937

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 4 || era.get('flag:7') == 2)) { // :3939
        if (rand_n(2) == 0) { // :3940
          await era.printAndWait(''); // :3941
        } else { // :3942
          await era.printAndWait(''); // :3943
        } // :3944
        // CFLAG:328  = 5（变量语义：CFLAG 族，328） // :3945
        era.set(`cflag:${target}:328`, 5); // :3945

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:328`) <= 3 || era.get('flag:7') == 2)) { // :3947
        await era.printAndWait(''); // :3948
        // CFLAG:328  = 4（变量语义：CFLAG 族，328） // :3949
        era.set(`cflag:${target}:328`, 4); // :3949

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 2 || era.get('flag:7') == 2)) { // :3951
        await era.printAndWait(''); // :3952
        // CFLAG:328  = 3（变量语义：CFLAG 族，328） // :3953
        era.set(`cflag:${target}:328`, 3); // :3953

      } else if (era.get(`cflag:${target}:328`) <= 1 || era.get('flag:7') == 2) { // :3955
        await era.printAndWait(''); // :3956
        // CFLAG:328  = 2（变量语义：CFLAG 族，328） // :3957
        era.set(`cflag:${target}:328`, 2); // :3957
      } // :3958
      return 0; // :3959
    } // :3960
  } // :3961




  if (era_flag.selectcom == 30) { // :3966

    if (era.get(`cflag:${target}:331`) == 0) { // :3968

      if (era.get(`talent:${target}:76`) == 1) { // :3970
        await era.printAndWait(''); // :3971

      } else if (era.get(`talent:${target}:85`) == 1) { // :3973
        await era.printAndWait(''); // :3974

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3976
        await era.printAndWait(''); // :3977

      } else { // :3979
        await era.printAndWait(''); // :3980
      } // :3981
      // CFLAG:TARGET:331  = 1（变量语义：CFLAG 族，TARGET:331） // :3982
      era.set(`cflag:${target}:331`, 1); // :3982
      return 0; // :3983

    } else { // :3985

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 6 || era.get('flag:7') == 2)) { // :3987
        if (rand_n(2) == 0) { // :3988
          await era.printAndWait(''); // :3989
        } else { // :3990
          await era.printAndWait(''); // :3991
        } // :3992
        // CFLAG:331  = 7（变量语义：CFLAG 族，331） // :3993
        era.set(`cflag:${target}:331`, 7); // :3993

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 5 || era.get('flag:7') == 2)) { // :3995
        if (rand_n(2) == 0) { // :3996
          await era.printAndWait(''); // :3997
        } else { // :3998
          await era.printAndWait(''); // :3999
        } // :4000
        // CFLAG:331  = 6（变量语义：CFLAG 族，331） // :4001
        era.set(`cflag:${target}:331`, 6); // :4001

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:331`) <= 4 || era.get('flag:7') == 2)) { // :4003
        if (rand_n(2) == 0) { // :4004
          await era.printAndWait(''); // :4005
        } else { // :4006
          await era.printAndWait(''); // :4007
        } // :4008
        // CFLAG:331  = 5（变量语义：CFLAG 族，331） // :4009
        era.set(`cflag:${target}:331`, 5); // :4009

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 3 || era.get('flag:7') == 2)) { // :4011
        await era.printAndWait(''); // :4012
        // CFLAG:331  = 4（变量语义：CFLAG 族，331） // :4013
        era.set(`cflag:${target}:331`, 4); // :4013

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 2 || era.get('flag:7') == 2)) { // :4015
        await era.printAndWait(''); // :4016
        // CFLAG:331  = 3（变量语义：CFLAG 族，331） // :4017
        era.set(`cflag:${target}:331`, 3); // :4017

      } else if (era.get(`cflag:${target}:331`) <= 1 || era.get('flag:7') == 2) { // :4019
        await era.printAndWait(''); // :4020
        // CFLAG:331  = 2（变量语义：CFLAG 族，331） // :4021
        era.set(`cflag:${target}:331`, 2); // :4021
      } // :4022
      return 0; // :4023
    } // :4024
  } // :4025




  if (era_flag.selectcom == 31) { // :4030

    if (era.get(`cflag:${target}:332`) == 0) { // :4032

      if (era.get(`talent:${target}:76`) == 1) { // :4034
        await era.printAndWait(''); // :4035

      } else if (era.get(`talent:${target}:85`) == 1) { // :4037
        await era.printAndWait(''); // :4038

      } else if (era.get(`abl:${target}:16`) >= 3) { // :4040
        await era.printAndWait(''); // :4041

      } else { // :4043
        await era.printAndWait(''); // :4044
      } // :4045
      // CFLAG:TARGET:332  = 1（变量语义：CFLAG 族，TARGET:332） // :4046
      era.set(`cflag:${target}:332`, 1); // :4046
      return 0; // :4047

    } else { // :4049

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 6 || era.get('flag:7') == 2)) { // :4051
        await era.printAndWait(''); // :4052
        // CFLAG:332  = 7（变量语义：CFLAG 族，332） // :4053
        era.set(`cflag:${target}:332`, 7); // :4053

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 5 || era.get('flag:7') == 2)) { // :4055
        await era.printAndWait(''); // :4056
        // CFLAG:332  = 6（变量语义：CFLAG 族，332） // :4057
        era.set(`cflag:${target}:332`, 6); // :4057

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 4 || era.get('flag:7') == 2)) { // :4059
        await era.printAndWait(''); // :4060
        // CFLAG:332  = 5（变量语义：CFLAG 族，332） // :4061
        era.set(`cflag:${target}:332`, 5); // :4061

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :4063
        await era.print(''); // :4064
        await era.printAndWait(''); // :4065
        // CFLAG:332  = 4（变量语义：CFLAG 族，332） // :4066
        era.set(`cflag:${target}:332`, 4); // :4066

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:332`) <= 2 || era.get('flag:7') == 2)) { // :4068
        await era.print(''); // :4069
        await era.printAndWait(''); // :4070
        // CFLAG:332  = 3（变量语义：CFLAG 族，332） // :4071
        era.set(`cflag:${target}:332`, 3); // :4071

      } else if (era.get(`cflag:${target}:332`) <= 1 || era.get('flag:7') == 2) { // :4073
        await era.printAndWait(''); // :4074
        // CFLAG:332  = 2（变量语义：CFLAG 族，332） // :4075
        era.set(`cflag:${target}:332`, 2); // :4075
      } // :4076
      return 0; // :4077
    } // :4078
  } // :4079




  if (era_flag.selectcom == 34) { // :4084

    if (era.get(`cflag:${target}:335`) == 0) { // :4086

      if (era.get(`talent:${target}:0`) == 1) { // :4088

        if (era.get(`talent:${target}:136`) == 1) { // :4090
          await era.printAndWait(''); // :4091

        } else if (era.get(`talent:${target}:76`) == 1) { // :4093
          await era.printAndWait(''); // :4094

        } else if (era.get(`talent:${target}:85`) == 1) { // :4096
          await era.printAndWait(''); // :4097

        } else { // :4099
          await era.printAndWait(''); // :4100
        } // :4101

      } else { // :4103

        if (era.get(`talent:${target}:136`) == 1) { // :4105
          await era.printAndWait(''); // :4106

        } else if (era.get(`talent:${target}:76`) == 1) { // :4108
          await era.printAndWait(''); // :4109

        } else if (era.get(`talent:${target}:85`) == 1) { // :4111
          await era.printAndWait(''); // :4112

        } else { // :4114
          await era.printAndWait(''); // :4115
        } // :4116
      } // :4117
      // CFLAG:TARGET:335  = 1（变量语义：CFLAG 族，TARGET:335） // :4118
      era.set(`cflag:${target}:335`, 1); // :4118
      return 0; // :4119

    } else { // :4121

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:335`) <= 6 || era.get('flag:7') == 2)) { // :4123
        if (rand_n(3) == 0) { // :4124
          await era.printAndWait(''); // :4125
        } else if (rand_n(2) == 0) { // :4126
          await era.printAndWait(''); // :4127
        } else { // :4128
          await era.printAndWait(''); // :4129
        } // :4130
        // CFLAG:335  = 7（变量语义：CFLAG 族，335） // :4131
        era.set(`cflag:${target}:335`, 7); // :4131

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:335`) <= 5 || era.get('flag:7') == 2)) { // :4133
        if (rand_n(4) == 0) { // :4134
          await era.printAndWait(''); // :4135
        } else if (rand_n(3) == 0) { // :4136
          await era.printAndWait(''); // :4137
        } else if (rand_n(2) == 0) { // :4138
          await era.printAndWait(''); // :4139
        } else { // :4140
          await era.printAndWait(''); // :4141
        } // :4142
        // CFLAG:335  = 6（变量语义：CFLAG 族，335） // :4143
        era.set(`cflag:${target}:335`, 6); // :4143

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:335`) <= 4 || era.get('flag:7') == 2)) { // :4145
        if (rand_n(4) == 0) { // :4146
          await era.print(''); // :4147
        } else if (rand_n(3) == 0) { // :4148
          await era.printAndWait(''); // :4149
        } else if (rand_n(2) == 0) { // :4150
          await era.printAndWait(''); // :4151
        } else { // :4152
          await era.printAndWait(''); // :4153
        } // :4154
        // CFLAG:335  = 5（变量语义：CFLAG 族，335） // :4155
        era.set(`cflag:${target}:335`, 5); // :4155

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:335`) <= 3 || era.get('flag:7') == 2)) { // :4157
        if (rand_n(4) == 0) { // :4158
          await era.printAndWait(''); // :4159
        } else if (rand_n(3) == 0) { // :4160
          await era.printAndWait(''); // :4161
        } else if (rand_n(2) == 0) { // :4162
          await era.printAndWait(''); // :4163
        } else { // :4164
          await era.printAndWait(''); // :4165
        } // :4166
        // CFLAG:335  = 4（变量语义：CFLAG 族，335） // :4167
        era.set(`cflag:${target}:335`, 4); // :4167

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:335`) <= 2 || era.get('flag:7') == 2)) { // :4169
        await era.print(''); // :4170
        await era.printAndWait(''); // :4171
        // CFLAG:335  = 3（变量语义：CFLAG 族，335） // :4172
        era.set(`cflag:${target}:335`, 3); // :4172

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :4174
        await era.printAndWait(''); // :4175
        // CFLAG:335  = 2（变量语义：CFLAG 族，335） // :4176
        era.set(`cflag:${target}:335`, 2); // :4176
      } // :4177
      return 0; // :4178
    } // :4179
  } // :4180




  if (era_flag.selectcom == 37) { // :4185

    if (era.get(`cflag:${target}:338`) == 0) { // :4187

      if (era.get(`abl:${target}:16`) >= 3) { // :4189
        await era.printAndWait(''); // :4190

      } else { // :4192
        await era.printAndWait(''); // :4193
      } // :4194
      // CFLAG:TARGET:338  = 1（变量语义：CFLAG 族，TARGET:338） // :4195
      era.set(`cflag:${target}:338`, 1); // :4195
      return 0; // :4196

    } else { // :4198

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 5 || era.get('flag:7') == 2)) { // :4200
        await era.printAndWait(''); // :4201
        // CFLAG:338  = 6（变量语义：CFLAG 族，338） // :4202
        era.set(`cflag:${target}:338`, 6); // :4202

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 4 || era.get('flag:7') == 2)) { // :4204
        await era.printAndWait(''); // :4205
        // CFLAG:338  = 5（变量语义：CFLAG 族，338） // :4206
        era.set(`cflag:${target}:338`, 5); // :4206

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 3 || era.get('flag:7') == 2)) { // :4208
        await era.print(''); // :4209
        // CFLAG:338  = 4（变量语义：CFLAG 族，338） // :4210
        era.set(`cflag:${target}:338`, 4); // :4210

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:338`) <= 2 || era.get('flag:7') == 2)) { // :4212
        await era.printAndWait(''); // :4213
        // CFLAG:338  = 3（变量语义：CFLAG 族，338） // :4214
        era.set(`cflag:${target}:338`, 3); // :4214

      } else if (era.get(`cflag:${target}:338`) <= 1 || era.get('flag:7') == 2) { // :4216
        await era.printAndWait(''); // :4217
        // CFLAG:338  = 2（变量语义：CFLAG 族，338） // :4218
        era.set(`cflag:${target}:338`, 2); // :4218
      } // :4219
      return 0; // :4220
    } // :4221
  } // :4222





  if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`)) { // :4228

    if (era.get(`cflag:${target}:344`) == 0) { // :4230

      if (era.get(`talent:${target}:136`) == 1) { // :4232
        await era.printAndWait(''); // :4233

      } else if (era.get(`talent:${target}:76`) == 1) { // :4235
        await era.printAndWait(''); // :4236

      } else if (era.get(`talent:${target}:85`) == 1) { // :4238
        await era.printAndWait(''); // :4239

      } else { // :4241
        await era.printAndWait(''); // :4242
      } // :4243
      // CFLAG:TARGET:344  = 1（变量语义：CFLAG 族，TARGET:344） // :4244
      era.set(`cflag:${target}:344`, 1); // :4244
      return 0; // :4245

    } else { // :4247

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:344`) <= 9 || era.get('flag:7') == 2)) { // :4249
        await era.printAndWait(''); // :4250
        // CFLAG:TARGET:344  = 10（变量语义：CFLAG 族，TARGET:344） // :4251
        era.set(`cflag:${target}:344`, 10); // :4251

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 8 || era.get('flag:7') == 2)) { // :4253
        await era.printAndWait(''); // :4254
        // CFLAG:TARGET:344  = 9（变量语义：CFLAG 族，TARGET:344） // :4255
        era.set(`cflag:${target}:344`, 9); // :4255

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 7 || era.get('flag:7') == 2)) { // :4257
        await era.printAndWait(''); // :4258
        // CFLAG:TARGET:344  = 8（变量语义：CFLAG 族，TARGET:344） // :4259
        era.set(`cflag:${target}:344`, 8); // :4259

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:344`) <= 6 || era.get('flag:7') == 2)) { // :4261
        await era.printAndWait(''); // :4262
        // CFLAG:TARGET:344  = 7（变量语义：CFLAG 族，TARGET:344） // :4263
        era.set(`cflag:${target}:344`, 7); // :4263

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 5 || era.get('flag:7') == 2)) { // :4265
        await era.printAndWait(''); // :4266
        // CFLAG:TARGET:344  = 6（变量语义：CFLAG 族，TARGET:344） // :4267
        era.set(`cflag:${target}:344`, 6); // :4267

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 4 || era.get('flag:7') == 2)) { // :4269
        await era.printAndWait(''); // :4270
        // CFLAG:TARGET:344  = 5（变量语义：CFLAG 族，TARGET:344） // :4271
        era.set(`cflag:${target}:344`, 5); // :4271

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:344`) <= 3 || era.get('flag:7') == 2)) { // :4273
        await era.printAndWait(''); // :4274
        // CFLAG:TARGET:344  = 4（变量语义：CFLAG 族，TARGET:344） // :4275
        era.set(`cflag:${target}:344`, 4); // :4275

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 2 || era.get('flag:7') == 2)) { // :4277
        await era.printAndWait(''); // :4278
        // CFLAG:TARGET:344  = 3（变量语义：CFLAG 族，TARGET:344） // :4279
        era.set(`cflag:${target}:344`, 3); // :4279

      } else if (era.get(`cflag:${target}:344`) <= 1 || era.get('flag:7') == 2) { // :4281
        await era.printAndWait(''); // :4282
        // CFLAG:TARGET:344  = 2（变量语义：CFLAG 族，TARGET:344） // :4283
        era.set(`cflag:${target}:344`, 2); // :4283
      } // :4284
      return 0; // :4285
    } // :4286

  } else if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`) == 0) { // :4288

    if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:338`) < 3 || era.get('flag:7') == 2)) { // :4290
      await era.printAndWait(''); // :4291
      // CFLAG:444  = 4（变量语义：CFLAG 族，444） // :4292
      era.set(`cflag:${target}:444`, 4); // :4292

    } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:338`) < 3 || era.get('flag:7') == 2)) { // :4294
      await era.printAndWait(''); // :4295
      // CFLAG:444  = 3（变量语义：CFLAG 族，444） // :4296
      era.set(`cflag:${target}:444`, 3); // :4296

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:338`) < 2 || era.get('flag:7') == 2)) { // :4298
      await era.printAndWait(''); // :4299
      // CFLAG:444  = 2（变量语义：CFLAG 族，444） // :4300
      era.set(`cflag:${target}:444`, 2); // :4300

    } else if (era.get(`cflag:${target}:444`) < 1 || era.get('flag:7') == 2) { // :4302
      await era.printAndWait(''); // :4303
      // CFLAG:444  = 1（变量语义：CFLAG 族，444） // :4304
      era.set(`cflag:${target}:444`, 1); // :4304
    } // :4305
    return 0; // :4306
  } // :4307





  if (era_flag.selectcom == 56) { // :4313

    if (era.get(`cflag:${target}:357`) == 0) { // :4315
      if (era.get(`tequip:${target}:53`)) { // :4316


        if (era.get(`talent:${target}:136`) == 1) { // :4319
          await era.printAndWait(''); // :4320

        } else if (era.get(`talent:${target}:76`) == 1) { // :4322
          await era.printAndWait(''); // :4323

        } else if (era.get(`talent:${target}:85`) == 1) { // :4325
          await era.printAndWait(''); // :4326

        } else { // :4328
          await era.printAndWait(''); // :4329
        } // :4330
      } // :4331
      // CFLAG:357  = 1（变量语义：CFLAG 族，357） // :4332
      era.set(`cflag:${target}:357`, 1); // :4332
      return 0; // :4333

    } else { // :4335
      if (era.get(`tequip:${target}:53`)) { // :4336


        if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:357`) <= 4 || era.get('flag:7') == 2)) { // :4339
          await era.printAndWait(''); // :4340
          // CFLAG:357  = 5（变量语义：CFLAG 族，357） // :4341
          era.set(`cflag:${target}:357`, 5); // :4341

        } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :4343
          await era.printAndWait(''); // :4344
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :4345
          era.set(`cflag:${target}:357`, 4); // :4345

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :4347
          await era.printAndWait(''); // :4348
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :4349
          era.set(`cflag:${target}:357`, 3); // :4349

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :4351
          await era.printAndWait(''); // :4352
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :4353
          era.set(`cflag:${target}:357`, 2); // :4353
        } // :4354
      } // :4355
      return 0; // :4356
    } // :4357
  } // :4358


  return 0; // :4361






}

// @KOJO_MESSAGE_PALAMCNG_14 // :4368
async function KOJO_MESSAGE_PALAMCNG_14() {




  if (era.get(`tequip:${target}:45`)) { // :4374
    return 0; // :4374
  } // :4374

  if (era.get('tflag:899')) { // :4377
    return 0; // :4377
  } // :4377







  // 赋值 P = PALAM:3 + UP:3 // :4385
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:221`) == 0) { // :4386

    if (era.get(`talent:${target}:85`) == 1) { // :4388

      if (era_flag.selectcom == 50) { // :4390
        await era.printAndWait(''); // :4391

      } else { // :4393
        await era.printAndWait(''); // :4394
      } // :4395

    } else { // :4397

      if (era_flag.selectcom == 50) { // :4399
        await era.printAndWait(''); // :4400

      } else { // :4402
        await era.printAndWait(''); // :4403
      } // :4404
    } // :4405
    // CFLAG:TARGET:221  = 1（变量语义：CFLAG 族，TARGET:221） // :4406
    era.set(`cflag:${target}:221`, 1); // :4406
  } // :4407




  // 赋值 P = PALAM:5 + UP:5 // :4412
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:222`) == 0) { // :4413

    if (era.get(`talent:${target}:85`) == 1) { // :4415

      if (era_flag.selectcom == 51) { // :4417
        await era.printAndWait(''); // :4418

      } else { // :4420
        await era.printAndWait(''); // :4421
      } // :4422

    } else { // :4424

      if (era_flag.selectcom == 51) { // :4426
        await era.printAndWait(''); // :4427

      } else { // :4429
        await era.printAndWait(''); // :4430
      } // :4431
    } // :4432
    // CFLAG:222  = 1（变量语义：CFLAG 族，222） // :4433
    era.set(`cflag:${target}:222`, 1); // :4433
  } // :4434




  // 赋值 P = PALAM:8 + UP:8 // :4439
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:223`) == 0) { // :4440

    if (era.get(`talent:${target}:85`) == 1) { // :4442
      await era.printAndWait(''); // :4443

    } else { // :4445
      await era.printAndWait(''); // :4446
    } // :4447
    // CFLAG:223  = 1（变量语义：CFLAG 族，223） // :4448
    era.set(`cflag:${target}:223`, 1); // :4448
  } // :4449




  // 赋值 P = PALAM:10 + UP:10 // :4454
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:224`) == 0) { // :4455

    if (era.get(`talent:${target}:85`) == 1) { // :4457
      await era.printAndWait(''); // :4458

    } else { // :4460
      await era.printAndWait(''); // :4461
    } // :4462
    // CFLAG:224  = 1（变量语义：CFLAG 族，224） // :4463
    era.set(`cflag:${target}:224`, 1); // :4463
  } // :4464




  if (era.get('nowex:0') > 0 && era.get(`cflag:${target}:214`) == 0) { // :4469

    if (era.get(`talent:${target}:85`) == 1) { // :4471
      await era.printAndWait(''); // :4472

    } else { // :4474
      await era.printAndWait(''); // :4475
    } // :4476
    // CFLAG:214  = 1（变量语义：CFLAG 族，214） // :4477
    era.set(`cflag:${target}:214`, 1); // :4477
  } // :4478




  if (era.get('nowex:1') > 0 && era.get(`cflag:${target}:226`) == 0) { // :4483

    if (era.get(`talent:${target}:76`) == 1) { // :4485
      await era.printAndWait(''); // :4486

    } else if (era.get(`talent:${target}:85`) == 1) { // :4488
      await era.printAndWait(''); // :4489

    } else { // :4491
      await era.printAndWait(''); // :4492
    } // :4493
    // CFLAG:TARGET:226  = 1（变量语义：CFLAG 族，TARGET:226） // :4494
    era.set(`cflag:${target}:226`, 1); // :4494
  } // :4495




  if (era.get('nowex:2') > 0 && era.get(`cflag:${target}:227`) == 0) { // :4500

    if (era.get(`talent:${target}:76`) == 1) { // :4502
      await era.printAndWait(''); // :4503

    } else if (era.get(`talent:${target}:85`) == 1) { // :4505
      await era.printAndWait(''); // :4506

    } else { // :4508
      await era.printAndWait(`「咕……啊啊」`); // :4509
    } // :4510
    // CFLAG:227  = 1（变量语义：CFLAG 族，227） // :4511
    era.set(`cflag:${target}:227`, 1); // :4511
  } // :4512




  if (era.get('nowex:3') > 0 && era.get(`cflag:${target}:228`) == 0) { // :4517

    if (era.get(`talent:${target}:85`) == 1) { // :4519
      await era.printAndWait(''); // :4520

    } else { // :4522
      await era.printAndWait(''); // :4523
    } // :4524
    // CFLAG:TARGET:228  = 1（变量语义：CFLAG 族，TARGET:228） // :4525
    era.set(`cflag:${target}:228`, 1); // :4525
  } // :4526




  // 赋值 A = UP:11 + UP:12 // :4531
  if (era.get('tflag:3') == 1 && era.get(`cflag:${target}:229`) == 0) { // :4532

    if (era.get('tflag:20') == 1) { // :4534

      if (era.get(`talent:${target}:76`) == 1 && (A < 500 || era.get('tflag:150') == 1)) { // :4536
        await era.printAndWait(''); // :4537

      } else if (era.get(`talent:${target}:85`) == 1 && (A < 500 || era.get('tflag:150') == 1)) { // :4539
        await era.printAndWait(''); // :4540

      } else { // :4542
        await era.printAndWait(''); // :4543
      } // :4544

    } else { // :4546

      if (era.get(`talent:${target}:76`) == 1) { // :4548
        await era.printAndWait(''); // :4549

      } else if (era.get(`talent:${target}:85`) == 1) { // :4551
        await era.printAndWait(''); // :4552

      } else { // :4554
        await era.printAndWait(''); // :4555
      } // :4556
    } // :4557
    // CFLAG:TARGET:229  = 1（变量语义：CFLAG 族，TARGET:229） // :4558
    era.set(`cflag:${target}:229`, 1); // :4558
  } // :4559






}

// @KOJO_MESSAGE_MARKCNG_14 // :4566
async function KOJO_MESSAGE_MARKCNG_14() {




  if (era.get(`tequip:${target}:45`)) { // :4572
    return 0; // :4572
  } // :4572



  if (era.get('tflag:22') == 3 && era.get(`cflag:${target}:297`) == 0) { // :4576

    if (era.get(`talent:${target}:85`) == 1) { // :4578
      await era.printAndWait(''); // :4579
    } else { // :4580
      await era.printAndWait(''); // :4581
    } // :4582
    // CFLAG:297  = 1（变量语义：CFLAG 族，297） // :4583
    era.set(`cflag:${target}:297`, 1); // :4583
  } // :4584




  if (era.get('tflag:23') == 3 && era.get(`cflag:${target}:298`) == 0) { // :4589

    if (era.get(`talent:${target}:85`) == 1) { // :4591
      await era.printAndWait(''); // :4592
    } else { // :4593
      await era.printAndWait(''); // :4594
    } // :4595
    // CFLAG:298  = 1（变量语义：CFLAG 族，298） // :4596
    era.set(`cflag:${target}:298`, 1); // :4596
  } // :4597




  if (era.get('tflag:24') == 3 && era.get(`cflag:${target}:299`) == 0) { // :4602

    if (era.get(`talent:${target}:85`) == 1) { // :4604
      await era.printAndWait(''); // :4605
    } else { // :4606
      await era.printAndWait(''); // :4607
    } // :4608
    // CFLAG:299  = 1（变量语义：CFLAG 族，299） // :4609
    era.set(`cflag:${target}:299`, 1); // :4609
  } // :4610




  if (era.get('tflag:21') == 3 && era.get(`cflag:${target}:300`) == 0) { // :4615

    if (era.get(`talent:${target}:85`) == 1) { // :4617
      await era.printAndWait(''); // :4618
    } else { // :4619
      await era.printAndWait(''); // :4620
    } // :4621
    // CFLAG:300  = 1（变量语义：CFLAG 族，300） // :4622
    era.set(`cflag:${target}:300`, 1); // :4622
  } // :4623





}

// @SELF_KOJO_K14 // :4629
async function SELF_KOJO_K14() {



  if (era.get('tflag:13') == 1) { // :4633

    if (era.get(`talent:${target}:9`) == 1) { // :4635
      await era.printAndWait(''); // :4636

    } else if (Q == 1) { // :4638
      await era.printAndWait(''); // :4639

    } else if (Q == 2) { // :4641
      await era.printAndWait(''); // :4642

    } else { // :4644

      if (era.get(`talent:${target}:76`) && (era.get(`cflag:${target}:261`) < 4 || era.get('flag:7') == 2)) { // :4646
        await era.printAndWait(''); // :4647
        // CFLAG:261  = 4（变量语义：CFLAG 族，261） // :4648
        era.set(`cflag:${target}:261`, 4); // :4648

      } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:261`) < 3 || era.get('flag:7') == 2)) { // :4650
        await era.printAndWait(''); // :4651
        // CFLAG:261  = 3（变量语义：CFLAG 族，261） // :4652
        era.set(`cflag:${target}:261`, 3); // :4652

      } else if (era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:261`) < 2 || era.get('flag:7') == 2)) { // :4654
        await era.printAndWait(''); // :4655
        // CFLAG:261  = 2（变量语义：CFLAG 族，261） // :4656
        era.set(`cflag:${target}:261`, 2); // :4656

      } else if (era.get(`cflag:${target}:261`) < 1 || era.get('flag:7') == 2) { // :4658
        await era.printAndWait(''); // :4659
        // CFLAG:261  = 1（变量语义：CFLAG 族，261） // :4660
        era.set(`cflag:${target}:261`, 1); // :4660
      } // :4661
    } // :4662
  } // :4663




  if (era.get('tflag:13') == 2) { // :4668

    if (era.get(`talent:${target}:76`) && (era.get(`cflag:${target}:262`) < 5 || era.get('flag:7') == 2)) { // :4670
      await era.printAndWait(''); // :4671
      // CFLAG:262  = 5（变量语义：CFLAG 族，262） // :4672
      era.set(`cflag:${target}:262`, 5); // :4672

    } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:262`) < 4 || era.get('flag:7') == 2)) { // :4674
      await era.printAndWait(''); // :4675
      // CFLAG:262  = 4（变量语义：CFLAG 族，262） // :4676
      era.set(`cflag:${target}:262`, 4); // :4676

    } else if (era.get(`abl:${target}:33`) >= 3 && (era.get(`cflag:${target}:262`) < 3 || era.get('flag:7') == 2)) { // :4678
      await era.printAndWait(''); // :4679
      // CFLAG:262  = 3（变量语义：CFLAG 族，262） // :4680
      era.set(`cflag:${target}:262`, 3); // :4680

    } else if (era.get(`abl:${target}:22`) >= 3 && (era.get(`cflag:${target}:262`) < 2 || era.get('flag:7') == 2)) { // :4682
      await era.printAndWait(''); // :4683
      // CFLAG:262  = 2（变量语义：CFLAG 族，262） // :4684
      era.set(`cflag:${target}:262`, 2); // :4684

    } else if (era.get(`cflag:${target}:262`) < 1 || era.get('flag:7') == 2) { // :4686
      await era.printAndWait(''); // :4687
      // CFLAG:262  = 1（变量语义：CFLAG 族，262） // :4688
      era.set(`cflag:${target}:262`, 1); // :4688
    } // :4689
  } // :4690




  if (era.get('tflag:13') == 3) { // :4695

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:263`) < 3 || era.get('flag:7') == 2)) { // :4697
      await era.printAndWait(''); // :4698
      // CFLAG:263  = 3（变量语义：CFLAG 族，263） // :4699
      era.set(`cflag:${target}:263`, 3); // :4699

    } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:263`) < 3 || era.get('flag:7') == 2)) { // :4701
      await era.printAndWait(''); // :4702
      // CFLAG:263  = 3（变量语义：CFLAG 族，263） // :4703
      era.set(`cflag:${target}:263`, 3); // :4703

    } else if (era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:263`) < 2 || era.get('flag:7') == 2)) { // :4705
      await era.printAndWait(''); // :4706
      // CFLAG:263  = 2（变量语义：CFLAG 族，263） // :4707
      era.set(`cflag:${target}:263`, 2); // :4707

    } else if (era.get(`cflag:${target}:263`) < 1 || era.get('flag:7') == 2) { // :4709
      await era.printAndWait(''); // :4710
      // CFLAG:263  = 1（变量语义：CFLAG 族，263） // :4711
      era.set(`cflag:${target}:263`, 1); // :4711
    } // :4712
  } // :4713




  if (era.get('tflag:13') == 4) { // :4718

    if (era.get(`abl:${target}:2`) >= 4 && (era.get(`cflag:${target}:264`) < 2 || era.get('flag:7') == 2)) { // :4720
      await era.printAndWait(''); // :4721
      // CFLAG:264  = 2（变量语义：CFLAG 族，264） // :4722
      era.set(`cflag:${target}:264`, 2); // :4722

    } else if (era.get(`cflag:${target}:264`) < 1 || era.get('flag:7') == 2) { // :4724
      await era.printAndWait(''); // :4725
      // CFLAG:264  = 1（变量语义：CFLAG 族，264） // :4726
      era.set(`cflag:${target}:264`, 1); // :4726
    } // :4727
  } // :4728




  if (era.get('tflag:13') == 5) { // :4733
    if (era.get(`cflag:${target}:265`) < 1 || era.get('flag:7') == 2) { // :4734
      await era.printAndWait(''); // :4735
      // CFLAG:265  = 1（变量语义：CFLAG 族，265） // :4736
      era.set(`cflag:${target}:265`, 1); // :4736
    } // :4737
  } // :4738




  if (era.get('tflag:13') == 6) { // :4743

    if (era.get(`talent:${target}:85`) && era.get(`mark:${target}:3`) < 3) { // :4745
      await era.printAndWait(''); // :4746

    } else if (era.get(`mark:${target}:3`) == 3) { // :4748
      await era.printAndWait(''); // :4749

    } else if (era.get(`talent:${target}:76`)) { // :4751
      await era.printAndWait(''); // :4752

    } else { // :4754
      await era.printAndWait(''); // :4755
    } // :4756
  } // :4757






  if (era.get('tflag:13') == 11) { // :4764
    if (era.get(`cflag:${target}:271`) >= 1) { // :4766
      return 0; // :4766
    } // :4766

    if (era.get(`talent:${target}:9`) == 1) { // :4768
      await era.printAndWait(''); // :4769

    } else if (era.get(`talent:${target}:85`) && era.get(`cflag:${target}:102`) == 1) { // :4771
      await era.printAndWait(''); // :4772

    } else { // :4774
      await era.printAndWait(''); // :4775
    } // :4776
    // CFLAG:271  = 1（变量语义：CFLAG 族，271） // :4777
    era.set(`cflag:${target}:271`, 1); // :4777
  } // :4778





  if (era.get('tflag:13') == 12) { // :4784
    if (era.get(`cflag:${target}:272`) >= 1) { // :4786
      return 0; // :4786
    } // :4786

    if (era.get(`talent:${target}:9`) == 1) { // :4788
      await era.printAndWait(''); // :4789

    } else if (era.get(`talent:${target}:85`) && era.get(`cflag:${target}:102`) == 1) { // :4791
      await era.printAndWait(''); // :4792

    } else { // :4794
      await era.printAndWait(''); // :4795
    } // :4796
    // CFLAG:272  = 1（变量语义：CFLAG 族，272） // :4797
    era.set(`cflag:${target}:272`, 1); // :4797
  } // :4798




  if (era.get('tflag:13') == 13) { // :4803

    if (era.get(`talent:${target}:153`)) { // :4805
      await era.printAndWait(''); // :4806

    } else if (era.get(`talent:${target}:154`)) { // :4808
      await era.printAndWait(''); // :4809
    } // :4810
    // CFLAG:273  = 1（变量语义：CFLAG 族，273） // :4811
    era.set(`cflag:${target}:273`, 1); // :4811
  } // :4812




  if (era.get('tflag:13') == 14) { // :4817
    await era.printAndWait(''); // :4818
    // CFLAG:274  = 1（变量语义：CFLAG 族，274） // :4819
    era.set(`cflag:${target}:274`, 1); // :4819
  } // :4820




  if (era.get('tflag:13') == 999) { // :4825

    if (era.get(`talent:${target}:85`)) { // :4827
      await era.printAndWait(''); // :4828

    } else { // :4830
      await era.printAndWait(''); // :4831
    } // :4832
  } // :4833




  if (era.get('tflag:13') == 998) { // :4838

    if (era.get(`talent:${target}:85`)) { // :4840
      await era.printAndWait(''); // :4841

    } else { // :4843
      await era.printAndWait(''); // :4844
    } // :4845
  } // :4846




  // TFLAG:13  = 0（变量语义：TFLAG 族，13） // :4851
  era.set('tflag:13', 0); // :4851

  return 0; // :4853






























}

// @DUNGEON_RYOUZYOKU_K14 // :4884
async function DUNGEON_RYOUZYOKU_K14() {




  if (era.get(`talent:${target}:0`) == 1) { // :4889

    await era.printAndWait(''); // :4891

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4893


      await era.printAndWait(''); // :4896

      return 0; // :4898
    } else if (era.get(`talent:${target}:17`) ==1 || era.get(`talent:${target}:31`) == 1 || era.get(`talent:${target}:36`) == 1) { // :4899


      await era.printAndWait(''); // :4902


      if (era.get(`talent:${target}:106`) == 1 || era.get(`exp:${target}:1`) > 0) { // :4906
        await era.printAndWait(''); // :4906
      } // :4906


      if (era.get(`exp:${target}:22`) > 0) { // :4910
        await era.printAndWait(''); // :4910
      } // :4910

    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4912



      await era.printAndWait(''); // :4916

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4918


      await era.printAndWait(''); // :4921

    } else { // :4923


      await era.printAndWait(''); // :4926

    } // :4928
  } else { // :4929

    await era.printAndWait(''); // :4931

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4933


      await era.printAndWait(''); // :4936

      return 0; // :4938
    } else if (era.get(`talent:${target}:17`) ==1 || era.get(`talent:${target}:31`) == 1 || era.get(`talent:${target}:36`) == 1) { // :4939


      await era.printAndWait(''); // :4942


      if (era.get(`talent:${target}:106`) == 1 || era.get(`exp:${target}:1`) > 0) { // :4946
        await era.printAndWait(''); // :4946
      } // :4946


      if (era.get(`exp:${target}:22`) > 0) { // :4950
        await era.printAndWait(''); // :4950
      } // :4950

    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4952



      await era.printAndWait(''); // :4956

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4958


      await era.printAndWait(''); // :4961

    } else { // :4963


      await era.printAndWait(''); // :4966

    } // :4968
  } // :4969

  return 0; // :4971


}

// @DUNGEON_RYOUZYOKU_AFTER_K14 // :4974
async function DUNGEON_RYOUZYOKU_AFTER_K14() {




  if (era.get(`talent:${target}:0`) == 1) { // :4979

    await era.printAndWait(''); // :4981

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4983


      await era.printAndWait(''); // :4986

      return 0; // :4988
    } // :4989


    if (era.get(`exp:${target}:1`) > 20) { // :4992
      await era.printAndWait(''); // :4993
      await era.printAndWait(''); // :4994
    } // :4995


    if (era.get(`exp:${target}:22`) > 20) { // :4999
      await era.printAndWait(''); // :4999
    } // :4999


    if (era.get(`exp:${target}:20`) > 20) { // :5003
      await era.printAndWait(''); // :5003
    } // :5003
  } else { // :5004

    await era.printAndWait(''); // :5006

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :5008


      await era.printAndWait(''); // :5011

      return 0; // :5013
    } // :5014


    if (era.get(`exp:${target}:0`) > 20) { // :5017
      await era.printAndWait(''); // :5018
      await era.printAndWait(''); // :5019
    } // :5020


    if (era.get(`exp:${target}:1`) > 20) { // :5023
      await era.printAndWait(''); // :5024
      await era.printAndWait(''); // :5025
    } // :5026


    if (era.get(`exp:${target}:22`) > 20) { // :5030
      await era.printAndWait(''); // :5030
    } // :5030


    if (era.get(`exp:${target}:20`) > 20) { // :5034
      await era.printAndWait(''); // :5034
    } // :5034
  } // :5035

  return 0; // :5037

}

// @BENKI_KOUJO_K14 // :5039
async function BENKI_KOUJO_K14() {


















  if (era.get('flag:62') == 0) { // :5058


    if (era.get('flag:63') == 1) { // :5061
      await era.printAndWait(''); // :5062

    } else if (era.get(`talent:${target}:122`) && era.get('flag:63') == 1) { // :5064
      await era.printAndWait(''); // :5065

    } else if (era.get(`talent:${a}:76`) == 1) { // :5067
      await era.printAndWait(''); // :5068

    } else if (era.get(`talent:${a}:85`)) { // :5070
      await era.printAndWait(''); // :5071

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5073
      await era.printAndWait(''); // :5074

    } else { // :5076
      await era.printAndWait(''); // :5077
    } // :5078
  } else if (era.get('flag:62') == 1) { // :5079


    if (era.get('flag:63') == 1) { // :5082
      await era.printAndWait(''); // :5083

    } else if (era.get(`talent:${target}:122`) && era.get('flag:63') == 1) { // :5085
      await era.printAndWait(''); // :5086

    } else if (era.get(`talent:${a}:76`) == 1) { // :5088
      await era.printAndWait(''); // :5089

    } else if (era.get(`talent:${a}:85`)) { // :5091
      await era.printAndWait(''); // :5092

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5094
      await era.printAndWait(''); // :5095

    } else { // :5097
      await era.printAndWait(''); // :5098
    } // :5099
  } else if (era.get('flag:62') == 2) { // :5100


    if (era.get('flag:63') == 1) { // :5103
      await era.printAndWait(''); // :5104

    } else if (era.get(`talent:${target}:122`) && era.get('flag:63') == 1) { // :5106
      await era.printAndWait(''); // :5107

    } else if (era.get(`talent:${a}:76`) == 1) { // :5109
      await era.printAndWait(''); // :5110

    } else if (era.get(`talent:${a}:85`)) { // :5112
      await era.printAndWait(''); // :5113

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5115
      await era.printAndWait(''); // :5116

    } else { // :5118
      await era.printAndWait(''); // :5119
    } // :5120
  } else if (era.get('flag:62') == 3) { // :5121


    if (era.get('flag:63') == 1) { // :5124
      await era.printAndWait(''); // :5125

    } else if (era.get(`talent:${a}:76`) == 1) { // :5127
      await era.printAndWait(''); // :5128

    } else if (era.get(`talent:${a}:85`)) { // :5130
      await era.printAndWait(''); // :5131

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5133
      await era.printAndWait(''); // :5134

    } else { // :5136
      await era.printAndWait(''); // :5137
    } // :5138
  } else if (era.get('flag:62') == 4) { // :5139


    if (era.get('flag:63') == 1) { // :5142
      await era.printAndWait(''); // :5143

    } else if (era.get(`talent:${a}:76`) == 1) { // :5145
      await era.printAndWait(''); // :5146

    } else if (era.get(`talent:${a}:85`)) { // :5148
      await era.printAndWait(''); // :5149

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5151
      await era.printAndWait(''); // :5152

    } else { // :5154
      await era.printAndWait(''); // :5155
    } // :5156
  } else if (era.get('flag:62') == 5) { // :5157


    if (era.get('flag:63') == 1) { // :5160
      await era.printAndWait(''); // :5161

    } else if (era.get(`talent:${target}:122`) && era.get('flag:63') == 1) { // :5163
      await era.printAndWait(''); // :5164

    } else if (era.get(`talent:${a}:76`) == 1) { // :5166
      await era.printAndWait(''); // :5167

    } else if (era.get(`talent:${a}:85`)) { // :5169
      await era.printAndWait(''); // :5170

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5172
      await era.printAndWait(''); // :5173

    } else { // :5175
      await era.printAndWait(''); // :5176
    } // :5177
  } else if (era.get('flag:62') == 6) { // :5178


    if (era.get('flag:63') == 1) { // :5181
      await era.print(''); // :5182

    } else if (era.get(`talent:${target}:122`) && era.get('flag:63') == 1) { // :5184
      await era.printAndWait(''); // :5185

    } else if (era.get(`talent:${a}:76`) == 1) { // :5187
      await era.printAndWait(''); // :5188

    } else if (era.get(`talent:${a}:85`)) { // :5190
      await era.printAndWait(''); // :5191

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5193
      await era.printAndWait(''); // :5194

    } else { // :5196
      await era.printAndWait(''); // :5197
    } // :5198
  } else if (era.get('flag:62') == 7) { // :5199


    if (era.get('flag:63') == 1) { // :5202
      await era.printAndWait(''); // :5203

    } else if (era.get(`talent:${target}:122`) && era.get('flag:63') == 1) { // :5205
      await era.printAndWait(''); // :5206

    } else if (era.get(`talent:${a}:76`) == 1) { // :5208
      await era.printAndWait(''); // :5209

    } else if (era.get(`talent:${a}:85`)) { // :5211
      await era.printAndWait(''); // :5212

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5214
      await era.printAndWait(''); // :5215

    } else { // :5217
      await era.printAndWait(''); // :5218
    } // :5219
  } else if (era.get('flag:62') == 9) { // :5220


    if (era.get('flag:63') == 1) { // :5223
      await era.printAndWait(`「啊、那个…元冒险者的${target_name}、来着」`); // :5224
      await era.printAndWait(`「%SELF_CALL(A)%败给了伟大的魔王大人…虽然身心都被彻底的玩弄了、但不会就此屈服的！」`); // :5225
      await era.printAndWait(`「然后啊、今天啊…是要让大家『看看原生态的%SELF_CALL(A)%』啊…」`); // :5226
      await era.printAndWait(`「虽然看『男人的身体』也没啥意思啦…但是%SELF_CALL(A)%会竭尽全力的、请好好看着咯♪」`); // :5227

    } else if (era.get(`talent:${target}:122`) && era.get('flag:63') == 1) { // :5229
      await era.printAndWait(`「啊、那个…元冒险者的${target_name}、来着」`); // :5230
      await era.printAndWait(`「%SELF_CALL(A)%败给了伟大的魔王大人…虽然身心都被彻底的玩弄了、但不会就此屈服的！」`); // :5231
      await era.printAndWait(`「然后啊、今天啊…是要让大家『看看原生态的%SELF_CALL(A)%』啊…」`); // :5232
      await era.printAndWait(`「虽然看『男人的身体』也没啥意思啦…但是%SELF_CALL(A)%会竭尽全力的、请好好看着咯♪」`); // :5233

    } else if (era.get(`talent:${a}:76`) == 1) { // :5235
      await era.printAndWait(''); // :5236

    } else if (era.get(`talent:${a}:85`)) { // :5238
      await era.printAndWait(''); // :5239

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5241
      await era.printAndWait(''); // :5242

    } else { // :5244
      await era.printAndWait(''); // :5245
    } // :5246
  } else if (era.get('flag:62') == 12) { // :5247


    if (era.get('flag:63') == 1) { // :5250
      await era.printAndWait(''); // :5251

    } else if (era.get(`talent:${target}:122`) && era.get('flag:63') == 1) { // :5253
      await era.printAndWait(''); // :5254

    } else if (era.get(`talent:${a}:76`) == 1) { // :5256
      await era.printAndWait(''); // :5257

    } else if (era.get(`talent:${a}:85`)) { // :5259
      await era.printAndWait(''); // :5260

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5262
      await era.printAndWait(''); // :5263

    } else { // :5265
      await era.printAndWait(''); // :5266
    } // :5267
  } // :5268

  return 0; // :5270


}

// @DUNGEON_VICTORY_K14 // :5273
async function DUNGEON_VICTORY_K14() {




  await era.printAndWait(''); // :5278

  if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :5280


    await era.printAndWait(`「……哈」`); // :5283

    return 0; // :5285
  } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :5286


    if (rand_n(3) == 0) { // :5289
      await era.printAndWait(`「……真是污秽」`); // :5290
    } else if (rand_n(2) == 0) { // :5291
      await era.printAndWait(`「魔力什么的，没有必要」`); // :5292
    } else { // :5293
      await era.printAndWait(`「消灭了吗」`); // :5294
    } // :5295

  } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :5297


    await era.printAndWait(`「魔の力、これほどとは……」`); // :5300

    return 0; // :5302
  } else { // :5303


    if (rand_n(3) == 0) { // :5306
      await era.printAndWait(`「这是光明的胜利！」`); // :5307
    } else if (rand_n(2) == 0) { // :5308
      await era.printAndWait(`「光明不灭！」`); // :5309
    } else { // :5310
      await era.printAndWait(`「怎么可能输给不净之物……」`); // :5311
    } // :5312

  } // :5314

  if ((era.get(`base:${a}:0`) * 100 / era.get(`maxbase:${a}:0`) < 50) || (era.get(`base:${a}:1`) * 100 / era.get(`maxbase:${a}:1`) < 50)) { // :5316

    await era.printAndWait(`（果不其然，真是强大……）`); // :5318
  } else { // :5319

    await era.printAndWait(`「不净的力量，抹杀之」`); // :5321
  } // :5322

  return 0; // :5324


}

// @DUNGEON_ATTACK_K14 // :5327
async function DUNGEON_ATTACK_K14() {




  if (era.get(`cflag:${target}:1`) == 2) { // :5332

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :5334


      await era.printAndWait(`「……准备咯」`); // :5337

      return 0; // :5339
    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :5340


      if (rand_n(3) == 0) { // :5343
        await era.printAndWait(`「怪物！」`); // :5344
      } else if (rand_n(2) == 0) { // :5345
        await era.printAndWait(`「不净之物！」`); // :5346
      } else { // :5347
        await era.printAndWait(`「……消失吧！」`); // :5348
      } // :5349

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :5351


      await era.printAndWait(`「切，不净之物……」`); // :5354

      return 0; // :5356
    } else { // :5357


      if (rand_n(3) == 0) { // :5360
        await era.printAndWait(`「毁灭吧！」`); // :5361
      } else if (rand_n(2) == 0) { // :5362
        await era.printAndWait(`「可不能输！」`); // :5363
      } else { // :5364
        await era.printAndWait(`「光明啊！」`); // :5365
      } // :5366

    } // :5368
  } else { // :5369

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :5371


      await era.printAndWait(`「……准备咯♪」`); // :5374

      return 0; // :5376
    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :5377


      if (rand_n(3) == 0) { // :5380
        await era.printAndWait(`「这就是魔力吗！」`); // :5381
      } else if (rand_n(2) == 0) { // :5382
        await era.printAndWait(`「真是美妙的力量……」`); // :5383
      } else { // :5384
        await era.printAndWait(`「好强……竟可以强成这样，魔力真是……」`); // :5385
      } // :5386

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :5388


      await era.printAndWait(`「切，魔力开始侵蚀了吗……」`); // :5391

      return 0; // :5393
    } else { // :5394


      if (rand_n(3) == 0) { // :5397
        await era.printAndWait(`「消失吧！」`); // :5398
      } else if (rand_n(2) == 0) { // :5399
        await era.printAndWait(`「黑暗啊！」`); // :5400
      } else { // :5401
        await era.printAndWait(`「堕入黑暗吧……」`); // :5402
      } // :5403

    } // :5405
  } // :5406



  return 0; // :5410





}

// @COLOSSEUM_KOJO_14 // :5416
async function COLOSSEUM_KOJO_14() {



  if (era_flag.selectcom == 55) { // :5420

    if (era.get(`base:${target}:1`) <= 0) { // :5422
      await era.printAndWait(''); // :5423
    } else { // :5424
      await era.printAndWait(''); // :5425
    } // :5426
    return 0; // :5427
  } // :5428



  if (era_flag.selectcom == 56) { // :5432

    if (era.get(`base:${target}:1`) <= 0) { // :5434

      if (era_flag.assi > 0 && era_flag.assiplay) { // :5436
        await era.printAndWait(''); // :5437
      } else { // :5438
        await era.printAndWait(''); // :5439
      } // :5440
    } else { // :5441

      if (era_flag.assi > 0 && era_flag.assiplay) { // :5443
        await era.printAndWait(''); // :5444
      } else { // :5445
        await era.printAndWait(''); // :5446
      } // :5447
    } // :5448
    return 0; // :5449
  } // :5450




  if (era_flag.selectcom == 31) { // :5455

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5457
      await era.printAndWait(''); // :5458
    } else { // :5459
      await era.printAndWait(''); // :5460
    } // :5461
    return 0; // :5462
  } // :5463



  if (era_flag.selectcom == 5) { // :5467

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5469
      await era.printAndWait(''); // :5470
    } else { // :5471
      await era.printAndWait(''); // :5472
    } // :5473
    return 0; // :5474
  } // :5475



  if (era_flag.selectcom == 21) { // :5479

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5481
      await era.printAndWait(''); // :5482

    } else if (era.get('tflag:400') == 206) { // :5484
      await era.printAndWait(''); // :5485
    } else { // :5486
      await era.printAndWait(''); // :5487
    } // :5488
    return 0; // :5489
  } // :5490




  if (era_flag.selectcom == 27) { // :5495

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5497
      await era.printAndWait(''); // :5498

    } else if (era.get('tflag:400') == 206) { // :5500
      await era.printAndWait(''); // :5501
    } else { // :5502
      await era.printAndWait(''); // :5503
    } // :5504
    return 0; // :5505
  } // :5506




  if (era_flag.selectcom == 51) { // :5511
    await era.printAndWait(''); // :5512
    return 0; // :5513
  } // :5514


  return 0; // :5517



}

// @NTR_KOUJO_K14 // :5521
async function NTR_KOUJO_K14() {



  if (era.get(`cflag:${target}:650`) == 0) { // :5526
    // CFLAG:650  = 1（变量语义：CFLAG 族，650） // :5526
    era.set(`cflag:${target}:650`, 1); // :5526
  } // :5526


  if (P == 1) { // :5529

    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5531
      await era.printAndWait(''); // :5532
    } else { // :5533
      await era.printAndWait(''); // :5534
    } // :5535
    // CFLAG:651  = 1（变量语义：CFLAG 族，651） // :5536
    era.set(`cflag:${target}:651`, 1); // :5536

  } else if (P == 2) { // :5538
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5539
      await era.printAndWait(''); // :5540
    } else { // :5541
      await era.printAndWait(''); // :5542
    } // :5543
    // CFLAG:652  = 1（变量语义：CFLAG 族，652） // :5544
    era.set(`cflag:${target}:652`, 1); // :5544

  } else if (P == 3) { // :5546
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5547
      await era.printAndWait(''); // :5548
    } else { // :5549
      await era.printAndWait(''); // :5550
    } // :5551
    // CFLAG:653  = 1（变量语义：CFLAG 族，653） // :5552
    era.set(`cflag:${target}:653`, 1); // :5552

  } else if (P == 4) { // :5554
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5555
      await era.printAndWait(''); // :5556
    } else { // :5557
      await era.printAndWait(''); // :5558
    } // :5559
    // CFLAG:654  = 1（变量语义：CFLAG 族，654） // :5560
    era.set(`cflag:${target}:654`, 1); // :5560

  } else if (P == 5) { // :5562
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5563
      await era.printAndWait(''); // :5564
    } else { // :5565
      await era.printAndWait(''); // :5566
    } // :5567
    // CFLAG:655  = 1（变量语义：CFLAG 族，655） // :5568
    era.set(`cflag:${target}:655`, 1); // :5568

  } else if (P == 6) { // :5570
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5571
      await era.printAndWait(''); // :5572
    } else { // :5573
      await era.printAndWait(''); // :5574
    } // :5575
    // CFLAG:656  = 1（变量语义：CFLAG 族，656） // :5576
    era.set(`cflag:${target}:656`, 1); // :5576

  } else if (P == 7) { // :5578
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5579
      await era.printAndWait(''); // :5580
    } else { // :5581
      await era.printAndWait(''); // :5582
    } // :5583
    // CFLAG:657  = 1（变量语义：CFLAG 族，657） // :5584
    era.set(`cflag:${target}:657`, 1); // :5584

  } else if (P == 20) { // :5586
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5587
      await era.printAndWait(''); // :5588
    } else { // :5589
      await era.printAndWait(''); // :5590
    } // :5591

  } // :5593

  return 0; // :5595


}

// @EXUCUTION_KOUJO_K14 // :5598
async function EXUCUTION_KOUJO_K14() {



  if (era.get('tflag:16') == 2) { // :5602
    await era.printAndWait(''); // :5603

  } else if (era.get('tflag:16') == 5) { // :5605
    await era.printAndWait(''); // :5606

  } else if (era.get('tflag:16') == 6) { // :5608
    await era.printAndWait(''); // :5609

  } else if (era.get('tflag:16') == 7) { // :5611
    await era.printAndWait(''); // :5612
  } // :5613


}

// @MUSEUM_KOUJO_K14 // :5616
async function MUSEUM_KOUJO_K14() {



  if (era.get('tflag:500') == 0) { // :5620
    await era.printAndWait(''); // :5621

  } else if (era.get('tflag:500') == 1) { // :5623
    await era.printAndWait(''); // :5624

  } else if (era.get('tflag:500') == 2) { // :5626
    await era.printAndWait(''); // :5627

  } else if (era.get('tflag:500') == 3) { // :5629
    await era.printAndWait(''); // :5630

  } else if (era.get('tflag:500') == 4) { // :5632
    await era.printAndWait(''); // :5633

  } else if (era.get('tflag:500') == 5) { // :5635
    await era.printAndWait(''); // :5636

  } else if (era.get('tflag:500') == 6) { // :5638
    await era.printAndWait(''); // :5639

  } else if (era.get('tflag:500') == 7) { // :5641
    await era.printAndWait(''); // :5642

  } else if (era.get('tflag:500') == 8) { // :5644
    await era.printAndWait(''); // :5645

  } else if (era.get('tflag:500') == 9) { // :5647
    await era.printAndWait(''); // :5648
  } // :5649


}

// @BANISHMENT_KOUJO_K14 // :5652
async function BANISHMENT_KOUJO_K14() {



  if (era.get('tflag:510') == 0) { // :5656
    await era.printAndWait(''); // :5657

  } else if (era.get('tflag:510') == 1) { // :5659
    await era.printAndWait(''); // :5660

  } else if (era.get('tflag:510') == 2) { // :5662
    await era.printAndWait(''); // :5663

  } else if (era.get('tflag:510') == 3) { // :5665
    await era.printAndWait(''); // :5666
  } // :5667


}

// @PUBLIC_EXUCUTION_KOUJO_K14 // :5670
async function PUBLIC_EXUCUTION_KOUJO_K14() {



  if (era.get('tflag:520') == 0) { // :5674
    await era.printAndWait(''); // :5675

  } else if (era.get('tflag:520') == 1) { // :5677
    await era.printAndWait(''); // :5678

  } else if (era.get('tflag:520') == 2) { // :5680
    await era.printAndWait(''); // :5681
  } // :5682


}

// @GROTESQUE_KOUJO_K14 // :5685
async function GROTESQUE_KOUJO_K14() {



  if (era.get('tflag:530') == 0) { // :5689
    await era.printAndWait(''); // :5690

  } else if (era.get('tflag:530') == 1) { // :5692
    await era.printAndWait(''); // :5693

  } else if (era.get('tflag:530') == 2) { // :5695
    await era.printAndWait(''); // :5696

  } else if (era.get('tflag:530') == 3) { // :5698
    await era.printAndWait(''); // :5699

  } else if (era.get('tflag:530') == 4) { // :5701
    await era.printAndWait(''); // :5702

  } else if (era.get('tflag:530') == 5) { // :5704
    await era.printAndWait(''); // :5705

  } else if (era.get('tflag:530') == 6) { // :5707
    await era.printAndWait(''); // :5708
  } // :5709


}

// @ENTERENEMY_KOUJO_K14 // :5712
async function ENTERENEMY_KOUJO_K14() {


  if (era.get(`talent:${a}:21`) == 1 || era.get(`talent:${a}:22`) == 1) { // :5715

    await era.printAndWait(`「魔王……」`); // :5717
  } else if (era.get(`talent:${a}:11`) == 1 || era.get(`talent:${a}:12`) == 1 || era.get(`talent:${a}:15`) == 1 || era.get(`talent:${a}:30`) == 1 || era.get(`talent:${a}:34`) == 1) { // :5718

    await era.printAndWait(`「魔王！　不可原谅！」`); // :5720
  } else if (era.get(`talent:${a}:10`) == 1 || era.get(`talent:${a}:26`) == 1) { // :5721

    await era.printAndWait(`「不可原谅啊，魔王……」`); // :5723
  } else { // :5724

    await era.printAndWait(`「打个魔王来看看！」`); // :5726
  } // :5727


}

// @GOHOUBI_REQUEST_KOUJO_K14 // :5730
async function GOHOUBI_REQUEST_KOUJO_K14() {


  if (era.get(`cflag:${a}:504`) == 0) { // :5733

    await era.printAndWait(`%SAVESTR:A%提出了想要钱当报酬。`); // :5735
  } else if (era.get(`cflag:${a}:504`) == 1 || era.get(`cflag:${a}:504`) == 2 || era.get(`cflag:${a}:504`) == 3) { // :5736

    await era.print(`%SAVESTR:A%提出了想要和`); // :5738
    if (era.get(`cflag:${a}:504`) == 1) { // :5739
      await era.print(`狗`); // :5740
    } else if (era.get(`cflag:${a}:504`) == 2) { // :5741
      await era.print(`猪`); // :5742
    } else if (era.get(`cflag:${a}:504`) == 3) { // :5743
      await era.print(`马`); // :5744
    } // :5745
    await era.printAndWait(`进行交配的奖励。`); // :5746
  } else if (era.get(`cflag:${a}:504`) == 4) { // :5747

    await era.printAndWait(`%SAVESTR:A%提出了回来之后想与你接吻的奖励。`); // :5749
  } else if (era.get(`cflag:${a}:504`) == 5) { // :5750

    await era.printAndWait(`%SAVESTR:A%提出了想与你做爱的奖励。`); // :5752
  } else if (era.get(`cflag:${a}:504`) == 6) { // :5753

    await era.printAndWait(`%SAVESTR:A%提出了想要精液的奖励。`); // :5755
  } else if (era.get(`cflag:${a}:504`) == 7) { // :5756

    await era.printAndWait(`%SAVESTR:A%提出了想要海天盛筵的奖励。`); // :5758
  } else if (era.get(`cflag:${a}:504`) == 8) { // :5759

    await era.printAndWait(`%SAVESTR:A%提出了饮用圣水的奖励。`); // :5761
  } else if (era.get(`cflag:${a}:504`) == 9) { // :5762

    await era.printAndWait(`%SAVESTR:A%提出了童贞狩猎的奖励。`); // :5764
  } // :5765


}

// @GOHOUBI_AFTER_KOUJO_K14 // :5768
async function GOHOUBI_AFTER_KOUJO_K14() {





  if (era.get('tflag:18') == 0) { // :5774
    await era.printAndWait(''); // :5775
    return 0; // :5776

  } else if (era.get('tflag:18') == 1) { // :5778
    await era.printAndWait(''); // :5779
    return 0; // :5780
  } else if (era.get('tflag:18') == 2) { // :5781

    if (era.get(`cflag:${a}:504`) == 0) { // :5783
      await era.printAndWait(''); // :5784

    } else if (era.get(`cflag:${a}:504`) == 1) { // :5786

      if (era.get(`talent:${a}:0`) == 1) { // :5788
        await era.printAndWait(''); // :5789
      } else { // :5790
        await era.printAndWait(''); // :5791
      } // :5792

    } else if (era.get(`cflag:${a}:504`) == 2) { // :5794

      if (era.get(`talent:${a}:0`) == 1) { // :5796
        await era.printAndWait(''); // :5797
      } else { // :5798
        await era.printAndWait(''); // :5799
      } // :5800

    } else if (era.get(`cflag:${a}:504`) == 3) { // :5802

      if (era.get(`talent:${a}:0`) == 1) { // :5804
        await era.printAndWait(''); // :5805
      } else { // :5806
        await era.printAndWait(''); // :5807
      } // :5808

    } else if (era.get(`cflag:${a}:504`) == 4) { // :5810
      await era.printAndWait(''); // :5811

    } else if (era.get(`cflag:${a}:504`) == 5) { // :5813

      if (era.get(`abl:${a}:2`) > era.get(`abl:${a}:3`)) { // :5815
        await era.printAndWait(''); // :5816

      } else { // :5818
        await era.printAndWait(''); // :5819
      } // :5820

    } else if (era.get(`cflag:${a}:504`) == 6) { // :5822
      await era.printAndWait(''); // :5823

    } else if (era.get(`cflag:${a}:504`) == 7) { // :5825

      if (era.get(`talent:${a}:0`) == 1) { // :5827
        await era.printAndWait(''); // :5828
      } else { // :5829
        await era.printAndWait(''); // :5830
      } // :5831

    } else if (era.get(`cflag:${a}:504`) == 8) { // :5833
      await era.printAndWait(''); // :5834

    } else if (era.get(`cflag:${a}:504`) == 9) { // :5836

      if (era.get(`abl:${a}:2`) > era.get(`abl:${a}:3`)) { // :5838
        await era.printAndWait(''); // :5839

      } else { // :5841
        await era.printAndWait(''); // :5842
      } // :5843
    } else { // :5844
    } // :5845
  } // :5846

}

// @OSIOKI_KOUJO_K14 // :5848
async function OSIOKI_KOUJO_K14() {





  if (era.get('tflag:18') == 0) { // :5854
    await era.printAndWait(''); // :5855

  } else if (era.get('tflag:18') == 1) { // :5857

    if (era.get(`abl:${a}:21`) >= 3) { // :5859
      await era.printAndWait(''); // :5860
    } else { // :5861
      await era.printAndWait(''); // :5862
    } // :5863

  } else if (era.get('tflag:18') == 2) { // :5865

    if (era.get(`abl:${a}:17`) >= 4) { // :5867
      await era.printAndWait(''); // :5868
    } else { // :5869
      await era.printAndWait(''); // :5870
    } // :5871

  } else if (era.get('tflag:18') == 3) { // :5873

    if (era.get(`abl:${a}:17`) >= 6) { // :5875
      await era.printAndWait(''); // :5876
    } else { // :5877
      await era.printAndWait(''); // :5878
    } // :5879

  } else if (era.get('tflag:18') == 4) { // :5881

    if (era.get(`abl:${a}:21`) >= 3) { // :5883
      await era.printAndWait(''); // :5884
    } else { // :5885
      await era.printAndWait(''); // :5886
    } // :5887

  } else if (era.get('tflag:18') == 5) { // :5889

    if (era.get(`talent:${a}:88`) == 1 || era.get(`talent:${a}:76`) == 1) { // :5891
      await era.printAndWait(''); // :5892
    } else { // :5893
      await era.printAndWait(''); // :5894
    } // :5895

  } else if (era.get('tflag:18') == 6) { // :5897
    await era.print(''); // :5898

  } else if (era.get('tflag:18') == 7) { // :5900
    await era.print(''); // :5901

  } else if (era.get('tflag:18') == 8) { // :5903
    await era.printAndWait(''); // :5904

  } else if (era.get('tflag:18') == 9) { // :5906
    await era.printAndWait(''); // :5907
  } // :5908


}

// @GOBI_KOUJO_K14, ARG:0 // :5911
async function GOBI_KOUJO_K14() {


  if (arg_0 == 1) { // :5914

    await era.print(`哦~♪`); // :5916
  } else if (arg_0 == 2) { // :5917

    await era.print(`哦！`); // :5919
  } else if (arg_0 == 3) { // :5920

    await era.print(`啦……。`); // :5922
  } else if (arg_0 == 4) { // :5923

    await era.print(`什么的……。`); // :5925
  } else if (arg_0 == 5) { // :5926

    await era.print(`什么啊……。`); // :5928
  } else { // :5929


    if (rand_n(3) == 0) { // :5932
      await era.print(`啦。`); // :5933
    } else if (rand_n(2) == 0) { // :5934
      await era.print(`嘛。`); // :5935
    } else { // :5936
      await era.print(`的啦。`); // :5937
    } // :5938
  } // :5939

  return 0; // :5941





}

// ===== 复核清单（转译器生成，agent 逐条处理后删除） =====
// 1. :53 同名函数 @EVENTTRAIN 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTTRAIN', …) 注册，参照 ere/kojo/kojo-k5.js
// 2. :319 假名残留 归一后仍含日文假名：あなたのでうっとりしくがまにをけよていはもわだろ —— 查 lang-table 是否应收
// 3. :360 CALL K14_KOJO2 —— 口上文件里多为存根调用，人工定存根名
// 4. :420 CALL K14_KOJO2 —— 口上文件里多为存根调用，人工定存根名
// 5. :472 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 6. :474 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 7. :485 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 8. :487 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 9. :500 同名函数 @EVENTEND 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTEND', …) 注册，参照 ere/kojo/kojo-k5.js
// 10. :615 CALL DOG_KOJO_14 —— 口上文件里多为存根调用，人工定存根名
// 11. :620 CALL COLOSSEUM_KOJO_14 —— 口上文件里多为存根调用，人工定存根名
// 12. :719 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
// 13. :721 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 14. :725 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 15. :729 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 16. :733 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 17. :737 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 18. :767 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 19. :769 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 20. :778 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 21. :791 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 22. :793 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 23. :802 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 24. :811 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 25. :820 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 26. :1663 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 27. :1665 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 28. :1673 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 29. :1675 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 30. :1736 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 31. :1738 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 32. :1746 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 33. :1748 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 34. :1808 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 35. :1810 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 36. :1818 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 37. :1820 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 38. :1865 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 39. :1867 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 40. :1879 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 41. :1924 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 42. :1969 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 43. :1971 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 44. :1983 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 45. :2028 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 46. :2040 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 47. :2088 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 48. :2096 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 49. :2195 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 50. :2207 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 51. :2311 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 52. :2313 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 53. :2315 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 54. :2323 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 55. :2325 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 56. :2327 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 57. :2335 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 58. :2337 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 59. :2339 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 60. :2419 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 61. :2431 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 62. :2930 表达式寻址 PALAMLV:3 → era.get('palamlv:3')——族名直译，语义与归属人工定
// 63. :3853 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 64. :3855 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 65. :3863 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 66. :3865 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 67. :3873 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 68. :3875 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 69. :3924 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 70. :3932 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 71. :3940 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 72. :3988 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 73. :3996 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 74. :4004 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 75. :4124 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 76. :4126 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 77. :4134 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 78. :4136 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 79. :4138 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 80. :4146 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 81. :4148 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 82. :4150 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 83. :4158 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 84. :4160 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 85. :4162 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 86. :4385 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
// 87. :4386 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 88. :4412 局部变量 P = PALAM:5 + UP:5 —— 单字母局部变量，JS 侧声明与赋值人工定
// 89. :4413 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 90. :4439 局部变量 P = PALAM:8 + UP:8 —— 单字母局部变量，JS 侧声明与赋值人工定
// 91. :4440 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 92. :4454 局部变量 P = PALAM:10 + UP:10 —— 单字母局部变量，JS 侧声明与赋值人工定
// 93. :4455 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 94. :4469 表达式寻址 NOWEX:0 → era.get('nowex:0')——族名直译，语义与归属人工定
// 95. :4483 表达式寻址 NOWEX:1 → era.get('nowex:1')——族名直译，语义与归属人工定
// 96. :4500 表达式寻址 NOWEX:2 → era.get('nowex:2')——族名直译，语义与归属人工定
// 97. :4517 表达式寻址 NOWEX:3 → era.get('nowex:3')——族名直译，语义与归属人工定
// 98. :4531 局部变量 A = UP:11 + UP:12 —— 单字母局部变量，JS 侧声明与赋值人工定
// 99. :5225 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 100. :5226 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 101. :5227 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 102. :5231 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 103. :5232 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 104. :5233 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 105. :5289 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 106. :5291 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 107. :5300 假名残留 归一后仍含日文假名：のこれほどとは —— 查 lang-table 是否应收
// 108. :5306 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 109. :5308 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 110. :5343 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 111. :5345 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 112. :5360 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 113. :5362 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 114. :5380 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 115. :5382 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 116. :5397 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 117. :5399 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 118. :5735 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 119. :5738 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 120. :5749 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 121. :5752 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 122. :5755 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 123. :5758 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 124. :5761 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 125. :5764 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 126. :5911 函数参数 @GOBI_KOUJO_K14, ARG:0 —— 参数声明已剥（JS 函数签名人工定）
// 127. :5914 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 128. :5917 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 129. :5920 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 130. :5923 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 131. :5926 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 132. :5932 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 133. :5934 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
