/* eslint-disable no-undef, no-unused-vars, no-irregular-whitespace, no-redeclare, no-unreachable, no-dupe-else-if */
/**
 * @file EVENT_K4_冷徹.ERB 的口上转译产物（issue #107 原型，待复核）
 *
 * 源: target/ERB/口上/EVENT_K4_冷徹.ERB
 *
 * == 已有门面名的下标（复核时可升级为门面读写，裁定一） ==
 *   cflag:1 = 状态（facade-names）
 *   cflag:201 = 初调教（facade-names）
 *   cflag:221 = 首次润滑Lv2（facade-names）
 *   cflag:222 = 首次欲情Lv2（facade-names）
 *   cflag:223 = 首次耻情Lv2（facade-names）
 *   cflag:224 = 首次恐怖Lv2（facade-names）
 *   cflag:225 = 首次C绝顶（facade-names）
 *   cflag:226 = 首次V绝顶（facade-names）
 *   cflag:227 = 首次A绝顶（facade-names）
 *   cflag:228 = 首次B绝顶（facade-names）
 *   cflag:229 = 处女丧失（facade-names）
 *   cflag:261 = 调教后自慰（facade-names）
 *   cflag:262 = 百合PLAY（facade-names）
 *   cflag:263 = 朝口交（facade-names）
 *   cflag:264 = 调教后性交（facade-names）
 *   cflag:265 = 夜袭（facade-names）
 *   cflag:271 = 妊娠发觉（facade-names）
 *   cflag:272 = 生产（facade-names）
 *   cflag:297 = 苦痛刻印Lv3（facade-names）
 *   cflag:298 = 快乐刻印Lv3（facade-names）
 *   cflag:299 = 屈服刻印Lv3（facade-names）
 *   cflag:300 = 反抗刻印Lv3（facade-names）
 *   cflag:301 = 爱抚（facade-names）
 *   cflag:302 = 舔阴（facade-names）
 *   cflag:303 = 肛门爱抚（facade-names）
 *   cflag:304 = 自慰（facade-names）
 *   cflag:306 = 胸爱抚（facade-names）
 *   cflag:307 = 接吻（facade-names）
 *   cflag:308 = 自己扒开（facade-names）
 *   cflag:309 = 插入手指（facade-names）
 *   cflag:310 = 舔肛（facade-names）
 *   cflag:311 = 振动宝石（facade-names）
 *   cflag:312 = 壶虫（facade-names）
 *   cflag:313 = 振动杖（facade-names）
 *   cflag:314 = 肛门虫（facade-names）
 *   cflag:315 = 阴蒂夹（facade-names）
 *   cflag:316 = 乳头夹（facade-names）
 *   cflag:317 = 榨乳器（facade-names）
 *   cflag:320 = 肛珠（facade-names）
 *   cflag:321 = 正常位（facade-names）
 *   cflag:322 = 背后位（facade-names）
 *   cflag:323 = 对面座位（facade-names）
 *   cflag:324 = 背面座位（facade-names）
 *   cflag:327 = 正常位肛交（facade-names）
 *   cflag:328 = 背后位肛交（facade-names）
 *   cflag:329 = 对面座位肛交（facade-names）
 *   cflag:330 = 背面座位肛交（facade-names）
 *   cflag:331 = 手淫（facade-names）
 *   cflag:332 = 口交_奴（facade-names）
 *   cflag:333 = 乳交（facade-names）
 *   cflag:334 = 股间性交（facade-names）
 *   cflag:335 = 骑乘位（facade-names）
 *   cflag:336 = 全身擦洗（facade-names）
 *   cflag:337 = 骑乘位肛交（facade-names）
 *   cflag:338 = 肛门侍奉（facade-names）
 *   cflag:341 = 打屁股（facade-names）
 *   cflag:342 = 鞭（facade-names）
 *   cflag:343 = 针（facade-names）
 *   cflag:344 = 眼罩（facade-names）
 *   cflag:345 = 绳子（facade-names）
 *   cflag:346 = 口塞（facade-names）
 *   cflag:347 = 灌肠肛塞（facade-names）
 *   cflag:356 = 放置PLAY（facade-names）
 *   cflag:357 = 交谈（facade-names）
 *   cflag:372 = 壶虫着脱（facade-names）
 *   cflag:374 = 肛门虫着脱（facade-names）
 *   cflag:375 = 阴蒂夹着脱（facade-names）
 *   cflag:376 = 乳头夹着脱（facade-names）
 *   cflag:377 = 榨乳器着脱（facade-names）
 *   cflag:379 = 肛珠着脱（facade-names）
 *   cflag:380 = 眼罩着脱（facade-names）
 *   cflag:381 = 强制口交（facade-names）
 *   cflag:385 = 绳子着脱（facade-names）
 *   cflag:386 = 口塞着脱（facade-names）
 *   cflag:650 = NTR再捕获（facade-names）
 *   flag:62 = 肉便器行动（facade-names）
 *   flag:63 = 肉便器常识改写（facade-names）
 *   flag:7 = 口上开关（facade-names）
 *   tflag:13 = 初吻与自我口上（facade-names）
 *   tflag:150 = 反抗刻印回避（facade-names）
 *   tflag:16 = 犬射精或处刑口上（facade-names）
 *   tflag:18 = 足交射精或处遇口上（facade-names）
 *   tflag:20 = 主人导致处女丧失（facade-names）
 *   tflag:21 = 反抗刻印变动（facade-names）
 *   tflag:22 = 苦痛刻印变动（facade-names）
 *   tflag:23 = 快乐刻印变动（facade-names）
 *   tflag:24 = 屈服刻印变动（facade-names）
 *   tflag:3 = 处女丧失（facade-names）
 *   tflag:400 = 死斗场敌种（facade-names）
 *   tflag:500 = 博物馆口上（facade-names）
 *   tflag:510 = 流放口上（facade-names）
 *   tflag:520 = 公开处刑口上（facade-names）
 *   tflag:530 = 猎奇处刑口上（facade-names）
 *   tflag:899 = 失神（facade-names）
 *
 * == 复核标记（130 处） ==
 * 本文件由 tools/kojo-transpiler.js 生成。以下位置是机械转换无法
 * 确定的，须 agent 逐字对照 ERB 源复核（裁定 7：agent 逐字对照，
 * 不是抽查）。复核成果 = 在本文件内改写成最终形态，并把本 REVIEW
 * 清单逐条删掉；转译器默认不覆盖本文件（产物边界，issue #10），
 * 复核成果不会被重跑覆盖。
 *   1. :75 同名函数 @EVENTTRAIN 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTTRAIN', …) 注册，参照 ere/kojo/kojo-k5.js
 *   2. :152 插值 未知插值 %CALLNAME:MASTER% —— 保真锁会红，须人工定归一
 *   3. :160 插值 未知插值 %CALLNAME:MASTER% —— 保真锁会红，须人工定归一
 *   4. :162 插值 未知插值 %CALLNAME:MASTER% —— 保真锁会红，须人工定归一
 *   5. :169 CALL K4_KOJO2 —— 口上文件里多为存根调用，人工定存根名
 *   6. :229 CALL K4_KOJO2 —— 口上文件里多为存根调用，人工定存根名
 *   7. :316 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   8. :318 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   9. :324 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   10. :326 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   11. :392 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   12. :394 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   13. :400 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   14. :402 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   15. :456 同名函数 @EVENTEND 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTEND', …) 注册，参照 ere/kojo/kojo-k5.js
 *   16. :533 CALL DOG_KOJO_4 —— 口上文件里多为存根调用，人工定存根名
 *   17. :541 CALL COLOSSEUM_KOJO_4 —— 口上文件里多为存根调用，人工定存根名
 *   18. :639 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   19. :641 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   20. :645 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   21. :649 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   22. :653 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   23. :657 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   24. :687 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   25. :689 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   26. :698 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   27. :711 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   28. :713 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   29. :722 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   30. :731 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   31. :740 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   32. :1524 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   33. :1526 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   34. :1588 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   35. :1590 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   36. :1598 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   37. :1600 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   38. :1625 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   39. :1627 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   40. :1635 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   41. :1637 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   42. :1696 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   43. :1698 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   44. :1706 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   45. :1708 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   46. :1767 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   47. :1769 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   48. :1777 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   49. :1779 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   50. :1824 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   51. :1826 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   52. :1838 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   53. :1883 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   54. :1895 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   55. :1940 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   56. :1942 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   57. :1954 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   58. :2000 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   59. :2012 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   60. :2073 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   61. :2094 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   62. :2192 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   63. :2204 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   64. :2313 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   65. :2315 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   66. :2317 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   67. :2326 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   68. :2328 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   69. :2330 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   70. :2339 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   71. :2341 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   72. :2343 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   73. :2427 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   74. :2443 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   75. :2950 表达式寻址 PALAMLV:3 → era.get('palamlv:3')——族名直译，语义与归属人工定
 *   76. :3391 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   77. :3395 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   78. :3457 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   79. :3517 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   80. :3525 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   81. :3533 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   82. :3652 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   83. :3654 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   84. :3662 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   85. :3664 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   86. :3666 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   87. :3674 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   88. :3676 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   89. :3678 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   90. :3686 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   91. :3688 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   92. :3690 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   93. :3936 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   94. :3937 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   95. :3965 局部变量 P = PALAM:5 + UP:5 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   96. :3966 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   97. :3992 局部变量 P = PALAM:8 + UP:8 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   98. :3993 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   99. :4007 局部变量 P = PALAM:10 + UP:10 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   100. :4008 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   101. :4022 表达式寻址 NOWEX:0 → era.get('nowex:0')——族名直译，语义与归属人工定
 *   102. :4036 表达式寻址 NOWEX:1 → era.get('nowex:1')——族名直译，语义与归属人工定
 *   103. :4053 表达式寻址 NOWEX:2 → era.get('nowex:2')——族名直译，语义与归属人工定
 *   104. :4070 表达式寻址 NOWEX:3 → era.get('nowex:3')——族名直译，语义与归属人工定
 *   105. :4084 局部变量 A = UP:11 + UP:12 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   106. :4305 CALL SELL_MATURO_K0 —— 口上文件里多为存根调用，人工定存根名
 *   107. :4650 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   108. :4774 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   109. :4776 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   110. :4791 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   111. :4793 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   112. :4829 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   113. :4831 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   114. :4846 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   115. :4848 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   116. :4866 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   117. :4868 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   118. :4883 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   119. :4885 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   120. :4955 表达式寻址 ITEM:PBAND → era.get('item:PBAND')——族名直译，语义与归属人工定
 *   121. :4988 表达式寻址 ITEM:PBAND → era.get('item:PBAND')——族名直译，语义与归属人工定
 *   122. :5012 表达式寻址 ITEM:PBAND → era.get('item:PBAND')——族名直译，语义与归属人工定
 *   123. :5429 函数参数 @GOBI_KOUJO_K4, ARG:0 —— 参数声明已剥（JS 函数签名人工定）
 *   124. :5432 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   125. :5435 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   126. :5438 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   127. :5441 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   128. :5444 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   129. :5450 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   130. :5452 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 */

'use strict';

// 需要复核 agent 补的导入（产物初稿不 require，保真锁会红）：
// const era = require('#/era-electron');
// const { heart, self_call, self_call_first } = require('#/kojo/kojo-text');
// const { chara_callname } = require('#/utils/callname-utils');
// 以及 target_name / player_name / assi_name / master_name / sc() / scf()
// 的取值（%SAVESTR:TARGET% 等插值的 JS 侧表达式）。




























































// @EVENTTRAIN // :61
async function EVENTTRAIN() {
  // #PRI（事件优先级修饰符，JS 侧用 on() 的 TIER 表达） // :62
  // FLAG:104  = 1（变量语义：FLAG 族，104） // :63
  era.set('flag:104', 1); // :63
  if (era.get('flag:7') == 0) { // :65
    // FLAG:7  = 2（变量语义：FLAG 族，7） // :65
    era.set('flag:7', 2); // :65
  } // :65

}

// @EVENTEND // :67
async function EVENTEND() {
  // #LATER（事件优先级修饰符，JS 侧用 on() 的 TIER 表达） // :68
  // FLAG:104  = 0（变量语义：FLAG 族，104） // :69
  era.set('flag:104', 0); // :69





}

// @EVENTTRAIN // :75
async function EVENTTRAIN() {
  if (era.get('flag:7') <= 0) { // :77
    return 0; // :77
  } // :77
  if (era.get(`talent:${target}:164`) != 1) { // :79
    return 0; // :79
  } // :79




  if (era.get(`cflag:${target}:201`) == 0) { // :84
    era.drawLine(); // :85
    if (era.get(`talent:${target}:314`) == 3) { // :86

      await era.printAndWait(`「如果以为我和那些在魔界堕落的同胞一样，就大错特错啦！」`); // :88
      await era.printAndWait(`「流淌着的高贵血脉，绝不会被你玷污！……」`); // :89
    } else if (era.get(`talent:${target}:314`) == 4) { // :90

      await era.printAndWait(`「身为精灵勇者的我，不可能屈从于黑暗！」`); // :92
      await era.printAndWait(`「哪怕同伴都在淫威下屈服了……我也坚贞不屈！」`); // :93
    } else { // :94
      await era.printAndWait(`「…原来如此，用这样的牢狱来封住我的力量啊…」`); // :95
      await era.printAndWait(`「哼，卑鄙。这都是徒劳的笑话罢了，我绝不屈服。」`); // :96
    } // :97

    if (era.get(`cflag:${target}:42`) == 83) { // :100
      await era.printAndWait(`这么说着、${target_name}推了推眼镜…`); // :100
    } // :100
    // CFLAG:201  = 1（变量语义：CFLAG 族，201） // :101
    era.set(`cflag:${target}:201`, 1); // :101
    return 1; // :102



  } else if (era.get(`cflag:${target}:201`) >= 1 && era.get(`cflag:${target}:650`) == 1) { // :106
    if (era.get(`talent:${target}:85`) || era.get(`talent:${target}:76`)) { // :107
      era.drawLine(); // :108
      await era.printAndWait(`将看了那水晶球的事告诉了${target_name}之后，她的脸色苍白了。`); // :109
      await era.printAndWait(`「………和别人私通的家伙摆出这样的脸，怎么，准备哭了么是要？」`); // :110
      await era.printAndWait(`「我……被狂王抱着，感受到了无上的快乐……那样……可耻……可耻的姿态……哇……呜呜呜……」`); // :111
      await era.printAndWait(`${target_name}泪流满面。对背叛${player_name}感到后悔，或对被狂王弄得高潮感到悔恨，也许两者都有吧………`); // :112

      // CFLAG:650  = 0（变量语义：CFLAG 族，650） // :114
      era.set(`cflag:${target}:650`, 0); // :114
    } else { // :115
      era.drawLine(); // :116
      await era.printAndWait(`「唉……曾经背叛过的我，已经没资格再成为魔王的东西了………」`); // :117
      await era.printAndWait(`「我只是战败之身，要杀要剐，悉随尊便。」`); // :118

      // CFLAG:650  = 0（变量语义：CFLAG 族，650） // :120
      era.set(`cflag:${target}:650`, 0); // :120
    } // :121
    return 1; // :122




  } else if (era.get(`cflag:${target}:201`) < 2 && era.get(`mark:${target}:2`) == 1) { // :127
    era.drawLine(); // :128
    await era.printAndWait(`「哼……都是徒劳的，徒劳的！」`); // :129
    // CFLAG:201  = 2（变量语义：CFLAG 族，201） // :130
    era.set(`cflag:${target}:201`, 2); // :130
    return 1; // :131


  } else if (era.get(`cflag:${target}:201`) < 3 && era.get(`mark:${target}:2`) == 2) { // :134
    era.drawLine(); // :135
    await era.printAndWait(`「我，是怎么了？不！我还是我！还是我！不会屈服！」`); // :136
    // CFLAG:201  = 3（变量语义：CFLAG 族，201） // :137
    era.set(`cflag:${target}:201`, 3); // :137
    return 1; // :138


  } else if (era.get(`cflag:${target}:201`) < 4 && era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0) { // :141
    era.drawLine(); // :142
    await era.printAndWait(`「再，再也受不了啦……哇！呜呜……是我输了……多么屈辱啊…」`); // :143
    // CFLAG:201  = 4（变量语义：CFLAG 族，201） // :144
    era.set(`cflag:${target}:201`, 4); // :144
    return 1; // :145


  } else if (era.get(`cflag:${target}:201`) < 5 && era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:85`) == 0) { // :148
    era.drawLine(); // :149
    await era.printAndWait(`${target_name}一贯以来的冷静感完全消失了。`); // :150
    await era.printAndWait(`彻底成为了一只被快感所俘虏的牝奴。`); // :151
    await era.printAndWait(`四脚爬爬，扭动着腰，用炽热的视线仰视着%CALLNAME:MASTER%。`); // :152
    await era.printAndWait(`「我，我不行了……我是肉棒最忠实的奴隶！……唔…喔……请，请给我……肉棒！」`); // :153
    // CFLAG:201  = 5（变量语义：CFLAG 族，201） // :154
    era.set(`cflag:${target}:201`, 5); // :154
    return 1; // :155


  } else if (era.get(`cflag:${target}:201`) < 6 && era.get(`talent:${target}:85`) == 1) { // :158
    era.drawLine(); // :159
    await era.printAndWait(`${target_name}害羞地向%CALLNAME:MASTER%表白了。`); // :160
    await era.printAndWait(`「我其实留意了很久了，魔王大人有着与众不同闪光点……啊～不不～不要说这种多余的话！」`); // :161
    await era.printAndWait(`${target_name}有点手足无措，看了%CALLNAME:MASTER%一眼，红着脸，说「魔王大人……以后，请让我侍奉左右吧……」`); // :162
    await era.printAndWait(`${target_name}顺势跪下，亲吻着你的手。`); // :163
    // CFLAG:201  = 6（变量语义：CFLAG 族，201） // :164
    era.set(`cflag:${target}:201`, 6); // :164
    return 1; // :165


  } else if (era_flag.assi < 0) { // :168
    // CALL K4_KOJO2 // :169


























































  } else { // :228
    // CALL K4_KOJO2 // :229
  } // :230





}

// @K4_KOJO2 // :236
async function K4_KOJO2() {

  if (era.get(`mark:${target}:3`) == 3 && era.get('flag:7') == 2) { // :238
    era.drawLine(); // :239
    await era.printAndWait(`「你这肮脏可悲的生物……」`); // :240
    await era.print(`${target_name}`); // :241

    if (era.get(`cflag:${target}:42`) == 83) { // :244
      await era.print(`眼镜下`); // :244
    } // :244
    await era.printAndWait(`的目光异常冰冷…`); // :245
    return 1; // :246


  } else if (era.get(`mark:${target}:2`) == 0 && era.get('flag:7') == 2) { // :249
    era.drawLine(); // :250
    if (era.get(`talent:${target}:11`) || era.get(`talent:${target}:12`) || era.get(`talent:${target}:16`)) { // :251

      await era.printAndWait(`「别逗我笑了，就凭你是无法得到我的心的！」`); // :253
    } else if (era.get(`talent:${target}:13`) || era.get(`talent:${target}:14`)) { // :254

      await era.printAndWait(`「到底想我怎么样嘛……？这，这样的话……」`); // :256
    } else { // :257
      await era.printAndWait(`「我不觉得你能搞定我啊。」`); // :258
    } // :259
    return 1; // :260


  } else if (era.get(`mark:${target}:2`) == 1 && era.get('flag:7') == 2) { // :263
    era.drawLine(); // :264

    if (era.get(`talent:${target}:11`) || era.get(`talent:${target}:12`) || era.get(`talent:${target}:16`)) { // :266

      await era.printAndWait(`「你这家伙……才不会输给你这样的家伙！」`); // :268
    } else if (era.get(`talent:${target}:13`) || era.get(`talent:${target}:14`)) { // :269

      await era.printAndWait(`「我很坚强……我……没事的」`); // :271
    } else { // :272
      await era.printAndWait(`「你，想干嘛？不不，不要去想这些……」`); // :273
    } // :274

    return 1; // :276


  } else if (era.get(`mark:${target}:2`) == 2 && era.get('flag:7') == 2) { // :279
    era.drawLine(); // :280

    if (era.get(`talent:${target}:11`) || era.get(`talent:${target}:12`) || era.get(`talent:${target}:16`)) { // :282

      await era.printAndWait(`「可恶……不是这样的，事情不是这样的！」`); // :284
    } else if (era.get(`talent:${target}:13`) || era.get(`talent:${target}:14`)) { // :285

      await era.printAndWait(`「至少……请不要弄痛我……」`); // :287
    } else { // :288
      await era.printAndWait(`「别开玩笑了……这样的事……不该有的……」`); // :289
    } // :290

    return 1; // :292


  } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0&& era.get(`talent:${target}:76`) == 0 && era.get('flag:7') == 2) { // :295
    era.drawLine(); // :296

    if (era.get(`talent:${target}:11`) || era.get(`talent:${target}:12`) || era.get(`talent:${target}:16`)) { // :298

      await era.printAndWait(`「哼……你高兴就好……」`); // :300
    } else if (era.get(`talent:${target}:13`) || era.get(`talent:${target}:14`)) { // :301

      await era.printAndWait(`「啊……这样的话……」`); // :303
    } else { // :304
      await era.printAndWait(`「这场胜负……是我输了吗……」`); // :305
    } // :306

    return 1; // :308


  } else if (era.get(`talent:${target}:76`) == 1 && era.get('flag:7') == 2) { // :311
    era.drawLine(); // :312

    if (era.get(`talent:${target}:314`) == 3) { // :314

      if (rand_n(3) == 0) { // :316
        await era.printAndWait(`「比对鲜血还要渴求……人家，已经忍不住了啦…！」`); // :317
      } else if (rand_n(2) == 0) { // :318
        await era.printAndWait(`「来把我弄满满地吧……呵呵～」`); // :319
      } else { // :320
        await era.printAndWait(`「我的高贵血脉……就献给魔王大人吧～」`); // :321
      } // :322
    } else { // :323
      if (rand_n(3) == 0) { // :324
        await era.printAndWait(`「嗯～不要再让人家等啦～快用肉棒，狠狠地干坏我的穴啊…！」`); // :325
      } else if (rand_n(2) == 0) { // :326
        await era.printAndWait(`「啊…！肉棒终于来了！」`); // :327
      } else { // :328
        await era.printAndWait(`「嘻嘻，从今早开始，腹中就一直在叫想被肉棒塞满啊！」`); // :329
      } // :330
    } // :331


    if (era.get(`talent:${target}:74`)) { // :334

      await era.printAndWait(`「今天也请来看我自慰吧……♪」`); // :336

    } else if (era.get(`talent:${target}:74`)) { // :338

      await era.printAndWait(`「子宫疼得没办法了啦……♪」`); // :340

    } else if (era.get(`talent:${target}:77`)) { // :342

      await era.printAndWait(`「今天也请用菊花把我操去吧……♪」`); // :344

    } else if (era.get(`talent:${target}:78`)) { // :346

      await era.printAndWait(`「人家的乳头，已经完全勃起了啦……♪」`); // :348

    } // :350

    if (era.get(`talent:${target}:83`)) { // :352

      await era.printAndWait(`「嘻嘻，还想更多更多地去欺负别人……」`); // :354

    } else if (era.get(`talent:${target}:88`)) { // :356

      await era.printAndWait(`「啊……请尽情地欺负作为下贱母猪的我吧……」`); // :358

    } // :360

    if (era.get(`talent:${target}:83`)) { // :362

      await era.printAndWait(`「那个，能早点去外面吗？在房间里不够刺激啦～」`); // :364

    } // :366

    if (era.get(`talent:${target}:136`)) { // :368

      await era.printAndWait(`「啊……好像快点交配啊……我……只是只牝兽…………」`); // :370

    } // :372

    if (era.get(`talent:${target}:204`)) { // :374

      await era.printAndWait(`「请对着作为肉便器的我，尽情地排泄吧！」`); // :376

    } // :378


    if (era.get(`cflag:${target}:42`) == 83) { // :382
      await era.printAndWait(`${target_name}眼镜后的双眼已经被情欲的火焰点燃了…`); // :382
    } // :382

    return 1; // :384


  } else if (era.get(`talent:${target}:85`) == 1 && era.get('flag:7') == 2) { // :387
    era.drawLine(); // :388

    if (era.get(`talent:${target}:314`) == 4) { // :390

      if (rand_n(3) == 0) { // :392
        await era.printAndWait(`「作为黑暗勇者的我，会常伴您左右……」`); // :393
      } else if (rand_n(2) == 0) { // :394
        await era.printAndWait(`「你的愉悦，我的幸福……」`); // :395
      } else { // :396
        await era.printAndWait(`「来下命令吧。你的话，我什么都会听从的………」`); // :397
      } // :398
    } else { // :399
      if (rand_n(3) == 0) { // :400
        await era.printAndWait(`「呵呵～来啦？　今天也要好好疼爱人家啊～」`); // :401
      } else if (rand_n(2) == 0) { // :402
        await era.printAndWait(`「能被你抱着……啊……真是迫不及待啊…」`); // :403
      } else { // :404
        await era.printAndWait(`「今天也请温柔地对人家吧…。人家已经做好准备啦～」`); // :405
      } // :406
    } // :407


    if (era.get(`talent:${target}:74`)) { // :410

      await era.printAndWait(`「看看啊……我这……羞耻的样子……♪」`); // :412

    } else if (era.get(`talent:${target}:74`)) { // :414

      await era.printAndWait(`「来一次灵肉交汇的爱爱吧！♪」`); // :416

    } else if (era.get(`talent:${target}:77`)) { // :418

      await era.printAndWait(`「后面的小穴，已经想你想得发疼了……♪」`); // :420

    } else if (era.get(`talent:${target}:78`)) { // :422

      await era.printAndWait(`「请彻底地玩弄我的胸部吧……♪」`); // :424

    } // :426

    if (era.get(`talent:${target}:83`)) { // :428

      await era.printAndWait(`「来玩严厉PLAY吧！」`); // :430

    } else if (era.get(`talent:${target}:88`)) { // :432

      await era.printAndWait(`「请对作为牝奴隶的我赐予恩惠……」`); // :434

    } // :436

    if (era.get(`talent:${target}:83`)) { // :438

      await era.printAndWait(`「今天会带我去哪里么……？」`); // :440

    } // :442


    if (era.get(`cflag:${target}:42`) == 83) { // :446
      await era.printAndWait(`${target_name}眼镜后的双眼已经被爱的火焰点燃了…`); // :446
    } // :446

    return 1; // :448
  } // :449
  return 0; // :450





}

// @EVENTEND // :456
async function EVENTEND() {
  if (era.get('flag:7') <= 0) { // :458
    return 0; // :458
  } // :458
  if (era.get(`talent:${target}:164`) != 1) { // :460
    return 0; // :460
  } // :460


  if (era.get(`base:${target}:0`) <= 0) { // :464
    return 0; // :464
  } // :464





  if (era.get(`mark:${target}:3`) == 3 && era.get(`talent:${target}:85`) == 0) { // :470
    era.drawLine(); // :471
    await era.printAndWait(`「可恶！」`); // :472
    return 1; // :473


  } else if (era.get(`mark:${target}:2`) <= 1 && era.get(`talent:${target}:85`) == 0) { // :476
    era.drawLine(); // :477
    await era.printAndWait(`「哈哈……就这，这种程度……」`); // :478
    return 1; // :479


  } else if (era.get(`mark:${target}:2`) == 2 && era.get(`talent:${target}:85`) == 0) { // :482
    era.drawLine(); // :483
    await era.printAndWait(`「还没，还不能屈服！…」`); // :484
    return 1; // :485


  } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0) { // :488
    era.drawLine(); // :489
    await era.printAndWait(`「已……已经不行了啊……我回不去了………」`); // :490
    return 1; // :491


  } else if (era.get(`talent:${target}:76`) == 1 && era.get(`base:${target}:0`) >= 500) { // :494
    era.drawLine(); // :495
    await era.printAndWait(`「请……请再用肉棒蹂躏我………可以吗？」`); // :496
    return 1; // :497

  } else if (era.get(`talent:${target}:76`) == 1 && era.get(`base:${target}:0`) <= 500) { // :499
    era.drawLine(); // :500
    await era.printAndWait(`「要……要死了！…」`); // :501
    return 1; // :502


  } else if (era.get(`talent:${target}:85`) == 1 && era.get(`base:${target}:0`) >= 500) { // :505
    era.drawLine(); // :506
    await era.printAndWait(`「嘻嘻，这次也很完美～～」`); // :507
    return 1; // :508

  } else if (era.get(`talent:${target}:85`) == 1 && era.get(`base:${target}:0`) <= 500) { // :510
    era.drawLine(); // :511
    await era.printAndWait(`「呆子，就不能对人家再温柔些吗？…坏人～」`); // :512
    return 1; // :513
  } // :514
  return 0; // :515





}

// @KOJO_MESSAGE_COM_4 // :521
async function KOJO_MESSAGE_COM_4() {




  if (era.get(`tequip:${target}:45`) && era_flag.selectcom != 45) { // :527
    return 0; // :527
  } // :527

  if (era.get('tflag:899')) { // :530
    return 0; // :530
  } // :530

  if (era.get(`tequip:${target}:89`)) { // :532
    // CALL DOG_KOJO_4 // :533
    return 0; // :534
  } // :535

  if (era.get(`tequip:${target}:90`)) { // :538
    return 0; // :538
  } // :538

  if (era.get(`tequip:${target}:55`)) { // :540
    // CALL COLOSSEUM_KOJO_4 // :541
    return 0; // :542
  } // :543







  if (era_flag.selectcom == 0) { // :551

    if (era.get(`cflag:${target}:301`) == 0) { // :553

      if (era.get(`mark:${target}:2`) >= 2) { // :555
        await era.printAndWait(`「唔～唔……」「哼，这不挺配合的嘛！」`); // :556

      } else { // :558
        await era.printAndWait(`「讨厌！这变态！」`); // :559
      } // :560
      // CFLAG:301  = 1（变量语义：CFLAG 族，301） // :561
      era.set(`cflag:${target}:301`, 1); // :561
      return 0; // :562

    } else { // :564

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:301`) <= 5 || era.get('flag:7') == 2)) { // :566
        await era.printAndWait(`「唔…噢～…再弄，再弄我……胸，胸部也好……那里！……还有屁股，再揉啊～～…」`); // :567
        // CFLAG:301  = 6（变量语义：CFLAG 族，301） // :568
        era.set(`cflag:${target}:301`, 6); // :568

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:301`) <= 4 || era.get('flag:7') == 2)) { // :570
        await era.printAndWait(`「那么细腻，温柔的手法……人家会……啊！……噢～啊啊！……有，有感觉了～～…」`); // :571
        // CFLAG:301  = 5（变量语义：CFLAG 族，301） // :572
        era.set(`cflag:${target}:301`, 5); // :572

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:301`) <= 3 || era.get('flag:7') == 2)) { // :574
        await era.printAndWait(`「啊…好…那里……」`); // :575
        // CFLAG:301  = 4（变量语义：CFLAG 族，301） // :576
        era.set(`cflag:${target}:301`, 4); // :576

      } else if (era.get(`mark:${target}:2`) == 2 && (era.get(`cflag:${target}:301`) <= 2 || era.get('flag:7') == 2)) { // :578
        await era.printAndWait(`「快住手啊……再这样摸的话……我会………」`); // :579
        // CFLAG:301  = 3（变量语义：CFLAG 族，301） // :580
        era.set(`cflag:${target}:301`, 3); // :580

      } else if (era.get(`mark:${target}:2`) <= 1 && (era.get(`cflag:${target}:301`) <= 1 || era.get('flag:7') == 2)) { // :582
        await era.printAndWait(`「变态！！…完全不舒服，不要再摸了！」`); // :583
        // CFLAG:301  = 2（变量语义：CFLAG 族，301） // :584
        era.set(`cflag:${target}:301`, 2); // :584
      } // :585
      return 0; // :586
    } // :587
  } // :588




  if (era_flag.selectcom == 1) { // :593

    if (era.get(`cflag:${target}:302`) == 0) { // :595

      if (era.get(`talent:${target}:0`) == 1) { // :597
        await era.printAndWait(`「哇！呜呜！！快住手！！」`); // :598

      } else { // :600
        await era.printAndWait(`「那，那样的地方都舔！这个……大变态！」`); // :601
      } // :602
      // CFLAG:302  = 1（变量语义：CFLAG 族，302） // :603
      era.set(`cflag:${target}:302`, 1); // :603
      return 0; // :604

    } else { // :606

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:302`) <= 4 || era.get('flag:7') == 2)) { // :608
        await era.printAndWait(`「啊啊啊啊！好～好啊～♪　再舔我！再用力地吸～………唔喔，爱液要出来了～！！…」`); // :609
        // CFLAG:302  = 5（变量语义：CFLAG 族，302） // :610
        era.set(`cflag:${target}:302`, 5); // :610

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:302`) <= 3 || era.get('flag:7') == 2)) { // :612
        await era.printAndWait(`「呃啊～！好，好舒服……魔王大人啊！…再这么弄的话……人家……人家，会…………噢！～」`); // :613
        // CFLAG:302  = 4（变量语义：CFLAG 族，302） // :614
        era.set(`cflag:${target}:302`, 4); // :614

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:302`) <= 2 || era.get('flag:7') == 2)) { // :616
        await era.printAndWait(`「随，随你喜欢弄了！　…呃～啊啊啊啊啊！」`); // :617
        // CFLAG:302  = 3（变量语义：CFLAG 族，302） // :618
        era.set(`cflag:${target}:302`, 3); // :618

      } else if (era.get(`cflag:${target}:302`) <= 1 || era.get('flag:7') == 2) { // :620
        await era.printAndWait(`「那地方！好脏的！不要舔了啦！！」`); // :621
        // CFLAG:302  = 2（变量语义：CFLAG 族，302） // :622
        era.set(`cflag:${target}:302`, 2); // :622
      } // :623
      return 0; // :624
    } // :625
  } // :626




  if (era_flag.selectcom == 2) { // :631

    if (era.get(`cflag:${target}:303`) == 0) { // :633
      await era.printAndWait(`「笨，笨蛋！！在想什么哪？！快停手！！」`); // :634
      // CFLAG:TARGET:303  = 1（变量语义：CFLAG 族，TARGET:303） // :635
      era.set(`cflag:${target}:303`, 1); // :635
      return 0; // :636

    } else { // :638
      // 赋值 P = PALAM:3 + UP:3 // :639

      if (era.get(`talent:${target}:76`) == 1 && P >= era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 6 || era.get('flag:7') == 2)) { // :641
        await era.printAndWait(`「唔哦哦哦哦哦哦！！好，好啊………菊穴要融化了………快，快给我吧………」`); // :642
        // CFLAG:303  = 7（变量语义：CFLAG 族，303） // :643
        era.set(`cflag:${target}:303`, 7); // :643

      } else if (era.get(`talent:${target}:76`) == 1 && P < era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 5 || era.get('flag:7') == 2)) { // :645
        await era.printAndWait(`「啊啊啊………再，再湿一些的话……我想会更舒服些的………」`); // :646
        // CFLAG:303  = 6（变量语义：CFLAG 族，303） // :647
        era.set(`cflag:${target}:303`, 6); // :647

      } else if (era.get(`talent:${target}:85`) == 1 && P >= era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 4 || era.get('flag:7') == 2)) { // :649
        await era.printAndWait(`「唔哦～！魔王大人……屁股，屁股要融化在您的手里了………你让人家如何是好………」`); // :650
        // CFLAG:303  = 5（变量语义：CFLAG 族，303） // :651
        era.set(`cflag:${target}:303`, 5); // :651

      } else if (era.get(`talent:${target}:85`) == 1 && P < era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 3 || era.get('flag:7') == 2)) { // :653
        await era.printAndWait(`「啊呜～！再把人家弄湿一些………有点痛呢………」`); // :654
        // CFLAG:303  = 4（变量语义：CFLAG 族，303） // :655
        era.set(`cflag:${target}:303`, 4); // :655

      } else if (P >= era.get('palamlv:2') && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:303`) <= 2 || era.get('flag:7') == 2)) { // :657
        await era.printAndWait(`「不，不行了！！……屁股……我的屁股……要融化了…………」`); // :658
        // CFLAG:303  = 3（变量语义：CFLAG 族，303） // :659
        era.set(`cflag:${target}:303`, 3); // :659

      } else if (era.get(`cflag:${target}:223`) <= 1 || era.get('flag:7') == 2) { // :661
        await era.printAndWait(`「笨蛋！快住手！！我叫你住手啊！！」`); // :662
        // CFLAG:303  = 2（变量语义：CFLAG 族，303） // :663
        era.set(`cflag:${target}:303`, 2); // :663
      } // :664
      return 0; // :665
    } // :666
  } // :667




  if (era_flag.selectcom == 3) { // :672

    if (era.get(`cflag:${target}:304`) == 0) { // :674
      await era.printAndWait(`「想，想看这种东西…！？」`); // :675
      // CFLAG:TARGET:304  = 1（变量语义：CFLAG 族，TARGET:304） // :676
      era.set(`cflag:${target}:304`, 1); // :676
      return 0; // :677

    } else { // :679

      if (era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:304`) <= 8 || era.get('flag:7') == 2)) { // :681
        await era.printAndWait(`「唔～噢～……我怎么这么淫乱啊……明明，明明还未试过男人的滋味………」`); // :682
        // CFLAG:304  = 9（变量语义：CFLAG 族，304） // :683
        era.set(`cflag:${target}:304`, 9); // :683

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:304`) <= 7 || era.get('flag:7') == 2)) { // :685

        if (rand_n(3) == 0) { // :687
          await era.printAndWait(`「唔……噢噢……啊！……」`); // :688
        } else if (rand_n(2) == 0) { // :689
          await era.printAndWait(`「唔……噢噢……啊！……哈……哈……」`); // :690
        } else { // :691
          await era.printAndWait(`「唔……噢噢……啊！……呃！！！！」`); // :692
        } // :693
        // CFLAG:304  = 8（变量语义：CFLAG 族，304） // :694
        era.set(`cflag:${target}:304`, 8); // :694

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:31`) < 3 && (era.get(`cflag:${target}:304`) <= 6 || era.get('flag:7') == 2)) { // :696

        if (rand_n(2) == 0) { // :698
          await era.printAndWait(`「相对于自摸，还是更喜欢小鸡鸡啦！…」`); // :699
        } else { // :700
          await era.printAndWait(`「丢下人家自己一个在自慰…好残忍啊！」`); // :701
        } // :702
        // CFLAG:304  = 7（变量语义：CFLAG 族，304） // :703
        era.set(`cflag:${target}:304`, 7); // :703

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:304`) <= 5 || era.get('flag:7') == 2)) { // :705
        await era.printAndWait(`「要看着魔王大人来自慰？……呜呜，魔王大人啊，早点占有我，早点贯穿我这小穴就好了………」`); // :706
        // CFLAG:304  = 6（变量语义：CFLAG 族，304） // :707
        era.set(`cflag:${target}:304`, 6); // :707

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:304`) <= 4 || era.get('flag:7') == 2)) { // :709

        if (rand_n(3) == 0) { // :711
          await era.printAndWait(`「唔……噢噢……啊！……」`); // :712
        } else if (rand_n(2) == 0) { // :713
          await era.printAndWait(`「唔……噢噢……啊！……哈……哈……」`); // :714
        } else { // :715
          await era.printAndWait(`「唔……噢噢……啊！……呃！！！！」`); // :716
        } // :717
        // CFLAG:304  = 5（变量语义：CFLAG 族，304） // :718
        era.set(`cflag:${target}:304`, 5); // :718

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:31`) < 3 && (era.get(`cflag:${target}:304`) <= 3 || era.get('flag:7') == 2)) { // :720

        if (rand_n(2) == 0) { // :722
          await era.printAndWait(`「呜呜…魔王大人，为什么不愿意抱人家呢……」`); // :723
        } else { // :724
          await era.printAndWait(`「一个人……可怜地在自慰……」`); // :725
        } // :726
        // CFLAG:304  = 4（变量语义：CFLAG 族，304） // :727
        era.set(`cflag:${target}:304`, 4); // :727

      } else if (era.get(`mark:${target}:2`) == 3 &&era.get(`abl:${target}:31`) >= 1 && (era.get(`cflag:${target}:304`) <= 2 || era.get('flag:7') == 2)) { // :729

        if (rand_n(2) == 0) { // :731
          await era.printAndWait(`（讨厌……有感觉……了吗？）`); // :732
        } else { // :733
          await era.printAndWait(`（我怎么…变成这样………）`); // :734
        } // :735
        // CFLAG:304  = 3（变量语义：CFLAG 族，304） // :736
        era.set(`cflag:${target}:304`, 3); // :736

      } else if (era.get(`cflag:${target}:304`) <= 1 || era.get('flag:7') == 2) { // :738

        if (rand_n(2) == 0) { // :740
          await era.printAndWait(`「要做……这样的事……」`); // :741
        } else { // :742
          await era.printAndWait(`「太丢人了……好羞耻……」`); // :743
        } // :744
        // CFLAG:304  = 2（变量语义：CFLAG 族，304） // :745
        era.set(`cflag:${target}:304`, 2); // :745
      } // :746
      return 0; // :747
    } // :748
  } // :749





  if (era_flag.selectcom == 5) { // :755

    if (era.get(`cflag:${target}:306`) == 0) { // :757

      if (era.get(`talent:${target}:85`) == 1) { // :759
        await era.printAndWait(`「想玩弄胸部么……？　嘻嘻，是你的话，可以哦～……」`); // :760

      } else { // :762
        await era.printAndWait(`「呜！摸我的胸！　你这痴汉！！！」`); // :763
      } // :764
      // CFLAG:TARGET:306  = 1（变量语义：CFLAG 族，TARGET:306） // :765
      era.set(`cflag:${target}:306`, 1); // :765
      return 0; // :766

    } else { // :768

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:306`) <= 4 || era.get('flag:7') == 2)) { // :770
        await era.printAndWait(`「哈……感觉胸都要融化了……好啊……♪」`); // :771
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :772
        era.set(`cflag:${target}:306`, 5); // :772

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:306`) <= 3 || era.get('flag:7') == 2)) { // :774
        await era.printAndWait(`「我这胸，随魔王大人玩弄啦～……嘻嘻～………………呃………………呃……………………噢～……………………」`); // :775
        await era.printAndWait(`${target_name}陶醉地闭上双眼，夸张地昂首挺胸，胸部不断起伏配合着你的手，发出了让人血脉偾张的可爱呻吟。`); // :776
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :777
        era.set(`cflag:${target}:306`, 4); // :777

      } else if (era.get(`abl:${target}:1`) >= 3 && (era.get(`cflag:${target}:306`) <= 2 || era.get('flag:7') == 2)) { // :779
        await era.printAndWait(`「啊啊……胸部……有感觉了……？」`); // :780
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :781
        era.set(`cflag:${target}:306`, 3); // :781

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :783
        await era.printAndWait(`「哼！揉胸什么的，不会有感觉的啦！」`); // :784
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :785
        era.set(`cflag:${target}:306`, 2); // :785
      } // :786
      return 0; // :787
    } // :788
  } // :789




  if (era_flag.selectcom == 6) { // :794

    if (era.get(`cflag:${target}:307`) == 0 && era.get('tflag:13')) { // :796

      if (era.get(`talent:${target}:76`) == 1 && era_flag.assiplay == 0 && era.get(`tequip:${target}:89`) == 0 && era.get(`tequip:${target}:90`) == 0) { // :798
        await era.printAndWait(`「嘻嘻，我的初吻是魔王大人耶……♪　太好了……亲亲～！」`); // :799

      } else if (era.get(`talent:${target}:85`) == 1 && era_flag.assiplay == 0 && era.get(`tequip:${target}:89`) == 0 && era.get(`tequip:${target}:90`) == 0) { // :801
        await era.printAndWait(`「魔王大人啊……啊……初吻给的是你，我真是太幸福了……♪」`); // :802

      } else { // :804
        await era.printAndWait(`「呜呜……明明是初吻啊……被这样的……」`); // :805
      } // :806
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :807
      era.set(`cflag:${target}:307`, 1); // :807
      return 0; // :808

    } else if (era.get(`cflag:${target}:307`) == 0) { // :810

      if (era.get(`talent:${target}:76`) == 1) { // :812
        await era.printAndWait(`「啊……要接吻吗？好啊～感觉上很浪漫呢～嘻嘻……♪」`); // :813

      } else if (era.get(`talent:${target}:85`) == 1) { // :815
        await era.printAndWait(`「魔王大人……啊啊……最喜欢您的吻了！……♪」`); // :816

      } else { // :818
        await era.printAndWait(`「舔了我的嘴唇……也不会改变任何事！」`); // :819
      } // :820
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :821
      era.set(`cflag:${target}:307`, 1); // :821
      return 0; // :822

    } else { // :824

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:307`) <= 4 || era.get('flag:7') == 2)) { // :826
        await era.printAndWait(`「唔……唔唔……喔～……嘻嘻～」`); // :827
        // CFLAG:307  = 5（变量语义：CFLAG 族，307） // :828
        era.set(`cflag:${target}:307`, 5); // :828

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:307`) <= 3 || era.get('flag:7') == 2)) { // :830
        await era.printAndWait(`「啊～魔王大人的吻～……唔……唔唔～……喔～」`); // :831
        // CFLAG:307  = 4（变量语义：CFLAG 族，307） // :832
        era.set(`cflag:${target}:307`, 4); // :832

      } else if (era.get(`abl:${target}:10`) >=2 && (era.get(`cflag:${target}:307`) <= 2 || era.get('flag:7') == 2)) { // :834
        await era.printAndWait(`「只，只是嘴唇的话，就可以……」`); // :835
        // CFLAG:307  = 3（变量语义：CFLAG 族，307） // :836
        era.set(`cflag:${target}:307`, 3); // :836

      } else if (era.get(`cflag:${target}:307`) <= 1 || era.get('flag:7') == 2) { // :838
        await era.printAndWait(`「哼！一张臭嘴！！」`); // :839
        // CFLAG:307  = 2（变量语义：CFLAG 族，307） // :840
        era.set(`cflag:${target}:307`, 2); // :840
      } // :841
      return 0; // :842
    } // :843
  } // :844




  if (era_flag.selectcom == 7) { // :849

    if (era.get(`cflag:${target}:308`) == 0) { // :851

      if (era.get(`talent:${target}:76`) == 1) { // :853
        await era.printAndWait(`「被看见了……啊……继续视奸我啊！！」`); // :854

      } else if (era.get(`talent:${target}:85`) == 1) { // :856
        await era.printAndWait(`「只，只想给你……一个人看……」`); // :857

      } else { // :859
        await era.printAndWait(`「这……这么羞耻的事……呜……」`); // :860
      } // :861
      // CFLAG:TARGET:308  = 1（变量语义：CFLAG 族，TARGET:308） // :862
      era.set(`cflag:${target}:308`, 1); // :862
      return 0; // :863

    } else { // :865

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:308`) <= 4 || era.get('flag:7') == 2)) { // :867
        await era.printAndWait(`「啊～哦～……再视奸我吧……看我那淫荡的肉穴里面……啊啊～……」`); // :868
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :869
        era.set(`cflag:${target}:306`, 5); // :869

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:308`) <= 3 || era.get('flag:7') == 2)) { // :871
        await era.printAndWait(`「这，这……是只为你敞开的地方……嘻嘻……」`); // :872
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :873
        era.set(`cflag:${target}:306`, 4); // :873

      } else if (era.get(`abl:${target}:17`) >= 3 && (era.get(`cflag:${target}:308`) <= 2 || era.get('flag:7') == 2)) { // :875
        await era.printAndWait(`「啊……被看见了……全部都……可恶～……」`); // :876
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :877
        era.set(`cflag:${target}:306`, 3); // :877

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :879
        await era.printAndWait(`「喜欢这种样子……你这人啊……」`); // :880
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :881
        era.set(`cflag:${target}:306`, 2); // :881
      } // :882
      return 0; // :883
    } // :884
  } // :885




  if (era_flag.selectcom == 8) { // :890

    if (era.get(`cflag:${target}:309`) == 0) { // :892

      if (era.get(`talent:${target}:76`) == 1) { // :894
        await era.printAndWait(`「啊啊，抠挖着，那手指！喔～伸进来了！！」`); // :895

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 1) { // :897
        await era.printAndWait(`「啊……魔王大人的手指……噢哦～」`); // :898

      } else { // :900
        await era.printAndWait(`「手指进来了～……唔唔」`); // :901
      } // :902
      // CFLAG:TARGET:309  = 1（变量语义：CFLAG 族，TARGET:309） // :903
      era.set(`cflag:${target}:309`, 1); // :903
      return 0; // :904

    } else { // :906

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:309`) <= 4 || era.get('flag:7') == 2)) { // :908
        await era.printAndWait(`「啊啊……再继续搅动啊～♪　求你！」`); // :909
        // CFLAG:309  = 5（变量语义：CFLAG 族，309） // :910
        era.set(`cflag:${target}:309`, 5); // :910

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:309`) <= 3 || era.get('flag:7') == 2)) { // :912
        await era.printAndWait(`「好厉害～……魔王大人的手指……哦哦哦！♪」`); // :913
        await era.printAndWait(`${target_name}浑身发烫，双腿直抖，软倒在你的怀里。`); // :914
        // CFLAG:309  = 4（变量语义：CFLAG 族，309） // :915
        era.set(`cflag:${target}:309`, 4); // :915

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:309`) <= 2 || era.get('flag:7') == 2)) { // :917
        await era.printAndWait(`「啊啊……手指……这么舒服……」`); // :918
        // CFLAG:309  = 3（变量语义：CFLAG 族，309） // :919
        era.set(`cflag:${target}:309`, 3); // :919

      } else if (era.get(`cflag:${target}:309`) <= 1 || era.get('flag:7') == 2) { // :921
        await era.printAndWait(`「住手！这……讨厌的手指！」`); // :922
        // CFLAG:309  = 2（变量语义：CFLAG 族，309） // :923
        era.set(`cflag:${target}:309`, 2); // :923
      } // :924
      return 0; // :925
    } // :926
  } // :927




  if (era_flag.selectcom == 9) { // :932

    if (era.get(`cflag:${target}:310`) == 0) { // :934

      if (era.get(`talent:${target}:76`) == 1) { // :936
        await era.printAndWait(`「哦啊～黏糊糊的……好棒♪」`); // :937

      } else if (era.get(`talent:${target}:85`) == 1) { // :939
        await era.printAndWait(`「不，不要嘛～舔那种地方……！」`); // :940

      } else { // :942
        await era.printAndWait(`「在干什么？不！！不要啊！！」`); // :943
      } // :944
      // CFLAG:TARGET:310  = 1（变量语义：CFLAG 族，TARGET:310） // :945
      era.set(`cflag:${target}:310`, 1); // :945
      return 0; // :946

    } else { // :948

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:310`) <= 4 || era.get('flag:7') == 2)) { // :950
        await era.printAndWait(`「唔哦！再拿舌头伸进去吧～♪」`); // :951
        // CFLAG:310  = 5（变量语义：CFLAG 族，310） // :952
        era.set(`cflag:${target}:310`, 5); // :952

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:310`) <= 3 || era.get('flag:7') == 2)) { // :954
        await era.printAndWait(`「被舔那里的话……受不了的……♪」`); // :955
        // CFLAG:310  = 4（变量语义：CFLAG 族，310） // :956
        era.set(`cflag:${target}:310`, 4); // :956

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:310`) <= 2 || era.get('flag:7') == 2)) { // :958
        await era.printAndWait(`「喜，喜欢的话……就舔呗……」`); // :959
        // CFLAG:310  = 3（变量语义：CFLAG 族，310） // :960
        era.set(`cflag:${target}:310`, 3); // :960

      } else if (era.get(`cflag:${target}:310`) <= 1 || era.get('flag:7') == 2) { // :962
        await era.printAndWait(`「不要舔……奇怪的地方啦！」`); // :963
        // CFLAG:310  = 2（变量语义：CFLAG 族，310） // :964
        era.set(`cflag:${target}:310`, 2); // :964
      } // :965
      return 0; // :966
    } // :967
  } // :968





  if (era_flag.selectcom == 10) { // :974

    if (era.get(`cflag:${target}:311`) == 0) { // :976

      if (era.get(`talent:${target}:76`) == 1) { // :978
        await era.printAndWait(`「噢哦喔，这是什么啊……唔！啊啊啊！…」`); // :979

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 1) { // :981
        await era.printAndWait(`「快麻痹啦……噢啊♪」`); // :982

      } else { // :984
        await era.printAndWait(`「这石头怎么回事！……别这样！！」`); // :985
      } // :986
      // CFLAG:TARGET:311  = 1（变量语义：CFLAG 族，TARGET:311） // :987
      era.set(`cflag:${target}:311`, 1); // :987
      return 0; // :988

    } else { // :990

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:311`) <= 4 || era.get('flag:7') == 2)) { // :992
        await era.printAndWait(`「啊啊喔～再用力按压……唔唔啊啊♪」`); // :993
        // CFLAG:311  = 5（变量语义：CFLAG 族，311） // :994
        era.set(`cflag:${target}:311`, 5); // :994

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:311`) <= 3 || era.get('flag:7') == 2)) { // :996
        await era.printAndWait(`「被道具玩弄了……啊啊～♪」`); // :997
        // CFLAG:311  = 4（变量语义：CFLAG 族，311） // :998
        era.set(`cflag:${target}:311`, 4); // :998

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:311`) <= 2 || era.get('flag:7') == 2)) { // :1000
        await era.printAndWait(`「这样的小石子……呜呜……有感觉了……」`); // :1001
        // CFLAG:311  = 3（变量语义：CFLAG 族，311） // :1002
        era.set(`cflag:${target}:311`, 3); // :1002

      } else if (era.get(`cflag:${target}:311`) <= 1 || era.get('flag:7') == 2) { // :1004
        await era.printAndWait(`「快住手！　不要把奇怪的东西……强加于我！！」`); // :1005
        // CFLAG:311  = 2（变量语义：CFLAG 族，311） // :1006
        era.set(`cflag:${target}:311`, 2); // :1006
      } // :1007
      return 0; // :1008
    } // :1009
  } // :1010





  if (era_flag.selectcom == 11 && era.get(`tequip:${target}:11`)) { // :1016

    if (era.get(`cflag:${target}:312`) == 0) { // :1018

      if (era.get(`talent:${target}:0`) == 1) { // :1020

        if (era.get(`talent:${target}:76`) == 1) { // :1022
          await era.printAndWait(`「啊啊……被蠕虫夺取了第一次！　好吧～……」`); // :1023

        } else if (era.get(`talent:${target}:85`) == 1) { // :1025
          await era.printAndWait(`「啊啊……如果是魔王大人的话……那该多好啊！」`); // :1026

        } else { // :1028
          await era.printAndWait(`「呃！！那个奇怪的恶心生物是什么……　吓？它要夺取我的第一次……这不是真的…………」`); // :1029
        } // :1030

      } else { // :1032

        if (era.get(`talent:${target}:76`) == 1) { // :1034
          await era.printAndWait(`「啊啊……多么下流的生物啊……嘻嘻，来吧……」`); // :1035

        } else if (era.get(`talent:${target}:85`) == 1) { // :1037
          await era.printAndWait(`「令人讨厌的东西……真的要放进去吗……」`); // :1038

        } else { // :1040
          await era.printAndWait(`「呃！！这个恶心的生物……不要啊！！不要拿过来！！！！」`); // :1041
        } // :1042
      } // :1043
      // CFLAG:312  = 1（变量语义：CFLAG 族，312） // :1044
      era.set(`cflag:${target}:312`, 1); // :1044
      return 0; // :1045

    } else { // :1047

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:312`) <= 4 || era.get('flag:7') == 2)) { // :1049
        await era.printAndWait(`「啊啊……拿过来……把它弄湿然后放到我里面来……♪」`); // :1050
        // CFLAG:312  = 5（变量语义：CFLAG 族，312） // :1051
        era.set(`cflag:${target}:312`, 5); // :1051

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:312`) <= 3 || era.get('flag:7') == 2)) { // :1053
        await era.printAndWait(`「要来了吗……我这边已经准备好了……♪」`); // :1054
        // CFLAG:312  = 4（变量语义：CFLAG 族，312） // :1055
        era.set(`cflag:${target}:312`, 4); // :1055

      } else if (era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:312`) <= 2 || era.get('flag:7') == 2)) { // :1057
        await era.printAndWait(`「呃……来了……居然对这么恶心的东西……弄出感觉……」`); // :1058
        // CFLAG:312  = 3（变量语义：CFLAG 族，312） // :1059
        era.set(`cflag:${target}:312`, 3); // :1059

      } else if (era.get(`cflag:${target}:312`) <= 1 || era.get('flag:7') == 2) { // :1061
        await era.printAndWait(`「不要，不要啊！！……这么恶心的生物……别！！！……」`); // :1062
        // CFLAG:312  = 2（变量语义：CFLAG 族，312） // :1063
        era.set(`cflag:${target}:312`, 2); // :1063
      } // :1064
      return 0; // :1065
    } // :1066

  } else if (era_flag.selectcom == 11 && era.get(`tequip:${target}:11`) == 0) { // :1068

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:372`) < 3 || era.get('flag:7') == 2)) { // :1070
      await era.printAndWait(`「啊啊……被拿出来了啊……」`); // :1071
      // CFLAG:372  = 3（变量语义：CFLAG 族，372） // :1072
      era.set(`cflag:${target}:372`, 3); // :1072

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:372`) < 2 || era.get('flag:7') == 2)) { // :1074
      await era.printAndWait(`「蠕虫大人……辛苦了～」`); // :1075
      // CFLAG:372  = 2（变量语义：CFLAG 族，372） // :1076
      era.set(`cflag:${target}:372`, 2); // :1076

    } else if (era.get(`cflag:${target}:372`) < 1 || era.get('flag:7') == 2) { // :1078
      await era.printAndWait(`「呼呼……终于……结束了」`); // :1079
      // CFLAG:372  = 1（变量语义：CFLAG 族，372） // :1080
      era.set(`cflag:${target}:372`, 1); // :1080
    } // :1081
    return 0; // :1082
  } // :1083




  if (era_flag.selectcom == 12) { // :1088

    if (era.get(`cflag:${target}:313`) == 0) { // :1090

      if (era.get(`talent:${target}:76`) == 1) { // :1092
        await era.printAndWait(`「嘻嘻……什么嘛这个杖？　是个好东西么？」`); // :1093

      } else if (era.get(`talent:${target}:85`) == 1) { // :1095
        await era.printAndWait(`「咦？保健器具么？……肩膀是有些酸痛了。」`); // :1096

      } else { // :1098
        await era.printAndWait(`「什，什么啊……这么大一根！　不要！不要过来！！！」`); // :1099
      } // :1100
      // CFLAG:313  = 1（变量语义：CFLAG 族，313） // :1101
      era.set(`cflag:${target}:313`, 1); // :1101
      return 0; // :1102

    } else { // :1104

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:313`) <= 4 || era.get('flag:7') == 2)) { // :1106
        await era.printAndWait(`「唔……哦！这个令人发麻的快感……呜……不行啦……啊啊！～♪」`); // :1107
        // CFLAG:313  = 5（变量语义：CFLAG 族，313） // :1108
        era.set(`cflag:${target}:313`, 5); // :1108

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:313`) <= 3 || era.get('flag:7') == 2)) { // :1110
        await era.printAndWait(`「啊啊啊……有，有，有感觉了……被这根杖……♪」`); // :1111
        // CFLAG:313  = 4（变量语义：CFLAG 族，313） // :1112
        era.set(`cflag:${target}:313`, 4); // :1112

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:313`) <= 2 || era.get('flag:7') == 2)) { // :1114
        await era.printAndWait(`「呜呜呜……这，这个……受不了啦……真是的…………」`); // :1115
        // CFLAG:313  = 3（变量语义：CFLAG 族，313） // :1116
        era.set(`cflag:${target}:313`, 3); // :1116

      } else if (era.get(`cflag:${target}:313`) <= 1 || era.get('flag:7') == 2) { // :1118
        await era.printAndWait(`「呃……呜……住手！……不要再弄啦！……啊！这感觉～！」`); // :1119
        // CFLAG:313  = 2（变量语义：CFLAG 族，313） // :1120
        era.set(`cflag:${target}:313`, 2); // :1120
      } // :1121
      return 0; // :1122
    } // :1123
  } // :1124





  if (era_flag.selectcom == 13 && era.get(`tequip:${target}:13`)) { // :1130

    if (era.get(`cflag:${target}:314`) == 0) { // :1132

      if (era.get(`talent:${target}:76`) == 1) { // :1134
        await era.printAndWait(`「啊啊……好样的生物啊！快让我尝尝它的滋味……♪」`); // :1135

      } else if (era.get(`talent:${target}:85`) == 1) { // :1137
        await era.printAndWait(`「嘻嘻……这丑陋的东西～♪」`); // :1138

      } else { // :1140
        await era.printAndWait(`「什，什么啊这玩意儿……住手！好恶心！！」`); // :1141
      } // :1142
      // CFLAG:TARGET:314  = 1（变量语义：CFLAG 族，TARGET:314） // :1143
      era.set(`cflag:${target}:314`, 1); // :1143
      return 0; // :1144

    } else { // :1146

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 6 || era.get('flag:7') == 2)) { // :1148
        await era.printAndWait(`「唔哦……在里面……不停搅动着……好厉害，太厉害啦～哦哦！♪」`); // :1149
        await era.printAndWait(`${target_name}因为肛门虫的活动，媚态尽显地高声呻吟着。`); // :1150
        // CFLAG:314  = 6（变量语义：CFLAG 族，314） // :1151
        era.set(`cflag:${target}:314`, 6); // :1151

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:314`) <= 5 || era.get('flag:7') == 2)) { // :1153
        await era.printAndWait(`「嘻嘻，好啊～再深入我的洞里……♪　再让我更兴奋吧♪」`); // :1154
        // CFLAG:314  = 6（变量语义：CFLAG 族，314） // :1155
        era.set(`cflag:${target}:314`, 6); // :1155

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 4 || era.get('flag:7') == 2)) { // :1157
        await era.printAndWait(`「啊，屁股里……好棒，多么出色的生物啊～……喔喔喔～！♪」`); // :1158
        await era.printAndWait(`${target_name}被肛门虫蹂躏着尻穴，心荡神驰了。`); // :1159
        // CFLAG:314  = 5（变量语义：CFLAG 族，314） // :1160
        era.set(`cflag:${target}:314`, 5); // :1160

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:314`) <= 3 || era.get('flag:7') == 2)) { // :1162
        await era.printAndWait(`「嘻嘻，屁股吗……把这东西放进去吧……♪」`); // :1163
        // CFLAG:314  = 4（变量语义：CFLAG 族，314） // :1164
        era.set(`cflag:${target}:314`, 4); // :1164

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 2 || era.get('flag:7') == 2)) { // :1166
        await era.printAndWait(`「呃呃……不行！感觉到了……被这种卑劣的生物……」`); // :1167
        // CFLAG:314  = 3（变量语义：CFLAG 族，314） // :1168
        era.set(`cflag:${target}:314`, 3); // :1168

      } else if (era.get(`cflag:${target}:314`) <= 1 || era.get('flag:7') == 2) { // :1170
        await era.printAndWait(`「不！！　这种卑劣的东西……不要拿过来！！！」`); // :1171
        // CFLAG:314  = 2（变量语义：CFLAG 族，314） // :1172
        era.set(`cflag:${target}:314`, 2); // :1172
      } // :1173
      return 0; // :1174
    } // :1175

  } else if (era_flag.selectcom == 13 && era.get(`tequip:${target}:13`) == 0) { // :1177

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:374`) < 4 || era.get('flag:7') == 2)) { // :1179
      await era.printAndWait(`「哈哈哈……好厉害……」`); // :1180
      // CFLAG:374  = 4（变量语义：CFLAG 族，374） // :1181
      era.set(`cflag:${target}:374`, 4); // :1181

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:374`) < 3 || era.get('flag:7') == 2)) { // :1183
      await era.printAndWait(`「嘻嘻，可爱的东西哦，从屁股里出来啦～♪」`); // :1184
      // CFLAG:374  = 3（变量语义：CFLAG 族，374） // :1185
      era.set(`cflag:${target}:374`, 3); // :1185

    } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:374`) < 2 || era.get('flag:7') == 2)) { // :1187
      await era.printAndWait(`「呼……呼……呃……请温柔一点拔出来……」`); // :1188
      // CFLAG:374  = 2（变量语义：CFLAG 族，374） // :1189
      era.set(`cflag:${target}:374`, 2); // :1189

    } else if (era.get(`cflag:${target}:374`) < 1 || era.get('flag:7') == 2) { // :1191
      await era.printAndWait(`「哈，哈……终于结束了……」`); // :1192
      // CFLAG:374  = 1（变量语义：CFLAG 族，374） // :1193
      era.set(`cflag:${target}:374`, 1); // :1193
    } // :1194
    return 0; // :1195
  } // :1196





  if (era_flag.selectcom == 14 && era.get(`tequip:${target}:14`)) { // :1202

    if (era.get(`cflag:${target}:315`) == 0) { // :1204

      if (era.get(`talent:${target}:76`) == 1) { // :1206
        await era.printAndWait(`「嘻嘻～还有这种东西啊～魔王大人～真绅士～！～♪」`); // :1207

      } else if (era.get(`talent:${target}:85`) == 1) { // :1209
        await era.printAndWait(`「它跳动的样子！就像是魔王大人满满的爱意～♪」`); // :1210

      } else { // :1212
        await era.printAndWait(`「被夹上会很痛的吧？！别！！！放开我！！！」`); // :1213
      } // :1214
      // CFLAG:315  = 1（变量语义：CFLAG 族，315） // :1215
      era.set(`cflag:${target}:315`, 1); // :1215
      return 0; // :1216

    } else { // :1218

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:315`) <= 3 || era.get('flag:7') == 2)) { // :1220
        await era.printAndWait(`「啊～久违的夹子～快！狠狠地夹着我的那里！让我发疯吧！」`); // :1221
        // CFLAG:315  = 4（变量语义：CFLAG 族，315） // :1222
        era.set(`cflag:${target}:315`, 4); // :1222

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:315`) <= 2 || era.get('flag:7') == 2)) { // :1224
        await era.printAndWait(`「看，我的小豆豆，已经为魔王大人而勃起了。只要能令你高兴，要人家……要人家怎么样都可以…………」`); // :1225
        // CFLAG:315  = 3（变量语义：CFLAG 族，315） // :1226
        era.set(`cflag:${target}:315`, 3); // :1226

      } else if (era.get(`cflag:${target}:315`) <= 1 || era.get('flag:7') == 2) { // :1228
        await era.printAndWait(`「这夹子………唔唔………」`); // :1229
        // CFLAG:315  = 2（变量语义：CFLAG 族，315） // :1230
        era.set(`cflag:${target}:315`, 2); // :1230
      } // :1231
      return 0; // :1232
    } // :1233

  } else if (era_flag.selectcom == 14 && era.get(`tequip:${target}:14`) == 0) { // :1235

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:375`) < 3 || era.get('flag:7') == 2)) { // :1237
      await era.printAndWait(`「呃～啊！！我的阴蒂，已经红肿得发疼了！继续！继续蹂躏我啊！」`); // :1238
      // CFLAG:375  = 3（变量语义：CFLAG 族，375） // :1239
      era.set(`cflag:${target}:375`, 3); // :1239

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:375`) < 2 || era.get('flag:7') == 2)) { // :1241
      await era.printAndWait(`「呃～噢！！魔……魔王大人……尽兴了吗？」`); // :1242
      // CFLAG:375  = 2（变量语义：CFLAG 族，375） // :1243
      era.set(`cflag:${target}:375`, 2); // :1243

    } else if (era.get(`cflag:${target}:375`) < 1 || era.get('flag:7') == 2) { // :1245
      await era.printAndWait(`「哎呀………唔唔………」`); // :1246
      // CFLAG:375  = 1（变量语义：CFLAG 族，375） // :1247
      era.set(`cflag:${target}:375`, 1); // :1247
    } // :1248
    return 0; // :1249
  } // :1250





  if (era_flag.selectcom == 15 && era.get(`tequip:${target}:15`)) { // :1256

    if (era.get(`cflag:${target}:316`) == 0) { // :1258

      if (era.get(`talent:${target}:76`) == 1) { // :1260
        await era.printAndWait(`「嘻嘻～还有这种东西啊～魔王大人～大～♪变～♪态～♪」`); // :1261

      } else if (era.get(`talent:${target}:85`) == 1) { // :1263
        await era.printAndWait(`「人家的身体，就是为了侍奉魔王大人而存在的～请尽情玩弄人家的胸部吧～♪」`); // :1264

      } else { // :1266
        await era.printAndWait(`「连乳头也？！感觉会很疼！！！饶了我！！！」`); // :1267
      } // :1268
      // CFLAG:316  = 1（变量语义：CFLAG 族，316） // :1269
      era.set(`cflag:${target}:316`, 1); // :1269
      return 0; // :1270

    } else { // :1272

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:316`) <= 3 || era.get('flag:7') == 2)) { // :1274
        await era.printAndWait(`「乳头完全勃起了～快！狠狠地夹着我的乳头！把我淫乱的乳头玩坏吧！」`); // :1275
        // CFLAG:316  = 4（变量语义：CFLAG 族，316） // :1276
        era.set(`cflag:${target}:316`, 4); // :1276

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:316`) <= 2 || era.get('flag:7') == 2)) { // :1278
        await era.printAndWait(`「乳……乳头勃起什么的……那是因为见到了魔王大人而情不自禁…………！我是魔王大人最忠实的奴隶，请尽情地玩弄我吧！」`); // :1279
        // CFLAG:316  = 3（变量语义：CFLAG 族，316） // :1280
        era.set(`cflag:${target}:316`, 3); // :1280

      } else if (era.get(`cflag:${target}:316`) <= 1 || era.get('flag:7') == 2) { // :1282
        await era.printAndWait(`「乳……乳头夹什么的…………啊……！」`); // :1283
        // CFLAG:316  = 2（变量语义：CFLAG 族，316） // :1284
        era.set(`cflag:${target}:316`, 2); // :1284
      } // :1285
      return 0; // :1286
    } // :1287

  } else if (era_flag.selectcom == 15 && era.get(`tequip:${target}:15`) == 0) { // :1289

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:376`) < 3 || era.get('flag:7') == 2)) { // :1291
      await era.printAndWait(`「嘻嘻！人家的乳头，被你弄得发麻了，好舒服～！」`); // :1292
      // CFLAG:376  = 3（变量语义：CFLAG 族，376） // :1293
      era.set(`cflag:${target}:376`, 3); // :1293

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:376`) < 2 || era.get('flag:7') == 2)) { // :1295
      await era.printAndWait(`「啊啊！乳头……乳头……已经切实地记住魔王大人的爱意了！」`); // :1296
      // CFLAG:376  = 2（变量语义：CFLAG 族，376） // :1297
      era.set(`cflag:${target}:376`, 2); // :1297

    } else if (era.get(`cflag:${target}:376`) < 1 || era.get('flag:7') == 2) { // :1299
      await era.printAndWait(`「乳……乳头……好像不再属于自己了一样…………唔哦！」`); // :1300
      // CFLAG:376  = 1（变量语义：CFLAG 族，376） // :1301
      era.set(`cflag:${target}:376`, 1); // :1301
    } // :1302
    return 0; // :1303
  } // :1304





  if (era_flag.selectcom == 16 && era.get(`tequip:${target}:16`)) { // :1310

    if (era.get(`cflag:${target}:317`) == 0) { // :1312

      if (era.get(`talent:${target}:76`) == 1) { // :1314
        await era.printAndWait(`「啊！啊！啊！～～～这么用力吸的话……会…………会………………」`); // :1315

      } else if (era.get(`talent:${target}:85`) == 1) { // :1317
        await era.printAndWait(`「哦～哦～麻麻的……好像被婴儿吸啜着一样………好想……好想为魔王大人生个小孩啊……………」`); // :1318

      } else { // :1320
        await era.printAndWait(`「母乳什么的……不可强求啦……拿下来！……拿下来啊！！」`); // :1321
      } // :1322
      // CFLAG:317  = 1（变量语义：CFLAG 族，317） // :1323
      era.set(`cflag:${target}:317`, 1); // :1323
      return 0; // :1324

    } else { // :1326

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:317`) <= 3 || era.get('flag:7') == 2)) { // :1328
        await era.printAndWait(`「啊！啊！啊！～～～太～太舒服了！！再吸！再用力吸……………」`); // :1329
        // CFLAG:317  = 4（变量语义：CFLAG 族，317） // :1330
        era.set(`cflag:${target}:317`, 4); // :1330

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:317`) <= 2 || era.get('flag:7') == 2)) { // :1332
        await era.printAndWait(`「唔～哦哦！～～～感觉胸中满满的……爱意……和奶水一起……被吸出来了！……魔王大人啊！魔王大人哦！………」`); // :1333
        // CFLAG:317  = 3（变量语义：CFLAG 族，317） // :1334
        era.set(`cflag:${target}:317`, 3); // :1334

      } else if (era.get(`cflag:${target}:317`) <= 1 || era.get('flag:7') == 2) { // :1336
        await era.printAndWait(`「呃……啊！！啊………明明是喂小孩的说…………」`); // :1337
        // CFLAG:317  = 2（变量语义：CFLAG 族，317） // :1338
        era.set(`cflag:${target}:317`, 2); // :1338
      } // :1339
      return 0; // :1340
    } // :1341

  } else if (era_flag.selectcom == 16 && era.get(`tequip:${target}:16`) == 0) { // :1343

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:377`) < 3 || era.get('flag:7') == 2)) { // :1345
      await era.printAndWait(`「嘻嘻～我的那些奶水，我自己能尝尝么～……………」`); // :1346
      // CFLAG:377  = 3（变量语义：CFLAG 族，377） // :1347
      era.set(`cflag:${target}:377`, 3); // :1347

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:377`) < 2 || era.get('flag:7') == 2)) { // :1349
      await era.printAndWait(`「那……那些……母乳……只是为了魔王大人……只能给魔王大人……………」`); // :1350
      // CFLAG:377  = 2（变量语义：CFLAG 族，377） // :1351
      era.set(`cflag:${target}:377`, 2); // :1351

    } else if (era.get(`cflag:${target}:377`) < 1 || era.get('flag:7') == 2) { // :1353
      await era.printAndWait(`「呼……呼呼…………吸得太用力了……………」`); // :1354
      // CFLAG:377  = 1（变量语义：CFLAG 族，377） // :1355
      era.set(`cflag:${target}:377`, 1); // :1355
    } // :1356
    return 0; // :1357
  } // :1358



























































  if (era_flag.selectcom == 19 && era.get(`tequip:${target}:19`)) { // :1418

    if (era.get(`cflag:${target}:320`) == 0) { // :1420

      if (era.get(`talent:${target}:76`) == 1) { // :1422
        await era.printAndWait(`「嘻嘻，又变出了什么邪恶的道具了～……♪」`); // :1423

      } else if (era.get(`talent:${target}:85`) == 1) { // :1425
        await era.printAndWait(`「啊？这个，要放到屁股里么……？　只……只要能令你高兴的话……♪」`); // :1426

      } else { // :1428
        await era.printAndWait(`「这是……什么？　啥？　屁股里！？　怎能这样……」`); // :1429
      } // :1430
      // CFLAG:TARGET:320  = 1（变量语义：CFLAG 族，TARGET:320） // :1431
      era.set(`cflag:${target}:320`, 1); // :1431
      return 0; // :1432

    } else { // :1434

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 6 || era.get('flag:7') == 2)) { // :1436
        await era.printAndWait(`「噢～便便的地方，被塞满了……呵呵～全部放进去了没？」`); // :1437
        // CFLAG:320  = 7（变量语义：CFLAG 族，320） // :1438
        era.set(`cflag:${target}:320`, 7); // :1438

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:320`) <= 5 || era.get('flag:7') == 2)) { // :1440
        await era.printAndWait(`「呃～～好痛苦……魔王大人！人家快受不了啦……♪」`); // :1441
        // CFLAG:320  = 6（变量语义：CFLAG 族，320） // :1442
        era.set(`cflag:${target}:320`, 6); // :1442

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 4 || era.get('flag:7') == 2)) { // :1444
        await era.printAndWait(`「好热，屁股好热……继续，继续放进去吧……♪」`); // :1445
        // CFLAG:320  = 5（变量语义：CFLAG 族，320） // :1446
        era.set(`cflag:${target}:320`, 5); // :1446

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:320`) <= 3 || era.get('flag:7') == 2)) { // :1448
        await era.printAndWait(`「呃！！屁股里……痛…痛…痛……啊！不用管我！请继续吧！……」`); // :1449
        // CFLAG:320  = 4（变量语义：CFLAG 族，320） // :1450
        era.set(`cflag:${target}:320`, 4); // :1450

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 2 || era.get('flag:7') == 2)) { // :1452
        await era.printAndWait(`「可恶，有感觉了……被这种玩具……弄菊花…………」`); // :1453
        // CFLAG:320  = 3（变量语义：CFLAG 族，320） // :1454
        era.set(`cflag:${target}:320`, 3); // :1454

      } else if (era.get(`cflag:${target}:320`) <= 1 || era.get('flag:7') == 2) { // :1456
        await era.printAndWait(`「痛！啊！！好痛啊！！停手！！停手！！不要再放进去啦！」`); // :1457
        // CFLAG:320  = 2（变量语义：CFLAG 族，320） // :1458
        era.set(`cflag:${target}:320`, 2); // :1458
      } // :1459
      return 0; // :1460
    } // :1461

  } else if (era_flag.selectcom == 19 && era.get(`tequip:${target}:19`) == 0) { // :1463

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:379`) < 4 || era.get('flag:7') == 2)) { // :1465
      await era.printAndWait(`「一下子，一下子拔出来吧！♪」`); // :1466
      // CFLAG:379  = 4（变量语义：CFLAG 族，379） // :1467
      era.set(`cflag:${target}:379`, 4); // :1467

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:379`) < 3 || era.get('flag:7') == 2)) { // :1469
      await era.printAndWait(`「请，请温柔点，慢慢拔……让我充分，感受到魔王大人……」`); // :1470
      // CFLAG:379  = 3（变量语义：CFLAG 族，379） // :1471
      era.set(`cflag:${target}:379`, 3); // :1471

    } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:379`) < 2 || era.get('flag:7') == 2)) { // :1473
      await era.printAndWait(`「呃……唔……哦哦～啊！！……」`); // :1474
      // CFLAG:379  = 2（变量语义：CFLAG 族，379） // :1475
      era.set(`cflag:${target}:379`, 2); // :1475

    } else if (era.get(`cflag:${target}:379`) < 1 || era.get('flag:7') == 2) { // :1477
      await era.printAndWait(`「好痛啊！！慢慢地，慢慢地拔啊！…………呜～……」`); // :1478
      // CFLAG:379  = 1（变量语义：CFLAG 族，379） // :1479
      era.set(`cflag:${target}:379`, 1); // :1479
    } // :1480
    return 0; // :1481
  } // :1482




  if (era_flag.selectcom == 20) { // :1487

    if (era.get(`cflag:${target}:321`) == 0) { // :1489

      if (era.get(`talent:${target}:0`) == 1) { // :1491

        if (era.get(`talent:${target}:76`) == 1) { // :1493
          await era.printAndWait(`「快啊～快贯穿我这没用过的私处！……♪」`); // :1494

        } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:10`) >= 5) { // :1496
          await era.printAndWait(`「好……好棒……终于…………♪」`); // :1497

        } else { // :1499
          await era.printAndWait(`「呜……第一次……是这样……」`); // :1500
        } // :1501

      } else { // :1503
        if (era.get(`talent:${target}:76`) == 1) { // :1504
          await era.printAndWait(`「这姿势么～好啊！来吧～……♪」`); // :1505

        } else if (era.get(`talent:${target}:85`) == 1) { // :1507
          await era.printAndWait(`「啊～魔王大人！抱着我……♪」`); // :1508

        } else { // :1510
          await era.printAndWait(`「哼～最多动下腰……」`); // :1511
        } // :1512
      } // :1513
      // CFLAG:321  = 1（变量语义：CFLAG 族，321） // :1514
      era.set(`cflag:${target}:321`, 1); // :1514
      return 0; // :1515

    } else { // :1517

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:321`) <= 5 || era.get('flag:7') == 2)) { // :1519
        await era.printAndWait(`「啊！哦！再来！再插我！……好棒～♪」`); // :1520
        // CFLAG:321  = 6（变量语义：CFLAG 族，321） // :1521
        era.set(`cflag:${target}:321`, 6); // :1521

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:321`) <= 4 || era.get('flag:7') == 2)) { // :1523
        if (rand_n(3) == 0) { // :1524
          await era.printAndWait(`「插进来！占有我！！……让我完全成为魔王大人的东西……让我只能想着魔王大人！！啊啊！～唔～啊！！～♪」`); // :1525
        } else if (rand_n(2) == 0) { // :1526
          await era.printAndWait(`「唔～哦！！从一放进来开始……身体就不受控制了！！啊～我的身体，已经不属于我了……全部都是属于魔王大人的！！」`); // :1527
        } else { // :1528
          await era.printAndWait(`「魔王大人～！操我！操我！再用力地操我！！已经无法思考了！！把我操得乱七八糟吧！！」`); // :1529
        } // :1530
        await era.printAndWait(`「啊啊、好棒啊……再用力点啊……♪」`); // :1531
        // CFLAG:321  = 5（变量语义：CFLAG 族，321） // :1532
        era.set(`cflag:${target}:321`, 5); // :1532

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:321`) <= 3 || era.get('flag:7') == 2)) { // :1534
        await era.printAndWait(`「唔、唔……喔……感、感觉到了……」`); // :1535
        // CFLAG:321  = 4（变量语义：CFLAG 族，321） // :1536
        era.set(`cflag:${target}:321`, 4); // :1536

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:321`) <= 2 || era.get('flag:7') == 2)) { // :1538
        await era.printAndWait(`「随你……喜欢…………」`); // :1539
        // CFLAG:321  = 3（变量语义：CFLAG 族，321） // :1540
        era.set(`cflag:${target}:321`, 3); // :1540

      } else if (era.get(`cflag:${target}:321`) <= 1 || era.get('flag:7') == 2) { // :1542
        await era.printAndWait(`「可恶！不要啊！　别弄我！！」`); // :1543
        // CFLAG:321  = 2（变量语义：CFLAG 族，321） // :1544
        era.set(`cflag:${target}:321`, 2); // :1544
      } // :1545
      return 0; // :1546
    } // :1547
  } // :1548




  if (era_flag.selectcom == 21) { // :1553

    if (era.get(`cflag:${target}:322`) == 0) { // :1555

      if (era.get(`talent:${target}:0`) == 1) { // :1557

        if (era.get(`talent:${target}:76`) == 1) { // :1559
          await era.printAndWait(`「快，快啊！　用肉棒！！　夺取我的第一次…！」`); // :1560

        } else if (era.get(`talent:${target}:85`) == 1) { // :1562
          await era.printAndWait(`「想要…想要啊！　你的……！」`); // :1563


        } else { // :1566
          await era.printAndWait(`「别开玩笑了……第一次……居然是这种形式……」`); // :1567
        } // :1568

      } else { // :1570

        if (era.get(`talent:${target}:76`) == 1) { // :1572
          await era.printAndWait(`「快，快啊！　肉棒！肉棒哦！！」`); // :1573

        } else if (era.get(`talent:${target}:85`) == 1) { // :1575
          await era.printAndWait(`「想要…想要啊！　你的……！」`); // :1576

        } else { // :1578
          await era.printAndWait(`「像野兽一样的姿势……讨厌……」`); // :1579
        } // :1580
      } // :1581
      // CFLAG:322  = 1（变量语义：CFLAG 族，322） // :1582
      era.set(`cflag:${target}:322`, 1); // :1582
      return 0; // :1583

    } else if (era.get(`talent:${target}:153`)) { // :1585

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:322`) <= 5 || era.get('flag:7') == 2)) { // :1587
        if (rand_n(3) == 0) { // :1588
          await era.printAndWait(`「让肚子里的小家伙也尝尝精液的味道吧！！」`); // :1589
        } else if (rand_n(2) == 0) { // :1590
          await era.printAndWait(`「连孕妇也上……简直就是野兽嘛♪」`); // :1591
        } else { // :1592
          await era.printAndWait(`「嗯啊啊啊啊噢噢噢！！」`); // :1593
        } // :1594
        // CFLAG:322  = 6（变量语义：CFLAG 族，322） // :1595
        era.set(`cflag:${target}:322`, 6); // :1595

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:322`) <= 4 || era.get('flag:7') == 2)) { // :1597
        if (rand_n(3) == 0) { // :1598
          await era.printAndWait(`「感觉到了……连肚子里的小家伙也感觉到咯♪」`); // :1599
        } else if (rand_n(2) == 0) { // :1600
          await era.printAndWait(`「肚子里、有感觉了……两个人一起感觉到咯♪」`); // :1601
        } else { // :1602
          await era.printAndWait(`「再深一点……把爱传给肚子里的小家伙把♪」`); // :1603
        } // :1604
        // CFLAG:322  = 5（变量语义：CFLAG 族，322） // :1605
        era.set(`cflag:${target}:322`, 5); // :1605

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:322`) <= 3 || era.get('flag:7') == 2)) { // :1607
        await era.printAndWait(`「好有感觉……这种姿势……孕妇怎么能……像野兽一样」`); // :1608
        // CFLAG:322  = 4（变量语义：CFLAG 族，322） // :1609
        era.set(`cflag:${target}:322`, 4); // :1609

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:322`) <= 2 || era.get('flag:7') == 2)) { // :1611
        await era.printAndWait(`「随便你了……无所谓了」`); // :1612
        // CFLAG:322  = 3（变量语义：CFLAG 族，322） // :1613
        era.set(`cflag:${target}:322`, 3); // :1613

      } else if (era.get(`cflag:${target}:322`) <= 1 || era.get('flag:7') == 2) { // :1615
        await era.printAndWait(`「强奸孕妇什么的……别开玩笑了」`); // :1616

        // CFLAG:322  = 2（变量语义：CFLAG 族，322） // :1618
        era.set(`cflag:${target}:322`, 2); // :1618
      } // :1619
      return 0; // :1620

    } else { // :1622

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:322`) <= 5 || era.get('flag:7') == 2)) { // :1624
        if (rand_n(3) == 0) { // :1625
          await era.printAndWait(`「再……再……再来！　使劲操我！！」`); // :1626
        } else if (rand_n(2) == 0) { // :1627
          await era.printAndWait(`「我……我就是你的一只母狗……！　太棒了……」`); // :1628
        } else { // :1629
          await era.printAndWait(`「唔啊啊啊啊啊啊！！」`); // :1630
        } // :1631
        // CFLAG:322  = 6（变量语义：CFLAG 族，322） // :1632
        era.set(`cflag:${target}:322`, 6); // :1632

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:322`) <= 4 || era.get('flag:7') == 2)) { // :1634
        if (rand_n(3) == 0) { // :1635
          await era.printAndWait(`「啊啊啊～……感觉太强烈了！魔王大人……请好好地疼爱你最忠实的母狗吧！」`); // :1636
        } else if (rand_n(2) == 0) { // :1637
          await era.printAndWait(`「这下流的姿势……也挺不错的…………只要是为了魔王大人……要人家做什么都可以！」`); // :1638
        } else { // :1639
          await era.printAndWait(`「深深地插我……深深地插我吧～♪　我是魔王大人的奴隶！我是魔王大人的母狗！　噢～～啊！！！」`); // :1640
        } // :1641
        // CFLAG:322  = 5（变量语义：CFLAG 族，322） // :1642
        era.set(`cflag:${target}:322`, 5); // :1642

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:322`) <= 3 || era.get('flag:7') == 2)) { // :1644
        await era.printAndWait(`「唔唔～感觉来了……这种姿势……像野兽一样…………」`); // :1645
        // CFLAG:322  = 4（变量语义：CFLAG 族，322） // :1646
        era.set(`cflag:${target}:322`, 4); // :1646

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:322`) <= 2 || era.get('flag:7') == 2)) { // :1648
        await era.printAndWait(`「喜，喜欢的话……也不是不行……」`); // :1649
        // CFLAG:322  = 3（变量语义：CFLAG 族，322） // :1650
        era.set(`cflag:${target}:322`, 3); // :1650

      } else if (era.get(`cflag:${target}:322`) <= 1 || era.get('flag:7') == 2) { // :1652
        await era.printAndWait(`「这样的姿势……好羞耻…………」`); // :1653

        // CFLAG:322  = 2（变量语义：CFLAG 族，322） // :1655
        era.set(`cflag:${target}:322`, 2); // :1655
      } // :1656
      return 0; // :1657
    } // :1658
  } // :1659



  if (era_flag.selectcom == 22) { // :1663
    if (era.get(`cflag:${target}:323`) == 0) { // :1664

      if (era.get(`talent:${target}:0`) == 1) { // :1666

        if (era.get(`talent:${target}:76`) == 1) { // :1668
          await era.printAndWait(`「用力地抱着我……夺取我的第一次把！……♪」`); // :1669

        } else if (era.get(`talent:${target}:85`) == 1) { // :1671
          await era.printAndWait(`「请，请温柔地抱着我……人家是第一次……」`); // :1672

        } else { // :1674
          await era.printAndWait(`「第一次……被你这家伙……！」`); // :1675
        } // :1676

      } else { // :1678

        if (era.get(`talent:${target}:76`) == 1) { // :1680
          await era.printAndWait(`「被这样地抱着……♪　好舒服啊……♪」`); // :1681

        } else if (era.get(`talent:${target}:85`) == 1) { // :1683
          await era.printAndWait(`「好棒啊……被这样地抱着……♪」`); // :1684

        } else { // :1686
          await era.printAndWait(`「你这家伙……这个样子……」`); // :1687
        } // :1688
      } // :1689
      // CFLAG:323  = 1（变量语义：CFLAG 族，323） // :1690
      era.set(`cflag:${target}:323`, 1); // :1690
      return 0; // :1691

    } else { // :1693

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:323`) <= 5 || era.get('flag:7') == 2)) { // :1695
        if (rand_n(3) == 0) { // :1696
          await era.printAndWait(`「来亲亲吧～……噢……啊啊～♪　好深，好深啊！……♪」`); // :1697
        } else if (rand_n(2) == 0) { // :1698
          await era.printAndWait(`「再用力地抱紧我吧……啊……再插到里面去……喔喔～啊！♪」`); // :1699
        } else { // :1700
          await era.printAndWait(`「好……好啊……能清楚地看到你的神情呢……嘻嘻～」`); // :1701
        } // :1702
        // CFLAG:323  = 6（变量语义：CFLAG 族，323） // :1703
        era.set(`cflag:${target}:323`, 6); // :1703

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:323`) <= 4 || era.get('flag:7') == 2)) { // :1705
        if (rand_n(3) == 0) { // :1706
          await era.printAndWait(`「紧紧相拥……深深凝视……好喜欢这样……我是魔王大人的东西～我永远都是魔王大人的东西～！！」`); // :1707
        } else if (rand_n(2) == 0) { // :1708
          await era.printAndWait(`「再用力地抱紧我吧……啊啊……身心都要融化了……在魔王大人的怀里～好幸福啊～！」`); // :1709
        } else { // :1710
          await era.printAndWait(`「亲亲……想亲亲……彼此相连着……温柔地……嘻嘻～……能遇见魔王大人，真是我上辈子的福气！」`); // :1711
        } // :1712
        // CFLAG:323  = 5（变量语义：CFLAG 族，323） // :1713
        era.set(`cflag:${target}:323`, 5); // :1713

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:323`) <= 3 || era.get('flag:7') == 2)) { // :1715
        await era.printAndWait(`「啊……这感觉……被你抱着……啊……噢…………」`); // :1716
        // CFLAG:323  = 4（变量语义：CFLAG 族，323） // :1717
        era.set(`cflag:${target}:323`, 4); // :1717

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:323`) <= 2 || era.get('flag:7') == 2)) { // :1719
        await era.printAndWait(`「哦！～哦！……喜，喜欢……的话……可以亲……亲哦……」`); // :1720
        // CFLAG:323  = 3（变量语义：CFLAG 族，323） // :1721
        era.set(`cflag:${target}:323`, 3); // :1721

      } else if (era.get(`cflag:${target}:323`) <= 1 || era.get('flag:7') == 2) { // :1723
        await era.printAndWait(`「住手……这样子的……快住手啊……！」`); // :1724
        // CFLAG:323  = 2（变量语义：CFLAG 族，323） // :1725
        era.set(`cflag:${target}:323`, 2); // :1725
      } // :1726
      return 0; // :1727
    } // :1728
  } // :1729




  if (era_flag.selectcom == 23) { // :1734
    if (era.get(`cflag:${target}:324`) == 0) { // :1735

      if (era.get(`talent:${target}:0`) == 1) { // :1737

        if (era.get(`talent:${target}:76`) == 1) { // :1739
          await era.printAndWait(`「第一次就从后面来啊……嘻嘻，有意思……」`); // :1740

        } else if (era.get(`talent:${target}:85`) == 1) { // :1742
          await era.printAndWait(`「第一次什么的……从后抱着我…………」`); // :1743

        } else { // :1745
          await era.printAndWait(`「明明是第一次还要从后面来……讨厌…………」`); // :1746
        } // :1747

      } else { // :1749

        if (era.get(`talent:${target}:76`) == 1) { // :1751
          await era.printAndWait(`「从后面来啊……嘻嘻，有意思……」`); // :1752

        } else if (era.get(`talent:${target}:85`) == 1) { // :1754
          await era.printAndWait(`「从后抱着我…………」`); // :1755

        } else { // :1757
          await era.printAndWait(`「从后面来……讨厌…………」`); // :1758
        } // :1759
      } // :1760
      // CFLAG:324  = 1（变量语义：CFLAG 族，324） // :1761
      era.set(`cflag:${target}:324`, 1); // :1761
      return 0; // :1762

    } else { // :1764

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:324`) <= 5 || era.get('flag:7') == 2)) { // :1766
        if (rand_n(3) == 0) { // :1767
          await era.printAndWait(`「揉我的胸！！……从后面，把我顶飞吧！！♪」`); // :1768
        } else if (rand_n(2) == 0) { // :1769
          await era.printAndWait(`「好棒……哈……唔……哦哦哦！……用力顶我！操我～♪」`); // :1770
        } else { // :1771
          await era.printAndWait(`「好……唔唔唔……啊……噢！！～好棒啊～♪」`); // :1772
        } // :1773
        // CFLAG:324  = 6（变量语义：CFLAG 族，324） // :1774
        era.set(`cflag:${target}:324`, 6); // :1774

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:324`) <= 4 || era.get('flag:7') == 2)) { // :1776
        if (rand_n(3) == 0) { // :1777
          await era.printAndWait(`「从后面……抱紧人家嘛～♪　啊……好深……魔王大人……我好爱你啊！～」`); // :1778
        } else if (rand_n(2) == 0) { // :1779
          await era.printAndWait(`「被这样抱着～……太舒服了……噢～♪　人家再也离不开魔王大人了……」`); // :1780
        } else { // :1781
          await era.printAndWait(`「唔唔～……想看你的脸～！嘻嘻～不过你也看不到我羞羞的样子～……♪　魔王大人的气息……喷在我脖子上……感觉全身都酸麻了」`); // :1782
        } // :1783
        // CFLAG:324  = 5（变量语义：CFLAG 族，324） // :1784
        era.set(`cflag:${target}:324`, 5); // :1784

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:324`) <= 3 || era.get('flag:7') == 2)) { // :1786
        await era.printAndWait(`「唔……啊……啊……哦哦哦！！」`); // :1787
        // CFLAG:324  = 4（变量语义：CFLAG 族，324） // :1788
        era.set(`cflag:${target}:324`, 4); // :1788

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:324`) <= 2 || era.get('flag:7') == 2)) { // :1790
        await era.printAndWait(`「不……不要这样……盯着我嘛……还这么近……」`); // :1791
        // CFLAG:324  = 3（变量语义：CFLAG 族，324） // :1792
        era.set(`cflag:${target}:324`, 3); // :1792

      } else if (era.get(`cflag:${target}:324`) <= 1 || era.get('flag:7') == 2) { // :1794
        await era.printAndWait(`「呃……从后面…不要啦！」`); // :1795
        // CFLAG:324  = 2（变量语义：CFLAG 族，324） // :1796
        era.set(`cflag:${target}:324`, 2); // :1796
      } // :1797
      return 0; // :1798
    } // :1799
  } // :1800




  if (era_flag.selectcom == 26) { // :1805

    if (era.get(`cflag:${target}:327`) == 0) { // :1807

      if (era.get(`talent:${target}:76`) == 1) { // :1809
        await era.printAndWait(`「哈哈，喜欢走后门吗～好哦……来吧！」`); // :1810

      } else if (era.get(`talent:${target}:85`) == 1) { // :1812
        await era.printAndWait(`「喜欢这种地方吗……？　大～变～态～」`); // :1813

      } else { // :1815
        await era.printAndWait(`「停、停下啊！　在想什么哪！」`); // :1816
      } // :1817
      // CFLAG:TARGET:327  = 1（变量语义：CFLAG 族，TARGET:327） // :1818
      era.set(`cflag:${target}:327`, 1); // :1818
      return 0; // :1819

    } else { // :1821

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 6 || era.get('flag:7') == 2)) { // :1823
        if (rand_n(3) == 0) { // :1824
          await era.printAndWait(`「唔～哦哦～♪　菊穴，感觉太强烈了～♪」`); // :1825
        } else if (rand_n(2) == 0) { // :1826
          await era.printAndWait(`「唔～啊啊～♪　插入便便的洞洞里了～♪」`); // :1827
        } else { // :1828
          await era.printAndWait(`「呃～后面的洞～哦！哦哦～♪」`); // :1829
        } // :1830
        // CFLAG:327  = 7（变量语义：CFLAG 族，327） // :1831
        era.set(`cflag:${target}:327`, 7); // :1831

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:327`) <= 5 || era.get('flag:7') == 2)) { // :1833
        await era.printAndWait(`「痛～好痛……没事～♪　没关系的，马上就会习惯的啦～」`); // :1834
        // CFLAG:327  = 6（变量语义：CFLAG 族，327） // :1835
        era.set(`cflag:${target}:327`, 6); // :1835

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 4 || era.get('flag:7') == 2)) { // :1837
        if (rand_n(2) == 0) { // :1838
          await era.printAndWait(`「屁股……啊～感觉到了～噢～♪」`); // :1839
        } else { // :1840
          await era.printAndWait(`「屁股……屁股好热～好烫啊～♪」`); // :1841
        } // :1842
        // CFLAG:327  = 5（变量语义：CFLAG 族，327） // :1843
        era.set(`cflag:${target}:327`, 5); // :1843

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:327`) <= 3 || era.get('flag:7') == 2)) { // :1845
        await era.printAndWait(`「呃！啊！痛！～人家，人家会努力提高屁股的感觉……没关系，很，很舒服」`); // :1846
        // CFLAG:327  = 4（变量语义：CFLAG 族，327） // :1847
        era.set(`cflag:${target}:327`, 4); // :1847

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 2 || era.get('flag:7') == 2)) { // :1849
        await era.printAndWait(`「唔哦……啊～♪　！　感，感觉到了～……」`); // :1850
        // CFLAG:327  = 3（变量语义：CFLAG 族，327） // :1851
        era.set(`cflag:${target}:327`, 3); // :1851

      } else if (era.get(`cflag:${target}:327`) <= 1 || era.get('flag:7') == 2) { // :1853
        await era.printAndWait(`「要用这种地方……」`); // :1854
        // CFLAG:327  = 2（变量语义：CFLAG 族，327） // :1855
        era.set(`cflag:${target}:327`, 2); // :1855
      } // :1856
      return 0; // :1857
    } // :1858
  } // :1859




  if (era_flag.selectcom == 27) { // :1864

    if (era.get(`cflag:${target}:328`) == 0) { // :1866

      if (era.get(`talent:${target}:76`) == 1) { // :1868
        await era.printAndWait(`「终……终于…要用肉棒插我了吗！　等好久了……」`); // :1869

      } else if (era.get(`talent:${target}:85`) == 1) { // :1871
        await era.printAndWait(`「这，这么脏的地方……会弄脏你的棒棒的……」`); // :1872

      } else { // :1874
        await era.printAndWait(`「你这人，整天在想些什么啊！　这个……变态狂！」`); // :1875
      } // :1876
      // CFLAG:TARGET:328  = 1（变量语义：CFLAG 族，TARGET:328） // :1877
      era.set(`cflag:${target}:328`, 1); // :1877
      return 0; // :1878

    } else { // :1880

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 6 || era.get('flag:7') == 2)) { // :1882
        if (rand_n(2) == 0) { // :1883
          await era.printAndWait(`「呼……呼……唔哦哦哦哦哦哦！！　用力插进去！把我里面弄得乱七八糟吧！！」`); // :1884
        } else { // :1885
          await era.printAndWait(`「啊……哦哦……光插进来，感觉就这么地强烈……我，我是你的菊穴奴隶了～♪」`); // :1886
        } // :1887
        // CFLAG:328  = 7（变量语义：CFLAG 族，328） // :1888
        era.set(`cflag:${target}:328`, 7); // :1888

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:328`) <= 5 || era.get('flag:7') == 2)) { // :1890
        await era.printAndWait(`「啊……好、好哦……再、再来……」`); // :1891
        // CFLAG:328  = 6（变量语义：CFLAG 族，328） // :1892
        era.set(`cflag:${target}:328`, 6); // :1892

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 4 || era.get('flag:7') == 2)) { // :1894
        if (rand_n(2) == 0) { // :1895
          await era.printAndWait(`「好…好棒……魔王大人…你…就喜欢这种地方么…噢哦哦！！」`); // :1896
        } else { // :1897
          await era.printAndWait(`「屁股…好舒服啊～…已经，已经回不去了………」`); // :1898
        } // :1899
        // CFLAG:328  = 5（变量语义：CFLAG 族，328） // :1900
        era.set(`cflag:${target}:328`, 5); // :1900

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:328`) <= 3 || era.get('flag:7') == 2)) { // :1902
        await era.printAndWait(`「这，这种姿势插这样的洞洞……好像野兽一样……」`); // :1903
        // CFLAG:328  = 4（变量语义：CFLAG 族，328） // :1904
        era.set(`cflag:${target}:328`, 4); // :1904

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 2 || era.get('flag:7') == 2)) { // :1906
        await era.printAndWait(`「这、这样的洞、我……我居然……有感觉了……」`); // :1907
        // CFLAG:328  = 3（变量语义：CFLAG 族，328） // :1908
        era.set(`cflag:${target}:328`, 3); // :1908

      } else if (era.get(`cflag:${target}:328`) <= 1 || era.get('flag:7') == 2) { // :1910
        await era.printAndWait(`「好、好脏……不要弄那里！」`); // :1911
        // CFLAG:328  = 2（变量语义：CFLAG 族，328） // :1912
        era.set(`cflag:${target}:328`, 2); // :1912
      } // :1913
      return 0; // :1914
    } // :1915
  } // :1916




  if (era_flag.selectcom == 28) { // :1921

    if (era.get(`cflag:${target}:329`) == 0) { // :1923

      if (era.get(`talent:${target}:76`) == 1) { // :1925
        await era.printAndWait(`「啊……这样面对面地欺负人家的菊花啊～……♪」`); // :1926

      } else if (era.get(`talent:${target}:85`) == 1) { // :1928
        await era.printAndWait(`「这样的地方……嘻嘻，真会玩！」`); // :1929

      } else { // :1931
        await era.printAndWait(`「哼……不想见到你这家伙的脸……」`); // :1932
      } // :1933
      // CFLAG:TARGET:329  = 1（变量语义：CFLAG 族，TARGET:329） // :1934
      era.set(`cflag:${target}:329`, 1); // :1934
      return 0; // :1935

    } else { // :1937

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 6 || era.get('flag:7') == 2)) { // :1939
        if (rand_n(3) == 0) { // :1940
          await era.printAndWait(`「啊……要融化了……再用力抱我啊～」`); // :1941
        } else if (rand_n(2) == 0) { // :1942
          await era.printAndWait(`「来嘛……来嘛……看着我这下贱的神色……♪」`); // :1943
        } else { // :1944
          await era.printAndWait(`「菊花要融化了……多么美妙啊……♪」`); // :1945
        } // :1946
        // CFLAG:329  = 7（变量语义：CFLAG 族，329） // :1947
        era.set(`cflag:${target}:329`, 7); // :1947

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:329`) <= 5 || era.get('flag:7') == 2)) { // :1949
        await era.printAndWait(`「呵呵……来的好……感觉到了……」`); // :1950
        // CFLAG:329  = 6（变量语义：CFLAG 族，329） // :1951
        era.set(`cflag:${target}:329`, 6); // :1951

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 4 || era.get('flag:7') == 2)) { // :1953
        if (rand_n(2) == 0) { // :1954
          await era.printAndWait(`「啊……好棒……哦～♪」`); // :1955
        } else { // :1956
          await era.printAndWait(`「再继续弄屁股……往里面去～……♪」`); // :1957
        } // :1958
        // CFLAG:329  = 5（变量语义：CFLAG 族，329） // :1959
        era.set(`cflag:${target}:329`, 5); // :1959

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:329`) <= 3 || era.get('flag:7') == 2)) { // :1961
        await era.printAndWait(`「会，会努力的……为了让魔王大人高兴……会让这里也很有感觉……」`); // :1962
        // CFLAG:329  = 4（变量语义：CFLAG 族，329） // :1963
        era.set(`cflag:${target}:329`, 4); // :1963

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 2 || era.get('flag:7') == 2)) { // :1965
        await era.printAndWait(`「呃……这种……地方……居然有感觉了……」`); // :1966
        // CFLAG:329  = 3（变量语义：CFLAG 族，329） // :1967
        era.set(`cflag:${target}:329`, 3); // :1967

      } else if (era.get(`cflag:${target}:329`) <= 1 || era.get('flag:7') == 2) { // :1969
        await era.printAndWait(`「好痛……痛死了！一点都不舒服！快停止！！」`); // :1970
        // CFLAG:329  = 2（变量语义：CFLAG 族，329） // :1971
        era.set(`cflag:${target}:329`, 2); // :1971
      } // :1972
      return 0; // :1973
    } // :1974
  } // :1975




  if (era_flag.selectcom == 29) { // :1980

    if (era.get(`cflag:${target}:330`) == 0) { // :1982

      if (era.get(`talent:${target}:76`) == 1) { // :1984
        await era.printAndWait(`「呵呵……来吧～……♪」`); // :1985
        await era.printAndWait(`${target_name}扭动着腰，诱惑着你。`); // :1986

      } else if (era.get(`talent:${target}:85`) == 1) { // :1988
        await era.printAndWait(`「请通过屁股……疼爱我吧……♪」`); // :1989

      } else { // :1991
        await era.printAndWait(`「你……你这家伙，居然走后门！」`); // :1992
      } // :1993
      // CFLAG:TARGET:330  = 1（变量语义：CFLAG 族，TARGET:330） // :1994
      era.set(`cflag:${target}:330`, 1); // :1994
      return 0; // :1995

    } else { // :1997

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 6 || era.get('flag:7') == 2)) { // :1999
        if (rand_n(2) == 0) { // :2000
          await era.printAndWait(`「啊……被从后……贯穿啦～♪」`); // :2001
        } else { // :2002
          await era.printAndWait(`「好棒……好棒……拉屎的洞，还能这么用～♪」`); // :2003
        } // :2004
        // CFLAG:330  = 7（变量语义：CFLAG 族，330） // :2005
        era.set(`cflag:${target}:330`, 7); // :2005

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:330`) <= 5 || era.get('flag:7') == 2)) { // :2007
        await era.printAndWait(`「嘻嘻……菊花也是好东西呢～」`); // :2008
        // CFLAG:330  = 6（变量语义：CFLAG 族，330） // :2009
        era.set(`cflag:${target}:330`, 6); // :2009

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 4 || era.get('flag:7') == 2)) { // :2011
        if (rand_n(2) == 0) { // :2012
          await era.printAndWait(`「啊……好有快感……用这样的地方……」`); // :2013
        } else { // :2014
          await era.printAndWait(`「为了魔王大人……用下流的地方……做下流的事了……」`); // :2015
        } // :2016
        // CFLAG:330  = 5（变量语义：CFLAG 族，330） // :2017
        era.set(`cflag:${target}:330`, 5); // :2017

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:330`) <= 3 || era.get('flag:7') == 2)) { // :2019
        await era.printAndWait(`「还是有点痛……会习惯的……」`); // :2020
        // CFLAG:330  = 4（变量语义：CFLAG 族，330） // :2021
        era.set(`cflag:${target}:330`, 4); // :2021

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 2 || era.get('flag:7') == 2)) { // :2023
        await era.printAndWait(`「被弄这地方……居然有快感了……」`); // :2024
        // CFLAG:330  = 3（变量语义：CFLAG 族，330） // :2025
        era.set(`cflag:${target}:330`, 3); // :2025

      } else if (era.get(`cflag:${target}:330`) <= 1 || era.get('flag:7') == 2) { // :2027
        await era.printAndWait(`「呃啊！……真的只有痛楚啦……！」`); // :2028
        // CFLAG:330  = 2（变量语义：CFLAG 族，330） // :2029
        era.set(`cflag:${target}:330`, 2); // :2029
      } // :2030
      return 0; // :2031
    } // :2032
  } // :2033




  if (era_flag.selectcom == 30) { // :2038

    if (era.get(`cflag:${target}:331`) == 0) { // :2040

      if (era.get(`talent:${target}:76`) == 1) { // :2042
        await era.printAndWait(`「嘻嘻……喜欢用手啊～……♪」`); // :2043

      } else if (era.get(`talent:${target}:85`) == 1) { // :2045
        await era.printAndWait(`「重要的地方……要慎重地对待！♪」`); // :2046

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2048
        await era.printAndWait(`「要我用手？！……好……好吧……！」`); // :2049

      } else { // :2051
        await era.printAndWait(`「光碰到就觉得恶心！」`); // :2052
      } // :2053
      // CFLAG:TARGET:331  = 1（变量语义：CFLAG 族，TARGET:331） // :2054
      era.set(`cflag:${target}:331`, 1); // :2054
      return 0; // :2055

    } else { // :2057

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 5 || era.get('flag:7') == 2)) { // :2059
        if (era.get(`talent:${player}:318`) == 1) { // :2060

          await era.printAndWait(`「呵呵，真是令人惊叹啊……两只手都握不住呢♪」`); // :2062
        } else if (era.get(`talent:${player}:318`) == 2) { // :2063

          await era.printAndWait(`「什么啊这个小玩意……呵呵，再多勃起一点，喏，在更努力一点吧♪」`); // :2065
        } else if (era.get(`talent:${player}:318`) == 3) { // :2066

          await era.printAndWait(`「呵呵，把皮裹着的肉棒一点一点剥开什么的真好意思说呢♪」`); // :2068
        } else if (era.get(`talent:${player}:318`) == 4) { // :2069

          await era.printAndWait(`「马肉棒……怎么做才好呢，呵呵，这样如何？」`); // :2071
        } // :2072
        if (rand_n(2) == 0) { // :2073
          await era.printAndWait(`「你看你看～……要出来了哦！……♪」`); // :2074
        } else { // :2075
          await era.printAndWait(`「嘻嘻～……不停地跳动着……看起来很美味呢～♪」`); // :2076
        } // :2077
        // CFLAG:331  = 6（变量语义：CFLAG 族，331） // :2078
        era.set(`cflag:${target}:331`, 6); // :2078

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:331`) <= 4 || era.get('flag:7') == 2)) { // :2080
        if (era.get(`talent:${player}:318`) == 1) { // :2081

          await era.printAndWait(`「哦，真是令人惊叹啊……两只手都握不住呢♪」`); // :2083
        } else if (era.get(`talent:${player}:318`) == 2) { // :2084

          await era.printAndWait(`「什么啊这个小玩意……呵呵，再多勃起一点，喏，在更努力一点吧♪」`); // :2086
        } else if (era.get(`talent:${player}:318`) == 3) { // :2087

          await era.printAndWait(`「呵呵，把皮裹着的棒棒一点一点剥开什么的真好意思说呢♪」`); // :2089
        } else if (era.get(`talent:${player}:318`) == 4) { // :2090

          await era.printAndWait(`「马棒棒……怎么做才好呢，呵呵，这样如何？」`); // :2092
        } // :2093
        if (rand_n(2) == 0) { // :2094
          await era.printAndWait(`「会好好地侍奉魔王大人的……你看！它变硬了呢～♪」`); // :2095
        } else { // :2096
          await era.printAndWait(`「舒服么……？　嘻嘻～」`); // :2097
        } // :2098
        // CFLAG:331  = 5（变量语义：CFLAG 族，331） // :2099
        era.set(`cflag:${target}:331`, 5); // :2099

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 3 || era.get('flag:7') == 2)) { // :2101
        await era.printAndWait(`「用手的感觉如何？」`); // :2102
        // CFLAG:331  = 4（变量语义：CFLAG 族，331） // :2103
        era.set(`cflag:${target}:331`, 4); // :2103

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 2 || era.get('flag:7') == 2)) { // :2105
        await era.printAndWait(`「我知道啦……只是摸一下哦」`); // :2106
        // CFLAG:331  = 3（变量语义：CFLAG 族，331） // :2107
        era.set(`cflag:${target}:331`, 3); // :2107

      } else if (era.get(`cflag:${target}:331`) <= 1 || era.get('flag:7') == 2) { // :2109
        await era.printAndWait(`「好脏……不知廉耻！！」`); // :2110
        // CFLAG:331  = 2（变量语义：CFLAG 族，331） // :2111
        era.set(`cflag:${target}:331`, 2); // :2111
      } // :2112
      return 0; // :2113
    } // :2114
  } // :2115




  if (era_flag.selectcom == 31) { // :2120

    if (era.get(`cflag:${target}:332`) == 0) { // :2122

      if (era.get(`talent:${target}:76`) == 1) { // :2124
        await era.printAndWait(`「为什么留到现在才让我舔呢……？　嘻嘻」`); // :2125

      } else if (era.get(`talent:${target}:85`) == 1) { // :2127
        await era.printAndWait(`「一直期待着，用嘴巴侍奉魔王大人……！」`); // :2128

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2130
        await era.printAndWait(`「只，只是舔一下的话……」`); // :2131

      } else { // :2133
        await era.printAndWait(`「这，这种东西也能舔啊……？！」`); // :2134
      } // :2135
      // CFLAG:TARGET:332  = 1（变量语义：CFLAG 族，TARGET:332） // :2136
      era.set(`cflag:${target}:332`, 1); // :2136
      return 0; // :2137

    } else { // :2139

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2141
        await era.printAndWait(`「把精液射给我……已经无法忍耐啦～　唔～～唔～～～」`); // :2142
        await era.print(`${target_name}用舌头舔舐着阴茎，时而含入嘴里。`); // :2143
        // CFLAG:332  = 6（变量语义：CFLAG 族，332） // :2144
        era.set(`cflag:${target}:332`, 6); // :2144

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2146
        await era.printAndWait(`「嘻嘻～它变得这么硬了～」`); // :2147
        // CFLAG:332  = 5（变量语义：CFLAG 族，332） // :2148
        era.set(`cflag:${target}:332`, 5); // :2148

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2150
        await era.printAndWait(`「魔王大人……想射的时候，随时可以射出来哦～人家会好好地接住的！」`); // :2151
        await era.print(`${target_name}充满爱意地将阴茎含入嘴里，头部有节奏地运动着。`); // :2152
        // CFLAG:332  = 4（变量语义：CFLAG 族，332） // :2153
        era.set(`cflag:${target}:332`, 4); // :2153

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:332`) <= 2 || era.get('flag:7') == 2)) { // :2155
        await era.printAndWait(`「我知道啦……只，只是舔一下哦…………」`); // :2156
        // CFLAG:332  = 3（变量语义：CFLAG 族，332） // :2157
        era.set(`cflag:${target}:332`, 3); // :2157

      } else if (era.get(`cflag:${target}:332`) <= 1 || era.get('flag:7') == 2) { // :2159
        await era.printAndWait(`「真……讨厌……好臭…………」`); // :2160
        // CFLAG:332  = 2（变量语义：CFLAG 族，332） // :2161
        era.set(`cflag:${target}:332`, 2); // :2161
      } // :2162
      return 0; // :2163
    } // :2164
  } // :2165




  if (era_flag.selectcom == 32) { // :2170

    if (era.get(`cflag:${target}:333`) == 0) { // :2172

      if (era.get(`talent:${target}:76`) == 1) { // :2174
        await era.printAndWait(`「也有喜欢用胸部夹着的人呢～♪」`); // :2175

      } else if (era.get(`talent:${target}:85`) == 1) { // :2177
        await era.printAndWait(`「看啊～用胸部夹起来了哦～♪」`); // :2178

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2180
        await era.printAndWait(`「这，这样就行了吧……」`); // :2181

      } else { // :2183
        await era.printAndWait(`「夹着你那玩意儿……别说傻话了！」`); // :2184
      } // :2185
      // CFLAG:TARGET:333  = 1（变量语义：CFLAG 族，TARGET:333） // :2186
      era.set(`cflag:${target}:333`, 1); // :2186
      return 0; // :2187

    } else { // :2189

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 5 || era.get('flag:7') == 2)) { // :2191
        if (rand_n(2) == 0) { // :2192
          await era.printAndWait(`「舒服么……？　嘻嘻～感觉胸部都开始变烫了～……」`); // :2193
        } else { // :2194
          await era.printAndWait(`「柔软么？　到底是什么样的感觉？　嗯？」`); // :2195
        } // :2196
        // CFLAG:333  = 6（变量语义：CFLAG 族，333） // :2197
        era.set(`cflag:${target}:333`, 6); // :2197

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 4 || era.get('flag:7') == 2)) { // :2199
        await era.printAndWait(`「喜欢用胸部啊……？　嘻嘻」`); // :2200
        // CFLAG:333  = 5（变量语义：CFLAG 族，333） // :2201
        era.set(`cflag:${target}:333`, 5); // :2201

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:333`) <= 3 || era.get('flag:7') == 2)) { // :2203
        if (rand_n(2) == 0) { // :2204
          await era.printAndWait(`「舒服的话，随时可以射出来哦！……人家会用嘴巴接住魔王大人那宝贵的精华的～♪」`); // :2205
        } else { // :2206
          await era.printAndWait(`「魔王大人，舒服么……？　很柔软吧？人家的胸，就是为了服侍魔王大人的……」`); // :2207
        } // :2208
        // CFLAG:333  = 4（变量语义：CFLAG 族，333） // :2209
        era.set(`cflag:${target}:333`, 4); // :2209

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:333`) <= 2 || era.get('flag:7') == 2)) { // :2211
        await era.printAndWait(`「只是夹着就好了……我知道啦」`); // :2212
        // CFLAG:333  = 3（变量语义：CFLAG 族，333） // :2213
        era.set(`cflag:${target}:333`, 3); // :2213

      } else if (era.get(`cflag:${target}:333`) <= 1 || era.get('flag:7') == 2) { // :2215
        await era.printAndWait(`「这种难以置信的行为……可恶…………」`); // :2216
        // CFLAG:333  = 2（变量语义：CFLAG 族，333） // :2217
        era.set(`cflag:${target}:333`, 2); // :2217
      } // :2218
      return 0; // :2219
    } // :2220
  } // :2221




  if (era_flag.selectcom == 33) { // :2226

    if (era.get(`cflag:${target}:334`) == 0) { // :2228

      if (era.get(`talent:${target}:76`) == 1) { // :2230
        await era.printAndWait(`「哦！还有这种玩法啊……！」`); // :2231

      } else if (era.get(`talent:${target}:85`) == 1) { // :2233
        await era.printAndWait(`「用胯间啊……嘻嘻，真有意思～」`); // :2234

      } else { // :2236
        await era.printAndWait(`「吓……什么动作？」`); // :2237
      } // :2238
      // CFLAG:TARGET:334  = 1（变量语义：CFLAG 族，TARGET:334） // :2239
      era.set(`cflag:${target}:334`, 1); // :2239
      return 0; // :2240

    } else { // :2242

      if (era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:334`) <= 5 || era.get('flag:7') == 2)) { // :2244
        await era.printAndWait(`「放错地方啦！……嘻嘻，开玩笑的～好想早点被破处啊～！」`); // :2245
        // CFLAG:334  = 6（变量语义：CFLAG 族，334） // :2246
        era.set(`cflag:${target}:334`, 6); // :2246

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:334`) <= 4 || era.get('flag:7') == 2)) { // :2248
        await era.printAndWait(`「摩擦摩擦～……真舒服～♪」`); // :2249
        // CFLAG:334  = 5（变量语义：CFLAG 族，334） // :2250
        era.set(`cflag:${target}:334`, 5); // :2250

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:334`) <= 3 || era.get('flag:7') == 2)) { // :2252
        await era.printAndWait(`「嘻嘻……想放进去么？　人家的第一次，想要献给魔王大人～」`); // :2253
        // CFLAG:334  = 4（变量语义：CFLAG 族，334） // :2254
        era.set(`cflag:${target}:334`, 4); // :2254

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:334`) <= 2 || era.get('flag:7') == 2)) { // :2256
        await era.printAndWait(`「嘻嘻……变的这么硬了……这么舒服哦？」`); // :2257
        // CFLAG:334  = 3（变量语义：CFLAG 族，334） // :2258
        era.set(`cflag:${target}:334`, 3); // :2258

      } else if (era.get(`cflag:${target}:334`) <= 1 || era.get('flag:7') == 2) { // :2260
        await era.printAndWait(`「嗯……这样就好了吗……？」`); // :2261
        // CFLAG:334  = 2（变量语义：CFLAG 族，334） // :2262
        era.set(`cflag:${target}:334`, 2); // :2262
      } // :2263
      return 0; // :2264
    } // :2265
  } // :2266




  if (era_flag.selectcom == 34) { // :2271

    if (era.get(`cflag:${target}:335`) == 0) { // :2273

      if (era.get(`talent:${target}:0`) == 1) { // :2275

        if (era.get(`talent:${target}:76`) == 1) { // :2277
          await era.printAndWait(`「呼……终于成为女人了……」`); // :2278
          await era.printAndWait(`「小鸡鸡好精神呢～那，我坐下来啦～」`); // :2279
          await era.printAndWait(`${target_name}迫不及待地沉下了腰……眉头闪过一丝痛苦，但还是慢慢地坐到底了。`); // :2280

        } else if (era.get(`talent:${target}:85`) == 1) { // :2282
          await era.printAndWait(`「谢谢魔王大人……终于……终于肯接受我的处女了……」`); // :2283
          await era.printAndWait(`「能……能把自己奉献给魔王大人……是我一生的荣幸……！　那……人家……坐下来了……」`); // :2284
          await era.printAndWait(`${target_name}带着期待又感动的表情，将${player_name}的阴茎吞入了。`); // :2285

        } else { // :2287
          await era.printAndWait(`「呜呜……居然是以这种形式破的处…………」`); // :2288
          await era.printAndWait(`和言语相反地，${target_name}遵从着命令，将${player_name}的阴茎吞入了。`); // :2289
        } // :2290

      } else { // :2292

        if (era.get(`talent:${target}:76`) == 1) { // :2294
          await era.printAndWait(`「小鸡鸡好精神呢～那，我不客气啦～」`); // :2295
          await era.printAndWait(`${target_name}迫不及待地沉下了腰……`); // :2296

        } else if (era.get(`talent:${target}:85`) == 1) { // :2298
          await era.printAndWait(`「喜欢这种体位吗？　嘻嘻～」`); // :2299
          await era.printAndWait(`${target_name}慢慢地沉下了腰，将${player_name}的阴茎吞入了。`); // :2300

        } else { // :2302
          await era.printAndWait(`「让，让我这样子…………」`); // :2303
          await era.printAndWait(`和言语相反地，${target_name}遵从着命令，将${player_name}的阴茎吞入了。`); // :2304
        } // :2305
      } // :2306
      // CFLAG:TARGET:335  = 1（变量语义：CFLAG 族，TARGET:335） // :2307
      era.set(`cflag:${target}:335`, 1); // :2307
      return 0; // :2308

    } else { // :2310

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:335`) <= 5 || era.get('flag:7') == 2)) { // :2312
        if (rand_n(4) == 0) { // :2313
          await era.printAndWait(`「只是躺着，这么轻松啊～嘻嘻」`); // :2314
        } else if (rand_n(3) == 0) { // :2315
          await era.printAndWait(`「看看～你的小鸡鸡慢慢地被我吞进去了哦？」`); // :2316
        } else if (rand_n(2) == 0) { // :2317
          await era.printAndWait(`「哼哼～欣赏我在你身上的舞蹈吧！」`); // :2318
        } else { // :2319
          await era.printAndWait(`「嘻嘻！等不及啦～」`); // :2320
        } // :2321
        await era.printAndWait(`${target_name}分开双腿，慢慢地坐了下来。`); // :2322
        // CFLAG:335  = 6（变量语义：CFLAG 族，335） // :2323
        era.set(`cflag:${target}:335`, 6); // :2323

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:335`) <= 4 || era.get('flag:7') == 2)) { // :2325
        if (rand_n(4) == 0) { // :2326
          await era.print(`「魔王大人……请躺好吧～让人家来侍奉你吧！……啊！好大……好满……喔……天啊……啊～啊～」`); // :2327
        } else if (rand_n(3) == 0) { // :2328
          await era.printAndWait(`「嘻嘻～要用力地往上顶人家哦～！……人家的小穴，是为了魔王大人而存在的……」`); // :2329
        } else if (rand_n(2) == 0) { // :2330
          await era.printAndWait(`「我重吗？感觉插得好深啊～好像插到脑海深处一般……噢……那里……啊……我无论灵魂还是肉体……都彻底是魔王大人的东西了……」`); // :2331
        } else { // :2332
          await era.printAndWait(`「身体被抠挖着一样……呵呵～我会用尽余生来好好地侍奉魔王大人的……」`); // :2333
        } // :2334
        await era.printAndWait(`展示出优秀的侍奉技术，${target_name}在${player_name}的身上翩翩起舞。`); // :2335
        // CFLAG:335  = 5（变量语义：CFLAG 族，335） // :2336
        era.set(`cflag:${target}:335`, 5); // :2336

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:335`) <= 3 || era.get('flag:7') == 2)) { // :2338
        if (rand_n(4) == 0) { // :2339
          await era.printAndWait(`「呃～啊～！……腰、腰自己动起来了……」`); // :2340
        } else if (rand_n(3) == 0) { // :2341
          await era.printAndWait(`「有什么在脑子里冲撞着……受不了啦……」`); // :2342
        } else if (rand_n(2) == 0) { // :2343
          await era.printAndWait(`「唔！在里面……好深啊……」`); // :2344
        } else { // :2345
          await era.printAndWait(`「不要啦！～再这么往上顶的话……的话…………」`); // :2346
        } // :2347
        // CFLAG:335  = 4（变量语义：CFLAG 族，335） // :2348
        era.set(`cflag:${target}:335`, 4); // :2348

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:335`) <= 2 || era.get('flag:7') == 2)) { // :2350
        await era.print(`${target_name}遵从着命令，跨坐在${player_name}的身上，把阴茎吞入体内了。`); // :2351
        await era.printAndWait(`「这，这样就可以了吗……要我保持这么下流的姿势……」`); // :2352
        // CFLAG:335  = 3（变量语义：CFLAG 族，335） // :2353
        era.set(`cflag:${target}:335`, 3); // :2353

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :2355
        await era.printAndWait(`「这种屈辱……咱们走着瞧！」`); // :2356
        // CFLAG:335  = 2（变量语义：CFLAG 族，335） // :2357
        era.set(`cflag:${target}:335`, 2); // :2357
      } // :2358
      return 0; // :2359
    } // :2360
  } // :2361




  if (era_flag.selectcom == 35) { // :2366

    if (era.get(`cflag:${target}:336`) == 0) { // :2368

      if (era.get(`abl:${target}:16`) >= 3) { // :2370
        await era.printAndWait(`「只是……擦身而已哦……！」`); // :2371

      } else { // :2373
        await era.printAndWait(`「洗……洗澡这东西……怎么帮？」`); // :2374
      } // :2375
      // CFLAG:TARGET:336  = 1（变量语义：CFLAG 族，TARGET:336） // :2376
      era.set(`cflag:${target}:336`, 1); // :2376
      return 0; // :2377

    } else { // :2379

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:336`) <= 4 || era.get('flag:7') == 2)) { // :2381
        await era.printAndWait(`「哈哈～！终于有机会，反过来对你上下其手啦！～」`); // :2382
        // CFLAG:336  = 5（变量语义：CFLAG 族，336） // :2383
        era.set(`cflag:${target}:336`, 5); // :2383

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:336`) <= 3 || era.get('flag:7') == 2)) { // :2385
        await era.printAndWait(`「魔王大人的身体……好漂亮……光是看着魔王大人的身体……人家…人家……就要…………噢！不！不是这样的！我是魔王大人最乖巧的奴隶！让我帮魔王大人清洁干净吧！」`); // :2386
        // CFLAG:336  = 4（变量语义：CFLAG 族，336） // :2387
        era.set(`cflag:${target}:336`, 4); // :2387

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:336`) <= 2 || era.get('flag:7') == 2)) { // :2389
        await era.printAndWait(`「我说啊……你平常洗澡也太不认真了吧……！」`); // :2390
        // CFLAG:336  = 3（变量语义：CFLAG 族，336） // :2391
        era.set(`cflag:${target}:336`, 3); // :2391

      } else if (era.get(`cflag:${target}:336`) <= 1 || era.get('flag:7') == 2) { // :2393
        await era.printAndWait(`「你妈妈没教你自己洗澡吗？」`); // :2394
        // CFLAG:336  = 2（变量语义：CFLAG 族，336） // :2395
        era.set(`cflag:${target}:336`, 2); // :2395
      } // :2396
      return 0; // :2397
    } // :2398
  } // :2399




  if (era_flag.selectcom == 36) { // :2404

    if (era.get(`cflag:${target}:337`) == 0) { // :2406

      if (era.get(`talent:${target}:76`) == 1) { // :2408
        await era.printAndWait(`「嘻嘻，躺下吧……让我来！！」`); // :2409
        await era.printAndWait(`「看啊！你的东西，被菊穴慢慢地吞进去了哦～……♪」`); // :2410
        await era.printAndWait(`${target_name}迫不及待地沉下了腰……`); // :2411

      } else if (era.get(`talent:${target}:85`) == 1) { // :2413
        await era.printAndWait(`「嘻嘻，好好地躺着吧……会让魔王大人的小鸡鸡很舒服的！」`); // :2414
        await era.printAndWait(`「啊……屁屁被疼爱的样子……魔王大人一定能看得很清楚的吧……」`); // :2415
        await era.printAndWait(`${target_name}慢慢地沉下了腰，将${player_name}的阴茎吞入了。`); // :2416

      } else { // :2418
        await era.printAndWait(`「要我自己来吗……还要用这个洞……讨厌……」`); // :2419
      } // :2420
      // CFLAG:TARGET:337  = 1（变量语义：CFLAG 族，TARGET:337） // :2421
      era.set(`cflag:${target}:337`, 1); // :2421
      return 0; // :2422

    } else { // :2424

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 6 || era.get('flag:7') == 2)) { // :2426
        if (rand_n(2) == 0) { // :2427
          await era.printAndWait(`「看哦！整根插进去啦哦……！」`); // :2428
          await era.printAndWait(`「唔～哦～！在里面……闹腾着～♪」`); // :2429
        } else { // :2430
          await era.printAndWait(`「便便的地方被欺负了……受不了啦～～」`); // :2431
          await era.printAndWait(`「要我再摇动屁股么？　噢～～……♪」`); // :2432
        } // :2433
        await era.printAndWait(`尻穴持续地侍奉着阴茎，${target_name}跨坐在${player_name}的身上，腰身扭动出淫秽的舞蹈。`); // :2434
        // CFLAG:337  = 7（变量语义：CFLAG 族，337） // :2435
        era.set(`cflag:${target}:337`, 7); // :2435

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:337`) <= 5 || era.get('flag:7') == 2)) { // :2437
        await era.printAndWait(`「呃～……里面……菊穴里面好热～」`); // :2438
        await era.printAndWait(`尻穴持续地侍奉着阴茎，${target_name}有节奏地起伏着身体。`); // :2439
        // CFLAG:337  = 6（变量语义：CFLAG 族，337） // :2440
        era.set(`cflag:${target}:337`, 6); // :2440

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 4 || era.get('flag:7') == 2)) { // :2442
        if (rand_n(2) == 0) { // :2443
          await era.printAndWait(`「魔王大人……人家的菊穴，舒服吗……？」`); // :2444
          await era.printAndWait(`「您的东西又硬又烫，弄得人家好舒服哦～♪」`); // :2445
        } else { // :2446
          await era.printAndWait(`「嘻嘻～魔王大人……我的屁股，还满意么？」`); // :2447
          await era.printAndWait(`「好舒服～……啊～！再顶我～……♪」`); // :2448
        } // :2449
        await era.printAndWait(`尻穴持续地侍奉着阴茎，${target_name}跨坐在${player_name}的身上，用力夹紧，不停抽动着。`); // :2450
        // CFLAG:337  = 5（变量语义：CFLAG 族，337） // :2451
        era.set(`cflag:${target}:337`, 5); // :2451

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:337`) <= 3 || era.get('flag:7') == 2)) { // :2453
        await era.printAndWait(`「再用力地夹紧会更好些吗……啊～要融化了～」`); // :2454
        await era.printAndWait(`尻穴持续地侍奉着阴茎，${target_name}有节奏地起伏着身体。`); // :2455
        // CFLAG:337  = 4（变量语义：CFLAG 族，337） // :2456
        era.set(`cflag:${target}:337`, 4); // :2456

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 2 || era.get('flag:7') == 2)) { // :2458
        await era.printAndWait(`「这么热……这么硬～……♪」`); // :2459
        await era.printAndWait(`尻穴持续地侍奉着阴茎，${target_name}扭动着腰肢，追求着快感。`); // :2460
        // CFLAG:337  = 3（变量语义：CFLAG 族，337） // :2461
        era.set(`cflag:${target}:337`, 3); // :2461

      } else if (era.get(`cflag:${target}:337`) <= 1 || era.get('flag:7') == 2) { // :2463
        await era.printAndWait(`「要我自己动……？　哼，给我记着！」`); // :2464
        // CFLAG:337  = 2（变量语义：CFLAG 族，337） // :2465
        era.set(`cflag:${target}:337`, 2); // :2465
      } // :2466
      return 0; // :2467
    } // :2468
  } // :2469




  if (era_flag.selectcom == 37) { // :2474

    if (era.get(`cflag:${target}:338`) == 0) { // :2476

      if (era.get(`abl:${target}:16`) >= 3) { // :2478
        await era.printAndWait(`「要……要舔屁股吗……我知道啦……」`); // :2479

      } else { // :2481
        await era.printAndWait(`「不要开玩笑了！这种……这种屈辱……！」`); // :2482
      } // :2483
      // CFLAG:TARGET:338  = 1（变量语义：CFLAG 族，TARGET:338） // :2484
      era.set(`cflag:${target}:338`, 1); // :2484
      return 0; // :2485

    } else { // :2487

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 4 || era.get('flag:7') == 2)) { // :2489
        await era.printAndWait(`「嘻嘻，舒服么？　舌头，要往里伸进去了哦……！」`); // :2490
        // CFLAG:338  = 5（变量语义：CFLAG 族，338） // :2491
        era.set(`cflag:${target}:338`, 5); // :2491

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 3 || era.get('flag:7') == 2)) { // :2493
        await era.print(`「会帮魔王大人漂亮地舔干净的……呵呵～……唔……唔……」`); // :2494
        // CFLAG:338  = 4（变量语义：CFLAG 族，338） // :2495
        era.set(`cflag:${target}:338`, 4); // :2495

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:338`) <= 2 || era.get('flag:7') == 2)) { // :2497
        await era.printAndWait(`「好吧……我舔……不过……还是挺……」`); // :2498
        // CFLAG:338  = 3（变量语义：CFLAG 族，338） // :2499
        era.set(`cflag:${target}:338`, 3); // :2499

      } else if (era.get(`cflag:${target}:338`) <= 1 || era.get('flag:7') == 2) { // :2501
        await era.printAndWait(`「可恶……这种事……不做也没关系吧……」`); // :2502
        // CFLAG:338  = 2（变量语义：CFLAG 族，338） // :2503
        era.set(`cflag:${target}:338`, 2); // :2503
      } // :2504
      return 0; // :2505
    } // :2506
  } // :2507




  if (era_flag.selectcom == 40) { // :2512

    if (era.get(`cflag:${target}:341`) == 0) { // :2514
      await era.printAndWait(`「住，住手！　不要打我！　好痛啊～」`); // :2515
      // CFLAG:TARGET:341  = 1（变量语义：CFLAG 族，TARGET:341） // :2516
      era.set(`cflag:${target}:341`, 1); // :2516
      return 0; // :2517

    } else { // :2519

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:341`) <= 4 || era.get('flag:7') == 2)) { // :2521
        await era.printAndWait(`「啊～！啊～！我是你的M奴隶！！　再，再更用力地打我吧！主人啊～♪」`); // :2522
        await era.printAndWait(`${target_name}流着口水，屁股不安分地扭来扭去。`); // :2523
        // CFLAG:TARGET:341  = 5（变量语义：CFLAG 族，TARGET:341） // :2524
        era.set(`cflag:${target}:341`, 5); // :2524

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:341`) <= 3 || era.get('flag:7') == 2)) { // :2526
        await era.printAndWait(`「惩罚我！欺负我！　狠狠地责备，这么下流的我吧！……魔王大人啊～♪」`); // :2527
        await era.printAndWait(`${target_name}满脸红晕，屁股不安分地扭来扭去。`); // :2528
        // CFLAG:TARGET:341  = 4（变量语义：CFLAG 族，TARGET:341） // :2529
        era.set(`cflag:${target}:341`, 4); // :2529
        return 0; // :2530

      } else if (era.get(`mark:${target}:0`) == 3 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:341`) <= 2 || era.get('flag:7') == 2)) { // :2532
        await era.printAndWait(`「这……这种程度的话……也不是不能接受……」`); // :2533
        // CFLAG:TARGET:341  = 3（变量语义：CFLAG 族，TARGET:341） // :2534
        era.set(`cflag:${target}:341`, 3); // :2534
        return 0; // :2535

      } else if (era.get(`cflag:${target}:341`) <= 1 && era.get('flag:7') == 2) { // :2537
        await era.printAndWait(`「再怎么打我都不会屈服的！！　你这白痴！」`); // :2538
        // CFLAG:TARGET:341  = 2（变量语义：CFLAG 族，TARGET:341） // :2539
        era.set(`cflag:${target}:341`, 2); // :2539
      } // :2540
      return 0; // :2541
    } // :2542
  } // :2543




  if (era_flag.selectcom == 41) { // :2548

    if (era.get(`cflag:${target}:342`) == 0) { // :2550

      if (era.get(`talent:${target}:76`) == 1) { // :2552
        await era.printAndWait(`「哦呵～要用这条鞭子来打我吗？～♪　来嘛～！」`); // :2553

      } else if (era.get(`talent:${target}:85`) == 1) { // :2555
        await era.printAndWait(`「人家做错了什么吗……？　如果有……人家道歉还不行吗…………」`); // :2556

      } else { // :2558
        await era.printAndWait(`「混蛋，别以为我会屈服于暴力！……」`); // :2559
      } // :2560
      // CFLAG:TARGET:342  = 1（变量语义：CFLAG 族，TARGET:342） // :2561
      era.set(`cflag:${target}:342`, 1); // :2561
      return 0; // :2562

    } else { // :2564

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:342`) <= 8 || era.get('flag:7') == 2)) { // :2566
        await era.printAndWait(`「好痛！好爽！～打我！打死我～！！」`); // :2567
        // CFLAG:TARGET:342  = 9（变量语义：CFLAG 族，TARGET:342） // :2568
        era.set(`cflag:${target}:342`, 9); // :2568

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 7 || era.get('flag:7') == 2)) { // :2570
        await era.printAndWait(`「再打我！～啊～好痛！！鞭我的背！鞭我的屁股！」`); // :2571
        // CFLAG:TARGET:342  = 8（变量语义：CFLAG 族，TARGET:342） // :2572
        era.set(`cflag:${target}:342`, 8); // :2572

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:342`) <= 6 || era.get('flag:7') == 2)) { // :2574
        await era.printAndWait(`「啊～这真的，非常……痛啊……」`); // :2575
        // CFLAG:TARGET:342  = 7（变量语义：CFLAG 族，TARGET:342） // :2576
        era.set(`cflag:${target}:342`, 7); // :2576

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:342`) <= 5 || era.get('flag:7') == 2)) { // :2578
        await era.printAndWait(`「啊！啊！！好痛！…………但，这就是魔王大人的爱！啊！～……再打我！在我身上留下鞭痕！让我无时无刻都感受到魔王大人！」`); // :2579
        // CFLAG:TARGET:342  = 6（变量语义：CFLAG 族，TARGET:342） // :2580
        era.set(`cflag:${target}:342`, 6); // :2580

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 4 || era.get('flag:7') == 2)) { // :2582
        await era.printAndWait(`「唔……哦！！！……我……真是不可救药的M奴隶啊……明明这么痛……却渐渐有感觉了…………」`); // :2583
        // CFLAG:TARGET:342  = 5（变量语义：CFLAG 族，TARGET:342） // :2584
        era.set(`cflag:${target}:342`, 5); // :2584

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:342`) <= 3 || era.get('flag:7') == 2)) { // :2586
        await era.printAndWait(`「啊！！……为了魔王大人，我……会忍耐……」`); // :2587
        // CFLAG:TARGET:342  = 4（变量语义：CFLAG 族，TARGET:342） // :2588
        era.set(`cflag:${target}:342`, 4); // :2588

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 2 || era.get('flag:7') == 2)) { // :2590
        await era.printAndWait(`「啊！！啊！！……被这么虐待……我……我居然…………」`); // :2591
        // CFLAG:TARGET:342  = 3（变量语义：CFLAG 族，TARGET:342） // :2592
        era.set(`cflag:${target}:342`, 3); // :2592

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :2594
        await era.printAndWait(`「你这家伙！打算用那鞭子打我吗……？　唔！啊！！！！……一点…………都不……痛…………」`); // :2595
        // CFLAG:TARGET:342  = 2（变量语义：CFLAG 族，TARGET:342） // :2596
        era.set(`cflag:${target}:342`, 2); // :2596
      } // :2597
      return 0; // :2598
    } // :2599
  } // :2600




  if (era_flag.selectcom == 42) { // :2605

    if (era.get(`cflag:${target}:343`) == 0) { // :2607

      if (era.get(`talent:${target}:76`) == 1) { // :2609
        await era.printAndWait(`「嘻嘻～原来你喜欢刺猬啊～…………」`); // :2610

      } else if (era.get(`talent:${target}:85`) == 1) { // :2612
        await era.printAndWait(`「呜呜……这都是为了魔王大人……这都是为了魔王大人………………」`); // :2613

      } else { // :2615
        await era.printAndWait(`「我！跟你没完！！！」`); // :2616
      } // :2617
      // CFLAG:TARGET:343  = 1（变量语义：CFLAG 族，TARGET:343） // :2618
      era.set(`cflag:${target}:343`, 1); // :2618
      return 0; // :2619

    } else { // :2621

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:343`) <= 8 || era.get('flag:7') == 2)) { // :2623
        await era.printAndWait(`「啊～这令人上瘾的刺痛……再扎我！！扎深一点！！」`); // :2624
        // CFLAG:TARGET:343  = 9（变量语义：CFLAG 族，TARGET:343） // :2625
        era.set(`cflag:${target}:343`, 9); // :2625

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 7 || era.get('flag:7') == 2)) { // :2627
        await era.printAndWait(`「痛……！！身上的针越来越多……好像肉体都被改造了似得……」`); // :2628
        // CFLAG:TARGET:343  = 8（变量语义：CFLAG 族，TARGET:343） // :2629
        era.set(`cflag:${target}:343`, 8); // :2629

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:343`) <= 6 || era.get('flag:7') == 2)) { // :2631
        await era.printAndWait(`「来吧！带我走向新世界～……」`); // :2632
        // CFLAG:TARGET:343  = 7（变量语义：CFLAG 族，TARGET:343） // :2633
        era.set(`cflag:${target}:343`, 7); // :2633

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:343`) <= 5 || era.get('flag:7') == 2)) { // :2635
        await era.printAndWait(`「好痛！！好爽！！再用力刺我！让我只能想着魔王大人！！……我是魔王大人最忠实的性奴！～彻底地将我玩坏吧！」`); // :2636
        // CFLAG:TARGET:343  = 6（变量语义：CFLAG 族，TARGET:343） // :2637
        era.set(`cflag:${target}:343`, 6); // :2637

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 4 || era.get('flag:7') == 2)) { // :2639
        await era.printAndWait(`「这痛楚！！就像魔王大人扎到了我的心里一样……魔王大人哦～人家平常能用这针痕，想着你自慰么？」`); // :2640
        // CFLAG:TARGET:343  = 5（变量语义：CFLAG 族，TARGET:343） // :2641
        era.set(`cflag:${target}:343`, 5); // :2641

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:343`) <= 3 || era.get('flag:7') == 2)) { // :2643
        await era.printAndWait(`「好痛！这是……魔王大人的恩赐……这是魔王大人的恩赐…………」`); // :2644
        // CFLAG:TARGET:343  = 4（变量语义：CFLAG 族，TARGET:343） // :2645
        era.set(`cflag:${target}:343`, 4); // :2645

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 2 || era.get('flag:7') == 2)) { // :2647
        await era.printAndWait(`「明明这么痛……心里却在欢迎……天啊……我……没救了啊…………」`); // :2648
        // CFLAG:TARGET:343  = 3（变量语义：CFLAG 族，TARGET:343） // :2649
        era.set(`cflag:${target}:343`, 3); // :2649

      } else if (era.get(`cflag:${target}:343`) <= 1 || era.get('flag:7') == 2) { // :2651
        await era.printAndWait(`「你！！你会遭报应的！！」`); // :2652
        // CFLAG:TARGET:343  = 2（变量语义：CFLAG 族，TARGET:343） // :2653
        era.set(`cflag:${target}:343`, 2); // :2653
      } // :2654
      return 0; // :2655
    } // :2656
  } // :2657





  if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`)) { // :2663

    if (era.get(`cflag:${target}:344`) == 0) { // :2665

      if (era.get(`talent:${target}:76`) == 1) { // :2667
        await era.printAndWait(`「原来如此，封闭视觉来提高其它感官的感觉吗？……有意思～」`); // :2668

      } else if (era.get(`talent:${target}:85`) == 1) { // :2670
        await era.printAndWait(`「偶尔试试这种玩法也不错呢～」`); // :2671

      } else { // :2673
        await era.printAndWait(`「看不见什么的……我……一点都不怕！」`); // :2674
      } // :2675
      // CFLAG:TARGET:344  = 1（变量语义：CFLAG 族，TARGET:344） // :2676
      era.set(`cflag:${target}:344`, 1); // :2676
      return 0; // :2677

    } else { // :2679

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 8 || era.get('flag:7') == 2)) { // :2681
        await era.printAndWait(`「喔～噢～！……全身……都变成敏感带了…………♪」`); // :2682
        // CFLAG:TARGET:344  = 9（变量语义：CFLAG 族，TARGET:344） // :2683
        era.set(`cflag:${target}:344`, 9); // :2683

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 7 || era.get('flag:7') == 2)) { // :2685
        await era.printAndWait(`「皮肤……好敏感……好像能感受到空气的流动…………」`); // :2686
        // CFLAG:TARGET:344  = 8（变量语义：CFLAG 族，TARGET:344） // :2687
        era.set(`cflag:${target}:344`, 8); // :2687

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:344`) <= 6 || era.get('flag:7') == 2)) { // :2689
        await era.printAndWait(`「呵呵～想干什么坏事～」`); // :2690
        // CFLAG:TARGET:344  = 7（变量语义：CFLAG 族，TARGET:344） // :2691
        era.set(`cflag:${target}:344`, 7); // :2691

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 5 || era.get('flag:7') == 2)) { // :2693
        await era.printAndWait(`「魔王大人，就在附近啦！……别的不说，魔王大人的气味我可是很熟悉的呢～♪　快来～快来欺负人家嘛～……」`); // :2694
        // CFLAG:TARGET:344  = 6（变量语义：CFLAG 族，TARGET:344） // :2695
        era.set(`cflag:${target}:344`, 6); // :2695

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 4 || era.get('flag:7') == 2)) { // :2697
        await era.printAndWait(`「魔……魔王大人？在哪里？　人家……已经准备好被你玩弄了…………」`); // :2698
        // CFLAG:TARGET:344  = 5（变量语义：CFLAG 族，TARGET:344） // :2699
        era.set(`cflag:${target}:344`, 5); // :2699

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:344`) <= 3 || era.get('flag:7') == 2)) { // :2701
        await era.printAndWait(`「呜呜……魔王大人……不见了……魔王大人哦？！」`); // :2702
        // CFLAG:TARGET:344  = 4（变量语义：CFLAG 族，TARGET:344） // :2703
        era.set(`cflag:${target}:344`, 4); // :2703

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 2 || era.get('flag:7') == 2)) { // :2705
        await era.printAndWait(`「皮肤好紧张……啊……有感觉了……」`); // :2706
        // CFLAG:TARGET:344  = 3（变量语义：CFLAG 族，TARGET:344） // :2707
        era.set(`cflag:${target}:344`, 3); // :2707

      } else if (era.get(`cflag:${target}:344`) <= 1 || era.get('flag:7') == 2) { // :2709
        await era.printAndWait(`「玩这种小把戏……就不敢堂堂正正的么！」`); // :2710
        // CFLAG:TARGET:344  = 2（变量语义：CFLAG 族，TARGET:344） // :2711
        era.set(`cflag:${target}:344`, 2); // :2711
      } // :2712
      return 0; // :2713
    } // :2714

  } else if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`) == 0) { // :2716

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:380`) < 3 || era.get('flag:7') == 2)) { // :2718
      await era.printAndWait(`「嘻嘻～意犹未尽～……♪」`); // :2719
      // CFLAG:380  = 3（变量语义：CFLAG 族，380） // :2720
      era.set(`cflag:${target}:380`, 3); // :2720

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:380`) < 2 || era.get('flag:7') == 2)) { // :2722
      await era.printAndWait(`「嘻嘻～发现魔王大人了～！」`); // :2723
      // CFLAG:380  = 2（变量语义：CFLAG 族，380） // :2724
      era.set(`cflag:${target}:380`, 2); // :2724

    } else if (era.get(`cflag:${target}:380`) < 1 || era.get('flag:7') == 2) { // :2726
      await era.printAndWait(`「这……这种程度……没什么大不了的……！」`); // :2727
      // CFLAG:380  = 1（变量语义：CFLAG 族，380） // :2728
      era.set(`cflag:${target}:380`, 1); // :2728
    } // :2729
    return 0; // :2730
  } // :2731





  if (era_flag.selectcom == 44 && era.get(`tequip:${target}:44`)) { // :2737

    if (era.get(`cflag:${target}:345`) == 0) { // :2739

      if (era.get(`talent:${target}:76`) == 1) { // :2741
        await era.printAndWait(`「要把我绑起来么～？～♪　嘻嘻～好期待啊！」`); // :2742

      } else if (era.get(`talent:${target}:85`) == 1) { // :2744
        await era.printAndWait(`「要把我绑起来么～？～♪　呵呵～魔王大人这癖好啊～！」`); // :2745

      } else { // :2747
        await era.printAndWait(`「这……这种绳子……没被封住力量的话我只需要一下子…………」`); // :2748
      } // :2749
      // CFLAG:TARGET:345  = 1（变量语义：CFLAG 族，TARGET:345） // :2750
      era.set(`cflag:${target}:345`, 1); // :2750
      return 0; // :2751

    } else { // :2753

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:345`) <= 8 || era.get('flag:7') == 2)) { // :2755
        await era.printAndWait(`「我……我是一只受虐待就会发情的母猪……！　请……请随意地蹂躏我吧！！……」`); // :2756
        // CFLAG:TARGET:345  = 9（变量语义：CFLAG 族，TARGET:345） // :2757
        era.set(`cflag:${target}:345`, 9); // :2757

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 7 || era.get('flag:7') == 2)) { // :2759
        await era.printAndWait(`「好兴奋啊……啊……被这么绑着……子宫都开始发烫了……」`); // :2760
        // CFLAG:TARGET:345  = 8（变量语义：CFLAG 族，TARGET:345） // :2761
        era.set(`cflag:${target}:345`, 8); // :2761

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:345`) <= 6 || era.get('flag:7') == 2)) { // :2763
        await era.printAndWait(`「偶尔这么玩也不错呢～」`); // :2764
        // CFLAG:TARGET:345  = 7（变量语义：CFLAG 族，TARGET:345） // :2765
        era.set(`cflag:${target}:345`, 7); // :2765

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:345`) <= 5 || era.get('flag:7') == 2)) { // :2767
        await era.printAndWait(`「宠物……我是魔王大人的宠物……请主人再继续调教我这受虐狂家畜吧！！！……」`); // :2768
        // CFLAG:TARGET:345  = 6（变量语义：CFLAG 族，TARGET:345） // :2769
        era.set(`cflag:${target}:345`, 6); // :2769

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 4 || era.get('flag:7') == 2)) { // :2771
        await era.printAndWait(`「啊～好棒……绳子……深深地勒进肉里了…………」`); // :2772
        // CFLAG:TARGET:345  = 5（变量语义：CFLAG 族，TARGET:345） // :2773
        era.set(`cflag:${target}:345`, 5); // :2773

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:345`) <= 3 || era.get('flag:7') == 2)) { // :2775
        await era.printAndWait(`「嘻嘻～不绑着我也不会离开魔王大人的啦～～」`); // :2776
        // CFLAG:TARGET:345  = 4（变量语义：CFLAG 族，TARGET:345） // :2777
        era.set(`cflag:${target}:345`, 4); // :2777

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 2 || era.get('flag:7') == 2)) { // :2779
        await era.printAndWait(`「唔……呃……啊～…………这……这种……屈辱………………居……然……有……感觉了…………」`); // :2780
        // CFLAG:TARGET:345  = 3（变量语义：CFLAG 族，TARGET:345） // :2781
        era.set(`cflag:${target}:345`, 3); // :2781

      } else if (era.get(`cflag:${target}:345`) <= 1 || era.get('flag:7') == 2) { // :2783
        await era.printAndWait(`「我不是挣不脱！只是力量被封住了而已！！」`); // :2784
        // CFLAG:TARGET:345  = 2（变量语义：CFLAG 族，TARGET:345） // :2785
        era.set(`cflag:${target}:345`, 2); // :2785
      } // :2786
      return 0; // :2787
    } // :2788

  } else if (era_flag.selectcom == 44 && era.get(`tequip:${target}:44`) == 0) { // :2790

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:385`) < 2 || era.get('flag:7') == 2)) { // :2792
      await era.printAndWait(`「完了么？　一直绑着我也可以哦！」`); // :2793
      // CFLAG:385  = 2（变量语义：CFLAG 族，385） // :2794
      era.set(`cflag:${target}:385`, 2); // :2794

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:385`) < 2 || era.get('flag:7') == 2)) { // :2796
      await era.printAndWait(`「啊……魔王大人的绳艺……让人家腿都软了…………」`); // :2797
      // CFLAG:385  = 2（变量语义：CFLAG 族，385） // :2798
      era.set(`cflag:${target}:385`, 2); // :2798

    } else if (era.get(`cflag:${target}:385`) < 1 || era.get('flag:7') == 2) { // :2800
      await era.printAndWait(`「哼！终于放弃了吗……我才不会输给这种绳子！」`); // :2801
      // CFLAG:385  = 1（变量语义：CFLAG 族，385） // :2802
      era.set(`cflag:${target}:385`, 1); // :2802
    } // :2803
    return 0; // :2804
  } // :2805





  if (era_flag.selectcom == 45 && era.get(`tequip:${target}:45`)) { // :2811

    if (era.get(`cflag:${target}:346`) == 0) { // :2813

      if (era.get(`talent:${target}:76`) == 1) { // :2815
        await era.printAndWait(`「口水会流得到处都是啦～！……唔……唔……唔唔…………」`); // :2816

      } else if (era.get(`talent:${target}:85`) == 1) { // :2818
        await era.printAndWait(`「呜呜……戴上这个之后……魔王大人还会亲吻人家吗？……」`); // :2819

      } else { // :2821
        await era.printAndWait(`「这？！……懦夫！怕被我骂是吧！」`); // :2822
      } // :2823
      // CFLAG:TARGET:346  = 1（变量语义：CFLAG 族，TARGET:346） // :2824
      era.set(`cflag:${target}:346`, 1); // :2824
      return 0; // :2825

    } else { // :2827

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:346`) <= 8 || era.get('flag:7') == 2)) { // :2829
        await era.printAndWait(`「嘻嘻～魔王大人～真鬼畜呢～～～来！啊～～～」`); // :2830
        // CFLAG:TARGET:346  = 9（变量语义：CFLAG 族，TARGET:346） // :2831
        era.set(`cflag:${target}:346`, 9); // :2831

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 7 || era.get('flag:7') == 2)) { // :2833
        await era.printAndWait(`「明明还没戴上……口水却快要留出来了…………」`); // :2834
        // CFLAG:TARGET:346  = 8（变量语义：CFLAG 族，TARGET:346） // :2835
        era.set(`cflag:${target}:346`, 8); // :2835

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:346`) <= 6 || era.get('flag:7') == 2)) { // :2837
        await era.printAndWait(`「啊！！啊！！！啊！！！！………………没什么～待会没机会叫了～先叫上几声～哈哈～」`); // :2838
        // CFLAG:TARGET:346  = 7（变量语义：CFLAG 族，TARGET:346） // :2839
        era.set(`cflag:${target}:346`, 7); // :2839

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:346`) <= 5 || era.get('flag:7') == 2)) { // :2841
        await era.printAndWait(`「魔王大人，是我一生的主人！！我真的好爱你～好爱你啊！…………咦？……没，只是一想到待会没机会说话，就忍不住要告白一下…………」`); // :2842
        await era.printAndWait(`「魔王大人哦，人家已经是你的了，待会请狠狠地蹂躏我，占有我，让我也彻底地感受魔王大人的爱意，好吗？」`); // :2843
        // CFLAG:TARGET:346  = 6（变量语义：CFLAG 族，TARGET:346） // :2844
        era.set(`cflag:${target}:346`, 6); // :2844

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 4 || era.get('flag:7') == 2)) { // :2846
        await era.printAndWait(`「被这么弄居然觉得很幸福……魔王大人啊……你要对人家负责～我以前不是这样子的…………」`); // :2847
        // CFLAG:TARGET:346  = 5（变量语义：CFLAG 族，TARGET:346） // :2848
        era.set(`cflag:${target}:346`, 5); // :2848

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:346`) <= 3 || era.get('flag:7') == 2)) { // :2850
        await era.printAndWait(`「要弄住嘴……呜呜…………魔王大人啊……解开之后……亲亲人家可以么？」`); // :2851
        // CFLAG:TARGET:346  = 4（变量语义：CFLAG 族，TARGET:346） // :2852
        era.set(`cflag:${target}:346`, 4); // :2852

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 2 || era.get('flag:7') == 2)) { // :2854
        await era.printAndWait(`「唉……我都搞不懂我自己了……究竟是败给了你……还是败给了这些玩具……还是败给了自己的身体…………」`); // :2855
        // CFLAG:TARGET:346  = 3（变量语义：CFLAG 族，TARGET:346） // :2856
        era.set(`cflag:${target}:346`, 3); // :2856

      } else if (era.get(`cflag:${target}:346`) <= 1 || era.get('flag:7') == 2) { // :2858
        await era.printAndWait(`「我才不会张嘴让你塞！…………唔！！……咳？！！…………咯……………唔！唔唔！！………」`); // :2859
        // CFLAG:TARGET:346  = 2（变量语义：CFLAG 族，TARGET:346） // :2860
        era.set(`cflag:${target}:346`, 2); // :2860
      } // :2861
      return 0; // :2862
    } // :2863

  } else if (era_flag.selectcom == 45 && era.get(`tequip:${target}:45`) == 0) { // :2865

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:386`) < 3 || era.get('flag:7') == 2)) { // :2867
      await era.printAndWait(`「差点被自己的口水呛到～哈哈～……」`); // :2868
      // CFLAG:386  = 3（变量语义：CFLAG 族，386） // :2869
      era.set(`cflag:${target}:386`, 3); // :2869

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:386`) < 2 || era.get('flag:7') == 2)) { // :2871
      await era.printAndWait(`「口水像爱液一样地滴下来……好淫秽啊～嘻嘻～」`); // :2872
      // CFLAG:386  = 2（变量语义：CFLAG 族，386） // :2873
      era.set(`cflag:${target}:386`, 2); // :2873

    } else if (era.get(`cflag:${target}:386`) < 1 || era.get('flag:7') == 2) { // :2875
      await era.printAndWait(`「咳……咳……咳…………骂……骂……都懒得……骂你了…………」`); // :2876
      // CFLAG:386  = 1（变量语义：CFLAG 族，386） // :2877
      era.set(`cflag:${target}:386`, 1); // :2877
    } // :2878
    return 0; // :2879
  } // :2880





  if (era_flag.selectcom == 46 && era.get(`tequip:${target}:46`)) { // :2886

    if (era.get(`cflag:${target}:347`) == 0) { // :2888

      if (era.get(`talent:${target}:76`) == 1) { // :2890
        await era.printAndWait(`「呃……这个……连我都不太敢玩…………」`); // :2891

      } else if (era.get(`talent:${target}:85`) == 1) { // :2893
        await era.printAndWait(`「呜呜～魔王大人！重口味！大变态！」`); // :2894

      } else { // :2896
        await era.printAndWait(`「呜呜呜！！！哇哇哇哇！！！救命啊！！！谁都好！！来人救救我吧！！！！！」`); // :2897
      } // :2898
      // CFLAG:TARGET:347  = 1（变量语义：CFLAG 族，TARGET:347） // :2899
      era.set(`cflag:${target}:347`, 1); // :2899
      return 0; // :2900

    } else { // :2902

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 6 || era.get('flag:7') == 2)) { // :2904
        await era.printAndWait(`「啊……这迷人的触感……凉凉的……灌进来了…………待会一定很精彩吧～」`); // :2905
        // CFLAG:347  = 7（变量语义：CFLAG 族，347） // :2906
        era.set(`cflag:${target}:347`, 7); // :2906

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:347`) <= 5 || era.get('flag:7') == 2)) { // :2908
        await era.printAndWait(`「唔～唔～感觉到肚子在叫了…………」`); // :2909
        // CFLAG:347  = 6（变量语义：CFLAG 族，347） // :2910
        era.set(`cflag:${target}:347`, 6); // :2910

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 4 || era.get('flag:7') == 2)) { // :2912
        await era.printAndWait(`「噢～噢～……灌进来的……是魔王大人满满的爱……我……会用……心感受…………用……心……感……受……的～」`); // :2913
        // CFLAG:347  = 5（变量语义：CFLAG 族，347） // :2914
        era.set(`cflag:${target}:347`, 5); // :2914

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:347`) <= 3 || era.get('flag:7') == 2)) { // :2916
        await era.printAndWait(`「灌……灌好了么……？魔王大人真鬼畜啊～」`); // :2917
        // CFLAG:347  = 4（变量语义：CFLAG 族，347） // :2918
        era.set(`cflag:${target}:347`, 4); // :2918

      } else if (era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 2 || era.get('flag:7') == 2)) { // :2920
        await era.printAndWait(`「啊～啊～啊～！我要疯了～！我要疯啦！！～」`); // :2921
        // CFLAG:347  = 3（变量语义：CFLAG 族，347） // :2922
        era.set(`cflag:${target}:347`, 3); // :2922

      } else if (era.get(`cflag:${target}:347`) <= 1 || era.get('flag:7') == 2) { // :2924
        await era.printAndWait(`「唔唔～啊！！好难受～好痛苦…………」`); // :2925
        // CFLAG:347  = 2（变量语义：CFLAG 族，347） // :2926
        era.set(`cflag:${target}:347`, 2); // :2926
      } // :2927
      return 0; // :2928
    } // :2929
  } // :2930




  if (era_flag.selectcom == 55) { // :2935

    if (era.get(`cflag:${target}:356`) == 0) { // :2937

      if (era.get(`talent:${target}:85`) == 1) { // :2939
        await era.printAndWait(`「被魔王大人这么看着……好害羞…………」`); // :2940

      } else { // :2942
        await era.printAndWait(`「干……干嘛……你累啦？…………」`); // :2943
      } // :2944
      // CFLAG:356  = 1（变量语义：CFLAG 族，356） // :2945
      era.set(`cflag:${target}:356`, 1); // :2945
      return 0; // :2946

    } else { // :2948

      if (era.get(`talent:${target}:85`) == 1 && era.get(`palam:${target}:5`) >= era.get('palamlv:3') && (era.get(`cflag:${target}:356`) <= 3 || era.get('flag:7') == 2)) { // :2950
        await era.printAndWait(`「魔王大人哦，求求你～过来疼爱一下人家啊！…………」`); // :2951
        // CFLAG:356  = 4（变量语义：CFLAG 族，356） // :2952
        era.set(`cflag:${target}:356`, 4); // :2952

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:356`) <= 2 || era.get('flag:7') == 2)) { // :2954
        await era.printAndWait(`「魔……魔王大人？……人家没做错什么惹你不高兴吧？…………」`); // :2955
        // CFLAG:356  = 3（变量语义：CFLAG 族，356） // :2956
        era.set(`cflag:${target}:356`, 3); // :2956

      } else if (era.get(`cflag:${target}:356`) <= 1 || era.get('flag:7') == 2) { // :2958
        await era.printAndWait(`「这……这是一个看谁先说话的比赛？」`); // :2959
        // CFLAG:356  = 2（变量语义：CFLAG 族，356） // :2960
        era.set(`cflag:${target}:356`, 2); // :2960
      } // :2961
      return 0; // :2962
    } // :2963
  } // :2964





  if (era_flag.selectcom == 56) { // :2970

    if (era.get(`cflag:${target}:357`) == 0) { // :2972
      if (era.get(`tequip:${target}:53`)) { // :2973


        if (era.get(`talent:${target}:76`) == 1) { // :2976
          await era.printAndWait(`「呃……我叫${target_name}……曾经是个勇者。不过现在已经放弃了自己的使命，彻底地对阴茎上瘾了。」`); // :2977
          await era.printAndWait(`${target_name}一边这么说着，一边对水晶球淫靡地扭腰摆臀。`); // :2978
          await era.printAndWait(`「虽然是第一次拍这种东西，不过我会努力的！呵呵～」`); // :2979
          await era.printAndWait(`「好了！那，接下来，还要说什么？……哎～不废话了！赶紧来做爱做的事吧！……嘻嘻～」`); // :2980

        } else if (era.get(`talent:${target}:85`) == 1) { // :2982
          await era.printAndWait(`「呃……我叫${target_name}……曾经……是个…………勇者………………」`); // :2983
          await era.printAndWait(`${target_name}一边这么说着，一边时不时害羞地偷看水晶球。`); // :2984
          await era.printAndWait(`「不过，在讨伐魔王大人的途中被抓住了……被魔王大人…………教会了……作为……女人的快乐…………」`); // :2985
          await era.printAndWait(`「现在……啊…………好羞人………………能不能别拍了啊？………………」`); // :2986

        } else { // :2988
          await era.printAndWait(`「别！别拍我！！」`); // :2989
        } // :2990
      } else { // :2991

        if (era.get(`talent:${target}:76`) == 1) { // :2993
          await era.printAndWait(`「嘻嘻～……闲聊的时候，突然抓人家来爱爱……爱爱的时候，又突然抓人家来闲聊……真是顽皮的魔王大人呢～」`); // :2994

        } else if (era.get(`talent:${target}:85`) == 1) { // :2996
          await era.printAndWait(`「是……是的……能被魔王大人宠幸……我觉得非常的幸福～」`); // :2997

        } else { // :2999
          await era.printAndWait(`「……你想我说什么？…………」`); // :3000
        } // :3001
      } // :3002
      // CFLAG:357  = 1（变量语义：CFLAG 族，357） // :3003
      era.set(`cflag:${target}:357`, 1); // :3003
      return 0; // :3004

    } else { // :3006
      if (era.get(`tequip:${target}:53`)) { // :3007


        if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :3010
          await era.printAndWait(`「呃……我叫${target_name}……曾经是个勇者。不过现在已经放弃了自己的使命，彻底地对阴茎上瘾了。」`); // :3011
          await era.printAndWait(`${target_name}一边这么说着，一边对水晶球淫靡地扭腰摆臀。`); // :3012
          await era.printAndWait(`「希望看到这个的你，也能跟我一样享受性爱的快乐……哦～…啊！………轻……轻…地去了…………」`); // :3013
          await era.printAndWait(`「那，接下来，让我们一起做很多舒服的事，尽情地射精吧！……嘻嘻～」`); // :3014
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :3015
          era.set(`cflag:${target}:357`, 4); // :3015

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :3017
          await era.printAndWait(`「呃……我叫${target_name}……没能拯救这个世界，对不起呢～」`); // :3018
          await era.printAndWait(`${target_name}一边这么说着，一边对水晶球甜甜地微笑着。`); // :3019
          await era.printAndWait(`「然而，我是幸福的……因为知道了这种种让人愉悦的事……」`); // :3020
          await era.printAndWait(`「在不知不觉中，身心都被魔王大人夺走了……现在也是，遵循着魔王大人的命令来拍这个…请大家好好地看着我吧！嘻嘻～」`); // :3021
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :3022
          era.set(`cflag:${target}:357`, 3); // :3022

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :3024
          await era.printAndWait(`「我……我……叫${target_name}……呜呜…………」`); // :3025
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :3026
          era.set(`cflag:${target}:357`, 2); // :3026
        } // :3027
      } else { // :3028

        if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :3030
          await era.printAndWait(`「关于那一次……呃……呃！……是啊！……嘻嘻～……所以下次这么弄的时候，就可以再用力些嘛～呵呵～～」`); // :3031
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :3032
          era.set(`cflag:${target}:357`, 4); // :3032

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :3034
          await era.printAndWait(`「魔……魔王……大人…………你……喜……喜……喜……喜欢我……么？」`); // :3035
          await era.printAndWait(`${target_name}脸红耳赤，低眉螓首地用几不可闻的声音轻轻说到。`); // :3036
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :3037
          era.set(`cflag:${target}:357`, 3); // :3037

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :3039
          await era.printAndWait(`「既然落入你手，我还能怎样呢…………」`); // :3040
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :3041
          era.set(`cflag:${target}:357`, 2); // :3041
        } // :3042
      } // :3043
      return 0; // :3044
    } // :3045
  } // :3046




  if (era_flag.selectcom == 80) { // :3051

    if (era.get(`cflag:${target}:381`) == 0) { // :3053

      if (era.get(`talent:${target}:76`) == 1) { // :3055
        await era.printAndWait(`「咳……咳……咳…………啊～魔王大人……好孔武有力啊……唔！～……咳……咳……咳…………」`); // :3056

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3058
        await era.printAndWait(`「咳……咳……咳…………慢！慢些！吸不过来啦！……唔！～……咳……咳……咳…………」`); // :3059

      } else { // :3061
        await era.printAndWait(`「咳……咳……咳…………要……要窒息了……！……唔！～……咳……咳……咳…………」`); // :3062
      } // :3063
      // CFLAG:TARGET:381  = 1（变量语义：CFLAG 族，TARGET:381） // :3064
      era.set(`cflag:${target}:381`, 1); // :3064
      return 0; // :3065

    } else { // :3067

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:381`) <= 4 || era.get('flag:7') == 2)) { // :3069
        await era.printAndWait(`「咳……唔……唔…………呼～还可以再深些哦…………唔！～……咳……呃……呃…………」`); // :3070
        // CFLAG:381  = 5（变量语义：CFLAG 族，381） // :3071
        era.set(`cflag:${target}:381`, 5); // :3071

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:381`) <= 3 || era.get('flag:7') == 2)) { // :3073
        await era.printAndWait(`「咳……唔……唔…………人家的…噢！………还可以……往喉咙……里…面…去…………唔！～……咳……呃……呃…………」`); // :3074
        // CFLAG:381  = 4（变量语义：CFLAG 族，381） // :3075
        era.set(`cflag:${target}:381`, 4); // :3075

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:381`) <= 2 || era.get('flag:7') == 2)) { // :3077
        await era.printAndWait(`「咳……咳……咳…………唔～唔～哦！……差点被口水呛到…………唔！～……咳……咳……咳…………」`); // :3078
        // CFLAG:381  = 3（变量语义：CFLAG 族，381） // :3079
        era.set(`cflag:${target}:381`, 3); // :3079

      } else if (era.get(`cflag:${target}:381`) <= 1 || era.get('flag:7') == 2) { // :3081
        await era.printAndWait(`「咳……咳……咳…………慢！慢些！……要窒息了……！……唔！～……咳……咳……咳…………」`); // :3082
        // CFLAG:381  = 2（变量语义：CFLAG 族，381） // :3083
        era.set(`cflag:${target}:381`, 2); // :3083
      } // :3084
      return 0; // :3085
    } // :3086
  } // :3087






}

// @DOG_KOJO_4 // :3094
async function DOG_KOJO_4() {




  if (era_flag.selectcom == 0) { // :3099

    if (era.get(`cflag:${target}:301`) == 0) { // :3101

      if (era.get(`mark:${target}:2`) >= 2) { // :3103
        await era.printAndWait(`「只……只是舔一下的话………」`); // :3104

      } else { // :3106
        await era.printAndWait(`「讨，讨厌！别舔啊！」`); // :3107
      } // :3108
      // CFLAG:301  = 1（变量语义：CFLAG 族，301） // :3109
      era.set(`cflag:${target}:301`, 1); // :3109
      return 0; // :3110

    } else { // :3112

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:301`) <= 6 || era.get('flag:7') == 2)) { // :3114
        await era.printAndWait(`「啊啊…唔～哦！…好孩子…真乖！～继续舔我吧～…♪」`); // :3115
        // CFLAG:301  = 7（变量语义：CFLAG 族，301） // :3116
        era.set(`cflag:${target}:301`, 7); // :3116

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:301`) <= 5 || era.get('flag:7') == 2)) { // :3118
        await era.printAndWait(`「啊哈～♪　和狗弄，也不错～…♪」`); // :3119
        // CFLAG:301  = 6（变量语义：CFLAG 族，301） // :3120
        era.set(`cflag:${target}:301`, 6); // :3120

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:301`) <= 4 || era.get('flag:7') == 2)) { // :3122
        await era.printAndWait(`「魔王大人哦……狗是不能满足人家的啦～…」`); // :3123
        // CFLAG:301  = 5（变量语义：CFLAG 族，301） // :3124
        era.set(`cflag:${target}:301`, 5); // :3124

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:301`) <= 3 || era.get('flag:7') == 2)) { // :3126
        await era.printAndWait(`「呼～…呼～…哦～」`); // :3127
        // CFLAG:301  = 4（变量语义：CFLAG 族，301） // :3128
        era.set(`cflag:${target}:301`, 4); // :3128

      } else if (era.get(`mark:${target}:2`) == 2 && (era.get(`cflag:${target}:301`) <= 2 || era.get('flag:7') == 2)) { // :3130
        await era.printAndWait(`「满意了没？」`); // :3131
        // CFLAG:301  = 3（变量语义：CFLAG 族，301） // :3132
        era.set(`cflag:${target}:301`, 3); // :3132

      } else if (era.get(`mark:${target}:2`) <= 1 && (era.get(`cflag:${target}:301`) <= 1 || era.get('flag:7') == 2)) { // :3134
        await era.printAndWait(`「咦……？！好臭……不要过来！…」`); // :3135
        // CFLAG:301  = 2（变量语义：CFLAG 族，301） // :3136
        era.set(`cflag:${target}:301`, 2); // :3136
      } // :3137
      return 0; // :3138
    } // :3139
  } // :3140




  if (era_flag.selectcom == 1) { // :3145

    if (era.get(`cflag:${target}:302`) == 0) { // :3147

      if (era.get(`talent:${target}:0`) == 1) { // :3149
        await era.printAndWait(`「不！不要啊！！」`); // :3150

      } else { // :3152
        await era.printAndWait(`「快！快停手！！」`); // :3153
      } // :3154
      // CFLAG:302  = 1（变量语义：CFLAG 族，302） // :3155
      era.set(`cflag:${target}:302`, 1); // :3155
      return 0; // :3156

    } else { // :3158

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:302`) <= 5 || era.get('flag:7') == 2)) { // :3160
        await era.printAndWait(`「嘻嘻…好孩子～乖孩子～。继续舔哦～…啊～哦～～♪」`); // :3161
        // CFLAG:302  = 6（变量语义：CFLAG 族，302） // :3162
        era.set(`cflag:${target}:302`, 6); // :3162

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:302`) <= 4 || era.get('flag:7') == 2)) { // :3164
        await era.printAndWait(`「噢～被狗舔舐着…有感觉了！～」`); // :3165
        // CFLAG:302  = 5（变量语义：CFLAG 族，302） // :3166
        era.set(`cflag:${target}:302`, 5); // :3166

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:302`) <= 3 || era.get('flag:7') == 2)) { // :3168
        await era.printAndWait(`「魔王大人啊～总是觉得……怪怪的……没你弄的舒服！…」`); // :3169
        // CFLAG:302  = 4（变量语义：CFLAG 族，302） // :3170
        era.set(`cflag:${target}:302`, 4); // :3170

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:302`) <= 2 || era.get('flag:7') == 2)) { // :3172
        await era.printAndWait(`「呃…呃……………」`); // :3173
        // CFLAG:302  = 3（变量语义：CFLAG 族，302） // :3174
        era.set(`cflag:${target}:302`, 3); // :3174

      } else if (era.get(`cflag:${target}:302`) <= 1 || era.get('flag:7') == 2) { // :3176
        await era.printAndWait(`「呜啊！！　不、不要弄！！」`); // :3177
        // CFLAG:302  = 2（变量语义：CFLAG 族，302） // :3178
        era.set(`cflag:${target}:302`, 2); // :3178
      } // :3179
      return 0; // :3180
    } // :3181
  } // :3182





  if (era_flag.selectcom == 5) { // :3188

    if (era.get(`cflag:${target}:306`) == 0) { // :3190

      if (era.get(`talent:${target}:85`) == 1) { // :3192
        await era.printAndWait(`「啊～！…我的胸……我的胸………」`); // :3193

      } else { // :3195
        await era.printAndWait(`「饶，饶了我！　狗什么的！？」`); // :3196
      } // :3197
      // CFLAG:TARGET:306  = 1（变量语义：CFLAG 族，TARGET:306） // :3198
      era.set(`cflag:${target}:306`, 1); // :3198
      return 0; // :3199

    } else { // :3201

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:306`) <= 5 || era.get('flag:7') == 2)) { // :3203
        await era.print(`「我的胸，好吃么？　嘻嘻…♪」`); // :3204
        await era.printAndWait(`${target_name}慈爱地抱着在她胸部不断舔舐着的狗。`); // :3205
        // CFLAG:306  = 6（变量语义：CFLAG 族，306） // :3206
        era.set(`cflag:${target}:306`, 6); // :3206

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:306`) <= 4 || era.get('flag:7') == 2)) { // :3208
        await era.printAndWait(`「好舒服～…继续！继续舔我～…♪」`); // :3209
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :3210
        era.set(`cflag:${target}:306`, 5); // :3210

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:306`) <= 3 || era.get('flag:7') == 2)) { // :3212
        await era.printAndWait(`「魔王大人啊～…这感觉…好奇怪啊！」`); // :3213
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :3214
        era.set(`cflag:${target}:306`, 4); // :3214

      } else if (era.get(`abl:${target}:1`) >= 3 && (era.get(`cflag:${target}:306`) <= 2 || era.get('flag:7') == 2)) { // :3216
        await era.printAndWait(`「啊～！哦～！…才……才没有……感觉呢！！…」`); // :3217
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :3218
        era.set(`cflag:${target}:306`, 3); // :3218

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :3220
        await era.printAndWait(`「被这样的动物………」`); // :3221
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :3222
        era.set(`cflag:${target}:306`, 2); // :3222
      } // :3223
      return 0; // :3224
    } // :3225
  } // :3226




  if (era_flag.selectcom == 6) { // :3231

    if (era.get(`cflag:${target}:307`) == 0 && era.get('tflag:13')) { // :3233

      if (era.get(`talent:${target}:136`) == 1) { // :3235
        await era.printAndWait(`「呵呵～人家的初吻…被你这家伙拿到了～…♪」`); // :3236

      } else if (era.get(`talent:${target}:76`) == 1) { // :3238
        await era.printAndWait(`「初吻给了狗啊～…也不错啦！」`); // :3239

      } else if (era.get(`talent:${target}:85`) == 1) { // :3241
        await era.printAndWait(`「明……明明想留给魔王大人的………」`); // :3242

      } else { // :3244
        await era.printAndWait(`「别！别这样！　我的初吻………怎么这样………呜呜……骗人的吧……………」`); // :3245
      } // :3246
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :3247
      era.set(`cflag:${target}:307`, 1); // :3247
      return 0; // :3248

    } else if (era.get(`cflag:${target}:307`) == 0) { // :3250

      if (era.get(`talent:${target}:136`) == 1) { // :3252
        await era.printAndWait(`「呵呵～人家的初吻…被你这家伙拿到了～…♪」`); // :3253

      } else if (era.get(`talent:${target}:76`) == 1) { // :3255
        await era.printAndWait(`「初吻给了狗啊～…也不错啦！」`); // :3256

      } else if (era.get(`talent:${target}:85`) == 1) { // :3258
        await era.printAndWait(`「明……明明想留给魔王大人的………」`); // :3259

      } else { // :3261
        await era.printAndWait(`「别！别这样！　我的初吻………怎么这样………呜呜……骗人的吧……………」`); // :3262
      } // :3263
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :3264
      era.set(`cflag:${target}:307`, 1); // :3264
      return 0; // :3265

    } else { // :3267

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:307`) <= 5 || era.get('flag:7') == 2)) { // :3269
        await era.print(`${target_name}专心致志地与野狗唇舌交缠着。`); // :3270
        await era.printAndWait(`「哦～啊～狗的臭味………好好闻………」`); // :3271
        // CFLAG:307  = 6（变量语义：CFLAG 族，307） // :3272
        era.set(`cflag:${target}:307`, 6); // :3272

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:307`) <= 4 || era.get('flag:7') == 2)) { // :3274
        await era.printAndWait(`「要人家和狗亲吻什么的…魔王大人，真是个大绅士呢～」`); // :3275
        // CFLAG:307  = 5（变量语义：CFLAG 族，307） // :3276
        era.set(`cflag:${target}:307`, 5); // :3276

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:307`) <= 3 || era.get('flag:7') == 2)) { // :3278
        await era.printAndWait(`「魔王大人刚才说什么？我听不懂～听不到！…」`); // :3279
        // CFLAG:307  = 4（变量语义：CFLAG 族，307） // :3280
        era.set(`cflag:${target}:307`, 4); // :3280

      } else if (era.get(`abl:${target}:10`) >=2 && (era.get(`cflag:${target}:307`) <= 2 || era.get('flag:7') == 2)) { // :3282
        await era.printAndWait(`「好…好吧………」`); // :3283
        // CFLAG:307  = 3（变量语义：CFLAG 族，307） // :3284
        era.set(`cflag:${target}:307`, 3); // :3284

      } else if (era.get(`cflag:${target}:307`) <= 1 || era.get('flag:7') == 2) { // :3286
        await era.printAndWait(`「好讨厌啊！…好臭！…请放过我吧！…」`); // :3287
        // CFLAG:307  = 2（变量语义：CFLAG 族，307） // :3288
        era.set(`cflag:${target}:307`, 2); // :3288
      } // :3289
      return 0; // :3290
    } // :3291
  } // :3292




  if (era_flag.selectcom == 9) { // :3297

    if (era.get(`cflag:${target}:310`) == 0) { // :3299

      if (era.get(`talent:${target}:136`) == 1) { // :3301
        await era.printAndWait(`「要你来舔我的屁股…真不好意思呢～汪汪君～…」`); // :3302

      } else if (era.get(`talent:${target}:76`) == 1) { // :3304
        await era.printAndWait(`「啊～！这…………」`); // :3305

      } else if (era.get(`talent:${target}:85`) == 1) { // :3307
        await era.printAndWait(`「呃……狗什么的………」`); // :3308

      } else { // :3310
        await era.printAndWait(`「不，不要啊！！　在舔哪里啊！！」`); // :3311
      } // :3312
      // CFLAG:TARGET:310  = 1（变量语义：CFLAG 族，TARGET:310） // :3313
      era.set(`cflag:${target}:310`, 1); // :3313
      return 0; // :3314

    } else { // :3316

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:310`) <= 5 || era.get('flag:7') == 2)) { // :3318
        await era.print(`${target_name}用手扒开尻穴，接受着狗的舌头。`); // :3319
        await era.printAndWait(`「哈…哈～…好……好啊～…」`); // :3320
        // CFLAG:310  = 6（变量语义：CFLAG 族，310） // :3321
        era.set(`cflag:${target}:310`, 6); // :3321

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:310`) <= 4 || era.get('flag:7') == 2)) { // :3323
        await era.printAndWait(`「被狗这么舔…好奇怪啊…………」`); // :3324
        // CFLAG:310  = 5（变量语义：CFLAG 族，310） // :3325
        era.set(`cflag:${target}:310`, 5); // :3325

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:310`) <= 3 || era.get('flag:7') == 2)) { // :3327
        await era.printAndWait(`「这到底怎么回事…这样的………」`); // :3328
        // CFLAG:310  = 4（变量语义：CFLAG 族，310） // :3329
        era.set(`cflag:${target}:310`, 4); // :3329

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:310`) <= 2 || era.get('flag:7') == 2)) { // :3331
        await era.printAndWait(`「好……好…吧……」`); // :3332
        // CFLAG:310  = 3（变量语义：CFLAG 族，310） // :3333
        era.set(`cflag:${target}:310`, 3); // :3333

      } else if (era.get(`cflag:${target}:310`) <= 1 || era.get('flag:7') == 2) { // :3335
        await era.printAndWait(`「啊！！　那种地方…不要舔啊～！」`); // :3336
        // CFLAG:310  = 2（变量语义：CFLAG 族，310） // :3337
        era.set(`cflag:${target}:310`, 2); // :3337
      } // :3338
      return 0; // :3339
    } // :3340
  } // :3341




  if (era_flag.selectcom == 21) { // :3346

    if (era.get(`cflag:${target}:322`) == 0) { // :3348

      if (era.get(`talent:${target}:0`) == 1) { // :3350

        if (era.get(`talent:${target}:136`) == 1) { // :3352
          await era.print(`${target_name}完全成为一只牝犬了，发情地摇动着屁股引诱着狗。`); // :3353
          await era.print(`「我把处女奉献给你！　我的逼，是狗大人专用的东西！请在我体内打种吧！！」`); // :3354
          await era.printAndWait(`${target_name}这么高叫着，因为交配的喜悦全身颤抖，流出口水了。`); // :3355

        } else if (era.get(`talent:${target}:76`) == 1) { // :3357
          await era.printAndWait(`「哎呀呀～第一次是和狗呢～」`); // :3358

        } else if (era.get(`talent:${target}:85`) == 1) { // :3360
          await era.printAndWait(`「明明想奉献给魔王大人的说………」`); // :3361


        } else { // :3364
          await era.printAndWait(`「救……命……救命啊啊啊啊啊啊啊！！」`); // :3365
        } // :3366

      } else { // :3368

        if (era.get(`talent:${target}:136`) == 1) { // :3370
          await era.print(`${target_name}完全成为一只牝犬了，发情地摇动着屁股引诱着狗。`); // :3371
          await era.print(`「我的逼，是狗大人专用的东西！狠狠地操我！请在我体内打种吧！！」`); // :3372
          await era.printAndWait(`${target_name}这么高叫着，因为交配的喜悦全身颤抖，流出口水了。`); // :3373

        } else if (era.get(`talent:${target}:76`) == 1) { // :3375
          await era.printAndWait(`「啊？要和狗哦？…呃……虽然也不错啦…………」`); // :3376

        } else if (era.get(`talent:${target}:85`) == 1) { // :3378
          await era.printAndWait(`「呜呜……魔王大人…为什么就不喜欢人家呢………」`); // :3379

        } else { // :3381
          await era.printAndWait(`「救……命……救命啊啊啊啊啊啊啊！！」`); // :3382
        } // :3383
      } // :3384
      // CFLAG:322  = 1（变量语义：CFLAG 族，322） // :3385
      era.set(`cflag:${target}:322`, 1); // :3385
      return 0; // :3386

    } else { // :3388

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:322`) <= 6 || era.get('flag:7') == 2)) { // :3390
        if (rand_n(3) == 0) { // :3391
          await era.print(`${target_name}用狗的姿势，接受着来自后方的征讨。`); // :3392
          await era.print(`「交……交配着！　我和狗交配着！　我只是一只母狗！！　一只下贱的母狗！！啊～～」`); // :3393
          await era.printAndWait(`两只走兽相互交缠着，沉醉在肉欲中了。`); // :3394
        } else if (rand_n(2) == 0) { // :3395
          await era.print(`${target_name}沉浸在交配的愉悦中，放声大叫着。`); // :3396
          await era.print(`「啊啊啊～！　好棒～！　操我！！　噢噢噢！！　再来！再来！！」`); // :3397
          await era.printAndWait(`${target_name}抛弃了作为人的身份，完全成为一只发情的母兽了。`); // :3398
        } else { // :3399
          await era.print(`${target_name}兴奋地摇动着屁股引诱着狗。`); // :3400
          await era.print(`「来嘛…这里是狗大人的专用肉穴哦！……来这里播种吧～…♪」`); // :3401
          await era.printAndWait(`那副发情野兽一样的表情，已经完全看不到当初冷静的样子了。`); // :3402
        } // :3403
        // CFLAG:322  = 7（变量语义：CFLAG 族，322） // :3404
        era.set(`cflag:${target}:322`, 7); // :3404

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:322`) <= 5 || era.get('flag:7') == 2)) { // :3406
        await era.printAndWait(`「和狗做爱的经验也是必须的呢～…」`); // :3407
        // CFLAG:322  = 6（变量语义：CFLAG 族，322） // :3408
        era.set(`cflag:${target}:322`, 6); // :3408

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:322`) <= 4 || era.get('flag:7') == 2)) { // :3410
        await era.printAndWait(`「如果是魔王大人的命令的话…………和狗也…………」`); // :3411
        // CFLAG:322  = 5（变量语义：CFLAG 族，322） // :3412
        era.set(`cflag:${target}:322`, 5); // :3412

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:322`) <= 3 || era.get('flag:7') == 2)) { // :3414
        await era.printAndWait(`「难以置信…我……居然和狗在交配………」`); // :3415
        // CFLAG:322  = 4（变量语义：CFLAG 族，322） // :3416
        era.set(`cflag:${target}:322`, 4); // :3416

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:322`) <= 2 || era.get('flag:7') == 2)) { // :3418
        await era.printAndWait(`「呜呜………要和狗……………」`); // :3419
        // CFLAG:322  = 3（变量语义：CFLAG 族，322） // :3420
        era.set(`cflag:${target}:322`, 3); // :3420

      } else if (era.get(`cflag:${target}:322`) <= 1 || era.get('flag:7') == 2) { // :3422
        await era.printAndWait(`「讨厌！…这种事………太过分了！…」`); // :3423

        // CFLAG:322  = 2（变量语义：CFLAG 族，322） // :3425
        era.set(`cflag:${target}:322`, 2); // :3425
      } // :3426
      return 0; // :3427
    } // :3428
  } // :3429




  if (era_flag.selectcom == 27) { // :3434

    if (era.get(`cflag:${target}:328`) == 0) { // :3436

      if (era.get(`talent:${target}:136`) == 1) { // :3438
        await era.print(`${target_name}完全成为一只牝犬了，发情地摇动着屁股引诱着狗。`); // :3439
        await era.printAndWait(`「请欺负我…欺负我后面的穴吧～♪」`); // :3440

      } else if (era.get(`talent:${target}:76`) == 1) { // :3442
        await era.printAndWait(`「带狗走后门………吗？」`); // :3443

      } else if (era.get(`talent:${target}:85`) == 1) { // :3445
        await era.printAndWait(`「呜……要和狗肛交………」`); // :3446

      } else { // :3448
        await era.printAndWait(`「什么…这……是骗我的……对吧…？」`); // :3449
      } // :3450
      // CFLAG:TARGET:328  = 1（变量语义：CFLAG 族，TARGET:328） // :3451
      era.set(`cflag:${target}:328`, 1); // :3451
      return 0; // :3452

    } else { // :3454

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 6 || era.get('flag:7') == 2)) { // :3456
        if (rand_n(2) == 0) { // :3457
          await era.print(`${target_name}感觉自己的尻穴都要融化了……`); // :3458
          await era.print(`「啊～狗大人的鸡鸡！塞满了我下贱的菊穴…♪」`); // :3459
          await era.printAndWait(`都成这个样子了，看来${target_name}是无法再回头了。`); // :3460
        } else { // :3461
          await era.print(`${target_name}兴奋地扒开自己的尻穴……`); // :3462
          await era.print(`「狗大人…喜欢的话，请尽情使用我这下烂的洞吧！…」`); // :3463
          await era.printAndWait(`向狗不断献媚着，${target_name}现在已经是畜生以下的存在了。`); // :3464
        } // :3465
        // CFLAG:328  = 7（变量语义：CFLAG 族，328） // :3466
        era.set(`cflag:${target}:328`, 7); // :3466

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 5 || era.get('flag:7') == 2)) { // :3468
        await era.printAndWait(`「和狗…也挺舒服的……」`); // :3469
        // CFLAG:328  = 6（变量语义：CFLAG 族，328） // :3470
        era.set(`cflag:${target}:328`, 6); // :3470

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 4 || era.get('flag:7') == 2)) { // :3472
        await era.printAndWait(`「如果是和魔王大人的话…就更好了………」`); // :3473
        // CFLAG:328  = 5（变量语义：CFLAG 族，328） // :3474
        era.set(`cflag:${target}:328`, 5); // :3474

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:328`) <= 3 || era.get('flag:7') == 2)) { // :3476
        await era.printAndWait(`「魔王大人…为什么啊………」`); // :3477
        // CFLAG:328  = 4（变量语义：CFLAG 族，328） // :3478
        era.set(`cflag:${target}:328`, 4); // :3478

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 2 || era.get('flag:7') == 2)) { // :3480
        await era.printAndWait(`「屁股…和狗…呜呜…………」`); // :3481
        // CFLAG:328  = 3（变量语义：CFLAG 族，328） // :3482
        era.set(`cflag:${target}:328`, 3); // :3482

      } else if (era.get(`cflag:${target}:328`) <= 1 || era.get('flag:7') == 2) { // :3484
        await era.printAndWait(`「你这疯子！！……」`); // :3485
        // CFLAG:328  = 2（变量语义：CFLAG 族，328） // :3486
        era.set(`cflag:${target}:328`, 2); // :3486
      } // :3487
      return 0; // :3488
    } // :3489
  } // :3490




  if (era_flag.selectcom == 30) { // :3495

    if (era.get(`cflag:${target}:331`) == 0) { // :3497

      if (era.get(`talent:${target}:76`) == 1) { // :3499
        await era.printAndWait(`「形状真独特！……这就是狗的鸡鸡啊！」`); // :3500

      } else if (era.get(`talent:${target}:85`) == 1) { // :3502
        await era.printAndWait(`「呜……它勃起来了……」`); // :3503

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3505
        await era.printAndWait(`「狗的臭味……满手都是……」`); // :3506

      } else { // :3508
        await era.printAndWait(`「呕…………好脏……这种东西…………」`); // :3509
      } // :3510
      // CFLAG:TARGET:331  = 1（变量语义：CFLAG 族，TARGET:331） // :3511
      era.set(`cflag:${target}:331`, 1); // :3511
      return 0; // :3512

    } else { // :3514

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 6 || era.get('flag:7') == 2)) { // :3516
        if (rand_n(2) == 0) { // :3517
          await era.printAndWait(`「舒服么？　嘻嘻！不停地脉动着呢！」`); // :3518
        } else { // :3519
          await era.printAndWait(`「野兽的味道……变浓烈的啊。来！把精液射出来吧！」`); // :3520
        } // :3521
        // CFLAG:331  = 7（变量语义：CFLAG 族，331） // :3522
        era.set(`cflag:${target}:331`, 7); // :3522

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 5 || era.get('flag:7') == 2)) { // :3524
        if (rand_n(2) == 0) { // :3525
          await era.printAndWait(`「哈哈～汪汪地叫着……真可爱！」`); // :3526
        } else { // :3527
          await era.printAndWait(`「有感觉了么？　原来狗也和人一样啊！」`); // :3528
        } // :3529
        // CFLAG:331  = 6（变量语义：CFLAG 族，331） // :3530
        era.set(`cflag:${target}:331`, 6); // :3530

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:331`) <= 4 || era.get('flag:7') == 2)) { // :3532
        if (rand_n(2) == 0) { // :3533
          await era.printAndWait(`「好厉害！野兽的气味！」`); // :3534
        } else { // :3535
          await era.printAndWait(`「这个……了不起的压迫感啊……」`); // :3536
        } // :3537
        // CFLAG:331  = 5（变量语义：CFLAG 族，331） // :3538
        era.set(`cflag:${target}:331`, 5); // :3538

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 3 || era.get('flag:7') == 2)) { // :3540
        await era.printAndWait(`「呵呵～有感觉饿了？」`); // :3541
        // CFLAG:331  = 4（变量语义：CFLAG 族，331） // :3542
        era.set(`cflag:${target}:331`, 4); // :3542

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 2 || era.get('flag:7') == 2)) { // :3544
        await era.printAndWait(`「狗的话……应该很容易搞定吧？」`); // :3545
        // CFLAG:331  = 3（变量语义：CFLAG 族，331） // :3546
        era.set(`cflag:${target}:331`, 3); // :3546

      } else if (era.get(`cflag:${target}:331`) <= 1 || era.get('flag:7') == 2) { // :3548
        await era.printAndWait(`「咦……好臭……这种……脏东西…………」`); // :3549
        // CFLAG:331  = 2（变量语义：CFLAG 族，331） // :3550
        era.set(`cflag:${target}:331`, 2); // :3550
      } // :3551
      return 0; // :3552
    } // :3553
  } // :3554




  if (era_flag.selectcom == 31) { // :3559

    if (era.get(`cflag:${target}:332`) == 0) { // :3561

      if (era.get(`talent:${target}:76`) == 1) { // :3563
        await era.printAndWait(`「原来如此，狗的小鸡鸡，是这个味道啊～♪」`); // :3564

      } else if (era.get(`talent:${target}:85`) == 1) { // :3566
        await era.printAndWait(`「人家不想舔这种东西！……但是……如果……是魔王大人的爱好……的话……」`); // :3567

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3569
        await era.printAndWait(`「知……知道了………只是吸一下哦！」`); // :3570

      } else { // :3572
        await era.printAndWait(`「讨，讨厌！　不想把这东西放嘴里！！　住！住手！！！」`); // :3573
      } // :3574
      // CFLAG:TARGET:332  = 1（变量语义：CFLAG 族，TARGET:332） // :3575
      era.set(`cflag:${target}:332`, 1); // :3575
      return 0; // :3576

    } else { // :3578

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 6 || era.get('flag:7') == 2)) { // :3580
        await era.print(`「唔～唔～滑溜溜的…………唔啊！……鸡鸡……鸡鸡……狗大人的鸡鸡……」`); // :3581
        await era.printAndWait(`${target_name}气息慌乱，癫狂地吸啜着狗的阴茎。`); // :3582
        // CFLAG:332  = 7（变量语义：CFLAG 族，332） // :3583
        era.set(`cflag:${target}:332`, 7); // :3583

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 5 || era.get('flag:7') == 2)) { // :3585
        await era.printAndWait(`「哪怕是狗！我都能用嘴搞定～♪」`); // :3586
        // CFLAG:332  = 6（变量语义：CFLAG 族，332） // :3587
        era.set(`cflag:${target}:332`, 6); // :3587

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 4 || era.get('flag:7') == 2)) { // :3589
        await era.printAndWait(`「臭臭的……不过，并不讨厌……」`); // :3590
        // CFLAG:332  = 5（变量语义：CFLAG 族，332） // :3591
        era.set(`cflag:${target}:332`, 5); // :3591

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :3593
        await era.printAndWait(`「是命令的话……不管是狗还是什么，我都会尽心地服侍好的……！」`); // :3594
        // CFLAG:332  = 4（变量语义：CFLAG 族，332） // :3595
        era.set(`cflag:${target}:332`, 4); // :3595

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:332`) <= 2 || era.get('flag:7') == 2)) { // :3597
        await era.printAndWait(`「知……知道啦……偶尔要也侍奉人以外的东西吗…………」`); // :3598
        // CFLAG:332  = 3（变量语义：CFLAG 族，332） // :3599
        era.set(`cflag:${target}:332`, 3); // :3599

      } else if (era.get(`cflag:${target}:332`) <= 1 || era.get('flag:7') == 2) { // :3601
        await era.printAndWait(`「讨！讨厌！　呕…………臭死了……这野兽！」`); // :3602
        // CFLAG:332  = 2（变量语义：CFLAG 族，332） // :3603
        era.set(`cflag:${target}:332`, 2); // :3603
      } // :3604
      return 0; // :3605
    } // :3606
  } // :3607




  if (era_flag.selectcom == 34) { // :3612

    if (era.get(`cflag:${target}:335`) == 0) { // :3614

      if (era.get(`talent:${target}:0`) == 1) { // :3616

        if (era.get(`talent:${target}:136`) == 1) { // :3618
          await era.printAndWait(''); // :3619

        } else if (era.get(`talent:${target}:76`) == 1) { // :3621
          await era.printAndWait(''); // :3622

        } else if (era.get(`talent:${target}:85`) == 1) { // :3624
          await era.printAndWait(''); // :3625

        } else { // :3627
          await era.printAndWait(''); // :3628
        } // :3629

      } else { // :3631

        if (era.get(`talent:${target}:136`) == 1) { // :3633
          await era.printAndWait(''); // :3634

        } else if (era.get(`talent:${target}:76`) == 1) { // :3636
          await era.printAndWait(''); // :3637

        } else if (era.get(`talent:${target}:85`) == 1) { // :3639
          await era.printAndWait(''); // :3640

        } else { // :3642
          await era.printAndWait(''); // :3643
        } // :3644
      } // :3645
      // CFLAG:TARGET:335  = 1（变量语义：CFLAG 族，TARGET:335） // :3646
      era.set(`cflag:${target}:335`, 1); // :3646
      return 0; // :3647

    } else { // :3649

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:335`) <= 6 || era.get('flag:7') == 2)) { // :3651
        if (rand_n(3) == 0) { // :3652
          await era.printAndWait(''); // :3653
        } else if (rand_n(2) == 0) { // :3654
          await era.printAndWait(''); // :3655
        } else { // :3656
          await era.printAndWait(''); // :3657
        } // :3658
        // CFLAG:335  = 7（变量语义：CFLAG 族，335） // :3659
        era.set(`cflag:${target}:335`, 7); // :3659

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:335`) <= 5 || era.get('flag:7') == 2)) { // :3661
        if (rand_n(4) == 0) { // :3662
          await era.printAndWait(''); // :3663
        } else if (rand_n(3) == 0) { // :3664
          await era.printAndWait(''); // :3665
        } else if (rand_n(2) == 0) { // :3666
          await era.printAndWait(''); // :3667
        } else { // :3668
          await era.printAndWait(''); // :3669
        } // :3670
        // CFLAG:335  = 6（变量语义：CFLAG 族，335） // :3671
        era.set(`cflag:${target}:335`, 6); // :3671

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:335`) <= 4 || era.get('flag:7') == 2)) { // :3673
        if (rand_n(4) == 0) { // :3674
          await era.print(''); // :3675
        } else if (rand_n(3) == 0) { // :3676
          await era.printAndWait(''); // :3677
        } else if (rand_n(2) == 0) { // :3678
          await era.printAndWait(''); // :3679
        } else { // :3680
          await era.printAndWait(''); // :3681
        } // :3682
        // CFLAG:335  = 5（变量语义：CFLAG 族，335） // :3683
        era.set(`cflag:${target}:335`, 5); // :3683

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:335`) <= 3 || era.get('flag:7') == 2)) { // :3685
        if (rand_n(4) == 0) { // :3686
          await era.printAndWait(''); // :3687
        } else if (rand_n(3) == 0) { // :3688
          await era.printAndWait(''); // :3689
        } else if (rand_n(2) == 0) { // :3690
          await era.printAndWait(''); // :3691
        } else { // :3692
          await era.printAndWait(''); // :3693
        } // :3694
        // CFLAG:335  = 4（变量语义：CFLAG 族，335） // :3695
        era.set(`cflag:${target}:335`, 4); // :3695

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:335`) <= 2 || era.get('flag:7') == 2)) { // :3697
        await era.print(''); // :3698
        await era.printAndWait(''); // :3699
        // CFLAG:335  = 3（变量语义：CFLAG 族，335） // :3700
        era.set(`cflag:${target}:335`, 3); // :3700

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :3702
        await era.printAndWait(''); // :3703
        // CFLAG:335  = 2（变量语义：CFLAG 族，335） // :3704
        era.set(`cflag:${target}:335`, 2); // :3704
      } // :3705
      return 0; // :3706
    } // :3707
  } // :3708




  if (era_flag.selectcom == 37) { // :3713

    if (era.get(`cflag:${target}:338`) == 0) { // :3715

      if (era.get(`abl:${target}:16`) >= 3) { // :3717
        await era.printAndWait(''); // :3718

      } else { // :3720
        await era.printAndWait(''); // :3721
      } // :3722
      // CFLAG:TARGET:338  = 1（变量语义：CFLAG 族，TARGET:338） // :3723
      era.set(`cflag:${target}:338`, 1); // :3723
      return 0; // :3724

    } else { // :3726

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 5 || era.get('flag:7') == 2)) { // :3728
        await era.printAndWait(''); // :3729
        // CFLAG:338  = 6（变量语义：CFLAG 族，338） // :3730
        era.set(`cflag:${target}:338`, 6); // :3730

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 4 || era.get('flag:7') == 2)) { // :3732
        await era.printAndWait(''); // :3733
        // CFLAG:338  = 5（变量语义：CFLAG 族，338） // :3734
        era.set(`cflag:${target}:338`, 5); // :3734

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 3 || era.get('flag:7') == 2)) { // :3736
        await era.print(''); // :3737
        // CFLAG:338  = 4（变量语义：CFLAG 族，338） // :3738
        era.set(`cflag:${target}:338`, 4); // :3738

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:338`) <= 2 || era.get('flag:7') == 2)) { // :3740
        await era.printAndWait(''); // :3741
        // CFLAG:338  = 3（变量语义：CFLAG 族，338） // :3742
        era.set(`cflag:${target}:338`, 3); // :3742

      } else if (era.get(`cflag:${target}:338`) <= 1 || era.get('flag:7') == 2) { // :3744
        await era.printAndWait(''); // :3745
        // CFLAG:338  = 2（变量语义：CFLAG 族，338） // :3746
        era.set(`cflag:${target}:338`, 2); // :3746
      } // :3747
      return 0; // :3748
    } // :3749
  } // :3750





  if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`)) { // :3756

    if (era.get(`cflag:${target}:344`) == 0) { // :3758

      if (era.get(`talent:${target}:136`) == 1) { // :3760
        await era.printAndWait(''); // :3761

      } else if (era.get(`talent:${target}:76`) == 1) { // :3763
        await era.printAndWait(''); // :3764

      } else if (era.get(`talent:${target}:85`) == 1) { // :3766
        await era.printAndWait(''); // :3767

      } else { // :3769
        await era.printAndWait(''); // :3770
      } // :3771
      // CFLAG:TARGET:344  = 1（变量语义：CFLAG 族，TARGET:344） // :3772
      era.set(`cflag:${target}:344`, 1); // :3772
      return 0; // :3773

    } else { // :3775

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:344`) <= 9 || era.get('flag:7') == 2)) { // :3777
        await era.printAndWait(''); // :3778
        // CFLAG:TARGET:344  = 10（变量语义：CFLAG 族，TARGET:344） // :3779
        era.set(`cflag:${target}:344`, 10); // :3779

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 8 || era.get('flag:7') == 2)) { // :3781
        await era.printAndWait(''); // :3782
        // CFLAG:TARGET:344  = 9（变量语义：CFLAG 族，TARGET:344） // :3783
        era.set(`cflag:${target}:344`, 9); // :3783

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 7 || era.get('flag:7') == 2)) { // :3785
        await era.printAndWait(''); // :3786
        // CFLAG:TARGET:344  = 8（变量语义：CFLAG 族，TARGET:344） // :3787
        era.set(`cflag:${target}:344`, 8); // :3787

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:344`) <= 6 || era.get('flag:7') == 2)) { // :3789
        await era.printAndWait(''); // :3790
        // CFLAG:TARGET:344  = 7（变量语义：CFLAG 族，TARGET:344） // :3791
        era.set(`cflag:${target}:344`, 7); // :3791

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 5 || era.get('flag:7') == 2)) { // :3793
        await era.printAndWait(''); // :3794
        // CFLAG:TARGET:344  = 6（变量语义：CFLAG 族，TARGET:344） // :3795
        era.set(`cflag:${target}:344`, 6); // :3795

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 4 || era.get('flag:7') == 2)) { // :3797
        await era.printAndWait(''); // :3798
        // CFLAG:TARGET:344  = 5（变量语义：CFLAG 族，TARGET:344） // :3799
        era.set(`cflag:${target}:344`, 5); // :3799

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:344`) <= 3 || era.get('flag:7') == 2)) { // :3801
        await era.printAndWait(''); // :3802
        // CFLAG:TARGET:344  = 4（变量语义：CFLAG 族，TARGET:344） // :3803
        era.set(`cflag:${target}:344`, 4); // :3803

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 2 || era.get('flag:7') == 2)) { // :3805
        await era.printAndWait(''); // :3806
        // CFLAG:TARGET:344  = 3（变量语义：CFLAG 族，TARGET:344） // :3807
        era.set(`cflag:${target}:344`, 3); // :3807

      } else if (era.get(`cflag:${target}:344`) <= 1 || era.get('flag:7') == 2) { // :3809
        await era.printAndWait(''); // :3810
        // CFLAG:TARGET:344  = 2（变量语义：CFLAG 族，TARGET:344） // :3811
        era.set(`cflag:${target}:344`, 2); // :3811
      } // :3812
      return 0; // :3813
    } // :3814

  } else if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`) == 0) { // :3816

    if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:380`) < 3 || era.get('flag:7') == 2)) { // :3818
      await era.printAndWait(''); // :3819
      // CFLAG:380  = 4（变量语义：CFLAG 族，380） // :3820
      era.set(`cflag:${target}:380`, 4); // :3820

    } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:380`) < 3 || era.get('flag:7') == 2)) { // :3822
      await era.printAndWait(''); // :3823
      // CFLAG:380  = 3（变量语义：CFLAG 族，380） // :3824
      era.set(`cflag:${target}:380`, 3); // :3824

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:380`) < 2 || era.get('flag:7') == 2)) { // :3826
      await era.printAndWait(''); // :3827
      // CFLAG:380  = 2（变量语义：CFLAG 族，380） // :3828
      era.set(`cflag:${target}:380`, 2); // :3828

    } else if (era.get(`cflag:${target}:380`) < 1 || era.get('flag:7') == 2) { // :3830
      await era.printAndWait(''); // :3831
      // CFLAG:380  = 1（变量语义：CFLAG 族，380） // :3832
      era.set(`cflag:${target}:380`, 1); // :3832
    } // :3833
    return 0; // :3834
  } // :3835





  if (era_flag.selectcom == 56) { // :3841

    if (era.get(`cflag:${target}:357`) == 0) { // :3843
      if (era.get(`tequip:${target}:53`)) { // :3844


        if (era.get(`talent:${target}:136`) == 1) { // :3847
          await era.printAndWait(`「呃……我叫${target_name}……原来是个勇者……但我现在已经不当勇者，改当魔王大人的家畜了。」`); // :3848
          await era.printAndWait(`${target_name}一边对水晶球这么说着，一边抱紧了旁边的狗。`); // :3849
          await era.printAndWait(`「在那以后，就一直在主人的命令下与狗大人在交配……享受着家畜的最高快乐～♪」`); // :3850
          await era.printAndWait(`「现在，就让我们围绕小鸡鸡，做许许多多舒服的事情吧！嘻嘻～」`); // :3851

        } else if (era.get(`talent:${target}:76`) == 1) { // :3853
          await era.printAndWait(`「呃……我叫${target_name}……原来是个勇者……但我现在已经不当勇者，成为魔王大人的牝奴隶了。」`); // :3854
          await era.printAndWait(`${target_name}一边这么说着，一边对水晶球淫靡地扭腰摆臀。`); // :3855
          await era.printAndWait(`「在主人的命令下，有时还要和狗交配……尽情地鄙视这样下贱的我吧！」`); // :3856
          await era.printAndWait(`「现在，就让我们围绕小鸡鸡，做许许多多舒服的事情吧！嘻嘻～」`); // :3857

        } else if (era.get(`talent:${target}:85`) == 1) { // :3859
          await era.printAndWait(`「呃……我叫${target_name}……原来是个勇者……但我现在已经不当勇者，成为魔王大人的奴隶了。」`); // :3860
          await era.printAndWait(`${target_name}一边这么说着，一边顺从地对水晶球分开双腿。`); // :3861
          await era.printAndWait(`「在主人的命令下，有时还要和狗交配……尽情地鄙视这样下贱的我吧！」`); // :3862
          await era.printAndWait(`「现在，就让我们围绕小鸡鸡，做许许多多舒服的事情吧！嘻嘻～」`); // :3863

        } else { // :3865
          await era.printAndWait(`「看到这个水晶球的人！谁都好！谁都可以！！请来救救我吧！！呜呜……呜呜呜…………哇！！！！」`); // :3866
        } // :3867
      } // :3868
      // CFLAG:357  = 1（变量语义：CFLAG 族，357） // :3869
      era.set(`cflag:${target}:357`, 1); // :3869
      return 0; // :3870

    } else { // :3872
      if (era.get(`tequip:${target}:53`)) { // :3873


        if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:357`) <= 4 || era.get('flag:7') == 2)) { // :3876
          await era.printAndWait(`「呃……我叫${target_name}……大家对我的上一部作品感觉如何呢？」`); // :3877
          await era.printAndWait(`${target_name}一边对水晶球这么说着，一边抱紧了旁边的狗。`); // :3878
          await era.printAndWait(`「这次，也是在主人的命令下要和狗大人交配了……嘻嘻～当家畜真幸福呢～♪」`); // :3879
          await era.printAndWait(`「现在，就让我们围绕小鸡鸡，做许许多多舒服的事情吧！嘻嘻～」`); // :3880
          // CFLAG:357  = 5（变量语义：CFLAG 族，357） // :3881
          era.set(`cflag:${target}:357`, 5); // :3881

        } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :3883
          await era.printAndWait(`「呃……我叫${target_name}……大家对我的上一部的兽交作品感觉如何呢？」`); // :3884
          await era.printAndWait(`${target_name}一边这么说着，一边对水晶球淫靡地扭腰摆臀。`); // :3885
          await era.printAndWait(`「这次，在主人的命令下，我又要和狗交配了……尽情地鄙视这样下贱的我吧！」`); // :3886
          await era.printAndWait(`「现在，就让我们围绕小鸡鸡，做许许多多舒服的事情吧！嘻嘻～」`); // :3887
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :3888
          era.set(`cflag:${target}:357`, 4); // :3888

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :3890
          await era.printAndWait(`「呃……我叫${target_name}……大家对我的上一次的交配感觉如何呢？」`); // :3891
          await era.printAndWait(`${target_name}一边这么说着，一边顺从地对水晶球分开双腿。`); // :3892
          await era.printAndWait(`「这次，在主人的命令下，我又要和狗交配了……尽情地鄙视这样下贱的我吧！」`); // :3893
          await era.printAndWait(`「现在，就让我们围绕小鸡鸡，做许许多多舒服的事情吧！嘻嘻～」`); // :3894
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :3895
          era.set(`cflag:${target}:357`, 3); // :3895

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :3897
          await era.printAndWait(''); // :3898
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :3899
          era.set(`cflag:${target}:357`, 2); // :3899
        } // :3900
      } // :3901
      return 0; // :3902
    } // :3903
  } // :3904


  return 0; // :3907







}

// @KOJO_MESSAGE_PALAMCNG_4 // :3915
async function KOJO_MESSAGE_PALAMCNG_4() {




  if (era.get(`tequip:${target}:45`)) { // :3921
    return 0; // :3921
  } // :3921

  if (era.get('tflag:899')) { // :3924
    return 0; // :3924
  } // :3924

  if (era.get(`tequip:${target}:55`)) { // :3926
    return 0; // :3927
  } // :3928







  // 赋值 P = PALAM:3 + UP:3 // :3936
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:221`) == 0) { // :3937

    if (era.get(`talent:${target}:85`) == 1) { // :3939

      if (era_flag.selectcom == 50) { // :3941
        await era.printAndWait(`「黏糊糊的……好想蹭到魔王大人身上！…嘻嘻～」`); // :3942
        await era.printAndWait(`「人家好感动哦～魔王大人，是不想让人家感到痛而使用润滑液的吧……魔王大人，爱你哦～！」`); // :3943

      } else { // :3945
        await era.printAndWait(`「魔王大人……看看嘛……人家的爱意，人家的爱液……都已经因你而溢出了啦～」`); // :3946
      } // :3947

    } else { // :3949

      if (era_flag.selectcom == 50) { // :3951
        await era.printAndWait(`「什么嘛！这黏糊糊的…」`); // :3952
        await era.printAndWait(`「这……是不想让我那么痛吧？……算你有点良心……」`); // :3953

      } else { // :3955
        await era.printAndWait(`「不会吧…我……我……居然这么湿了………不！不……不！！……我不是这样子的！………」`); // :3956
      } // :3957
    } // :3958
    // CFLAG:TARGET:221  = 1（变量语义：CFLAG 族，TARGET:221） // :3959
    era.set(`cflag:${target}:221`, 1); // :3959
  } // :3960




  // 赋值 P = PALAM:5 + UP:5 // :3965
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:222`) == 0) { // :3966

    if (era.get(`talent:${target}:85`) == 1) { // :3968

      if (era_flag.selectcom == 51) { // :3970
        await era.printAndWait(`「啊～五彩缤纷的！好棒～好棒啊！…」`); // :3971

      } else { // :3973
        await era.printAndWait(`「魔王大人…我已经是你的人了……请好好地疼爱人家吧………」`); // :3974
      } // :3975

    } else { // :3977

      if (era_flag.selectcom == 51) { // :3979
        await era.printAndWait(`「卑鄙！…用这种手段………」`); // :3980

      } else { // :3982
        await era.printAndWait(`「难以置信…我………居然……感到愉悦………？」`); // :3983
      } // :3984
    } // :3985
    // CFLAG:222  = 1（变量语义：CFLAG 族，222） // :3986
    era.set(`cflag:${target}:222`, 1); // :3986
  } // :3987




  // 赋值 P = PALAM:8 + UP:8 // :3992
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:223`) == 0) { // :3993

    if (era.get(`talent:${target}:85`) == 1) { // :3995
      await era.printAndWait(`「魔王大人！…你好坏！人家羞羞嘛～…」`); // :3996

    } else { // :3998
      await era.printAndWait(`「讨厌……这样…好羞耻啊………」`); // :3999
    } // :4000
    // CFLAG:223  = 1（变量语义：CFLAG 族，223） // :4001
    era.set(`cflag:${target}:223`, 1); // :4001
  } // :4002




  // 赋值 P = PALAM:10 + UP:10 // :4007
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:224`) == 0) { // :4008

    if (era.get(`talent:${target}:85`) == 1) { // :4010
      await era.printAndWait(`「魔王……大人……　有时候…你……好可怕啊………」`); // :4011

    } else { // :4013
      await era.printAndWait(`「呜……呜……我……我……知道为什么你是魔王了！！」`); // :4014
    } // :4015
    // CFLAG:224  = 1（变量语义：CFLAG 族，224） // :4016
    era.set(`cflag:${target}:224`, 1); // :4016
  } // :4017




  if (era.get('nowex:0') > 0 && era.get(`cflag:${target}:225`) == 0) { // :4022

    if (era.get(`talent:${target}:85`) == 1) { // :4024
      await era.printAndWait(`「魔……魔王……大人……要去了！！……人家要去了！！……可以去了吗？…………谢谢…魔王大人………啊～！！」`); // :4025

    } else { // :4027
      await era.printAndWait(`「来了…要来了！　体内有什么东西！　唔哦哦哦哦哦哦！！」`); // :4028
    } // :4029
    // CFLAG:225  = 1（变量语义：CFLAG 族，225） // :4030
    era.set(`cflag:${target}:225`, 1); // :4030
  } // :4031




  if (era.get('nowex:1') > 0 && era.get(`cflag:${target}:226`) == 0) { // :4036

    if (era.get(`talent:${target}:76`) == 1) { // :4038
      await era.printAndWait(`「啊啊啊！　小穴！小穴要去了！！　要对这感觉上瘾了啦！～！」`); // :4039

    } else if (era.get(`talent:${target}:85`) == 1) { // :4041
      await era.printAndWait(`「啊啊啊！　里面…有什么…这…………？！……唔……哦哦哦哦哦！…！」`); // :4042

    } else { // :4044
      await era.printAndWait(`「别！别这样！…呃……忍不住了！…唔哦哦哦哦哦哦！！」`); // :4045
    } // :4046
    // CFLAG:TARGET:226  = 1（变量语义：CFLAG 族，TARGET:226） // :4047
    era.set(`cflag:${target}:226`, 1); // :4047
  } // :4048




  if (era.get('nowex:2') > 0 && era.get(`cflag:${target}:227`) == 0) { // :4053

    if (era.get(`talent:${target}:76`) == 1) { // :4055
      await era.printAndWait(`「啊哈！　好！　好棒！　第一次……第一次用菊花高潮了！！」`); // :4056

    } else if (era.get(`talent:${target}:85`) == 1) { // :4058
      await era.printAndWait(`「难以置信……我的屁股……我的屁股………要变成性器官了！！」`); // :4059

    } else { // :4061
      await era.printAndWait(`「讨厌！讨厌！！　这个洞是错的啊！！　不想去！不要去！！…不……要……哇啊啊啊啊啊！！」`); // :4062
    } // :4063
    // CFLAG:227  = 1（变量语义：CFLAG 族，227） // :4064
    era.set(`cflag:${target}:227`, 1); // :4064
  } // :4065




  if (era.get('nowex:3') > 0 && era.get(`cflag:${target}:228`) == 0) { // :4070

    if (era.get(`talent:${target}:85`) == 1) { // :4072
      await era.printAndWait(`「胸部…好幸福………啊啊！去了！要去了啦！！！」`); // :4073

    } else { // :4075
      await era.printAndWait(`「啊！唔啊啊！！　我的胸……我的胸………呜哦哦哦哦哦！～！」`); // :4076
    } // :4077
    // CFLAG:TARGET:228  = 1（变量语义：CFLAG 族，TARGET:228） // :4078
    era.set(`cflag:${target}:228`, 1); // :4078
  } // :4079




  // 赋值 A = UP:11 + UP:12 // :4084
  if (era.get('tflag:3') == 1 && era.get(`cflag:${target}:229`) == 0) { // :4085

    if (era.get('tflag:20') == 1) { // :4087

      if (era.get(`talent:${target}:76`) == 1 && (A < 500 || era.get('tflag:150') == 1)) { // :4089
        await era.printAndWait(`「啊～终于……终于成为女人了……比想象中还要痛啊！」`); // :4090

      } else if (era.get(`talent:${target}:85`) == 1 && (A < 500 || era.get('tflag:150') == 1)) { // :4092
        await era.printAndWait(`「啊～终于……终于把自己奉献给了魔王大人……好痛……就不能轻一点吗？」`); // :4093

      } else { // :4095
        await era.printAndWait(`「这就是……做为女人的感觉…………」`); // :4096
      } // :4097

    } else { // :4099

      if (era.get(`talent:${target}:76`) == 1) { // :4101
        await era.printAndWait(`「啊～终于……从今往后，百无禁忌！！」`); // :4102

      } else if (era.get(`talent:${target}:85`) == 1) { // :4104
        await era.printAndWait(`「呜呜……明明……明明……想把处女奉献给魔王大人的嘛…………」`); // :4105

      } else { // :4107
        await era.printAndWait(`「这就是……做为女人的感觉…………」`); // :4108
      } // :4109
    } // :4110
    // CFLAG:TARGET:229  = 1（变量语义：CFLAG 族，TARGET:229） // :4111
    era.set(`cflag:${target}:229`, 1); // :4111
  } // :4112






}

// @KOJO_MESSAGE_MARKCNG_4 // :4119
async function KOJO_MESSAGE_MARKCNG_4() {




  if (era.get(`tequip:${target}:45`)) { // :4125
    return 0; // :4125
  } // :4125



  if (era.get('tflag:22') == 3 && era.get(`cflag:${target}:297`) == 0) { // :4129

    if (era.get(`talent:${target}:85`) == 1) { // :4131
      await era.printAndWait(`「感觉好痛苦……这难道……也是……爱的滋味吗？……」`); // :4132
    } else { // :4133
      await era.printAndWait(`「我……我会听话的……请不要再这么弄我了…………」`); // :4134
    } // :4135
    // CFLAG:297  = 1（变量语义：CFLAG 族，297） // :4136
    era.set(`cflag:${target}:297`, 1); // :4136
  } // :4137




  if (era.get('tflag:23') == 3 && era.get(`cflag:${target}:298`) == 0) { // :4142

    if (era.get(`talent:${target}:85`) == 1) { // :4144
      await era.printAndWait(`「有……有感觉了…………魔王大人……魔王大人哦！！……人家……人家变得好奇怪啊！！」`); // :4145
    } else { // :4146
      await era.printAndWait(`「有……有感觉了…………我……我变得好奇怪啊！！」`); // :4147
    } // :4148
    // CFLAG:298  = 1（变量语义：CFLAG 族，298） // :4149
    era.set(`cflag:${target}:298`, 1); // :4149
  } // :4150




  if (era.get('tflag:24') == 3 && era.get(`cflag:${target}:299`) == 0) { // :4155

    if (era.get(`talent:${target}:85`) == 1) { // :4157
      await era.printAndWait(`「人家会对魔王大人唯命是从的！………以后，请疼爱人家啊………」`); // :4158
    } else { // :4159
      await era.printAndWait(`「呜呜呜……我……我听话还不行吗…………」`); // :4160
    } // :4161
    // CFLAG:299  = 1（变量语义：CFLAG 族，299） // :4162
    era.set(`cflag:${target}:299`, 1); // :4162
  } // :4163




  if (era.get('tflag:21') == 3 && era.get(`cflag:${target}:300`) == 0) { // :4168

    if (era.get(`talent:${target}:85`) == 1) { // :4170
      await era.printAndWait(`「魔王大人！……我讨厌你！！！」`); // :4171
    } else { // :4172
      await era.printAndWait(`「你这家伙！……从现在开始！……不会再忍让你啦！！」`); // :4173
    } // :4174
    // CFLAG:300  = 1（变量语义：CFLAG 族，300） // :4175
    era.set(`cflag:${target}:300`, 1); // :4175
  } // :4176





}

// @SELF_KOJO_K4 // :4182
async function SELF_KOJO_K4() {



  if (era.get('tflag:13') == 1) { // :4186

    if (era.get(`talent:${target}:76`) && (era.get(`cflag:${target}:261`) < 4 || era.get('flag:7') == 2)) { // :4188
      await era.printAndWait(`「啊……我是多么下流的女人啊…这么…这么自慰……根本停不下来……」`); // :4189
      // CFLAG:261  = 4（变量语义：CFLAG 族，261） // :4190
      era.set(`cflag:${target}:261`, 4); // :4190

    } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:261`) < 3 || era.get('flag:7') == 2)) { // :4192
      await era.printAndWait(`「魔王大人………魔王大人啊…………唔……唔…………噢！！～…」`); // :4193
      // CFLAG:261  = 3（变量语义：CFLAG 族，261） // :4194
      era.set(`cflag:${target}:261`, 3); // :4194

    } else if (era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:261`) < 2 || era.get('flag:7') == 2)) { // :4196
      await era.printAndWait(`「停不下来…停不下来啦～…变得好奇怪！…」`); // :4197
      // CFLAG:261  = 2（变量语义：CFLAG 族，261） // :4198
      era.set(`cflag:${target}:261`, 2); // :4198

    } else if (era.get(`cflag:${target}:261`) < 1 || era.get('flag:7') == 2) { // :4200
      await era.printAndWait(`「唔……呃……哦！～………啊啊啊！」`); // :4201
      // CFLAG:261  = 1（变量语义：CFLAG 族，261） // :4202
      era.set(`cflag:${target}:261`, 1); // :4202
    } // :4203
  } // :4204




  if (era.get('tflag:13') == 2) { // :4209

    if (era.get(`talent:${target}:76`) && (era.get(`cflag:${target}:262`) < 5 || era.get('flag:7') == 2)) { // :4211
      await era.printAndWait(`「女人的身体…真是好东西啊…唔哦！！……再更加……再更加粗暴地对待我吧！…」`); // :4212
      // CFLAG:262  = 5（变量语义：CFLAG 族，262） // :4213
      era.set(`cflag:${target}:262`, 5); // :4213

    } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:262`) < 4 || era.get('flag:7') == 2)) { // :4215
      await era.printAndWait(`「女人的身体…真是好东西啊…嘻嘻！……再更加抱紧我吧！…」`); // :4216
      // CFLAG:262  = 4（变量语义：CFLAG 族，262） // :4217
      era.set(`cflag:${target}:262`, 4); // :4217

    } else if (era.get(`abl:${target}:33`) >= 3 && (era.get(`cflag:${target}:262`) < 3 || era.get('flag:7') == 2)) { // :4219
      await era.printAndWait(`「没有百合的话…我可能活不下去了………」`); // :4220
      // CFLAG:262  = 3（变量语义：CFLAG 族，262） // :4221
      era.set(`cflag:${target}:262`, 3); // :4221

    } else if (era.get(`abl:${target}:22`) >= 3 && (era.get(`cflag:${target}:262`) < 2 || era.get('flag:7') == 2)) { // :4223
      await era.printAndWait(`「女人哦………可是………好棒啊…………」`); // :4224
      // CFLAG:262  = 2（变量语义：CFLAG 族，262） // :4225
      era.set(`cflag:${target}:262`, 2); // :4225

    } else if (era.get(`cflag:${target}:262`) < 1 || era.get('flag:7') == 2) { // :4227
      await era.printAndWait(`「我居然…对…女人………」`); // :4228
      // CFLAG:262  = 1（变量语义：CFLAG 族，262） // :4229
      era.set(`cflag:${target}:262`, 1); // :4229
    } // :4230
  } // :4231




  if (era.get('tflag:13') == 3) { // :4236

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:263`) < 3 || era.get('flag:7') == 2)) { // :4238
      await era.printAndWait(`「我吸！唔～哦～唔～唔～～」`); // :4239
      await era.printAndWait(`「果然！主人的精华，在早上是最浓的呢…！」`); // :4240
      await era.printAndWait(`${target_name}伸出舌头，把嘴里滴落的精液舔干净了。`); // :4241
      // CFLAG:263  = 3（变量语义：CFLAG 族，263） // :4242
      era.set(`cflag:${target}:263`, 3); // :4242

    } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:263`) < 3 || era.get('flag:7') == 2)) { // :4244
      await era.printAndWait(`「嘻嘻～醒啦～？♪」`); // :4245
      await era.printAndWait(`${target_name}甜甜地笑着，用心地侍奉着你的阴茎。`); // :4246
      await era.printAndWait(`「主人的小鸡鸡～今天也很精神呢～♪　请今天也，好好地疼爱人家吧！」`); // :4247
      await era.printAndWait(`${target_name}这么说着，柔情无限地看着你。突然，往你阴茎上亲了一口。`); // :4248
      // CFLAG:263  = 3（变量语义：CFLAG 族，263） // :4249
      era.set(`cflag:${target}:263`, 3); // :4249

    } else if (era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:263`) < 2 || era.get('flag:7') == 2)) { // :4251
      await era.printAndWait(`「主人！主人！日上三竿了哦～♪」`); // :4252
      // CFLAG:263  = 2（变量语义：CFLAG 族，263） // :4253
      era.set(`cflag:${target}:263`, 2); // :4253

    } else if (era.get(`cflag:${target}:263`) < 1 || era.get('flag:7') == 2) { // :4255
      await era.printAndWait(`「早上好～！～♪」`); // :4256
      // CFLAG:263  = 1（变量语义：CFLAG 族，263） // :4257
      era.set(`cflag:${target}:263`, 1); // :4257
    } // :4258
  } // :4259




  if (era.get('tflag:13') == 4) { // :4264

    if (era.get(`abl:${target}:2`) >= 4 && (era.get(`cflag:${target}:264`) < 2 || era.get('flag:7') == 2)) { // :4266
      await era.printAndWait(`「唔～哦哦哦哦！……好棒！……好……棒……啊～！……啊啊啊啊啊！～♪」`); // :4267
      // CFLAG:264  = 2（变量语义：CFLAG 族，264） // :4268
      era.set(`cflag:${target}:264`, 2); // :4268

    } else if (era.get(`cflag:${target}:264`) < 1 || era.get('flag:7') == 2) { // :4270
      await era.printAndWait(`「再来！再来！魔王大人那热热的肉棒！再给我吧～♪」`); // :4271
      // CFLAG:264  = 1（变量语义：CFLAG 族，264） // :4272
      era.set(`cflag:${target}:264`, 1); // :4272
    } // :4273
  } // :4274




  if (era.get('tflag:13') == 5) { // :4279
    if (era.get(`cflag:${target}:265`) < 1 || era.get('flag:7') == 2) { // :4280
      await era.printAndWait(`「请……用力地抱着我吧……实在是疼得没办法了………想你想得快发疯啦！！」`); // :4281
      // CFLAG:265  = 1（变量语义：CFLAG 族，265） // :4282
      era.set(`cflag:${target}:265`, 1); // :4282
    } // :4283
  } // :4284




  if (era.get('tflag:13') == 6) { // :4289

    if (era.get(`talent:${target}:85`) && era.get(`mark:${target}:3`) < 3) { // :4291
      await era.printAndWait(''); // :4292

    } else if (era.get(`mark:${target}:3`) == 3) { // :4294
      await era.printAndWait(''); // :4295

    } else if (era.get(`talent:${target}:76`)) { // :4297
      await era.printAndWait(''); // :4298

    } else { // :4300
      await era.printAndWait(''); // :4301
    } // :4302
    await era.print(''); // :4303
    if (era.get(`talent:${target}:122`) != 1) { // :4305
      // CALL SELL_MATURO_K0 // :4305
    } // :4305
  } // :4306





  if (era.get('tflag:13') == 11) { // :4312
    if (era.get(`cflag:${target}:271`) >= 1) { // :4314
      return 0; // :4314
    } // :4314

    if (era.get(`talent:${target}:9`) == 1) { // :4316
      await era.printAndWait(''); // :4317

    } else if (era.get(`talent:${target}:85`) && era.get(`cflag:${target}:102`) == 1) { // :4319
      await era.printAndWait(''); // :4320

    } else if (era.get(`cflag:${target}:102`) == 2) { // :4322
      await era.printAndWait(''); // :4323

    } else if (era.get(`cflag:${target}:102`) == 3) { // :4325
      await era.printAndWait(''); // :4326

    } else if (era.get(`cflag:${target}:102`) == 5) { // :4328
      await era.printAndWait(''); // :4329

    } else if (era.get(`cflag:${target}:102`) == 7) { // :4331
      await era.printAndWait(''); // :4332

    } else { // :4334
      await era.printAndWait(''); // :4335
    } // :4336
    // CFLAG:271  = 1（变量语义：CFLAG 族，271） // :4337
    era.set(`cflag:${target}:271`, 1); // :4337
  } // :4338





  if (era.get('tflag:13') == 12) { // :4344
    if (era.get(`cflag:${target}:272`) >= 1) { // :4346
      return 0; // :4346
    } // :4346

    if (era.get(`talent:${target}:9`) == 1) { // :4348
      await era.printAndWait(''); // :4349

    } else if (era.get(`talent:${target}:85`) && era.get(`cflag:${target}:102`) == 1) { // :4351
      await era.printAndWait(''); // :4352

    } else if (era.get(`cflag:${target}:102`) == 2) { // :4354
      await era.printAndWait(''); // :4355

    } else if (era.get(`cflag:${target}:102`) == 3) { // :4357
      await era.printAndWait(''); // :4358

    } else if (era.get(`cflag:${target}:102`) == 5) { // :4360
      await era.printAndWait(''); // :4361

    } else if (era.get(`cflag:${target}:102`) == 7) { // :4363
      await era.printAndWait(''); // :4364

    } else { // :4366
      await era.printAndWait(''); // :4367
    } // :4368
    // CFLAG:272  = 1（变量语义：CFLAG 族，272） // :4369
    era.set(`cflag:${target}:272`, 1); // :4369
  } // :4370




  if (era.get('tflag:13') == 13) { // :4375

    if (era.get(`talent:${target}:85`) || era.get(`talent:${target}:76`)) { // :4377

      if (era.get(`talent:${target}:153`)) { // :4379
        await era.printAndWait(''); // :4380

      } else if (era.get(`talent:${target}:154`)) { // :4382
        await era.printAndWait(''); // :4383
      } // :4384
    } // :4385
    // CFLAG:273  = 1（变量语义：CFLAG 族，273） // :4386
    era.set(`cflag:${target}:273`, 1); // :4386
  } // :4387




  if (era.get('tflag:13') == 14) { // :4392

    if (era.get(`talent:${target}:85`) || era.get(`talent:${target}:76`)) { // :4394
      await era.printAndWait(''); // :4395
    } // :4396
    // CFLAG:274  = 1（变量语义：CFLAG 族，274） // :4397
    era.set(`cflag:${target}:274`, 1); // :4397
  } // :4398





  if (era.get('tflag:13') == 999) { // :4404

    if (era.get(`talent:${target}:85`)) { // :4406
      await era.printAndWait(''); // :4407

    } else { // :4409
      await era.printAndWait(''); // :4410
    } // :4411
  } // :4412




  if (era.get('tflag:13') == 998) { // :4417

    if (era.get(`talent:${target}:85`)) { // :4419
      await era.printAndWait(''); // :4420

    } else { // :4422
      await era.printAndWait(''); // :4423
    } // :4424
  } // :4425




  // TFLAG:13  = 0（变量语义：TFLAG 族，13） // :4430
  era.set('tflag:13', 0); // :4430

  return 0; // :4432






























}

// @DUNGEON_RYOUZYOKU_K4 // :4463
async function DUNGEON_RYOUZYOKU_K4() {




  if (era.get(`talent:${target}:0`) == 1) { // :4468

    await era.printAndWait(`「这种蠢事………我还是第一次………」`); // :4470

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4472


      await era.printAndWait(`「可恶！」`); // :4475

      return 0; // :4477
    } else if (era.get(`talent:${target}:17`) ==1 || era.get(`talent:${target}:31`) == 1 || era.get(`talent:${target}:36`) == 1) { // :4478


      await era.printAndWait(`「求你们！　放过我………这种事，要和喜欢的人做………」`); // :4481


      if (era.get(`talent:${target}:106`) == 1 || era.get(`exp:${target}:1`) > 0) { // :4485
        await era.printAndWait(`「用……用后面吧！！　虽然有点脏…不过我不介意的…………」`); // :4485
      } // :4485


      if (era.get(`exp:${target}:22`) > 0) { // :4489
        await era.printAndWait(`「呜……我用嘴！……我用嘴可以么？…」`); // :4489
      } // :4489

    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4491



      await era.printAndWait(`「开什么玩笑！！　我绝不承认！…绝不屈服！！」`); // :4495

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4497


      await era.printAndWait(`「这……莫非就是我的命运………」`); // :4500

    } else { // :4502


      await era.printAndWait(`「这种屈辱……我绝不屈服！！…」`); // :4505

    } // :4507
  } else { // :4508
    if (!era.get(`talent:${target}:122`)) { // :4511
      await era.printAndWait(`（她早就把处女用掉了！）`); // :4511
    } // :4511

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4513


      await era.printAndWait(`「唉………………」`); // :4516

      return 0; // :4518
    } else if (era.get(`talent:${target}:17`) ==1 || era.get(`talent:${target}:31`) == 1 || era.get(`talent:${target}:36`) == 1) { // :4519


      await era.printAndWait(`「把我救出去吧？我再好好地报答你们？」`); // :4522


      if (era.get(`talent:${target}:106`) == 1 || era.get(`exp:${target}:1`) > 0) { // :4526
        await era.printAndWait(`「用后面……用后面的话……就随你们弄………」`); // :4526
      } // :4526


      if (era.get(`exp:${target}:22`) > 0) { // :4530
        await era.printAndWait(`「其它放过我！我用嘴！我用嘴尽力地满足你们？…」`); // :4530
      } // :4530

    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4532



      await era.printAndWait(`「你们也许可以摆布我的身体！但我的内心绝不屈服！！！」`); // :4536

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4538


      await era.printAndWait(`「我……要完蛋了吗………」`); // :4541

    } else { // :4543


      await era.printAndWait(`「我不会屈服！！　绝对…」`); // :4546

    } // :4548
  } // :4549

  return 0; // :4551


}

// @DUNGEON_RYOUZYOKU_AFTER_K4 // :4554
async function DUNGEON_RYOUZYOKU_AFTER_K4() {




  if (era.get(`talent:${target}:0`) == 1) { // :4559

    await era.printAndWait(`「呼………我…的……纯洁……平安无恙………」`); // :4561

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4563


      await era.printAndWait(`「哼！」`); // :4566

      return 0; // :4568
    } // :4569


    if (era.get(`exp:${target}:1`) > 20) { // :4572
      await era.printAndWait(`「屁股……好痛苦………」`); // :4573
      await era.printAndWait(`「如此的……粗暴……………」`); // :4574
    } // :4575


    if (era.get(`exp:${target}:22`) > 20) { // :4579
      await era.printAndWait(`「这么舔……还是……第一次………」`); // :4579
    } // :4579


    if (era.get(`exp:${target}:20`) > 20) { // :4583
      await era.printAndWait(`「射了……好多………」`); // :4583
    } // :4583
  } else { // :4584

    await era.printAndWait(`「终于……结束了吗……？」`); // :4586

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4588


      await era.printAndWait(`「…………」`); // :4591

      return 0; // :4593
    } // :4594


    if (era.get(`exp:${target}:0`) > 20) { // :4597
      await era.printAndWait(`「里面……要被弄坏了啦………」`); // :4598
      await era.printAndWait(`「好过分……………」`); // :4599
    } // :4600


    if (era.get(`exp:${target}:1`) > 20) { // :4603
      await era.printAndWait(`「屁股……已经没有感觉了………」`); // :4604
      await era.printAndWait(`「真糟糕………」`); // :4605
    } // :4606


    if (era.get(`exp:${target}:22`) > 20) { // :4610
      await era.printAndWait(`「舔得我都快吐了………」`); // :4610
    } // :4610


    if (era.get(`exp:${target}:20`) > 20) { // :4614
      await era.printAndWait(`「射了……好多………」`); // :4614
    } // :4614
  } // :4615

  return 0; // :4617


}

// @BENKI_KOUJO_K4 // :4620
async function BENKI_KOUJO_K4() {




















  if (era.get('flag:62') == 0) { // :4641


    if (era.get('flag:63') == 1) { // :4644
      await era.printAndWait(`「唔嗯、了解了。作为肉便器来服务他们就可以了是吧？」`); // :4645
      await era.printAndWait(`「没问题。毕竟『常识改变成毫不怕脏』了嘛」`); // :4646
      await era.printAndWait(`「来吧、把你们这群家伙的肉棒都掏出来。会把你们榨个干净的」`); // :4647

    } else if (era.get(`talent:${a}:76`) == 1) { // :4649
      if (rand_n(2) == 0) { // :4650
        await era.print(`「呵……你们这群家伙应该感到荣幸。人称冷澈勇者的我」`); // :4651
        await era.printAndWait(`「作为肉便器来服务你们了呢」`); // :4652
        await era.printAndWait(`「怎么啦？　不这么更直接点就硬不起来了吗？」`); // :4653
        await era.printAndWait(`「呵呵、真乖……嗯、这不是挺大的嘛……♪」`); // :4654
      } else { // :4655
        await era.printAndWait(`「嘻嘻…肮脏的鸡鸡…！　让我来把它弄的漂漂亮亮吧…！」`); // :4656
      } // :4657

    } else if (era.get(`talent:${a}:85`)) { // :4659
      await era.printAndWait(`「这也是……为了那个人………」`); // :4660

    } else if (era.get(`abl:${a}:16`) >= 5) { // :4662
      await era.printAndWait(`「请让我…好好地侍奉吧…」`); // :4663

    } else { // :4665
      await era.printAndWait(`「讨厌…这种肮脏的家伙………」`); // :4666
    } // :4667
  } else if (era.get('flag:62') == 1) { // :4668


    if (era.get(`talent:${a}:76`) == 1) { // :4671
      await era.printAndWait(`「我是个男女均可的变态…！　再弄我！再弄我吧！…」`); // :4672

    } else if (era.get(`talent:${a}:85`)) { // :4674
      await era.printAndWait(`「女人的气味……好舒心……」`); // :4675

    } else if (era.get(`abl:${a}:16`) >= 5) { // :4677
      await era.printAndWait(`「好的……现在就来舔小穴………」`); // :4678

    } else { // :4680
      await era.printAndWait(`「女人什么的………」`); // :4681
    } // :4682
  } else if (era.get('flag:62') == 2) { // :4683


    if (era.get('flag:63') == 1) { // :4686
      await era.printAndWait(`「呵呵、兽奸吗……还以为是『更过分的催眠』呢、真是扫兴」`); // :4687
      await era.printAndWait(`「已经『催眠得会对野兽发情』什么的、这么做也是理所应当的吧？　呵呵、好期待啊」`); // :4688
      await era.printAndWait(`「让狗兴奋起来吧。准备要像兽爱变态女一样痴喘咯」`); // :4689

    } else if (era.get(`talent:${a}:76`) == 1) { // :4691
      await era.printAndWait(`「看着我吧！我是个喜欢和动物做爱的变态！～♪」`); // :4692

    } else if (era.get(`talent:${a}:85`)) { // :4694
      await era.printAndWait(`「唔唔～动物的臭味～♪」`); // :4695

    } else if (era.get(`abl:${a}:16`) >= 5) { // :4697
      await era.printAndWait(`「好……好的……现在去抱动物………」`); // :4698

    } else { // :4700
      await era.printAndWait(`「再……再来………」`); // :4701
    } // :4702
  } else if (era.get('flag:62') == 3) { // :4703


    if (era.get('flag:63') == 1) { // :4706
      await era.printAndWait(`「明白了。把菊穴和肉穴都掰开来做『肉便器服务』就好了是吧？」`); // :4707
      await era.printAndWait(`「我也是见过风浪的人。这种程度的『肉便器服务』怎么可能会犹豫呢」`); // :4708
      await era.printAndWait(`「来吧、小子们！　肉棒硬了的家伙就放马过来吧！」`); // :4709

    } else if (era.get(`talent:${a}:76`) == 1) { // :4711
      await era.printAndWait(`「不管前面也好，后面也好……请把我塞满吧！～♪」`); // :4712

    } else if (era.get(`talent:${a}:85`)) { // :4714
      await era.printAndWait(`「那里……和屁股………都……哦～啊啊啊啊！～♪」`); // :4715

    } else if (era.get(`abl:${a}:16`) >= 5) { // :4717
      await era.printAndWait(`「请，请用光我所有的穴吧………」`); // :4718

    } else { // :4720
      await era.printAndWait(`「双管齐下什么的………！」`); // :4721
    } // :4722
  } else if (era.get('flag:62') == 4) { // :4723


    if (era.get(`talent:${a}:76`) == 1) { // :4726
      await era.printAndWait(`「我的小穴，舒服么？随你喜欢来用哦～…♪」`); // :4727

    } else if (era.get(`talent:${a}:85`)) { // :4729
      await era.printAndWait(`「把安全套拿走吧！」`); // :4730

    } else if (era.get(`abl:${a}:16`) >= 5) { // :4732
      await era.printAndWait(`「请……随意使用我的小穴……」`); // :4733

    } else { // :4735
      await era.printAndWait(`「那里………啊！」`); // :4736
    } // :4737
  } else if (era.get('flag:62') == 5) { // :4738


    if (era.get(`talent:${a}:76`) == 1) { // :4741
      await era.printAndWait(`「啊～我是菊穴也很有感觉的尻穴奴隶！…～♪　再来！…啊啊～！啊～」`); // :4742

    } else if (era.get(`talent:${a}:85`)) { // :4744
      await era.printAndWait(`「屁股…好厉害…啊！！噢～～」`); // :4745

    } else if (era.get(`abl:${a}:16`) >= 5) { // :4747
      await era.printAndWait(`「屁，屁股………♪」`); // :4748

    } else { // :4750
      await era.printAndWait(`「啊～！……那里是………」`); // :4751
    } // :4752
  } // :4753

  return 0; // :4755


}

// @DUNGEON_VICTORY_K4 // :4758
async function DUNGEON_VICTORY_K4() {




  await era.printAndWait(`「哼…这些杂鱼～」`); // :4763

  if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4765


    await era.printAndWait(`「……」`); // :4768

    return 0; // :4770
  } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4771


    if (rand_n(3) == 0) { // :4774
      await era.printAndWait(`「别做无谓的抵抗啦…」`); // :4775
    } else if (rand_n(2) == 0) { // :4776
      await era.printAndWait(`「就这水平…」`); // :4777
    } else { // :4778
      await era.printAndWait(`「真难看啊…」`); // :4779
    } // :4780

  } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4782


    await era.printAndWait(`「呼……真惊险…哈哈！」`); // :4785

    return 0; // :4787
  } else { // :4788


    if (rand_n(3) == 0) { // :4791
      await era.printAndWait(`「好！赢了！」`); // :4792
    } else if (rand_n(2) == 0) { // :4793
      await era.printAndWait(`「我赢啦！」`); // :4794
    } else { // :4795
      await era.printAndWait(`「嗯……」`); // :4796
    } // :4797

  } // :4799

  if ((era.get(`base:${a}:0`) * 100 / era.get(`maxbase:${a}:0`) < 50) || (era.get(`base:${a}:1`) * 100 / era.get(`maxbase:${a}:1`) < 50)) { // :4801

    await era.printAndWait(`「呜…太轻敌了吗………」`); // :4803
  } else { // :4804

    await era.printAndWait(`「给魔王带话，让他带更厉害的家伙来见我！」`); // :4806
  } // :4807

  return 0; // :4809



}

// @DUNGEON_ATTACK_K4 // :4813
async function DUNGEON_ATTACK_K4() {




  if (era.get(`cflag:${target}:1`) == 2) { // :4818

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4820


      await era.printAndWait(`「……」`); // :4823

      return 0; // :4825
    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4826


      if (rand_n(3) == 0) { // :4829
        await era.printAndWait(`「渣滓！！　消失吧！」`); // :4830
      } else if (rand_n(2) == 0) { // :4831
        await era.printAndWait(`「踢飞你们！」`); // :4832
      } else { // :4833
        await era.printAndWait(`「你们的末日到啦！」`); // :4834
      } // :4835

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4837


      await era.printAndWait(`「呜……要一直战斗下去么……？」`); // :4840

      return 0; // :4842
    } else { // :4843


      if (rand_n(3) == 0) { // :4846
        await era.printAndWait(`「让我来教你们什么是战斗。」`); // :4847
      } else if (rand_n(2) == 0) { // :4848
        await era.printAndWait(`「哼……就这程度」`); // :4849
      } else { // :4850
        await era.printAndWait(`「你这家伙……死了么？」`); // :4851
      } // :4852

    } // :4854
  } else { // :4855

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4857


      await era.printAndWait(`「……」`); // :4860

      return 0; // :4862
    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4863


      if (rand_n(3) == 0) { // :4866
        await era.printAndWait(`「嘻嘻……这甘甜的力量，你也来品尝一下嘛～」`); // :4867
      } else if (rand_n(2) == 0) { // :4868
        await era.printAndWait(`「哇哈哈～新的力量……太厉害了……」`); // :4869
      } else { // :4870
        await era.printAndWait(`「教你愉悦的事……在地下室里……有许多好玩的～！」`); // :4871
      } // :4872

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4874


      await era.printAndWait(`「再……再给我力量……！」`); // :4877

      return 0; // :4879
    } else { // :4880


      if (rand_n(3) == 0) { // :4883
        await era.printAndWait(`「从魔王大人处获得的力量……就让你见识一下吧！」`); // :4884
      } else if (rand_n(2) == 0) { // :4885
        await era.printAndWait(`「过来这边吧……你也就明白了……」`); // :4886
      } else { // :4887
        await era.printAndWait(`「呵呵～可爱的家伙，不过你什么都不知道啊！」`); // :4888
      } // :4889

    } // :4891
  } // :4892



  return 0; // :4896








}

// @COLOSSEUM_KOJO_4 // :4905
async function COLOSSEUM_KOJO_4() {



  if (era_flag.selectcom == 55) { // :4909

    if (era.get(`base:${target}:1`) <= 0) { // :4911
      await era.printAndWait(`${target_name}连站都站不稳了……`); // :4912
    } else { // :4913
      await era.printAndWait(`${target_name}在死斗场的热情及对方凌厉的眼神中哆嗦着。`); // :4914
    } // :4915
    return 0; // :4916
  } // :4917



  if (era_flag.selectcom == 56) { // :4921

    if (era.get(`base:${target}:1`) <= 0) { // :4923

      if (era_flag.assi > 0 && era_flag.assiplay) { // :4925
        await era.printAndWait(`「才…才不会输给${assi_name}呢！……」`); // :4926
        await era.printAndWait(`筋疲力尽的${target_name}屁股向后跌坐在地上……`); // :4927
      } else { // :4928
        await era.printAndWait(`「啊…啊…不……不要…才不要被这种怪物侵犯！…不要！不要！……」`); // :4929
        await era.printAndWait(`筋疲力尽的${target_name}连滚带爬地企图逃离死斗场。`); // :4930
      } // :4931
    } else { // :4932

      if (era_flag.assi > 0 && era_flag.assiplay) { // :4934
        await era.printAndWait(`「难……难道……要和${assi_name}做对手么……」`); // :4935
        await era.printAndWait(`${target_name}皱着眉头，看着在${master_name}命令之下武装起来的${assi_name}……`); // :4936
      } else { // :4937
        await era.printAndWait(`「呕……这……这么恶心的怪物……………」`); // :4938
        await era.printAndWait(`${target_name}看着对面丑陋的怪物，表情都扭曲了。`); // :4939
      } // :4940
    } // :4941
    return 0; // :4942
  } // :4943




  if (era_flag.selectcom == 31) { // :4948

    if (era_flag.assi > 0 && era_flag.assiplay) { // :4950
      await era.printAndWait(`「啊…唔……唔唔………就……就在这里吗？…咳……！」`); // :4951
      await era.print(`${assi_name}把`); // :4952
      if (era.get(`talent:${assi}:121`) == 1 || era.get(`talent:${assi}:122`) == 1) { // :4954
        await era.print(`阴茎`); // :4954
      } // :4954
      if (era.get(`talent:${assi}:121`) != 1 && era.get(`talent:${assi}:122`) != 1 && era.get('item:PBAND') == 1) { // :4956
        await era.print(`假阳具`); // :4956
      } // :4956
      await era.printAndWait(`粗暴地塞入${target_name}的嘴里，露出了心满意足的神情……`); // :4957
    } else { // :4958
      await era.printAndWait(`「啊………会……会好好地舔的啦…………所以……所以……不要再做其它过分的事啦……呃……唔…………唔唔…………咳……」`); // :4959
      await era.printAndWait(`${target_name}舔啜着带着令人作呕的气味的阴茎……`); // :4960
    } // :4961
    return 0; // :4962
  } // :4963



  if (era_flag.selectcom == 5) { // :4967

    if (era_flag.assi > 0 && era_flag.assiplay) { // :4969
      await era.printAndWait(`「啊…${assi_name}啊…停……手…快停手啦………」`); // :4970
      await era.printAndWait(`${target_name}无力反抗……任由${assi_name}肆意地玩弄着她的胸部……`); // :4971
    } else { // :4972
      await era.printAndWait(`「呜………为……为什么……我要遇上这种事啊………呜呜！」`); // :4973
      await era.printAndWait(`${target_name}的胸部被粗鲁地揉捏着，发出了痛苦的呻吟……`); // :4974
    } // :4975
    return 0; // :4976
  } // :4977



  if (era_flag.selectcom == 21) { // :4981

    if (era_flag.assi > 0 && era_flag.assiplay) { // :4983
      await era.printAndWait(`「啊…！唔……啊啊啊！…好深………弄的好深啦……！」`); // :4984
      await era.print(`${assi_name}听到悲鸣，更加兴奋了，继续用`); // :4985
      if (era.get(`talent:${assi}:121`) == 1 || era.get(`talent:${assi}:122`) == 1) { // :4987
        await era.print(`阴茎`); // :4987
      } // :4987
      if (era.get(`talent:${assi}:121`) != 1 && era.get(`talent:${assi}:122`) != 1 && era.get('item:PBAND') == 1) { // :4989
        await era.print(`假阳具`); // :4989
      } // :4989
      await era.printAndWait(`毫不留情地蹂躏着${target_name}的私处……`); // :4990

    } else if (era.get('tflag:400') == 206) { // :4992
      await era.printAndWait(`「死………死………要…死掉了……」`); // :4993
      await era.printAndWait(`可怜的${target_name}断断续续地发出崩溃的声音，承受着巨魔的糟蹋。`); // :4994
    } else { // :4995
      await era.printAndWait(`「被……被这样的家伙……呜……唔……哎呀！！」`); // :4996
      await era.printAndWait(`${target_name}被怪物尽情侵犯着……`); // :4997
    } // :4998
    return 0; // :4999
  } // :5000




  if (era_flag.selectcom == 27) { // :5005

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5007
      await era.printAndWait(`「呜！啊啊啊啊！屁股……屁股…要被弄坏啦！！」」`); // :5008
      await era.print(`${assi_name}听到悲鸣，更加兴奋了，继续用`); // :5009
      if (era.get(`talent:${assi}:121`) == 1 || era.get(`talent:${assi}:122`) == 1) { // :5011
        await era.print(`阴茎`); // :5011
      } // :5011
      if (era.get(`talent:${assi}:121`) != 1 && era.get(`talent:${assi}:122`) != 1 && era.get('item:PBAND') == 1) { // :5013
        await era.print(`假阳具`); // :5013
      } // :5013
      await era.printAndWait(`毫不留情地蹂躏着${target_name}的肛门……`); // :5014

    } else if (era.get('tflag:400') == 206) { // :5016
      await era.printAndWait(`「死………死………要…死掉了……」`); // :5017
      await era.printAndWait(`可怜的${target_name}断断续续地发出崩溃的声音，承受着巨魔的糟蹋。`); // :5018
    } else { // :5019
      await era.printAndWait(`「被……被这样的家伙……呜……唔……哎呀！！屁股……屁股……要被弄坏啦！」`); // :5020
      await era.printAndWait(`${target_name}被怪物尽情地侵犯着肛门……`); // :5021
    } // :5022
    return 0; // :5023
  } // :5024




  if (era_flag.selectcom == 51) { // :5029
    await era.printAndWait(`「这……这种药………我………我………呃！！……噢噢哦噢～！」`); // :5030
    return 0; // :5031
  } // :5032


  return 0; // :5035


}

// @NTR_KOUJO_K4 // :5038
async function NTR_KOUJO_K4() {


  if (era.get(`cflag:${target}:650`) == 0) { // :5042
    // CFLAG:650  = 1（变量语义：CFLAG 族，650） // :5042
    era.set(`cflag:${target}:650`, 1); // :5042
  } // :5042


  if (P == 1) { // :5045

    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5047
      await era.printAndWait(`「停…停手…啊………我……我…是你的了………呜……！」`); // :5048
    } else { // :5049
      await era.printAndWait(`「呜………啊…………我……被你征服啦………………呜……！」`); // :5050
    } // :5051
    // CFLAG:651  = 1（变量语义：CFLAG 族，651） // :5052
    era.set(`cflag:${target}:651`, 1); // :5052

  } else if (P == 2) { // :5054
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5055
      await era.printAndWait(`「啊啊…啊…啊啊！…不……不要再来了………我的…屁股………唔……哦哦哦哦！」`); // :5056
    } else { // :5057
      await era.printAndWait(`「啊！……你这变态…不要再来了…………唔………哦哦哦哦！」`); // :5058
    } // :5059
    // CFLAG:652  = 1（变量语义：CFLAG 族，652） // :5060
    era.set(`cflag:${target}:652`, 1); // :5060

  } else if (P == 3) { // :5062
    if (era.get(`talent:${target}:136`)) { // :5063
      await era.printAndWait(`「唔哦…～啊啊！ 要坏掉了……要被狗大人玩坏掉啦！～${heart(1)}」`); // :5064
    } else if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5065
      await era.printAndWait(`「啊…啊………讨厌………被看着……被看着啦………呜呜…唔！…噢…啊啊啊啊！」`); // :5066
    } else { // :5067
      await era.printAndWait(`「可恶…这么做的话…以后给我记住！…呜…唔…啊啊！」`); // :5068
    } // :5069
    // CFLAG:653  = 1（变量语义：CFLAG 族，653） // :5070
    era.set(`cflag:${target}:653`, 1); // :5070

  } else if (P == 4) { // :5072
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5073
      await era.printAndWait(`「唔～…啊啊！…我…我……我是狂王大人…的…东西…再来…再来…再操我吧！${heart(1)}」`); // :5074
    } else { // :5075
      await era.printAndWait(`「唔～…哦！啊啊…噢！啊……！ 好、好深啊………要去了…要……去……了！！！～♪」`); // :5076
    } // :5077
    // CFLAG:654  = 1（变量语义：CFLAG 族，654） // :5078
    era.set(`cflag:${target}:654`, 1); // :5078

  } else if (P == 5) { // :5080
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5081
      await era.printAndWait(`「看啊…我的小穴也好，尻穴也好，都塞进了你们的小鸡鸡哦～${heart(1)} 嘻嘻～啊！同时被插入太舒服啦！${heart(1)}」`); // :5082
    } else { // :5083
      await era.printAndWait(`「呵呵…再…再侵犯我………把我弄得乱七八糟吧！………♪」`); // :5084
    } // :5085
    // CFLAG:655  = 1（变量语义：CFLAG 族，655） // :5086
    era.set(`cflag:${target}:655`, 1); // :5086

  } else if (P == 6) { // :5088
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5089
      await era.printAndWait(`「啊…再来…再狠狠地弄我…噢…已经……回不去那人的身边了………操我！…弄我！…把我操坏吧！～${heart(1)}」`); // :5090
    } else { // :5091
      await era.printAndWait(`「啊…啊……输掉了话………就失去一切啊………噢！……对不起……会……会用心侍奉的………啊！唔唔！」`); // :5092
    } // :5093
    // CFLAG:656  = 1（变量语义：CFLAG 族，656） // :5094
    era.set(`cflag:${target}:656`, 1); // :5094

  } else if (P == 7) { // :5096
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5097
      await era.printAndWait(`「狂王大人…啊啊…好舒服……请……请继续…使用我吧…」`); // :5098
    } else { // :5099
      await era.printAndWait(`「啊啊…会……会继续…侍奉您的………」`); // :5100
    } // :5101
    // CFLAG:657  = 1（变量语义：CFLAG 族，657） // :5102
    era.set(`cflag:${target}:657`, 1); // :5102

  } else if (P == 20) { // :5104
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5105
      if (era.get(`cflag:${target}:102`) == 1) { // :5106
        await era.printAndWait(`「还……还给我！…那……那是……我和魔王大人的孩子啊！…」`); // :5107
      } else { // :5108
        await era.printAndWait(`「是……是啊……我的子宫，是属于狂王大人的东西～…${heart(1)}」`); // :5109
      } // :5110
    } else { // :5111
      await era.printAndWait(`「啊…好想～继续怀上啊～♪」`); // :5112
    } // :5113
  } // :5114
  return 0; // :5115



}

// @EXUCUTION_KOUJO_K4 // :5119
async function EXUCUTION_KOUJO_K4() {


  if (era.get('tflag:16') == 4) { // :5122
    await era.printAndWait(`「放，放开我！我…侍奉怪物什么的………呜…呜哇哇！！」`); // :5123

  } else if (era.get('tflag:16') == 5) { // :5125
    await era.printAndWait(`「讨厌…讨厌！我变得不像我自己了…不要！……不、要、啊…啊………」`); // :5126

  } else if (era.get('tflag:16') == 6) { // :5128
    await era.printAndWait(`「混蛋！给我记住！！………」`); // :5129

  } else if (era.get('tflag:16') == 7) { // :5131
    await era.printAndWait(''); // :5132
  } // :5133


}

// @MUSEUM_KOUJO_K4 // :5136
async function MUSEUM_KOUJO_K4() {


  if (era.get('tflag:500') == 0) { // :5139
    await era.printAndWait(''); // :5140

  } else if (era.get('tflag:500') == 1) { // :5142
    await era.printAndWait(''); // :5143

  } else if (era.get('tflag:500') == 2) { // :5145
    await era.printAndWait(''); // :5146

  } else if (era.get('tflag:500') == 3) { // :5148
    await era.printAndWait(`「短时间内、维持这个姿势还可以做到…这样吗？」`); // :5149

  } else if (era.get('tflag:500') == 4) { // :5151
    await era.printAndWait(`「怎、怎么了…？身体…动、动不…了、呃…」`); // :5152

  } else if (era.get('tflag:500') == 5) { // :5154
    await era.printAndWait(''); // :5155

  } else if (era.get('tflag:500') == 6) { // :5157
    await era.printAndWait(''); // :5158

  } else if (era.get('tflag:500') == 7) { // :5160
    await era.printAndWait(''); // :5161

  } else if (era.get('tflag:500') == 8) { // :5163
    await era.printAndWait(''); // :5164

  } else if (era.get('tflag:500') == 9) { // :5166
    await era.printAndWait(''); // :5167
  } // :5168


}

// @BANISHMENT_KOUJO_K4 // :5171
async function BANISHMENT_KOUJO_K4() {



  if (era.get('tflag:510') == 0) { // :5175
    await era.printAndWait(`「我的…力量…被那样地………骗人…吧…………」`); // :5176

  } else if (era.get('tflag:510') == 1) { // :5178
    await era.printAndWait(''); // :5179

  } else if (era.get('tflag:510') == 2) { // :5181
    await era.printAndWait(''); // :5182

  } else if (era.get('tflag:510') == 3) { // :5184
    await era.printAndWait(''); // :5185

  } else if (era.get('tflag:510') == 4) { // :5187
    await era.printAndWait(''); // :5188
  } // :5189


}

// @PUBLIC_EXUCUTION_KOUJO_K4 // :5192
async function PUBLIC_EXUCUTION_KOUJO_K4() {



  if (era.get('tflag:520') == 0) { // :5196
    await era.printAndWait(`「到死为止都要被侵犯？呃……有趣…来试试呗！！」`); // :5197

  } else if (era.get('tflag:520') == 1) { // :5199
    await era.printAndWait(`「这种…罪犯似的结局…我绝不认可！…放开我！…放开我！！」`); // :5200

  } else if (era.get('tflag:520') == 2) { // :5202
    await era.printAndWait(''); // :5203
  } // :5204


}

// @GROTESQUE_KOUJO_K4 // :5207
async function GROTESQUE_KOUJO_K4() {



  if (era.get('tflag:530') == 0) { // :5211
    await era.printAndWait(''); // :5212

  } else if (era.get('tflag:530') == 1) { // :5214
    await era.printAndWait(''); // :5215

  } else if (era.get('tflag:530') == 2) { // :5217
    await era.printAndWait(''); // :5218

  } else if (era.get('tflag:530') == 3) { // :5220
    await era.printAndWait(''); // :5221

  } else if (era.get('tflag:530') == 4) { // :5223
    await era.printAndWait(''); // :5224

  } else if (era.get('tflag:530') == 5) { // :5226
    await era.printAndWait(''); // :5227

  } else if (era.get('tflag:530') == 6) { // :5229
    await era.printAndWait(''); // :5230
  } // :5231


}

// @ENTERENEMY_KOUJO_K4 // :5234
async function ENTERENEMY_KOUJO_K4() {


  if (era.get(`talent:${a}:21`) == 1 || era.get(`talent:${a}:22`) == 1) { // :5237

    await era.printAndWait(`「………呃……魔王……吗………」`); // :5239
  } else if (era.get(`talent:${a}:11`) == 1 || era.get(`talent:${a}:12`) == 1 || era.get(`talent:${a}:15`) == 1 || era.get(`talent:${a}:30`) == 1 || era.get(`talent:${a}:34`) == 1) { // :5240

    await era.printAndWait(`「就让我来干掉魔王吧！」`); // :5242
  } else if (era.get(`talent:${a}:10`) == 1 || era.get(`talent:${a}:26`) == 1) { // :5243

    await era.printAndWait(`「我，应该能干掉魔王吧………？」`); // :5245
  } else { // :5246

    await era.printAndWait(`「遇到魔王的话，就干掉他！！」`); // :5248
  } // :5249

}

// @GOHOUBI_REQUEST_KOUJO_K4 // :5251
async function GOHOUBI_REQUEST_KOUJO_K4() {


  if (era.get(`cflag:${a}:504`) == 0) { // :5254

    await era.printAndWait(`「钱钱钱！嘻嘻嘻～」`); // :5256
  } else if (era.get(`cflag:${a}:504`) == 1 || era.get(`cflag:${a}:504`) == 2 || era.get(`cflag:${a}:504`) == 3) { // :5257

    await era.print(`「拜托了…让我和`); // :5259
    if (era.get(`cflag:${a}:504`) == 1) { // :5260
      await era.print(`狗`); // :5261
    } else if (Y == 2) { // :5262
      await era.print(`猪`); // :5263
    } else if (Y == 3) { // :5264
      await era.print(`马`); // :5265
    } // :5266
    await era.printAndWait(`交配吧……！」`); // :5267
  } else if (era.get(`cflag:${a}:504`) == 4) { // :5268

    await era.printAndWait(`「嘻嘻！…魔王大人要和我来个很长很长的湿吻哦～」`); // :5270
  } else if (era.get(`cflag:${a}:504`) == 5) { // :5271

    await era.printAndWait(`「…人家回来的时候，想被魔王大人温情地抱一阵子。」`); // :5273
  } else if (era.get(`cflag:${a}:504`) == 6) { // :5274

    await era.printAndWait(`「嘻嘻！魔王大人先把精液存着！等我回来拿～」`); // :5276
  } else if (era.get(`cflag:${a}:504`) == 7) { // :5277

    await era.printAndWait(`「想进行一场了不得的乱交呢！…都是你害得啦！…」`); // :5279
  } else if (era.get(`cflag:${a}:504`) == 8) { // :5280

    await era.printAndWait(`「回来之后…想喝魔王大人的尿………可以么？」`); // :5282
  } else if (era.get(`cflag:${a}:504`) == 9) { // :5283

    await era.printAndWait(`「嘻嘻！想收一个童贞啊！」`); // :5285
  } // :5286

}

// @GOHOUBI_AFTER_KOUJO_K4 // :5288
async function GOHOUBI_AFTER_KOUJO_K4() {





  if (era.get('tflag:18') == 0) { // :5294
    await era.printAndWait(`「是这样啊……我明白了」`); // :5295

  } else if (era.get('tflag:18') == 1) { // :5297
    await era.printAndWait(`「呵呵呵～谢谢谢谢～！」`); // :5298
  } else if (era.get('tflag:18') == 2) { // :5299

    if (era.get(`cflag:${a}:504`) == 0) { // :5301
      await era.printAndWait(`「钱，是人类最好的朋友～」`); // :5302

    } else if (era.get(`cflag:${a}:504`) == 1) { // :5304

      if (era.get(`talent:${a}:0`) == 1) { // :5306
        await era.printAndWait(`「啊！～哦～！…用肛门和狗交配……停不下来～停不下来啦！！」`); // :5307
      } else { // :5308
        await era.printAndWait(`「啊！～哦～！…和狗交配什么的……停不下来～停不下来啦！！」`); // :5309
      } // :5310

    } else if (era.get(`cflag:${a}:504`) == 2) { // :5312

      if (era.get(`talent:${a}:0`) == 1) { // :5314
        await era.printAndWait(`「啊！～哦～！…用肛门和猪交配……停不下来～停不下来啦！！」`); // :5315
      } else { // :5316
        await era.printAndWait(`「啊！～哦～！…和猪交配什么的……停不下来～停不下来啦！！」`); // :5317
      } // :5318

    } else if (era.get(`cflag:${a}:504`) == 3) { // :5320

      if (era.get(`talent:${a}:0`) == 1) { // :5322
        await era.printAndWait(`「啊！～哦～！…用肛门和马交配……停不下来～停不下来啦！！」`); // :5323
      } else { // :5324
        await era.printAndWait(`「啊！～哦～！…和马交配什么的……停不下来～停不下来啦！！」`); // :5325
      } // :5326

    } else if (era.get(`cflag:${a}:504`) == 4) { // :5328
      await era.printAndWait(`「嘻嘻～和魔王大人接吻的时候，回想起了自己少女的时光呢～…啊！失言了。请忘掉吧～」`); // :5329

    } else if (era.get(`cflag:${a}:504`) == 5) { // :5331

      if (era.get(`abl:${a}:2`) > era.get(`abl:${a}:3`)) { // :5333
        await era.printAndWait(`「啊！魔王大人……请～请继续侵犯我吧！～噢哦～…♪」`); // :5334

      } else { // :5336
        await era.printAndWait(`「啊！魔王大人……请～请继续侵犯我吧！～噢哦～…♪」`); // :5337
      } // :5338

    } else if (era.get(`cflag:${a}:504`) == 6) { // :5340
      await era.printAndWait(`「嘻嘻～为了喝魔王大人的精液，我活着回来啦！～♪」`); // :5341

    } else if (era.get(`cflag:${a}:504`) == 7) { // :5343

      if (era.get(`talent:${a}:0`) == 1) { // :5345
        await era.printAndWait(`「啊…第一次就这么结束了么…还想继续啊～♪」`); // :5346
      } else { // :5347
        await era.printAndWait(`「啊…结束了么…还想继续啊～♪」`); // :5348
      } // :5349

    } else if (era.get(`cflag:${a}:504`) == 8) { // :5351
      await era.printAndWait(`「嘻嘻～为了喝魔王大人的尿尿，我活着回来啦！～♪」`); // :5352

    } else if (era.get(`cflag:${a}:504`) == 9) { // :5354

      if (era.get(`abl:${a}:2`) > era.get(`abl:${a}:3`)) { // :5356
        await era.printAndWait(`「呵呵呵，童贞的感觉就是不一样呢～我的那里，舒服么？${heart(1)}」`); // :5357

      } else { // :5359
        await era.printAndWait(`「呵呵呵，童贞的感觉就是不一样呢～我的菊穴，舒服么？${heart(1)}」`); // :5360
      } // :5361
    } else { // :5362
    } // :5363
  } // :5364

}

// @OSIOKI_KOUJO_K4 // :5366
async function OSIOKI_KOUJO_K4() {





  if (era.get('tflag:18') == 0) { // :5372
    await era.printAndWait(`「魔王大人真宽容……下次不会再失败的了！」`); // :5373

  } else if (era.get('tflag:18') == 1) { // :5375

    if (era.get(`abl:${a}:21`) >= 3) { // :5377
      await era.printAndWait(`「哦…好！…好啊～♪」`); // :5378
    } else { // :5379
      await era.printAndWait(`「啊！…讨厌电流！！讨厌！讨厌！」`); // :5380
    } // :5381

  } else if (era.get('tflag:18') == 2) { // :5383

    if (era.get(`abl:${a}:17`) >= 4) { // :5385
      await era.printAndWait(`「快……快看我淫贱自慰的样子～♪」`); // :5386
    } else { // :5387
      await era.printAndWait(`「好……好羞耻啊……………」`); // :5388
    } // :5389

  } else if (era.get('tflag:18') == 3) { // :5391

    if (era.get(`abl:${a}:17`) >= 6) { // :5393
      await era.printAndWait(`「来！来看吧！……看我不知廉耻，拉屎的样子……！」`); // :5394
    } else { // :5395
      await era.printAndWait(`「呜呜…唔………呜呜呜」`); // :5396
    } // :5397

  } else if (era.get('tflag:18') == 4) { // :5399

    if (era.get(`abl:${a}:21`) >= 3) { // :5401
      await era.printAndWait(`「啊！！魔王大人的鞭子！！最棒了！再……再用力地打我！」`); // :5402
    } else { // :5403
      await era.printAndWait(`「呜！……啊？！…………唔哦！！」`); // :5404
    } // :5405

  } else if (era.get('tflag:18') == 5) { // :5407

    if (era.get(`talent:${a}:88`) == 1 || era.get(`talent:${a}:76`) == 1) { // :5409
      await era.printAndWait(`「果然还是魔王大人的尿味道更好啊………」`); // :5410
    } else { // :5411
      await era.printAndWait(`「这……这不是我的爱好………」`); // :5412
    } // :5413

  } else if (era.get('tflag:18') == 6) { // :5415
    await era.printAndWait(`「唉……不想做这种事啊……」`); // :5416

  } else if (era.get('tflag:18') == 7) { // :5418
    await era.printAndWait(`「呜…肚子饿了………」`); // :5419

  } else if (era.get('tflag:18') == 8) { // :5421
    await era.printAndWait(`「噢～喔喔喔喔喔！求求你！魔王大人！快来侵犯人家啊！！呜呜呜呜………………谁都好！什么东西都行！！来侵犯我！！强奸我吧！……啊啊啊啊！！！」`); // :5422

  } else if (era.get('tflag:18') == 9) { // :5424
    await era.printAndWait(`「………好」`); // :5425
  } // :5426


}

// @GOBI_KOUJO_K4, ARG:0 // :5429
async function GOBI_KOUJO_K4() {


  if (arg_0 == 1) { // :5432

    await era.print(`哦～♪`); // :5434
  } else if (arg_0 == 2) { // :5435

    await era.print(`哦！`); // :5437
  } else if (arg_0 == 3) { // :5438

    await era.print(`啦……。`); // :5440
  } else if (arg_0 == 4) { // :5441

    await era.print(`吧……算是……。`); // :5443
  } else if (arg_0 == 5) { // :5444

    await era.print(`什么的……。`); // :5446
  } else { // :5447


    if (rand_n(3) == 0) { // :5450
      await era.print(`呢。`); // :5451
    } else if (rand_n(2) == 0) { // :5452
      await era.print(`嘛。`); // :5453
    } else { // :5454
      await era.print(`啦。`); // :5455
    } // :5456
  } // :5457





}

// ===== 复核清单（转译器生成，agent 逐条处理后删除） =====
// 1. :75 同名函数 @EVENTTRAIN 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTTRAIN', …) 注册，参照 ere/kojo/kojo-k5.js
// 2. :152 插值 未知插值 %CALLNAME:MASTER% —— 保真锁会红，须人工定归一
// 3. :160 插值 未知插值 %CALLNAME:MASTER% —— 保真锁会红，须人工定归一
// 4. :162 插值 未知插值 %CALLNAME:MASTER% —— 保真锁会红，须人工定归一
// 5. :169 CALL K4_KOJO2 —— 口上文件里多为存根调用，人工定存根名
// 6. :229 CALL K4_KOJO2 —— 口上文件里多为存根调用，人工定存根名
// 7. :316 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 8. :318 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 9. :324 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 10. :326 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 11. :392 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 12. :394 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 13. :400 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 14. :402 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 15. :456 同名函数 @EVENTEND 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTEND', …) 注册，参照 ere/kojo/kojo-k5.js
// 16. :533 CALL DOG_KOJO_4 —— 口上文件里多为存根调用，人工定存根名
// 17. :541 CALL COLOSSEUM_KOJO_4 —— 口上文件里多为存根调用，人工定存根名
// 18. :639 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
// 19. :641 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 20. :645 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 21. :649 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 22. :653 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 23. :657 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 24. :687 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 25. :689 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 26. :698 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 27. :711 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 28. :713 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 29. :722 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 30. :731 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 31. :740 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 32. :1524 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 33. :1526 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 34. :1588 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 35. :1590 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 36. :1598 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 37. :1600 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 38. :1625 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 39. :1627 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 40. :1635 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 41. :1637 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 42. :1696 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 43. :1698 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 44. :1706 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 45. :1708 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 46. :1767 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 47. :1769 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 48. :1777 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 49. :1779 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 50. :1824 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 51. :1826 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 52. :1838 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 53. :1883 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 54. :1895 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 55. :1940 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 56. :1942 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 57. :1954 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 58. :2000 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 59. :2012 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 60. :2073 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 61. :2094 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 62. :2192 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 63. :2204 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 64. :2313 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 65. :2315 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 66. :2317 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 67. :2326 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 68. :2328 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 69. :2330 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 70. :2339 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 71. :2341 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 72. :2343 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 73. :2427 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 74. :2443 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 75. :2950 表达式寻址 PALAMLV:3 → era.get('palamlv:3')——族名直译，语义与归属人工定
// 76. :3391 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 77. :3395 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 78. :3457 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 79. :3517 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 80. :3525 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 81. :3533 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 82. :3652 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 83. :3654 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 84. :3662 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 85. :3664 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 86. :3666 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 87. :3674 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 88. :3676 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 89. :3678 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 90. :3686 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 91. :3688 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 92. :3690 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 93. :3936 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
// 94. :3937 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 95. :3965 局部变量 P = PALAM:5 + UP:5 —— 单字母局部变量，JS 侧声明与赋值人工定
// 96. :3966 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 97. :3992 局部变量 P = PALAM:8 + UP:8 —— 单字母局部变量，JS 侧声明与赋值人工定
// 98. :3993 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 99. :4007 局部变量 P = PALAM:10 + UP:10 —— 单字母局部变量，JS 侧声明与赋值人工定
// 100. :4008 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 101. :4022 表达式寻址 NOWEX:0 → era.get('nowex:0')——族名直译，语义与归属人工定
// 102. :4036 表达式寻址 NOWEX:1 → era.get('nowex:1')——族名直译，语义与归属人工定
// 103. :4053 表达式寻址 NOWEX:2 → era.get('nowex:2')——族名直译，语义与归属人工定
// 104. :4070 表达式寻址 NOWEX:3 → era.get('nowex:3')——族名直译，语义与归属人工定
// 105. :4084 局部变量 A = UP:11 + UP:12 —— 单字母局部变量，JS 侧声明与赋值人工定
// 106. :4305 CALL SELL_MATURO_K0 —— 口上文件里多为存根调用，人工定存根名
// 107. :4650 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 108. :4774 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 109. :4776 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 110. :4791 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 111. :4793 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 112. :4829 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 113. :4831 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 114. :4846 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 115. :4848 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 116. :4866 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 117. :4868 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 118. :4883 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 119. :4885 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 120. :4955 表达式寻址 ITEM:PBAND → era.get('item:PBAND')——族名直译，语义与归属人工定
// 121. :4988 表达式寻址 ITEM:PBAND → era.get('item:PBAND')——族名直译，语义与归属人工定
// 122. :5012 表达式寻址 ITEM:PBAND → era.get('item:PBAND')——族名直译，语义与归属人工定
// 123. :5429 函数参数 @GOBI_KOUJO_K4, ARG:0 —— 参数声明已剥（JS 函数签名人工定）
// 124. :5432 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 125. :5435 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 126. :5438 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 127. :5441 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 128. :5444 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 129. :5450 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 130. :5452 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
