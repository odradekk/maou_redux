/* eslint-disable no-undef, no-unused-vars, no-irregular-whitespace, no-redeclare, no-unreachable, no-dupe-else-if */
/**
 * @file EVENT_K12_知的.ERB 的口上转译产物（issue #107 原型，待复核）
 *
 * 源: target/ERB/口上/EVENT_K12_知的.ERB
 *
 * == 已有门面名的下标（复核时可升级为门面读写，裁定一） ==
 *   cflag:1 = 状态（facade-names）
 *   cflag:201 = 初调教（facade-names）
 *   cflag:221 = 首次润滑Lv2（facade-names）
 *   cflag:222 = 首次欲情Lv2（facade-names）
 *   cflag:223 = 首次耻情Lv2（facade-names）
 *   cflag:224 = 首次恐怖Lv2（facade-names）
 *   cflag:225 = 首次C绝顶（facade-names）
 *   cflag:226 = 首次V绝顶（facade-names）
 *   cflag:227 = 首次A绝顶（facade-names）
 *   cflag:228 = 首次B绝顶（facade-names）
 *   cflag:229 = 处女丧失（facade-names）
 *   cflag:261 = 调教后自慰（facade-names）
 *   cflag:262 = 百合PLAY（facade-names）
 *   cflag:263 = 朝口交（facade-names）
 *   cflag:264 = 调教后性交（facade-names）
 *   cflag:265 = 夜袭（facade-names）
 *   cflag:271 = 妊娠发觉（facade-names）
 *   cflag:272 = 生产（facade-names）
 *   cflag:297 = 苦痛刻印Lv3（facade-names）
 *   cflag:298 = 快乐刻印Lv3（facade-names）
 *   cflag:299 = 屈服刻印Lv3（facade-names）
 *   cflag:300 = 反抗刻印Lv3（facade-names）
 *   cflag:301 = 爱抚（facade-names）
 *   cflag:302 = 舔阴（facade-names）
 *   cflag:303 = 肛门爱抚（facade-names）
 *   cflag:304 = 自慰（facade-names）
 *   cflag:306 = 胸爱抚（facade-names）
 *   cflag:307 = 接吻（facade-names）
 *   cflag:308 = 自己扒开（facade-names）
 *   cflag:309 = 插入手指（facade-names）
 *   cflag:310 = 舔肛（facade-names）
 *   cflag:311 = 振动宝石（facade-names）
 *   cflag:312 = 壶虫（facade-names）
 *   cflag:313 = 振动杖（facade-names）
 *   cflag:314 = 肛门虫（facade-names）
 *   cflag:320 = 肛珠（facade-names）
 *   cflag:321 = 正常位（facade-names）
 *   cflag:322 = 背后位（facade-names）
 *   cflag:323 = 对面座位（facade-names）
 *   cflag:324 = 背面座位（facade-names）
 *   cflag:327 = 正常位肛交（facade-names）
 *   cflag:328 = 背后位肛交（facade-names）
 *   cflag:329 = 对面座位肛交（facade-names）
 *   cflag:330 = 背面座位肛交（facade-names）
 *   cflag:331 = 手淫（facade-names）
 *   cflag:332 = 口交_奴（facade-names）
 *   cflag:333 = 乳交（facade-names）
 *   cflag:334 = 股间性交（facade-names）
 *   cflag:335 = 骑乘位（facade-names）
 *   cflag:336 = 全身擦洗（facade-names）
 *   cflag:337 = 骑乘位肛交（facade-names）
 *   cflag:338 = 肛门侍奉（facade-names）
 *   cflag:341 = 打屁股（facade-names）
 *   cflag:342 = 鞭（facade-names）
 *   cflag:343 = 针（facade-names）
 *   cflag:344 = 眼罩（facade-names）
 *   cflag:345 = 绳子（facade-names）
 *   cflag:346 = 口塞（facade-names）
 *   cflag:347 = 灌肠肛塞（facade-names）
 *   cflag:348 = 穿环（facade-names）
 *   cflag:356 = 放置PLAY（facade-names）
 *   cflag:357 = 交谈（facade-names）
 *   cflag:360 = 乳夹口交（facade-names）
 *   cflag:361 = 口交时自慰（facade-names）
 *   cflag:362 = 手搓口交（facade-names）
 *   cflag:363 = 真空口交（facade-names）
 *   cflag:364 = 六九式（facade-names）
 *   cflag:365 = 深喉（facade-names）
 *   cflag:372 = 壶虫着脱（facade-names）
 *   cflag:374 = 肛门虫着脱（facade-names）
 *   cflag:379 = 肛珠着脱（facade-names）
 *   cflag:380 = 眼罩着脱（facade-names）
 *   cflag:381 = 强制口交（facade-names）
 *   cflag:385 = 绳子着脱（facade-names）
 *   cflag:386 = 口塞着脱（facade-names）
 *   cflag:444 = 兽奸眼罩（facade-names）
 *   cflag:650 = NTR再捕获（facade-names）
 *   flag:62 = 肉便器行动（facade-names）
 *   flag:63 = 肉便器常识改写（facade-names）
 *   flag:7 = 口上开关（facade-names）
 *   tflag:13 = 初吻与自我口上（facade-names）
 *   tflag:150 = 反抗刻印回避（facade-names）
 *   tflag:16 = 犬射精或处刑口上（facade-names）
 *   tflag:18 = 足交射精或处遇口上（facade-names）
 *   tflag:20 = 主人导致处女丧失（facade-names）
 *   tflag:21 = 反抗刻印变动（facade-names）
 *   tflag:22 = 苦痛刻印变动（facade-names）
 *   tflag:23 = 快乐刻印变动（facade-names）
 *   tflag:24 = 屈服刻印变动（facade-names）
 *   tflag:3 = 处女丧失（facade-names）
 *   tflag:400 = 死斗场敌种（facade-names）
 *   tflag:500 = 博物馆口上（facade-names）
 *   tflag:510 = 流放口上（facade-names）
 *   tflag:520 = 公开处刑口上（facade-names）
 *   tflag:530 = 猎奇处刑口上（facade-names）
 *   tflag:899 = 失神（facade-names）
 *
 * == 复核标记（166 处） ==
 * 本文件由 tools/kojo-transpiler.js 生成。以下位置是机械转换无法
 * 确定的，须 agent 逐字对照 ERB 源复核（裁定 7：agent 逐字对照，
 * 不是抽查）。复核成果 = 在本文件内改写成最终形态，并把本 REVIEW
 * 清单逐条删掉；转译器默认不覆盖本文件（产物边界，issue #10），
 * 复核成果不会被重跑覆盖。
 *   1. :81 同名函数 @EVENTTRAIN 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTTRAIN', …) 注册，参照 ere/kojo/kojo-k5.js
 *   2. :170 CALL K12_KOJO2 —— 口上文件里多为存根调用，人工定存根名
 *   3. :230 CALL K12_KOJO2 —— 口上文件里多为存根调用，人工定存根名
 *   4. :272 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   5. :274 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   6. :291 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   7. :293 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   8. :312 同名函数 @EVENTEND 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTEND', …) 注册，参照 ere/kojo/kojo-k5.js
 *   9. :413 CALL DOG_KOJO_12 —— 口上文件里多为存根调用，人工定存根名
 *   10. :418 CALL COLOSSEUM_KOJO_12 —— 口上文件里多为存根调用，人工定存根名
 *   11. :560 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   12. :562 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   13. :566 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   14. :570 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   15. :574 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   16. :578 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   17. :608 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   18. :610 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   19. :612 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   20. :613 插值 未知插值 %阴核(TARGET)% —— 保真锁会红，须人工定归一
 *   21. :621 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   22. :634 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   23. :636 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   24. :637 插值 未知插值 %阴核(TARGET)% —— 保真锁会红，须人工定归一
 *   25. :639 插值 未知插值 %阴核(TARGET)% —— 保真锁会红，须人工定归一
 *   26. :645 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   27. :654 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   28. :657 插值 未知插值 %阴核(TARGET)% —— 保真锁会红，须人工定归一
 *   29. :663 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   30. :693 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   31. :701 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   32. :1544 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   33. :1546 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   34. :1558 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   35. :1560 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   36. :1589 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   37. :1591 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   38. :1603 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   39. :1605 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   40. :1669 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   41. :1671 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   42. :1679 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   43. :1681 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   44. :1740 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   45. :1742 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   46. :1750 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   47. :1752 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   48. :1797 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   49. :1799 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   50. :1811 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   51. :1856 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   52. :1858 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   53. :1870 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   54. :1915 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   55. :1917 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   56. :1929 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   57. :1974 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   58. :1986 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   59. :2034 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   60. :2054 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   61. :2155 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   62. :2167 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   63. :2268 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   64. :2270 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   65. :2272 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   66. :2280 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   67. :2282 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   68. :2284 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   69. :2292 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   70. :2294 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   71. :2296 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   72. :2375 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   73. :2387 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   74. :2889 表达式寻址 PALAMLV:3 → era.get('palamlv:3')——族名直译，语义与归属人工定
 *   75. :3826 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   76. :3828 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   77. :3836 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   78. :3838 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   79. :3846 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   80. :3848 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   81. :3897 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   82. :3905 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   83. :3913 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   84. :3961 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   85. :3969 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   86. :3977 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   87. :4097 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   88. :4099 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   89. :4107 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   90. :4109 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   91. :4111 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   92. :4119 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   93. :4121 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   94. :4123 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   95. :4131 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   96. :4133 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   97. :4135 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   98. :4366 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   99. :4367 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   100. :4397 局部变量 P = PALAM:5 + UP:5 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   101. :4398 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   102. :4424 局部变量 P = PALAM:8 + UP:8 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   103. :4425 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   104. :4439 局部变量 P = PALAM:10 + UP:10 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   105. :4440 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
 *   106. :4454 表达式寻址 NOWEX:0 → era.get('nowex:0')——族名直译，语义与归属人工定
 *   107. :4468 表达式寻址 NOWEX:1 → era.get('nowex:1')——族名直译，语义与归属人工定
 *   108. :4485 表达式寻址 NOWEX:2 → era.get('nowex:2')——族名直译，语义与归属人工定
 *   109. :4502 表达式寻址 NOWEX:3 → era.get('nowex:3')——族名直译，语义与归属人工定
 *   110. :4516 局部变量 A = UP:11 + UP:12 —— 单字母局部变量，JS 侧声明与赋值人工定
 *   111. :4743 CALL SELL_MATURO_K0 —— 口上文件里多为存根调用，人工定存根名
 *   112. :5047 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   113. :5047 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   114. :5047 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   115. :5065 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   116. :5086 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   117. :5087 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   118. :5106 CALL BENKI_PLAYER_NAME —— 口上文件里多为存根调用，人工定存根名
 *   119. :5108 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   120. :5128 CALL BENKI_PLAYER_NAME —— 口上文件里多为存根调用，人工定存根名
 *   121. :5130 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   122. :5150 CALL BENKI_PLAYER_NAME —— 口上文件里多为存根调用，人工定存根名
 *   123. :5152 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   124. :5172 CALL BENKI_PLAYER_NAME —— 口上文件里多为存根调用，人工定存根名
 *   125. :5174 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   126. :5200 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   127. :5202 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   128. :5230 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   129. :5257 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   130. :5259 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   131. :5294 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   132. :5296 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   133. :5311 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   134. :5313 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   135. :5348 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   136. :5350 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   137. :5365 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   138. :5367 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   139. :5385 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   140. :5387 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   141. :5402 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   142. :5404 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   143. :5725 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   144. :5728 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   145. :5734 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   146. :5743 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   147. :5747 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   148. :5759 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   149. :5763 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   150. :5767 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   151. :5771 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   152. :5775 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   153. :5779 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
 *   154. :5780 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   155. :5795 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   156. :5892 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   157. :5908 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   158. :5910 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
 *   159. :5927 函数参数 @GOBI_KOUJO_K12, ARG:0 —— 参数声明已剥（JS 函数签名人工定）
 *   160. :5930 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   161. :5933 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   162. :5936 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   163. :5939 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   164. :5942 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
 *   165. :5948 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 *   166. :5950 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
 */

'use strict';

// 需要复核 agent 补的导入（产物初稿不 require，保真锁会红）：
// const era = require('#/era-electron');
// const { heart, self_call, self_call_first } = require('#/kojo/kojo-text');
// const { chara_callname } = require('#/utils/callname-utils');
// 以及 target_name / player_name / assi_name / master_name / sc() / scf()
// 的取值（%SAVESTR:TARGET% 等插值的 JS 侧表达式）。


































































// @EVENTTRAIN // :67
async function EVENTTRAIN() {
  // #PRI（事件优先级修饰符，JS 侧用 on() 的 TIER 表达） // :68
  // FLAG:112  = 1（变量语义：FLAG 族，112） // :69
  era.set('flag:112', 1); // :69
  if (era.get('flag:7') == 0) { // :71
    // FLAG:7  = 2（变量语义：FLAG 族，7） // :71
    era.set('flag:7', 2); // :71
  } // :71

}

// @EVENTEND // :73
async function EVENTEND() {
  // #LATER（事件优先级修饰符，JS 侧用 on() 的 TIER 表达） // :74
  // FLAG:112  = 0（变量语义：FLAG 族，112） // :75
  era.set('flag:112', 0); // :75





}

// @EVENTTRAIN // :81
async function EVENTTRAIN() {
  if (era.get('flag:7') <= 0) { // :83
    return 0; // :83
  } // :83
  if (era.get(`talent:${target}:172`) != 1) { // :85
    return 0; // :85
  } // :85




  if (era.get(`cflag:${target}:201`) == 0) { // :90
    era.drawLine(); // :91
    if (era.get(`talent:${target}:种族`) == 2) { // :92

      await era.printAndWait(`「就表扬你一下吧。这是超出了${sc()}预想的力量」`); // :94
      await era.printAndWait(`「但是没用的哦。${sc()}作为自豪的人狼、还拥有最高的智能……」`); // :95
      await era.printAndWait(`${target_name}虽然带着清爽的表情逞强着、但轻飘飘的耳朵害怕的低了下来。`); // :96
    } else { // :97
      await era.printAndWait(`「看起来你比${sc()}更厉害呢」`); // :98
      await era.printAndWait(`「但${sc()}可是接受过特殊训练的。不管对我做什么都是没用的」`); // :99
      await era.printAndWait(`${target_name}表情冷淡强装镇定、声音微微发颤`); // :100
    } // :101
    // CFLAG:201  = 1（变量语义：CFLAG 族，201） // :102
    era.set(`cflag:${target}:201`, 1); // :102
    return 1; // :103



  } else if (era.get(`cflag:${target}:201`) >= 1 && era.get(`cflag:${target}:650`) == 1) { // :107
    if (era.get(`talent:${target}:85`) || era.get(`talent:${target}:76`)) { // :108
      era.drawLine(); // :109
      await era.printAndWait(`「对不住了呢……在其他人的身体上做了活塞运动的确是事实呢」`); // :110
      await era.printAndWait(`「但是从生物学上看这并没有什么问题、只是感情上的问题哦、所以原谅我吧」`); // :111
      await era.printAndWait(`${target_name}低着头小声辩解道`); // :112

      // CFLAG:650  = 0（变量语义：CFLAG 族，650） // :114
      era.set(`cflag:${target}:650`, 0); // :114
    } else { // :115
      era.drawLine(); // :116
      await era.printAndWait(`「又被抓住了呢……${sc()}的运气真不好」`); // :117
      await era.printAndWait(`「是要继续调教我吗？　还是当成肉便器处理？　随便你吧」`); // :118
      await era.printAndWait(`${target_name}冷冷的看着你`); // :119

      // CFLAG:650  = 0（变量语义：CFLAG 族，650） // :121
      era.set(`cflag:${target}:650`, 0); // :121
    } // :122
    return 1; // :123




  } else if (era.get(`cflag:${target}:201`) < 2 && era.get(`mark:${target}:2`) == 1) { // :128
    era.drawLine(); // :129
    await era.printAndWait(`「看起来你的能力好像比资料上要高呢……」`); // :130
    // CFLAG:201  = 2（变量语义：CFLAG 族，201） // :131
    era.set(`cflag:${target}:201`, 2); // :131
    return 1; // :132


  } else if (era.get(`cflag:${target}:201`) < 3 && era.get(`mark:${target}:2`) == 2) { // :135
    era.drawLine(); // :136
    await era.printAndWait(`「你到底是……何方神圣、能把${sc()}逼到这种地步……」`); // :137
    // CFLAG:201  = 3（变量语义：CFLAG 族，201） // :138
    era.set(`cflag:${target}:201`, 3); // :138
    return 1; // :139


  } else if (era.get(`cflag:${target}:201`) < 4 && era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0) { // :142
    era.drawLine(); // :143
    await era.printAndWait(`「难以置信……这样的情况、不管是数据还是资料上都从未见过呢！？」`); // :144
    // CFLAG:201  = 4（变量语义：CFLAG 族，201） // :145
    era.set(`cflag:${target}:201`, 4); // :145
    return 1; // :146


  } else if (era.get(`cflag:${target}:201`) < 5 && era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:85`) == 0) { // :149
    era.drawLine(); // :150
    await era.printAndWait(`「竟然还存在着如此美妙的新世界……让${sc()}更多地对此进行研究吧、拜托了！」`); // :151
    await era.printAndWait(`「想尝试一下、${sc()}的身体能淫靡化到什么地步……已经、睡不着了！」`); // :152
    await era.printAndWait(`${target_name}一边流着口水一边用腰蹭着你的腿`); // :153
    await era.printAndWait(`她的脑海中已经填满了对性知识的渴求了……`); // :154
    // CFLAG:201  = 5（变量语义：CFLAG 族，201） // :155
    era.set(`cflag:${target}:201`, 5); // :155
    return 1; // :156


  } else if (era.get(`cflag:${target}:201`) < 6 && era.get(`talent:${target}:85`) == 1) { // :159
    era.drawLine(); // :160
    await era.printAndWait(`「竟然还存在着如此美妙的新世界……拜托了！　让我和你一起来研究吧」`); // :161
    await era.printAndWait(`「魔界的动植物和文化、魔法……全都是我还不懂的东西呢」`); // :162
    await era.printAndWait(`进入房间的${target_name}正专心致志地在笔记本上写着什么`); // :163
    await era.printAndWait(`完全被魔之知识迷住了的样子……`); // :164
    // CFLAG:201  = 6（变量语义：CFLAG 族，201） // :165
    era.set(`cflag:${target}:201`, 6); // :165
    return 1; // :166


  } else if (era_flag.assi < 0) { // :169
    // CALL K12_KOJO2 // :170


























































  } else { // :229
    // CALL K12_KOJO2 // :230
  } // :231





}

// @K12_KOJO2 // :237
async function K12_KOJO2() {

  if (era.get(`mark:${target}:3`) == 3 && era.get('flag:7') == 2) { // :239
    era.drawLine(); // :240
    await era.printAndWait(`「不要再进入我的视线里……我很不高兴」`); // :241
    return 1; // :242


  } else if (era.get(`mark:${target}:2`) == 0 && era.get('flag:7') == 2) { // :245
    era.drawLine(); // :246
    await era.printAndWait(`「根据${sc()}的计算、即使是你的力量也无法让${sc()}屈服哦」`); // :247
    return 1; // :248


  } else if (era.get(`mark:${target}:2`) == 1 && era.get('flag:7') == 2) { // :251
    era.drawLine(); // :252
    await era.printAndWait(`「不管你使出什么手段、都在我计算之中！」`); // :253
    return 1; // :254


  } else if (era.get(`mark:${target}:2`) == 2 && era.get('flag:7') == 2) { // :257
    era.drawLine(); // :258
    await era.printAndWait(`「噫、好卑鄙……居然这样对待${sc()}……」`); // :259
    return 1; // :260


  } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0 && era.get(`talent:${target}:76`) == 0 && era.get('flag:7') == 2) { // :263
    era.drawLine(); // :264
    await era.printAndWait(`「我知道了、就按你说的做……是${sc()}输了」`); // :265
    return 1; // :266


  } else if (era.get(`talent:${target}:76`) == 1 && era.get('flag:7') == 2) { // :269
    era.drawLine(); // :270

    if (rand_n(3) == 0) { // :272
      await era.printAndWait(`「今天要研究什么Play呢、好期待啊♪」`); // :273
    } else if (rand_n(2) == 0) { // :274
      await era.printAndWait(`「再多多开发${sc()}的身体嘛、还完全不够呢」`); // :275
    } else { // :276
      await era.printAndWait(`「今天也被开发了一番呢、好开心啊、真想再提升一下敏感度呢」`); // :277
    } // :278
    if (era.get(`talent:${target}:种族`) == 2) { // :279

      await era.printAndWait(`${target_name}像狗一样伸出舌头，吐出慌乱的吐息迎接了出来。`); // :281
    } else { // :282
      await era.printAndWait(`${target_name}从研究中的桌子旁站了起来，迎了出来。`); // :283
    } // :284
    return 1; // :285


  } else if (era.get(`talent:${target}:85`) == 1 && era.get('flag:7') == 2) { // :288
    era.drawLine(); // :289

    if (rand_n(3) == 0) { // :291
      await era.printAndWait(`「呵呵、你来了啊……正好是我研究的有些疲劳的时候呢」`); // :292
    } else if (rand_n(2) == 0) { // :293
      await era.printAndWait(`「今天的研究进展很大。好想被表扬呢」`); // :294
    } else { // :295
      await era.printAndWait(`「啊、已经到休憩的时间了？　饶了我吧……和你做对手的话不是反而会更累吗」`); // :296
    } // :297
    if (era.get(`talent:${target}:种族`) == 2) { // :298

      await era.printAndWait(`${target_name}轻飘飘的尾巴像摇出了残影一样摆动着迎了出来。`); // :300
    } else { // :301
      await era.printAndWait(`${target_name}从研究中的桌子旁站了起来，迎了出来。`); // :302
    } // :303
    return 1; // :304
  } // :305
  return 0; // :306





}

// @EVENTEND // :312
async function EVENTEND() {
  if (era.get('flag:7') <= 0) { // :314
    return 0; // :314
  } // :314
  if (era.get(`talent:${target}:172`) != 1) { // :316
    return 0; // :316
  } // :316


  if (era.get(`base:${target}:0`) <= 0) { // :320
    return 0; // :320
  } // :320





  if (era.get(`mark:${target}:3`) == 3 && era.get(`talent:${target}:85`) == 0) { // :326
    era.drawLine(); // :327
    await era.printAndWait(`「真是无聊的时光呢」`); // :328
    return 1; // :329


  } else if (era.get(`mark:${target}:2`) <= 1 && era.get(`talent:${target}:85`) == 0) { // :332
    era.drawLine(); // :333
    await era.printAndWait(`「已经结束了吗、这种程度、在我的预料之内呢」`); // :334
    return 1; // :335


  } else if (era.get(`mark:${target}:2`) == 2 && era.get(`talent:${target}:85`) == 0) { // :338
    era.drawLine(); // :339
    await era.printAndWait(`「原来如此、和资料上一样呢……果然、好累」`); // :340
    return 1; // :341


  } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 0) { // :344
    era.drawLine(); // :345
    await era.printAndWait(`「结束了吗……体力值还挺高的嘛」`); // :346
    return 1; // :347


  } else if (era.get(`talent:${target}:76`) == 1 && era.get(`base:${target}:0`) >= 500) { // :350
    era.drawLine(); // :351
    await era.printAndWait(`「怎么这样就停了、再多多开发${sc()}淫乱的身体吧」`); // :352
    if (era.get(`talent:${target}:种族`) == 2) { // :353

      await era.printAndWait(`${target_name}软绵绵的耳朵立了起来，好像很不满。`); // :355
    } else { // :356
      await era.printAndWait(`${target_name}鼓着脸颊，好像很不满。`); // :357
    } // :358
    return 1; // :359

  } else if (era.get(`talent:${target}:76`) == 1 && era.get(`base:${target}:0`) <= 500) { // :361
    era.drawLine(); // :362
    await era.printAndWait(`「${sc()}身体的耐久极限……差不多就是这样吗、哈～哈～」`); // :363
    if (era.get(`talent:${target}:种族`) == 2) { // :364

      await era.printAndWait(`${target_name}像狗一样伸出舌头，混乱的喘息着。`); // :366
    } else { // :367
      await era.printAndWait(`${target_name}就那样倒在床上，不停地喘着粗气。`); // :368
    } // :369
    return 1; // :370


  } else if (era.get(`talent:${target}:85`) == 1 && era.get(`base:${target}:0`) >= 500) { // :373
    era.drawLine(); // :374
    await era.printAndWait(`「哎呀、研究不能继续了呢。很开心哦、与你的幽会」`); // :375
    if (era.get(`talent:${target}:种族`) == 2) { // :376

      await era.printAndWait(`背向这边的${target_name}软绵绵的尾巴呼噜呼噜的左右摇动着。`); // :378
    } else { // :379
      await era.printAndWait(`${target_name}就那样以调教中的姿势回到了研究中的桌子旁，继续开始了工作。`); // :380
    } // :381
    return 1; // :382

  } else if (era.get(`talent:${target}:85`) == 1 && era.get(`base:${target}:0`) <= 500) { // :384
    era.drawLine(); // :385
    await era.printAndWait(`「果然、这种程度的体力消耗、是对研究的一大障碍呢……」`); // :386
    if (era.get(`talent:${target}:种族`) == 2) { // :387

      await era.printAndWait(`${target_name}打了一个哈欠、用像狗一样团起来的姿势打起了瞌睡。`); // :389
    } else { // :390
      await era.printAndWait(`${target_name}就那样以调教中的姿势回到了研究中的桌子旁，继续开始了工作。`); // :391
    } // :392
    return 1; // :393
  } // :394
  return 0; // :395





}

// @KOJO_MESSAGE_COM_12 // :401
async function KOJO_MESSAGE_COM_12() {




  if (era.get(`tequip:${target}:45`) && era_flag.selectcom != 45) { // :407
    return 0; // :407
  } // :407

  if (era.get('tflag:899')) { // :410
    return 0; // :410
  } // :410

  if (era.get(`tequip:${target}:89`)) { // :412
    // CALL DOG_KOJO_12 // :413
    return 0; // :414
  } // :415

  if (era.get(`tequip:${target}:55`)) { // :417
    // CALL COLOSSEUM_KOJO_12 // :418
    return 0; // :419
  } // :420







  if (era_flag.selectcom == 0) { // :428

    if (era.get(`cflag:${target}:301`) == 0) { // :430

      if (era.get(`mark:${target}:2`) >= 2) { // :432
        await era.printAndWait(`「你知道这个理论吗？　一开始要先抚摸女性的肌肤呢」`); // :433

      } else { // :435
        await era.printAndWait(`「哼、真无聊呢。跟教科书一样的步骤呢」`); // :436
      } // :437
      // CFLAG:301  = 1（变量语义：CFLAG 族，301） // :438
      era.set(`cflag:${target}:301`, 1); // :438
      return 0; // :439

    } else if (era.get(`talent:${target}:153`) && era.get(`cflag:${target}:111`) == 0) { // :441

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:301`) <= 5 || era.get('flag:7') == 2)) { // :443
        await era.printAndWait(`「孩子还在里面看着呢。还请温柔点哦」`); // :444
        if (era.get(`talent:${target}:种族`) == 2) { // :445

          await era.printAndWait(`「如果是像${sc()}一样活泼可爱的孩子就好啦♪」`); // :447
        } // :448
        // CFLAG:301  = 6（变量语义：CFLAG 族，301） // :449
        era.set(`cflag:${target}:301`, 6); // :449

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:301`) <= 4 || era.get('flag:7') == 2)) { // :451
        await era.printAndWait(`「又长大了呢、好想快点生下来啊♪」`); // :452
        if (era.get(`talent:${target}:种族`) == 2) { // :453

          await era.printAndWait(`「嗯……就这么抚摸${sc()}的头。叨着『谢谢你怀上孩子哦』」`); // :455
        } else { // :456
          await era.printAndWait(`「嗯……${sc()}好想多做做脚部按摩啊。挺着大肚子可累了」`); // :457
        } // :458
        // CFLAG:301  = 5（变量语义：CFLAG 族，301） // :459
        era.set(`cflag:${target}:301`, 5); // :459

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:301`) <= 3 || era.get('flag:7') == 2)) { // :461
        await era.printAndWait(`「老用这种方式来抚摸、要忍受这种待遇的……可是你的孩子啊。就不能更小心翼翼一些吗？」`); // :462
        // CFLAG:301  = 4（变量语义：CFLAG 族，301） // :463
        era.set(`cflag:${target}:301`, 4); // :463

      } else if (era.get(`mark:${target}:2`) == 2 && (era.get(`cflag:${target}:301`) <= 2 || era.get('flag:7') == 2)) { // :465
        await era.printAndWait(`「嗯、咕……别、别摸了啦……」`); // :466
        // CFLAG:301  = 3（变量语义：CFLAG 族，301） // :467
        era.set(`cflag:${target}:301`, 3); // :467

      } else if (era.get(`mark:${target}:2`) <= 1 && (era.get(`cflag:${target}:301`) <= 1 || era.get('flag:7') == 2)) { // :469
        await era.printAndWait(`「就算怀上了你的孩子……也别想让${sc()}动心」`); // :470
        // CFLAG:301  = 2（变量语义：CFLAG 族，301） // :471
        era.set(`cflag:${target}:301`, 2); // :471
      } // :472
      return 0; // :473

    } else { // :475

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:301`) <= 5 || era.get('flag:7') == 2)) { // :477
        await era.printAndWait(`「不要再挑逗我了……明明知道单是这样子已经无法满足我了」`); // :478
        if (era.get(`talent:${target}:种族`) == 2) { // :479

          await era.printAndWait(`「知道${sc()}感觉舒服的地方吗？　想让你抚摸喉咙呢♪」`); // :481
        } // :482
        // CFLAG:301  = 6（变量语义：CFLAG 族，301） // :483
        era.set(`cflag:${target}:301`, 6); // :483

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:301`) <= 4 || era.get('flag:7') == 2)) { // :485
        await era.printAndWait(`「最近很疲劳呢。谢谢你为我按摩」`); // :486
        if (era.get(`talent:${target}:种族`) == 2) { // :487

          await era.printAndWait(`「嗯……多按摩一下${sc()}的脚吧。散步有些累了」`); // :489
        } else { // :490
          await era.printAndWait(`「嗯……多按摩一下${sc()}的腰吧。在桌子边坐得有些累了」`); // :491
        } // :492
        // CFLAG:301  = 5（变量语义：CFLAG 族，301） // :493
        era.set(`cflag:${target}:301`, 5); // :493

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:301`) <= 3 || era.get('flag:7') == 2)) { // :495
        await era.printAndWait(`「只是被你的手摸着、就想向你屈服了呢……看来${sc()}的计算错误了呢」`); // :496
        // CFLAG:301  = 4（变量语义：CFLAG 族，301） // :497
        era.set(`cflag:${target}:301`, 4); // :497

      } else if (era.get(`mark:${target}:2`) == 2 && (era.get(`cflag:${target}:301`) <= 2 || era.get('flag:7') == 2)) { // :499
        await era.printAndWait(`「嗯、唔～……继、继续吧……」`); // :500
        // CFLAG:301  = 3（变量语义：CFLAG 族，301） // :501
        era.set(`cflag:${target}:301`, 3); // :501

      } else if (era.get(`mark:${target}:2`) <= 1 && (era.get(`cflag:${target}:301`) <= 1 || era.get('flag:7') == 2)) { // :503
        await era.printAndWait(`「这种程度全在预料之中呢。${sc()}的心是不会动摇的」`); // :504
        // CFLAG:301  = 2（变量语义：CFLAG 族，301） // :505
        era.set(`cflag:${target}:301`, 2); // :505
      } // :506
      return 0; // :507
    } // :508
  } // :509




  if (era_flag.selectcom == 1) { // :514

    if (era.get(`cflag:${target}:302`) == 0) { // :516

      if (era.get(`talent:${target}:0`) == 1) { // :518
        await era.printAndWait(`「哼、性器没被男人碰过真是对不住呢……」`); // :519

      } else { // :521
        await era.printAndWait(`「喂、不要舔性器！　再怎么说那也是排泄器官！」`); // :522
      } // :523
      // CFLAG:302  = 1（变量语义：CFLAG 族，302） // :524
      era.set(`cflag:${target}:302`, 1); // :524
      return 0; // :525

    } else { // :527

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:302`) <= 4 || era.get('flag:7') == 2)) { // :529
        await era.printAndWait(`「再用力点吸吸淫核……呵呵、勃起来了吧？」`); // :530
        // CFLAG:302  = 5（变量语义：CFLAG 族，302） // :531
        era.set(`cflag:${target}:302`, 5); // :531

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:302`) <= 3 || era.get('flag:7') == 2)) { // :533
        await era.printAndWait(`「我可以一边看书吗？　这样会轻松点……」`); // :534
        // CFLAG:302  = 4（变量语义：CFLAG 族，302） // :535
        era.set(`cflag:${target}:302`, 4); // :535

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:302`) <= 2 || era.get('flag:7') == 2)) { // :537
        await era.printAndWait(`「喜欢的话就随便舔吧。${sc()}已经不会再反抗了……」`); // :538
        // CFLAG:302  = 3（变量语义：CFLAG 族，302） // :539
        era.set(`cflag:${target}:302`, 3); // :539

      } else if (era.get(`cflag:${target}:302`) <= 1 || era.get('flag:7') == 2) { // :541
        await era.printAndWait(`「住、住手！　那里的粘膜很敏感啊！」`); // :542
        // CFLAG:302  = 2（变量语义：CFLAG 族，302） // :543
        era.set(`cflag:${target}:302`, 2); // :543
      } // :544
      return 0; // :545
    } // :546
  } // :547




  if (era_flag.selectcom == 2) { // :552

    if (era.get(`cflag:${target}:303`) == 0) { // :554
      await era.printAndWait(`「这是在做肛交的准备吗、变态」`); // :555
      // CFLAG:TARGET:303  = 1（变量语义：CFLAG 族，TARGET:303） // :556
      era.set(`cflag:${target}:303`, 1); // :556
      return 0; // :557

    } else { // :559
      // 赋值 P = PALAM:3 + UP:3 // :560

      if (era.get(`talent:${target}:76`) == 1 && P >= era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 6 || era.get('flag:7') == 2)) { // :562
        await era.printAndWait(`「啊啊～、已经做好肛交的准备了哦！　排泄……不、已经变成性器啦♪」`); // :563
        // CFLAG:303  = 7（变量语义：CFLAG 族，303） // :564
        era.set(`cflag:${target}:303`, 7); // :564

      } else if (era.get(`talent:${target}:76`) == 1 && P < era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 5 || era.get('flag:7') == 2)) { // :566
        await era.printAndWait(`「抱歉、还很紧……最好、再润滑一下呢」`); // :567
        // CFLAG:303  = 6（变量语义：CFLAG 族，303） // :568
        era.set(`cflag:${target}:303`, 6); // :568

      } else if (era.get(`talent:${target}:85`) == 1 && P >= era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 4 || era.get('flag:7') == 2)) { // :570
        await era.printAndWait(`「怎么样、${sc()}的第二性器……这样一来就能肛交了呢♪」`); // :571
        // CFLAG:303  = 5（变量语义：CFLAG 族，303） // :572
        era.set(`cflag:${target}:303`, 5); // :572

      } else if (era.get(`talent:${target}:85`) == 1 && P < era.get('palamlv:2') && (era.get(`cflag:${target}:303`) <= 3 || era.get('flag:7') == 2)) { // :574
        await era.printAndWait(`「你、能让我再湿一点吗……里面还没放松下来呢」`); // :575
        // CFLAG:303  = 4（变量语义：CFLAG 族，303） // :576
        era.set(`cflag:${target}:303`, 4); // :576

      } else if (P >= era.get('palamlv:2') && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:303`) <= 2 || era.get('flag:7') == 2)) { // :578
        await era.printAndWait(`「咕呜～、不行啊……这么湿漉漉下去的话……性器、会变成性器的啊……」`); // :579
        // CFLAG:303  = 3（变量语义：CFLAG 族，303） // :580
        era.set(`cflag:${target}:303`, 3); // :580

      } else if (era.get(`cflag:${target}:223`) <= 1 || era.get('flag:7') == 2) { // :582
        await era.printAndWait(`「不管再怎么玩弄排泄器官、都不会有什么快感的」`); // :583
        // CFLAG:303  = 2（变量语义：CFLAG 族，303） // :584
        era.set(`cflag:${target}:303`, 2); // :584
      } // :585
      return 0; // :586
    } // :587
  } // :588




  if (era_flag.selectcom == 3) { // :593

    if (era.get(`cflag:${target}:304`) == 0) { // :595
      await era.printAndWait(`「自慰什么的谁都有过吧。诶、让${sc()}来……」`); // :596
      // CFLAG:TARGET:304  = 1（变量语义：CFLAG 族，TARGET:304） // :597
      era.set(`cflag:${target}:304`, 1); // :597
      return 0; // :598

    } else { // :600

      if (era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:304`) <= 8 || era.get('flag:7') == 2)) { // :602
        await era.printAndWait(`「没有被男人碰过的这个小穴、会疼也是没办法的……什么时候都可以给你哦」`); // :603
        // CFLAG:304  = 9（变量语义：CFLAG 族，304） // :604
        era.set(`cflag:${target}:304`, 9); // :604

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:304`) <= 7 || era.get('flag:7') == 2)) { // :606

        if (rand_n(4) == 0) { // :608
          await era.printAndWait(`「${sc()}的痴态、没有被记录下来吗……？　想作为下次自慰的参考呢」`); // :609
        } else if (rand_n(3) == 0) { // :610
          await era.printAndWait(`「猴子……要变成猴子了！　变成自慰猴子了啊！」`); // :611
        } else if (rand_n(2) == 0) { // :612
          await era.printAndWait(`「看吧……像猴子一样玩弄%阴核(TARGET)%的${sc()}的姿态……！」`); // :613
        } else { // :614
          await era.printAndWait(`「啊～、啊～、去了、去了、去了……像猴子一样揉着阴部去了啊」`); // :615
        } // :616
        // CFLAG:304  = 8（变量语义：CFLAG 族，304） // :617
        era.set(`cflag:${target}:304`, 8); // :617

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:31`) < 3 && (era.get(`cflag:${target}:304`) <= 6 || era.get('flag:7') == 2)) { // :619

        if (rand_n(2) == 0) { // :621
          await era.printAndWait(`「${sc()}对自慰已经很擅长了！」`); // :622
        } else { // :623
          await era.printAndWait(`「这个身体已经快要研究透了呢」`); // :624
        } // :625
        // CFLAG:304  = 7（变量语义：CFLAG 族，304） // :626
        era.set(`cflag:${target}:304`, 7); // :626

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:304`) <= 5 || era.get('flag:7') == 2)) { // :628
        await era.printAndWait(`「请看、想吞下你阴茎的性器躁动得没办法了呢」`); // :629
        // CFLAG:304  = 6（变量语义：CFLAG 族，304） // :630
        era.set(`cflag:${target}:304`, 6); // :630

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:304`) <= 4 || era.get('flag:7') == 2)) { // :632

        if (rand_n(3) == 0) { // :634
          await era.printAndWait(`「来吧……拜托了、都一边自慰一边求你了啊」`); // :635
        } else if (rand_n(2) == 0) { // :636
          await era.printAndWait(`「因为实在太想你了、%阴核(TARGET)%好像都快磨破了呢」`); // :637
        } else { // :638
          await era.printAndWait(`「已经习惯了呢、%阴核(TARGET)%已经元气十足地勃起来了哦」`); // :639
        } // :640
        // CFLAG:304  = 5（变量语义：CFLAG 族，304） // :641
        era.set(`cflag:${target}:304`, 5); // :641

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:31`) < 3 && (era.get(`cflag:${target}:304`) <= 3 || era.get('flag:7') == 2)) { // :643

        if (rand_n(2) == 0) { // :645
          await era.printAndWait(`「自慰什么的很正常啊。研究的间隙也会想去做呢」`); // :646
        } else { // :647
          await era.printAndWait(`「嘿欸、你还有这样的癖好呢」`); // :648
        } // :649
        // CFLAG:304  = 4（变量语义：CFLAG 族，304） // :650
        era.set(`cflag:${target}:304`, 4); // :650

      } else if (era.get(`mark:${target}:2`) == 3 &&era.get(`abl:${target}:31`) >= 1 && (era.get(`cflag:${target}:304`) <= 2 || era.get('flag:7') == 2)) { // :652

        if (rand_n(2) == 0) { // :654
          await era.printAndWait(`「啊～、啊～……好爽～……」`); // :655
        } else { // :656
          await era.printAndWait(`「自慰过度%阴核(TARGET)%可能会肥大化的……」`); // :657
        } // :658
        // CFLAG:304  = 3（变量语义：CFLAG 族，304） // :659
        era.set(`cflag:${target}:304`, 3); // :659

      } else if (era.get(`cflag:${target}:304`) <= 1 || era.get('flag:7') == 2) { // :661

        if (rand_n(2) == 0) { // :663
          await era.printAndWait(`「无意义的行为呢……白白浪费脑细胞」`); // :664
        } else { // :665
          await era.printAndWait(`「没有收益的行为呢、没有意义」`); // :666
        } // :667
        // CFLAG:304  = 2（变量语义：CFLAG 族，304） // :668
        era.set(`cflag:${target}:304`, 2); // :668
      } // :669
      return 0; // :670
    } // :671
  } // :672




  if (era_flag.selectcom == 5) { // :677

    if (era.get(`cflag:${target}:306`) == 0) { // :679

      if (era.get(`talent:${target}:85`) == 1) { // :681
        await era.printAndWait(`「这么喜欢胸部……你是小孩子吗」`); // :682

      } else { // :684
        await era.printAndWait(`「一般意义上说乳头并不是性感带哦」`); // :685
      } // :686
      // CFLAG:TARGET:306  = 1（变量语义：CFLAG 族，TARGET:306） // :687
      era.set(`cflag:${target}:306`, 1); // :687
      return 0; // :688

    } else { // :690

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:306`) <= 4 || era.get('flag:7') == 2)) { // :692
        if (rand_n(2) == 0) { // :693
          await era.printAndWait(`「连胸部也成性器了你打算怎么样嘛♪」`); // :694
        } else { // :695
          await era.printAndWait(`「呵呵、发生幼儿退化现象了吗……？」`); // :696
        } // :697
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :698
        era.set(`cflag:${target}:306`, 5); // :698

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:306`) <= 3 || era.get('flag:7') == 2)) { // :700
        if (rand_n(2) == 0) { // :701
          await era.printAndWait(`「胸部已经完全被开发好了呢。都是你的错呢」`); // :702
        } else { // :703
          await era.printAndWait(`「这就是、母性萌发的现象吗……」`); // :704
        } // :705
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :706
        era.set(`cflag:${target}:306`, 4); // :706

      } else if (era.get(`abl:${target}:1`) >= 3 && (era.get(`cflag:${target}:306`) <= 2 || era.get('flag:7') == 2)) { // :708
        await era.printAndWait(`「胸部……乳头好有感觉啊～！」`); // :709
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :710
        era.set(`cflag:${target}:306`, 3); // :710

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :712
        await era.printAndWait(`「果然呢、胸部一点感觉都没有」`); // :713
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :714
        era.set(`cflag:${target}:306`, 2); // :714
      } // :715
      return 0; // :716
    } // :717
  } // :718




  if (era_flag.selectcom == 6) { // :723

    if (era.get(`cflag:${target}:307`) == 0 && era.get('tflag:13')) { // :725

      if (era.get(`talent:${target}:76`) == 1 && era_flag.assiplay == 0 && era.get(`tequip:${target}:89`) == 0 && era.get(`tequip:${target}:90`) == 0) { // :727
        await era.printAndWait(`「初吻什么的、感伤的感情是不必要的……不过还不错」`); // :728

      } else if (era.get(`talent:${target}:85`) == 1 && era_flag.assiplay == 0 && era.get(`tequip:${target}:89`) == 0 && era.get(`tequip:${target}:90`) == 0) { // :730
        await era.printAndWait(`「今天是和你的纪念日呢♪」`); // :731

      } else { // :733
        await era.printAndWait(`「初吻什么的、带上感情是无意义的」`); // :734
      } // :735
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :736
      era.set(`cflag:${target}:307`, 1); // :736
      return 0; // :737

    } else if (era.get(`cflag:${target}:307`) == 0) { // :739

      if (era.get(`talent:${target}:76`) == 1) { // :741
        await era.printAndWait(`「通过唾液交换来做性爱的相性确认……你合格了哦♪」`); // :742

      } else if (era.get(`talent:${target}:85`) == 1) { // :744
        await era.printAndWait(`「终于可以和你做唾液交换了呢♪」`); // :745

      } else { // :747
        await era.printAndWait(`「既没有气氛也没有技巧……0分呢」`); // :748
      } // :749
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :750
      era.set(`cflag:${target}:307`, 1); // :750
      return 0; // :751

    } else { // :753

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:307`) <= 4 || era.get('flag:7') == 2)) { // :755
        await era.printAndWait(`「更多的交换唾液吧……你的体液、想要更多」`); // :756
        if (era.get(`talent:${target}:种族`) == 2) { // :757

          await era.printAndWait(`${target_name}虽然闭着眼、但耳朵却立了起来bikobiko的动着。`); // :759
        } // :760
        // CFLAG:307  = 5（变量语义：CFLAG 族，307） // :761
        era.set(`cflag:${target}:307`, 5); // :761

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:307`) <= 3 || era.get('flag:7') == 2)) { // :763
        await era.printAndWait(`「把你的全部……都给${sc()}吧」`); // :764
        if (era.get(`talent:${target}:种族`) == 2) { // :765

          await era.printAndWait(`${target_name}虽然闭着眼、但耳朵却立了起来bikobiko的动着。`); // :767
        } // :768
        // CFLAG:307  = 4（变量语义：CFLAG 族，307） // :769
        era.set(`cflag:${target}:307`, 4); // :769

      } else if (era.get(`abl:${target}:10`) >=2 && (era.get(`cflag:${target}:307`) <= 2 || era.get('flag:7') == 2)) { // :771
        await era.printAndWait(`「好吧、体液交换这种程度的事情没有问题」`); // :772
        // CFLAG:307  = 3（变量语义：CFLAG 族，307） // :773
        era.set(`cflag:${target}:307`, 3); // :773

      } else if (era.get(`cflag:${target}:307`) <= 1 || era.get('flag:7') == 2) { // :775
        await era.printAndWait(`「呜～……你、在磨牙吗？」`); // :776
        // CFLAG:307  = 2（变量语义：CFLAG 族，307） // :777
        era.set(`cflag:${target}:307`, 2); // :777
      } // :778
      return 0; // :779
    } // :780
  } // :781




  if (era_flag.selectcom == 7) { // :786

    if (era.get(`cflag:${target}:308`) == 0) { // :788

      if (era.get(`talent:${target}:76`) == 1) { // :790
        await era.printAndWait(`「${sc()}的大受欢迎的地方、想被更多的看着呢♪」`); // :791

      } else if (era.get(`talent:${target}:85`) == 1) { // :793
        await era.printAndWait(`「不对${sc()}的重要的地方、来个素描吗？」`); // :794

      } else { // :796
        await era.printAndWait(`「唔～、做出如此羞人的姿势什么的……」`); // :797
      } // :798
      // CFLAG:TARGET:308  = 1（变量语义：CFLAG 族，TARGET:308） // :799
      era.set(`cflag:${target}:308`, 1); // :799
      return 0; // :800

    } else { // :802

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:308`) <= 4 || era.get('flag:7') == 2)) { // :804
        await era.printAndWait(`「怎么样、${sc()}性器的开发情况……♪　想让阴核变的多大呢？」`); // :805
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :806
        era.set(`cflag:${target}:306`, 5); // :806

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:308`) <= 3 || era.get('flag:7') == 2)) { // :808
        await era.printAndWait(`「今天也做了记录呢。来展示一下${sc()}的性器发生了什么样的变化吧」`); // :809
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :810
        era.set(`cflag:${target}:306`, 4); // :810

      } else if (era.get(`abl:${target}:17`) >= 3 && (era.get(`cflag:${target}:308`) <= 2 || era.get('flag:7') == 2)) { // :812
        await era.printAndWait(`「唔……这可真是、羞耻心都被引出来了……呐」`); // :813
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :814
        era.set(`cflag:${target}:306`, 3); // :814

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :816
        await era.printAndWait(`「被迫作出这种屈辱的姿势……但${sc()}不得不屈服呢」`); // :817
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :818
        era.set(`cflag:${target}:306`, 2); // :818
      } // :819
      return 0; // :820
    } // :821
  } // :822




  if (era_flag.selectcom == 8) { // :827

    if (era.get(`cflag:${target}:309`) == 0) { // :829

      if (era.get(`talent:${target}:76`) == 1) { // :831
        await era.printAndWait(`「你的指功究竟如何呢？　很期待呢」`); // :832

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 1) { // :834
        await era.printAndWait(`「想通过你的手、来做个彻底的放松呢」`); // :835

      } else { // :837
        await era.printAndWait(`「手指伸进去的地方……只会感到恶心呢」`); // :838
      } // :839
      // CFLAG:TARGET:309  = 1（变量语义：CFLAG 族，TARGET:309） // :840
      era.set(`cflag:${target}:309`, 1); // :840
      return 0; // :841

    } else { // :843

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:309`) <= 4 || era.get('flag:7') == 2)) { // :845
        await era.printAndWait(`「里面想更多地被来回搅动呢、呼～……咕～、真不错呢」`); // :846
        // CFLAG:309  = 5（变量语义：CFLAG 族，309） // :847
        era.set(`cflag:${target}:309`, 5); // :847

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:309`) <= 3 || era.get('flag:7') == 2)) { // :849
        await era.printAndWait(`「你的手……体贴入微的、一直在${sc()}很舒服的地方进攻着呢……♪」`); // :850
        // CFLAG:309  = 4（变量语义：CFLAG 族，309） // :851
        era.set(`cflag:${target}:309`, 4); // :851

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:309`) <= 2 || era.get('flag:7') == 2)) { // :853
        await era.printAndWait(`「咕～……阴道、起反应了……」`); // :854
        // CFLAG:309  = 3（变量语义：CFLAG 族，309） // :855
        era.set(`cflag:${target}:309`, 3); // :855

      } else if (era.get(`cflag:${target}:309`) <= 1 || era.get('flag:7') == 2) { // :857
        await era.printAndWait(`「额……好恶心……」`); // :858
        // CFLAG:309  = 2（变量语义：CFLAG 族，309） // :859
        era.set(`cflag:${target}:309`, 2); // :859
      } // :860
      return 0; // :861
    } // :862
  } // :863




  if (era_flag.selectcom == 9) { // :868

    if (era.get(`cflag:${target}:310`) == 0) { // :870

      if (era.get(`talent:${target}:76`) == 1) { // :872
        await era.printAndWait(`「想舔排泄器官吗……真是变态♪」`); // :873

      } else if (era.get(`talent:${target}:85`) == 1) { // :875
        await era.printAndWait(`「那个地方细菌很多呢……真的可以吗」`); // :876

      } else { // :878
        await era.printAndWait(`「呀啊啊、住手！！」`); // :879
      } // :880
      // CFLAG:TARGET:310  = 1（变量语义：CFLAG 族，TARGET:310） // :881
      era.set(`cflag:${target}:310`, 1); // :881
      return 0; // :882

    } else { // :884

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:310`) <= 4 || era.get('flag:7') == 2)) { // :886
        await era.printAndWait(`「${sc()}的肛门、被竖着分开了呢……？　想被更多的好好舔舐呢♪」`); // :887
        // CFLAG:310  = 5（变量语义：CFLAG 族，310） // :888
        era.set(`cflag:${target}:310`, 5); // :888

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:310`) <= 3 || era.get('flag:7') == 2)) { // :890
        await era.printAndWait(`「被舔着排泄器官……也还不坏嘛♪」`); // :891
        // CFLAG:310  = 4（变量语义：CFLAG 族，310） // :892
        era.set(`cflag:${target}:310`, 4); // :892

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:310`) <= 2 || era.get('flag:7') == 2)) { // :894
        await era.printAndWait(`「咕～、随便你吧……要舔排泄器官也行……嗯啊～」`); // :895
        // CFLAG:310  = 3（变量语义：CFLAG 族，310） // :896
        era.set(`cflag:${target}:310`, 3); // :896

      } else if (era.get(`cflag:${target}:310`) <= 1 || era.get('flag:7') == 2) { // :898
        await era.printAndWait(`「尽做些傻事呢……嗯～」`); // :899
        // CFLAG:310  = 2（变量语义：CFLAG 族，310） // :900
        era.set(`cflag:${target}:310`, 2); // :900
      } // :901
      return 0; // :902
    } // :903
  } // :904




  if (era_flag.selectcom == 10) { // :909

    if (era.get(`cflag:${target}:311`) == 0) { // :911

      if (era.get(`talent:${target}:76`) == 1) { // :913
        await era.printAndWait(`「有趣的道具呢、快点使用吧♪」`); // :914

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`talent:${target}:85`) == 1) { // :916
        await era.printAndWait(`「这道具还挺有意思的呢……真的」`); // :917

      } else { // :919
        await era.printAndWait(`「这、这嗡嗡震动的玩意儿是什么啊！？」`); // :920
      } // :921
      // CFLAG:TARGET:311  = 1（变量语义：CFLAG 族，TARGET:311） // :922
      era.set(`cflag:${target}:311`, 1); // :922
      return 0; // :923

    } else { // :925

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:311`) <= 4 || era.get('flag:7') == 2)) { // :927
        await era.printAndWait(`「啊啊啊……淫核好麻……不错嘛、这个～」`); // :928
        // CFLAG:311  = 5（变量语义：CFLAG 族，311） // :929
        era.set(`cflag:${target}:311`, 5); // :929

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:311`) <= 3 || era.get('flag:7') == 2)) { // :931
        await era.printAndWait(`「呼～……咕～、身体……放松下来了呢」`); // :932
        // CFLAG:311  = 4（变量语义：CFLAG 族，311） // :933
        era.set(`cflag:${target}:311`, 4); // :933

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:311`) <= 2 || era.get('flag:7') == 2)) { // :935
        await era.printAndWait(`「道具的性能已经清楚了……但是、可以不把这个按在阴核上吗」`); // :936
        // CFLAG:311  = 3（变量语义：CFLAG 族，311） // :937
        era.set(`cflag:${target}:311`, 3); // :937

      } else if (era.get(`cflag:${target}:311`) <= 1 || era.get('flag:7') == 2) { // :939
        await era.printAndWait(`「跟往常一样……嗡嗡的震动着呢。真想看看开发者是长什么样的呢」`); // :940
        // CFLAG:311  = 2（变量语义：CFLAG 族，311） // :941
        era.set(`cflag:${target}:311`, 2); // :941
      } // :942
      return 0; // :943
    } // :944
  } // :945





  if (era_flag.selectcom == 11 && era.get(`tequip:${target}:11`)) { // :951

    if (era.get(`cflag:${target}:312`) == 0) { // :953

      if (era.get(`talent:${target}:0`) == 1) { // :955

        if (era.get(`talent:${target}:76`) == 1) { // :957
          await era.printAndWait(`「原来如此、将寄生虫家畜化吗。有意思……对于献出处女来说是个不错的研究对象呢♪」`); // :958

        } else if (era.get(`talent:${target}:85`) == 1) { // :960
          await era.printAndWait(`「比起这种寄生虫变异体来说、还是更想被你夺走处女呢」`); // :961

        } else { // :963
          await era.printAndWait(`「呜～、寄生虫的变异体吗……哼、才不可惜处女什么的呢……」`); // :964
          await era.printAndWait(`虽然嘴上这么说着、${target_name}的腰还是颤抖不已`); // :965
        } // :966

      } else { // :968

        if (era.get(`talent:${target}:76`) == 1) { // :970
          await era.printAndWait(`「原来如此、将寄生虫家畜化吗。有意思……真想快点放进阴道品尝一下滋味呢♪」`); // :971

        } else if (era.get(`talent:${target}:85`) == 1) { // :973
          await era.printAndWait(`「寄生虫的变异体吗。交给我吧。用${sc()}的阴道来试试看吧」`); // :974

        } else { // :976
          await era.printAndWait(`「呜～、寄生虫的变异体吗……无耻！」`); // :977
        } // :978
      } // :979
      // CFLAG:312  = 1（变量语义：CFLAG 族，312） // :980
      era.set(`cflag:${target}:312`, 1); // :980
      return 0; // :981

    } else { // :983

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:312`) <= 4 || era.get('flag:7') == 2)) { // :985
        if (era.get(`talent:${target}:190`) == 1) { // :986

          await era.printAndWait(`「这就是壶虫的寄生状态吗……嗯～、每次产卵、都会摩擦阴道……♪　要对这个着迷了呢」`); // :988
        } else { // :989
          await era.printAndWait(`「确认壶虫已进入……嗯～、摩擦着阴道……好舒服♪」`); // :990
        } // :991
        // CFLAG:312  = 5（变量语义：CFLAG 族，312） // :992
        era.set(`cflag:${target}:312`, 5); // :992

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:312`) <= 3 || era.get('flag:7') == 2)) { // :994
        if (era.get(`talent:${target}:190`) == 1) { // :995

          await era.printAndWait(`「壶虫寄生状态有影响敏感度的效果……嗯～、真有意思」`); // :997
        } else { // :998
          await era.printAndWait(`「壶虫的触手吗……都伸到子宫口了呢……嗯～」`); // :999
        } // :1000
        // CFLAG:312  = 4（变量语义：CFLAG 族，312） // :1001
        era.set(`cflag:${target}:312`, 4); // :1001

      } else if (era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:312`) <= 2 || era.get('flag:7') == 2)) { // :1003
        await era.printAndWait(`「区区寄生生物、${sc()}是不会输的。被这种下等生物……」`); // :1004
        // CFLAG:312  = 3（变量语义：CFLAG 族，312） // :1005
        era.set(`cflag:${target}:312`, 3); // :1005

      } else if (era.get(`cflag:${target}:312`) <= 1 || era.get('flag:7') == 2) { // :1007
        await era.printAndWait(`「你不借助这种下等生物之手就不行吗？」`); // :1008
        // CFLAG:312  = 2（变量语义：CFLAG 族，312） // :1009
        era.set(`cflag:${target}:312`, 2); // :1009
      } // :1010
      return 0; // :1011
    } // :1012

  } else if (era_flag.selectcom == 11 && era.get(`tequip:${target}:11`) == 0) { // :1014

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:372`) < 3 || era.get('flag:7') == 2)) { // :1016
      await era.printAndWait(`「实验已经结束了吗？　再多蹂躙一会儿也可以哦」`); // :1017
      // CFLAG:372  = 3（变量语义：CFLAG 族，372） // :1018
      era.set(`cflag:${target}:372`, 3); // :1018

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:372`) < 2 || era.get('flag:7') == 2)) { // :1020
      await era.printAndWait(`「……真有意思呢。下次再研究看看吧」`); // :1021
      // CFLAG:372  = 2（变量语义：CFLAG 族，372） // :1022
      era.set(`cflag:${target}:372`, 2); // :1022

    } else if (era.get(`cflag:${target}:372`) < 1 || era.get('flag:7') == 2) { // :1024
      await era.printAndWait(`「……快点把这恶心的寄生生物丢回培养槽里去啊～」`); // :1025
      // CFLAG:372  = 1（变量语义：CFLAG 族，372） // :1026
      era.set(`cflag:${target}:372`, 1); // :1026
    } // :1027
    return 0; // :1028
  } // :1029




  if (era_flag.selectcom == 12) { // :1034

    if (era.get(`cflag:${target}:313`) == 0) { // :1036

      if (era.get(`talent:${target}:76`) == 1) { // :1038
        await era.printAndWait(`「有趣的道具呢！　动力有多少？」`); // :1039

      } else if (era.get(`talent:${target}:85`) == 1) { // :1041
        await era.printAndWait(`「这个是……什么用途的道具呢？　淫具……吗？」`); // :1042

      } else { // :1044
        await era.printAndWait(`「哼、拿着这种道具到底意欲何为？」`); // :1045
      } // :1046
      // CFLAG:313  = 1（变量语义：CFLAG 族，313） // :1047
      era.set(`cflag:${target}:313`, 1); // :1047
      return 0; // :1048

    } else { // :1050

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:313`) <= 4 || era.get('flag:7') == 2)) { // :1052
        await era.printAndWait(`「啊啊啊……好棒、发麻了……下次借给我吧……♪」`); // :1053
        // CFLAG:313  = 5（变量语义：CFLAG 族，313） // :1054
        era.set(`cflag:${target}:313`, 5); // :1054

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:313`) <= 3 || era.get('flag:7') == 2)) { // :1056
        await era.printAndWait(`「研究疲劳的时候使用……有不错的保健效果呢」`); // :1057
        // CFLAG:313  = 4（变量语义：CFLAG 族，313） // :1058
        era.set(`cflag:${target}:313`, 4); // :1058

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:313`) <= 2 || era.get('flag:7') == 2)) { // :1060
        await era.printAndWait(`「咕～、机械的振动……一直传到腰骨上了……嗯」`); // :1061
        // CFLAG:313  = 3（变量语义：CFLAG 族，313） // :1062
        era.set(`cflag:${target}:313`, 3); // :1062

      } else if (era.get(`cflag:${target}:313`) <= 1 || era.get('flag:7') == 2) { // :1064
        await era.printAndWait(`「这种程度的、脑内物质分泌……对我没有效果呢……嗯」`); // :1065
        // CFLAG:313  = 2（变量语义：CFLAG 族，313） // :1066
        era.set(`cflag:${target}:313`, 2); // :1066
      } // :1067
      return 0; // :1068
    } // :1069
  } // :1070





  if (era_flag.selectcom == 13 && era.get(`tequip:${target}:13`)) { // :1076

    if (era.get(`cflag:${target}:314`) == 0) { // :1078

      if (era.get(`talent:${target}:76`) == 1) { // :1080
        await era.printAndWait(`「好大的寄生虫呢……难道说、要把这个放进去？　好期待呢♪」`); // :1081

      } else if (era.get(`talent:${target}:85`) == 1) { // :1083
        await era.printAndWait(`「在肠内生活的寄生虫吗……有意思的生物」`); // :1084

      } else { // :1086
        await era.printAndWait(`「原始的寄生虫吗……哼、据说体液有催淫作用呢……」`); // :1087
      } // :1088
      // CFLAG:TARGET:314  = 1（变量语义：CFLAG 族，TARGET:314） // :1089
      era.set(`cflag:${target}:314`, 1); // :1089
      return 0; // :1090

    } else { // :1092

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 6 || era.get('flag:7') == 2)) { // :1094
        await era.printAndWait(`「咕呜～、直肠…･･･被钻进去了～。停、停不下来了……♪」`); // :1095
        // CFLAG:314  = 6（变量语义：CFLAG 族，314） // :1096
        era.set(`cflag:${target}:314`, 6); // :1096

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:314`) <= 5 || era.get('flag:7') == 2)) { // :1098
        await era.printAndWait(`「在直肠内运动着呢……据说有催淫作用、快点生效吧」`); // :1099
        // CFLAG:314  = 6（变量语义：CFLAG 族，314） // :1100
        era.set(`cflag:${target}:314`, 6); // :1100

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 4 || era.get('flag:7') == 2)) { // :1102
        await era.printAndWait(`「被这样的、下等生物……挖掘着直肠、有感觉了……嗯♪」`); // :1103
        // CFLAG:314  = 5（变量语义：CFLAG 族，314） // :1104
        era.set(`cflag:${target}:314`, 5); // :1104

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:314`) <= 3 || era.get('flag:7') == 2)) { // :1106
        await era.printAndWait(`「这样很难有感觉呢……不过据说寄生虫对健康有益」`); // :1107
        // CFLAG:314  = 4（变量语义：CFLAG 族，314） // :1108
        era.set(`cflag:${target}:314`, 4); // :1108

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:314`) <= 2 || era.get('flag:7') == 2)) { // :1110
        await era.printAndWait(`「这、这种下等生物……嗯」`); // :1111
        // CFLAG:314  = 3（变量语义：CFLAG 族，314） // :1112
        era.set(`cflag:${target}:314`, 3); // :1112

      } else if (era.get(`cflag:${target}:314`) <= 1 || era.get('flag:7') == 2) { // :1114
        await era.printAndWait(`「好恶心的寄生虫……就好像你一样」`); // :1115
        // CFLAG:314  = 2（变量语义：CFLAG 族，314） // :1116
        era.set(`cflag:${target}:314`, 2); // :1116
      } // :1117
      return 0; // :1118
    } // :1119

  } else if (era_flag.selectcom == 13 && era.get(`tequip:${target}:13`) == 0) { // :1121

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:374`) < 4 || era.get('flag:7') == 2)) { // :1123
      await era.printAndWait(`「肛门括约筋变的松弛下来了呢……♪　还想被继续开发呢」`); // :1124
      // CFLAG:374  = 4（变量语义：CFLAG 族，374） // :1125
      era.set(`cflag:${target}:374`, 4); // :1125

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:374`) < 3 || era.get('flag:7') == 2)) { // :1127
      await era.printAndWait(`「异物感不见了、有点寂寞呢」`); // :1128
      // CFLAG:374  = 3（变量语义：CFLAG 族，374） // :1129
      era.set(`cflag:${target}:374`, 3); // :1129

    } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:374`) < 2 || era.get('flag:7') == 2)) { // :1131
      await era.printAndWait(`「咕呜～、肛门括约筋……麻麻的～……」`); // :1132
      // CFLAG:374  = 2（变量语义：CFLAG 族，374） // :1133
      era.set(`cflag:${target}:374`, 2); // :1133

    } else if (era.get(`cflag:${target}:374`) < 1 || era.get('flag:7') == 2) { // :1135
      await era.printAndWait(`「哈啊～……真是恶心的生物……」`); // :1136
      // CFLAG:374  = 1（变量语义：CFLAG 族，374） // :1137
      era.set(`cflag:${target}:374`, 1); // :1137
    } // :1138
    return 0; // :1139
  } // :1140





























































































































































































































  if (era_flag.selectcom == 19 && era.get(`tequip:${target}:19`)) { // :1362

    if (era.get(`cflag:${target}:320`) == 0) { // :1364

      if (era.get(`talent:${target}:76`) == 1) { // :1366
        await era.printAndWait(`「要把这个全部放进去吗？　好期待呢」`); // :1367

      } else if (era.get(`talent:${target}:85`) == 1) { // :1369
        await era.printAndWait(`「一个一个的好好放进去哦」`); // :1370

      } else { // :1372
        await era.printAndWait(`「这样变态的器具……难以理解呢」`); // :1373
      } // :1374
      // CFLAG:TARGET:320  = 1（变量语义：CFLAG 族，TARGET:320） // :1375
      era.set(`cflag:${target}:320`, 1); // :1375
      return 0; // :1376

    } else { // :1378

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 6 || era.get('flag:7') == 2)) { // :1380
        await era.printAndWait(`「嗯～……一个接一个的、放进去了呢……好期待拔出来的时候呢」`); // :1381
        if (era.get(`talent:${target}:种族`) == 2) { // :1382

          await era.printAndWait(`${target_name}的屁股后面、另一条下流的尾巴摇动着。`); // :1384
        } // :1385
        // CFLAG:320  = 7（变量语义：CFLAG 族，320） // :1386
        era.set(`cflag:${target}:320`, 7); // :1386

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:320`) <= 5 || era.get('flag:7') == 2)) { // :1388
        await era.printAndWait(`「这可真是……有趣的道具呢。肚子里面塞得满满的呢」`); // :1389
        // CFLAG:320  = 6（变量语义：CFLAG 族，320） // :1390
        era.set(`cflag:${target}:320`, 6); // :1390

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 4 || era.get('flag:7') == 2)) { // :1392
        await era.printAndWait(`「呼呜～…哈啊～、全、全部放进去了吧？　想被一口气拔出来呢」`); // :1393
        if (era.get(`talent:${target}:种族`) == 2) { // :1394

          await era.printAndWait(`${target_name}的屁股后面、另一条下流的尾巴摇动着。`); // :1396
        } // :1397
        // CFLAG:320  = 5（变量语义：CFLAG 族，320） // :1398
        era.set(`cflag:${target}:320`, 5); // :1398

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:320`) <= 3 || era.get('flag:7') == 2)) { // :1400
        await era.printAndWait(`「想一个一个地被你的手放进去呢」`); // :1401
        // CFLAG:320  = 4（变量语义：CFLAG 族，320） // :1402
        era.set(`cflag:${target}:320`, 4); // :1402

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:320`) <= 2 || era.get('flag:7') == 2)) { // :1404
        await era.printAndWait(`「嗯～、咕～……哈啊～、肛、肛门……」`); // :1405
        // CFLAG:320  = 3（变量语义：CFLAG 族，320） // :1406
        era.set(`cflag:${target}:320`, 3); // :1406

      } else if (era.get(`cflag:${target}:320`) <= 1 || era.get('flag:7') == 2) { // :1408
        await era.printAndWait(`「对这样的器具拿出干劲什么的……做不到呢」`); // :1409
        // CFLAG:320  = 2（变量语义：CFLAG 族，320） // :1410
        era.set(`cflag:${target}:320`, 2); // :1410
      } // :1411
      return 0; // :1412
    } // :1413

  } else if (era_flag.selectcom == 19 && era.get(`tequip:${target}:19`) == 0) { // :1415

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:379`) < 4 || era.get('flag:7') == 2)) { // :1417
      await era.printAndWait(`「嗯哈啊～♪　这个、太棒了……♪　滑溜溜的拔出来了～」`); // :1418
      // CFLAG:379  = 4（变量语义：CFLAG 族，379） // :1419
      era.set(`cflag:${target}:379`, 4); // :1419

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:379`) < 3 || era.get('flag:7') == 2)) { // :1421
      await era.printAndWait(`「拔出来了……总觉得、好像在产卵呢」`); // :1422
      // CFLAG:379  = 3（变量语义：CFLAG 族，379） // :1423
      era.set(`cflag:${target}:379`, 3); // :1423

    } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:379`) < 2 || era.get('flag:7') == 2)) { // :1425
      await era.printAndWait(`「嗯～……咕～、哈啊～、哈啊～……」`); // :1426
      // CFLAG:379  = 2（变量语义：CFLAG 族，379） // :1427
      era.set(`cflag:${target}:379`, 2); // :1427

    } else if (era.get(`cflag:${target}:379`) < 1 || era.get('flag:7') == 2) { // :1429
      await era.printAndWait(`「结束了吗……？　只感到难受呢」`); // :1430
      // CFLAG:379  = 1（变量语义：CFLAG 族，379） // :1431
      era.set(`cflag:${target}:379`, 1); // :1431
    } // :1432
    return 0; // :1433
  } // :1434




  if (era_flag.selectcom == 20) { // :1439

    if (era.get(`cflag:${target}:321`) == 0) { // :1441

      if (era.get(`talent:${target}:0`) == 1) { // :1443

        if (era.get(`talent:${target}:76`) == 1) { // :1445
          await era.printAndWait(`「这个我知道、是叫做深度授精式吧？　请让我头一次的受精吧」`); // :1446

        } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:10`) >= 5) { // :1448
          await era.printAndWait(`「能把处女献给你……${sc()}觉得好光荣」`); // :1449

        } else { // :1451
          await era.printAndWait(`「只是粘膜被弄破了而已……在生物学意义上、什么变化都算不上」`); // :1452
        } // :1453

      } else { // :1455
        if (era.get(`talent:${target}:76`) == 1) { // :1456
          await era.printAndWait(`「这个我知道、是叫做深度授精式吧？　请让我受精吧」`); // :1457

        } else if (era.get(`talent:${target}:85`) == 1) { // :1459
          await era.printAndWait(`「因为怀孕而产生的母体变化……想试试看呢」`); // :1460

        } else { // :1462
          await era.printAndWait(`「只有怀孕……只有怀孕千万不要啊！」`); // :1463
        } // :1464
      } // :1465
      // CFLAG:321  = 1（变量语义：CFLAG 族，321） // :1466
      era.set(`cflag:${target}:321`, 1); // :1466
      return 0; // :1467

    } else { // :1469

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:321`) <= 5 || era.get('flag:7') == 2)) { // :1471
        await era.printAndWait(`「想被你弄怀孕呢……子宫已经躁动的不得了了呢」`); // :1472
        // CFLAG:321  = 6（变量语义：CFLAG 族，321） // :1473
        era.set(`cflag:${target}:321`, 6); // :1473

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:321`) <= 4 || era.get('flag:7') == 2)) { // :1475
        await era.printAndWait(`「想怀上和你的孩子呢……把大量的精子射进来吧」`); // :1476
        // CFLAG:321  = 5（变量语义：CFLAG 族，321） // :1477
        era.set(`cflag:${target}:321`, 5); // :1477

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:321`) <= 3 || era.get('flag:7') == 2)) { // :1479
        await era.printAndWait(`「感、感觉到了……子宫在期盼着受精……？　难以置信……」`); // :1480
        // CFLAG:321  = 4（变量语义：CFLAG 族，321） // :1481
        era.set(`cflag:${target}:321`, 4); // :1481

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:321`) <= 2 || era.get('flag:7') == 2)) { // :1483
        await era.printAndWait(`「我知道了……${sc()}的身体、随便你怎么使用吧」`); // :1484
        // CFLAG:321  = 3（变量语义：CFLAG 族，321） // :1485
        era.set(`cflag:${target}:321`, 3); // :1485

      } else if (era.get(`cflag:${target}:321`) <= 1 || era.get('flag:7') == 2) { // :1487
        await era.printAndWait(`「住、住手……敢在${sc()}的身体里射精可饶不了你哦！」`); // :1488
        // CFLAG:321  = 2（变量语义：CFLAG 族，321） // :1489
        era.set(`cflag:${target}:321`, 2); // :1489
      } // :1490
      return 0; // :1491
    } // :1492
  } // :1493




  if (era_flag.selectcom == 21) { // :1498

    if (era.get(`cflag:${target}:322`) == 0) { // :1500

      if (era.get(`talent:${target}:0`) == 1) { // :1502

        if (era.get(`talent:${target}:76`) == 1) { // :1504
          if (era.get(`talent:${target}:种族`) == 2) { // :1505

            await era.printAndWait(`「这是适合野兽的姿态呢、不觉得有些变态吗？」`); // :1507
          } else { // :1508
            await era.printAndWait(`「好像野生动物似的呢……这样的第一次、不觉得太过变态了吗？」`); // :1509
          } // :1510

        } else if (era.get(`talent:${target}:85`) == 1) { // :1512
          if (era.get(`talent:${target}:种族`) == 2) { // :1513

            await era.printAndWait(`「这是适合野兽的姿态呢……那么就这样让我怀孕吧♪」`); // :1515
          } else { // :1516
            await era.printAndWait(`「第一次就是这种野蛮的体位吗……那么就这样让我怀孕吧♪」`); // :1517
          } // :1518



        } else { // :1522
          await era.printAndWait(`「屈辱啊……咕呜、屈辱啊！」`); // :1523
        } // :1524

      } else { // :1526

        if (era.get(`talent:${target}:76`) == 1) { // :1528
          await era.printAndWait(`「好像野生动物似的呢、你喜欢这样吗？」`); // :1529

        } else if (era.get(`talent:${target}:85`) == 1) { // :1531
          await era.printAndWait(`「这是很适合受孕的体位呢」`); // :1532

        } else { // :1534
          await era.printAndWait(`「真野蛮……跟你真配呢」`); // :1535
        } // :1536
      } // :1537
      // CFLAG:322  = 1（变量语义：CFLAG 族，322） // :1538
      era.set(`cflag:${target}:322`, 1); // :1538
      return 0; // :1539

    } else if (era.get(`talent:${target}:153`) == 1) { // :1541

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:322`) <= 5 || era.get('flag:7') == 2)) { // :1543
        if (rand_n(3) == 0) { // :1544
          await era.printAndWait(`「原来还有会给怀孕的雌性授精的雄性呢……♪」`); // :1545
        } else if (rand_n(2) == 0) { // :1546
          await era.printAndWait(`「居然连孕妇都上……这样不合理的事、难道是你的兴趣吗……？　真是太棒啦……♪」`); // :1547
        } else { // :1548
          await era.printAndWait(`「再更用力地操我啊……让肚子里孩子也一起感受一下吧♪」`); // :1549
        } // :1550
        if (era.get(`talent:${target}:种族`) == 2) { // :1551

          await era.printAndWait(`${target_name}完全抛弃了狼人的的自尊心、沦为一头纯粹的母兽了。`); // :1553
        } // :1554
        // CFLAG:322  = 6（变量语义：CFLAG 族，322） // :1555
        era.set(`cflag:${target}:322`, 6); // :1555

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:322`) <= 4 || era.get('flag:7') == 2)) { // :1557
        if (rand_n(3) == 0) { // :1558
          await era.printAndWait(`「这样如同动物一般的做爱、就算怀孕也不奇怪啦♪」`); // :1559
        } else if (rand_n(2) == 0) { // :1560
          await era.printAndWait(`「把你的精液……射在……肚子里……孩子的身上吧！」`); // :1561
        } else { // :1562
          await era.printAndWait(`「这么色情的体位、还真有点不想让里面的孩子看到呢♪」`); // :1563
        } // :1564
        if (era.get(`talent:${target}:种族`) == 2) { // :1565

          await era.printAndWait(`${target_name}抛弃了狼人的的自尊心、沦为一头发情的母兽了。`); // :1567
        } // :1568
        // CFLAG:322  = 5（变量语义：CFLAG 族，322） // :1569
        era.set(`cflag:${target}:322`, 5); // :1569

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:322`) <= 3 || era.get('flag:7') == 2)) { // :1571
        await era.printAndWait(`「呜呜呜……肚子里……鸡巴……在乱撞啊……嗯啊」`); // :1572
        // CFLAG:322  = 4（变量语义：CFLAG 族，322） // :1573
        era.set(`cflag:${target}:322`, 4); // :1573

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:322`) <= 2 || era.get('flag:7') == 2)) { // :1575
        await era.printAndWait(`「太耻辱了……竟然完全无法抵抗什么的……」`); // :1576
        // CFLAG:322  = 3（变量语义：CFLAG 族，322） // :1577
        era.set(`cflag:${target}:322`, 3); // :1577

      } else if (era.get(`cflag:${target}:322`) <= 1 || era.get('flag:7') == 2) { // :1579
        await era.printAndWait(`「可恶……竟然用这种野蛮下等的体位……」`); // :1580

        // CFLAG:322  = 2（变量语义：CFLAG 族，322） // :1582
        era.set(`cflag:${target}:322`, 2); // :1582
      } // :1583
      return 0; // :1584

    } else { // :1586

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:322`) <= 5 || era.get('flag:7') == 2)) { // :1588
        if (rand_n(3) == 0) { // :1589
          await era.printAndWait(`「现在这个瞬间、只剩下雄性和雌性了哦……♪」`); // :1590
        } else if (rand_n(2) == 0) { // :1591
          await era.printAndWait(`「这种野生动物般的体位、是你的癖好吗……？　好棒……♪」`); // :1592
        } else { // :1593
          await era.printAndWait(`「再使劲点撞我的腰……让屁股肉也跳动起来吧♪」`); // :1594
        } // :1595
        if (era.get(`talent:${target}:种族`) == 2) { // :1596

          await era.printAndWait(`${target_name}已经舍弃了人狼的自豪，完全变成了一匹雌性。`); // :1598
        } // :1599
        // CFLAG:322  = 6（变量语义：CFLAG 族，322） // :1600
        era.set(`cflag:${target}:322`, 6); // :1600

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:322`) <= 4 || era.get('flag:7') == 2)) { // :1602
        if (rand_n(3) == 0) { // :1603
          await era.printAndWait(`「这般像动物似的做爱、绝对会怀……怀孕的♪」`); // :1604
        } else if (rand_n(2) == 0) { // :1605
          await era.printAndWait(`「让我确实的受孕吧……用你的精液！」`); // :1606
        } else { // :1607
          await era.printAndWait(`「就决定用这种下流的体位来受孕吧♪」`); // :1608
        } // :1609
        if (era.get(`talent:${target}:种族`) == 2) { // :1610

          await era.printAndWait(`${target_name}已经舍弃了人狼的自豪、变成了为了怀孕而发情的雌性了。`); // :1612
        } // :1613
        // CFLAG:322  = 5（变量语义：CFLAG 族，322） // :1614
        era.set(`cflag:${target}:322`, 5); // :1614

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:322`) <= 3 || era.get('flag:7') == 2)) { // :1616
        await era.printAndWait(`「呜呜～……阴茎……在里面……嘎吱嘎吱地……嗯啊～」`); // :1617
        // CFLAG:322  = 4（变量语义：CFLAG 族，322） // :1618
        era.set(`cflag:${target}:322`, 4); // :1618

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:322`) <= 2 || era.get('flag:7') == 2)) { // :1620
        await era.printAndWait(`「屈辱啊……什么也做不了……」`); // :1621
        // CFLAG:322  = 3（变量语义：CFLAG 族，322） // :1622
        era.set(`cflag:${target}:322`, 3); // :1622

      } else if (era.get(`cflag:${target}:322`) <= 1 || era.get('flag:7') == 2) { // :1624
        await era.printAndWait(`「好恨啊……被这种野蛮下流的体位……」`); // :1625

        // CFLAG:322  = 2（变量语义：CFLAG 族，322） // :1627
        era.set(`cflag:${target}:322`, 2); // :1627
      } // :1628
      return 0; // :1629
    } // :1630
  } // :1631




  if (era_flag.selectcom == 22) { // :1636
    if (era.get(`cflag:${target}:323`) == 0) { // :1637

      if (era.get(`talent:${target}:0`) == 1) { // :1639

        if (era.get(`talent:${target}:76`) == 1) { // :1641
          await era.printAndWait(`「亲个嘴吧……作为第一次的纪念呢♪」`); // :1642

        } else if (era.get(`talent:${target}:85`) == 1) { // :1644
          await era.printAndWait(`「要成为女人了呢……想被你紧紧的抱住呢」`); // :1645

        } else { // :1647
          await era.printAndWait(`「咕呜～……处女膜没了……才没什么感想呢」`); // :1648
        } // :1649

      } else { // :1651

        if (era.get(`talent:${target}:76`) == 1) { // :1653
          await era.printAndWait(`「一边亲吻一边缠在一起……真不错呢」`); // :1654

        } else if (era.get(`talent:${target}:85`) == 1) { // :1656
          await era.printAndWait(`「真想记下受精瞬间的${sc()}的脸呢」`); // :1657

        } else { // :1659
          await era.printAndWait(`「咕呜～……不要抱得这么紧……」`); // :1660
        } // :1661
      } // :1662
      // CFLAG:323  = 1（变量语义：CFLAG 族，323） // :1663
      era.set(`cflag:${target}:323`, 1); // :1663
      return 0; // :1664

    } else { // :1666

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:323`) <= 5 || era.get('flag:7') == 2)) { // :1668
        if (rand_n(3) == 0) { // :1669
          await era.printAndWait(`「被抱在你的怀里……感觉还不坏」`); // :1670
        } else if (rand_n(2) == 0) { // :1671
          await era.printAndWait(`「看到高潮脸了吗、因为你的突刺而喜悦的${sc()}的脸」`); // :1672
        } else { // :1673
          await era.printAndWait(`「嗯～……啊啊～……深深的、插我啊♪」`); // :1674
        } // :1675
        // CFLAG:323  = 6（变量语义：CFLAG 族，323） // :1676
        era.set(`cflag:${target}:323`, 6); // :1676

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:323`) <= 4 || era.get('flag:7') == 2)) { // :1678
        if (rand_n(3) == 0) { // :1679
          await era.printAndWait(`「受精了……啊啊、被你抱着受精了啊」`); // :1680
        } else if (rand_n(2) == 0) { // :1681
          await era.printAndWait(`「子宫……降下来了。因为想要你的精液、一颤一颤的呢」`); // :1682
        } else { // :1683
          await era.printAndWait(`「求你了、射精吧！　在${sc()}的里面把精液射出来吧！」`); // :1684
        } // :1685
        // CFLAG:323  = 5（变量语义：CFLAG 族，323） // :1686
        era.set(`cflag:${target}:323`, 5); // :1686

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:323`) <= 3 || era.get('flag:7') == 2)) { // :1688
        await era.printAndWait(`「咕呜……这种、感觉……不对」`); // :1689
        // CFLAG:323  = 4（变量语义：CFLAG 族，323） // :1690
        era.set(`cflag:${target}:323`, 4); // :1690

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:323`) <= 2 || era.get('flag:7') == 2)) { // :1692
        await era.printAndWait(`「服从你了吗……子宫也随你使唤了」`); // :1693
        // CFLAG:323  = 3（变量语义：CFLAG 族，323） // :1694
        era.set(`cflag:${target}:323`, 3); // :1694

      } else if (era.get(`cflag:${target}:323`) <= 1 || era.get('flag:7') == 2) { // :1696
        await era.printAndWait(`「咕呜……这样子……什么感觉、也没有。那是汗……好恶心」`); // :1697
        // CFLAG:323  = 2（变量语义：CFLAG 族，323） // :1698
        era.set(`cflag:${target}:323`, 2); // :1698
      } // :1699
      return 0; // :1700
    } // :1701
  } // :1702




  if (era_flag.selectcom == 23) { // :1707
    if (era.get(`cflag:${target}:324`) == 0) { // :1708

      if (era.get(`talent:${target}:0`) == 1) { // :1710

        if (era.get(`talent:${target}:76`) == 1) { // :1712
          await era.printAndWait(`「有镜子的话真想看一看呢……${sc()}失去处女的瞬间……」`); // :1713

        } else if (era.get(`talent:${target}:85`) == 1) { // :1715
          await era.printAndWait(`「拜托了这样可看不到你的脸啊……一定、会哭出来的」`); // :1716

        } else { // :1718
          await era.printAndWait(`「咕呜～……处女膜就这样没了……才没有什么感想呢」`); // :1719
        } // :1720

      } else { // :1722

        if (era.get(`talent:${target}:76`) == 1) { // :1724
          await era.printAndWait(`「从背后抱着吗？　好像变成小孩子了呢……」`); // :1725

        } else if (era.get(`talent:${target}:85`) == 1) { // :1727
          await era.printAndWait(`「被从背后抱住了呢、要在受精的瞬间好好接住呢……」`); // :1728

        } else { // :1730
          await era.printAndWait(`「咕呜～……不要我耳边说悄悄话……」`); // :1731
        } // :1732
      } // :1733
      // CFLAG:324  = 1（变量语义：CFLAG 族，324） // :1734
      era.set(`cflag:${target}:324`, 1); // :1734
      return 0; // :1735

    } else { // :1737

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:324`) <= 5 || era.get('flag:7') == 2)) { // :1739
        if (rand_n(3) == 0) { // :1740
          await era.printAndWait(`「感觉到了……你胸部的呼吸、从背后传过来……」`); // :1741
        } else if (rand_n(2) == 0) { // :1742
          await era.printAndWait(`「嗯～、哈啊……好深……感觉到了哦、你的蠢动……」`); // :1743
        } else { // :1744
          await era.printAndWait(`「这可真是上乘的椅子呢……嗯～、坐起来的感觉、最棒了……！」`); // :1745
        } // :1746
        // CFLAG:324  = 6（变量语义：CFLAG 族，324） // :1747
        era.set(`cflag:${target}:324`, 6); // :1747

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:324`) <= 4 || era.get('flag:7') == 2)) { // :1749
        if (rand_n(3) == 0) { // :1750
          await era.printAndWait(`「嗯啊啊啊啊～！　绝对、会受精的♪」`); // :1751
        } else if (rand_n(2) == 0) { // :1752
          await era.printAndWait(`「要、要死了……你、太激烈了……」`); // :1753
        } else { // :1754
          await era.printAndWait(`「好深……在这个深处、送出来吧……让我怀上孩子吧……欸」`); // :1755
        } // :1756
        // CFLAG:324  = 5（变量语义：CFLAG 族，324） // :1757
        era.set(`cflag:${target}:324`, 5); // :1757

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:324`) <= 3 || era.get('flag:7') == 2)) { // :1759
        await era.printAndWait(`「嗯～……嗯啊～、感、感觉到了……灼热的、情欲……」`); // :1760
        // CFLAG:324  = 4（变量语义：CFLAG 族，324） // :1761
        era.set(`cflag:${target}:324`, 4); // :1761

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:324`) <= 2 || era.get('flag:7') == 2)) { // :1763
        await era.printAndWait(`「身体归你所有之后、连心也……嗯！」`); // :1764
        // CFLAG:324  = 3（变量语义：CFLAG 族，324） // :1765
        era.set(`cflag:${target}:324`, 3); // :1765

      } else if (era.get(`cflag:${target}:324`) <= 1 || era.get('flag:7') == 2) { // :1767
        await era.printAndWait(`「这样子……什么感觉、也没有……只不过是摩擦粘膜罢了……」`); // :1768
        // CFLAG:324  = 2（变量语义：CFLAG 族，324） // :1769
        era.set(`cflag:${target}:324`, 2); // :1769
      } // :1770
      return 0; // :1771
    } // :1772
  } // :1773




  if (era_flag.selectcom == 26) { // :1778

    if (era.get(`cflag:${target}:327`) == 0) { // :1780

      if (era.get(`talent:${target}:76`) == 1) { // :1782
        await era.printAndWait(`「快请插进来吧、这个菊穴里～」`); // :1783

      } else if (era.get(`talent:${target}:85`) == 1) { // :1785
        await era.printAndWait(`「其实、前面更想要的说……」`); // :1786

      } else { // :1788
        await era.printAndWait(`「这、这么野蛮的行为……饶不了你……」`); // :1789
      } // :1790
      // CFLAG:TARGET:327  = 1（变量语义：CFLAG 族，TARGET:327） // :1791
      era.set(`cflag:${target}:327`, 1); // :1791
      return 0; // :1792

    } else { // :1794

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 6 || era.get('flag:7') == 2)) { // :1796
        if (rand_n(3) == 0) { // :1797
          await era.printAndWait(`「啊啊啊～、肛、肛门、完全、变成性器了！　啊啊啊～」`); // :1798
        } else if (rand_n(2) == 0) { // :1799
          await era.printAndWait(`「肛、肛门……麻麻的样子、好舒服……」`); // :1800
        } else { // :1801
          await era.printAndWait(`「把阴茎吞下去了……${sc()}的肛门、把阴茎完全的吞下去了！」`); // :1802
        } // :1803
        // CFLAG:327  = 7（变量语义：CFLAG 族，327） // :1804
        era.set(`cflag:${target}:327`, 7); // :1804

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:327`) <= 5 || era.get('flag:7') == 2)) { // :1806
        await era.printAndWait(`「还、还想再感受一下……肛门、还想再、感受一下阴茎！」`); // :1807
        // CFLAG:327  = 6（变量语义：CFLAG 族，327） // :1808
        era.set(`cflag:${target}:327`, 6); // :1808

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 4 || era.get('flag:7') == 2)) { // :1810
        if (rand_n(2) == 0) { // :1811
          await era.printAndWait(`「让${sc()}的肛门、好好地和阴茎做游戏吧……？」`); // :1812
        } else { // :1813
          await era.printAndWait(`「肛门、${sc()}的肛门、变得好奇怪……好想要阴茎！」`); // :1814
        } // :1815
        // CFLAG:327  = 5（变量语义：CFLAG 族，327） // :1816
        era.set(`cflag:${target}:327`, 5); // :1816

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:327`) <= 3 || era.get('flag:7') == 2)) { // :1818
        await era.printAndWait(`「还、还没……看起来还没习惯的样子……」`); // :1819
        // CFLAG:327  = 4（变量语义：CFLAG 族，327） // :1820
        era.set(`cflag:${target}:327`, 4); // :1820

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 2 || era.get('flag:7') == 2)) { // :1822
        await era.printAndWait(`「咕呜～、肛、肛门、变的好奇怪了～……」`); // :1823
        // CFLAG:327  = 3（变量语义：CFLAG 族，327） // :1824
        era.set(`cflag:${target}:327`, 3); // :1824

      } else if (era.get(`cflag:${target}:327`) <= 1 || era.get('flag:7') == 2) { // :1826
        await era.printAndWait(`「好痛……呀啊啊！　好痛！」`); // :1827
        // CFLAG:327  = 2（变量语义：CFLAG 族，327） // :1828
        era.set(`cflag:${target}:327`, 2); // :1828
      } // :1829
      return 0; // :1830
    } // :1831
  } // :1832




  if (era_flag.selectcom == 27) { // :1837

    if (era.get(`cflag:${target}:328`) == 0) { // :1839

      if (era.get(`talent:${target}:76`) == 1) { // :1841
        await era.printAndWait(`「等待多时了、这下流的体位！」`); // :1842

      } else if (era.get(`talent:${target}:85`) == 1) { // :1844
        await era.printAndWait(`「已经做好被插入的准备了哦」`); // :1845

      } else { // :1847
        await era.printAndWait(`「咕呜～、好、好羞耻……」`); // :1848
      } // :1849
      // CFLAG:TARGET:328  = 1（变量语义：CFLAG 族，TARGET:328） // :1850
      era.set(`cflag:${target}:328`, 1); // :1850
      return 0; // :1851

    } else { // :1853

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:327`) <= 6 || era.get('flag:7') == 2)) { // :1855
        if (rand_n(3) == 0) { // :1856
          await era.printAndWait(`「这、这样子好喜欢～！　野生的、非文明的、下流的姿势……像这样地、被操肛门！」`); // :1857
        } else if (rand_n(2) == 0) { // :1858
          await era.printAndWait(`「脑袋都变的傻乎乎的了……肛门像要溶化似的、${sc()}、要变成白痴了！」`); // :1859
        } else { // :1860
          await era.printAndWait(`「再深点插肛门！　${sc()}、好喜欢肛门被穿刺啊……」`); // :1861
        } // :1862
        // CFLAG:327  = 7（变量语义：CFLAG 族，327） // :1863
        era.set(`cflag:${target}:327`, 7); // :1863

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:327`) <= 5 || era.get('flag:7') == 2)) { // :1865
        await era.printAndWait(`「还、还不太习惯呢……有进一步开发的必要呢……」`); // :1866
        // CFLAG:327  = 6（变量语义：CFLAG 族，327） // :1867
        era.set(`cflag:${target}:327`, 6); // :1867

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 4 || era.get('flag:7') == 2)) { // :1869
        if (rand_n(2) == 0) { // :1870
          await era.printAndWait(`「${sc()}的肛门、变的好奇怪呢……你要、负起责任哦」`); // :1871
        } else { // :1872
          await era.printAndWait(`「已经把阴茎的形状、给记下来了呢……${sc()}的肛门」`); // :1873
        } // :1874
        // CFLAG:328  = 5（变量语义：CFLAG 族，328） // :1875
        era.set(`cflag:${target}:328`, 5); // :1875

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:328`) <= 3 || era.get('flag:7') == 2)) { // :1877
        await era.printAndWait(`「嗯……还、有点痛……」`); // :1878
        // CFLAG:328  = 4（变量语义：CFLAG 族，328） // :1879
        era.set(`cflag:${target}:328`, 4); // :1879

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 2 || era.get('flag:7') == 2)) { // :1881
        await era.printAndWait(`「咕呜……肛门、屁眼、变松了……」`); // :1882
        // CFLAG:328  = 3（变量语义：CFLAG 族，328） // :1883
        era.set(`cflag:${target}:328`, 3); // :1883

      } else if (era.get(`cflag:${target}:328`) <= 1 || era.get('flag:7') == 2) { // :1885
        await era.printAndWait(`「好痛！　肛门……要裂开了……不要这么粗暴啊」`); // :1886
        // CFLAG:328  = 2（变量语义：CFLAG 族，328） // :1887
        era.set(`cflag:${target}:328`, 2); // :1887
      } // :1888
      return 0; // :1889
    } // :1890
  } // :1891




  if (era_flag.selectcom == 28) { // :1896

    if (era.get(`cflag:${target}:329`) == 0) { // :1898

      if (era.get(`talent:${target}:76`) == 1) { // :1900
        await era.printAndWait(`「可以像这样面对面的作肛交……好像在做梦一样呢」`); // :1901

      } else if (era.get(`talent:${target}:85`) == 1) { // :1903
        await era.printAndWait(`「想亲个嘴呢……当然、肛门也请拜托您肏的火热吧」`); // :1904

      } else { // :1906
        await era.printAndWait(`「这样的……这样的性交、是异常的……」`); // :1907
      } // :1908
      // CFLAG:TARGET:329  = 1（变量语义：CFLAG 族，TARGET:329） // :1909
      era.set(`cflag:${target}:329`, 1); // :1909
      return 0; // :1910

    } else { // :1912

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 6 || era.get('flag:7') == 2)) { // :1914
        if (rand_n(3) == 0) { // :1915
          await era.printAndWait(`「${sc()}淫荡扭曲的脸……真想好好看看呢、因肛门性交而满足的表情……」`); // :1916
        } else if (rand_n(2) == 0) { // :1917
          await era.printAndWait(`「嗯～……好深、好深啊！　肛门变得下流起来了！」`); // :1918
        } else { // :1919
          await era.printAndWait(`「这样子、好喜欢……肛门、快溶化了……」`); // :1920
        } // :1921
        // CFLAG:329  = 7（变量语义：CFLAG 族，329） // :1922
        era.set(`cflag:${target}:329`, 7); // :1922

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:329`) <= 5 || era.get('flag:7') == 2)) { // :1924
        await era.printAndWait(`「不再多多开发肛门可不行呢……${sc()}、会加油的」`); // :1925
        // CFLAG:329  = 6（变量语义：CFLAG 族，329） // :1926
        era.set(`cflag:${target}:329`, 6); // :1926

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 4 || era.get('flag:7') == 2)) { // :1928
        if (rand_n(2) == 0) { // :1929
          await era.printAndWait(`「肛、肛门……变成你的形状了哦♪」`); // :1930
        } else { // :1931
          await era.printAndWait(`「哈啊～、呼呜……感、感觉到了……肛门、变的下流起来了……」`); // :1932
        } // :1933
        // CFLAG:329  = 5（变量语义：CFLAG 族，329） // :1934
        era.set(`cflag:${target}:329`, 5); // :1934

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:329`) <= 3 || era.get('flag:7') == 2)) { // :1936
        await era.printAndWait(`「无论什么部位都能被爱上吗、不得不仔细研究呢……」`); // :1937
        // CFLAG:329  = 4（变量语义：CFLAG 族，329） // :1938
        era.set(`cflag:${target}:329`, 4); // :1938

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:329`) <= 2 || era.get('flag:7') == 2)) { // :1940
        await era.printAndWait(`「不行、不行……好有感觉啊啊啊！　肛门、肛门变的不是肛门了啊啊啊啊！」`); // :1941
        // CFLAG:329  = 3（变量语义：CFLAG 族，329） // :1942
        era.set(`cflag:${target}:329`, 3); // :1942

      } else if (era.get(`cflag:${target}:329`) <= 1 || era.get('flag:7') == 2) { // :1944
        await era.printAndWait(`「噫～、好痛、好痛啊、快住手～！」`); // :1945
        // CFLAG:329  = 2（变量语义：CFLAG 族，329） // :1946
        era.set(`cflag:${target}:329`, 2); // :1946
      } // :1947
      return 0; // :1948
    } // :1949
  } // :1950




  if (era_flag.selectcom == 29) { // :1955

    if (era.get(`cflag:${target}:330`) == 0) { // :1957

      if (era.get(`talent:${target}:76`) == 1) { // :1959
        await era.printAndWait(`「肛门好感动呢、乳头那边也拜托了」`); // :1960

      } else if (era.get(`talent:${target}:85`) == 1) { // :1962
        await era.printAndWait(`「感觉到你的呼吸了……唔嗯、脖子好痒呢」`); // :1963

      } else { // :1965
        await era.printAndWait(`「咕呜……不要碰乳头～……」`); // :1966
      } // :1967
      // CFLAG:TARGET:330  = 1（变量语义：CFLAG 族，TARGET:330） // :1968
      era.set(`cflag:${target}:330`, 1); // :1968
      return 0; // :1969

    } else { // :1971

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 6 || era.get('flag:7') == 2)) { // :1973
        if (rand_n(2) == 0) { // :1974
          await era.printAndWait(`「跳起来了……${sc()}、因为肛门被串刺而跳动起来了！」`); // :1975
        } else { // :1976
          await era.printAndWait(`「知道吗……？　因为肛交、乳头也昂然耸立了……♪」`); // :1977
        } // :1978
        // CFLAG:330  = 7（变量语义：CFLAG 族，330） // :1979
        era.set(`cflag:${target}:330`, 7); // :1979

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:330`) <= 5 || era.get('flag:7') == 2)) { // :1981
        await era.printAndWait(`「${sc()}的肛门、看起来还没开发好呢……」`); // :1982
        // CFLAG:330  = 6（变量语义：CFLAG 族，330） // :1983
        era.set(`cflag:${target}:330`, 6); // :1983

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 4 || era.get('flag:7') == 2)) { // :1985
        if (rand_n(2) == 0) { // :1986
          await era.printAndWait(`「哈啊～、呼呜、有、有感觉了！　再激烈点玩弄肛门和乳头吧！」`); // :1987
        } else { // :1988
          await era.printAndWait(`「${sc()}、肛门也要怀孕了！」`); // :1989
        } // :1990
        // CFLAG:330  = 5（变量语义：CFLAG 族，330） // :1991
        era.set(`cflag:${target}:330`, 5); // :1991

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:330`) <= 3 || era.get('flag:7') == 2)) { // :1993
        await era.printAndWait(`「还很痛呢……${sc()}、得多多加油呢」`); // :1994
        // CFLAG:330  = 4（变量语义：CFLAG 族，330） // :1995
        era.set(`cflag:${target}:330`, 4); // :1995

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:330`) <= 2 || era.get('flag:7') == 2)) { // :1997
        await era.printAndWait(`「肛门、好像变的不是肛门了……好、好有感觉……」`); // :1998
        // CFLAG:330  = 3（变量语义：CFLAG 族，330） // :1999
        era.set(`cflag:${target}:330`, 3); // :1999

      } else if (era.get(`cflag:${target}:330`) <= 1 || era.get('flag:7') == 2) { // :2001
        await era.printAndWait(`「疼、好痛……咕呜～」`); // :2002
        // CFLAG:330  = 2（变量语义：CFLAG 族，330） // :2003
        era.set(`cflag:${target}:330`, 2); // :2003
      } // :2004
      return 0; // :2005
    } // :2006
  } // :2007




  if (era_flag.selectcom == 30) { // :2012

    if (era.get(`cflag:${target}:331`) == 0) { // :2014

      if (era.get(`talent:${target}:76`) == 1) { // :2016
        await era.printAndWait(`「为什么直到现在才让我碰啊～」`); // :2017

      } else if (era.get(`talent:${target}:85`) == 1) { // :2019
        await era.printAndWait(`「用${sc()}的手、来让它勃起来吧～」`); // :2020

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2022
        await era.printAndWait(`「让${sc()}搓弄也没问题是吗……我知道了」`); // :2023

      } else { // :2025
        await era.printAndWait(`「讨厌讨厌不要啊、${sc()}、才不会干这种事呢！」`); // :2026
      } // :2027
      // CFLAG:TARGET:331  = 1（变量语义：CFLAG 族，TARGET:331） // :2028
      era.set(`cflag:${target}:331`, 1); // :2028
      return 0; // :2029

    } else { // :2031

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 5 || era.get('flag:7') == 2)) { // :2033
        if (rand_n(2) == 0) { // :2034
          await era.printAndWait(`「啊哈～、一副很想要的样子、一颤一颤的呢……好吧、我撸我撸」`); // :2035
        } else { // :2036
          await era.printAndWait(`「粘乎乎的东西都已经出来了呢。这种液体、叫什么名字来着～？　啊哈～」`); // :2037
        } // :2038
        // CFLAG:331  = 6（变量语义：CFLAG 族，331） // :2039
        era.set(`cflag:${target}:331`, 6); // :2039

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:331`) <= 4 || era.get('flag:7') == 2)) { // :2041
        if (era.get(`talent:${player}:318`) == 1) { // :2042

          await era.printAndWait(`「你这远超平均值的鸡巴……啊哈、一只手完全把握不住哇${heart(1)}」`); // :2044
        } else if (era.get(`talent:${player}:318`) == 2) { // :2045

          await era.printAndWait(`「不脸红吗你？　这种差劲肉棒${heart(1)}　得了、就这么把劣等精子射在手里吧${heart(1)}」`); // :2047
        } else if (era.get(`talent:${player}:318`) == 3) { // :2048

          await era.printAndWait(`「包皮肉棒里的脏东西都跑出来了啦${heart(1)}　啊哈、好厉害的味道${heart(1)}」`); // :2050
        } else if (era.get(`talent:${player}:318`) == 4) { // :2051

          await era.printAndWait(`「什么嘛、根本就不是人的鸡巴了吧……啊哈、给你按摩咯${heart(1)}」`); // :2053
        } else if (rand_n(2) == 0) { // :2054

          await era.printAndWait(`「射精可不行哦、明明想让你在${sc()}的里面全部射出来的……」`); // :2056
        } else { // :2057

          await era.printAndWait(`「不能再多忍耐一会儿吗？　已经、想射了吗？　想射了吗？」`); // :2059
        } // :2060
        // CFLAG:331  = 5（变量语义：CFLAG 族，331） // :2061
        era.set(`cflag:${target}:331`, 5); // :2061

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 3 || era.get('flag:7') == 2)) { // :2063
        await era.printAndWait(`「跳动的很厉害呢……快射吧快射吧、忍不住了？」`); // :2064
        // CFLAG:331  = 4（变量语义：CFLAG 族，331） // :2065
        era.set(`cflag:${target}:331`, 4); // :2065

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 2 || era.get('flag:7') == 2)) { // :2067
        await era.printAndWait(`「我知道了、用手来辅助自慰行为就行了是吧」`); // :2068
        // CFLAG:331  = 3（变量语义：CFLAG 族，331） // :2069
        era.set(`cflag:${target}:331`, 3); // :2069

      } else if (era.get(`cflag:${target}:331`) <= 1 || era.get('flag:7') == 2) { // :2071
        await era.printAndWait(`「呜呜、热热的……好恶心……」`); // :2072
        // CFLAG:331  = 2（变量语义：CFLAG 族，331） // :2073
        era.set(`cflag:${target}:331`, 2); // :2073
      } // :2074
      return 0; // :2075
    } // :2076
  } // :2077




  if (era_flag.selectcom == 31) { // :2082

    if (era.get(`cflag:${target}:332`) == 0) { // :2084

      if (era.get(`talent:${target}:76`) == 1) { // :2086
        await era.printAndWait(`「${sc()}的口活、还没试过吧？」`); // :2087

      } else if (era.get(`talent:${target}:85`) == 1) { // :2089
        await era.printAndWait(`「精液、直接喝下去了呢」`); // :2090

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2092
        await era.printAndWait(`「我知道了、口交就行了是吧」`); // :2093

      } else { // :2095
        await era.printAndWait(`「这样子去舔什么的……呜诶～」`); // :2096
      } // :2097
      // CFLAG:TARGET:332  = 1（变量语义：CFLAG 族，TARGET:332） // :2098
      era.set(`cflag:${target}:332`, 1); // :2098
      return 0; // :2099

    } else { // :2101

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2103
        await era.print(`「嗞噜～、嗞噜～、嗞啾～～……噗哈啊」`); // :2104
        await era.printAndWait(`${target_name}发出了下流的声音贪求着阴茎`); // :2105
        // CFLAG:332  = 6（变量语义：CFLAG 族，332） // :2106
        era.set(`cflag:${target}:332`, 6); // :2106

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2108
        await era.printAndWait(`「我要多多练习口交、想学会厉害的口活呢」`); // :2109
        // CFLAG:332  = 5（变量语义：CFLAG 族，332） // :2110
        era.set(`cflag:${target}:332`, 5); // :2110

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :2112
        await era.print(`「哈啊、这个阴茎、刚才也忍不住喷出了精液的样子呢……」`); // :2113
        await era.printAndWait(`${target_name}发出了下流的声音贪求着阴茎`); // :2114
        // CFLAG:332  = 4（变量语义：CFLAG 族，332） // :2115
        era.set(`cflag:${target}:332`, 4); // :2115

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:332`) <= 2 || era.get('flag:7') == 2)) { // :2117
        await era.print(`「口交、变的挺擅长了呢」`); // :2118
        await era.printAndWait(`${target_name}一边这样说着一边用嘴巴含着阴茎`); // :2119
        // CFLAG:332  = 3（变量语义：CFLAG 族，332） // :2120
        era.set(`cflag:${target}:332`, 3); // :2120

      } else if (era.get(`cflag:${target}:332`) <= 1 || era.get('flag:7') == 2) { // :2122
        await era.printAndWait(`「呜诶……为什么会一跳一跳的呢。不可思议的肉块……」`); // :2123
        // CFLAG:332  = 2（变量语义：CFLAG 族，332） // :2124
        era.set(`cflag:${target}:332`, 2); // :2124
      } // :2125
      return 0; // :2126
    } // :2127
  } // :2128




  if (era_flag.selectcom == 32) { // :2133

    if (era.get(`cflag:${target}:333`) == 0) { // :2135

      if (era.get(`talent:${target}:76`) == 1) { // :2137
        await era.printAndWait(`「${sc()}的乳房、还挺管用的吧」`); // :2138

      } else if (era.get(`talent:${target}:85`) == 1) { // :2140
        await era.printAndWait(`「乳房好用的话、${sc()}、就尽情的使劲蹭了哦」`); // :2141

      } else if (era.get(`abl:${target}:16`) >= 3) { // :2143
        await era.printAndWait(`「用乳房摩擦还可以吧……」`); // :2144

      } else { // :2146
        await era.printAndWait(`「用胸部来！？　变、变态！」`); // :2147
      } // :2148
      // CFLAG:TARGET:333  = 1（变量语义：CFLAG 族，TARGET:333） // :2149
      era.set(`cflag:${target}:333`, 1); // :2149
      return 0; // :2150

    } else { // :2152

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 5 || era.get('flag:7') == 2)) { // :2154
        if (rand_n(2) == 0) { // :2155
          await era.printAndWait(`「这样挤压着会舒服吗？」`); // :2156
        } else { // :2157
          await era.printAndWait(`「快看快看快看。乳房把你的阴茎吞进去了哦」`); // :2158
        } // :2159
        // CFLAG:333  = 6（变量语义：CFLAG 族，333） // :2160
        era.set(`cflag:${target}:333`, 6); // :2160

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 4 || era.get('flag:7') == 2)) { // :2162
        await era.printAndWait(`「好难啊……你、真的会舒服吗？」`); // :2163
        // CFLAG:333  = 5（变量语义：CFLAG 族，333） // :2164
        era.set(`cflag:${target}:333`, 5); // :2164

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:333`) <= 3 || era.get('flag:7') == 2)) { // :2166
        if (rand_n(2) == 0) { // :2167
          await era.printAndWait(`「我会竭尽全力来奉仕的哦。用${sc()}柔软的乳房！」`); // :2168
        } else { // :2169
          await era.printAndWait(`「Biu的射出来也行哦？」`); // :2170
        } // :2171
        // CFLAG:333  = 4（变量语义：CFLAG 族，333） // :2172
        era.set(`cflag:${target}:333`, 4); // :2172

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:333`) <= 2 || era.get('flag:7') == 2)) { // :2174
        await era.printAndWait(`「你、真的有感觉吗？　那就好……」`); // :2175
        // CFLAG:333  = 3（变量语义：CFLAG 族，333） // :2176
        era.set(`cflag:${target}:333`, 3); // :2176

      } else if (era.get(`cflag:${target}:333`) <= 1 || era.get('flag:7') == 2) { // :2178
        await era.printAndWait(`「呜呜、这样子到底有什么好高兴的嘛……」`); // :2179
        // CFLAG:333  = 2（变量语义：CFLAG 族，333） // :2180
        era.set(`cflag:${target}:333`, 2); // :2180
      } // :2181
      return 0; // :2182
    } // :2183
  } // :2184




  if (era_flag.selectcom == 33) { // :2189

    if (era.get(`cflag:${target}:334`) == 0) { // :2191

      if (era.get(`talent:${target}:76`) == 1) { // :2193
        await era.printAndWait(`「快看快看、要进到${sc()}的阴道里去了哦？」`); // :2194

      } else if (era.get(`talent:${target}:85`) == 1) { // :2196
        await era.printAndWait(`「只在表面摩擦吗……好想进到里面去呢」`); // :2197

      } else { // :2199
        await era.printAndWait(`「这样摩擦到底有什么好高兴的……」`); // :2200
      } // :2201
      // CFLAG:TARGET:334  = 1（变量语义：CFLAG 族，TARGET:334） // :2202
      era.set(`cflag:${target}:334`, 1); // :2202
      return 0; // :2203

    } else { // :2205

      if (era.get(`talent:${target}:76`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:334`) <= 5 || era.get('flag:7') == 2)) { // :2207
        await era.printAndWait(`「呐、只在表面摩擦你会觉得舒服吗？　${sc()}……对这不是很了解呢」`); // :2208
        // CFLAG:334  = 6（变量语义：CFLAG 族，334） // :2209
        era.set(`cflag:${target}:334`, 6); // :2209

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:334`) <= 4 || era.get('flag:7') == 2)) { // :2211
        await era.printAndWait(`「呐、只在表面摩擦你会觉得舒服吗？　${sc()}……有点遗憾呢」`); // :2212
        // CFLAG:334  = 5（变量语义：CFLAG 族，334） // :2213
        era.set(`cflag:${target}:334`, 5); // :2213

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`talent:${target}:0`) == 1 && (era.get(`cflag:${target}:334`) <= 3 || era.get('flag:7') == 2)) { // :2215
        await era.printAndWait(`「${sc()}已经到极限了……明明好想被插进去、明明好想……被插到里面啊！」`); // :2216
        // CFLAG:334  = 4（变量语义：CFLAG 族，334） // :2217
        era.set(`cflag:${target}:334`, 4); // :2217

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:334`) <= 2 || era.get('flag:7') == 2)) { // :2219
        await era.printAndWait(`「假如你、一不小心把你的阴茎插进了${sc()}的阴道里的话……该怎么办呢？　啊哈哈～」`); // :2220
        // CFLAG:334  = 3（变量语义：CFLAG 族，334） // :2221
        era.set(`cflag:${target}:334`, 3); // :2221

      } else if (era.get(`cflag:${target}:334`) <= 1 || era.get('flag:7') == 2) { // :2223
        await era.printAndWait(`「看起来这样的摩擦让你很高兴嘛……」`); // :2224
        // CFLAG:334  = 2（变量语义：CFLAG 族，334） // :2225
        era.set(`cflag:${target}:334`, 2); // :2225
      } // :2226
      return 0; // :2227
    } // :2228
  } // :2229




  if (era_flag.selectcom == 34) { // :2234

    if (era.get(`cflag:${target}:335`) == 0) { // :2236

      if (era.get(`talent:${target}:0`) == 1) { // :2238

        if (era.get(`talent:${target}:76`) == 1) { // :2240
          await era.printAndWait(`「我期待已久了！　把你的阴茎、交给我吧」`); // :2241

        } else if (era.get(`talent:${target}:85`) == 1) { // :2243
          await era.printAndWait(`「终于、想要让我${sc()}怀孕了吗！　好开心哦」`); // :2244

        } else { // :2246
          await era.printAndWait(`「让我自己来……你实在太无耻了」`); // :2247
        } // :2248

      } else { // :2250

        if (era.get(`talent:${target}:76`) == 1) { // :2252
          await era.printAndWait(`「嘿诶、这么想让${sc()}来吗」`); // :2253

        } else if (era.get(`talent:${target}:85`) == 1) { // :2255
          await era.printAndWait(`「这样子嘎吱嘎吱的……并不讨厌哦」`); // :2256

        } else { // :2258
          await era.printAndWait(`「让我自己来……可恶」`); // :2259
        } // :2260
      } // :2261
      // CFLAG:TARGET:335  = 1（变量语义：CFLAG 族，TARGET:335） // :2262
      era.set(`cflag:${target}:335`, 1); // :2262
      return 0; // :2263

    } else { // :2265

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:335`) <= 5 || era.get('flag:7') == 2)) { // :2267
        if (rand_n(4) == 0) { // :2268
          await era.printAndWait(`「来吧来吧、更多的从下面来插${sc()}吧！」`); // :2269
        } else if (rand_n(3) == 0) { // :2270
          await era.printAndWait(`「${sc()}扭腰的样子、请多多欣赏吧」`); // :2271
        } else if (rand_n(2) == 0) { // :2272
          await era.printAndWait(`「停不下来了、你也多用阴茎来顶我吧」`); // :2273
        } else { // :2274
          await era.printAndWait(`「哈啊～、从下面、操我吧～♪」`); // :2275
        } // :2276
        // CFLAG:335  = 6（变量语义：CFLAG 族，335） // :2277
        era.set(`cflag:${target}:335`, 6); // :2277

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:335`) <= 4 || era.get('flag:7') == 2)) { // :2279
        if (rand_n(4) == 0) { // :2280
          await era.printAndWait(`「要把精液一滴不剩的榨干哦♪」`); // :2281
        } else if (rand_n(3) == 0) { // :2282
          await era.printAndWait(`「快看快看、要出来了哦、精液！」`); // :2283
        } else if (rand_n(2) == 0) { // :2284
          await era.printAndWait(`「想更多的被阴茎操呢、啊哈哈～」`); // :2285
        } else { // :2286
          await era.printAndWait(`「出来了～！　把精液、更多的、射进来吧！　啊啊啊～♪」`); // :2287
        } // :2288
        // CFLAG:335  = 5（变量语义：CFLAG 族，335） // :2289
        era.set(`cflag:${target}:335`, 5); // :2289

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:335`) <= 3 || era.get('flag:7') == 2)) { // :2291
        if (rand_n(4) == 0) { // :2292
          await era.printAndWait(`「不行了、${sc()}的腰、自己动起来了！」`); // :2293
        } else if (rand_n(3) == 0) { // :2294
          await era.printAndWait(`「竟然……让${sc()}这样做……不过……」`); // :2295
        } else if (rand_n(2) == 0) { // :2296
          await era.printAndWait(`「哈啊～、哈啊～、咕呜～♪」`); // :2297
        } else { // :2298
          await era.printAndWait(`「不行啊、这样子……往上顶……」`); // :2299
        } // :2300
        // CFLAG:335  = 4（变量语义：CFLAG 族，335） // :2301
        era.set(`cflag:${target}:335`, 4); // :2301

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:335`) <= 2 || era.get('flag:7') == 2)) { // :2303
        await era.printAndWait(`「唔～……我知道了。跨在你身上就行了吧？」`); // :2304
        // CFLAG:335  = 3（变量语义：CFLAG 族，335） // :2305
        era.set(`cflag:${target}:335`, 3); // :2305

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :2307
        await era.printAndWait(`「竟让${sc()}……这样做……好屈辱～」`); // :2308
        // CFLAG:335  = 2（变量语义：CFLAG 族，335） // :2309
        era.set(`cflag:${target}:335`, 2); // :2309
      } // :2310
      return 0; // :2311
    } // :2312
  } // :2313




  if (era_flag.selectcom == 35) { // :2318

    if (era.get(`cflag:${target}:336`) == 0) { // :2320

      if (era.get(`abl:${target}:16`) >= 3) { // :2322
        await era.printAndWait(`「你喜欢提供这种服务的店吗……？」`); // :2323

      } else { // :2325
        await era.printAndWait(`「跟按摩女似的……额」`); // :2326
      } // :2327
      // CFLAG:TARGET:336  = 1（变量语义：CFLAG 族，TARGET:336） // :2328
      era.set(`cflag:${target}:336`, 1); // :2328
      return 0; // :2329

    } else { // :2331

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:336`) <= 4 || era.get('flag:7') == 2)) { // :2333
        await era.printAndWait(`「快看快看、被${sc()}的肌肤哧溜哧溜的摩擦着哦～♪　舒服吗？」`); // :2334
        // CFLAG:336  = 5（变量语义：CFLAG 族，336） // :2335
        era.set(`cflag:${target}:336`, 5); // :2335

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:336`) <= 3 || era.get('flag:7') == 2)) { // :2337
        await era.printAndWait(`「这身体是只属于你的哦、为了你……嫩呼呼的呢」`); // :2338
        // CFLAG:336  = 4（变量语义：CFLAG 族，336） // :2339
        era.set(`cflag:${target}:336`, 4); // :2339

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:336`) <= 2 || era.get('flag:7') == 2)) { // :2341
        await era.printAndWait(`「变的全是泡泡了呢……舒服吗？」`); // :2342
        // CFLAG:336  = 3（变量语义：CFLAG 族，336） // :2343
        era.set(`cflag:${target}:336`, 3); // :2343

      } else if (era.get(`cflag:${target}:336`) <= 1 || era.get('flag:7') == 2) { // :2345
        await era.printAndWait(`「喂、这样做应该可以了吧……真是的」`); // :2346
        // CFLAG:336  = 2（变量语义：CFLAG 族，336） // :2347
        era.set(`cflag:${target}:336`, 2); // :2347
      } // :2348
      return 0; // :2349
    } // :2350
  } // :2351




  if (era_flag.selectcom == 36) { // :2356

    if (era.get(`cflag:${target}:337`) == 0) { // :2358

      if (era.get(`talent:${target}:76`) == 1) { // :2360
        await era.printAndWait(`「这样骑在你的身上就好像做梦一样呢」`); // :2361

      } else if (era.get(`talent:${target}:85`) == 1) { // :2363
        await era.printAndWait(`「呜嗯～……用肛门裹住了呢……♪」`); // :2364

      } else { // :2366
        await era.printAndWait(`「这么卑劣的肛交还是第一次……」`); // :2367
      } // :2368
      // CFLAG:TARGET:337  = 1（变量语义：CFLAG 族，TARGET:337） // :2369
      era.set(`cflag:${target}:337`, 1); // :2369
      return 0; // :2370

    } else { // :2372

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 6 || era.get('flag:7') == 2)) { // :2374
        if (rand_n(2) == 0) { // :2375
          await era.printAndWait(`「不行了、${sc()}的肛门要溶化了啊！」`); // :2376
        } else { // :2377
          await era.printAndWait(`「${sc()}、一被串刺着……从肛门到大脑、都在回响！」`); // :2378
        } // :2379
        // CFLAG:337  = 7（变量语义：CFLAG 族，337） // :2380
        era.set(`cflag:${target}:337`, 7); // :2380

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:337`) <= 5 || era.get('flag:7') == 2)) { // :2382
        await era.printAndWait(`「${sc()}一动起来、你就盯着不放呢♪」`); // :2383
        // CFLAG:337  = 6（变量语义：CFLAG 族，337） // :2384
        era.set(`cflag:${target}:337`, 6); // :2384

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 4 || era.get('flag:7') == 2)) { // :2386
        if (rand_n(2) == 0) { // :2387
          await era.printAndWait(`「啊啊、难以置信、肛门要溶化了♪」`); // :2388
        } else { // :2389
          await era.printAndWait(`「肛、肛门、肛门要……不行～、太有感觉了～♪」`); // :2390
        } // :2391
        // CFLAG:337  = 5（变量语义：CFLAG 族，337） // :2392
        era.set(`cflag:${target}:337`, 5); // :2392

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:337`) <= 3 || era.get('flag:7') == 2)) { // :2394
        await era.printAndWait(`「肛门含住阴茎的样子、好好看看吧」`); // :2395
        // CFLAG:337  = 4（变量语义：CFLAG 族，337） // :2396
        era.set(`cflag:${target}:337`, 4); // :2396

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:337`) <= 2 || era.get('flag:7') == 2)) { // :2398
        await era.printAndWait(`「咕呜、像这样的肛交什么的……尽管让${sc()}做吧」`); // :2399
        // CFLAG:337  = 3（变量语义：CFLAG 族，337） // :2400
        era.set(`cflag:${target}:337`, 3); // :2400

      } else if (era.get(`cflag:${target}:337`) <= 1 || era.get('flag:7') == 2) { // :2402
        await era.printAndWait(`「痛、好疼、好痛啊……${sc()}已经不想再做了啊」`); // :2403
        // CFLAG:337  = 2（变量语义：CFLAG 族，337） // :2404
        era.set(`cflag:${target}:337`, 2); // :2404
      } // :2405
      return 0; // :2406
    } // :2407
  } // :2408




  if (era_flag.selectcom == 37) { // :2413

    if (era.get(`cflag:${target}:338`) == 0) { // :2415

      if (era.get(`abl:${target}:16`) >= 3) { // :2417
        await era.printAndWait(`「竟然要我舔这么脏的地方……你真是变态呢」`); // :2418

      } else { // :2420
        await era.printAndWait(`「讨、讨厌、竟然要我舔那种地方……呜呜～」`); // :2421
      } // :2422
      // CFLAG:TARGET:338  = 1（变量语义：CFLAG 族，TARGET:338） // :2423
      era.set(`cflag:${target}:338`, 1); // :2423
      return 0; // :2424

    } else { // :2426

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 4 || era.get('flag:7') == 2)) { // :2428
        await era.printAndWait(`「这样把舌头伸进肛门、你喜欢吗？」`); // :2429
        // CFLAG:338  = 5（变量语义：CFLAG 族，338） // :2430
        era.set(`cflag:${target}:338`, 5); // :2430

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 3 || era.get('flag:7') == 2)) { // :2432
        await era.print(`「你的肛门……被${sc()}弄干净了哦♪」`); // :2433
        // CFLAG:338  = 4（变量语义：CFLAG 族，338） // :2434
        era.set(`cflag:${target}:338`, 4); // :2434

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:338`) <= 2 || era.get('flag:7') == 2)) { // :2436
        await era.printAndWait(`「我知道了……舔就行了吧？　有好好洗过吗？」`); // :2437
        // CFLAG:338  = 3（变量语义：CFLAG 族，338） // :2438
        era.set(`cflag:${target}:338`, 3); // :2438

      } else if (era.get(`cflag:${target}:338`) <= 1 || era.get('flag:7') == 2) { // :2440
        await era.printAndWait(`「呜呜……咕呜～、呜呜……」`); // :2441
        // CFLAG:338  = 2（变量语义：CFLAG 族，338） // :2442
        era.set(`cflag:${target}:338`, 2); // :2442
      } // :2443
      return 0; // :2444
    } // :2445
  } // :2446




  if (era_flag.selectcom == 40) { // :2451

    if (era.get(`cflag:${target}:341`) == 0) { // :2453
      await era.printAndWait(`「呀啊～、好痛、住手～、饶了${sc()}吧！」`); // :2454
      // CFLAG:TARGET:341  = 1（变量语义：CFLAG 族，TARGET:341） // :2455
      era.set(`cflag:${target}:341`, 1); // :2455
      return 0; // :2456

    } else { // :2458

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:341`) <= 4 || era.get('flag:7') == 2)) { // :2460
        await era.printAndWait(`「啊啊～、再继续打吧♪　再多教育一下我这只淫乱受虐狂母猪吧♪　咿～～♪」`); // :2461
        // CFLAG:TARGET:341  = 5（变量语义：CFLAG 族，TARGET:341） // :2462
        era.set(`cflag:${target}:341`, 5); // :2462

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:341`) <= 3 || era.get('flag:7') == 2)) { // :2464
        await era.printAndWait(`「啊啊～、请再用力点打！　再更多的惩罚一下受虐狂母猪${target_name}吧～♪」`); // :2465
        // CFLAG:TARGET:341  = 4（变量语义：CFLAG 族，TARGET:341） // :2466
        era.set(`cflag:${target}:341`, 4); // :2466
        return 0; // :2467

      } else if (era.get(`mark:${target}:0`) == 3 && era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:341`) <= 2 || era.get('flag:7') == 2)) { // :2469
        await era.printAndWait(`「啊啊～、咿～～、啊～～、咿啊～」`); // :2470
        // CFLAG:TARGET:341  = 3（变量语义：CFLAG 族，TARGET:341） // :2471
        era.set(`cflag:${target}:341`, 3); // :2471
        return 0; // :2472

      } else if (era.get(`cflag:${target}:341`) <= 1 && era.get('flag:7') == 2) { // :2474
        await era.printAndWait(`「痛～、好痛啊～……${sc()}什么坏事也没做啊」`); // :2475
        // CFLAG:TARGET:341  = 2（变量语义：CFLAG 族，TARGET:341） // :2476
        era.set(`cflag:${target}:341`, 2); // :2476
      } // :2477
      return 0; // :2478
    } // :2479
  } // :2480




  if (era_flag.selectcom == 41) { // :2485

    if (era.get(`cflag:${target}:342`) == 0) { // :2487

      if (era.get(`talent:${target}:76`) == 1) { // :2489
        await era.printAndWait(`「咿呀啊啊啊！　好有效～……」`); // :2490

      } else if (era.get(`talent:${target}:85`) == 1) { // :2492
        await era.printAndWait(`「咿呀啊～、咿呀啊～」`); // :2493

      } else { // :2495
        await era.printAndWait(`「住手、住……呜啊啊！」`); // :2496
      } // :2497
      // CFLAG:TARGET:342  = 1（变量语义：CFLAG 族，TARGET:342） // :2498
      era.set(`cflag:${target}:342`, 1); // :2498
      return 0; // :2499

    } else { // :2501

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:342`) <= 8 || era.get('flag:7') == 2)) { // :2503
        await era.printAndWait(`「咿呀啊啊啊！　再来♪　再用力点♪　虐待我这只淫乱受虐狂母猪吧～～～♪」`); // :2504
        // CFLAG:TARGET:342  = 9（变量语义：CFLAG 族，TARGET:342） // :2505
        era.set(`cflag:${target}:342`, 9); // :2505

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 7 || era.get('flag:7') == 2)) { // :2507
        await era.printAndWait(`「咿呀啊啊啊！　再来♪　再用力点♪　有感觉了～……♪」`); // :2508
        // CFLAG:TARGET:342  = 8（变量语义：CFLAG 族，TARGET:342） // :2509
        era.set(`cflag:${target}:342`, 8); // :2509

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:342`) <= 6 || era.get('flag:7') == 2)) { // :2511
        await era.printAndWait(`「咿呀啊啊啊！　痛、好痛啊！」`); // :2512
        // CFLAG:TARGET:342  = 7（变量语义：CFLAG 族，TARGET:342） // :2513
        era.set(`cflag:${target}:342`, 7); // :2513

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:342`) <= 5 || era.get('flag:7') == 2)) { // :2515
        await era.printAndWait(`「咿呀啊～！　再来！　再用力点！　来教育受虐狂母猪${target_name}吧～～～♪」`); // :2516
        // CFLAG:TARGET:342  = 6（变量语义：CFLAG 族，TARGET:342） // :2517
        era.set(`cflag:${target}:342`, 6); // :2517

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 4 || era.get('flag:7') == 2)) { // :2519
        await era.printAndWait(`「咿呀啊～！　再来！　再用力点！　再多多教育我！」`); // :2520
        // CFLAG:TARGET:342  = 5（变量语义：CFLAG 族，TARGET:342） // :2521
        era.set(`cflag:${target}:342`, 5); // :2521

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:342`) <= 3 || era.get('flag:7') == 2)) { // :2523
        await era.printAndWait(`「咿呀啊～！　痛、好痛啊……」`); // :2524
        // CFLAG:TARGET:342  = 4（变量语义：CFLAG 族，TARGET:342） // :2525
        era.set(`cflag:${target}:342`, 4); // :2525

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:342`) <= 2 || era.get('flag:7') == 2)) { // :2527
        await era.printAndWait(`「再来！　再用力点！　再多多教育我！」`); // :2528
        // CFLAG:TARGET:342  = 3（变量语义：CFLAG 族，TARGET:342） // :2529
        era.set(`cflag:${target}:342`, 3); // :2529

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :2531
        await era.printAndWait(`「住手、痛……好痛啊！」`); // :2532
        // CFLAG:TARGET:342  = 2（变量语义：CFLAG 族，TARGET:342） // :2533
        era.set(`cflag:${target}:342`, 2); // :2533
      } // :2534
      return 0; // :2535
    } // :2536
  } // :2537




  if (era_flag.selectcom == 42) { // :2542

    if (era.get(`cflag:${target}:343`) == 0) { // :2544

      if (era.get(`talent:${target}:76`) == 1) { // :2546
        await era.printAndWait(`「要刺哪里呢？　嗯？」`); // :2547

      } else if (era.get(`talent:${target}:85`) == 1) { // :2549
        await era.printAndWait(`「扑哧一下被刺进去、一想到这个、就好像要高潮了呢」`); // :2550

      } else { // :2552
        await era.printAndWait(`「哈哈、注射什么的我早就习惯了」`); // :2553
      } // :2554
      // CFLAG:TARGET:343  = 1（变量语义：CFLAG 族，TARGET:343） // :2555
      era.set(`cflag:${target}:343`, 1); // :2555
      return 0; // :2556

    } else { // :2558

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:343`) <= 8 || era.get('flag:7') == 2)) { // :2560
        await era.printAndWait(`「刺进去后……使劲、捻动。这是${sc()}最喜欢做的哦♪」`); // :2561
        // CFLAG:TARGET:343  = 9（变量语义：CFLAG 族，TARGET:343） // :2562
        era.set(`cflag:${target}:343`, 9); // :2562

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 7 || era.get('flag:7') == 2)) { // :2564
        await era.printAndWait(`「咕呜呜、一下子……刺进来了～」`); // :2565
        // CFLAG:TARGET:343  = 8（变量语义：CFLAG 族，TARGET:343） // :2566
        era.set(`cflag:${target}:343`, 8); // :2566

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:343`) <= 6 || era.get('flag:7') == 2)) { // :2568
        await era.printAndWait(`「呜～……呜呜、看起来${sc()}的感觉还需要再开发呢……」`); // :2569
        // CFLAG:TARGET:343  = 7（变量语义：CFLAG 族，TARGET:343） // :2570
        era.set(`cflag:${target}:343`, 7); // :2570

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:343`) <= 5 || era.get('flag:7') == 2)) { // :2572
        await era.printAndWait(`「感、感觉到了♪　${sc()}的身体正被冰冷的金属……穿凿着♪」`); // :2573
        // CFLAG:TARGET:343  = 6（变量语义：CFLAG 族，TARGET:343） // :2574
        era.set(`cflag:${target}:343`, 6); // :2574

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 4 || era.get('flag:7') == 2)) { // :2576
        await era.printAndWait(`「嗯……啊～、进来了～」`); // :2577
        // CFLAG:TARGET:343  = 5（变量语义：CFLAG 族，TARGET:343） // :2578
        era.set(`cflag:${target}:343`, 5); // :2578

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:343`) <= 3 || era.get('flag:7') == 2)) { // :2580
        await era.printAndWait(`「呜～、呜～……好痛～……」`); // :2581
        // CFLAG:TARGET:343  = 4（变量语义：CFLAG 族，TARGET:343） // :2582
        era.set(`cflag:${target}:343`, 4); // :2582

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:343`) <= 2 || era.get('flag:7') == 2)) { // :2584
        await era.printAndWait(`「不行啊……这样下去……${sc()}的感觉要变的奇怪了～」`); // :2585
        // CFLAG:TARGET:343  = 3（变量语义：CFLAG 族，TARGET:343） // :2586
        era.set(`cflag:${target}:343`, 3); // :2586

      } else if (era.get(`cflag:${target}:343`) <= 1 || era.get('flag:7') == 2) { // :2588
        await era.printAndWait(`「咿呀啊啊～！　咿、咿呀啊啊！」`); // :2589
        // CFLAG:TARGET:343  = 2（变量语义：CFLAG 族，TARGET:343） // :2590
        era.set(`cflag:${target}:343`, 2); // :2590
      } // :2591
      return 0; // :2592
    } // :2593
  } // :2594





  if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`)) { // :2600

    if (era.get(`cflag:${target}:344`) == 0) { // :2602

      if (era.get(`talent:${target}:76`) == 1) { // :2604
        await era.printAndWait(''); // :2605

      } else if (era.get(`talent:${target}:85`) == 1) { // :2607
        await era.printAndWait(''); // :2608

      } else { // :2610
        await era.printAndWait(''); // :2611
      } // :2612
      // CFLAG:TARGET:344  = 1（变量语义：CFLAG 族，TARGET:344） // :2613
      era.set(`cflag:${target}:344`, 1); // :2613
      return 0; // :2614

    } else { // :2616

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 8 || era.get('flag:7') == 2)) { // :2618
        await era.printAndWait(''); // :2619
        // CFLAG:TARGET:344  = 9（变量语义：CFLAG 族，TARGET:344） // :2620
        era.set(`cflag:${target}:344`, 9); // :2620

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 7 || era.get('flag:7') == 2)) { // :2622
        await era.printAndWait(''); // :2623
        // CFLAG:TARGET:344  = 8（变量语义：CFLAG 族，TARGET:344） // :2624
        era.set(`cflag:${target}:344`, 8); // :2624

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:344`) <= 6 || era.get('flag:7') == 2)) { // :2626
        await era.printAndWait(''); // :2627
        // CFLAG:TARGET:344  = 7（变量语义：CFLAG 族，TARGET:344） // :2628
        era.set(`cflag:${target}:344`, 7); // :2628

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 5 || era.get('flag:7') == 2)) { // :2630
        await era.printAndWait(''); // :2631
        // CFLAG:TARGET:344  = 6（变量语义：CFLAG 族，TARGET:344） // :2632
        era.set(`cflag:${target}:344`, 6); // :2632

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 4 || era.get('flag:7') == 2)) { // :2634
        await era.printAndWait(''); // :2635
        // CFLAG:TARGET:344  = 5（变量语义：CFLAG 族，TARGET:344） // :2636
        era.set(`cflag:${target}:344`, 5); // :2636

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:344`) <= 3 || era.get('flag:7') == 2)) { // :2638
        await era.printAndWait(''); // :2639
        // CFLAG:TARGET:344  = 4（变量语义：CFLAG 族，TARGET:344） // :2640
        era.set(`cflag:${target}:344`, 4); // :2640

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 2 || era.get('flag:7') == 2)) { // :2642
        await era.printAndWait(''); // :2643
        // CFLAG:TARGET:344  = 3（变量语义：CFLAG 族，TARGET:344） // :2644
        era.set(`cflag:${target}:344`, 3); // :2644

      } else if (era.get(`cflag:${target}:344`) <= 1 || era.get('flag:7') == 2) { // :2646
        await era.printAndWait(''); // :2647
        // CFLAG:TARGET:344  = 2（变量语义：CFLAG 族，TARGET:344） // :2648
        era.set(`cflag:${target}:344`, 2); // :2648
      } // :2649
      return 0; // :2650
    } // :2651

  } else if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`) == 0) { // :2653

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:380`) < 3 || era.get('flag:7') == 2)) { // :2655
      await era.printAndWait(''); // :2656
      // CFLAG:380  = 3（变量语义：CFLAG 族，380） // :2657
      era.set(`cflag:${target}:380`, 3); // :2657

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:380`) < 2 || era.get('flag:7') == 2)) { // :2659
      await era.printAndWait(''); // :2660
      // CFLAG:380  = 2（变量语义：CFLAG 族，380） // :2661
      era.set(`cflag:${target}:380`, 2); // :2661

    } else if (era.get(`cflag:${target}:380`) < 1 || era.get('flag:7') == 2) { // :2663
      await era.printAndWait(''); // :2664
      // CFLAG:380  = 1（变量语义：CFLAG 族，380） // :2665
      era.set(`cflag:${target}:380`, 1); // :2665
    } // :2666
    return 0; // :2667
  } // :2668





  if (era_flag.selectcom == 44 && era.get(`tequip:${target}:44`)) { // :2674

    if (era.get(`cflag:${target}:345`) == 0) { // :2676

      if (era.get(`talent:${target}:76`) == 1) { // :2678
        await era.printAndWait(`「紧紧地绑上来吧♪」`); // :2679

      } else if (era.get(`talent:${target}:85`) == 1) { // :2681
        await era.printAndWait(`「我喜欢束缚的紧一点」`); // :2682

      } else { // :2684
        await era.printAndWait(`「嗯……要来束缚这手吗」`); // :2685
      } // :2686
      // CFLAG:TARGET:345  = 1（变量语义：CFLAG 族，TARGET:345） // :2687
      era.set(`cflag:${target}:345`, 1); // :2687
      return 0; // :2688

    } else { // :2690

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:345`) <= 8 || era.get('flag:7') == 2)) { // :2692
        await era.printAndWait(`「哈……要怎么处理动不了的${sc()}呢？　我期待着呢？」`); // :2693
        // CFLAG:TARGET:345  = 9（变量语义：CFLAG 族，TARGET:345） // :2694
        era.set(`cflag:${target}:345`, 9); // :2694

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 7 || era.get('flag:7') == 2)) { // :2696
        await era.printAndWait(`「呵呵、身体动不了了呢。好像触电一样麻痹的快感啊……」`); // :2697
        // CFLAG:TARGET:345  = 8（变量语义：CFLAG 族，TARGET:345） // :2698
        era.set(`cflag:${target}:345`, 8); // :2698

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:345`) <= 6 || era.get('flag:7') == 2)) { // :2700
        await era.printAndWait(`「嗯、${sc()}的性癖还没开发到这方面……对不起」`); // :2701
        // CFLAG:TARGET:345  = 7（变量语义：CFLAG 族，TARGET:345） // :2702
        era.set(`cflag:${target}:345`, 7); // :2702

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:345`) <= 5 || era.get('flag:7') == 2)) { // :2704
        await era.printAndWait(`「已经不论如何都逃不了了……这下${sc()}就是你的俘虏了♪」`); // :2705
        // CFLAG:TARGET:345  = 6（变量语义：CFLAG 族，TARGET:345） // :2706
        era.set(`cflag:${target}:345`, 6); // :2706

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 4 || era.get('flag:7') == 2)) { // :2708
        await era.printAndWait(`「呵呵、身体动不了了呢。真是让人受不了的家伙呢、你啊」`); // :2709
        // CFLAG:TARGET:345  = 5（变量语义：CFLAG 族，TARGET:345） // :2710
        era.set(`cflag:${target}:345`, 5); // :2710

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:345`) <= 3 || era.get('flag:7') == 2)) { // :2712
        await era.printAndWait(`「虽然我不知道绳子的好处……但是被你的话不管什么都很舒服」`); // :2713
        // CFLAG:TARGET:345  = 4（变量语义：CFLAG 族，TARGET:345） // :2714
        era.set(`cflag:${target}:345`, 4); // :2714

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:345`) <= 2 || era.get('flag:7') == 2)) { // :2716
        await era.printAndWait(`「呵呵、身体动不了了呢……好为难好为难」`); // :2717
        // CFLAG:TARGET:345  = 3（变量语义：CFLAG 族，TARGET:345） // :2718
        era.set(`cflag:${target}:345`, 3); // :2718

      } else if (era.get(`cflag:${target}:345`) <= 1 || era.get('flag:7') == 2) { // :2720
        await era.printAndWait(`「只是被束缚住而已、早就习惯了」`); // :2721
        // CFLAG:TARGET:345  = 2（变量语义：CFLAG 族，TARGET:345） // :2722
        era.set(`cflag:${target}:345`, 2); // :2722
      } // :2723
      return 0; // :2724
    } // :2725

  } else if (era_flag.selectcom == 44 && era.get(`tequip:${target}:44`) == 0) { // :2727

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:385`) < 2 || era.get('flag:7') == 2)) { // :2729
      await era.printAndWait(`「已经结束了吗♪」`); // :2730
      // CFLAG:385  = 2（变量语义：CFLAG 族，385） // :2731
      era.set(`cflag:${target}:385`, 2); // :2731

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:385`) < 2 || era.get('flag:7') == 2)) { // :2733
      await era.printAndWait(`「明明直到最后都束缚住我就好了呢」`); // :2734
      // CFLAG:385  = 2（变量语义：CFLAG 族，385） // :2735
      era.set(`cflag:${target}:385`, 2); // :2735

    } else if (era.get(`cflag:${target}:385`) < 1 || era.get('flag:7') == 2) { // :2737
      await era.printAndWait(`「已经够了吗？」`); // :2738
      // CFLAG:385  = 1（变量语义：CFLAG 族，385） // :2739
      era.set(`cflag:${target}:385`, 1); // :2739
    } // :2740
    return 0; // :2741
  } // :2742





  if (era_flag.selectcom == 45 && era.get(`tequip:${target}:45`)) { // :2748

    if (era.get(`cflag:${target}:346`) == 0) { // :2750

      if (era.get(`talent:${target}:76`) == 1) { // :2752
        await era.printAndWait(`「呼咕呜♪　呼呜、呼呜呜～～♪」`); // :2753
      } else if (era.get(`talent:${target}:85`) == 1) { // :2754
        await era.printAndWait(`「呼嘎、呼咕呜……呼咕♪」`); // :2755

      } else { // :2757
        await era.printAndWait(`「嘎呼～……呼咕～……呼嘎啊！」`); // :2758
      } // :2759
      // CFLAG:TARGET:346  = 1（变量语义：CFLAG 族，TARGET:346） // :2760
      era.set(`cflag:${target}:346`, 1); // :2760
      return 0; // :2761

    } else { // :2763

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:346`) <= 8 || era.get('flag:7') == 2)) { // :2765
        await era.printAndWait(`「噗呼呜♪　呼咕呜……呼苟、噗呼呜♪」`); // :2766
        await era.printAndWait(`欢喜的${target_name}像猪一样喘息着`); // :2767
        // CFLAG:TARGET:346  = 9（变量语义：CFLAG 族，TARGET:346） // :2768
        era.set(`cflag:${target}:346`, 9); // :2768

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 7 || era.get('flag:7') == 2)) { // :2770
        await era.printAndWait(`「噗咕呜、呼咕呜……呼苟、噗呼呜♪」`); // :2771
        await era.printAndWait(`${target_name}像猪一样喘息着`); // :2772
        // CFLAG:TARGET:346  = 8（变量语义：CFLAG 族，TARGET:346） // :2773
        era.set(`cflag:${target}:346`, 8); // :2773

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:346`) <= 6 || era.get('flag:7') == 2)) { // :2775
        await era.printAndWait(`「呼咕呜♪　呼呜、呼呜呜～～♪」`); // :2776
        // CFLAG:TARGET:346  = 7（变量语义：CFLAG 族，TARGET:346） // :2777
        era.set(`cflag:${target}:346`, 7); // :2777

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:346`) <= 5 || era.get('flag:7') == 2)) { // :2779
        await era.printAndWait(`「哈呼呜♪　哈咕呜……呼呜、哈呼呜♪」`); // :2780
        await era.printAndWait(`欢喜的${target_name}激动的喘息着`); // :2781
        // CFLAG:TARGET:346  = 6（变量语义：CFLAG 族，TARGET:346） // :2782
        era.set(`cflag:${target}:346`, 6); // :2782

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 4 || era.get('flag:7') == 2)) { // :2784
        await era.printAndWait(`「哈呼呜、哈呼呜……呼呜、哈咕呜♪」`); // :2785
        await era.printAndWait(`${target_name}开心的喘息着`); // :2786
        // CFLAG:TARGET:346  = 5（变量语义：CFLAG 族，TARGET:346） // :2787
        era.set(`cflag:${target}:346`, 5); // :2787

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:346`) <= 3 || era.get('flag:7') == 2)) { // :2789
        await era.printAndWait(`「呼嘎、呼咕呜……呼咕♪」`); // :2790
        // CFLAG:TARGET:346  = 4（变量语义：CFLAG 族，TARGET:346） // :2791
        era.set(`cflag:${target}:346`, 4); // :2791

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:346`) <= 2 || era.get('flag:7') == 2)) { // :2793
        await era.printAndWait(`「呼嘎、呼咕呜……呼咕♪」`); // :2794
        // CFLAG:TARGET:346  = 3（变量语义：CFLAG 族，TARGET:346） // :2795
        era.set(`cflag:${target}:346`, 3); // :2795

      } else if (era.get(`cflag:${target}:346`) <= 1 || era.get('flag:7') == 2) { // :2797
        await era.printAndWait(`「嘎呼～……呼咕呜……呼嘎啊！」`); // :2798
        // CFLAG:TARGET:346  = 2（变量语义：CFLAG 族，TARGET:346） // :2799
        era.set(`cflag:${target}:346`, 2); // :2799
      } // :2800
      return 0; // :2801
    } // :2802

  } else if (era_flag.selectcom == 45 && era.get(`tequip:${target}:45`) == 0) { // :2804

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:386`) < 3 || era.get('flag:7') == 2)) { // :2806
      await era.printAndWait(`「噗哈啊……。已、已经结束了吗？」`); // :2807
      // CFLAG:386  = 3（变量语义：CFLAG 族，386） // :2808
      era.set(`cflag:${target}:386`, 3); // :2808

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:386`) < 2 || era.get('flag:7') == 2)) { // :2810
      await era.printAndWait(`「哈呼呜……好辛苦呢」`); // :2811
      // CFLAG:386  = 2（变量语义：CFLAG 族，386） // :2812
      era.set(`cflag:${target}:386`, 2); // :2812

    } else if (era.get(`cflag:${target}:386`) < 1 || era.get('flag:7') == 2) { // :2814
      await era.printAndWait(`「哈啊哈啊……唔、真是屈辱啊」`); // :2815
      // CFLAG:386  = 1（变量语义：CFLAG 族，386） // :2816
      era.set(`cflag:${target}:386`, 1); // :2816
    } // :2817
    return 0; // :2818
  } // :2819





  if (era_flag.selectcom == 46 && era.get(`tequip:${target}:46`)) { // :2825

    if (era.get(`cflag:${target}:347`) == 0) { // :2827

      if (era.get(`talent:${target}:76`) == 1) { // :2829
        await era.printAndWait(''); // :2830

      } else if (era.get(`talent:${target}:85`) == 1) { // :2832
        await era.printAndWait(''); // :2833

      } else { // :2835
        await era.printAndWait(''); // :2836
      } // :2837
      // CFLAG:TARGET:347  = 1（变量语义：CFLAG 族，TARGET:347） // :2838
      era.set(`cflag:${target}:347`, 1); // :2838
      return 0; // :2839

    } else { // :2841

      if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 6 || era.get('flag:7') == 2)) { // :2843
        await era.printAndWait(''); // :2844
        // CFLAG:347  = 7（变量语义：CFLAG 族，347） // :2845
        era.set(`cflag:${target}:347`, 7); // :2845

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:347`) <= 5 || era.get('flag:7') == 2)) { // :2847
        await era.printAndWait(''); // :2848
        // CFLAG:347  = 6（变量语义：CFLAG 族，347） // :2849
        era.set(`cflag:${target}:347`, 6); // :2849

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 4 || era.get('flag:7') == 2)) { // :2851
        await era.printAndWait(''); // :2852
        // CFLAG:347  = 5（变量语义：CFLAG 族，347） // :2853
        era.set(`cflag:${target}:347`, 5); // :2853

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:347`) <= 3 || era.get('flag:7') == 2)) { // :2855
        await era.printAndWait(''); // :2856
        // CFLAG:347  = 4（变量语义：CFLAG 族，347） // :2857
        era.set(`cflag:${target}:347`, 4); // :2857

      } else if (era.get(`abl:${target}:3`) >= 3 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:347`) <= 2 || era.get('flag:7') == 2)) { // :2859
        await era.printAndWait(''); // :2860
        // CFLAG:347  = 3（变量语义：CFLAG 族，347） // :2861
        era.set(`cflag:${target}:347`, 3); // :2861

      } else if (era.get(`cflag:${target}:347`) <= 1 || era.get('flag:7') == 2) { // :2863
        await era.printAndWait(''); // :2864
        // CFLAG:347  = 2（变量语义：CFLAG 族，347） // :2865
        era.set(`cflag:${target}:347`, 2); // :2865
      } // :2866
      return 0; // :2867
    } // :2868
  } // :2869




  if (era_flag.selectcom == 55) { // :2874

    if (era.get(`cflag:${target}:356`) == 0) { // :2876

      if (era.get(`talent:${target}:85`) == 1) { // :2878
        await era.printAndWait(''); // :2879

      } else { // :2881
        await era.printAndWait(''); // :2882
      } // :2883
      // CFLAG:356  = 1（变量语义：CFLAG 族，356） // :2884
      era.set(`cflag:${target}:356`, 1); // :2884
      return 0; // :2885

    } else { // :2887

      if (era.get(`talent:${target}:85`) == 1 && era.get(`palam:${target}:5`) >= era.get('palamlv:3') && (era.get(`cflag:${target}:356`) <= 3 || era.get('flag:7') == 2)) { // :2889
        await era.printAndWait(''); // :2890
        // CFLAG:356  = 4（变量语义：CFLAG 族，356） // :2891
        era.set(`cflag:${target}:356`, 4); // :2891

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:356`) <= 2 || era.get('flag:7') == 2)) { // :2893
        await era.printAndWait(''); // :2894
        // CFLAG:356  = 3（变量语义：CFLAG 族，356） // :2895
        era.set(`cflag:${target}:356`, 3); // :2895

      } else if (era.get(`cflag:${target}:356`) <= 1 || era.get('flag:7') == 2) { // :2897
        await era.printAndWait(''); // :2898
        // CFLAG:356  = 2（变量语义：CFLAG 族，356） // :2899
        era.set(`cflag:${target}:356`, 2); // :2899
      } // :2900
      return 0; // :2901
    } // :2902
  } // :2903





  if (era_flag.selectcom == 56) { // :2909

    if (era.get(`cflag:${target}:357`) == 0) { // :2911
      if (era.get(`tequip:${target}:53`)) { // :2912


        if (era.get(`talent:${target}:76`) == 1) { // :2915
          await era.printAndWait(`「初次见面、我是前勇者${target_name}哦。很感谢大家今天的收看」`); // :2916
          await era.printAndWait(`「${sc()}现在已经对拯救世界啊、为了大家而战啊、等诸如此类的事物没有兴趣了」`); // :2917
          await era.printAndWait(`「现在${sc()}最大的兴趣爱好是、如何最舒服的做爱的方法」`); // :2918
          await era.printAndWait(`「这个身体将会产生怎样的淫乱变化呢、希望大家从现在开始好好看着哦」`); // :2919
          await era.printAndWait(`${target_name}一边这样说着一边煽情地舒展着身体……`); // :2920

        } else if (era.get(`talent:${target}:85`) == 1) { // :2922
          await era.printAndWait(`「初次见面、我是前勇者${target_name}哦。很感谢大家今天的收看」`); // :2923
          await era.printAndWait(`「${sc()}现在已经对拯救世界啊、与邪恶战斗啊、等诸如此类的事物没有兴趣了」`); // :2924
          await era.printAndWait(`「现在${sc()}最大的兴趣爱好是、用这个肉体孕育魔王的孩子」`); // :2925
          await era.printAndWait(`「用这个身体孕育爱的结晶的姿态……希望你们好好看着吧」`); // :2926
          await era.printAndWait(`${target_name}一边这样说着一边煽情地舒展着身体……`); // :2927

        } else { // :2929
          await era.printAndWait(''); // :2930
        } // :2931
      } else { // :2932

        if (era.get(`talent:${target}:76`) == 1) { // :2934
          await era.printAndWait(''); // :2935

        } else if (era.get(`talent:${target}:85`) == 1) { // :2937
          await era.printAndWait(''); // :2938

        } else { // :2940
          await era.printAndWait(''); // :2941
        } // :2942
      } // :2943
      // CFLAG:357  = 1（变量语义：CFLAG 族，357） // :2944
      era.set(`cflag:${target}:357`, 1); // :2944
      return 0; // :2945

    } else { // :2947
      if (era.get(`tequip:${target}:53`)) { // :2948


        if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :2951
          await era.printAndWait(`「又见面了呢？　我是肉奴隶${target_name}哦。一直以来多谢关照」`); // :2952
          await era.printAndWait(`「${sc()}的肉体将被培养成什么样呢、最近感觉越来越淫乱了呢」`); // :2953
          await era.printAndWait(`「能感受到最高快乐的魔之性爱、今天也要开始研究了哦」`); // :2954
          await era.printAndWait(`「这个身体将会产生怎样的淫乱变化呢、希望大家从现在开始好好看着哦」`); // :2955
          await era.printAndWait(`${target_name}一边这样说着一边煽情地舒展着身体……`); // :2956
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :2957
          era.set(`cflag:${target}:357`, 4); // :2957

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :2959
          await era.printAndWait(`「又见面了呢？　我是爱奴隶${target_name}哦。一直以来多谢关照」`); // :2960
          await era.printAndWait(`「为了最美妙的怀孕、今天也要为生孩子而做爱呢……呐？」`); // :2961
          await era.printAndWait(`「单单是想象这卑微的身体怀上魔王的孩子的时候……哈啊、好像就要高潮了呢」`); // :2962
          await era.printAndWait(`「用这个身体孕育爱的结晶的姿态……希望你们好好看着吧」`); // :2963
          await era.printAndWait(`${target_name}一边这样说着一边煽情地舒展着身体……`); // :2964
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :2965
          era.set(`cflag:${target}:357`, 3); // :2965

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :2967
          await era.printAndWait(''); // :2968
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :2969
          era.set(`cflag:${target}:357`, 2); // :2969
        } // :2970
      } else { // :2971

        if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :2973
          await era.printAndWait(''); // :2974
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :2975
          era.set(`cflag:${target}:357`, 4); // :2975

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :2977
          await era.printAndWait(''); // :2978
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :2979
          era.set(`cflag:${target}:357`, 3); // :2979

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :2981
          await era.printAndWait(''); // :2982
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :2983
          era.set(`cflag:${target}:357`, 2); // :2983
        } // :2984
      } // :2985
      return 0; // :2986
    } // :2987
  } // :2988



  if (era_flag.selectcom == 123) { // :2992

    if (era.get(`cflag:${target}:360`) == 0) { // :2994

      if (era.get(`talent:${target}:76`) == 1) { // :2996
        await era.printAndWait(''); // :2997

      } else if (era.get(`talent:${target}:85`) == 1) { // :2999
        await era.printAndWait(''); // :3000

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3002
        await era.printAndWait(''); // :3003

      } else { // :3005
        await era.printAndWait(''); // :3006
      } // :3007
      // CFLAG:TARGET:360  = 1（变量语义：CFLAG 族，TARGET:360） // :3008
      era.set(`cflag:${target}:360`, 1); // :3008
      return 0; // :3009

    } else { // :3011

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:360`) <= 4 || era.get('flag:7') == 2)) { // :3013
        await era.printAndWait(''); // :3014
        // CFLAG:360  = 5（变量语义：CFLAG 族，360） // :3015
        era.set(`cflag:${target}:360`, 5); // :3015

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:360`) <= 3 || era.get('flag:7') == 2)) { // :3017
        await era.printAndWait(''); // :3018
        // CFLAG:360  = 4（变量语义：CFLAG 族，360） // :3019
        era.set(`cflag:${target}:360`, 4); // :3019

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:360`) <= 2 || era.get('flag:7') == 2)) { // :3021
        await era.printAndWait(''); // :3022
        // CFLAG:360  = 3（变量语义：CFLAG 族，360） // :3023
        era.set(`cflag:${target}:360`, 3); // :3023

      } else if (era.get(`cflag:${target}:360`) <= 1 || era.get('flag:7') == 2) { // :3025
        await era.printAndWait(''); // :3026
        // CFLAG:360  = 2（变量语义：CFLAG 族，360） // :3027
        era.set(`cflag:${target}:360`, 2); // :3027
      } // :3028
      return 0; // :3029
    } // :3030
  } // :3031



  if (era_flag.selectcom == 125) { // :3035

    if (era.get(`cflag:${target}:361`) == 0) { // :3037

      if (era.get(`talent:${target}:76`) == 1) { // :3039
        await era.printAndWait(''); // :3040

      } else if (era.get(`talent:${target}:85`) == 1) { // :3042
        await era.printAndWait(''); // :3043

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3045
        await era.printAndWait(''); // :3046

      } else { // :3048
        await era.printAndWait(''); // :3049
      } // :3050
      // CFLAG:TARGET:361  = 1（变量语义：CFLAG 族，TARGET:361） // :3051
      era.set(`cflag:${target}:361`, 1); // :3051
      return 0; // :3052

    } else { // :3054

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:361`) <= 4 || era.get('flag:7') == 2)) { // :3056
        await era.printAndWait(''); // :3057
        // CFLAG:361  = 5（变量语义：CFLAG 族，361） // :3058
        era.set(`cflag:${target}:361`, 5); // :3058

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:361`) <= 3 || era.get('flag:7') == 2)) { // :3060
        await era.printAndWait(''); // :3061
        // CFLAG:361  = 4（变量语义：CFLAG 族，361） // :3062
        era.set(`cflag:${target}:361`, 4); // :3062

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:361`) <= 2 || era.get('flag:7') == 2)) { // :3064
        await era.printAndWait(''); // :3065
        // CFLAG:361  = 3（变量语义：CFLAG 族，361） // :3066
        era.set(`cflag:${target}:361`, 3); // :3066

      } else if (era.get(`cflag:${target}:361`) <= 1 || era.get('flag:7') == 2) { // :3068
        await era.printAndWait(''); // :3069
        // CFLAG:361  = 2（变量语义：CFLAG 族，361） // :3070
        era.set(`cflag:${target}:361`, 2); // :3070
      } // :3071
      return 0; // :3072
    } // :3073
  } // :3074




  if (era_flag.selectcom == 126) { // :3079

    if (era.get(`cflag:${target}:362`) == 0) { // :3081

      if (era.get(`talent:${target}:76`) == 1) { // :3083
        await era.printAndWait(''); // :3084

      } else if (era.get(`talent:${target}:85`) == 1) { // :3086
        await era.printAndWait(''); // :3087

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3089
        await era.printAndWait(''); // :3090

      } else { // :3092
        await era.printAndWait(''); // :3093
      } // :3094
      // CFLAG:TARGET:362  = 1（变量语义：CFLAG 族，TARGET:362） // :3095
      era.set(`cflag:${target}:362`, 1); // :3095
      return 0; // :3096

    } else { // :3098

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:362`) <= 4 || era.get('flag:7') == 2)) { // :3100
        await era.printAndWait(''); // :3101
        // CFLAG:362  = 5（变量语义：CFLAG 族，362） // :3102
        era.set(`cflag:${target}:362`, 5); // :3102

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:362`) <= 3 || era.get('flag:7') == 2)) { // :3104
        await era.printAndWait(''); // :3105
        // CFLAG:362  = 4（变量语义：CFLAG 族，362） // :3106
        era.set(`cflag:${target}:362`, 4); // :3106

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:362`) <= 2 || era.get('flag:7') == 2)) { // :3108
        await era.printAndWait(''); // :3109
        // CFLAG:362  = 3（变量语义：CFLAG 族，362） // :3110
        era.set(`cflag:${target}:362`, 3); // :3110

      } else if (era.get(`cflag:${target}:362`) <= 1 || era.get('flag:7') == 2) { // :3112
        await era.printAndWait(''); // :3113
        // CFLAG:362  = 2（变量语义：CFLAG 族，362） // :3114
        era.set(`cflag:${target}:362`, 2); // :3114
      } // :3115
      return 0; // :3116
    } // :3117
  } // :3118




  if (era_flag.selectcom == 127) { // :3123

    if (era.get(`cflag:${target}:363`) == 0) { // :3125

      if (era.get(`talent:${target}:76`) == 1) { // :3127
        await era.printAndWait(''); // :3128

      } else if (era.get(`talent:${target}:85`) == 1) { // :3130
        await era.printAndWait(''); // :3131

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3133
        await era.printAndWait(''); // :3134

      } else { // :3136
        await era.printAndWait(''); // :3137
      } // :3138
      // CFLAG:TARGET:363  = 1（变量语义：CFLAG 族，TARGET:363） // :3139
      era.set(`cflag:${target}:363`, 1); // :3139
      return 0; // :3140

    } else { // :3142

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:363`) <= 4 || era.get('flag:7') == 2)) { // :3144
        await era.printAndWait(''); // :3145
        // CFLAG:363  = 5（变量语义：CFLAG 族，363） // :3146
        era.set(`cflag:${target}:363`, 5); // :3146

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:363`) <= 3 || era.get('flag:7') == 2)) { // :3148
        await era.printAndWait(''); // :3149
        // CFLAG:363  = 4（变量语义：CFLAG 族，363） // :3150
        era.set(`cflag:${target}:363`, 4); // :3150

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:363`) <= 2 || era.get('flag:7') == 2)) { // :3152
        // CFLAG:363  = 3（变量语义：CFLAG 族，363） // :3153
        era.set(`cflag:${target}:363`, 3); // :3153

      } else if (era.get(`cflag:${target}:363`) <= 1 || era.get('flag:7') == 2) { // :3155
        await era.printAndWait(''); // :3156
        // CFLAG:363  = 2（变量语义：CFLAG 族，363） // :3157
        era.set(`cflag:${target}:363`, 2); // :3157
      } // :3158
      return 0; // :3159
    } // :3160
  } // :3161




  if (era_flag.selectcom == 69) { // :3166

    if (era.get(`cflag:${target}:364`) == 0) { // :3168

      if (era.get(`talent:${target}:76`) == 1) { // :3170
        await era.printAndWait(''); // :3171

      } else if (era.get(`talent:${target}:85`) == 1) { // :3173
        await era.printAndWait(''); // :3174

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3176
        await era.printAndWait(''); // :3177

      } else { // :3179
        await era.printAndWait(''); // :3180
      } // :3181
      // CFLAG:TARGET:364  = 1（变量语义：CFLAG 族，TARGET:364） // :3182
      era.set(`cflag:${target}:364`, 1); // :3182
      return 0; // :3183

    } else { // :3185

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:364`) <= 4 || era.get('flag:7') == 2)) { // :3187
        await era.printAndWait(''); // :3188
        // CFLAG:364  = 5（变量语义：CFLAG 族，364） // :3189
        era.set(`cflag:${target}:364`, 5); // :3189

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:364`) <= 3 || era.get('flag:7') == 2)) { // :3191
        await era.printAndWait(''); // :3192
        // CFLAG:364  = 4（变量语义：CFLAG 族，364） // :3193
        era.set(`cflag:${target}:364`, 4); // :3193

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:364`) <= 2 || era.get('flag:7') == 2)) { // :3195
        await era.printAndWait(''); // :3196
        // CFLAG:364  = 3（变量语义：CFLAG 族，364） // :3197
        era.set(`cflag:${target}:364`, 3); // :3197

      } else if (era.get(`cflag:${target}:364`) <= 1 || era.get('flag:7') == 2) { // :3199
        await era.printAndWait(''); // :3200
        // CFLAG:364  = 2（变量语义：CFLAG 族，364） // :3201
        era.set(`cflag:${target}:364`, 2); // :3201
      } // :3202
      return 0; // :3203
    } // :3204
  } // :3205




  if (era_flag.selectcom == 124) { // :3210

    if (era.get(`cflag:${target}:365`) == 0) { // :3212

      if (era.get(`talent:${target}:76`) == 1) { // :3214
        await era.printAndWait(''); // :3215

      } else if (era.get(`talent:${target}:85`) == 1) { // :3217
        await era.printAndWait(''); // :3218

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3220
        await era.printAndWait(''); // :3221

      } else { // :3223
        await era.printAndWait(''); // :3224
      } // :3225
      // CFLAG:TARGET:365  = 1（变量语义：CFLAG 族，TARGET:365） // :3226
      era.set(`cflag:${target}:365`, 1); // :3226
      return 0; // :3227

    } else { // :3229

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:363`) <= 4 || era.get('flag:7') == 2)) { // :3231
        await era.printAndWait(''); // :3232
        // CFLAG:365  = 5（变量语义：CFLAG 族，365） // :3233
        era.set(`cflag:${target}:365`, 5); // :3233

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:363`) <= 3 || era.get('flag:7') == 2)) { // :3235
        await era.printAndWait(''); // :3236
        // CFLAG:365  = 4（变量语义：CFLAG 族，365） // :3237
        era.set(`cflag:${target}:365`, 4); // :3237

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:363`) <= 2 || era.get('flag:7') == 2)) { // :3239
        await era.printAndWait(''); // :3240
        // CFLAG:365  = 3（变量语义：CFLAG 族，365） // :3241
        era.set(`cflag:${target}:365`, 3); // :3241

      } else if (era.get(`cflag:${target}:363`) <= 1 || era.get('flag:7') == 2) { // :3243
        await era.printAndWait(''); // :3244
        // CFLAG:365  = 2（变量语义：CFLAG 族，365） // :3245
        era.set(`cflag:${target}:365`, 2); // :3245
      } // :3246
      return 0; // :3247
    } // :3248
  } // :3249




  if (era_flag.selectcom == 80) { // :3254

    if (era.get(`cflag:${target}:381`) == 0) { // :3256

      if (era.get(`talent:${target}:76`) == 1) { // :3258
        await era.printAndWait(''); // :3259

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3261
        await era.printAndWait(''); // :3262

      } else { // :3264
        await era.printAndWait(''); // :3265
      } // :3266
      // CFLAG:TARGET:381  = 1（变量语义：CFLAG 族，TARGET:381） // :3267
      era.set(`cflag:${target}:381`, 1); // :3267
      return 0; // :3268

    } else { // :3270

      if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:381`) <= 4 || era.get('flag:7') == 2)) { // :3272
        await era.printAndWait(''); // :3273
        // CFLAG:381  = 5（变量语义：CFLAG 族，381） // :3274
        era.set(`cflag:${target}:381`, 5); // :3274

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:381`) <= 3 || era.get('flag:7') == 2)) { // :3276
        await era.printAndWait(''); // :3277
        // CFLAG:381  = 4（变量语义：CFLAG 族，381） // :3278
        era.set(`cflag:${target}:381`, 4); // :3278

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:381`) <= 2 || era.get('flag:7') == 2)) { // :3280
        await era.printAndWait(''); // :3281
        // CFLAG:381  = 3（变量语义：CFLAG 族，381） // :3282
        era.set(`cflag:${target}:381`, 3); // :3282

      } else if (era.get(`cflag:${target}:381`) <= 1 || era.get('flag:7') == 2) { // :3284
        await era.printAndWait(''); // :3285
        // CFLAG:381  = 2（变量语义：CFLAG 族，381） // :3286
        era.set(`cflag:${target}:381`, 2); // :3286
      } // :3287
      return 0; // :3288
    } // :3289
  } // :3290






  if (era_flag.selectcom == 87) { // :3297


    if (era.get(`cflag:${target}:348`) == 0) { // :3300

      if (era_flag.assi > 0 && era_flag.assiplay) { // :3302
        await era.printAndWait(''); // :3303

      } else if (era.get(`talent:${target}:76`) == 1) { // :3305

        if (era.get(`cflag:${target}:7`) & P) { // :3307
          await era.printAndWait(''); // :3308

          if (P == 1) { // :3310
            await era.printAndWait(`「${sc()}的乳头、变漂亮了哦」`); // :3311

          } else if (P == 2) { // :3313
            await era.printAndWait(`「${sc()}的肚脐、变漂亮了哦」`); // :3314

          } else if (P == 4) { // :3316
            await era.printAndWait(`「${sc()}的阴唇、变漂亮了哦」`); // :3317

          } else if (P == 8) { // :3319

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3321
              await era.printAndWait(`「${sc()}的阴茎、闪闪发光了呢……」`); // :3322
            } else { // :3323
              await era.printAndWait(`「${sc()}的阴蒂、变的好下流呢」`); // :3324
            } // :3325

          } else if (P == 16) { // :3327
            await era.printAndWait(`「不可思议的感觉呢……在嘴里面……」`); // :3328

          } else if (P == 32) { // :3330
            await era.printAndWait(''); // :3331

          } else if (P == 64) { // :3333
            await era.printAndWait(''); // :3334
          } // :3335

        } else { // :3337
          await era.printAndWait(''); // :3338
        } // :3339

      } else if (era.get(`talent:${target}:85`) == 1) { // :3341

        if (era.get(`cflag:${target}:7`) & P) { // :3343
          await era.printAndWait(''); // :3344

          if (P == 1) { // :3346
            await era.printAndWait(`「${sc()}的乳头、变漂亮了哦」`); // :3347

          } else if (P == 2) { // :3349
            await era.printAndWait(`「${sc()}的肚脐、变漂亮了哦」`); // :3350

          } else if (P == 4) { // :3352
            await era.printAndWait(`「${sc()}的阴唇、变漂亮了哦」`); // :3353

          } else if (P == 8) { // :3355

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3357
              await era.printAndWait(`「${sc()}的阴茎、闪闪发光了呢……」`); // :3358
            } else { // :3359
              await era.printAndWait(`「${sc()}的阴蒂、变的好下流呢」`); // :3360
            } // :3361

          } else if (P == 16) { // :3363
            await era.printAndWait(`「不可思议的感觉……在嘴里面……」`); // :3364

          } else if (P == 32) { // :3366
            await era.printAndWait(''); // :3367

          } else if (P == 64) { // :3369
            await era.printAndWait(''); // :3370
          } // :3371

        } else { // :3373
          await era.printAndWait(''); // :3374
        } // :3375

      } else { // :3377

        if (era.get(`cflag:${target}:7`) & P) { // :3379
          await era.printAndWait(''); // :3380

          if (P == 1) { // :3382
            await era.printAndWait(''); // :3383

          } else if (P == 2) { // :3385
            await era.printAndWait(''); // :3386

          } else if (P == 4) { // :3388
            await era.printAndWait(''); // :3389

          } else if (P == 8) { // :3391

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3393
              await era.printAndWait(''); // :3394
            } else { // :3395
              await era.printAndWait(''); // :3396
            } // :3397

          } else if (P == 16) { // :3399
            await era.printAndWait(''); // :3400

          } else if (P == 32) { // :3402
            await era.printAndWait(''); // :3403

          } else if (P == 64) { // :3405
            await era.printAndWait(''); // :3406
          } // :3407

        } else { // :3409
          await era.printAndWait(''); // :3410
        } // :3411
      } // :3412
      // CFLAG:TARGET:348  = 1（变量语义：CFLAG 族，TARGET:348） // :3413
      era.set(`cflag:${target}:348`, 1); // :3413
      return 0; // :3414

    } else { // :3416

      if (era_flag.assi > 0 && era_flag.assiplay) { // :3418
        await era.printAndWait(''); // :3419

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:348`) <= 3 || era.get('flag:7') == 2)) { // :3421

        if (era.get(`cflag:${target}:7`) & P) { // :3423

          if (P == 1) { // :3425
            await era.printAndWait(`「${sc()}的乳头、变漂亮了哦」`); // :3426

          } else if (P == 2) { // :3428
            await era.printAndWait(`「${sc()}的肚脐、变漂亮了哦」`); // :3429

          } else if (P == 4) { // :3431
            await era.printAndWait(`「${sc()}的阴唇、变漂亮了哦」`); // :3432

          } else if (P == 8) { // :3434

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3436
              await era.printAndWait(`「${sc()}的阴茎、闪闪发光了呢……」`); // :3437
            } else { // :3438
              await era.printAndWait(`「${sc()}的阴蒂、变的好下流呢」`); // :3439
            } // :3440

          } else if (P == 16) { // :3442
            await era.printAndWait(`「嘴里……感觉好奇怪」`); // :3443

          } else if (P == 32) { // :3445
            await era.printAndWait(''); // :3446

          } else if (P == 64) { // :3448
            await era.printAndWait(''); // :3449
          } // :3450

        } else { // :3452
          await era.printAndWait(''); // :3453
        } // :3454
        // CFLAG:348  = 4（变量语义：CFLAG 族，348） // :3455
        era.set(`cflag:${target}:348`, 4); // :3455

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:348`) <= 2 || era.get('flag:7') == 2)) { // :3457

        if (era.get(`cflag:${target}:7`) & P) { // :3459

          if (P == 1) { // :3461
            await era.printAndWait(`「${sc()}的乳头、变漂亮了哦」`); // :3462

          } else if (P == 2) { // :3464
            await era.printAndWait(`「${sc()}的肚脐、变漂亮了哦」`); // :3465

          } else if (P == 4) { // :3467
            await era.printAndWait(`「${sc()}的阴唇、变漂亮了哦」`); // :3468

          } else if (P == 8) { // :3470
            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3471
              await era.printAndWait(`「${sc()}的阴茎、闪闪发光了呢……」`); // :3472
            } else { // :3473
              await era.printAndWait(`「${sc()}的阴蒂、变的好下流呢」`); // :3474
            } // :3475

          } else if (P == 16) { // :3477
            await era.printAndWait(`「嘴里……感觉好奇怪」`); // :3478

          } else if (P == 32) { // :3480
            await era.printAndWait(''); // :3481

          } else if (P == 64) { // :3483
            await era.printAndWait(''); // :3484
          } // :3485

        } else { // :3487
          await era.printAndWait(''); // :3488
        } // :3489
        // CFLAG:348  = 3（变量语义：CFLAG 族，348） // :3490
        era.set(`cflag:${target}:348`, 3); // :3490

      } else if (era.get(`cflag:${target}:348`) <= 1 || era.get('flag:7') == 2) { // :3492

        if (era.get(`cflag:${target}:7`) & P) { // :3494

          if (P == 1) { // :3496
            await era.printAndWait(''); // :3497

          } else if (P == 2) { // :3499
            await era.printAndWait(''); // :3500

          } else if (P == 4) { // :3502
            await era.printAndWait(''); // :3503

          } else if (P == 8) { // :3505

            if (era.get(`talent:${target}:121`) || era.get(`talent:${target}:122`)) { // :3507
              await era.printAndWait(''); // :3508
            } else { // :3509
              await era.printAndWait(''); // :3510
            } // :3511

          } else if (P == 16) { // :3513
            await era.printAndWait(''); // :3514

          } else if (P == 32) { // :3516
            await era.printAndWait(''); // :3517

          } else if (P == 64) { // :3519
            await era.printAndWait(''); // :3520
          } // :3521

        } else { // :3523
          await era.printAndWait(''); // :3524
        } // :3525
        // CFLAG:348  = 2（变量语义：CFLAG 族，348） // :3526
        era.set(`cflag:${target}:348`, 2); // :3526
      } // :3527
    } // :3528
    return 0; // :3529
  } // :3530







}

// @DOG_KOJO_12 // :3538
async function DOG_KOJO_12() {




  if (era_flag.selectcom == 0) { // :3543

    if (era.get(`cflag:${target}:301`) == 0) { // :3545

      if (era.get(`mark:${target}:2`) >= 2) { // :3547
        await era.printAndWait(`「呜呜、我知道了、我会乖乖做的……」`); // :3548

      } else { // :3550
        await era.printAndWait(`「讨厌！　不要啊！　住手～！！」`); // :3551
      } // :3552
      // CFLAG:301  = 1（变量语义：CFLAG 族，301） // :3553
      era.set(`cflag:${target}:301`, 1); // :3553
      return 0; // :3554

    } else { // :3556

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:301`) <= 6 || era.get('flag:7') == 2)) { // :3558
        await era.printAndWait(`「${sc()}的身体、还想再被舔遍各个角落呢」`); // :3559
        // CFLAG:301  = 7（变量语义：CFLAG 族，301） // :3560
        era.set(`cflag:${target}:301`, 7); // :3560

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:301`) <= 5 || era.get('flag:7') == 2)) { // :3562
        await era.printAndWait(`「涂上黄油会更好些吧」`); // :3563
        // CFLAG:301  = 6（变量语义：CFLAG 族，301） // :3564
        era.set(`cflag:${target}:301`, 6); // :3564

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:301`) <= 4 || era.get('flag:7') == 2)) { // :3566
        await era.printAndWait(`「涂上黄油会更好些吧」`); // :3567
        // CFLAG:301  = 5（变量语义：CFLAG 族，301） // :3568
        era.set(`cflag:${target}:301`, 5); // :3568

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:301`) <= 3 || era.get('flag:7') == 2)) { // :3570
        await era.printAndWait(`「我知道了、我会乖乖做的……」`); // :3571
        // CFLAG:301  = 4（变量语义：CFLAG 族，301） // :3572
        era.set(`cflag:${target}:301`, 4); // :3572

      } else if (era.get(`mark:${target}:2`) == 2 && (era.get(`cflag:${target}:301`) <= 2 || era.get('flag:7') == 2)) { // :3574
        await era.printAndWait(`「讨厌、住手……不要啊」`); // :3575
        // CFLAG:301  = 3（变量语义：CFLAG 族，301） // :3576
        era.set(`cflag:${target}:301`, 3); // :3576

      } else if (era.get(`mark:${target}:2`) <= 1 && (era.get(`cflag:${target}:301`) <= 1 || era.get('flag:7') == 2)) { // :3578
        await era.printAndWait(`「讨厌、讨厌！　不要啊！」`); // :3579
        // CFLAG:301  = 2（变量语义：CFLAG 族，301） // :3580
        era.set(`cflag:${target}:301`, 2); // :3580
      } // :3581
      return 0; // :3582
    } // :3583
  } // :3584




  if (era_flag.selectcom == 1) { // :3589

    if (era.get(`cflag:${target}:302`) == 0) { // :3591

      if (era.get(`talent:${target}:0`) == 1) { // :3593
        await era.printAndWait(`「呜呜……这么重要的地方被舔了」`); // :3594

      } else { // :3596
        await era.printAndWait(`「呜呜……这么敏感的地方被舔着」`); // :3597
      } // :3598
      // CFLAG:302  = 1（变量语义：CFLAG 族，302） // :3599
      era.set(`cflag:${target}:302`, 1); // :3599
      return 0; // :3600

    } else { // :3602

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:302`) <= 5 || era.get('flag:7') == 2)) { // :3604
        await era.printAndWait(`「味道不错吧、${sc()}的阴部……呵呵、尽管舔吧」`); // :3605
        // CFLAG:302  = 6（变量语义：CFLAG 族，302） // :3606
        era.set(`cflag:${target}:302`, 6); // :3606

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:302`) <= 4 || era.get('flag:7') == 2)) { // :3608
        await era.printAndWait(`「这个地方也要涂上黄油吗？」`); // :3609
        // CFLAG:302  = 5（变量语义：CFLAG 族，302） // :3610
        era.set(`cflag:${target}:302`, 5); // :3610

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:302`) <= 3 || era.get('flag:7') == 2)) { // :3612
        await era.printAndWait(`「这个地方也要涂上黄油吗？」`); // :3613
        // CFLAG:302  = 4（变量语义：CFLAG 族，302） // :3614
        era.set(`cflag:${target}:302`, 4); // :3614

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:302`) <= 2 || era.get('flag:7') == 2)) { // :3616
        await era.printAndWait(`「遵命……涂上黄油就好了吧」`); // :3617
        // CFLAG:302  = 3（变量语义：CFLAG 族，302） // :3618
        era.set(`cflag:${target}:302`, 3); // :3618

      } else if (era.get(`cflag:${target}:302`) <= 1 || era.get('flag:7') == 2) { // :3620
        await era.printAndWait(`「呜呜、好恶心……这样子、违反人伦啊……」`); // :3621
        // CFLAG:302  = 2（变量语义：CFLAG 族，302） // :3622
        era.set(`cflag:${target}:302`, 2); // :3622
      } // :3623
      return 0; // :3624
    } // :3625
  } // :3626





  if (era_flag.selectcom == 5) { // :3632

    if (era.get(`cflag:${target}:306`) == 0) { // :3634

      if (era.get(`talent:${target}:85`) == 1) { // :3636
        await era.printAndWait(`「不要、快停下啊」`); // :3637

      } else { // :3639
        await era.printAndWait(`「唔……好奇怪的感觉」`); // :3640
      } // :3641
      // CFLAG:TARGET:306  = 1（变量语义：CFLAG 族，TARGET:306） // :3642
      era.set(`cflag:${target}:306`, 1); // :3642
      return 0; // :3643

    } else { // :3645

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:306`) <= 5 || era.get('flag:7') == 2)) { // :3647
        await era.printAndWait(`「牙咬在乳头上……bilibili的♪」`); // :3648
        // CFLAG:306  = 6（变量语义：CFLAG 族，306） // :3649
        era.set(`cflag:${target}:306`, 6); // :3649

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:306`) <= 4 || era.get('flag:7') == 2)) { // :3651
        await era.printAndWait(`「哈啊、胸部好吃吗？」`); // :3652
        // CFLAG:306  = 5（变量语义：CFLAG 族，306） // :3653
        era.set(`cflag:${target}:306`, 5); // :3653

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:306`) <= 3 || era.get('flag:7') == 2)) { // :3655
        await era.printAndWait(`「哈啊、继续吸胸部吧」`); // :3656
        // CFLAG:306  = 4（变量语义：CFLAG 族，306） // :3657
        era.set(`cflag:${target}:306`, 4); // :3657

      } else if (era.get(`abl:${target}:1`) >= 3 && (era.get(`cflag:${target}:306`) <= 2 || era.get('flag:7') == 2)) { // :3659
        await era.printAndWait(`「咕……被狗、弄得有感觉了……」`); // :3660
        // CFLAG:306  = 3（变量语义：CFLAG 族，306） // :3661
        era.set(`cflag:${target}:306`, 3); // :3661

      } else if (era.get(`cflag:${target}:306`) <= 1 || era.get('flag:7') == 2) { // :3663
        await era.printAndWait(`「卑鄙……这只会让我感觉不舒服」`); // :3664
        // CFLAG:306  = 2（变量语义：CFLAG 族，306） // :3665
        era.set(`cflag:${target}:306`, 2); // :3665
      } // :3666
      return 0; // :3667
    } // :3668
  } // :3669



  if (era_flag.selectcom == 6) { // :3673

    if (era.get(`cflag:${target}:307`) == 0 && era.get('tflag:13')) { // :3675

      if (era.get(`talent:${target}:136`) == 1) { // :3677
        await era.printAndWait(''); // :3678

      } else if (era.get(`talent:${target}:76`) == 1) { // :3680
        await era.printAndWait(''); // :3681

      } else if (era.get(`talent:${target}:85`) == 1) { // :3683
        await era.printAndWait(''); // :3684

      } else { // :3686
        await era.printAndWait(''); // :3687
      } // :3688
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :3689
      era.set(`cflag:${target}:307`, 1); // :3689
      return 0; // :3690

    } else if (era.get(`cflag:${target}:307`) == 0) { // :3692

      if (era.get(`talent:${target}:136`) == 1) { // :3694
        await era.printAndWait(''); // :3695

      } else if (era.get(`talent:${target}:76`) == 1) { // :3697
        await era.printAndWait(''); // :3698

      } else if (era.get(`talent:${target}:85`) == 1) { // :3700
        await era.printAndWait(''); // :3701

      } else { // :3703
        await era.printAndWait(''); // :3704
      } // :3705
      // CFLAG:307  = 1（变量语义：CFLAG 族，307） // :3706
      era.set(`cflag:${target}:307`, 1); // :3706
      return 0; // :3707

    } else { // :3709

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:307`) <= 5 || era.get('flag:7') == 2)) { // :3711
        await era.printAndWait(''); // :3712
        // CFLAG:307  = 6（变量语义：CFLAG 族，307） // :3713
        era.set(`cflag:${target}:307`, 6); // :3713

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:307`) <= 4 || era.get('flag:7') == 2)) { // :3715
        await era.printAndWait(''); // :3716
        // CFLAG:307  = 5（变量语义：CFLAG 族，307） // :3717
        era.set(`cflag:${target}:307`, 5); // :3717

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:307`) <= 3 || era.get('flag:7') == 2)) { // :3719
        await era.printAndWait(''); // :3720
        // CFLAG:307  = 4（变量语义：CFLAG 族，307） // :3721
        era.set(`cflag:${target}:307`, 4); // :3721

      } else if (era.get(`abl:${target}:10`) >=2 && (era.get(`cflag:${target}:307`) <= 2 || era.get('flag:7') == 2)) { // :3723
        await era.printAndWait(''); // :3724
        // CFLAG:307  = 3（变量语义：CFLAG 族，307） // :3725
        era.set(`cflag:${target}:307`, 3); // :3725

      } else if (era.get(`cflag:${target}:307`) <= 1 || era.get('flag:7') == 2) { // :3727
        await era.printAndWait(''); // :3728
        // CFLAG:307  = 2（变量语义：CFLAG 族，307） // :3729
        era.set(`cflag:${target}:307`, 2); // :3729
      } // :3730
      return 0; // :3731
    } // :3732
  } // :3733




  if (era_flag.selectcom == 9) { // :3738

    if (era.get(`cflag:${target}:310`) == 0) { // :3740

      if (era.get(`talent:${target}:136`) == 1) { // :3742
        await era.printAndWait(''); // :3743

      } else if (era.get(`talent:${target}:76`) == 1) { // :3745
        await era.printAndWait(''); // :3746

      } else if (era.get(`talent:${target}:85`) == 1) { // :3748
        await era.printAndWait(''); // :3749

      } else { // :3751
        await era.printAndWait(''); // :3752
      } // :3753
      // CFLAG:TARGET:310  = 1（变量语义：CFLAG 族，TARGET:310） // :3754
      era.set(`cflag:${target}:310`, 1); // :3754
      return 0; // :3755

    } else { // :3757

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:310`) <= 5 || era.get('flag:7') == 2)) { // :3759
        await era.printAndWait(''); // :3760
        // CFLAG:310  = 6（变量语义：CFLAG 族，310） // :3761
        era.set(`cflag:${target}:310`, 6); // :3761

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:310`) <= 4 || era.get('flag:7') == 2)) { // :3763
        await era.printAndWait(''); // :3764
        // CFLAG:310  = 5（变量语义：CFLAG 族，310） // :3765
        era.set(`cflag:${target}:310`, 5); // :3765

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:310`) <= 3 || era.get('flag:7') == 2)) { // :3767
        await era.printAndWait(''); // :3768
        // CFLAG:310  = 4（变量语义：CFLAG 族，310） // :3769
        era.set(`cflag:${target}:310`, 4); // :3769

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:310`) <= 2 || era.get('flag:7') == 2)) { // :3771
        await era.printAndWait(''); // :3772
        // CFLAG:310  = 3（变量语义：CFLAG 族，310） // :3773
        era.set(`cflag:${target}:310`, 3); // :3773

      } else if (era.get(`cflag:${target}:310`) <= 1 || era.get('flag:7') == 2) { // :3775
        await era.printAndWait(''); // :3776
        // CFLAG:310  = 2（变量语义：CFLAG 族，310） // :3777
        era.set(`cflag:${target}:310`, 2); // :3777
      } // :3778
      return 0; // :3779
    } // :3780
  } // :3781




  if (era_flag.selectcom == 21) { // :3786

    if (era.get(`cflag:${target}:322`) == 0) { // :3788

      if (era.get(`talent:${target}:0`) == 1) { // :3790

        if (era.get(`talent:${target}:136`) == 1) { // :3792
          await era.printAndWait(`「讨厌～！　${sc()}、终于要成为母狗了呢！」`); // :3793

        } else if (era.get(`talent:${target}:76`) == 1) { // :3795
          await era.printAndWait(`「交配实验吗……好期待呢♪」`); // :3796
        } else if (era.get(`talent:${target}:85`) == 1) { // :3797
          await era.printAndWait(`「要怀上狗宝宝了吗」`); // :3798


        } else { // :3801
          await era.printAndWait(`「咿呀啊、异常、这样太异常了……」`); // :3802
        } // :3803

      } else { // :3805

        if (era.get(`talent:${target}:136`) == 1) { // :3807
          await era.printAndWait(`「讨厌～！　${sc()}、终于可以交尾了呢！」`); // :3808

        } else if (era.get(`talent:${target}:76`) == 1) { // :3810
          await era.printAndWait(`「嗯、终于可以做交配实验了呢。让我来帮忙吧」`); // :3811

        } else if (era.get(`talent:${target}:85`) == 1) { // :3813
          await era.printAndWait(`「真的、要怀孕了吗……」`); // :3814

        } else { // :3816
          await era.printAndWait(`「咕呜～、讨厌、已经、够了……」`); // :3817
        } // :3818
      } // :3819
      // CFLAG:322  = 1（变量语义：CFLAG 族，322） // :3820
      era.set(`cflag:${target}:322`, 1); // :3820
      return 0; // :3821

    } else { // :3823

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:322`) <= 6 || era.get('flag:7') == 2)) { // :3825
        if (rand_n(3) == 0) { // :3826
          await era.printAndWait(`「汪汪！　狗的阴茎好爽啊♪　${sc()}、想怀上狗宝宝～♪」`); // :3827
        } else if (rand_n(2) == 0) { // :3828
          await era.printAndWait(`「嗯啊啊啊～、${sc()}、要变成母狗了哦♪」`); // :3829
        } else { // :3830
          await era.printAndWait(`「在交尾呢……${sc()}、在做异种交配呢♪」`); // :3831
        } // :3832
        // CFLAG:322  = 7（变量语义：CFLAG 族，322） // :3833
        era.set(`cflag:${target}:322`, 7); // :3833

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:322`) <= 5 || era.get('flag:7') == 2)) { // :3835
        if (rand_n(3) == 0) { // :3836
          await era.printAndWait(`「哦、今天也是交配实验吗。让我来帮忙吧」`); // :3837
        } else if (rand_n(2) == 0) { // :3838
          await era.printAndWait(`「随时都可以继续哦？　这个交配实验……」`); // :3839
        } else { // :3840
          await era.printAndWait(`「我知道了。让我来帮忙吧」`); // :3841
        } // :3842
        // CFLAG:322  = 6（变量语义：CFLAG 族，322） // :3843
        era.set(`cflag:${target}:322`, 6); // :3843

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:322`) <= 4 || era.get('flag:7') == 2)) { // :3845
        if (rand_n(3) == 0) { // :3846
          await era.printAndWait(`「真的要怀上了？」`); // :3847
        } else if (rand_n(2) == 0) { // :3848
          await era.printAndWait(`「看来不会怀孕呢……」`); // :3849
        } else { // :3850
          await era.printAndWait(`「很担心会不会得病呢」`); // :3851
        } // :3852
        // CFLAG:322  = 5（变量语义：CFLAG 族，322） // :3853
        era.set(`cflag:${target}:322`, 5); // :3853

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:322`) <= 3 || era.get('flag:7') == 2)) { // :3855
        await era.printAndWait(`「咕呜～、被狗弄得……有感觉了……」`); // :3856
        // CFLAG:322  = 4（变量语义：CFLAG 族，322） // :3857
        era.set(`cflag:${target}:322`, 4); // :3857

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:322`) <= 2 || era.get('flag:7') == 2)) { // :3859
        await era.printAndWait(`「交配实验吗。我知道了……」`); // :3860
        // CFLAG:322  = 3（变量语义：CFLAG 族，322） // :3861
        era.set(`cflag:${target}:322`, 3); // :3861

      } else if (era.get(`cflag:${target}:322`) <= 1 || era.get('flag:7') == 2) { // :3863
        await era.printAndWait(`「呜咕～、受够了……这样子、太异常了……」`); // :3864

        // CFLAG:322  = 2（变量语义：CFLAG 族，322） // :3866
        era.set(`cflag:${target}:322`, 2); // :3866
      } // :3867
      return 0; // :3868
    } // :3869
  } // :3870




  if (era_flag.selectcom == 27) { // :3875

    if (era.get(`cflag:${target}:328`) == 0) { // :3877

      if (era.get(`talent:${target}:136`) == 1) { // :3879
        await era.printAndWait(''); // :3880

      } else if (era.get(`talent:${target}:76`) == 1) { // :3882
        await era.printAndWait(''); // :3883

      } else if (era.get(`talent:${target}:85`) == 1) { // :3885
        await era.printAndWait(''); // :3886

      } else { // :3888
        await era.printAndWait(''); // :3889
      } // :3890
      // CFLAG:TARGET:328  = 1（变量语义：CFLAG 族，TARGET:328） // :3891
      era.set(`cflag:${target}:328`, 1); // :3891
      return 0; // :3892

    } else { // :3894

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 6 || era.get('flag:7') == 2)) { // :3896
        if (rand_n(2) == 0) { // :3897
          await era.printAndWait(''); // :3898
        } else { // :3899
          await era.printAndWait(''); // :3900
        } // :3901
        // CFLAG:328  = 7（变量语义：CFLAG 族，328） // :3902
        era.set(`cflag:${target}:328`, 7); // :3902

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 5 || era.get('flag:7') == 2)) { // :3904
        if (rand_n(2) == 0) { // :3905
          await era.printAndWait(''); // :3906
        } else { // :3907
          await era.printAndWait(''); // :3908
        } // :3909
        // CFLAG:328  = 6（变量语义：CFLAG 族，328） // :3910
        era.set(`cflag:${target}:328`, 6); // :3910

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 4 || era.get('flag:7') == 2)) { // :3912
        if (rand_n(2) == 0) { // :3913
          await era.printAndWait(''); // :3914
        } else { // :3915
          await era.printAndWait(''); // :3916
        } // :3917
        // CFLAG:328  = 5（变量语义：CFLAG 族，328） // :3918
        era.set(`cflag:${target}:328`, 5); // :3918

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:328`) <= 3 || era.get('flag:7') == 2)) { // :3920
        await era.printAndWait(''); // :3921
        // CFLAG:328  = 4（变量语义：CFLAG 族，328） // :3922
        era.set(`cflag:${target}:328`, 4); // :3922

      } else if (era.get(`abl:${target}:3`) >= 3 && (era.get(`cflag:${target}:328`) <= 2 || era.get('flag:7') == 2)) { // :3924
        await era.printAndWait(''); // :3925
        // CFLAG:328  = 3（变量语义：CFLAG 族，328） // :3926
        era.set(`cflag:${target}:328`, 3); // :3926

      } else if (era.get(`cflag:${target}:328`) <= 1 || era.get('flag:7') == 2) { // :3928
        await era.printAndWait(''); // :3929
        // CFLAG:328  = 2（变量语义：CFLAG 族，328） // :3930
        era.set(`cflag:${target}:328`, 2); // :3930
      } // :3931
      return 0; // :3932
    } // :3933
  } // :3934




  if (era_flag.selectcom == 30) { // :3939

    if (era.get(`cflag:${target}:331`) == 0) { // :3941

      if (era.get(`talent:${target}:76`) == 1) { // :3943
        await era.printAndWait(''); // :3944

      } else if (era.get(`talent:${target}:85`) == 1) { // :3946
        await era.printAndWait(''); // :3947

      } else if (era.get(`abl:${target}:16`) >= 3) { // :3949
        await era.printAndWait(''); // :3950

      } else { // :3952
        await era.printAndWait(''); // :3953
      } // :3954
      // CFLAG:TARGET:331  = 1（变量语义：CFLAG 族，TARGET:331） // :3955
      era.set(`cflag:${target}:331`, 1); // :3955
      return 0; // :3956

    } else { // :3958

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 6 || era.get('flag:7') == 2)) { // :3960
        if (rand_n(2) == 0) { // :3961
          await era.printAndWait(''); // :3962
        } else { // :3963
          await era.printAndWait(''); // :3964
        } // :3965
        // CFLAG:331  = 7（变量语义：CFLAG 族，331） // :3966
        era.set(`cflag:${target}:331`, 7); // :3966

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 5 || era.get('flag:7') == 2)) { // :3968
        if (rand_n(2) == 0) { // :3969
          await era.printAndWait(''); // :3970
        } else { // :3971
          await era.printAndWait(''); // :3972
        } // :3973
        // CFLAG:331  = 6（变量语义：CFLAG 族，331） // :3974
        era.set(`cflag:${target}:331`, 6); // :3974

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:331`) <= 4 || era.get('flag:7') == 2)) { // :3976
        if (rand_n(2) == 0) { // :3977
          await era.printAndWait(''); // :3978
        } else { // :3979
          await era.printAndWait(''); // :3980
        } // :3981
        // CFLAG:331  = 5（变量语义：CFLAG 族，331） // :3982
        era.set(`cflag:${target}:331`, 5); // :3982

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 3 || era.get('flag:7') == 2)) { // :3984
        await era.printAndWait(''); // :3985
        // CFLAG:331  = 4（变量语义：CFLAG 族，331） // :3986
        era.set(`cflag:${target}:331`, 4); // :3986

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:331`) <= 2 || era.get('flag:7') == 2)) { // :3988
        await era.printAndWait(''); // :3989
        // CFLAG:331  = 3（变量语义：CFLAG 族，331） // :3990
        era.set(`cflag:${target}:331`, 3); // :3990

      } else if (era.get(`cflag:${target}:331`) <= 1 || era.get('flag:7') == 2) { // :3992
        await era.printAndWait(''); // :3993
        // CFLAG:331  = 2（变量语义：CFLAG 族，331） // :3994
        era.set(`cflag:${target}:331`, 2); // :3994
      } // :3995
      return 0; // :3996
    } // :3997
  } // :3998




  if (era_flag.selectcom == 31) { // :4003

    if (era.get(`cflag:${target}:332`) == 0) { // :4005

      if (era.get(`talent:${target}:76`) == 1) { // :4007
        await era.printAndWait(''); // :4008

      } else if (era.get(`talent:${target}:85`) == 1) { // :4010
        await era.printAndWait(''); // :4011

      } else if (era.get(`abl:${target}:16`) >= 3) { // :4013
        await era.printAndWait(''); // :4014

      } else { // :4016
        await era.printAndWait(''); // :4017
      } // :4018
      // CFLAG:TARGET:332  = 1（变量语义：CFLAG 族，TARGET:332） // :4019
      era.set(`cflag:${target}:332`, 1); // :4019
      return 0; // :4020

    } else { // :4022

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 6 || era.get('flag:7') == 2)) { // :4024
        await era.printAndWait(''); // :4025
        // CFLAG:332  = 7（变量语义：CFLAG 族，332） // :4026
        era.set(`cflag:${target}:332`, 7); // :4026

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 5 || era.get('flag:7') == 2)) { // :4028
        await era.printAndWait(''); // :4029
        // CFLAG:332  = 6（变量语义：CFLAG 族，332） // :4030
        era.set(`cflag:${target}:332`, 6); // :4030

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:332`) <= 4 || era.get('flag:7') == 2)) { // :4032
        await era.printAndWait(''); // :4033
        // CFLAG:332  = 5（变量语义：CFLAG 族，332） // :4034
        era.set(`cflag:${target}:332`, 5); // :4034

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:332`) <= 3 || era.get('flag:7') == 2)) { // :4036
        await era.print(''); // :4037
        await era.printAndWait(''); // :4038
        // CFLAG:332  = 4（变量语义：CFLAG 族，332） // :4039
        era.set(`cflag:${target}:332`, 4); // :4039

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:332`) <= 2 || era.get('flag:7') == 2)) { // :4041
        await era.print(''); // :4042
        await era.printAndWait(''); // :4043
        // CFLAG:332  = 3（变量语义：CFLAG 族，332） // :4044
        era.set(`cflag:${target}:332`, 3); // :4044

      } else if (era.get(`cflag:${target}:332`) <= 1 || era.get('flag:7') == 2) { // :4046
        await era.printAndWait(''); // :4047
        // CFLAG:332  = 2（变量语义：CFLAG 族，332） // :4048
        era.set(`cflag:${target}:332`, 2); // :4048
      } // :4049
      return 0; // :4050
    } // :4051
  } // :4052




  if (era_flag.selectcom == 34) { // :4057

    if (era.get(`cflag:${target}:335`) == 0) { // :4059

      if (era.get(`talent:${target}:0`) == 1) { // :4061

        if (era.get(`talent:${target}:136`) == 1) { // :4063
          await era.printAndWait(''); // :4064

        } else if (era.get(`talent:${target}:76`) == 1) { // :4066
          await era.printAndWait(''); // :4067

        } else if (era.get(`talent:${target}:85`) == 1) { // :4069
          await era.printAndWait(''); // :4070

        } else { // :4072
          await era.printAndWait(''); // :4073
        } // :4074

      } else { // :4076

        if (era.get(`talent:${target}:136`) == 1) { // :4078
          await era.printAndWait(''); // :4079

        } else if (era.get(`talent:${target}:76`) == 1) { // :4081
          await era.printAndWait(''); // :4082

        } else if (era.get(`talent:${target}:85`) == 1) { // :4084
          await era.printAndWait(''); // :4085

        } else { // :4087
          await era.printAndWait(''); // :4088
        } // :4089
      } // :4090
      // CFLAG:TARGET:335  = 1（变量语义：CFLAG 族，TARGET:335） // :4091
      era.set(`cflag:${target}:335`, 1); // :4091
      return 0; // :4092

    } else { // :4094

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:335`) <= 6 || era.get('flag:7') == 2)) { // :4096
        if (rand_n(3) == 0) { // :4097
          await era.printAndWait(''); // :4098
        } else if (rand_n(2) == 0) { // :4099
          await era.printAndWait(''); // :4100
        } else { // :4101
          await era.printAndWait(''); // :4102
        } // :4103
        // CFLAG:335  = 7（变量语义：CFLAG 族，335） // :4104
        era.set(`cflag:${target}:335`, 7); // :4104

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:335`) <= 5 || era.get('flag:7') == 2)) { // :4106
        if (rand_n(4) == 0) { // :4107
          await era.printAndWait(''); // :4108
        } else if (rand_n(3) == 0) { // :4109
          await era.printAndWait(''); // :4110
        } else if (rand_n(2) == 0) { // :4111
          await era.printAndWait(''); // :4112
        } else { // :4113
          await era.printAndWait(''); // :4114
        } // :4115
        // CFLAG:335  = 6（变量语义：CFLAG 族，335） // :4116
        era.set(`cflag:${target}:335`, 6); // :4116

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:335`) <= 4 || era.get('flag:7') == 2)) { // :4118
        if (rand_n(4) == 0) { // :4119
          await era.print(''); // :4120
        } else if (rand_n(3) == 0) { // :4121
          await era.printAndWait(''); // :4122
        } else if (rand_n(2) == 0) { // :4123
          await era.printAndWait(''); // :4124
        } else { // :4125
          await era.printAndWait(''); // :4126
        } // :4127
        // CFLAG:335  = 5（变量语义：CFLAG 族，335） // :4128
        era.set(`cflag:${target}:335`, 5); // :4128

      } else if (era.get(`mark:${target}:2`) == 3 && era.get(`abl:${target}:2`) >= 3 && (era.get(`cflag:${target}:335`) <= 3 || era.get('flag:7') == 2)) { // :4130
        if (rand_n(4) == 0) { // :4131
          await era.printAndWait(''); // :4132
        } else if (rand_n(3) == 0) { // :4133
          await era.printAndWait(''); // :4134
        } else if (rand_n(2) == 0) { // :4135
          await era.printAndWait(''); // :4136
        } else { // :4137
          await era.printAndWait(''); // :4138
        } // :4139
        // CFLAG:335  = 4（变量语义：CFLAG 族，335） // :4140
        era.set(`cflag:${target}:335`, 4); // :4140

      } else if (era.get(`mark:${target}:2`) == 3 && (era.get(`cflag:${target}:335`) <= 2 || era.get('flag:7') == 2)) { // :4142
        await era.print(''); // :4143
        await era.printAndWait(''); // :4144
        // CFLAG:335  = 3（变量语义：CFLAG 族，335） // :4145
        era.set(`cflag:${target}:335`, 3); // :4145

      } else if (era.get(`cflag:${target}:335`) <= 1 || era.get('flag:7') == 2) { // :4147
        await era.printAndWait(''); // :4148
        // CFLAG:335  = 2（变量语义：CFLAG 族，335） // :4149
        era.set(`cflag:${target}:335`, 2); // :4149
      } // :4150
      return 0; // :4151
    } // :4152
  } // :4153




  if (era_flag.selectcom == 37) { // :4158

    if (era.get(`cflag:${target}:338`) == 0) { // :4160

      if (era.get(`abl:${target}:16`) >= 3) { // :4162
        await era.printAndWait(''); // :4163

      } else { // :4165
        await era.printAndWait(''); // :4166
      } // :4167
      // CFLAG:TARGET:338  = 1（变量语义：CFLAG 族，TARGET:338） // :4168
      era.set(`cflag:${target}:338`, 1); // :4168
      return 0; // :4169

    } else { // :4171

      if (era.get(`talent:${target}:136`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 5 || era.get('flag:7') == 2)) { // :4173
        await era.printAndWait(''); // :4174
        // CFLAG:338  = 6（变量语义：CFLAG 族，338） // :4175
        era.set(`cflag:${target}:338`, 6); // :4175

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 4 || era.get('flag:7') == 2)) { // :4177
        await era.printAndWait(''); // :4178
        // CFLAG:338  = 5（变量语义：CFLAG 族，338） // :4179
        era.set(`cflag:${target}:338`, 5); // :4179

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:338`) <= 3 || era.get('flag:7') == 2)) { // :4181
        await era.print(''); // :4182
        // CFLAG:338  = 4（变量语义：CFLAG 族，338） // :4183
        era.set(`cflag:${target}:338`, 4); // :4183

      } else if (era.get(`abl:${target}:16`) >= 3 && (era.get(`cflag:${target}:338`) <= 2 || era.get('flag:7') == 2)) { // :4185
        await era.printAndWait(''); // :4186
        // CFLAG:338  = 3（变量语义：CFLAG 族，338） // :4187
        era.set(`cflag:${target}:338`, 3); // :4187

      } else if (era.get(`cflag:${target}:338`) <= 1 || era.get('flag:7') == 2) { // :4189
        await era.printAndWait(''); // :4190
        // CFLAG:338  = 2（变量语义：CFLAG 族，338） // :4191
        era.set(`cflag:${target}:338`, 2); // :4191
      } // :4192
      return 0; // :4193
    } // :4194
  } // :4195





  if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`)) { // :4201

    if (era.get(`cflag:${target}:344`) == 0) { // :4203

      if (era.get(`talent:${target}:136`) == 1) { // :4205
        await era.printAndWait(''); // :4206

      } else if (era.get(`talent:${target}:76`) == 1) { // :4208
        await era.printAndWait(''); // :4209

      } else if (era.get(`talent:${target}:85`) == 1) { // :4211
        await era.printAndWait(''); // :4212

      } else { // :4214
        await era.printAndWait(''); // :4215
      } // :4216
      // CFLAG:TARGET:344  = 1（变量语义：CFLAG 族，TARGET:344） // :4217
      era.set(`cflag:${target}:344`, 1); // :4217
      return 0; // :4218

    } else { // :4220

      if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:344`) <= 9 || era.get('flag:7') == 2)) { // :4222
        await era.printAndWait(''); // :4223
        // CFLAG:TARGET:344  = 10（变量语义：CFLAG 族，TARGET:344） // :4224
        era.set(`cflag:${target}:344`, 10); // :4224

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 8 || era.get('flag:7') == 2)) { // :4226
        await era.printAndWait(''); // :4227
        // CFLAG:TARGET:344  = 9（变量语义：CFLAG 族，TARGET:344） // :4228
        era.set(`cflag:${target}:344`, 9); // :4228

      } else if (era.get(`talent:${target}:76`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 7 || era.get('flag:7') == 2)) { // :4230
        await era.printAndWait(''); // :4231
        // CFLAG:TARGET:344  = 8（变量语义：CFLAG 族，TARGET:344） // :4232
        era.set(`cflag:${target}:344`, 8); // :4232

      } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:344`) <= 6 || era.get('flag:7') == 2)) { // :4234
        await era.printAndWait(''); // :4235
        // CFLAG:TARGET:344  = 7（变量语义：CFLAG 族，TARGET:344） // :4236
        era.set(`cflag:${target}:344`, 7); // :4236

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 5 && (era.get(`cflag:${target}:344`) <= 5 || era.get('flag:7') == 2)) { // :4238
        await era.printAndWait(''); // :4239
        // CFLAG:TARGET:344  = 6（变量语义：CFLAG 族，TARGET:344） // :4240
        era.set(`cflag:${target}:344`, 6); // :4240

      } else if (era.get(`talent:${target}:85`) == 1 && era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 4 || era.get('flag:7') == 2)) { // :4242
        await era.printAndWait(''); // :4243
        // CFLAG:TARGET:344  = 5（变量语义：CFLAG 族，TARGET:344） // :4244
        era.set(`cflag:${target}:344`, 5); // :4244

      } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:344`) <= 3 || era.get('flag:7') == 2)) { // :4246
        await era.printAndWait(''); // :4247
        // CFLAG:TARGET:344  = 4（变量语义：CFLAG 族，TARGET:344） // :4248
        era.set(`cflag:${target}:344`, 4); // :4248

      } else if (era.get(`abl:${target}:21`) >= 3 && (era.get(`cflag:${target}:344`) <= 2 || era.get('flag:7') == 2)) { // :4250
        await era.printAndWait(''); // :4251
        // CFLAG:TARGET:344  = 3（变量语义：CFLAG 族，TARGET:344） // :4252
        era.set(`cflag:${target}:344`, 3); // :4252

      } else if (era.get(`cflag:${target}:344`) <= 1 || era.get('flag:7') == 2) { // :4254
        await era.printAndWait(''); // :4255
        // CFLAG:TARGET:344  = 2（变量语义：CFLAG 族，TARGET:344） // :4256
        era.set(`cflag:${target}:344`, 2); // :4256
      } // :4257
      return 0; // :4258
    } // :4259

  } else if (era_flag.selectcom == 43 && era.get(`tequip:${target}:43`) == 0) { // :4261

    if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:338`) < 3 || era.get('flag:7') == 2)) { // :4263
      await era.printAndWait(''); // :4264
      // CFLAG:444  = 4（变量语义：CFLAG 族，444） // :4265
      era.set(`cflag:${target}:444`, 4); // :4265

    } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:338`) < 3 || era.get('flag:7') == 2)) { // :4267
      await era.printAndWait(''); // :4268
      // CFLAG:444  = 3（变量语义：CFLAG 族，444） // :4269
      era.set(`cflag:${target}:444`, 3); // :4269

    } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:338`) < 2 || era.get('flag:7') == 2)) { // :4271
      await era.printAndWait(''); // :4272
      // CFLAG:444  = 2（变量语义：CFLAG 族，444） // :4273
      era.set(`cflag:${target}:444`, 2); // :4273

    } else if (era.get(`cflag:${target}:444`) < 1 || era.get('flag:7') == 2) { // :4275
      await era.printAndWait(''); // :4276
      // CFLAG:444  = 1（变量语义：CFLAG 族，444） // :4277
      era.set(`cflag:${target}:444`, 1); // :4277
    } // :4278
    return 0; // :4279
  } // :4280





  if (era_flag.selectcom == 56) { // :4286

    if (era.get(`cflag:${target}:357`) == 0) { // :4288
      if (era.get(`tequip:${target}:53`)) { // :4289


        if (era.get(`talent:${target}:136`) == 1) { // :4292
          await era.printAndWait(`「初次见面、我是前勇者${target_name}哦。很感谢大家今天的收看」`); // :4293
          await era.printAndWait(`「${sc()}现在正研究着超越种族的性爱呢」`); // :4294
          await era.printAndWait(`「与狗的交配实验、狗崽的妊娠……啊啊、想研究的东西堆积如山呢」`); // :4295
          await era.printAndWait(`「${sc()}的研究成果、请从现在开始好好看着吧」`); // :4296
          await era.printAndWait(`${target_name}一边这样说着一边淫荡地摇着腰……`); // :4297

        } else if (era.get(`talent:${target}:76`) == 1) { // :4299
          await era.printAndWait(''); // :4300

        } else if (era.get(`talent:${target}:85`) == 1) { // :4302
          await era.printAndWait(''); // :4303

        } else { // :4305
          await era.printAndWait(''); // :4306
        } // :4307
      } // :4308
      // CFLAG:357  = 1（变量语义：CFLAG 族，357） // :4309
      era.set(`cflag:${target}:357`, 1); // :4309
      return 0; // :4310

    } else { // :4312
      if (era.get(`tequip:${target}:53`)) { // :4313


        if (era.get(`talent:${target}:136`) == 1 && (era.get(`cflag:${target}:357`) <= 4 || era.get('flag:7') == 2)) { // :4316
          await era.printAndWait(`「又见面了呢？　我是母狗家畜${target_name}。一直以来多谢关照」`); // :4317
          await era.printAndWait(`「${sc()}的超越种族的性爱研究现在有了很大进展。内心也渐渐地变成和野兽一样了哦」`); // :4318
          await era.printAndWait(`「与狗的交配实验、狗崽的妊娠……啊啊、还想更多的研究下去呢」`); // :4319
          await era.printAndWait(`「${sc()}的研究成果、请从现在开始好好看着吧」`); // :4320
          await era.printAndWait(`${target_name}一边这样说着一边淫荡地摇着腰……`); // :4321
          // CFLAG:357  = 5（变量语义：CFLAG 族，357） // :4322
          era.set(`cflag:${target}:357`, 5); // :4322

        } else if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:357`) <= 3 || era.get('flag:7') == 2)) { // :4324
          await era.printAndWait(''); // :4325
          // CFLAG:357  = 4（变量语义：CFLAG 族，357） // :4326
          era.set(`cflag:${target}:357`, 4); // :4326

        } else if (era.get(`talent:${target}:85`) == 1 && (era.get(`cflag:${target}:357`) <= 2 || era.get('flag:7') == 2)) { // :4328
          await era.printAndWait(''); // :4329
          // CFLAG:357  = 3（变量语义：CFLAG 族，357） // :4330
          era.set(`cflag:${target}:357`, 3); // :4330

        } else if (era.get(`cflag:${target}:357`) <= 1 || era.get('flag:7') == 2) { // :4332
          await era.printAndWait(''); // :4333
          // CFLAG:357  = 2（变量语义：CFLAG 族，357） // :4334
          era.set(`cflag:${target}:357`, 2); // :4334
        } // :4335
      } // :4336
      return 0; // :4337
    } // :4338
  } // :4339


  return 0; // :4342






}

// @KOJO_MESSAGE_PALAMCNG_12 // :4349
async function KOJO_MESSAGE_PALAMCNG_12() {




  if (era.get(`tequip:${target}:45`)) { // :4355
    return 0; // :4355
  } // :4355

  if (era.get('tflag:899')) { // :4358
    return 0; // :4358
  } // :4358







  // 赋值 P = PALAM:3 + UP:3 // :4366
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:221`) == 0) { // :4367

    if (era.get(`talent:${target}:85`) == 1) { // :4369

      if (era_flag.selectcom == 50) { // :4371
        await era.printAndWait(`「用这样的粘液……涂在${sc()}的身体上吗」`); // :4372
      } else if (era.get(`talent:${target}:122`)) { // :4373
        await era.printAndWait(`「从${sc()}的阴茎里……分泌出了粘液！？」`); // :4374

      } else { // :4376
        await era.printAndWait(`「从${sc()}的阴道里……分泌出了粘液！？」`); // :4377
      } // :4378

    } else { // :4380

      if (era_flag.selectcom == 50) { // :4382
        await era.printAndWait(`「用这样的粘液……涂在${sc()}的身体上吗」`); // :4383
      } else if (era.get(`talent:${target}:122`)) { // :4384
        await era.printAndWait(`「从${sc()}的阴茎里……分泌出了粘液！？」`); // :4385

      } else { // :4387
        await era.printAndWait(`「从${sc()}的阴道里……分泌出了粘液！？」`); // :4388
      } // :4389
    } // :4390
    // CFLAG:TARGET:221  = 1（变量语义：CFLAG 族，TARGET:221） // :4391
    era.set(`cflag:${target}:221`, 1); // :4391
  } // :4392




  // 赋值 P = PALAM:5 + UP:5 // :4397
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:222`) == 0) { // :4398

    if (era.get(`talent:${target}:85`) == 1) { // :4400

      if (era_flag.selectcom == 51) { // :4402
        await era.printAndWait(`「竟用……这样的薬物……${sc()}的思考乱成了一团」`); // :4403

      } else { // :4405
        await era.printAndWait(`「脑内的麻薬分泌吗……没法停止呢……」`); // :4406
      } // :4407

    } else { // :4409

      if (era_flag.selectcom == 51) { // :4411
        await era.printAndWait(`「竟用……这样的薬物……${sc()}的思考乱成了一团」`); // :4412

      } else { // :4414
        await era.printAndWait(`「脑内的麻薬分泌吗……没法停止呢……」`); // :4415
      } // :4416
    } // :4417
    // CFLAG:222  = 1（变量语义：CFLAG 族，222） // :4418
    era.set(`cflag:${target}:222`, 1); // :4418
  } // :4419




  // 赋值 P = PALAM:8 + UP:8 // :4424
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:223`) == 0) { // :4425

    if (era.get(`talent:${target}:85`) == 1) { // :4427
      await era.printAndWait(`「停手吧、这么羞耻的事情……${sc()}、要变的奇怪了」`); // :4428

    } else { // :4430
      await era.printAndWait(`「停手吧、这么羞耻的事情……${sc()}、要变的奇怪了」`); // :4431
    } // :4432
    // CFLAG:223  = 1（变量语义：CFLAG 族，223） // :4433
    era.set(`cflag:${target}:223`, 1); // :4433
  } // :4434




  // 赋值 P = PALAM:10 + UP:10 // :4439
  if (P > era.get('palamlv:2') && era.get(`cflag:${target}:224`) == 0) { // :4440

    if (era.get(`talent:${target}:85`) == 1) { // :4442
      await era.printAndWait(`「呀啊啊、快住手、好可怕」`); // :4443

    } else { // :4445
      await era.printAndWait(`「呀啊啊、快住手、好可怕」`); // :4446
    } // :4447
    // CFLAG:224  = 1（变量语义：CFLAG 族，224） // :4448
    era.set(`cflag:${target}:224`, 1); // :4448
  } // :4449




  if (era.get('nowex:0') > 0 && era.get(`cflag:${target}:225`) == 0) { // :4454

    if (era.get(`talent:${target}:85`) == 1) { // :4456
      await era.printAndWait(`「啊啊～、不行了、${sc()}、要高潮了～！」`); // :4457

    } else { // :4459
      await era.printAndWait(`「啊啊～、不行了、${sc()}、要高潮了～！」`); // :4460
    } // :4461
    // CFLAG:225  = 1（变量语义：CFLAG 族，225） // :4462
    era.set(`cflag:${target}:225`, 1); // :4462
  } // :4463




  if (era.get('nowex:1') > 0 && era.get(`cflag:${target}:226`) == 0) { // :4468

    if (era.get(`talent:${target}:76`) == 1) { // :4470
      await era.printAndWait(`「啊啊～、不行了、${sc()}的阴道、痉挛的停不下来啦～！」`); // :4471

    } else if (era.get(`talent:${target}:85`) == 1) { // :4473
      await era.printAndWait(`「啊啊～、不行了、${sc()}的阴道、痉挛的停不下来啦～！」`); // :4474

    } else { // :4476
      await era.printAndWait(`「啊啊～、不行了、${sc()}的阴道、痉挛的停不下来啦～！」`); // :4477
    } // :4478
    // CFLAG:TARGET:226  = 1（变量语义：CFLAG 族，TARGET:226） // :4479
    era.set(`cflag:${target}:226`, 1); // :4479
  } // :4480




  if (era.get('nowex:2') > 0 && era.get(`cflag:${target}:227`) == 0) { // :4485

    if (era.get(`talent:${target}:76`) == 1) { // :4487
      await era.printAndWait(`「不行、不要啊啊～！　${sc()}的肛门要、肛门要、高潮了～！」`); // :4488

    } else if (era.get(`talent:${target}:85`) == 1) { // :4490
      await era.printAndWait(`「不行、不要啊啊～！　${sc()}的肛门要、肛门要、高潮了～！」`); // :4491

    } else { // :4493
      await era.printAndWait(`「不行、不要啊啊～！　${sc()}的肛门要、肛门要、高潮了～！」`); // :4494
    } // :4495
    // CFLAG:227  = 1（变量语义：CFLAG 族，227） // :4496
    era.set(`cflag:${target}:227`, 1); // :4496
  } // :4497




  if (era.get('nowex:3') > 0 && era.get(`cflag:${target}:228`) == 0) { // :4502

    if (era.get(`talent:${target}:85`) == 1) { // :4504
      await era.printAndWait(`「这不可能、胸部、胸部也要高潮了啊～！」`); // :4505

    } else { // :4507
      await era.printAndWait(`「这不可能、胸部、胸部也要高潮了啊～！」`); // :4508
    } // :4509
    // CFLAG:TARGET:228  = 1（变量语义：CFLAG 族，TARGET:228） // :4510
    era.set(`cflag:${target}:228`, 1); // :4510
  } // :4511




  // 赋值 A = UP:11 + UP:12 // :4516
  if (era.get('tflag:3') == 1 && era.get(`cflag:${target}:229`) == 0) { // :4517

    if (era.get('tflag:20') == 1) { // :4519

      if (era.get(`talent:${target}:76`) == 1 && (A < 500 || era.get('tflag:150') == 1)) { // :4521
        await era.printAndWait(`「${sc()}、终于成为大人了呢……！」`); // :4522

      } else if (era.get(`talent:${target}:85`) == 1 && (A < 500 || era.get('tflag:150') == 1)) { // :4524
        await era.printAndWait(`「尽情的、为怀上孩子而做爱吧……！」`); // :4525

      } else { // :4527
        await era.printAndWait(`「咿呀～、咿呀啊～」`); // :4528
      } // :4529

    } else { // :4531

      if (era.get(`talent:${target}:76`) == 1) { // :4533
        await era.printAndWait(`「${sc()}、终于成为大人了呢……！」`); // :4534

      } else if (era.get(`talent:${target}:85`) == 1) { // :4536
        await era.printAndWait(`「讨厌、才不想和你生孩子呢」`); // :4537

      } else { // :4539
        await era.printAndWait(`「咿呀～、咿呀啊～」`); // :4540
      } // :4541
    } // :4542
    // CFLAG:TARGET:229  = 1（变量语义：CFLAG 族，TARGET:229） // :4543
    era.set(`cflag:${target}:229`, 1); // :4543
  } // :4544






}

// @KOJO_MESSAGE_MARKCNG_12 // :4551
async function KOJO_MESSAGE_MARKCNG_12() {




  if (era.get(`tequip:${target}:45`)) { // :4557
    return 0; // :4557
  } // :4557



  if (era.get('tflag:22') == 3 && era.get(`cflag:${target}:297`) == 0) { // :4561

    if (era.get(`talent:${target}:85`) == 1) { // :4563
      await era.printAndWait(`「啊嘎嘎、啊嘎啊～、痛～」`); // :4564
    } else { // :4565
      await era.printAndWait(`「啊嘎嘎、啊嘎啊～、痛～」`); // :4566
    } // :4567
    // CFLAG:297  = 1（变量语义：CFLAG 族，297） // :4568
    era.set(`cflag:${target}:297`, 1); // :4568
  } // :4569




  if (era.get('tflag:23') == 3 && era.get(`cflag:${target}:298`) == 0) { // :4574

    if (era.get(`talent:${target}:85`) == 1) { // :4576
      await era.printAndWait(`「不行、不行了、好、好有感觉啊～！」`); // :4577
    } else { // :4578
      await era.printAndWait(`「不行、不行了、好、好有感觉啊～！」`); // :4579
    } // :4580
    // CFLAG:298  = 1（变量语义：CFLAG 族，298） // :4581
    era.set(`cflag:${target}:298`, 1); // :4581
  } // :4582




  if (era.get('tflag:24') == 3 && era.get(`cflag:${target}:299`) == 0) { // :4587

    if (era.get(`talent:${target}:85`) == 1) { // :4589
      await era.printAndWait(`「再、再也不会反抗你了……」`); // :4590
    } else { // :4591
      await era.printAndWait(`「再、再也不会反抗你了……」`); // :4592
    } // :4593
    // CFLAG:299  = 1（变量语义：CFLAG 族，299） // :4594
    era.set(`cflag:${target}:299`, 1); // :4594
  } // :4595




  if (era.get('tflag:21') == 3 && era.get(`cflag:${target}:300`) == 0) { // :4600

    if (era.get(`talent:${target}:85`) == 1) { // :4602
      await era.printAndWait(`「去死吧……」`); // :4603
    } else { // :4604
      await era.printAndWait(`「去死吧……」`); // :4605
    } // :4606
    // CFLAG:300  = 1（变量语义：CFLAG 族，300） // :4607
    era.set(`cflag:${target}:300`, 1); // :4607
  } // :4608





}

// @SELF_KOJO_K12 // :4614
async function SELF_KOJO_K12() {



  if (era.get('tflag:13') == 1) { // :4618

    if (era.get(`talent:${target}:9`) == 1) { // :4620
      await era.printAndWait(`「啊哈、啊哈哈、啊哈哈哈」`); // :4621

    } else if (Q == 1) { // :4623
      await era.printAndWait(`「${sc()}……一想到女孩子的裸体……就自慰起来了呢……」`); // :4624

    } else if (Q == 2) { // :4626
      await era.printAndWait(`「${sc()}……果然对和异种交配……很有兴趣呢」`); // :4627

    } else { // :4629

      if (era.get(`talent:${target}:76`) && (era.get(`cflag:${target}:261`) < 4 || era.get('flag:7') == 2)) { // :4631
        await era.printAndWait(`「不行了、停不下来了～」`); // :4632
        // CFLAG:261  = 4（变量语义：CFLAG 族，261） // :4633
        era.set(`cflag:${target}:261`, 4); // :4633

      } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:261`) < 3 || era.get('flag:7') == 2)) { // :4635
        await era.printAndWait(`「啊啊、停不下来了、变成猴子了～！」`); // :4636
        // CFLAG:261  = 3（变量语义：CFLAG 族，261） // :4637
        era.set(`cflag:${target}:261`, 3); // :4637

      } else if (era.get(`abl:${target}:31`) >= 3 && (era.get(`cflag:${target}:261`) < 2 || era.get('flag:7') == 2)) { // :4639
        await era.printAndWait(`「这样自慰下去的话……要变成白痴了……」`); // :4640
        // CFLAG:261  = 2（变量语义：CFLAG 族，261） // :4641
        era.set(`cflag:${target}:261`, 2); // :4641

      } else if (era.get(`cflag:${target}:261`) < 1 || era.get('flag:7') == 2) { // :4643
        await era.printAndWait(`「嗯、咕呜……呜嗯……」`); // :4644
        // CFLAG:261  = 1（变量语义：CFLAG 族，261） // :4645
        era.set(`cflag:${target}:261`, 1); // :4645
      } // :4646
    } // :4647
  } // :4648




  if (era.get('tflag:13') == 2) { // :4653

    if (era.get(`talent:${target}:76`) && (era.get(`cflag:${target}:262`) < 5 || era.get('flag:7') == 2)) { // :4655
      await era.printAndWait(''); // :4656
      // CFLAG:262  = 5（变量语义：CFLAG 族，262） // :4657
      era.set(`cflag:${target}:262`, 5); // :4657

    } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:262`) < 4 || era.get('flag:7') == 2)) { // :4659
      await era.printAndWait(''); // :4660
      // CFLAG:262  = 4（变量语义：CFLAG 族，262） // :4661
      era.set(`cflag:${target}:262`, 4); // :4661

    } else if (era.get(`abl:${target}:33`) >= 3 && (era.get(`cflag:${target}:262`) < 3 || era.get('flag:7') == 2)) { // :4663
      await era.printAndWait(''); // :4664
      // CFLAG:262  = 3（变量语义：CFLAG 族，262） // :4665
      era.set(`cflag:${target}:262`, 3); // :4665

    } else if (era.get(`abl:${target}:22`) >= 3 && (era.get(`cflag:${target}:262`) < 2 || era.get('flag:7') == 2)) { // :4667
      await era.printAndWait(''); // :4668
      // CFLAG:262  = 2（变量语义：CFLAG 族，262） // :4669
      era.set(`cflag:${target}:262`, 2); // :4669

    } else if (era.get(`cflag:${target}:262`) < 1 || era.get('flag:7') == 2) { // :4671
      await era.printAndWait(''); // :4672
      // CFLAG:262  = 1（变量语义：CFLAG 族，262） // :4673
      era.set(`cflag:${target}:262`, 1); // :4673
    } // :4674
  } // :4675




  if (era.get('tflag:13') == 3) { // :4680

    if (era.get(`talent:${target}:76`) == 1 && (era.get(`cflag:${target}:263`) < 3 || era.get('flag:7') == 2)) { // :4682
      await era.printAndWait(`「早上好。啊啊、生理现象……就交给${sc()}吧♪」`); // :4683
      // CFLAG:263  = 3（变量语义：CFLAG 族，263） // :4684
      era.set(`cflag:${target}:263`, 3); // :4684

    } else if (era.get(`talent:${target}:85`) && (era.get(`cflag:${target}:263`) < 3 || era.get('flag:7') == 2)) { // :4686
      await era.printAndWait(`「早上好。虽然早了点、开始研究吧。早上的话脑细胞会活性化呦」`); // :4687
      // CFLAG:263  = 3（变量语义：CFLAG 族，263） // :4688
      era.set(`cflag:${target}:263`, 3); // :4688

    } else if (era.get(`abl:${target}:16`) >= 5 && (era.get(`cflag:${target}:263`) < 2 || era.get('flag:7') == 2)) { // :4690
      await era.printAndWait(`「嗯、${sc()}的好意。你就不用动了」`); // :4691
      // CFLAG:263  = 2（变量语义：CFLAG 族，263） // :4692
      era.set(`cflag:${target}:263`, 2); // :4692

    } else if (era.get(`cflag:${target}:263`) < 1 || era.get('flag:7') == 2) { // :4694
      await era.printAndWait(`「不要在意。只是我一时兴起」`); // :4695
      // CFLAG:263  = 1（变量语义：CFLAG 族，263） // :4696
      era.set(`cflag:${target}:263`, 1); // :4696
    } // :4697
  } // :4698




  if (era.get('tflag:13') == 4) { // :4703

    if (era.get(`abl:${target}:2`) >= 4 && (era.get(`cflag:${target}:264`) < 2 || era.get('flag:7') == 2)) { // :4705
      await era.printAndWait(''); // :4706
      // CFLAG:264  = 2（变量语义：CFLAG 族，264） // :4707
      era.set(`cflag:${target}:264`, 2); // :4707

    } else if (era.get(`cflag:${target}:264`) < 1 || era.get('flag:7') == 2) { // :4709
      await era.printAndWait(''); // :4710
      // CFLAG:264  = 1（变量语义：CFLAG 族，264） // :4711
      era.set(`cflag:${target}:264`, 1); // :4711
    } // :4712
  } // :4713




  if (era.get('tflag:13') == 5) { // :4718
    if (era.get(`cflag:${target}:265`) < 1 || era.get('flag:7') == 2) { // :4719
      await era.printAndWait(`「呵呵、能陪我加一下班吗？　你和${sc()}的……淫乱的实验♪」`); // :4720
      // CFLAG:265  = 1（变量语义：CFLAG 族，265） // :4721
      era.set(`cflag:${target}:265`, 1); // :4721
    } // :4722
  } // :4723




  if (era.get('tflag:13') == 6) { // :4728

    if (era.get(`talent:${target}:85`) && era.get(`mark:${target}:3`) < 3) { // :4730
      await era.printAndWait(''); // :4731

    } else if (era.get(`mark:${target}:3`) == 3) { // :4733
      await era.printAndWait(''); // :4734

    } else if (era.get(`talent:${target}:76`)) { // :4736
      await era.printAndWait(''); // :4737

    } else { // :4739
      await era.printAndWait(''); // :4740
    } // :4741
    if (era.get(`talent:${target}:122`) != 1) { // :4743
      // CALL SELL_MATURO_K0 // :4743
    } // :4743
  } // :4744




  if (era.get('tflag:13') == 9) { // :4749
    era.drawLine(); // :4750
    await era.printAndWait(''); // :4751
    era.drawLine(); // :4752
  } // :4753




  if (era.get('tflag:13') == 10) { // :4758
    era.drawLine(); // :4759
    await era.printAndWait(''); // :4760
    era.drawLine(); // :4761
  } // :4762





  if (era.get('tflag:13') == 11) { // :4768
    if (era.get(`cflag:${target}:271`) >= 1) { // :4770
      return 0; // :4770
    } // :4770

    if (era.get(`talent:${target}:9`) == 1) { // :4772
      await era.printAndWait(''); // :4773
      await era.printAndWait(`「不研究不研究的话……」`); // :4774

    } else if (era.get(`talent:${target}:85`) && era.get(`cflag:${target}:102`) == 1) { // :4776
      await era.printAndWait(`「来摸一下肚子……这是和你的爱的结晶哦……♪」`); // :4777

    } else if (era.get(`talent:${target}:136`) && era.get(`cflag:${target}:102`) == 5) { // :4779
      await era.printAndWait(`「终于……作为交尾的结果、怀上来狗的孩子了……♪」`); // :4780

    } else { // :4782
      await era.printAndWait(`「原来如此、怀上孩子了啊……」`); // :4783
    } // :4784
    // CFLAG:271  = 1（变量语义：CFLAG 族，271） // :4785
    era.set(`cflag:${target}:271`, 1); // :4785
  } // :4786





  if (era.get('tflag:13') == 12) { // :4792
    if (era.get(`cflag:${target}:272`) >= 1) { // :4794
      return 0; // :4794
    } // :4794

    if (era.get(`talent:${target}:9`) == 1) { // :4796
      await era.printAndWait(''); // :4797

    } else if (era.get(`talent:${target}:85`) && era.get(`cflag:${target}:102`) == 1) { // :4799
      await era.printAndWait(''); // :4800

    } else { // :4802
      await era.printAndWait(''); // :4803
    } // :4804
    // CFLAG:272  = 1（变量语义：CFLAG 族，272） // :4805
    era.set(`cflag:${target}:272`, 1); // :4805
  } // :4806




  if (era.get('tflag:13') == 999) { // :4811

    if (era.get(`talent:${target}:85`)) { // :4813
      await era.printAndWait(''); // :4814

    } else { // :4816
      await era.printAndWait(''); // :4817
    } // :4818
  } // :4819




  if (era.get('tflag:13') == 998) { // :4824

    if (era.get(`talent:${target}:85`)) { // :4826
      await era.printAndWait(''); // :4827

    } else { // :4829
      await era.printAndWait(''); // :4830
    } // :4831
  } // :4832




  // TFLAG:13  = 0（变量语义：TFLAG 族，13） // :4837
  era.set('tflag:13', 0); // :4837

  return 0; // :4839






























}

// @DUNGEON_RYOUZYOKU_K12 // :4870
async function DUNGEON_RYOUZYOKU_K12() {




  if (era.get(`talent:${target}:0`) == 1) { // :4875

    await era.printAndWait(`「噫～、请你们、冷、冷静一点……」`); // :4877

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4879


      await era.printAndWait(`「救命啊……」`); // :4882

      return 0; // :4884
    } else if (era.get(`talent:${target}:17`) ==1 || era.get(`talent:${target}:31`) == 1 || era.get(`talent:${target}:36`) == 1) { // :4885


      await era.printAndWait(`「你、你们、快看、我还是处女哦。所以、请至少饶我一命……」`); // :4888


      if (era.get(`talent:${target}:106`) == 1 || era.get(`exp:${target}:1`) > 0) { // :4892
        await era.printAndWait(`「不、不管怎么用我的肛门都可以哦……」`); // :4892
      } // :4892


      if (era.get(`exp:${target}:22`) > 0) { // :4896
        await era.printAndWait(`「口交的经验我有、一定、一定能满足你们的！」`); // :4896
      } // :4896

    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4898



      await era.printAndWait(`「怎、怎么能认输啊！　区区凌辱……${sc()}是绝对不会认输的！」`); // :4902

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4904


      await era.printAndWait(`「已、已经完蛋了……」`); // :4907

    } else { // :4909


      await era.printAndWait(`「你、你们……能商量一下吗。如果听得懂的话……」`); // :4912

    } // :4914
  } else { // :4915

    await era.printAndWait(`「不、不好意思。能请你们、冷、冷静一下吗……」`); // :4917

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4919


      await era.printAndWait(`「拜托了……」`); // :4922

      return 0; // :4924
    } else if (era.get(`talent:${target}:17`) ==1 || era.get(`talent:${target}:31`) == 1 || era.get(`talent:${target}:36`) == 1) { // :4925


      await era.printAndWait(`「做、做爱的经验我有哦。一定能满足你们的。所以、请至少饶我一命……」`); // :4928


      if (era.get(`talent:${target}:106`) == 1 || era.get(`exp:${target}:1`) > 0) { // :4932
        await era.printAndWait(`「肛、肛门的话随便怎么用都可以……所以……」`); // :4932
      } // :4932


      if (era.get(`exp:${target}:22`) > 0) { // :4936
        await era.printAndWait(`「口交的经验我有、一定、一定能满足你们的！」`); // :4936
      } // :4936

    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :4938



      await era.printAndWait(`「怎、怎么能认输啊！　区区凌辱……${sc()}是绝对不会认输的！」`); // :4942

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :4944


      await era.printAndWait(`「已、已经完蛋了……」`); // :4947

    } else { // :4949


      await era.printAndWait(`「你、你们……能商量一下吗。如果听得懂的话……」`); // :4952

    } // :4954
  } // :4955

  return 0; // :4957


}

// @DUNGEON_RYOUZYOKU_AFTER_K12 // :4960
async function DUNGEON_RYOUZYOKU_AFTER_K12() {




  if (era.get(`talent:${target}:0`) == 1) { // :4965

    await era.printAndWait(`（得救了……连贞洁也、守住了吗……）`); // :4967

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4969


      await era.printAndWait(`「……」`); // :4972

      return 0; // :4974
    } // :4975


    if (era.get(`exp:${target}:1`) > 20) { // :4978
      await era.printAndWait(`「肛门……咿呀～」`); // :4979
      await era.printAndWait(`「已经破破烂烂的了……」`); // :4980
    } // :4981


    if (era.get(`exp:${target}:22`) > 20) { // :4985
      await era.printAndWait(`「啊、下巴……」`); // :4985
    } // :4985


    if (era.get(`exp:${target}:20`) > 20) { // :4989
      await era.printAndWait(`「呜欸欸……这就是精液的味道吗……」`); // :4989
    } // :4989
  } else { // :4990

    await era.printAndWait(`（只有小命还在、算是得救了吗……）`); // :4992

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :4994


      await era.printAndWait(`「……」`); // :4997

      return 0; // :4999
    } // :5000


    if (era.get(`exp:${target}:0`) > 20) { // :5003
      await era.printAndWait(`「小穴……再也变不回去了」`); // :5004
    } // :5005


    if (era.get(`exp:${target}:1`) > 20) { // :5008
      await era.printAndWait(`「肛门……咿呀～」`); // :5009
      await era.printAndWait(`「已经破破烂烂的了……」`); // :5010
    } // :5011


    if (era.get(`exp:${target}:22`) > 20) { // :5015
      await era.printAndWait(`「啊、下巴……」`); // :5015
    } // :5015


    if (era.get(`exp:${target}:20`) > 20) { // :5019
      await era.printAndWait(`「呜欸欸……这就是精液的味道吗……」`); // :5019
    } // :5019
  } // :5020

  return 0; // :5022

}

// @BENKI_KOUJO_K12 // :5024
async function BENKI_KOUJO_K12() {


















  if (era.get('flag:62') == 0) { // :5043


    if (era.get('flag:63') == 1) { // :5046
      await era.printAndWait(`「感谢前来协助%SELF_CALL(A)%的『研究』♪%SELF_CALL(A)%也会更努力的、可以继续帮助%SELF_CALL(A)%做更多『实验』吗？拜托咯…♪」`); // :5047

    } else if (era.get(`talent:${a}:76`) == 1) { // :5049
      await era.printAndWait(`「你们的龟头垢、一会儿能让我进行回收吗？　看起来能好好研究一下呢」`); // :5050

    } else if (era.get(`talent:${a}:85`)) { // :5052
      await era.printAndWait(`「喂喂、再坚持一下。再过一会儿就射精了」`); // :5053

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5055
      await era.printAndWait(`「把精液全射出来吧……」`); // :5056

    } else { // :5058
      await era.printAndWait(`「呜呜……不洁啊……好污」`); // :5059
    } // :5060
  } else if (era.get('flag:62') == 1) { // :5061


    if (era.get('flag:63') == 1) { // :5064
      await era.printAndWait(`「比起雄性的阴茎还是女孩子的身体更有趣啊…%SELF_CALL(A)%、可以进行这项『研究』真是太『幸福』啦♪」`); // :5065
      await era.printAndWait(`「刚听说常识被改变了的时候还是有些抵触的…可认真一想『女孩子之间做快乐的事是普通的』什么的、那不是理所当然的吗♪」`); // :5066
      await era.printAndWait(`「真是不知道该怎么感谢、赐予了这么『美妙』的催眠的魔王大人呢♪」`); // :5067

    } else if (era.get(`talent:${a}:76`) == 1) { // :5069
      await era.printAndWait(`「哈啊哈啊……再抱紧一点……」`); // :5070

    } else if (era.get(`talent:${a}:85`)) { // :5072
      await era.printAndWait(`「真是好孩子……再把我抱紧一点……」`); // :5073

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5075
      await era.printAndWait(`「让我亲亲吧……」`); // :5076

    } else { // :5078
      await era.printAndWait(`「是女人吗……」`); // :5079
    } // :5080
  } else if (era.get('flag:62') == 2) { // :5081


    if (era.get('flag:63') == 1) { // :5084
      await era.printAndWait(`「…之前便开始作为牝犬家畜的肉便器、像这样和野兽重复着『交配实验』的『研究』…」`); // :5085
      await era.printAndWait(`「%SELF_CALL(A)%对于负责这项『研究』还是『十分满足』的来着…难道有什么疑虑吗？」`); // :5086
      await era.printAndWait(`「常识改变？…真搞不懂到底在说什么玩意…这可是『%SELF_CALL(A)%自愿参与』的哦？」`); // :5087

    } else if (era.get(`talent:${a}:76`) == 1) { // :5089
      await era.printAndWait(`「噗唏、噗唏！　噗唏～……哈啊哈啊」`); // :5090

    } else if (era.get(`talent:${a}:85`)) { // :5092
      await era.printAndWait(`「汪、汪！　汪～……哈啊哈啊」`); // :5093

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5095
      await era.printAndWait(`「让我蹭一蹭吧……」`); // :5096

    } else { // :5098
      await era.printAndWait(`「呜呜……动物的臭味……」`); // :5099
    } // :5100
  } else if (era.get('flag:62') == 3) { // :5101


    if (era.get('flag:63') == 1) { // :5104
      await era.printAndWait(`「多亏`); // :5105
      // CALL BENKI_PLAYER_NAME // :5106
      await era.printAndWait(`的帮助、使用肛门和性器的『交配实验』得以进行咯♪」`); // :5107
      await era.printAndWait(`「虽然被魔王大人做了肉便器洗脑、但是拜托%SELF_CALL(A)%新的『研究』的魔王大人真是太温柔了呢♪」`); // :5108
      await era.printAndWait(`「这副身体到底能给多少雄性做快活的事呢…每天都可以做『实验』『实在是太爽了』啦♪」`); // :5109

    } else if (era.get(`talent:${a}:76`) == 1) { // :5111
      await era.printAndWait(`「哈啊哈啊……里面……被摩擦着呢……」`); // :5112

    } else if (era.get(`talent:${a}:85`)) { // :5114
      await era.printAndWait(`「哈啊哈啊……里面……被摩擦着呢……」`); // :5115

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5117
      await era.printAndWait(`「换我来吧……拜托了」`); // :5118

    } else { // :5120
      await era.printAndWait(`「呜呜……两边都被……」`); // :5121
    } // :5122
  } else if (era.get('flag:62') == 4) { // :5123


    if (era.get('flag:63') == 1) { // :5126
      await era.printAndWait(`「多亏`); // :5127
      // CALL BENKI_PLAYER_NAME // :5128
      await era.printAndWait(`的帮助、几乎让性器松弛的『交配实验』得以进行咯♪」`); // :5129
      await era.printAndWait(`「虽然被魔王大人做了肉便器洗脑、但是拜托%SELF_CALL(A)%新的『研究』的魔王大人真是太温柔了呢♪」`); // :5130
      await era.printAndWait(`「为了了解性器到底能让大家多舒服、于是直接向大家『发表』啦…每天都可以做『实验』『实在是太爽了』啦♪」`); // :5131

    } else if (era.get(`talent:${a}:76`) == 1) { // :5133
      await era.printAndWait(`「阴道……被扩张了……」`); // :5134

    } else if (era.get(`talent:${a}:85`)) { // :5136
      await era.printAndWait(`「阴道……被扩张了……」`); // :5137

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5139
      await era.printAndWait(`「后面可不行哦、把前面给你弄吧……」`); // :5140

    } else { // :5142
      await era.printAndWait(`「呜呜、您的奉仕我就收下了……」`); // :5143
    } // :5144
  } else if (era.get('flag:62') == 5) { // :5145


    if (era.get('flag:63') == 1) { // :5148
      await era.printAndWait(`「多亏`); // :5149
      // CALL BENKI_PLAYER_NAME // :5150
      await era.printAndWait(`的帮助、几乎让肛门松弛的『交配实验』得以进行咯♪」`); // :5151
      await era.printAndWait(`「虽然被魔王大人做了肉便器洗脑、但是拜托%SELF_CALL(A)%新的『研究』的魔王大人真是太温柔了呢♪」`); // :5152
      await era.printAndWait(`「『用肛门做爱很正常』嘛『没什么好羞耻』的啊…每天都可以做『实验』『实在是太爽了』啦♪」`); // :5153

    } else if (era.get(`talent:${a}:76`) == 1) { // :5155
      await era.printAndWait(`「肛门、湿漉漉的……」`); // :5156

    } else if (era.get(`talent:${a}:85`)) { // :5158
      await era.printAndWait(`「肛门、湿漉漉的……」`); // :5159

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5161
      await era.printAndWait(`「您的肛交奉仕我就收下了……」`); // :5162

    } else { // :5164
      await era.printAndWait(`「呜呜……屁股被……」`); // :5165
    } // :5166
  } else if (era.get('flag:62') == 6) { // :5167


    if (era.get('flag:63') == 1) { // :5170
      await era.printAndWait(`「多亏`); // :5171
      // CALL BENKI_PLAYER_NAME // :5172
      await era.printAndWait(`的阴茎的帮助、几乎让下巴脱臼的『实验』得以进行咯♪」`); // :5173
      await era.printAndWait(`「虽然被魔王大人做了肉便器洗脑、但是拜托%SELF_CALL(A)%新的『研究』的魔王大人真是太温柔了呢♪」`); // :5174
      await era.printAndWait(`「眼前的阴茎、传来那股『非它不可的香味』啦…每天都可以做『实验』『实在是太爽了』啦♪」`); // :5175

    } else if (era.get(`talent:${a}:76`) == 1) { // :5177
      await era.printAndWait(''); // :5178

    } else if (era.get(`talent:${a}:85`)) { // :5180
      await era.printAndWait(''); // :5181

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5183
      await era.printAndWait(''); // :5184

    } else { // :5186
      await era.printAndWait(''); // :5187
    } // :5188
  } else if (era.get('flag:62') == 7) { // :5189


    if (era.get('flag:63') == 1) { // :5192
      await era.printAndWait(`「大家好、元`); // :5193
      if (era.get(`talent:${a}:122`) == 0) { // :5194
        await era.print(`勇者`); // :5195
      } else if (era.get(`talent:${a}:122`)) { // :5196
        await era.print(`冒险者`); // :5197
      } // :5198
      await era.print(`${target_name}哟♪」`); // :5199
      await era.printAndWait(`「%SELF_CALL(A)%败给了伟大的魔王大人之后…毫无抵抗地被洗脑成牝犬家畜肉便器啦♪」`); // :5200
      await era.printAndWait(`「现在正作为对野兽阴茎感兴趣的大变态、在魔王大人手下做『研究』呢♪」`); // :5201
      await era.printAndWait(`「这种『异种交配实验』绝对会变成厉害的玩意的…所以想让更多的人看到%SELF_CALL(A)%交尾着的样子啊♪」`); // :5202

    } else if (era.get(`talent:${a}:牝犬`)) { // :5204
      await era.printAndWait(''); // :5205

    } else if (era.get(`talent:${a}:76`) == 1) { // :5207
      await era.printAndWait(''); // :5208

    } else if (era.get(`talent:${a}:85`)) { // :5210
      await era.printAndWait(''); // :5211

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5213
      await era.printAndWait(''); // :5214

    } else { // :5216
      await era.printAndWait(''); // :5217
    } // :5218
  } else if (era.get('flag:62') == 9) { // :5219


    if (era.get('flag:63') == 1) { // :5222
      await era.printAndWait(`「大家好、元`); // :5223
      if (era.get(`talent:${a}:122`) == 0) { // :5224
        await era.print(`勇者`); // :5225
      } else if (era.get(`talent:${a}:122`)) { // :5226
        await era.print(`冒险者`); // :5227
      } // :5228
      await era.print(`${target_name}哟♪」`); // :5229
      await era.printAndWait(`「%SELF_CALL(A)%败给了伟大的魔王大人之后…被彻头彻尾地调教并洗脑咯♪」`); // :5230
      await era.printAndWait(`「现在作为喜欢在野外裸体的露出狂、在魔王大人手下做『研究』呢♪」`); // :5231
      await era.printAndWait(`「这种『野外研究』非常浅显易懂…让别人看见自己裸体的样子真是件非常舒服的事啊♪」`); // :5232

    } else if (era.get(`talent:${a}:76`) == 1) { // :5234
      await era.printAndWait(''); // :5235

    } else if (era.get(`talent:${a}:85`)) { // :5237
      await era.printAndWait(''); // :5238

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5240
      await era.printAndWait(''); // :5241

    } else { // :5243
      await era.printAndWait(''); // :5244
    } // :5245
  } else if (era.get('flag:62') == 12) { // :5246


    if (era.get('flag:63') == 1) { // :5249
      await era.printAndWait(`「大家好、元`); // :5250
      if (era.get(`talent:${a}:122`) == 0) { // :5251
        await era.print(`勇者`); // :5252
      } else if (era.get(`talent:${a}:122`)) { // :5253
        await era.print(`冒险者`); // :5254
      } // :5255
      await era.print(`${target_name}哟♪」`); // :5256
      await era.printAndWait(`「%SELF_CALL(A)%被伟大的魔王大人打败了之后…毫无抵抗的被开发了身体的每个角落啦♪」`); // :5257
      await era.printAndWait(`「现在成了除了自慰什么都不会思考的自慰狂、一直一个人做着『研究』哦♪」`); // :5258
      await era.printAndWait(`「啊啊、大家正在看着%SELF_CALL(A)%的『研究』啊…只是这么想想就又要绝顶了的样子啊♪」`); // :5259

    } else if (era.get(`talent:${a}:76`) == 1) { // :5261
      await era.printAndWait(''); // :5262

    } else if (era.get(`talent:${a}:85`)) { // :5264
      await era.printAndWait(''); // :5265

    } else if (era.get(`abl:${a}:16`) >= 5) { // :5267
      await era.printAndWait(''); // :5268

    } else { // :5270
      await era.printAndWait(''); // :5271
    } // :5272
  } // :5273

  return 0; // :5275


}

// @DUNGEON_VICTORY_K12 // :5278
async function DUNGEON_VICTORY_K12() {




  await era.printAndWait(`「${sc()}的胜率达到了95％哦」`); // :5283

  if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :5285


    await era.printAndWait(`「这是毋庸置疑的」`); // :5288

    return 0; // :5290
  } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :5291


    if (rand_n(3) == 0) { // :5294
      await era.printAndWait(`「你们最好再多动动脑筋呢」`); // :5295
    } else if (rand_n(2) == 0) { // :5296
      await era.printAndWait(`「${sc()}的计算是完美的」`); // :5297
    } else { // :5298
      await era.printAndWait(`「不管模拟几次结果都是一样的」`); // :5299
    } // :5300

  } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :5302


    await era.printAndWait(`「应该、没问题吧、没问题……」`); // :5305

    return 0; // :5307
  } else { // :5308


    if (rand_n(3) == 0) { // :5311
      await era.printAndWait(`「${sc()}没有输的理由」`); // :5312
    } else if (rand_n(2) == 0) { // :5313
      await era.printAndWait(`「下次模拟一下新战术试试吧」`); // :5314
    } else { // :5315
      await era.printAndWait(`「${sc()}每次战斗后都会有进步呢」`); // :5316
    } // :5317

  } // :5319

  if ((era.get(`base:${a}:0`) * 100 / era.get(`maxbase:${a}:0`) < 50) || (era.get(`base:${a}:1`) * 100 / era.get(`maxbase:${a}:1`) < 50)) { // :5321

    await era.printAndWait(`「……然而、这是预料外的攻击……如果修正计算公式的话」`); // :5323
  } else { // :5324

    await era.printAndWait(`「这种程度的攻击、完全在预料的范围内」`); // :5326
  } // :5327

  return 0; // :5329


}

// @DUNGEON_ATTACK_K12 // :5332
async function DUNGEON_ATTACK_K12() {




  if (era.get(`cflag:${target}:1`) == 2) { // :5337

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :5339


      await era.printAndWait(`「……我要上了」`); // :5342

      return 0; // :5344
    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :5345


      if (rand_n(3) == 0) { // :5348
        await era.printAndWait(`「虽然只是计算、这个攻击应该是无法闪避的！」`); // :5349
      } else if (rand_n(2) == 0) { // :5350
        await era.printAndWait(`「来吧、还要攻击几次才会死呢？」`); // :5351
      } else { // :5352
        await era.printAndWait(`「${sc()}是绝对不会输的哦！　证明给你看！」`); // :5353
      } // :5354

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :5356


      await era.printAndWait(`「这种程度、也在预料范围内哦！」`); // :5359

      return 0; // :5361
    } else { // :5362


      if (rand_n(3) == 0) { // :5365
        await era.printAndWait(`「你的胜率连万分之一也没有哦！」`); // :5366
      } else if (rand_n(2) == 0) { // :5367
        await era.printAndWait(`「躲躲看吧！　试着超越${sc()}的计算吧！」`); // :5368
      } else { // :5369
        await era.printAndWait(`「${sc()}是……绝对无法战胜的！」`); // :5370
      } // :5371

    } // :5373
  } else { // :5374

    if (era.get(`talent:${target}:21`) == 1 || era.get(`talent:${target}:22`) == 1) { // :5376


      await era.printAndWait(`「……我要上了」`); // :5379

      return 0; // :5381
    } else if (era.get(`talent:${target}:11`) == 1 || era.get(`talent:${target}:12`) == 1 || era.get(`talent:${target}:15`) == 1 || era.get(`talent:${target}:30`) == 1 || era.get(`talent:${target}:34`) == 1) { // :5382


      if (rand_n(3) == 0) { // :5385
        await era.printAndWait(`「要我来帮你算一算失败的几率吗？」`); // :5386
      } else if (rand_n(2) == 0) { // :5387
        await era.printAndWait(`「好想教育一下无知的你呢、用这个美妙的世界！」`); // :5388
      } else { // :5389
        await era.printAndWait(`「你是绝对胜不了的……让${sc()}来证明这一点吧！」`); // :5390
      } // :5391

    } else if (era.get(`talent:${target}:10`) == 1 || era.get(`talent:${target}:26`) == 1) { // :5393


      await era.printAndWait(`「这种程度、也在预料范围内哦！」`); // :5396

      return 0; // :5398
    } else { // :5399


      if (rand_n(3) == 0) { // :5402
        await era.printAndWait(`「魔族的优秀之处……真想从你那学习一下呢」`); // :5403
      } else if (rand_n(2) == 0) { // :5404
        await era.printAndWait(`「${sc()}的胜率超过9成了呢！」`); // :5405
      } else { // :5406
        await era.printAndWait(`「计算之中的行动……真无聊呢」`); // :5407
      } // :5408

    } // :5410
  } // :5411



  return 0; // :5415





}

// @COLOSSEUM_KOJO_12 // :5421
async function COLOSSEUM_KOJO_12() {



  if (era_flag.selectcom == 55) { // :5425

    if (era.get(`base:${target}:1`) <= 0) { // :5427
      await era.printAndWait(''); // :5428
    } else { // :5429
      await era.printAndWait(''); // :5430
    } // :5431
    return 0; // :5432
  } // :5433



  if (era_flag.selectcom == 56) { // :5437

    if (era.get(`base:${target}:1`) <= 0) { // :5439

      if (era_flag.assi > 0 && era_flag.assiplay) { // :5441
        await era.printAndWait(`「已经不行了……要完了……」`); // :5442
      } else { // :5443
        await era.printAndWait(`「已经不行了……要完了……」`); // :5444
      } // :5445
    } else { // :5446

      if (era_flag.assi > 0 && era_flag.assiplay) { // :5448
        await era.printAndWait(`「${sc()}是绝对……不会认输的！」`); // :5449
      } else { // :5450
        await era.printAndWait(`「${sc()}是绝对……不会认输的！」`); // :5451
      } // :5452
    } // :5453
    return 0; // :5454
  } // :5455




  if (era_flag.selectcom == 31) { // :5460

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5462
      await era.printAndWait(''); // :5463
    } else { // :5464
      await era.printAndWait(''); // :5465
    } // :5466
    return 0; // :5467
  } // :5468



  if (era_flag.selectcom == 5) { // :5472

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5474
      await era.printAndWait(''); // :5475
    } else { // :5476
      await era.printAndWait(''); // :5477
    } // :5478
    return 0; // :5479
  } // :5480



  if (era_flag.selectcom == 21) { // :5484

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5486
      await era.printAndWait(''); // :5487

    } else if (era.get('tflag:400') == 206) { // :5489
      await era.printAndWait(''); // :5490
    } else { // :5491
      await era.printAndWait(''); // :5492
    } // :5493
    return 0; // :5494
  } // :5495




  if (era_flag.selectcom == 27) { // :5500

    if (era_flag.assi > 0 && era_flag.assiplay) { // :5502
      await era.printAndWait(''); // :5503

    } else if (era.get('tflag:400') == 206) { // :5505
      await era.printAndWait(''); // :5506
    } else { // :5507
      await era.printAndWait(''); // :5508
    } // :5509
    return 0; // :5510
  } // :5511




  if (era_flag.selectcom == 51) { // :5516
    await era.printAndWait(''); // :5517
    return 0; // :5518
  } // :5519


  return 0; // :5522



}

// @NTR_KOUJO_K12 // :5526
async function NTR_KOUJO_K12() {



  if (era.get(`cflag:${target}:650`) == 0) { // :5531
    // CFLAG:650  = 1（变量语义：CFLAG 族，650） // :5531
    era.set(`cflag:${target}:650`, 1); // :5531
  } // :5531


  if (P == 1) { // :5534

    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5536
      await era.printAndWait(`「看到了吗？　魔王大人……${sc()}、把处女献出去了哦」`); // :5537
    } else { // :5538
      await era.printAndWait(`「好开心……${sc()}、把处女献出去了」`); // :5539
    } // :5540
    // CFLAG:651  = 1（变量语义：CFLAG 族，651） // :5541
    era.set(`cflag:${target}:651`, 1); // :5541

  } else if (P == 2) { // :5543
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5544
      await era.printAndWait(`「看到了吗？　魔王大人……${sc()}的肛门、因为狂王大人……而觉醒了哦」`); // :5545
    } else { // :5546
      await era.printAndWait(`「好开心……${sc()}的身体是狂王大人的东西了……」`); // :5547
    } // :5548
    // CFLAG:652  = 1（变量语义：CFLAG 族，652） // :5549
    era.set(`cflag:${target}:652`, 1); // :5549

  } else if (P == 3) { // :5551
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5552
      await era.printAndWait(`「魔王大人看到了吗？　${sc()}、变成狂王大人的家畜了哦……」`); // :5553
    } else { // :5554
      await era.printAndWait(`「${sc()}是狂王大人的……家畜哦……」`); // :5555
    } // :5556
    // CFLAG:653  = 1（变量语义：CFLAG 族，653） // :5557
    era.set(`cflag:${target}:653`, 1); // :5557

  } else if (P == 4) { // :5559
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5560
      await era.printAndWait(`「魔王大人快看吧、${sc()}被狂王大人授精的地方……」`); // :5561
    } else { // :5562
      await era.printAndWait(`「${sc()}、要怀上……狂王大人的孩子了！」`); // :5563
    } // :5564
    // CFLAG:654  = 1（变量语义：CFLAG 族，654） // :5565
    era.set(`cflag:${target}:654`, 1); // :5565

  } else if (P == 5) { // :5567
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5568
      await era.printAndWait(`「好厉害啊、狂王大人、好厉害啊！」`); // :5569
    } else { // :5570
      await era.printAndWait(`「好厉害啊、狂王大人、好厉害啊！」`); // :5571
    } // :5572
    // CFLAG:655  = 1（变量语义：CFLAG 族，655） // :5573
    era.set(`cflag:${target}:655`, 1); // :5573

  } else if (P == 6) { // :5575
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5576
      await era.printAndWait(`「好厉害啊、狂王大人、好厉害啊！」`); // :5577
    } else { // :5578
      await era.printAndWait(`「好厉害啊、狂王大人、好厉害啊！」`); // :5579
    } // :5580
    // CFLAG:656  = 1（变量语义：CFLAG 族，656） // :5581
    era.set(`cflag:${target}:656`, 1); // :5581

  } else if (P == 7) { // :5583
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5584
      await era.printAndWait(`「让我全心全意的奉仕您吧……」`); // :5585
    } else { // :5586
      await era.printAndWait(`「让我全心全意的奉仕您吧……」`); // :5587
    } // :5588
    // CFLAG:657  = 1（变量语义：CFLAG 族，657） // :5589
    era.set(`cflag:${target}:657`, 1); // :5589

  } else if (P == 20) { // :5591
    if (era.get(`talent:${target}:76`) || era.get(`talent:${target}:85`)) { // :5592
      await era.printAndWait(`「魔王大人快看吧！　${sc()}生出狂王大人孩子的地方……」`); // :5593
    } else { // :5594
      await era.printAndWait(`「能生下狂王大人的孩子真是荣幸呢……」`); // :5595
    } // :5596

  } // :5598

  return 0; // :5600


}

// @EXUCUTION_KOUJO_K12 // :5603
async function EXUCUTION_KOUJO_K12() {



  if (era.get('tflag:16') == 4) { // :5607
    await era.printAndWait(`「不要啊、${sc()}、要变成白痴了……」`); // :5608

  } else if (era.get('tflag:16') == 5) { // :5610
    await era.printAndWait(`「嗯……知道了」`); // :5611

  } else if (era.get('tflag:16') == 6) { // :5613
    await era.printAndWait(`「这样子、好讨厌！」`); // :5614

  } else if (era.get('tflag:16') == 7) { // :5616
    await era.printAndWait(''); // :5617
  } // :5618


}

// @MUSEUM_KOUJO_K12 // :5621
async function MUSEUM_KOUJO_K12() {



  if (era.get('tflag:500') == 0) { // :5625
    await era.printAndWait(`「不要啊、变成石头什么的……」`); // :5626

  } else if (era.get('tflag:500') == 1) { // :5628
    await era.printAndWait(`「救命！」`); // :5629

  } else if (era.get('tflag:500') == 2) { // :5631
    await era.printAndWait(`「咕、咕欸欸……」`); // :5632

  } else if (era.get('tflag:500') == 3) { // :5634
    await era.printAndWait(`「真是被要求摆出了耻辱的姿势呢…这样就行了吧？」`); // :5635

  } else if (era.get('tflag:500') == 4) { // :5637
    await era.printAndWait(`「身、身体、结构被改变……的感觉、什么的…真是稀奇的…体…验」`); // :5638

  } else if (era.get('tflag:500') == 5) { // :5640
    await era.printAndWait(''); // :5641

  } else if (era.get('tflag:500') == 6) { // :5643
    await era.printAndWait(''); // :5644

  } else if (era.get('tflag:500') == 7) { // :5646
    await era.printAndWait(''); // :5647

  } else if (era.get('tflag:500') == 8) { // :5649
    await era.printAndWait(''); // :5650

  } else if (era.get('tflag:500') == 9) { // :5652
    await era.printAndWait(''); // :5653
  } // :5654


}

// @BANISHMENT_KOUJO_K12 // :5657
async function BANISHMENT_KOUJO_K12() {



  if (era.get('tflag:510') == 0) { // :5661
    await era.printAndWait(`「${sc()}的研究、就到此为止了吗……」`); // :5662

  } else if (era.get('tflag:510') == 1) { // :5664
    await era.printAndWait(''); // :5665

  } else if (era.get('tflag:510') == 2) { // :5667
    await era.printAndWait(''); // :5668

  } else if (era.get('tflag:510') == 3) { // :5670
    await era.printAndWait(''); // :5671

  } else if (era.get('tflag:510') == 4) { // :5673
    await era.printAndWait(''); // :5674
  } // :5675


}

// @PUBLIC_EXUCUTION_KOUJO_K12 // :5678
async function PUBLIC_EXUCUTION_KOUJO_K12() {



  if (era.get('tflag:520') == 0) { // :5682
    await era.printAndWait(`「咿呀啊～！　救命啊～！」`); // :5683

  } else if (era.get('tflag:520') == 1) { // :5685
    await era.printAndWait(`「我不想死……谁来救救我……」`); // :5686

  } else if (era.get('tflag:520') == 2) { // :5688
    await era.printAndWait(''); // :5689
  } // :5690


}

// @GROTESQUE_KOUJO_K12 // :5693
async function GROTESQUE_KOUJO_K12() {



  if (era.get('tflag:530') == 0) { // :5697
    await era.printAndWait(''); // :5698

  } else if (era.get('tflag:530') == 1) { // :5700
    await era.printAndWait(''); // :5701

  } else if (era.get('tflag:530') == 2) { // :5703
    await era.printAndWait(''); // :5704

  } else if (era.get('tflag:530') == 3) { // :5706
    await era.printAndWait(''); // :5707

  } else if (era.get('tflag:530') == 4) { // :5709
    await era.printAndWait(''); // :5710

  } else if (era.get('tflag:530') == 5) { // :5712
    await era.printAndWait(''); // :5713

  } else if (era.get('tflag:530') == 6) { // :5715
    await era.printAndWait(''); // :5716
  } // :5717


}

// @ENTERENEMY_KOUJO_K12 // :5720
async function ENTERENEMY_KOUJO_K12() {


  if (era.get(`talent:${a}:21`) == 1 || era.get(`talent:${a}:22`) == 1) { // :5723

    await era.printAndWait(`「%SELF_CALL(A)%的计算是不会错的」`); // :5725
  } else if (era.get(`talent:${a}:11`) == 1 || era.get(`talent:${a}:12`) == 1 || era.get(`talent:${a}:15`) == 1 || era.get(`talent:${a}:30`) == 1 || era.get(`talent:${a}:34`) == 1) { // :5726

    await era.printAndWait(`「%SELF_CALL(A)%的计算如果无误的话、魔王的失败已经确定下来了！」`); // :5728
  } else if (era.get(`talent:${a}:10`) == 1 || era.get(`talent:${a}:26`) == 1) { // :5729

    await era.printAndWait(`「如果能再稍微多做点研究就好了……」`); // :5731
  } else { // :5732

    await era.printAndWait(`「%SELF_CALL(A)%的胜率有98％哦」`); // :5734
  } // :5735


}

// @GOHOUBI_REQUEST_KOUJO_K12 // :5738
async function GOHOUBI_REQUEST_KOUJO_K12() {


  if (era.get(`cflag:${a}:504`) == 0) { // :5741

    await era.printAndWait(`%SAVESTR:A%想要钱的样子`); // :5743
    await era.printAndWait(`「请给我研究资金！」`); // :5744
  } else if (era.get(`cflag:${a}:504`) == 1 || era.get(`cflag:${a}:504`) == 2 || era.get(`cflag:${a}:504`) == 3) { // :5745

    await era.print(`%SAVESTR:A%提出了与`); // :5747
    if (era.get(`cflag:${a}:504`) == 1) { // :5748
      await era.print(`狗`); // :5749
    } else if (era.get(`cflag:${a}:504`) == 2) { // :5750
      await era.print(`猪`); // :5751
    } else if (era.get(`cflag:${a}:504`) == 3) { // :5752
      await era.print(`马`); // :5753
    } // :5754
    await era.printAndWait(`交尾的要求`); // :5755
    await era.printAndWait(`「想要继续进行异种交配实验」`); // :5756
  } else if (era.get(`cflag:${a}:504`) == 4) { // :5757

    await era.printAndWait(`%SAVESTR:A%想要归来的吻`); // :5759
    await era.printAndWait(`「你的吻对研究很有帮助」`); // :5760
  } else if (era.get(`cflag:${a}:504`) == 5) { // :5761

    await era.printAndWait(`%SAVESTR:A%想要和你做爱`); // :5763
    await era.printAndWait(`「想要你的精液呢」`); // :5764
  } else if (era.get(`cflag:${a}:504`) == 6) { // :5765

    await era.printAndWait(`%SAVESTR:A%想要精液`); // :5767
    await era.printAndWait(`「一喝下你的精液、头脑就格外清晰呢」`); // :5768
  } else if (era.get(`cflag:${a}:504`) == 7) { // :5769

    await era.printAndWait(`%SAVESTR:A%想要开乱交派对`); // :5771
    await era.printAndWait(`「偶尔也想开开派对试试呢」`); // :5772
  } else if (era.get(`cflag:${a}:504`) == 8) { // :5773

    await era.printAndWait(`%SAVESTR:A%想要你的尿液作为报酬`); // :5775
    await era.printAndWait(`「只要是你的东西不管是什么我都会很开心呢」`); // :5776
  } else if (era.get(`cflag:${a}:504`) == 9) { // :5777

    await era.printAndWait(`%SAVESTR:A%想要处男作为报酬`); // :5779
    await era.printAndWait(`「对不懂性知识的家伙进行教育是%SELF_CALL(A)%的义务呢」`); // :5780
  } // :5781


}

// @GOHOUBI_AFTER_KOUJO_K12 // :5784
async function GOHOUBI_AFTER_KOUJO_K12() {





  if (era.get('tflag:18') == 0) { // :5790
    await era.printAndWait(`「诶诶、怎么能这样」`); // :5791
    return 0; // :5792

  } else if (era.get('tflag:18') == 1) { // :5794
    await era.printAndWait(`「太棒了～！　下次%SELF_CALL(A)%也得拿出什么研究成果呢！」`); // :5795
    return 0; // :5796
  } else if (era.get('tflag:18') == 2) { // :5797

    if (era.get(`cflag:${a}:504`) == 0) { // :5799
      await era.printAndWait(`「太棒了～！　这样一来研究就能更进一步了」`); // :5800

    } else if (era.get(`cflag:${a}:504`) == 1) { // :5802

      if (era.get(`talent:${a}:0`) == 1) { // :5804
        await era.printAndWait(`「把处女献给狗什么的、不错的纪念日呢」`); // :5805
      } else { // :5806
        await era.printAndWait(`「太棒了～！　研究研究♪」`); // :5807
      } // :5808

    } else if (era.get(`cflag:${a}:504`) == 2) { // :5810

      if (era.get(`talent:${a}:0`) == 1) { // :5812
        await era.printAndWait(`「把处女献给猪什么的、不错的纪念日呢」`); // :5813
      } else { // :5814
        await era.printAndWait(`「太棒了～！　早就想研究猪的生殖行为试试了♪」`); // :5815
      } // :5816

    } else if (era.get(`cflag:${a}:504`) == 3) { // :5818

      if (era.get(`talent:${a}:0`) == 1) { // :5820
        await era.printAndWait(`「把处女献给马什么的、不错的纪念日呢」`); // :5821
      } else { // :5822
        await era.printAndWait(`「太棒了～！　与马做爱阴道能不能承受住、就让我自己试试吧」`); // :5823
      } // :5824

    } else if (era.get(`cflag:${a}:504`) == 4) { // :5826
      await era.printAndWait(`「亲脸蛋也行哦、啾的一下」`); // :5827

    } else if (era.get(`cflag:${a}:504`) == 5) { // :5829

      if (era.get(`abl:${a}:2`) > era.get(`abl:${a}:3`)) { // :5831
        await era.printAndWait(`「绝对要受精哦！　好期待呢！」`); // :5832

      } else { // :5834
        await era.printAndWait(`「无论何时都想受精呢……这次也拜托了」`); // :5835
      } // :5836

    } else if (era.get(`cflag:${a}:504`) == 6) { // :5838
      await era.printAndWait(`「这可以用来美容呢、在研究中。谢谢」`); // :5839

    } else if (era.get(`cflag:${a}:504`) == 7) { // :5841

      if (era.get(`talent:${a}:0`) == 1) { // :5843
        await era.printAndWait(`「偶尔把研究忘掉、来做爱吧～！」`); // :5844
      } else { // :5845
        await era.printAndWait(`「偶尔把研究忘掉、来做爱吧～！」`); // :5846
      } // :5847

    } else if (era.get(`cflag:${a}:504`) == 8) { // :5849
      await era.printAndWait(`「饮尿疗法究竟效果如何、下次就研究看看吧」`); // :5850

    } else if (era.get(`cflag:${a}:504`) == 9) { // :5852

      if (era.get(`abl:${a}:2`) > era.get(`abl:${a}:3`)) { // :5854
        await era.printAndWait(`「你、觉得怎样？　这可是女人的阴部哦」`); // :5855

      } else { // :5857
        await era.printAndWait(`「你、觉得怎样？　这可是女人的肛门哦」`); // :5858
      } // :5859
    } else { // :5860
    } // :5861
  } // :5862

}

// @OSIOKI_KOUJO_K12 // :5864
async function OSIOKI_KOUJO_K12() {





  if (era.get('tflag:18') == 0) { // :5870
    await era.printAndWait(`「请你、不要吓我……」`); // :5871

  } else if (era.get('tflag:18') == 1) { // :5873

    if (era.get(`abl:${a}:21`) >= 3) { // :5875
      await era.printAndWait(`「痛觉转换成了快感……好有效啊啊啊啊♪」`); // :5876
    } else { // :5877
      await era.printAndWait(`「呀啊啊啊啊啊！」`); // :5878
    } // :5879

  } else if (era.get('tflag:18') == 2) { // :5881

    if (era.get(`abl:${a}:17`) >= 4) { // :5883
      await era.printAndWait(`「没有一点知性的母猪般的自慰……好好看着吧♪」`); // :5884
    } else { // :5885
      await era.printAndWait(`「呜呜～、这样没有一点知性的行为……」`); // :5886
    } // :5887

  } else if (era.get('tflag:18') == 3) { // :5889

    if (era.get(`abl:${a}:17`) >= 6) { // :5891
      await era.printAndWait(`「在路上排泄、%SELF_CALL(A)%、可真是个笨蛋啊～♪」`); // :5892
    } else { // :5893
      await era.printAndWait(`「呜呜～、这样没有一点知性的行为……」`); // :5894
    } // :5895

  } else if (era.get('tflag:18') == 4) { // :5897

    if (era.get(`abl:${a}:21`) >= 3) { // :5899
      await era.printAndWait(`「再更多的、更多的用力鞭笞我吧♪　啊呜～♪」`); // :5900
    } else { // :5901
      await era.printAndWait(`「唔……天才、必须在痛苦中学习～」`); // :5902
    } // :5903

  } else if (era.get('tflag:18') == 5) { // :5905

    if (era.get(`talent:${a}:88`) == 1 || era.get(`talent:${a}:76`) == 1) { // :5907
      await era.printAndWait(`「啊啊啊、这样尿出来、要变成白痴了♪　%SELF_CALL(A)%、要变成白痴了啊♪」`); // :5908
    } else { // :5909
      await era.printAndWait(`「太不卫生了……%SELF_CALL(A)%好失败呢」`); // :5910
    } // :5911

  } else if (era.get('tflag:18') == 6) { // :5913
    await era.print(`「唉、掃厕所吗……」`); // :5914

  } else if (era.get('tflag:18') == 7) { // :5916
    await era.print(`「明明对研究来说体力是必要的……」`); // :5917

  } else if (era.get('tflag:18') == 8) { // :5919
    await era.printAndWait(`「薬物的影响……也当成一种研究的话……」`); // :5920

  } else if (era.get('tflag:18') == 9) { // :5922
    await era.printAndWait(''); // :5923
  } // :5924


}

// @GOBI_KOUJO_K12, ARG:0 // :5927
async function GOBI_KOUJO_K12() {


  if (arg_0 == 1) { // :5930

    await era.print(`的哟♪`); // :5932
  } else if (arg_0 == 2) { // :5933

    await era.print(`的呢！`); // :5935
  } else if (arg_0 == 3) { // :5936

    await era.print(`的哟……。`); // :5938
  } else if (arg_0 == 4) { // :5939

    await era.print(`的样子……呢。`); // :5941
  } else if (arg_0 == 5) { // :5942

    await era.print(`的哟……。`); // :5944
  } else { // :5945


    if (rand_n(3) == 0) { // :5948
      await era.print(`的哟。`); // :5949
    } else if (rand_n(2) == 0) { // :5950
      await era.print(`的样子哦。`); // :5951
    } else { // :5952
      await era.print(`什么的。`); // :5953
    } // :5954
  } // :5955




}

// ===== 复核清单（转译器生成，agent 逐条处理后删除） =====
// 1. :81 同名函数 @EVENTTRAIN 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTTRAIN', …) 注册，参照 ere/kojo/kojo-k5.js
// 2. :170 CALL K12_KOJO2 —— 口上文件里多为存根调用，人工定存根名
// 3. :230 CALL K12_KOJO2 —— 口上文件里多为存根调用，人工定存根名
// 4. :272 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 5. :274 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 6. :291 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 7. :293 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 8. :312 同名函数 @EVENTEND 重复定义（ERB 事件函数可多重定义）——JS 侧需改 on('EVENTEND', …) 注册，参照 ere/kojo/kojo-k5.js
// 9. :413 CALL DOG_KOJO_12 —— 口上文件里多为存根调用，人工定存根名
// 10. :418 CALL COLOSSEUM_KOJO_12 —— 口上文件里多为存根调用，人工定存根名
// 11. :560 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
// 12. :562 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 13. :566 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 14. :570 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 15. :574 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 16. :578 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 17. :608 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 18. :610 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 19. :612 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 20. :613 插值 未知插值 %阴核(TARGET)% —— 保真锁会红，须人工定归一
// 21. :621 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 22. :634 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 23. :636 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 24. :637 插值 未知插值 %阴核(TARGET)% —— 保真锁会红，须人工定归一
// 25. :639 插值 未知插值 %阴核(TARGET)% —— 保真锁会红，须人工定归一
// 26. :645 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 27. :654 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 28. :657 插值 未知插值 %阴核(TARGET)% —— 保真锁会红，须人工定归一
// 29. :663 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 30. :693 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 31. :701 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 32. :1544 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 33. :1546 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 34. :1558 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 35. :1560 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 36. :1589 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 37. :1591 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 38. :1603 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 39. :1605 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 40. :1669 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 41. :1671 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 42. :1679 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 43. :1681 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 44. :1740 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 45. :1742 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 46. :1750 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 47. :1752 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 48. :1797 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 49. :1799 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 50. :1811 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 51. :1856 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 52. :1858 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 53. :1870 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 54. :1915 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 55. :1917 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 56. :1929 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 57. :1974 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 58. :1986 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 59. :2034 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 60. :2054 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 61. :2155 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 62. :2167 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 63. :2268 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 64. :2270 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 65. :2272 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 66. :2280 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 67. :2282 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 68. :2284 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 69. :2292 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 70. :2294 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 71. :2296 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 72. :2375 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 73. :2387 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 74. :2889 表达式寻址 PALAMLV:3 → era.get('palamlv:3')——族名直译，语义与归属人工定
// 75. :3826 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 76. :3828 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 77. :3836 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 78. :3838 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 79. :3846 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 80. :3848 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 81. :3897 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 82. :3905 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 83. :3913 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 84. :3961 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 85. :3969 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 86. :3977 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 87. :4097 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 88. :4099 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 89. :4107 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 90. :4109 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 91. :4111 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 92. :4119 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 93. :4121 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 94. :4123 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 95. :4131 RAND RAND:4 → rand_n(4)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 96. :4133 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 97. :4135 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 98. :4366 局部变量 P = PALAM:3 + UP:3 —— 单字母局部变量，JS 侧声明与赋值人工定
// 99. :4367 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 100. :4397 局部变量 P = PALAM:5 + UP:5 —— 单字母局部变量，JS 侧声明与赋值人工定
// 101. :4398 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 102. :4424 局部变量 P = PALAM:8 + UP:8 —— 单字母局部变量，JS 侧声明与赋值人工定
// 103. :4425 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 104. :4439 局部变量 P = PALAM:10 + UP:10 —— 单字母局部变量，JS 侧声明与赋值人工定
// 105. :4440 表达式寻址 PALAMLV:2 → era.get('palamlv:2')——族名直译，语义与归属人工定
// 106. :4454 表达式寻址 NOWEX:0 → era.get('nowex:0')——族名直译，语义与归属人工定
// 107. :4468 表达式寻址 NOWEX:1 → era.get('nowex:1')——族名直译，语义与归属人工定
// 108. :4485 表达式寻址 NOWEX:2 → era.get('nowex:2')——族名直译，语义与归属人工定
// 109. :4502 表达式寻址 NOWEX:3 → era.get('nowex:3')——族名直译，语义与归属人工定
// 110. :4516 局部变量 A = UP:11 + UP:12 —— 单字母局部变量，JS 侧声明与赋值人工定
// 111. :4743 CALL SELL_MATURO_K0 —— 口上文件里多为存根调用，人工定存根名
// 112. :5047 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 113. :5047 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 114. :5047 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 115. :5065 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 116. :5086 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 117. :5087 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 118. :5106 CALL BENKI_PLAYER_NAME —— 口上文件里多为存根调用，人工定存根名
// 119. :5108 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 120. :5128 CALL BENKI_PLAYER_NAME —— 口上文件里多为存根调用，人工定存根名
// 121. :5130 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 122. :5150 CALL BENKI_PLAYER_NAME —— 口上文件里多为存根调用，人工定存根名
// 123. :5152 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 124. :5172 CALL BENKI_PLAYER_NAME —— 口上文件里多为存根调用，人工定存根名
// 125. :5174 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 126. :5200 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 127. :5202 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 128. :5230 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 129. :5257 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 130. :5259 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 131. :5294 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 132. :5296 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 133. :5311 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 134. :5313 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 135. :5348 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 136. :5350 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 137. :5365 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 138. :5367 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 139. :5385 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 140. :5387 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 141. :5402 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 142. :5404 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 143. :5725 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 144. :5728 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 145. :5734 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 146. :5743 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 147. :5747 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 148. :5759 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 149. :5763 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 150. :5767 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 151. :5771 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 152. :5775 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 153. :5779 插值 未知插值 %SAVESTR:A% —— 保真锁会红，须人工定归一
// 154. :5780 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 155. :5795 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 156. :5892 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 157. :5908 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 158. :5910 插值 未知插值 %SELF_CALL(A)% —— 保真锁会红，须人工定归一
// 159. :5927 函数参数 @GOBI_KOUJO_K12, ARG:0 —— 参数声明已剥（JS 函数签名人工定）
// 160. :5930 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 161. :5933 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 162. :5936 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 163. :5939 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 164. :5942 局部参数 ARG:0 → arg_0（JS 局部变量，形参名人工定）
// 165. :5948 RAND RAND:3 → rand_n(3)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
// 166. :5950 RAND RAND:2 → rand_n(2)——随机源需注入（参照 kojo-k5.js 的 rand 参数）
