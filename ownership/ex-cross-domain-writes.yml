# 跨域写入清单（EX）——由 tools/ownership-scan.js 从 target/ 实测生成（issue #70 实测，#133 文件级粒度重跑）
# 本文件归人工维护：扫描器重跑默认不覆盖，需要重新生成请加 --force --table ex
#
# 定义：写入者域 ≠ 该下标属主域 的每一次写入（含 VARSET 展开的逐下标写入），
#       按「下标、文件、行号」排序。动态/表级写入无属主可比，不入本清单。
# 共 2 条。
- index: 5
  writer: train
  owner: system
  file: "target/ERB/調教相關/COMF_VAGINASEX.ERB"
  line: 849
- index: 5
  writer: train
  owner: system
  file: "target/ERB/調教相關/COMF_VAGINASEX.ERB"
  line: 866
