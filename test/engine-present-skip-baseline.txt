# 有引擎环境（CI 的 engine / mutation job）下 npm test 的预期跳过数：0。
#
# 与 engine-skip-baseline.txt（无引擎，现为 72）是同一道守护的两侧：
# 那边守「引擎缺席的代价必须是看得见的数字」，这边守「引擎在场时不许
# 还有东西被跳过」。引擎装上了却仍有用例跳过，说明门控条件写错了或
# asar 没被 locate_asar 认出来——两种都该红，而不是静默少跑一批。
#
# 这个数字没有「有意识地改」的空间：它只能是 0。
0
